# Состояние проекта АРХИМЕД и очередь работ

Файл для передачи в новую сессию. Читать первым: здесь всё, что нужно, чтобы продолжить без потери контекста.

## Где что лежит

| Что | Где |
|---|---|
| Приложение (прод) | `https://123.teramont.pro/MVP/` — **версия v561** |
| Локальный QA | `(cd MVP && python3 -m http.server 8123 &)` → `http://127.0.0.1:8123/` |
| Шлюз (локальный) | `ARH_DATA=/tmp/<dir> PYTHONPATH=.py-libs python3 шлюз_сервер.py 8135` → `http://127.0.0.1:8135/MVP/` |
| Выкладка | `PYTHONPATH=.py-libs python3 deploy/deploy.py` (5–7 с, проверяет версию на сервере) |
| Ритуал версии | `sed -i '' 's/v=N/v=N+1/g' MVP/index.html MVP/parent/index.html` + `MVP/sw.js` `CACHE='arhimed-mvp-vN'` |
| Русский язык (данные и рисовалки) | `MVP/data/vis_ru.js`, `MVP/data/tasks_ru.js`, `MVP/data/tasks_mishutka.js`, `MVP/data/tasks_fg6.js`, `MVP/data/tasks_dict.js` |
| Стандарт интерфейса | `.agents/skills/ui-typography-usability/SKILL.md` (разделы 1–9, включая запрет шаблонов) |
| План переделки 615 | `deploy/ПЕРЕДЕЛКА_615.md` (порядок 1 → 10, выкладка только целиком) |
| План русского направления | `deploy/ПЛАН_РУССКИЙ_ЯЗЫК.md` |

## Проверки (запускать из корня репозитория)

| Инструмент | Что делает |
|---|---|
| `PYTHONPATH=.py-libs python3 qa_layout.py 601 … 615` | гейт раскладки: наложения, обрезка, пустые кадры, сдвиг центра; **мерит на 390 и 320 px**; декоративные слои не считает |
| `PYTHONPATH=.py-libs python3 qa_ui.py` | аудит: контраст (WCAG AA), кегли по шкале, цели касания, масштаб текста, reduced-motion, переполнение; делит находки на русское направление и оболочку приложения |
| `PYTHONPATH=.py-libs python3 qa_feedback.py` | падает, если отклик на ответ остался голым текстом без анимации |
| `node qa_ru_items.js` | инварианты заданий работ (подстановка буквы = слово и т. п.) |
| `PYTHONPATH=.py-libs python3 qa_ru_trainers.py` | прогон тренажёров 601–610 через реальный интерфейс |
| `PYTHONPATH=.py-libs python3 qa_ru.py` | общий набор русского направления (включая работы и МЦКО) |

Правило: **выкладывать только при зелёном гейте**. Выкладку запускать командой с условием (если гейт красный — не выкладывать). Локальные правки без зелёной проверки не коммитить и не выкладывать.

## Что сделано (русское направление)

- 10 уроков 601–610, 3 проверочные работы 611–613, работа МЦКО 614, урок «Путь Мишутки» 615.
- 163 словарных слова, привязаны к урокам **нашей** нумерации: 134 → урок 603, 29 → урок 604 (решение владельца зафиксировано в плане).
- Интерактивы: 601 — сортировка по ящикам; 603–604 — вставка буквы с живой анимацией; 611–614 — работы со столбчатыми диаграммами, картой, таблицей.
- Отклик на ответ — движок `RUFEED`: печать с волной при верном ответе, чернильный оттиск и объяснение при ошибке.
- Мишутка (картинка `MVP/img/mishutka.png`): остров на «Пути», карточка в «Туре», строка урока «Путь Мишутки» в «Учебниках», шапка листа задания. У предмета «Русский язык» — **книга** (по решению владельца).
- Урок 615 — лист задания на кремовой тетрадной бумаге в клетку с розовым полем; десять иллюстраций в векторе.

## Очередь: что делать дальше, по порядку

1. **615, задание 3 — ось температуры.** В PDF на оси есть подпись **18** сверху, у нас ось кончается на 16. Попытка поднять подпись провалилась: при шаге 2 единицы на 6,8 px подписи 16 и 18 налезают, а после растяжки −16 выходит за кадр. **Лечить одной правкой: увеличить `viewBox` и `height` SVG графика вместе с масштабом оси**, затем прогнать гейт. Правки в функции `tempChart` (ищите `const v=[-8,-10,0,10,12,16,17,16,12,4,-4,-8]`).
2. **615, задание 4 — прямая.** В PDF делений больше (около девяти), у нас шесть; подписи только у 0 и 1. Функция `coordLine`, цикл `for(let u=0;u<=5;u++)`.
3. **615, задание 9 — таблица.** В PDF под таблицей есть сноска про условия тарифа «90 минут»; добавить подпись под таблицей (аккуратно, без наложений — гейт ловит).
4. **615, задания 2, 8, 10** — сверить ширину столбцов, контуры карты, вылет линий за треугольник.
5. **Карты.** Найдена настоящая контурная карта Москвы: [Moscow all districts.svg](https://commons.wikimedia.org/wiki/File:Moscow_all_districts.svg) (Wikimedia Commons, 137 контурных путей, но это районы, а не 12 округов; подписей округов нет). Карта Дальнего Востока не найдена — три имени на Commons дали 404. Пути: либо сгруппировать районы в 12 округов и раскрасить группы, либо дорисовать контуры самим по опорным точкам, либо искать прицельно по категориям Commons (часовые пояса России). При использовании Commons — указать источник.
6. **615 — общий прогон и выкладка целиком** (сетка из десяти фигур зрением, гейт на 390 и 320 px, `qa_ui`, консоль), потом одна выкладка.
7. **Уроки 602, 605–610** — свои интерактивы и анимированный отклик (сейчас `qa_feedback` показывает 6/9).
8. **Рукописный вариант 611** — на паузе: на 390 px чист, на 320 px 12 отметок, причину не изолировали. Диагностика начинается с замера координат декоративных слоёв (`.grain`, `.vine`, `.haloGlow`) на 320 px.
9. **Шаг 2 редизайна** (перекладка сцен 601–610 под крупный шрифт) — не начат: доказано, что общий множитель шрифта ломает кадры, нужна перекладка по одному.

## Правила, которые нельзя нарушать

- Правки касаются **только русского языка**; оболочка приложения — как есть.
- **Никаких шаблонов**: запрещены три одинаковые карточки в ряд, общий каркас кадров у разных тем, центрированный герой по умолчанию, одна форма для разного содержания, эффекты ради эффекта (раздел 9 стандарта).
- Не выкладывать частями, если владелец просит собрать целиком: сначала всё, потом одна выкладка с приложенным выводом проверок.
- Проверять **зрением**: снимать фигуру и смотреть снимок, а не только полагаться на гейт.
- Кегли по шкале 12/14/16/20/24/32/48, трекинг −0.02em от 24 px, контраст ≥ 4,5:1, цели касания ≥ 44 px, `prefers-reduced-motion` обязателен.
- Не выдавать упрощённый силуэт за точный контур; выдуманные этимологии и «на глаз» номера уроков — недопустимы.
