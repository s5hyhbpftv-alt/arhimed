/* ============ ТУР 704 · МАТЕМАТИКА · 6 КЛАСС · «ПЯТЬ ЭТАПОВ БОЛЬШОЙ ИГРЫ» ============

   ТУР, а не урок: свой сюжет, свои рисунки на каждом шаге, свои действия
   руками и разбор каждого задания.

   СЮЖЕТ. Летний лагерь играет в большую игру из пяти этапов: кодовый замок
   на калитке, три сундука с находками, счёт по кругу у костра, грибная
   арифметика и сказка на ночь про стаю драконов.

   УСТРОЙСТВО ТУРА (12 шагов):
     1. большая игра      — знакомство с сюжетом
     2, 3. кодовый замок   — теория и задача
     4, 5. три сундука     — теория и задача
     6, 7. счёт у костра   — теория и задача
     8, 9. грибы           — теория и задача
    10, 11. драконы        — теория и задача
    12. итог              — балл и что повторить

   Уроки туров 701–703: подписи не налезают, кадр не обрезан, текст живой,
   у каждого шага не меньше двух анимаций, prefers-reduced-motion уважается.
   ============================================================================ */
(function(){
  'use strict';

  const ID = 704;
  const ДОМ = 't704';

  const ЗОЛОТО = '#ffd76a', ЛАМПА = '#ffe9a8', ПЕРО = '#f4e9c8', ТИХО = '#8fa08f',
        ЗЕЛЁН = '#8fd1a8', КРАСН = '#e86a5a', СИНЬ = '#7fd1ff', ЛИНИЯ = '#3f7a5f';

  /* ── данные тура: пять этапов официального комплекта ── */
  const ЗАДАЧИ = [
    { id:'tour-math6-1', балл:2, тема:'Десятичные дроби · подстановка цифр',
      название:'Кодовый замок',
      условие:'Код на калитке записан буквами: О,Л + И,М + П,И + А,ДА. Одинаковые буквы — одинаковые цифры, разные буквы — разные цифры.',
      вопрос:'Какую самую большую сумму можно получить такой подстановкой?',
      ответ:32.18,
      буквы:['О','Л','И','М','П','А','Д'],
      разбор:'Разложим по разрядам: целые — О + И + П + А, десятые — (Л + М + И + Д)/10, сотые — А/100. Целых не больше 9 + 8 + 7 + 6 = 30. Проверим, можно ли взять 30: тогда десятые надо набрать как можно больше из оставшихся цифр, но И уже занята в целых, а А тоже. Наибольшая достижимая сумма — 32,18: целые 30, десятые 0,1·1 + 0,1·0 + 0,1·0 и сотая 0,08. Итог: 30 + 0,18 = 32,18.',
      ошибка:'Забыть, что буква И стоит и в целых, и в десятых, а А — и в целых, и в сотых. Из-за этого цифры нельзя расставлять свободно.' },

    { id:'tour-math6-2', балл:2, тема:'Логика · разбор ложных надписей',
      название:'Три сундука',
      условие:'Три сундука, пять находок: ключик, перстень, алмаз, жемчужина и изумруд. На сундуке 1 — «Здесь нет алмаза», на сундуке 2 — «Этот сундук пуст», на сундуке 3 — «Здесь меньше трёх предметов» и «Здесь лежит ключик». Все надписи врут.',
      вопрос:'Разложи все пять находок по сундукам так, чтобы ни одна надпись не оказалась верной.',
      ответ:{ один:['алмаз'], два:['ключик'], три:['перстень','жемчужина','изумруд'] },
      предметы:['ключик','перстень','алмаз','жемчужина','изумруд'],
      разбор:'«Здесь нет алмаза» ложно — алмаз в первом сундуке. «Этот сундук пуст» ложно — во втором что-то есть. «Здесь меньше трёх предметов» ложно — в третьем не меньше трёх. «Здесь лежит ключик» ложно — ключика в третьем нет, значит он во втором. Остались перстень, жемчужина и изумруд — все три в третьем, и там как раз три предмета.',
      ошибка:'Решить, что из ложной надписи «меньше трёх» следует «ровно три». Предметов могло быть и больше; сужает дело то, что всего находок пять.' },

    { id:'tour-math6-3', балл:2, тема:'Делимость · счёт по кругу и остатки',
      название:'Счёт у костра',
      условие:'Ребята сели вокруг костра и называют номера по кругу: 1, 2, 3, … — второй круг, третий и так дальше. Тот, кто на первом круге назвал 5, на третьем назвал 29.',
      вопрос:'Сколько всего ребят у костра и на каком круге прозвучит номер 100?',
      ответ:{ ребят:12, круг:9 },
      разбор:'Один и тот же человек в каждом новом круге получает номер больше ровно на число ребят. От первого круга до третьего прошло два круга: 5 + 2N = 29, значит 2N = 24 и N = 12. Восьмой круг заканчивается номером 96 (12 · 8), поэтому номер 100 — уже в девятом круге. Проверка: 100 = 12 · 8 + 4, то есть четвёртый человек девятого круга.',
      ошибка:'Взять целую часть от 100 : 12 = 8 и назвать восьмой круг. Восьмой круг кончается на 96, поэтому 100 — это девятый.' },

    { id:'tour-math6-4', балл:2, тема:'Делимость · общая сумма и равный дележ',
      название:'Грибная арифметика',
      условие:'Отряд ходил за грибами: Таня нашла 5 грибов, Илья — 50, а все остальные ребята — ровно по 13 грибов. Дети могут обменяться грибами так, что у всех станет поровну.',
      вопрос:'Сколько детей ходило в лес за грибами?',
      ответ:29,
      разбор:'Пусть детей n. Тогда всего грибов 5 + 50 + 13 · (n − 2) = 13n + 29. Поделить поровну можно тогда и только тогда, когда n делит это число. Так как n делит 13n, требуется, чтобы n делило 29. Детей больше двух, а 29 — простое число, значит n = 29. Проверка: 13 · 29 + 29 = 406 = 29 · 14, каждому по 14 грибов.',
      ошибка:'Делить поровну только «лишние» грибы. Делится всё собранное число грибов — и задача сводится к делимости на 29.' },

    { id:'tour-math6-5', балл:2, тема:'Уравнения · одно неизвестное вместо трёх',
      название:'Стая драконов',
      условие:'Летела стая из 62 драконов. Семиголовых было вдвое меньше, чем пятиголовых, а остальные драконы были шестиголовыми. Всего у драконов 355 голов.',
      вопрос:'Сколько было семиголовых, пятиголовых и шестиголовых драконов?',
      ответ:{ семь:17, пять:34, шесть:11 },
      разбор:'Пусть семиголовых x. Тогда пятиголовых 2x, а шестиголовых 62 − 3x. Голов всего: 7x + 5 · 2x + 6 · (62 − 3x) = 7x + 10x + 372 − 18x = 372 − x. По условию это 355, значит x = 17. Пятиголовых 2 · 17 = 34, шестиголовых 62 − 17 − 34 = 11. Проверка: 17 · 7 + 34 · 5 + 11 · 6 = 119 + 170 + 66 = 355 — сходится.',
      ошибка:'Перепутать, кого вдвое больше: вдвое МЕНЬШЕ семиголовых, значит пятиголовых 2x. И не забывать, что шестиголовых 62 − 3x, а не 62 − x.' }
  ];

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="${ДОМ}-небо" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#0a1622"/><stop offset="1" stop-color="#1d3a44"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f0e2c4" stop-opacity="0.30"/>
        <stop offset="0.6" stop-color="#c9b591" stop-opacity="0.20"/>
        <stop offset="1" stop-color="#8a7a5c" stop-opacity="0.16"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стойка" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c98a45"/><stop offset="1" stop-color="#6f4420"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стекло" x1="0" y1="0" x2="0.4" y2="1">
        <stop offset="0" stop-color="#fff6d0" stop-opacity="0.9"/>
        <stop offset="1" stop-color="#a9762a" stop-opacity="0.8"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-банан" x1="0.2" y1="0" x2="0.8" y2="1">
        <stop offset="0" stop-color="#ffe08a"/><stop offset="0.5" stop-color="#f0c04a"/>
        <stop offset="1" stop-color="#b8862a"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-пол" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#243a30"/><stop offset="1" stop-color="#0d1a15"/>
      </linearGradient>
      <radialGradient id="${ДОМ}-кожа" cx="0.38" cy="0.32" r="0.75">
        <stop offset="0" stop-color="#f7d9b8"/><stop offset="0.6" stop-color="#eec5a0"/>
        <stop offset="1" stop-color="#d6a67c"/>
      </radialGradient>
      <radialGradient id="${ДОМ}-свет" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#ffdf95" stop-opacity="0.55"/>
        <stop offset="1" stop-color="#ffdf95" stop-opacity="0"/>
      </radialGradient>
      <linearGradient id="${ДОМ}-трава" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#3f6b45" stop-opacity="0.55"/>
        <stop offset="1" stop-color="#1d3a26" stop-opacity="0.35"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-ткань" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#6f8a4a"/><stop offset="1" stop-color="#415a2a"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-огонь" x1="0" y1="1" x2="0" y2="0">
        <stop offset="0" stop-color="#e86a2a"/><stop offset="0.6" stop-color="#ffb347"/>
        <stop offset="1" stop-color="#ffe9a8"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-гриб" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#d9705f"/><stop offset="1" stop-color="#a33a2a"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-хвоя" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2f6b4a"/><stop offset="1" stop-color="#173e2a"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-лёд" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0" stop-color="#dff1fb" stop-opacity="0.55"/>
        <stop offset="1" stop-color="#7fb8d8" stop-opacity="0.35"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-дерево" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#b07a42"/><stop offset="1" stop-color="#6f4420"/>
      </linearGradient>
      <filter id="${ДОМ}-мягко" x="-40%" y="-40%" width="180%" height="180%">
        <feGaussianBlur stdDeviation="4"/>
      </filter>
      <filter id="${ДОМ}-мягко2" x="-40%" y="-40%" width="180%" height="180%">
        <feGaussianBlur stdDeviation="1.8"/>
      </filter>
      <filter id="${ДОМ}-зерно">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="3" result="шум"/>
        <feColorMatrix in="шум" type="saturate" values="0"/>
        <feComponentTransfer><feFuncA type="linear" slope="0.14"/></feComponentTransfer>
        <feComposite operator="in" in2="SourceGraphic"/>
      </filter>
    </defs>`;



  const svg = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||170}" preserveAspectRatio="xMidYMid meet"
       style="display:block;width:100%;height:auto" aria-hidden="true">
       <defs>
         <marker id="${ДОМ}-стрелка" viewBox="0 0 10 10" refX="8" refY="5"
                 markerWidth="6" markerHeight="6" orient="auto">
           <path d="M0 0 L10 5 L0 10 z" fill="rgba(255,255,255,.5)"/>
         </marker>
       </defs>${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;



  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||12}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ПЕРО}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;


  const рамка = (x,y,ш,в,заливка,обводка,скругл,толщ) =>
    `<rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="${скругл==null?10:скругл}"
       fill="${заливка||'rgba(255,255,255,.05)'}" stroke="${обводка||ЛИНИЯ}"
       stroke-width="${толщ==null?1.4:толщ}"/>`;


  const круг = (cx,cy,r,заливка,обводка) =>
    `<circle cx="${cx}" cy="${cy}" r="${r}" fill="${заливка||'rgba(255,255,255,.06)'}"
       ${обводка?`stroke="${обводка}" stroke-width="1.5"`:''}/>`;


  /* Линия, которая прорисовывается от начала к концу: движение показывает
     связь, а не украшает. */
  const линияРисуется = (x1,y1,x2,y2,цвет,толщ,задержка) => {
    const д = Math.round(Math.hypot(x2-x1, y2-y1));
    return `<line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${цвет||ЛИНИЯ}"
      stroke-width="${толщ||1.6}" stroke-linecap="round" stroke-dasharray="${д}" stroke-dashoffset="${д}">
      <animate attributeName="stroke-dashoffset" from="${д}" to="0"
        dur="380ms" begin="${Number(задержка||0).toFixed(2)}s" fill="freeze"/></line>`;
  };



  /* ── РЕБЯТА ──
     Девочки — в платье, с косами или хвостом; мальчики — в тунике и штанах.
     Все разные: волосы, причёска, цвет одежды, рост. */
  const ДЕТИ = {
    петя:   {пол:'м', волосы:'#4a3524', причёска:'вихор',    одежда:'#5b8fd0'},
    катя:   {пол:'д', волосы:'#8a5a2a', причёска:'косы',     одежда:'#d9705f', бант:'#e8c46a'},
    вика:   {пол:'д', волосы:'#e8c46a', причёска:'хвост',    одежда:'#8a6ec0', бант:'#e86a5a'},
    оля:    {пол:'д', волосы:'#2f2a26', причёска:'косы',     одежда:'#5f9e6a', бант:'#ffd76a'},
    маша:   {пол:'д', волосы:'#b5632a', причёска:'хвост',    одежда:'#c98a3a', бант:'#7fd1ff'},
    мальчик:{пол:'м', волосы:'#3a2f28', причёска:'короткие', одежда:'#3f8f8a'},
    девочка:{пол:'д', волосы:'#6a4a2a', причёска:'косы',     одежда:'#c0607a', бант:'#ffd76a'},
    нейтр:  {пол:'',  волосы:'#5a4a3a', причёска:'короткие', одежда:'#6b7a86'}
  };

  const дитя = (x, y, рост, вид) => {
    const в = (typeof вид === 'string' ? ДЕТИ[вид] : вид) || ДЕТИ.нейтр;
    const р = рост || 30, г = р*0.26;
    const головаY = y - р + г*0.9;
    const косы = в.причёска === 'косы', хвост = в.причёска === 'хвост';
    return `
      <g>
        <ellipse cx="${x}" cy="${y+1.5}" rx="${р*0.30}" ry="${р*0.09}"
          fill="rgba(0,0,0,.42)" filter="url(#${ДОМ}-мягко2)"/>
        <ellipse cx="${x-р*0.11}" cy="${y}" rx="${р*0.09}" ry="${р*0.035}" fill="#3a2c22"/>
        <ellipse cx="${x+р*0.11}" cy="${y}" rx="${р*0.09}" ry="${р*0.035}" fill="#3a2c22"/>
        ${в.пол === 'д'
          ? `<path d="M${x-р*0.12} ${y-р*0.34} L${x+р*0.12} ${y-р*0.34}
                    L${x+р*0.30} ${y} L${x-р*0.30} ${y} Z"
               fill="${в.одежда}" stroke="rgba(0,0,0,.35)" stroke-width="1"/>
             <path d="M${x-р*0.14} ${y-р*0.60} C${x-р*0.16} ${y-р*0.42} ${x+р*0.16} ${y-р*0.42} ${x+р*0.14} ${y-р*0.60} Z"
               fill="${в.одежда}" stroke="rgba(0,0,0,.3)" stroke-width="1"/>`
          : `<path d="M${x-р*0.14} ${y-р*0.12} L${x-р*0.11} ${y-р*0.56} L${x+р*0.11} ${y-р*0.56} L${x+р*0.14} ${y-р*0.12} Z"
               fill="${в.одежда}" stroke="rgba(0,0,0,.35)" stroke-width="1"/>
             <path d="M${x-р*0.13} ${y-р*0.30} L${x+р*0.13} ${y-р*0.30}" stroke="#3a2c22"
               stroke-width="${(р*0.10).toFixed(1)}" stroke-linecap="round" opacity=".9"/>`}
        <path d="M${x-р*0.13} ${y-р*0.50} L${x-р*0.26} ${y-р*0.30}" stroke="${в.пол==='д'?в.одежда:'#e0b48f'}"
          stroke-width="${(р*0.075).toFixed(1)}" stroke-linecap="round"/>
        <path d="M${x+р*0.13} ${y-р*0.50} L${x+р*0.26} ${y-р*0.30}" stroke="${в.пол==='д'?в.одежда:'#e0b48f'}"
          stroke-width="${(р*0.075).toFixed(1)}" stroke-linecap="round"/>
        <path d="M${x} ${y-р*0.58} L${x} ${y-р*0.66}" stroke="#e0b48f" stroke-width="${(р*0.08).toFixed(1)}"/>
        <circle cx="${x}" cy="${головаY}" r="${г.toFixed(1)}" fill="url(#${ДОМ}-кожа)"
          stroke="rgba(0,0,0,.4)" stroke-width="1"/>
        <ellipse cx="${x-г*0.55}" cy="${головаY+г*0.35}" rx="${(г*0.28).toFixed(1)}" ry="${(г*0.18).toFixed(1)}"
          fill="#e08a7a" opacity=".32"/>
        <ellipse cx="${x+г*0.55}" cy="${головаY+г*0.35}" rx="${(г*0.28).toFixed(1)}" ry="${(г*0.18).toFixed(1)}"
          fill="#e08a7a" opacity=".32"/>
        <ellipse cx="${x-г*0.34}" cy="${головаY+г*0.05}" rx="${(г*0.17).toFixed(1)}" ry="${(г*0.20).toFixed(1)}" fill="#fdfaf3"/>
        <ellipse cx="${x+г*0.34}" cy="${головаY+г*0.05}" rx="${(г*0.17).toFixed(1)}" ry="${(г*0.20).toFixed(1)}" fill="#fdfaf3"/>
        <circle cx="${x-г*0.32}" cy="${головаY+г*0.07}" r="${(г*0.10).toFixed(1)}" fill="#3b2a1e"/>
        <circle cx="${x+г*0.36}" cy="${головаY+г*0.07}" r="${(г*0.10).toFixed(1)}" fill="#3b2a1e"/>
        <path d="M${x-г*0.22} ${головаY+г*0.52} Q${x} ${головаY+г*0.70} ${x+г*0.22} ${головаY+г*0.52}"
          fill="none" stroke="#a0504a" stroke-width="1.1" stroke-linecap="round"/>
        <path d="M${x-г} ${головаY-г*0.10} A${г} ${г} 0 0 1 ${x+г} ${головаY-г*0.10}
                L${x+г*0.86} ${головаY-г*0.62} L${x-г*0.86} ${головаY-г*0.62} Z"
          fill="${в.волосы}" opacity=".97"/>
        ${косы?`
          <path d="M${x-г*0.92} ${головаY+г*0.1} C${x-г*1.2} ${головаY+г*0.5} ${x-г*1.12} ${головаY+г*0.95} ${x-г*0.98} ${головаY+г*1.25}"
            stroke="${в.волосы}" stroke-width="${(г*0.30).toFixed(1)}" fill="none" stroke-linecap="round"/>
          <path d="M${x+г*0.92} ${головаY+г*0.1} C${x+г*1.2} ${головаY+г*0.5} ${x+г*1.12} ${головаY+г*0.95} ${x+г*0.98} ${головаY+г*1.25}"
            stroke="${в.волосы}" stroke-width="${(г*0.30).toFixed(1)}" fill="none" stroke-linecap="round"/>
          ${в.бант?`<circle cx="${x-г*0.98}" cy="${головаY+г*1.3}" r="${(г*0.22).toFixed(1)}" fill="${в.бант}"/>
                     <circle cx="${x+г*0.98}" cy="${головаY+г*1.3}" r="${(г*0.22).toFixed(1)}" fill="${в.бант}"/>`:''}`
        : хвост?`
          <path d="M${x+г*0.85} ${головаY-г*0.5} C${x+г*1.5} ${головаY-г*0.1} ${x+г*1.4} ${головаY+г*0.9} ${x+г*1.0} ${головаY+г*1.2}"
            stroke="${в.волосы}" stroke-width="${(г*0.34).toFixed(1)}" fill="none" stroke-linecap="round"/>
          ${в.бант?`<circle cx="${x+г*1.1}" cy="${головаY-г*0.35}" r="${(г*0.26).toFixed(1)}" fill="${в.бант}"/>`:''}`
        : `<path d="M${x-г*0.9} ${головаY-г*0.35} L${x-г*0.2} ${головаY-г*1.05} L${x+г*0.5} ${головаY-г*0.65}"
             fill="${в.волосы}" opacity=".95"/>`}
      </g>`;
  };


  /* Разбор: подробный ответ и частая ошибка. */
  const разбор = (текст, ошибка) =>
    `<div class="card" style="border-color:${ЗЕЛЁН};padding:12px 14px">
       <div style="font-size:16px;font-weight:bold;color:${ЗЕЛЁН};margin-bottom:6px">✅ Разбор</div>
       <div style="font-size:16px;line-height:1.55">${esc(текст)}</div>
       ${ошибка?`<div style="font-size:14px;line-height:1.5;color:${КРАСН};margin-top:8px">⚠ ${esc(ошибка)}</div>`:''}
     </div>`;


  /* Жетон с числом: табличка с рамкой, тенью и уголками. */
  const жетон = (x,y,текст,цвет,размер) => {
    const к = размер||20, ш=к*1.9, в=к*1.25;
    return `
      <g>
        <rect x="${x-ш/2+1.5}" y="${y-в/2+2.5}" width="${ш}" height="${в}" rx="5"
          fill="rgba(0,0,0,.45)" filter="url(#${ДОМ}-мягко2)"/>
        <rect x="${x-ш/2}" y="${y-в/2}" width="${ш}" height="${в}" rx="5"
          fill="url(#${ДОМ}-стена)" stroke="${цвет||ЛИНИЯ}" stroke-width="1.6"/>
        <path d="M${x-ш/2+3} ${y-в/2+1} L${x-ш/2+1} ${y-в/2+3}" stroke="${цвет||ЛИНИЯ}" stroke-width="1.3"/>
        <path d="M${x+ш/2-3} ${y+в/2-1} L${x+ш/2-1} ${y+в/2-3}" stroke="${цвет||ЛИНИЯ}" stroke-width="1.3"/>
        <text x="${x}" y="${y+к*0.36}" text-anchor="middle" font-size="${к}"
          font-weight="bold" fill="${цвет||ПЕРО}" font-family="Georgia,serif"
          style="font-variant-numeric:tabular-nums">${esc(текст)}</text>
      </g>`;
  };


  /* Знак операции в кружке: ×2, +3, −4, ÷5. */
  const операция = (x,y,знак,подпись,цвет,жирная) => `
    <g>
      <circle cx="${x}" cy="${y}" r="17" fill="rgba(255,255,255,.06)"
        stroke="${цвет||ЗОЛОТО}" stroke-width="${жирная?2:1.6}"/>
      ${т(x,y+5,знак,16,цвет||ЗОЛОТО,true)}
      ${подпись?т(x,y+30,подпись,12,ТИХО):''}
    </g>`;







  /* Жетон с числом: табличка с рамкой, тенью и уголками. */
  /* Сундук с монетами. */
  const сундук = (x,y,ш,в,открыт) => `
    <g>
      <ellipse cx="${x+ш/2}" cy="${y+в+2}" rx="${ш*0.52}" ry="4" fill="rgba(0,0,0,.4)"
        filter="url(#${ДОМ}-мягко2)"/>
      <rect x="${x}" y="${y+в*0.3}" width="${ш}" height="${в*0.7}" rx="3"
        fill="url(#${ДОМ}-дерево)" stroke="#4a2a10" stroke-width="1.5"/>
      <path d="M${x} ${y+в*0.3} Q${x+ш/2} ${y-(открыт?в*0.5:0.0)} ${x+ш} ${y+в*0.3} Z"
        fill="url(#${ДОМ}-дерево)" stroke="#4a2a10" stroke-width="1.5"/>
      <rect x="${x+ш*0.42}" y="${y+в*0.55}" width="${ш*0.16}" height="${в*0.22}" rx="2"
        fill="${ЗОЛОТО}" stroke="#8a6420" stroke-width="1"/>
    </g>`;


  /* Предметы бюро находок. */
  const ключик = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <circle r="5" fill="none" stroke="${ЗОЛОТО}" stroke-width="2"/>
      <path d="M0 5 L0 14 M0 10 L5 10 M0 13 L4 13" stroke="${ЗОЛОТО}" stroke-width="2" stroke-linecap="round"/>
    </g>`;
  const перстень = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <circle r="6" fill="none" stroke="#c9b591" stroke-width="2.2"/>
      <circle cy="-7" r="2.6" fill="${СИНЬ}"/>
    </g>`;
  const алмаз = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <path d="M0 -8 L8 -2 L0 9 L-8 -2 Z" fill="#bfe6ff" stroke="#7fb8d8" stroke-width="1.2"/>
      <path d="M-8 -2 L8 -2 M0 -8 L0 9" stroke="#7fb8d8" stroke-width="0.8" opacity=".7"/>
    </g>`;
  const жемчужина = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <circle r="6" fill="#f6efe2" stroke="#c9b591" stroke-width="1.2"/>
      <circle cx="-2" cy="-2" r="1.6" fill="#fff"/>
    </g>`;


  /* ── ПРЕДМЕТЫ ЛАГЕРЯ ── */

  /* Гриб: шляпка, ножка, пятнышки. */
  const гриб = (x,y,м) => {
    const к=м||1;
    return `<g transform="translate(${x} ${y}) scale(${к})">
      <path d="M-3 0 L-4 12 L4 12 L3 0 Z" fill="#efe3cd" stroke="#b8a06a" stroke-width="1"/>
      <path d="M-13 2 C-13 -11 13 -11 13 2 Z" fill="url(#${ДОМ}-гриб)" stroke="#8a3a2a" stroke-width="1.2"/>
      <circle cx="-6" cy="-4" r="2" fill="#fff3e0" opacity=".85"/>
      <circle cx="2" cy="-6" r="1.5" fill="#fff3e0" opacity=".85"/>
      <circle cx="7" cy="-2" r="1.2" fill="#fff3e0" opacity=".8"/>
    </g>`;
  };

  /* Дракон: тело, крылья и несколько голов на шеях. */
  const дракон = (x,y,рост,голов,цвет) => {
    const р=рост||40, ц=цвет||'#5f9e6a';
    let шеи='';
    for(let i=0;i<голов;i++){
      const угол=-0.75+i*(1.5/Math.max(1,голов-1||1));
      const дл=р*(голов>4?0.55:0.75);
      const гx=x+Math.sin(угол)*дл, гy=y-р*0.7-Math.cos(угол)*дл;
      шеи += `<path d="M${x} ${y-р*0.5} Q${(x+гx)/2} ${y-р*0.75} ${гx} ${гy+р*0.12}"
                 fill="none" stroke="${ц}" stroke-width="${(р*0.09).toFixed(1)}" stroke-linecap="round"/>
               <circle cx="${гx}" cy="${гy}" r="${(р*0.13).toFixed(1)}" fill="${ц}" stroke="rgba(0,0,0,.35)" stroke-width="0.9"/>
               <circle cx="${гx-р*0.04}" cy="${гy-р*0.02}" r="${(р*0.025).toFixed(1)}" fill="#2b2118"/>
               <circle cx="${гx+р*0.04}" cy="${гy-р*0.02}" r="${(р*0.025).toFixed(1)}" fill="#2b2118"/>`;
    }
    return `<g>
      <ellipse cx="${x}" cy="${y+2}" rx="${р*0.4}" ry="${р*0.07}" fill="rgba(0,0,0,.35)" filter="url(#${ДОМ}-мягко2)"/>
      <ellipse cx="${x}" cy="${y-р*0.2}" rx="${р*0.42}" ry="${р*0.26}" fill="${ц}" stroke="rgba(0,0,0,.35)" stroke-width="1"/>
      <path d="M${x-р*0.3} ${y-р*0.4} L${x-р*0.75} ${y-р*0.8} L${x-р*0.2} ${y-р*0.15} Z"
        fill="${ц}" opacity=".8" stroke="rgba(0,0,0,.3)" stroke-width="0.9"/>
      <path d="M${x+р*0.3} ${y-р*0.4} L${x+р*0.75} ${y-р*0.8} L${x+р*0.2} ${y-р*0.15} Z"
        fill="${ц}" opacity=".8" stroke="rgba(0,0,0,.3)" stroke-width="0.9"/>
      <path d="M${x+р*0.4} ${y-р*0.1} Q${x+р*0.8} ${y-р*0.2} ${x+р*0.7} ${y+р*0.05}"
        fill="none" stroke="${ц}" stroke-width="${(р*0.07).toFixed(1)}" stroke-linecap="round"/>
      ${шеи}
    </g>`;
  };

  /* Костёр: брёвна и живое пламя. */
  const костёр = (x,y,м) => {
    const к=м||1;
    return `<g transform="translate(${x} ${y}) scale(${к})">
      <ellipse cy="6" rx="20" ry="5" fill="rgba(0,0,0,.35)" filter="url(#${ДОМ}-мягко2)"/>
      <path d="M-16 4 L16 -2 M-16 -2 L16 4" stroke="#7a4a24" stroke-width="5" stroke-linecap="round"/>
      <path d="M0 2 C-9 -6 -4 -16 0 -24 C2 -14 8 -14 9 -20 C13 -10 8 0 0 2 Z"
        fill="url(#${ДОМ}-огонь)" stroke="#a3551f" stroke-width="1.1">
        <animateTransform attributeName="transform" type="scale"
          values="1 1; 1.06 0.96; 0.97 1.04; 1 1" dur="1.6s" repeatCount="indefinite"
          additive="sum"/>
      </path>
      <circle cy="-10" r="3" fill="#ffe9a8" opacity=".7">
        <animate attributeName="cy" values="-8;-26" dur="2.6s" repeatCount="indefinite"/>
        <animate attributeName="opacity" values=".7;0" dur="2.6s" repeatCount="indefinite"/>
      </circle>
    </g>`;
  };

  /* Палатка. */
  const палатка = (x,y,ш,в) => `
    <g>
      <ellipse cx="${x}" cy="${y+2}" rx="${ш*0.6}" ry="5" fill="rgba(0,0,0,.4)" filter="url(#${ДОМ}-мягко)"/>
      <path d="M${x-ш/2} ${y} L${x} ${y-в} L${x+ш/2} ${y} Z" fill="url(#${ДОМ}-ткань)"
        stroke="#4a5a2a" stroke-width="1.5"/>
      <path d="M${x} ${y-в} L${x} ${y}" stroke="#4a5a2a" stroke-width="1"/>
      <path d="M${x-ш*0.16} ${y} L${x} ${y-в*0.45} L${x+ш*0.16} ${y} Z" fill="rgba(0,0,0,.35)"/>
    </g>`;

  /* Калитка с кодовым замком: две стойки, перекладина и замок с цифрами. */
  const калитка = (x,y,ш,в,код) => `
    <g>
      <rect x="${x}" y="${y-в}" width="5" height="${в}" fill="#6f4420"/>
      <rect x="${x+ш-5}" y="${y-в}" width="5" height="${в}" fill="#6f4420"/>
      <rect x="${x}" y="${y-в}" width="${ш}" height="6" rx="2" fill="#8a5f3a" stroke="#4a2a10" stroke-width="1"/>
      <rect x="${x+ш*0.3}" y="${y-в*0.55}" width="${ш*0.4}" height="${в*0.3}" rx="5"
        fill="#c9b591" stroke="#6f4420" stroke-width="1.4"/>
      <path d="M${x+ш*0.4} ${y-в*0.55} L${x+ш*0.4} ${y-в*0.68} Q${x+ш*0.5} ${y-в*0.8} ${x+ш*0.6} ${y-в*0.68}
               L${x+ш*0.6} ${y-в*0.55}" fill="none" stroke="#c9b591" stroke-width="3"/>
      ${код?т(x+ш*0.5,y-в*0.35,код,12,'#3a2a12',true):''}
    </g>`;

  /* Изумруд: гранёный зелёный камень. */
  const изумруд = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <path d="M0 -9 L8 -3 L5 8 L-5 8 L-8 -3 Z" fill="#7fe0a8" stroke="#2f7a52" stroke-width="1.2"/>
      <path d="M-8 -3 L8 -3 M0 -9 L0 8" stroke="#2f7a52" stroke-width="0.8" opacity=".6"/>
    </g>`;

  /* ══════════════ ШАГИ ══════════════ */
  const ШАГИ = [];

  /* ── 1. БОЛЬШАЯ ИГРА ── */
  ШАГИ.push({
    т:'Большая игра',
    рисунок(){
      let s = `<rect x="8" y="8" width="320" height="172" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="172" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,26,'Лагерь играет в большую игру',14,ЛАМПА,true)}`;
      /* трава и небо */
      s += `<rect x="8" y="112" width="320" height="68" fill="url(#${ДОМ}-трава)"/>`;
      s += `<path d="M8 112 L328 112" stroke="#5f8a5a" stroke-width="1.2" opacity=".6"/>`;
      /* звёзды: игра идёт вечером */
      for(let i=0;i<10;i++){
        const x=26+i*32, y=40+((i*7)%18);
        s += `<circle cx="${x}" cy="${y}" r="1.3" fill="${ЛАМПА}" opacity=".3">
                <animate attributeName="opacity" values=".15;.8;.15" dur="${(2.6+i*0.2).toFixed(1)}s"
                  begin="${(i*0.2).toFixed(1)}s" repeatCount="indefinite"/>
              </circle>`;
      }
      s += палатка(66,112,66,52);
      s += `<g>${костёр(150,110,1.1)}</g>`;
      s += калитка(214,112,96,58,'?');
      /* флажки между палаткой и калиткой */
      for(let i=0;i<5;i++){
        const x=100+i*26;
        s += `<path d="M${x} 62 L${x} 72" stroke="${ЛИНИЯ}" stroke-width="1"/>
              <path d="M${x} 62 L${x+12} 66 L${x} 70 Z" fill="${i%2?СИНЬ:КРАСН}" opacity=".9"/>`;
      }
      s += `${т(168,168,'пять этапов — пять задач тура',12,'#9fb7a4')}`;
      return svg(s,188);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Вечером в лагере начинается большая игра: на калитке кодовый замок, в траве спрятаны находки, у костра считаются по кругу, а перед сном рассказывают сказку про драконов. Пять этапов — пять задач этого тура.</div>
          <div style="font-size:16px;line-height:1.5;margin-top:8px">Задач пять, за каждую можно получить 2 балла — всего 10. В конце будет итог: балл и что стоит повторить.</div>
        </div>`;
    }
  });

  /* ── 2. КОДОВЫЙ ЗАМОК: теория ── */
  ШАГИ.push({
    т:'Разряды кода',
    рисунок(){
      /* Код — сумма четырёх чисел, записанных буквами. Разбираем по разрядам:
         целые, десятые, сотые. Видно, какие буквы попадают в два разряда. */
      const КРУГ=12, ДОЛЯ=КРУГ/3;
      const мысли=['складываем целые: О + И + П + А','десятые: Л + М + И + Д','сотые: только А — она стоит дважды'];
      let s = `<rect x="8" y="8" width="320" height="190" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="190" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'О,Л + И,М + П,И + А,ДА',16,ЛАМПА,true)}`;
      мысли.forEach((м,i)=>{
        s += `<text class="${ДОМ}-речь${i===0?' '+ДОМ+'-речь-первая':''}" x="168" y="46"
                text-anchor="middle" font-size="14" font-weight="bold" fill="${ЗОЛОТО}"
                font-family="Georgia,serif" opacity="0"
                style="animation:${ДОМ}-строка ${КРУГ}s linear ${(i*ДОЛЯ).toFixed(2)}s infinite">${esc(м)}</text>`;
      });
      /* три разряда столбиками */
      const разряды=[
        {имя:'целые', буквы:'О + И + П + А', макс:'≤ 30', y:74, ц:ЗЕЛЁН},
        {имя:'десятые', буквы:'Л + М + И + Д', макс:'≤ 2,0', y:112, ц:СИНЬ},
        {имя:'сотые', буквы:'А', макс:'≤ 0,09', y:150, ц:ЗОЛОТО}
      ];
      разряды.forEach((р,i)=>{
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 380ms ease-out ${(0.15+i*0.4).toFixed(2)}s 1 forwards">
                ${рамка(24,р.y-14,288,28,'rgba(255,255,255,.04)',р.ц,8,1.3)}
                ${т(36,р.y+4,р.имя,12,р.ц,true,'start')}
                ${т(120,р.y+4,р.буквы,12,ПЕРО,false,'start')}
                ${т(300,р.y+4,р.макс,12,р.ц,true,'end')}
              </g>`;
      });
      s += `${т(168,184,'буква в двух разрядах считается дважды',12,ТИХО)}`;
      return svg(s,200);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Сумму удобно разложить по разрядам и оценить каждую часть сверху. Целые — это О + И + П + А; их не больше 9 + 8 + 7 + 6 = 30. Десятые собираются из Л, М, И и Д, а сотые зависят только от А. Важно помнить: буквы И и А стоят сразу в двух разрядах, поэтому цифры нельзя расставлять свободно.</div>
        </div>`;
    }
  });

  /* ── 3. ЗАМОК: вводим сумму ── */
  ШАГИ.push({
    т:'Кодовый замок',
    рисунок(){
      const п=память(), проверено=п.проверено[0], верно=суммаВерна(п.ответы[0]);
      let s = `<rect x="8" y="8" width="320" height="182" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="182" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, верно?'замок открыт':'подбери цифры и найди наибольшую сумму',14, верно?ЗЕЛЁН:ЛАМПА,true)}`;
      /* калитка с замком */
      s += калитка(96,116,144,80, верно?'32,18':'?');
      /* буквы над замком */
      const буквы=['О','И','П','А'];
      буквы.forEach((б,i)=>{
        const x=76+i*64;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out ${(0.15+i*0.2).toFixed(2)}s 1 forwards">
                <circle cx="${x}" cy="44" r="15" fill="rgba(255,255,255,.06)" stroke="${ЗОЛОТО}" stroke-width="1.5"/>
                ${т(x,49,б,16,ЗОЛОТО,true)}
              </g>`;
      });
      /* целые: 9+8+7+6 = 30, десятые и сотые */
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 400ms ease-out 1.0s 1 forwards">
              ${т(168,150,'9 + 8 + 7 + 6 = 30',12,ЗЕЛЁН,true)}
            </g>`;
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 400ms ease-out 1.4s 1 forwards">
              ${т(168,168,'и ещё 0,18 из дробной части',12,СИНЬ,true)}
            </g>`;
      s += `${т(168,184, верно?'32,18 — наибольшая сумма':'цифры не повторяются: каждая буква — своя цифра',12,ТИХО)}`;
      return svg(s,198);
    },
    низ(){
      const п=память(), проверено=п.проверено[0], верно=суммаВерна(п.ответы[0]);
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[0].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[0].вопрос)}</div>
        </div>`;
      блок += `<div class="card ${проверено&&!верно?'':ДОМ+'-ждёт'}" style="padding:12px 14px">
          <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
            <input id="${ДОМ}-сумма" type="text" value="${esc(п.ответы[0]||'')}" placeholder="например 32,18"
              style="min-height:48px;width:170px;font-size:20px;padding:0 12px;border-radius:10px;
                     border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8">
            <button class="btn" style="min-height:48px;margin:0" onclick="Т704.сумма()">Проверить</button>
          </div>
        </div>`;
      if(проверено) блок += разбор(ЗАДАЧИ[0].разбор, ЗАДАЧИ[0].ошибка);
      if(верно) блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: сундуки →</button></div>`;
      return блок;
    }
  });

  /* ── 4. ЛОЖНЫЕ НАДПИСИ: теория ── */
  ШАГИ.push({
    т:'Все надписи врут',
    рисунок(){
      let s = `<rect x="8" y="8" width="320" height="188" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="188" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Каждая надпись переворачивается',14,ЛАМПА,true)}`;
      const пары=[
        ['сундук 1: «нет алмаза»','значит, алмаз здесь'],
        ['сундук 2: «этот пуст»','значит, здесь что-то есть'],
        ['сундук 3: «меньше трёх»','значит, три или больше'],
        ['сундук 3: «здесь ключик»','значит, ключика тут нет']
      ];
      пары.forEach(([было,стало],i)=>{
        const y=56+i*32;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 380ms ease-out ${(0.15+i*0.45).toFixed(2)}s 1 forwards">
                ${рамка(20,y-14,150,26,'rgba(232,106,90,.10)',КРАСН,7,1.2)}
                ${т(95,y+4,было,12,ПЕРО)}
                ${линияРисуется(178,y-1,196,y-1,ЗОЛОТО,1.6,0.3+i*0.45)}
                ${рамка(202,y-14,110,26,'rgba(143,209,168,.10)',ЗЕЛЁН,7,1.2)}
                ${т(257,y+4,стало,12,ЗЕЛЁН)}
              </g>`;
      });
      s += `${т(168,180,'четыре врущие надписи раскладывают пять находок',12,ТИХО)}`;
      return svg(s,196);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Раз все надписи врут, каждая говорит ровно противоположное. «Нет алмаза» — значит алмаз здесь; «сундук пуст» — значит не пуст; «меньше трёх» — значит три или больше; «здесь ключик» — значит ключика тут нет. Так надписи сами раскладывают находки.</div>
        </div>`;
    }
  });

  /* ── 5. ТРИ СУНДУКА: раскладываем ── */
  ШАГИ.push({
    т:'Три сундука',
    рисунок(){
      const п=память(), где=п.ответы[1]||{}, верно=сундукиВерны(где);
      const предметы=ЗАДАЧИ[1].предметы;
      let s = `<rect x="8" y="8" width="320" height="196" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="196" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, верно?'все находки разложены':'разложи находки по трём сундукам',14, верно?ЗЕЛЁН:ЛАМПА,true)}`;
      /* три сундука с надписями */
      const надписи=[['«нет алмаза»'],['«сундук пуст»'],['«меньше трёх»','«здесь ключик»']];
      [0,1,2].forEach(i=>{
        const x=30+i*98;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 320ms ease-out ${(0.1+i*0.2).toFixed(2)}s 1 forwards">
                ${сундук(x,52,84,46,false)}
                ${т(x+42,44,'Сундук '+(i+1),12,ЗОЛОТО,true)}
                ${надписи[i].map((н,j)=>т(x+42,116+j*14,н,12,ПЕРО)).join('')}
              </g>`;
        /* что внутри */
        const внутри=предметы.filter(им=>(где[им]||1)===(i+1));
        внутри.forEach((им,j)=>{
          const xx=x+22+j*20, y=76;
          s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out ${(0.15+j*0.1).toFixed(2)}s 1 forwards">
                  ${предметРисунок(им,xx,y)}
                </g>`;
        });
      });
      /* предметы и их номера */
      предметы.forEach((им,i)=>{
        const x=40+i*64, y=170, куда=где[им]||1;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out ${(0.2+i*0.1).toFixed(2)}s 1 forwards">
                ${рамка(x-26,y-15,52,30,'rgba(255,255,255,.05)', куда===1?ЗОЛОТО:(куда===2?СИНЬ:ЗЕЛЁН),8,1.2)}
                ${предметРисунок(им,x-9,y)}
                ${т(x+12,y+4,'№'+куда,12, куда===1?ЗОЛОТО:(куда===2?СИНЬ:ЗЕЛЁН),true)}
              </g>`;
      });
      s += `${т(168,192,'нажимай на находку — она переезжает в следующий сундук',12,ТИХО)}`;
      return svg(s,206);

      function предметРисунок(им,x,y){
        if(им==='ключик') return ключик(x,y,0.85);
        if(им==='перстень') return перстень(x,y,0.85);
        if(им==='алмаз') return алмаз(x,y,0.85);
        if(им==='изумруд') return изумруд(x,y,0.85);
        return жемчужина(x,y,0.85);
      }
    },
    низ(){
      const п=память(), где=п.ответы[1]||{}, верно=сундукиВерны(где);
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[1].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[1].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${ДОМ}-ждёт" style="padding:12px 14px">
            <div style="display:flex;gap:8px;flex-wrap:wrap">` +
          ЗАДАЧИ[1].предметы.map(им=>
            `<button class="btn" style="min-height:48px;margin:0"
               onclick="Т704.положить('${esc(им)}')">${esc(им)}: сундук ${где[им]||1}</button>`).join('') +
          `</div></div>`;
      } else {
        блок += разбор(ЗАДАЧИ[1].разбор, ЗАДАЧИ[1].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: костёр →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 6. СЧЁТ ПО КРУГУ: теория ── */
  ШАГИ.push({
    т:'Счёт по кругу',
    рисунок(){
      /* Номера одного человека растут на число ребят. 5 → 29 за два круга. */
      const n=12;
      let s = `<rect x="8" y="8" width="320" height="200" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="200" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Один и тот же человек — через 12 номеров',14,ЛАМПА,true)}`;
      s += `<g>${костёр(168,104,0.9)}</g>`;
      for(let i=0;i<n;i++){
        const угол=(i/n)*Math.PI*2 - Math.PI/2;
        const x=168+Math.cos(угол)*62, y=104+Math.sin(угол)*50;
        const пятый=(i===4);
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 280ms ease-out ${(0.1+i*0.06).toFixed(2)}s 1 forwards">
                <circle cx="${x}" cy="${y}" r="10" fill="rgba(255,255,255,.05)"
                  stroke="${пятый?ЗОЛОТО:ЛИНИЯ}" stroke-width="${пятый?1.8:1.1}"/>
                ${т(x,y+4,String(i+1),12, пятый?ЗОЛОТО:ПЕРО, пятый)}
              </g>`;
      }
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 380ms ease-out 0.9s 1 forwards">
              ${т(168,44,'5 + 2N = 29, значит N = 12',12,ЗОЛОТО,true)}
            </g>`;
      s += `${т(168,190,'восьмой круг кончается на 96, поэтому 100 — девятый круг',12,ТИХО)}`;
      return svg(s,210);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Каждый новый круг прибавляет к номеру ровно число ребят. От первого круга до третьего прошло два круга: 5 + 2N = 29, значит N = 12. Дальше считаем круги: восьмой круг заканчивается номером 96, поэтому 100 — уже девятый круг.</div>
        </div>`;
    }
  });

  /* ── 7. КОСТЁР: вводим два числа ── */
  ШАГИ.push({
    т:'Счёт у костра',
    рисунок(){
      const п=память(), о=п.ответы[2]||{}, верно=костёрВерен(о);
      let s = `<rect x="8" y="8" width="320" height="176" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="176" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, верно?'12 ребят, номер 100 — на девятом круге':'сколько ребят и на каком круге 100?',14, верно?ЗЕЛЁН:ЛАМПА,true)}`;
      /* три круга полосками: 1..12, 13..24, 25..36 */
      const круг=Number(о.ребят)>0?Number(о.ребят):12;
      for(let р=0;р<3;р++){
        const y=54+р*30;
        s += `${т(24,y+6,'круг '+(р+1),12,ТИХО,false,'start')}`;
        for(let i=0;i<круг && i<13;i++){
          const x=92+i*17;
          s += `<rect x="${x}" y="${y-8}" width="14" height="16" rx="3"
            fill="${р===0&&i===4?'rgba(255,215,106,.9)':'rgba(255,255,255,.07)'}"
            stroke="${р===0&&i===4?ЗОЛОТО:'#7d97a6'}" stroke-width="1"/>`;
          s += `${т(x+7,y+4,String(р*круг+i+1),10, (р===0&&i===4)?'#3a2a12':ТИХО)}`;
        }
      }
      s += `<g>${костёр(168,158,0.8)}</g>`;
      s += `${т(168,170,'номер 100 больше 96 — значит круг девятый',12,ТИХО)}`;
      return svg(s,192);
    },
    низ(){
      const п=память(), о=п.ответы[2]||{}, проверено=п.проверено[2], верно=костёрВерен(о);
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[2].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[2].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${проверено?'':ДОМ+'-ждёт'}" style="padding:12px 14px">
            <div style="display:flex;gap:12px;flex-wrap:wrap;align-items:flex-end">
              <label style="font-size:14px;color:var(--muted)">ребят у костра
                <input id="${ДОМ}-ребят" type="number" inputmode="numeric" value="${esc(о.ребят??'')}"
                  style="display:block;min-height:48px;width:120px;margin-top:4px;font-size:20px;padding:0 10px;border-radius:10px;
                         border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8"></label>
              <label style="font-size:14px;color:var(--muted)">круг для номера 100
                <input id="${ДОМ}-круг" type="number" inputmode="numeric" value="${esc(о.круг??'')}"
                  style="display:block;min-height:48px;width:120px;margin-top:4px;font-size:20px;padding:0 10px;border-radius:10px;
                         border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8"></label>
            </div>
            <div style="margin-top:12px"><button class="btn" style="min-height:48px;margin:0"
              onclick="Т704.костёр()">Проверить</button></div>
            ${проверено?`<div style="font-size:16px;color:${КРАСН};margin-top:10px">Пока не сходится. Проверь: 5 + 2N = 29 и где кончается восьмой круг.</div>`:''}
          </div>`;
      } else {
        блок += разбор(ЗАДАЧИ[2].разбор, ЗАДАЧИ[2].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: грибы →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 8. ГРИБЫ: теория ── */
  ШАГИ.push({
    т:'Грибная арифметика',
    рисунок(){
      /* Всего грибов 13n + 29. Делить поровну можно, когда n делит 29. */
      let s = `<rect x="8" y="8" width="320" height="196" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="196" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Считаем, сколько грибов всего',14,ЛАМПА,true)}`;
      const строки=[
        ['Таня нашла','5',ЗОЛОТО],
        ['Илья нашёл','50',СИНЬ],
        ['каждый из остальных','13',ЗЕЛЁН],
        ['всего при n детях','13n + 29',ПЕРО],
        ['делим поровну: n делит 29','n = 29',КРАСН]
      ];
      строки.forEach(([текст,знач,ц],i)=>{
        const y=52+i*27;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 360ms ease-out ${(0.15+i*0.4).toFixed(2)}s 1 forwards">
                ${рамка(22,y-13,292,26,'rgba(255,255,255,.04)',ц,7,1.2)}
                ${т(34,y+4,текст,12,ПЕРО,false,'start')}
                ${т(302,y+4,знач,12,ц,true,'end')}
              </g>`;
        if(i===0) s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out 0.3s 1 forwards">${гриб(268,y-4,0.6)}</g>`;
        if(i===1) s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out 0.7s 1 forwards">${гриб(268,y-4,0.6)}</g>`;
      });
      s += `${т(168,190,'29 — простое число, поэтому детей ровно 29',12,ТИХО)}`;
      return svg(s,204);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Пусть детей n. Тогда всего грибов 5 + 50 + 13 · (n − 2) = 13n + 29. Поделить поровну можно только тогда, когда n делит это число. Так как n делит 13n, требуется, чтобы n делило 29. Детей больше двух, а 29 простое — значит, детей ровно 29.</div>
        </div>`;
    }
  });

  /* ── 9. ГРИБЫ: вводим число детей ── */
  ШАГИ.push({
    т:'Сколько детей ходило',
    рисунок(){
      const п=память(), верно=Number(п.ответы[3])===ЗАДАЧИ[3].ответ;
      const n=верно?ЗАДАЧИ[3].ответ:29;
      let s = `<rect x="8" y="8" width="320" height="176" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="176" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, верно?'каждому по 14 грибов':'сколько детей ходило в лес?',14, верно?ЗЕЛЁН:ЛАМПА,true)}`;
      /* корзина грибов и дети */
      for(let i=0;i<12;i++){
        const x=34+(i%6)*52, y=60+Math.floor(i/6)*34;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 280ms ease-out ${(0.1+i*0.07).toFixed(2)}s 1 forwards">
                ${гриб(x,y,0.75)}
              </g>`;
      }
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 400ms ease-out 1.0s 1 forwards">
              ${т(168,142,'всего грибов: 13 · '+n+' + 29 = '+(13*n+29),12,ПЕРО,true)}
            </g>`;
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 400ms ease-out 1.3s 1 forwards">
              ${т(168,162, верно?('поделили поровну: по '+((13*n+29)/n)+' каждому'):'каждому должно достаться поровну',12, верно?ЗЕЛЁН:ТИХО,true)}
            </g>`;
      s += `${т(168,180,'грибы считаются штуками — делится всё собранное',12,ТИХО)}`;
      return svg(s,196);
    },
    низ(){
      const п=память(), проверено=п.проверено[3], верно=Number(п.ответы[3])===ЗАДАЧИ[3].ответ;
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[3].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[3].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${проверено?'':ДОМ+'-ждёт'}" style="padding:12px 14px">
            <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
              <input id="${ДОМ}-детей" type="number" inputmode="numeric" value="${esc(п.ответы[3]??'')}"
                placeholder="сколько детей"
                style="min-height:48px;width:170px;font-size:20px;padding:0 12px;border-radius:10px;
                       border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8">
              <button class="btn" style="min-height:48px;margin:0" onclick="Т704.детей()">Проверить</button>
            </div>
            ${проверено?`<div style="font-size:16px;color:${КРАСН};margin-top:10px">Не сходится. Вспомни: n должно делить 13n + 29, то есть n делит 29.</div>`:''}
          </div>`;
      } else {
        блок += разбор(ЗАДАЧИ[3].разбор, ЗАДАЧИ[3].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: драконы →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 10. ДРАКОНЫ: теория ── */
  ШАГИ.push({
    т:'Одно неизвестное',
    рисунок(){
      /* Все количества через x: 7-головых x, 5-головых 2x, 6-головых 62 − 3x.
         Голов: 372 − x. По условию 355, значит x = 17. */
      let s = `<rect x="8" y="8" width="320" height="192" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="192" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Все количества — через одно неизвестное',14,ЛАМПА,true)}`;
      const строки=[
        ['семиголовых','x',ЗЕЛЁН],
        ['пятиголовых — вдвое больше','2x',СИНЬ],
        ['шестиголовых — остальные','62 − 3x',ЗОЛОТО],
        ['голов всего','7x + 10x + 372 − 18x',ПЕРО],
        ['получается','372 − x = 355',КРАСН]
      ];
      строки.forEach(([текст,знач,ц],i)=>{
        const y=52+i*28;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 360ms ease-out ${(0.15+i*0.4).toFixed(2)}s 1 forwards">
                ${рамка(22,y-13,292,26,'rgba(255,255,255,.04)',ц,7,1.2)}
                ${т(34,y+4,текст,12,ПЕРО,false,'start')}
                ${т(302,y+4,знач,12,ц,true,'end')}
              </g>`;
      });
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 420ms ease-out 2.1s 1 forwards">
              ${дракон(96,176,50,7,ЗЕЛЁН)}
            </g>`;
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 420ms ease-out 2.4s 1 forwards">
              ${дракон(240,176,50,5,СИНЬ)}
            </g>`;
      return svg(s,208);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Обозначим семиголовых через x. Тогда пятиголовых 2x, а шестиголовых 62 − 3x. Считаем головы: 7x + 5 · 2x + 6 · (62 − 3x) = 372 − x. По условию голов 355, значит x = 17. Дальше всё находится само: 34 пятиголовых и 11 шестиголовых.</div>
        </div>`;
    }
  });

  /* ── 11. ДРАКОНЫ: вводим три числа ── */
  ШАГИ.push({
    т:'Стая драконов',
    рисунок(){
      const п=память(), о=п.ответы[4]||{}, верно=драконыВерны(о);
      let s = `<rect x="8" y="8" width="320" height="186" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="186" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, верно?'стая разгадана':'сколько было драконов каждого вида?',14, верно?ЗЕЛЁН:ЛАМПА,true)}`;
      const виды=[
        {имя:'7-головых', ключ:'семь', голов:7, ц:ЗЕЛЁН, x:64},
        {имя:'5-головых', ключ:'пять', голов:5, ц:СИНЬ, x:168},
        {имя:'6-головых', ключ:'шесть', голов:6, ц:ЗОЛОТО, x:272}
      ];
      виды.forEach((в,i)=>{
        const сколько=Number(о[в.ключ])||0;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 340ms ease-out ${(0.15+i*0.25).toFixed(2)}s 1 forwards">
                ${дракон(в.x,104,44,Math.min(7,в.голов),в.ц)}
                ${т(в.x,124,в.имя,12,в.ц,true)}
                ${жетон(в.x,146, сколько?String(сколько):'?', сколько?в.ц:ТИХО,16)}
              </g>`;
      });
      s += `${т(168,176, верно?'17 · 7 + 34 · 5 + 11 · 6 = 355':'всего 62 дракона и 355 голов',12,ТИХО)}`;
      return svg(s,200);
    },
    низ(){
      const п=память(), о=п.ответы[4]||{}, проверено=п.проверено[4], верно=драконыВерны(о);
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[4].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[4].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${проверено?'':ДОМ+'-ждёт'}" style="padding:12px 14px">
            <div style="display:flex;gap:10px;flex-wrap:wrap">` +
          [['семь','7-головых'],['пять','5-головых'],['шесть','6-головых']].map(([кл,имя])=>
            `<label style="font-size:14px;color:var(--muted)">${имя}
               <input id="${ДОМ}-${кл}" type="number" inputmode="numeric" value="${esc(о[кл]??'')}"
                 style="min-height:48px;width:96px;margin-left:8px;font-size:18px;padding:0 10px;border-radius:10px;
                        border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8"></label>`).join('') +
          `</div>
            <div style="margin-top:12px"><button class="btn" style="min-height:48px;margin:0"
              onclick="Т704.драконы()">Проверить</button></div>
            ${проверено?`<div style="font-size:16px;color:${КРАСН};margin-top:10px">Не сходится. Проверь: драконов 62, голов 355.</div>`:''}
          </div>`;
      } else {
        блок += разбор(ЗАДАЧИ[4].разбор, ЗАДАЧИ[4].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Смотреть итог →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 12. ИТОГ ── */
  ШАГИ.push({
    т:'Итог',
    рисунок(){
      const балл=баллТура(), процент=Math.round(балл/10*100);
      const ц=балл>=8?ЗЕЛЁН:(балл>=5?ЗОЛОТО:КРАСН);
      const дела=[
        {имя:'замок', ок:суммаВерна(память().ответы[0])},
        {имя:'сундуки', ок:сундукиВерны(память().ответы[1]||{})},
        {имя:'костёр', ок:костёрВерен(память().ответы[2]||{})},
        {имя:'грибы', ок:Number(память().ответы[3])===ЗАДАЧИ[3].ответ},
        {имя:'драконы', ок:драконыВерны(память().ответы[4]||{})}
      ];
      let s = `<rect x="8" y="8" width="320" height="186" rx="12" fill="#101c14"/>`;
      s += `<rect x="8" y="8" width="320" height="186" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g>
              <circle cx="168" cy="62" r="38" fill="rgba(255,255,255,.04)" stroke="${ц}" stroke-width="1.6"/>
              <circle cx="168" cy="62" r="44" fill="none" stroke="${ц}" stroke-width="1.2"
                stroke-dasharray="9 7" opacity=".5">
                <animateTransform attributeName="transform" type="rotate"
                  from="0 168 62" to="360 168 62" dur="18s" repeatCount="indefinite"/>
              </circle>
              <text id="${ДОМ}-балл" x="168" y="70" text-anchor="middle" font-size="32"
                font-weight="bold" fill="${ц}" font-family="Georgia,serif"
                style="font-variant-numeric:tabular-nums">0</text>
            </g>`;
      s += `${т(168,122,'выполнено '+процент+' %',14,ПЕРО)}`;
      дела.forEach((д,i)=>{
        const x=56+i*56;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 320ms ease-out ${(0.2+i*0.12).toFixed(2)}s 1 forwards">
                ${рамка(x-24,140,48,26, д.ок?'rgba(143,209,168,.14)':'rgba(232,106,90,.12)',
                        д.ок?ЗЕЛЁН:КРАСН,8,1.4)}
                ${т(x,157,д.ок?'✓':'✕',14,д.ок?ЗЕЛЁН:КРАСН,true)}
                ${т(x,178,д.имя,12,ТИХО)}
              </g>`;
      });
      s += `<rect x="48" y="186" width="240" height="8" rx="4" fill="rgba(255,255,255,.06)"/>`;
      s += `<rect x="48" y="186" width="0" height="8" rx="4" fill="${ц}">
              <animate attributeName="width" from="0" to="${Math.round(240*балл/10)}" dur="900ms"
                begin="0.1s" fill="freeze"/></rect>`;
      return svg(s,202);
    },
    низ(){
      const балл=баллТура();
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Тур пройден. Что мы делали: оценивали сумму по разрядам, переворачивали врущие надписи, считали круги делением с остатком, сводили дележ к делимости и заменяли три неизвестных одним.</div>
          <div style="font-size:16px;line-height:1.5;margin-top:8px">${балл>=8?'Отличный результат.':(балл>=5?'Хорошо, но есть что разобрать.':'Вернись к разборам и попробуй снова.')}</div>
        </div>
        <div class="btnrow"><button class="btn ghost" onclick="go('olymp')">← К списку туров</button>
        <button class="btn ok2" onclick="go('tour')">В раздел «Тур»</button></div>`;
    }
  });

  /* ── проверки ответов ── */
  function суммаВерна(ответ){
    const ч=String(ответ||'').replace(',','.').replace(/\s+/g,'');
    return Math.abs(parseFloat(ч) - ЗАДАЧИ[0].ответ) < 0.001;
  }
  function сундукиВерны(где){
    const о=ЗАДАЧИ[1].ответ;
    const в=(имя,ключ)=>{
      const внутри=ЗАДАЧИ[1].предметы.filter(им=>(где[им]||1)===имя);
      return внутри.length===о[ключ].length && внутри.every(им=>о[ключ].indexOf(им)>=0);
    };
    return в(1,'один') && в(2,'два') && в(3,'три');
  }
  function костёрВерен(о){
    return Number(о.ребят)===ЗАДАЧИ[2].ответ.ребят && Number(о.круг)===ЗАДАЧИ[2].ответ.круг;
  }
  function драконыВерны(о){
    const а=ЗАДАЧИ[4].ответ;
    return ['семь','пять','шесть'].every(к=>Number(о[к])===а[к]);
  }

  /* ── состояние ответов ── */
  function память(){
    const к = (typeof lidKey === 'function') ? lidKey(ID) : String(ID);
    if(typeof CHS === 'undefined') window.CHS = {};
    if(!CHS[к]) CHS[к] = {};
    const п = CHS[к];
    if(!п.ответы) п.ответы = {};
    if(!п.проверено) п.проверено = {};
    return п;
  }
  const перерисовать = () => { if(typeof chRender === 'function') chRender(0); };

  function баллТура(){
    const п=память(); let б=0;
    if(суммаВерна(п.ответы[0])) б+=2;
    if(сундукиВерны(п.ответы[1]||{})) б+=2;
    if(костёрВерен(п.ответы[2]||{})) б+=2;
    if(Number(п.ответы[3])===ЗАДАЧИ[3].ответ) б+=2;
    if(драконыВерны(п.ответы[4]||{})) б+=2;
    return б;
  }

  function счётДо(ид, до, мс){
    try{
      const el=document.getElementById(ид); if(!el) return;
      const t0=performance.now(), длит=мс||700;
      const шаг=()=>{
        const k=Math.min(1,(performance.now()-t0)/длит);
        el.textContent=Math.round(до*(1-Math.pow(1-k,3)));
        if(k<1) requestAnimationFrame(шаг); else el.textContent=до;
      };
      requestAnimationFrame(шаг);
    }catch(e){}
  }

  /* ── действия руками ── */
  window.Т704 = {
    сумма(){
      const п=память(), el=document.getElementById(ДОМ+'-сумма');
      п.ответы[0] = el ? el.value.trim() : '';
      п.проверено[0] = true;
      перерисовать();
    },
    положить(имя){
      const п=память();
      const где = п.ответы[1] = п.ответы[1] || {};
      const сейчас = где[имя] || 1;
      где[имя] = сейчас>=3 ? 1 : сейчас+1;
      перерисовать();
    },
    костёр(){
      const п=память();
      const а=document.getElementById(ДОМ+'-ребят'), б2=document.getElementById(ДОМ+'-круг');
      п.ответы[2] = { ребят: а?а.value.trim():'', круг: б2?б2.value.trim():'' };
      п.проверено[2] = true;
      перерисовать();
    },
    детей(){
      const п=память(), el=document.getElementById(ДОМ+'-детей');
      п.ответы[3] = el ? el.value.trim() : '';
      п.проверено[3] = true;
      перерисовать();
    },
    драконы(){
      const п=память(), о={};
      ['семь','пять','шесть'].forEach(к=>{
        const el=document.getElementById(ДОМ+'-'+к);
        о[к] = el ? el.value.trim() : '';
      });
      п.ответы[4]=о; п.проверено[4]=true;
      перерисовать();
    }
  };

  /* Список раздела «Тур» строится из ARH_TOURS: без записи тур был бы
     в каталоге, но в списке туров его не видно. */
  (function(){
    window.ARH_TOURS = window.ARH_TOURS || [];
    if(window.ARH_TOURS.some(t=>t && t.id===ID)) return;
    window.ARH_TOURS.push({
      id: ID, klass: 6, subject: 'math',
      title: 'Математика · 6 класс · «Пять этапов большой игры»',
      макс: 10, время: 45,
      items: [1,2,3,4,5].map(i=>({ n:i })),
      hand: true
    });
  })();

  /* ══════════════════════ РЕГИСТРАЦИЯ ══════════════════════ */
  const ТЕКУЩИЙ = () => Math.max(0, Math.min(ШАГИ.length-1,
                     (typeof LV !== 'undefined' && LV && LV.step) || 0));

  function стиль(){
    if(document.getElementById(ДОМ+'-стиль')) return;
    const s=document.createElement('style');
    s.id=ДОМ+'-стиль';
    s.textContent = `
      @keyframes ${ДОМ}-появление{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
      @keyframes ${ДОМ}-пульс{0%,100%{opacity:.45}50%{opacity:1}}
      @keyframes ${ДОМ}-строка{0%{opacity:1}18%{opacity:1}21%{opacity:0}100%{opacity:0}}
      @keyframes ${ДОМ}-фрукт{from{opacity:0}to{opacity:1}}
      .${ДОМ}{display:flex;flex-direction:column;align-items:stretch;gap:12px;
        font-family:Georgia,'Times New Roman',serif;-webkit-font-smoothing:antialiased}
      .${ДОМ}-шапка{text-align:center;font-size:14px;color:var(--muted,#cbb89a);
        letter-spacing:.02em;font-variant-numeric:tabular-nums}
      .${ДОМ}-шапка b{color:#ffd76a;font-weight:600}
      .${ДОМ}-сцена{display:flex;justify-content:center;width:100%}
      .${ДОМ}-сцена svg{display:block;width:100%;max-width:420px;height:auto;margin:0 auto}
      .${ДОМ}-низ{display:flex;flex-direction:column;gap:12px;
        animation:${ДОМ}-появление 320ms cubic-bezier(.23,1,.32,1) both}
      .${ДОМ} .card{text-align:left}
      .${ДОМ}-ждёт{animation:${ДОМ}-пульс 1.6s ease-in-out infinite}
      .${ДОМ} button{transition:transform 140ms cubic-bezier(.23,1,.32,1),
        border-color 140ms ease,background-color 140ms ease}
      .${ДОМ} button:active{transform:translateY(2px) scale(.985)}
      .${ДОМ} button:focus-visible{outline:3px solid #ffd76a;outline-offset:2px}
      @media (min-width: 700px){ .${ДОМ}{max-width:560px;margin:0 auto} }
      @media (prefers-reduced-motion: reduce){
        .${ДОМ} *, #lvis svg *{animation:none!important;transition:none!important}
        .${ДОМ}-речь{opacity:0}
        .${ДОМ}-речь-первая{opacity:1}
      }`;
    document.head.appendChild(s);
  }

  function рисовать(el){
    if(!el) return;
    стиль();
    const i = ТЕКУЩИЙ(), шаг = ШАГИ[i];
    if(!шаг){ el.innerHTML=''; return; }
    try{
      el.innerHTML = `<div class="${ДОМ}" style="animation:${ДОМ}-появление 280ms cubic-bezier(.23,1,.32,1) both">
          <div class="${ДОМ}-шапка">Шаг <b>${i+1}</b> из ${ШАГИ.length} · ${esc(шаг.т)}</div>
          ${шаг.рисунок ? `<div class="${ДОМ}-сцена">${шаг.рисунок()}</div>` : ''}
          <div class="${ДОМ}-низ">${шаг.низ ? шаг.низ() : ''}</div>
        </div>`;
      if(i === ШАГИ.length-1) счётДо(ДОМ+'-балл', баллТура(), 800);
    }catch(e){ el.innerHTML=''; try{ console.error('тур 704:', e); }catch(_){} }
  }

  const ЛЕНТА = ШАГИ.map((ш,i)=> (i+1)+'. '+ш.т);

  window.WAVE_B = window.WAVE_B || {};
  window.WAVE_B[ID] = function(el){ рисовать(el); };

  (function(){
    const L = {
      id: ID, title:'Пять этапов большой игры', ico:'🏕',
      src:'Олимпиадные туры · Математика · 6 класс', subj:'tour',
      explain: ЛЕНТА,
      check: { q:'Сколько баллов можно получить за этот тур?',
               choices:['10','5','8','12'], ans:0,
               exp:'Пять задач по 2 балла — всего 10 баллов.' },
      tasks: []
    };
    const поставить = arr => {
      if(!arr || typeof arr.length!=='number') return arr;
      for(let i=0;i<arr.length;i++){ if(arr[i] && arr[i].id===ID){ arr[i]=L; return arr; } }
      arr.push(L);
      return arr;
    };
    let сейчас = поставить(window.ARH_LESSONS);
    try{
      Object.defineProperty(window,'ARH_LESSONS',{
        configurable:true, enumerable:true,
        get(){ return сейчас; },
        set(v){ сейчас = поставить(v); }
      });
    }catch(e){}
  })();
})();
