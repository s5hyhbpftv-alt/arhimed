/* ====== МАТЕМАТИКА · УРОК 387 · «ПРОЦЕНТЫ: ОЛИМПИАДНЫЕ ЗАДАЧИ» ===================
   5–6 класс, олимпиадная тема «проценты». Переделан с нуля по эталону 1022
   (deploy/ЭТАЛОН_УРОКА.md), рисунки — сцена из сюжета. Прежние версии
   (vis_bw.js visB387, lessons_draft.js) остаются в общих файлах; этот файл
   регистрируется поверх.

   ГЛАВНАЯ МЫСЛЬ. Процент — сотая часть ТОГО, от чего его считают. Всякий раз
   спрашивай: «процент от чего?» Изменение на p% — это умножение на
   (1 ± p/100), поэтому проценты подряд не складываются, а перемножаются.

   СЮЖЕТ. На агоре Сиракуз торгует карфагенский купец Ганнон — пурпурные
   ткани, масло, смоквы. Его «выгодные» скидки и честные на вид расчёты
   обманывают покупателей. Ученик Архимеда разоблачает три обмана.

   РУКАМИ: «+p%, потом −p%» кнопками (10, 20, 50) — стопки серебра
   100 → 100 + p → меньше 100; возврат цены: кнопки «−15%», «−20%», «−25%»
   после подъёма на 25%.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ:
     +10% и −10%: 100 → 110 → 99; +20/−20: 120 → 96; +50/−50: 150 → 75
       (потеря p²/100); в обратном порядке то же: 100 · 0,9 · 1,1 = 99;
     скидки 20% и 30% на 200: 200 → 160 → 112 (0,8 · 0,7 = 0,56 — скидка 44%);
     20 и 25 амфор: у Диона на 5/20 = 25% больше, у Ганнона на 5/25 = 20% меньше;
     +25% → 125; вернуть к 100 — снизить на 25/125 = 20% (−15%: 106,25;
       −25%: 93,75);
     смоквы 100 кг, вода 99% → сухого 1 кг; вода 98% → 1 кг = 2% → 50 кг;
     корона 1 кг, золота 80% → серебра 0,2 кг; 90% золота → серебро 10% →
       всего 2 кг → добавить 1 кг золота;
     40% кораблей с зерном, из них 25% — сицилийское: 0,4 · 0,25 = 10% (2 из 20).

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md; в каждом кадре есть <animate>. */
(function(){
  'use strict';

  const ID = 387;
  const GOLD='#ffd76a', GREEN='#8fe0b0', RED='#ff8a78', ЛАЗУРЬ='#8fd0f0', ПУРПУР='#c070d0';
  const ИНК='#f5efe2', МУТ='#b8bfcf', ЛИНИЯ='#4d5a80', ОБВОД='#0c0a08';

  const ДЕЛА = [
    {ключ:'цена',    имя:'Поймать «+10% и −10%»',  итог:'99'},
    {ключ:'скидки',  имя:'Раскрыть «скидку 50%»',   итог:'112'},
    {ключ:'смоквы',  имя:'Взвесить сушёные смоквы', итог:'50 кг'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const CSS=`
  #lvis .s6.l387{gap:14px}
  #lvis .s6.l387 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l387 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l387 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#1f2a44,#131a2e);border:1.5px solid var(--line)}
  #lvis .s6.l387 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l387 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l387 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l387 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l387 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l387 .карт .текст b{color:${GOLD}}
  #lvis .s6.l387 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l387 .правило b{color:${GOLD}}
  #lvis .s6.l387 .лист{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(192,112,208,.16),rgba(192,112,208,.04));
    border:1.5px solid rgba(192,112,208,.55);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l387 .лист .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l387 .лист .шапка b{font-size:20px;letter-spacing:0;text-transform:none;color:${ПУРПУР}}
  #lvis .s6.l387 .лист .шапка b.готово{color:${GREEN}}
  #lvis .s6.l387 .лист ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l387 .лист li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l387 .лист li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;
    border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l387 .лист li.есть{color:${ИНК}}
  #lvis .s6.l387 .лист li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,224,176,.14)}
  #lvis .s6.l387 .лист li span{margin-left:auto;white-space:nowrap;font-variant-numeric:tabular-nums;color:var(--mut);font-size:14px}
  #lvis .s6.l387 .лист li.есть span{color:${GREEN}}
  #lvis .s6.l387 .ряд{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;width:100%}
  #lvis .s6.l387 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:18px;font-weight:700;
    border:1.5px solid rgba(192,112,208,.55);background:rgba(192,112,208,.13);color:${ИНК};
    font-variant-numeric:tabular-nums;white-space:nowrap;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l387 .ряд button:active{transform:translateY(2px);background:rgba(192,112,208,.28)}
  #lvis .s6.l387 .ряд button.вкл{border-color:${GOLD};background:rgba(255,215,106,.18)}
  #lvis .s6.l387 .ряд button.hit{border-color:var(--ok)}
  #lvis .s6.l387 .ряд button.miss{border-color:var(--no)}
  #lvis .s6.l387 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l387 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l387 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l387 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l387 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l387 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-variant-numeric:tabular-nums;font-size:18px}
  #lvis .s6.l387 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-variant-numeric:tabular-nums;font-size:17px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l387 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l387 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l387 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l387 .уровни .точка.сейчас{background:${GOLD};animation:l387dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l387dot{0%,100%{opacity:1}50%{opacity:.6}}
  #lvis .s6.l387 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l387{-webkit-text-size-adjust:100%}
  #lvis .s6.l387 [data-anim]{animation:l387rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l387rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l387 [data-anim]{animation:none!important}
    #lvis .s6.l387 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l387 button{transition:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l387-style');
      if(!s){ s=document.createElement('style'); s.id='l387-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,выкл) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}"${выкл?' disabled':''}>${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Агора Сиракуз</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,обр) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, обр+"("+к+")")).join('')}</div>`;
  const ЛИСТ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'лист',
      `<div class="шапка"><span>Обманы Ганнона</span><b class="${всё?'готово':''}">${
        всё?'купец разоблачён':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}
           <span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };
  const число = (x) => String(Math.round(x*100)/100).replace('.',',');

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const анСдвиг = (значения,длит,keyTimes) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}"
              repeatCount="indefinite" calcMode="spline" keyTimes="${keyTimes}"
              keySplines="${сплайны(keyTimes.split(';').length-1)}"/>${ЗАВОД}` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  /* вырастает снизу вверх и держится */
  const вырасти = (длит,доля) => анК('opacity','0.2;0.2;1;1',длит,'0;'+кт(доля)+';'+кт(доля+0.06)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c387-небо" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#3f78c0"/><stop offset="0.7" stop-color="#9cc8e8"/><stop offset="1" stop-color="#f0dcb0"/>
      </linearGradient>
      <linearGradient id="c387-вечер" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2a2450"/><stop offset="0.6" stop-color="#7a4a6a"/><stop offset="1" stop-color="#e09a60"/>
      </linearGradient>
      <linearGradient id="c387-площадь" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#d8c498"/><stop offset="1" stop-color="#a88c5c"/>
      </linearGradient>
      <linearGradient id="c387-мрамор" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#c8bca0"/><stop offset="0.5" stop-color="#f4ecd8"/><stop offset="1" stop-color="#b8ac90"/>
      </linearGradient>
      <linearGradient id="c387-серебро" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#8a92a0"/><stop offset="0.4" stop-color="#f0f4fa"/><stop offset="0.7" stop-color="#c0c8d4"/><stop offset="1" stop-color="#6a7280"/>
      </linearGradient>
      <linearGradient id="c387-золото" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0" stop-color="#fff0b0"/><stop offset="0.45" stop-color="#e0b040"/><stop offset="1" stop-color="#8a6010"/>
      </linearGradient>
      <linearGradient id="c387-пурпур" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#d080e0"/><stop offset="0.5" stop-color="#8a2a9a"/><stop offset="1" stop-color="#4a0c5a"/>
      </linearGradient>
      <linearGradient id="c387-вода" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#9fe6ff"/><stop offset="1" stop-color="#2a86c0"/>
      </linearGradient>
      <linearGradient id="c387-дерево" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#9a6536"/><stop offset="1" stop-color="#4a2a12"/>
      </linearGradient>
      <linearGradient id="c387-глина" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#7a3a1c"/><stop offset="0.35" stop-color="#d0804c"/><stop offset="0.65" stop-color="#b0602e"/><stop offset="1" stop-color="#5a2610"/>
      </linearGradient>
      <radialGradient id="c387-солнце" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#fff8d8"/><stop offset="0.45" stop-color="#ffe08a" stop-opacity=".7"/><stop offset="1" stop-color="#ffc060" stop-opacity="0"/>
      </radialGradient>
      <filter id="c387-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="0" dy="2" stdDeviation="1.8" flood-color="#000" flood-opacity=".5"/>
      </filter>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.66+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c387-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(12,10,8,.9)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  /* агора: небо, портик с колоннами, мостовая */
  const агора = (Н,вечер) => `
    <rect x="0" y="0" width="336" height="${Н}" fill="url(#c387-${вечер?'вечер':'небо'})"/>
    <circle cx="${вечер?60:280}" cy="${вечер?130:46}" r="40" fill="url(#c387-солнце)" data-декор="1">${анЛин('r','38;44;38','6s')}</circle>
    <path d="M20 96 L168 60 L316 96 Z" fill="#e8dcc0" opacity=".85"/>
    <rect x="24" y="96" width="288" height="8" fill="#d8ccb0" opacity=".85"/>
    ${Array.from({length:8},(_,i)=>`<rect x="${34+i*38}" y="104" width="12" height="${Н-150}" fill="url(#c387-мрамор)" opacity=".75" data-декор="1"/>`).join('')}
    <rect x="0" y="${Н-46}" width="336" height="46" fill="url(#c387-площадь)"/>
    ${Array.from({length:12},(_,i)=>`<line x1="${i*30}" y1="${Н-46}" x2="${i*30-20}" y2="${Н}" stroke="#8a7050" stroke-width=".8" opacity=".5"/>`).join('')}`;
  /* затемнённая доска для чисел поверх сцены */
  const доска = (x,y,w,h) => `<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="12" fill="rgba(14,12,20,.78)" stroke="rgba(255,255,255,.12)"/>`;
  /* купец Ганнон: пурпурный хитон, чёрная борода, высокая шапка */
  const купец = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})" filter="url(#c387-тень)">
    <path d="M-20 60 q2 -44 20 -48 q18 4 20 48 z" fill="url(#c387-пурпур)"/>
    <path d="M-14 22 q14 8 28 0" stroke="${GOLD}" stroke-width="2" fill="none"/>
    <circle cx="0" cy="0" r="12" fill="#c89068"/>
    <path d="M-10 4 q10 18 20 0 q-2 8 -10 12 q-8 -4 -10 -12 z" fill="#1a1010"/>
    <path d="M-11 -6 q11 -24 22 0 z" fill="#5a1a6a"/><rect x="-12" y="-8" width="24" height="4" rx="2" fill="${GOLD}"/>
    <circle cx="-4" cy="-1" r="1.5" fill="#1a1010"/><circle cx="4" cy="-1" r="1.5" fill="#1a1010"/>
    <path d="M18 26 q12 -10 16 -24" stroke="#c89068" stroke-width="5" stroke-linecap="round" fill="none">
      ${анЛин('d','M18 26 q12 -10 16 -24;M18 26 q14 -6 20 -18;M18 26 q12 -10 16 -24','2.4s')}</path></g>`;
  /* стопка серебряных драхм: высота пропорциональна сумме */
  const стопка = (x,база,сумма,к,подп,цвет) => {
    const h=сумма*к, n=Math.max(1,Math.round(h/6));
    return `<g filter="url(#c387-тень)">
      <rect x="${x-20}" y="${(база-h).toFixed(1)}" width="40" height="${h.toFixed(1)}" fill="url(#c387-серебро)"/>
      ${Array.from({length:n},(_,i)=>`<line x1="${x-20}" y1="${(база-i*h/n).toFixed(1)}" x2="${x+20}" y2="${(база-i*h/n).toFixed(1)}" stroke="#6a7280" stroke-width=".7"/>`).join('')}
      <ellipse cx="${x}" cy="${(база-h).toFixed(1)}" rx="20" ry="5" fill="#f4f8ff" stroke="#8a92a0" stroke-width=".8"/>
      ${т(x,(база-h-12).toFixed(1),подп,16,цвет||ИНК,true,undefined,'#1a1a24')}</g>`;
  };
  const стрелка = (x1,x2,y,подп,цвет) => `<g>
    <path d="M${x1} ${y} Q${(x1+x2)/2} ${y-20} ${x2} ${y}" fill="none" stroke="${цвет}" stroke-width="2" stroke-dasharray="5 4">${анЛин('stroke-dashoffset','0;-18','1s')}</path>
    <path d="M${x2} ${y} l-8 -4 l1 8 z" fill="${цвет}"/>
    ${т((x1+x2)/2,y-22,подп,13,цвет,true,undefined,'#1a1a24')}</g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Ганнон на агоре */
  function F1(s){
    const Н=280;
    return ЛИСТ(s) +
      ЗАДАЧА('На агору Сиракуз приплыл карфагенский купец <b>Ганнон</b>: пурпурные ткани, масло, сушёные смоквы. Он кричит о «небывалых скидках», а покупатели почему-то уходят без денег. Архимед шепчет ученику: «Он честно считает проценты. Только <b>от разных чисел</b>».') +
      `<div class="pic">${свг(`
        ${агора(Н)}
        <g filter="url(#c387-тень)">
          <rect x="60" y="150" width="200" height="60" fill="url(#c387-дерево)"/>
          ${Array.from({length:8},(_,i)=>`<path d="M${56+i*26} 118 h26 v18 q-13 10 -26 0 z" fill="${i%2?'#f4ecd8':'#8a2a9a'}">${анЛин('d',`M${56+i*26} 118 h26 v18 q-13 10 -26 0 z;M${56+i*26} 118 h26 v20 q-13 8 -26 0 z;M${56+i*26} 118 h26 v18 q-13 10 -26 0 z`,(2+i*0.1).toFixed(1)+'s')}</path>`).join('')}
          <rect x="58" y="112" width="210" height="8" fill="#5a3a1a"/>
          <rect x="74" y="136" width="44" height="18" rx="3" fill="url(#c387-пурпур)"/><rect x="122" y="140" width="40" height="14" rx="3" fill="#a040b0"/>
          ${[190,212,234].map(x=>`<g transform="translate(${x} 150)"><path d="M-6 -24 h12 l2 6 q8 4 8 14 q0 6 -8 10 h-16 q-8 -4 -8 -10 q0 -10 8 -14 z" fill="url(#c387-глина)"/></g>`).join('')}</g>
        ${купец(40,176,1)}
        <g filter="url(#c387-тень)" transform="rotate(-4 290 150)"><rect x="262" y="128" width="58" height="46" rx="4" fill="#f4ecd8" stroke="#6a4a2a"/>
          ${т(291,148,'−50%',16,'#a02a1a',true)}${т(291,166,'скидка!',11,'#3a2410',true)}</g>
        ${подпись(168,30,'1% — сотая часть',GOLD,14)}
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Процент','<b>1%</b> — сотая часть величины. 10% от 300 драхм — это 300 : 100 · 10 = 30 драхм. Но «10%» само по себе ничего не значит: всегда спрашивай, <b>от чего</b>.') +
      ПРАВИЛО('<b>p% от числа</b> = число · p : 100. Первый вопрос: процент <b>от чего</b>?');
  }

  /* 2. +p% и −p% */
  function F2(s){
    const Н=300, p=s.p2||10, в=s.ответ2;
    const a=100, b=100+p, c=b*(1-p/100), база=Н-54, к=1.05;
    const разборы = ['100 было бы, если бы оба процента считали от 100. Но скидку считают от <b>новой</b> цены 110: 10% от 110 = 11, а 110 − 11 = 99.',
      '<b>100 → 110 → 99</b>. Надбавку считали от 100, а скидку — от 110. Минус вышел больше плюса.',
      '90 — это «−10%» от 100 без надбавки. А сначала цена стала 110, и 10% от 110 = 11: итог 99.'];
    return ЛИСТ(s) +
      ЗАДАЧА('Первый трюк Ганнона: «Утром подниму цену ткани на 10%, а вечером сброшу на 10% — выйдет та же цена, что и была!» Цена сейчас <b>100 драхм</b>. Меняй p кнопками и смотри на стопки серебра.') +
      `<div class="pic">${свг(`
        ${агора(Н)}
        ${доска(14,40,308,Н-84)}
        <line x1="24" y1="${база-a*к}" x2="312" y2="${база-a*к}" stroke="${GOLD}" stroke-width="1.4" stroke-dasharray="5 5" opacity=".8"/>
        <g>${вырасти('6s',0.02)}${стопка(62,база,a,к,'100')}</g>
        <g>${вырасти('6s',0.22)}${стопка(168,база,b,к,число(b),ЛАЗУРЬ)}</g>
        <g>${вырасти('6s',0.42)}${стопка(274,база,c,к,число(c),c<100?RED:ИНК)}</g>
        ${стрелка(84,146,база-b*к-36,'+'+p+'%',ЛАЗУРЬ)}
        ${стрелка(190,252,база-b*к-36,'−'+p+'%',RED)}
        ${подпись(168,Н-10, 'потеря '+число(p*p/100)+' драхм: −'+p+'% считают от '+число(b), GOLD,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="ряд">${[10,20,50].map(x=>BTN(3,x===p?'вкл':'','p = '+x,"r387P("+x+")")).join('')}</div>` +
      ОТВЕТЫ('три',['100','99','90'],1,в,'r387Отв2') +
      (в==null ? СКАЗ('Вопрос','При p = 10: какой станет цена вечером?') : РАЗБОР(в===1, разборы[в])) +
      (в===1 ? ПРАВИЛО('Процент считают от <b>текущей</b> величины. После надбавки база уже другая.') : '');
  }

  /* 3. Порядок не важен */
  function F3(s){
    const Н=240, в=s.ответ3;
    return ЛИСТ(s) +
      ЗАДАЧА('Ганнон выкручивается: «Ладно! Тогда сначала <b>сброшу на 10%</b>, а потом <b>подниму на 10%</b> — уж так-то вам выгоднее!» Правда?') +
      `<div class="pic">${свг(`
        ${агора(Н,true)}
        ${доска(14,40,308,Н-86)}
        ${т(168,74,'+10%, потом −10%:',14,МУТ,true)}
        ${т(168,100,'100 · 1,1 · 0,9 = 99',18,ИНК,true)}
        ${т(168,136,'−10%, потом +10%:',14,МУТ,true)}
        ${т(168,162,'100 · 0,9 · 1,1 = '+(в===2?'99':'?'),18,в===2?GREEN:ИНК,true)}
        <g>${анЛин('opacity','1;0.5;1','1.4s')}<line x1="60" y1="176" x2="276" y2="176" stroke="${в===2?GREEN:GOLD}" stroke-width="1.6" stroke-dasharray="6 5"/></g>
        ${подпись(168,Н-12, в===2?'от перестановки множителей…':'+10% — это ×1,1; −10% — это ×0,9', в===2?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Да, так выгоднее покупателю','Нет, так выгоднее купцу','Всё равно: в обоих случаях 99'],2,в,'r387Отв3') +
      (в==null ? СКАЗ('Вопрос','Какой порядок выгоднее?') : РАЗБОР(в===2, ['100 → 90 → 99: тот же результат. Надбавку теперь считают от 90, она всего 9 драхм.','Купцу не выгоднее и не хуже — обе цепочки дают <b>99</b>.','Изменение на p% — это умножение. 1,1 · 0,9 = 0,9 · 1,1 = 0,99: порядок <b>не важен</b>.'][в])) +
      (в===2 ? ПРАВИЛО('+p% — это <b>× (1 + p/100)</b>, −p% — это <b>× (1 − p/100)</b>. Множители можно переставлять.') : '');
  }

  /* 4. Две скидки */
  function F4(s){
    const Н=280, в=s.ответ4;
    const к=1.2, x0=44, ясно=в===1;
    const полоса = (y,длина,подп,цвет,доля) => `<g>${вырасти('7s',доля)}
      <rect x="${x0}" y="${y}" width="${(длина*к).toFixed(1)}" height="26" rx="4" fill="url(#c387-пурпур)" filter="url(#c387-тень)"/>
      ${Array.from({length:Math.floor(длина*к/14)},(_,i)=>`<line x1="${x0+8+i*14}" y1="${y+2}" x2="${x0+2+i*14}" y2="${y+24}" stroke="#e0a0f0" stroke-width=".8" opacity=".5"/>`).join('')}
      ${т(x0,y-6,подп,14,цвет,true,'start','#1a1a24')}</g>`;
    return ЛИСТ(s) +
      ЗАДАЧА('Второй трюк. Отрез пурпурной ткани — <b>200 драхм</b>. Ганнон: «Сегодня скидка <b>20%</b>, а для вас — ещё <b>30%</b> сверху! Итого <b>50%</b>, платите 100!» Сколько на самом деле стоит ткань после двух скидок?') +
      `<div class="pic">${свг(`
        ${агора(Н)}
        ${доска(14,40,308,Н-84)}
        ${полоса(70,200,'было 200',ИНК,0.02)}
        ${полоса(124,160,'−20% → 160',ЛАЗУРЬ,0.25)}
        ${полоса(178,ясно?112:100,ясно?'−30% → 112':'−30% → ?',ясно?GREEN:GOLD,0.5)}
        ${т(168,228,ясно?'200 · 0,8 · 0,7 = 112':'30% — от какой цены?',14,ясно?GREEN:ИНК,true)}
        ${подпись(168,Н-12, ясно?'скидка 44%, а не 50%':'вторая скидка — от 160', ясно?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['100','112','120'],1,в,'r387Отв4') +
      (в==null ? СКАЗ('Вопрос','Сколько платить?') : РАЗБОР(в===1, ['100 было бы при скидке 50% от 200. Но 30% считают от <b>160</b>: 48 драхм. 160 − 48 = <b>112</b>.','200 → 160 (−40) → 112 (−48). Настоящая скидка: 88 из 200 — <b>44%</b>. Проценты подряд не складываются.','120 = 200 − 80: это сложили 20 + 20. Вторая скидка 30% от 160 = 48: цена 112.'][в])) +
      (в===1 ? ПРАВИЛО('Скидки подряд <b>перемножаются</b>: 0,8 · 0,7 = 0,56 — это скидка 44%.') : '');
  }

  /* 5. Больше / меньше */
  function F5(s){
    const Н=260, в=s.ответ5;
    const амф = (x,y,лишн) => `<path transform="translate(${x} ${y})" d="M-3 -10 h6 l1 2 q5 2 5 7 q0 6 -5 9 h-8 q-5 -3 -5 -9 q0 -5 5 -7 z" fill="${лишн?'#e0b040':'url(#c387-глина)'}" stroke="${ОБВОД}" stroke-width=".5"/>`;
    const блок = (x0,n,имя,цв) => Array.from({length:n},(_,i)=>{ const x=x0+(i%5)*20, y=90+Math.floor(i/5)*28, лишн=n===25&&i>=20;
        return лишн?`<g>${анЛин('opacity','1;0.45;1','1.4s')}${амф(x,y,true)}</g>`:амф(x,y,false); }).join('') +
      т(x0+40,72,имя+': '+n,14,цв,true,undefined,'#1a1a24');
    return ЛИСТ(s) +
      ЗАДАЧА('Сосед Ганнона, сиракузянин Дион, торгует маслом. У Ганнона <b>20 амфор</b>, у Диона <b>25</b>. Ганнон кричит: «У Диона всего на 20% больше!» Дион: «Нет, на 25%!» Оба уверены. Кто прав — и на сколько процентов у Ганнона <b>меньше</b>?') +
      `<div class="pic">${свг(`
        ${агора(Н)}
        ${доска(14,44,308,Н-88)}
        ${блок(40,20,'Ганнон',ПУРПУР)}
        ${блок(200,25,'Дион',GOLD)}
        ${подпись(168,Н-12, в===1?'5 от 20 — 25%, 5 от 25 — 20%':'разница — 5 амфор; от чего считать?', в===1?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['У Диона на 25% больше, у Ганнона на 25% меньше','У Диона на 25% больше, у Ганнона на 20% меньше','У Диона на 20% больше, у Ганнона на 25% меньше'],1,в,'r387Отв5') +
      (в==null ? СКАЗ('Вопрос','Выбери верную пару.') : РАЗБОР(в===1, ['«Больше» сравнивают с Ганноном: 5 из 20 = 25%. «Меньше» — с Дионом: 5 из 25 = <b>20%</b>. Проценты разные!','«На сколько больше» — от 20: 5/20 = <b>25%</b>. «На сколько меньше» — от 25: 5/25 = <b>20%</b>. Прав Дион.','Наоборот: больше — это 5 от 20 = 25%, меньше — 5 от 25 = 20%. Ганнон перепутал базу.'][в])) +
      (в===1 ? ПРАВИЛО('Процент считают от того, <b>с чем сравнивают</b>. «Больше на 25%» и «меньше на 20%» — одна и та же пара.') : '');
  }

  /* 6. Вернуть цену */
  function F6(s){
    const Н=290, в=s.ответ6, база=Н-54, к=1.15;
    const ВАР=[15,20,25], q=в==null?null:ВАР[в], итог=q==null?null:125*(1-q/100);
    const разборы = ['125 − 15% = 106,25: цена осталась выше прежней. Нужно снять 25 драхм из 125 — это <b>20%</b>.',
      '25 из 125 — ровно <b>20%</b>: 125 · 0,8 = 100. Цена вернулась.',
      '125 − 25% = 93,75 — стало дешевле, чем было. 25% считали от 100, а снижать надо от 125: <b>20%</b>.'];
    return ЛИСТ(s) +
      ЗАДАЧА('Ганнон поднял цену масла на <b>25%</b>: было 100 драхм, стало 125. Совет рынка требует вернуть прежнюю цену. Ганнон: «Снижу на 25% — и квиты!» На сколько процентов <b>на самом деле</b> нужно снизить? Выбери кнопкой.') +
      `<div class="pic">${свг(`
        ${агора(Н,true)}
        ${доска(14,40,308,Н-84)}
        <line x1="24" y1="${база-100*к}" x2="312" y2="${база-100*к}" stroke="${GOLD}" stroke-width="1.4" stroke-dasharray="5 5" opacity=".8"/>
        <g>${вырасти('6s',0.02)}${стопка(62,база,100,к,'100')}</g>
        <g>${вырасти('6s',0.22)}${стопка(168,база,125,к,'125',ЛАЗУРЬ)}</g>
        ${итог!=null?`<g>${вырасти('6s',0.42)}${стопка(274,база,итог,к,число(итог),итог===100?GREEN:RED)}</g>`:`${т(274,база-40,'?',30,GOLD,true)}`}
        ${стрелка(84,146,база-125*к-34,'+25%',ЛАЗУРЬ)}
        ${стрелка(190,252,база-125*к-34,q==null?'−?%':'−'+q+'%',RED)}
        ${подпись(168,Н-10, итог===100?'снизить на 20% — ровно 100':'снижение считают от 125', итог===100?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="ряд">${ВАР.map((x,i)=>BTN(3,в===i?(i===1?'hit':'miss'):'','−'+x+'%',"r387Отв6("+i+")")).join('')}</div>` +
      (в==null ? СКАЗ('Вопрос','На сколько процентов снизить, чтобы снова стало 100?') : РАЗБОР(в===1, разборы[в])) +
      (в===1 ? ПРАВИЛО('Подъём на 25% гасит снижение на <b>20%</b>: 1,25 · 0,8 = 1.') : '');
  }

  /* 7. Смоквы */
  function F7(s){
    const Н=290, в=s.ответ7, ясно=в===2;
    const сетка = (x0,y0,n,колонок) => Array.from({length:n},(_,i)=>{ const сух=i===n-1;
      return `<rect x="${x0+(i%колонок)*11}" y="${y0+Math.floor(i/колонок)*11}" width="10" height="10" rx="2" fill="${сух?'#8a5a2a':'url(#c387-вода)'}" ${сух?'stroke="#ffd76a" stroke-width="1.2"':''}/>`; }).join('');
    return ЛИСТ(s) +
      ЗАДАЧА('Третий трюк. Ганнон продаёт Диону <b>100 кг</b> свежих смокв: «В них <b>99%</b> воды». Смоквы день лежат на солнце, и воды в них становится <b>98%</b>. Ганнон: «Подумаешь, один процент — потеряли пару килограммов». Сколько весят смоквы теперь?') +
      `<div class="pic">${свг(`
        ${агора(Н)}
        ${доска(14,40,308,Н-84)}
        ${т(90,62,'утро: 100 кг',13,ИНК,true)}
        ${сетка(36,72,100,10)}
        ${т(90,196,'99 воды + 1 сухое',12,МУТ)}
        ${ясно
          ? `<g>${вырасти('5s',0.1)}${т(248,62,'вечер: 50 кг',13,GREEN,true)}${сетка(194,72,50,10)}${т(248,140,'49 воды + 1 сухое',12,МУТ)}${т(248,160,'1 кг — это 2%',12,GREEN,true)}</g>`
          : `${т(248,120,'?',40,GOLD,true)}${т(248,150,'98% воды',12,МУТ)}`}
        <g>${анЛин('opacity','1;0.4;1','1.2s')}<rect x="${36+9*11-1.5}" y="${72+9*11-1.5}" width="13" height="13" rx="3" fill="none" stroke="${GOLD}" stroke-width="1.6"/></g>
        ${подпись(168,Н-10, ясно?'сухая часть та же — 1 кг':'сухое не испаряется', ясно?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['99 кг','98 кг','50 кг'],2,в,'r387Отв7') +
      (в==null ? СКАЗ('Вопрос','Вес смокв вечером?') : РАЗБОР(в===2, ['99 кг — это вычли 1 кг. Но сухой части — 1 кг — было 1%, а стало <b>2%</b>. Раз 1 кг = 2%, всего <b>50 кг</b>.','«Один процент — один килограмм» — ошибка: проценты вечером считают от нового, меньшего веса. Сухое 1 кг = 2% → 50 кг.','Сухое вещество не испаряется: 1 кг. Вечером оно 2% веса: 1 кг · 50 = <b>50 кг</b>. Смоквы потеряли половину!'][в])) +
      (в===2 ? ПРАВИЛО('Ищи то, что <b>не меняется</b> (сухое вещество), и считай проценты от него.') : '');
  }

  /* 8. Корона */
  function F8(s){
    const Н=270, в=s.ответ8, ясно=в===2;
    const ст = (x,зол,сер,подп,доля) => { const к=58, hз=зол*к, hс=сер*к, низ=204;
      return `<g>${вырасти('6s',доля)}
        <rect x="${x-26}" y="${(низ-hз).toFixed(1)}" width="52" height="${hз.toFixed(1)}" rx="3" fill="url(#c387-золото)" filter="url(#c387-тень)"/>
        <rect x="${x-26}" y="${(низ-hз-hс).toFixed(1)}" width="52" height="${hс.toFixed(1)}" rx="3" fill="url(#c387-серебро)"/>
        ${т(x,(низ-hз-hс-10).toFixed(1),подп,14,ИНК,true,undefined,'#1a1a24')}</g>`; };
    return ЛИСТ(s) +
      ЗАДАЧА('Ганнон продаёт Гиерону корону: «1 кг, золота <b>90%</b>!» Архимед, как когда-то в ванне, проверяет — золота только <b>80%</b>. Ганнон: «Долью золота и сделаю 90%». Сколько золота добавить в сплав?') +
      `<div class="pic">${свг(`
        ${агора(Н,true)}
        ${доска(14,40,308,Н-84)}
        ${ст(84,0.8,0.2,'1 кг: 80%',0.03)}
        ${ясно?ст(236,1.8,0.2,'2 кг: 90%',0.3):`${т(236,150,'?',36,GOLD,true)}`}
        <g transform="translate(160 150)">${анЛин('opacity','1;0.6;1','1.6s')}
          <path d="M-18 8 l4 -18 l7 9 l7 -14 l7 14 l7 -9 l4 18 z" fill="url(#c387-золото)" stroke="#6a4a10"/></g>
        ${т(84,220,'серебро 0,2 кг',12,МУТ,true)}${т(236,220,ясно?'серебро 0,2 = 10%':'серебро — 10%',12,МУТ,true)}
        ${подпись(168,Н-10, ясно?'0,2 кг = 10% → всего 2 кг':'серебра не прибавится', ясно?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['0,1 кг','0,5 кг','1 кг'],2,в,'r387Отв8') +
      (в==null ? СКАЗ('Вопрос','Сколько золота добавить?') : РАЗБОР(в===2, ['1,1 кг сплава, серебра 0,2 — это 18%, а не 10%. Серебро 0,2 кг должно стать 10%: весь сплав 2 кг, золота добавить <b>1 кг</b>.','1,5 кг сплава: серебро 0,2 — около 13%. Нужно, чтобы 0,2 кг было 10%: сплав 2 кг, добавить <b>1 кг</b>.','Серебро не меняется: 0,2 кг. Если это 10%, весь сплав — 2 кг. Было 1 кг, значит, добавить <b>1 кг золота</b>.'][в])) +
      (в===2 ? ПРАВИЛО('Снова ищи <b>неизменную часть</b>: серебро 0,2 кг было 20%, стало 10% — вес сплава вырос вдвое.') : '');
  }

  /* 9. Процент от процента */
  function F9(s){
    const Н=260, в=s.ответ9, ясно=в===2;
    const корабль = (x,y,зерно,краден) => `<g transform="translate(${x} ${y})">
      <path d="M-14 0 q2 6 14 6 q12 0 14 -6 z" fill="url(#c387-дерево)"/>
      <line x1="0" y1="0" x2="0" y2="-20" stroke="#3a2410" stroke-width="1.4"/>
      <path d="M0 -19 q10 8 0 16 z" fill="${зерно?'#e0b040':'#f4ecd8'}"/>
      ${краден&&ясно?`<path d="M0 -20 l8 -3 l-8 -3 z" fill="${RED}">${анЛин('opacity','1;0.3;1','1s')}</path>`:''}</g>`;
    return ЛИСТ(s) +
      ЗАДАЧА('Лазутчики Архимеда считают флот Ганнона в гавани. <b>40%</b> его кораблей везут зерно. Из них <b>25%</b> — с зерном, украденным у сицилийских крестьян. Какая часть <b>всех</b> кораблей везёт краденое зерно?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c387-небо)"/>
        <rect x="0" y="90" width="336" height="${Н-90}" fill="#2d5c8a"/>
        ${Array.from({length:20},(_,i)=>{ const зерно=i<8, краден=i<2;
          return `<g>${анСдвиг('0 0;0 2;0 0',(2+(i%4)*0.3).toFixed(1)+'s','0;0.5;1')}${корабль(40+(i%5)*64,122+Math.floor(i/5)*28,зерно,краден)}</g>`; }).join('')}
        ${т(168,40,'20 кораблей: 8 с зерном',14,'#1a2a4a',true,undefined,'#f0dcb0')}
        ${подпись(168,Н-10, ясно?'0,4 · 0,25 = 0,1 → 2 из 20':'25% не от всех, а от кораблей с зерном', ясно?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['65%','15%','10%'],2,в,'r387Отв9') +
      (в==null ? СКАЗ('Вопрос','Какой процент всех кораблей?') : РАЗБОР(в===2, ['40 + 25 = 65 — проценты сложили, хотя 25% считаются только от кораблей с зерном.','15 = 40 − 25 — вычитать проценты от разных баз нельзя. 25% от 40% — это <b>10%</b>.','25% от 40% — это 0,25 · 0,4 = 0,1: <b>10%</b>. Из 20 кораблей 8 с зерном и 2 — с краденым.'][в])) +
      (в===2 ? ПРАВИЛО('Процент от процента — <b>произведение долей</b>: 25% от 40% = 10%.') : '');
  }

  /* 10. Купец разоблачён */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=270;
    return ЛИСТ(s) +
      ЗАДАЧА(всё
        ? 'Совет рынка слушает ученика Архимеда: «+10% и −10%» — это потеря, «скидка 50%» — только 44%, а смоквы «потеряли пару килограммов» — половину веса. Ганнон грузит ткани на корабль и отплывает в Карфаген. Над агорой смеются.'
        : 'Не все обманы раскрыты — вернись к делам в списке. Вот главное.') +
      `<div class="pic">${свг(`
        ${агора(Н,true)}
        <rect x="0" y="${Н-80}" width="336" height="34" fill="#2d5c8a"/>
        <g>${анСдвиг('0 0;120 0;120 0','9s','0;0.8;1')}<g transform="translate(150 ${Н-70}) scale(1.4)" filter="url(#c387-тень)">
          <path d="M-40 0 q6 12 40 12 q34 0 40 -12 z" fill="url(#c387-дерево)"/>
          <line x1="0" y1="0" x2="0" y2="-44" stroke="#3a2410" stroke-width="2"/>
          <path d="M0 -42 q24 16 0 34 z" fill="url(#c387-пурпур)"/></g></g>
        <g>${проявить('9s',0.1,0.2)}${подпись(168,30,'процент — от чего?',GOLD,12)}</g>
        <g>${проявить('9s',0.3,0.4)}${подпись(168,58,'проценты подряд перемножаются',GOLD,12)}</g>
        <g>${проявить('9s',0.5,0.6)}${подпись(168,Н-12,'ищи то, что не меняется',GREEN,12)}</g>
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','Процент — сотая часть того, <b>от чего</b> его считают. +p% — умножение на (1 + p/100), −p% — на (1 − p/100); проценты подряд перемножаются, а порядок не важен. «Больше на» и «меньше на» считают от разных чисел. В задачах на сушку и сплавы ищи часть, которая не меняется.') +
      ПРАВИЛО('<b>Прежде чем считать процент — назови базу.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Сколько драхм составляют 15% от 60 драхм?', варианты:[{т:'9',ок:true},{т:'6',ок:false}], разбор:'60 : 100 · 15 = 9.' },
    { вопрос:'Цена 80 драхм выросла на 25%. Новая цена?', варианты:[{т:'105',ок:false},{т:'100',ок:true}], разбор:'80 · 1,25 = 100: надбавка 20, а не 25.' },
    { вопрос:'После скидки 10% товар стоит 90. Сколько стоил?', варианты:[{т:'100',ок:true},{т:'99',ок:false}], разбор:'90 — это 90% от старой цены: 100.' },
    { вопрос:'Цену дважды подняли на 10%. На сколько процентов она выросла?', варианты:[{т:'на 20%',ок:false},{т:'на 21%',ок:true}], разбор:'1,1 · 1,1 = 1,21.' },
    { вопрос:'У А 40 монет, у Б 50. На сколько процентов у А меньше?', варианты:[{т:'на 20%',ок:true},{т:'на 25%',ок:false}], разбор:'10 из 50 — 20%.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r387Reset()")}</div>` +
        ПРАВИЛО('<b>Процент — от чего?</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r387Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Процент от числа', задания:[
    {q:'10% от 250?', в:1, варианты:['2,5','25'], раз:'250 : 10 = 25.'},
    {q:'50% от 90?', в:0, варианты:['45','40'], раз:'Половина.'},
    {q:'1% от 700?', в:1, варианты:['70','7'], раз:'700 : 100.'},
    {q:'25% от 80?', в:0, варианты:['20','25'], раз:'Четверть.'}
  ]};
  const Т2 = { имя:'Изменения подряд', задания:[
    {q:'100 → +20% → −20%. Итог?', в:1, варианты:['100','96'], раз:'120 · 0,8 = 96.'},
    {q:'Скидка 50%, потом ещё 50%. Итоговая скидка?', в:0, варианты:['75%','100%'], раз:'0,5 · 0,5 = 0,25 — платим четверть.'},
    {q:'Умножить на 1,5 — это…', в:1, варианты:['+15%','+50%'], раз:'1,5 = 1 + 0,5.'},
    {q:'Цена упала на 20%. На сколько поднять, чтобы вернуть?', в:0, варианты:['на 25%','на 20%'], раз:'0,8 · 1,25 = 1.'}
  ]};
  const Т3 = { имя:'Олимпиадные', задания:[
    {q:'20 кг свежих грибов, воды 90%. Сухие грибы — воды 20%. Сколько весят сухие?', в:1, варианты:['4 кг','2,5 кг'], раз:'Сухого 2 кг = 80% → 2,5 кг.'},
    {q:'50% от 50% — это…', в:0, варианты:['25%','100%'], раз:'0,5 · 0,5.'},
    {q:'Число выросло на 100%. Во сколько раз?', в:1, варианты:['в 100 раз','в 2 раза'], раз:'+100% = ×2.'},
    {q:'20 кг рассола, соли 10%. Выпарили 10 кг воды. Соли стало…', в:0, варианты:['20%','10%'], раз:'Соли 2 кг из 10 кг.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r387T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r387TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L387 = {
    id: ID,
    title: 'Проценты: олимпиадные задачи',
    ico: '🏷️',
    src: 'Математика · 5–6 класс · Олимп-5: проценты',
    subj: 'math',
    explain: [
      'Карфагенский купец Ганнон на агоре Сиракуз обманывает процентами. Процент — сотая часть величины; p% от числа = число · p : 100. Первый вопрос — процент от чего.',
      '+10%, потом −10%: 100 → 110 → 99. Скидку считали от новой цены 110. Процент считают от текущей величины.',
      'Порядок не важен: 100 · 0,9 · 1,1 = 100 · 1,1 · 0,9 = 99. Изменение на p% — умножение на (1 ± p/100).',
      'Скидки 20% и 30% на 200: 200 → 160 → 112. Скидки перемножаются: 0,8 · 0,7 = 0,56 — скидка 44%, а не 50%.',
      '20 и 25 амфор: у Диона на 25% больше (5 от 20), у Ганнона на 20% меньше (5 от 25). Процент считают от того, с чем сравнивают.',
      'Цену подняли на 25% (125). Чтобы вернуть 100, снижают на 20%: 25 из 125.',
      'Смоквы 100 кг, воды 99%; после сушки воды 98%. Сухого 1 кг было 1%, стало 2% — смоквы весят 50 кг.',
      'Корона 1 кг, золота 80%. Серебро 0,2 кг должно стать 10% — сплав 2 кг, добавить 1 кг золота.',
      'Процент от процента: 25% от 40% = 0,25 · 0,4 = 10%.',
      'Итог: база процента, перемножение, «больше на» и «меньше на», неизменная часть.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: процент от числа.',
      'Тренажёр 2: изменения подряд.',
      'Тренажёр 3: олимпиадные задачи.'
    ],
    check: {
      q: 'Цена 100 драхм выросла на 10%, затем упала на 10%. Итоговая цена?',
      choices: ['100 драхм','99 драхм','110 драхм','90 драхм'],
      ans: 1,
      exp: '110 · 0,9 = 99: скидку считают от 110.'
    },
    tasks: [
      { q:'Товар стоил 200 драхм, скидка 20%. Новая цена?', kind:'unit', ans:160, tol:0,
        hints:['20% от 200 = 40.','200 − 40.'], sol:'160' },
      { q:'Скидка 20%, потом ещё 30%. Какую часть цены платим?', kind:'choice', choices:['56%','50%'], ans:0,
        hints:['0,8 · 0,7.','Проценты подряд перемножаются.'], sol:'56% — скидка 44%.' },
      { q:'100 кг смокв, воды 99%. После сушки воды 98%. Сколько килограммов весят смоквы?', kind:'unit', ans:50, tol:0,
        hints:['Сухого 1 кг.','1 кг — это 2%.'], sol:'50 кг.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L387.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    const ЗАГОЛОВКИ={1:'Купец Ганнон',2:'Плюс и минус',3:'Другой порядок',4:'Две скидки',5:'Больше или меньше',
      6:'Вернуть цену',7:'Сушёные смоквы',8:'Корона Гиерона',9:'Флот Ганнона',10:'Купец разоблачён',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l387" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Проценты'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r387P=(p)=>{ S().p2=p; chRender(0); };
  window.r387Отв2=(к)=>{ const s=S(); s.ответ2=к; s.p2=10; if(к===1) s.дело_цена=true; chRender(0); };
  window.r387Отв3=(к)=>{ S().ответ3=к; chRender(0); };
  window.r387Отв4=(к)=>{ const s=S(); s.ответ4=к; if(к===1) s.дело_скидки=true; chRender(0); };
  window.r387Отв5=(к)=>{ S().ответ5=к; chRender(0); };
  window.r387Отв6=(к)=>{ S().ответ6=к; chRender(0); };
  window.r387Отв7=(к)=>{ const s=S(); s.ответ7=к; if(к===2) s.дело_смоквы=true; chRender(0); };
  window.r387Отв8=(к)=>{ S().ответ8=к; chRender(0); };
  window.r387Отв9=(к)=>{ S().ответ9=к; chRender(0); };
  window.r387Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r387Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r387T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r387TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L387; else arr.push(L387); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA387={render:render, L:L387};
})();
