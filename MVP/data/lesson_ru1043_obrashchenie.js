/* ============ РУССКИЙ ЯЗЫК · УРОК 1043 · «ОБРАЩЕНИЕ И ВВОДНЫЕ СЛОВА» ============
   5 класс, «Синтаксис и пунктуация». Продолжение линии 877–891.

   ФОРМАТ — КАК У РУССКИХ УРОКОВ: window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT: .kicker, h2, .lead, .cap, .steps/.st,
   .ex-line, .cases, .rail, .pair/.card/.tag/.row/.split, .verdict, .score.

   ИНТЕРАКТИВ В КАДРАХ (состояние в CHS, перерисовка через chRender):
     кадр 1  «найди обращение» — подсвечивает обращение и показывает его место;
     кадр 3  выбор места обращения: в начале, в середине, в конце;
     кадр 6  переключатель: обращение или подлежащее;
     кадр 9  выбор группы вводного слова с примером;
     кадр 13 разбор по клику: где запятые и почему.
   Плюс тренажёр из десяти заданий.

   ПРАВИЛА (школьный курс, 5 класс):
     обращение называет того, к кому обращаются, и НЕ является членом
       предложения: Маша, помоги мне. / Помоги мне, Маша. / Маша! Помоги мне.;
     обращение выделяется запятыми или восклицательным знаком;
     личные местоимения я, ты, вы обращениями НЕ бывают;
     вводные слова выражают отношение говорящего и тоже не члены предложения:
       конечно, вероятно, кажется, во-первых, к счастью, по-моему;
     вводные слова выделяются запятыми с двух сторон, если стоят в середине:
       Мы, конечно, успеем.;
     группы вводных слов: уверенность, неуверенность, порядок мыслей, чувства.
   ЛОВУШКА: «Маша помогла» — подлежащее (запятых нет), «Маша, помоги!» —
     обращение (запятая есть). */
(function(){
  'use strict';

  const ID = 1043;
  const R = window.RUKIT;
  if(!R){ return; }
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;
  const пр    = (t)=>`<p class="ex-line">${esc(t)}</p>`;
  const рельс = (стиль,...xs)=>`<div class="rail" style="${стиль||''}">${xs.join('')}</div>`;
  const двa    = (a,b)=>`<div class="cases">${a}${b}</div>`;
  const шаги  = (items)=>`<div class="steps">${items.map(([t,s],i)=>
    `<div class="st"><span class="n">${i+1}</span><span class="t">${esc(t)}<small>${esc(s)}</small></span></div>`).join('')}</div>`;
  const кноп  = (t,on)=>`<button type="button" class="chip" style="flex:1 1 40%;font-size:17px" onclick="${on}">${esc(t)}</button>`;
  const кнопки= (...xs)=>`<div class="row">${xs.join('')}</div>`;

  /* ── данные интерактивных кадров ── */
  const МЕСТА=[
    ['Маша, помоги мне.','обращение в начале: после него запятая'],
    ['Помоги мне, Маша.','обращение в конце: перед ним запятая'],
    ['Маша! Помоги мне.','обращение с восклицательным знаком']
  ];
  const СРАВНЕНИЕ=[
    ['Маша помогла маме.','подлежащее','Маша — кто? что? Это подлежащее, запятых нет.'],
    ['Маша, помоги маме.','обращение','Обращаемся к Маше: имя выделено запятой.'],
    ['Ты помог маме.','подлежащее','Личное местоимение «ты» — подлежащее, обращением не бывает.'],
    ['Дорогая бабушка, здравствуй!','обращение','Распространённое обращение: дорогая бабушка.']
  ];
  const ГРУППЫ=[
    ['конечно, разумеется','уверенность','Конечно, мы успеем.'],
    ['кажется, вероятно, может быть','неуверенность','Мы, кажется, опоздаем.'],
    ['во-первых, наконец','порядок мыслей','Во-первых, надо подумать.'],
    ['к счастью, к сожалению','чувства','К счастью, дождь кончился.']
  ];
  const РАЗБОР=[
    ['Маша, помоги мне.','Маша — обращение','После обращения ставится запятая. Обращение не член предложения.'],
    ['Мы, конечно, успеем.','конечно — вводное слово','Вводное слово в середине выделяется запятыми с двух сторон.'],
    ['Помоги мне, Маша.','Маша — обращение в конце','Перед обращением в конце ставится запятая.'],
    ['К счастью, дождь кончился.','к счастью — вводное слово','Вводное слово в начале выделяется запятой.']
  ];

  /* ── тренажёр ── */
  const GAME=[
    ['«Маша, помоги мне». Что такое «Маша»?','обращение','Обращаемся к Маше — это обращение.','подлежащее'],
    ['«Маша помогла маме». Что такое «Маша»?','подлежащее','Отвечает кто? — это подлежащее.','обращение'],
    ['Чем выделяется обращение?','запятыми','Обращение выделяется запятыми или восклицательным знаком.','кавычками'],
    ['«Мы, конечно, успеем». Что такое «конечно»?','вводное слово','Выражает уверенность говорящего.','обращение'],
    ['Является ли обращение членом предложения?','нет','Обращение не член предложения.','да'],
    ['«К счастью, дождь кончился». Что такое «к счастью»?','вводное слово','Вводное слово выражает чувство.','обращение'],
    ['Бывает ли обращением местоимение «ты»?','нет','Личные местоимения обращениями не бывают.','да'],
    ['Где ставится запятая при обращении в начале?','после обращения','Маша, помоги мне.','перед обращением'],
    ['«Во-первых, надо подумать». Какая группа вводного слова?','порядок мыслей','Во-первых указывает порядок.','чувства'],
    ['«Дорогая бабушка, здравствуй!» Что такое «дорогая бабушка»?','распространённое обращение','Обращение может быть с определением.','подлежащее']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    const кн = варианты.map(о=>кноп(о,`ru1043Game('${esc(о)}')`)).join('');
    const ответ = готово ? (верно ? R.NOTE('ok','верно',it[2]) : R.NOTE('no','исправить',it[2]))
                         : `<p class="verdict">Сначала реши: к кому обращаются или о чём говорят?</p>`;
    return `${kick('16 · Тренажёр')}
      ${word(it[0],'выбери ответ')}
      ${ответ}
      ${кнопки(кн)}
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее задание.':'Сначала вопрос, потом ответ.')}`;
  }

  window.RULESSON({id:ID, game:GAME, frames:[
    (st)=>{
      const ок = st.о1!=null;
      const отв = ок ? `<p class="verdict ok">Обращение — «Маша». Оно выделено запятой и не является членом предложения.</p>`
                     : `<p class="verdict">Нажми кнопку — покажу, где здесь обращение.</p>`;
      return `${kick('01 · Разминка')}
        ${h2('К кому обращаются в предложении')}
        ${lead('Сравним два предложения. В одном «Маша» — подлежащее, в другом — обращение. Разница только в знаках и в том, к кому направлена речь.')}
        ${пр('Маша, помоги мне.')}
        ${отв}
        ${кнопки(кноп('найти обращение','ru1043Найти()'))}
        ${pair(card('Маша помогла.','подлежащее: кто? — Маша'),card('Маша, помоги!','обращение: к кому? — к Маше'))}
        ${cap('Обращение всегда выделяется запятыми или восклицательным знаком.')}`;
    },

    ()=>`${kick('02 · Что такое обращение')}
      ${h2('Слово, которое называет того, к кому обращаются')}
      ${lead('Обращение — это слово или слова, называющие того, к кому обращена речь. Обращение не является членом предложения.')}
      ${шаги([['1 задай вопрос','к кому обращаются в предложении?'],['2 проверь','можно ли убрать слово без потери смысла?'],['3 поставь знак','запятая или восклицательный знак']])}
      ${пр('Маша, помоги мне. → обращение «Маша», знак — запятая')}
      ${cap('Обращение произносят со звательной интонацией: «Ма-а-ша!»')}`,

    (st)=>{
      const к = (st.о3||0);
      const п = МЕСТА[к];
      const кн = ['в начале','в середине','в конце'].map((х,i)=>кноп(х,`ru1043Место(${i})`)).join('');
      return `${kick('03 · Интерактив: где стоит обращение')}
        ${h2('Нажми место — увидишь пример')}
        ${lead('Обращение может стоять в начале, в конце и в середине предложения. Знак зависит от места.')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">${esc(п[1])}</p>
        ${кап_подсказка_3}
        ${cap('В середине предложения обращение выделяется запятыми с двух сторон: «Помоги, Маша, мне».')}`;
    },

    ()=>`${kick('04 · Знаки при обращении')}
      ${h2('Запятая или восклицательный знак')}
      ${cards([['в начале','Маша, помоги мне.'],['в конце','Помоги мне, Маша.'],['в середине','Помоги, Маша, мне.'],['с восклицанием','Маша! Помоги мне.']])}
      ${lead('Если обращение произносят с сильным чувством, после него ставят восклицательный знак, а следующее слово пишут с большой буквы.')}
      ${cap('Кавычки при обращении не нужны: это не чужая речь.')}`,

    ()=>`${kick('05 · Распространённое обращение')}
      ${h2('Обращение бывает из нескольких слов')}
      ${lead('Вместе с обращением могут стоять определения — тогда обращение называют распространённым.')}
      ${cards([['Дорогая бабушка, здравствуй!','обращение «дорогая бабушка»'],['Милый друг, спасибо!','обращение «милый друг»'],['Ребята, внимание!','обращение «ребята»']])}
      ${пр('Знаки те же: запятая после всего обращения целиком.')}
      ${cap('Запятая ставится не после каждого слова, а после всего обращения.')}`,

    (st)=>{
      const к = (st.о6||0);
      const п = СРАВНЕНИЕ[к];
      const кн = ['пример 1','пример 2','пример 3','пример 4'].map((х,i)=>кноп(х,`ru1043Сравни(${i})`)).join('');
      return `${kick('06 · Интерактив: обращение или подлежащее')}
        ${h2('Нажми пример — увидишь разбор')}
        ${lead('Главная ловушка: имя в начале предложения может быть и подлежащим, и обращением. Решают знаки и смысл.')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">Это ${esc(п[1])}. ${esc(п[2])}</p>
        ${двa(card('подлежащее','Маша помогла маме — кто? Маша'),card('обращение','Маша, помоги маме — к кому? к Маше'))}
        ${cap('Есть запятая и просьба — обращение; отвечает на кто? — подлежащее.')}`;
    },

    ()=>`${kick('07 · Вводные слова')}
      ${h2('Слова, которые выражают отношение говорящего')}
      ${lead('Вводные слова показывают, как говорящий относится к сказанному: уверен, сомневается, радуется, перечисляет. Они тоже не члены предложения и выделяются запятыми.')}
      ${рельс('flex-wrap:wrap',chip('конечно'),chip('вероятно'),chip('кажется'),chip('во-первых'),chip('к счастью'),chip('по-моему'))}
      ${пр('Мы, конечно, успеем. → «конечно» — вводное слово')}
      ${cap('Проверка: убери слово — предложение не разрушится, изменится только оттенок.')}`,

    ()=>`${kick('08 · Четыре группы вводных слов')}
      ${h2('Уверенность, неуверенность, порядок, чувства')}
      ${cards([['уверенность','конечно, разумеется, безусловно'],['неуверенность','кажется, вероятно, может быть'],['порядок мыслей','во-первых, наконец, итак'],['чувства','к счастью, к сожалению, к удивлению']])}
      ${lead('Одна и та же мысль с разными вводными словами звучит по-разному: «Конечно, мы успеем» и «Кажется, мы успеем».')}
      ${cap('Группа подсказывает, какой оттенок вносит слово.')}`,

    (st)=>{
      const к = (st.о9||0);
      const п = ГРУППЫ[к];
      const кн = ГРУППЫ.map((х,i)=>кноп(х[1],`ru1043Группа(${i})`)).join('');
      return `${kick('09 · Интерактив: выбери группу')}
        ${h2('Нажми группу — увидишь слова и пример')}
        ${кнопки(кн)}
        ${пр(п[2])}
        <p class="verdict ok">${esc(п[0])} — ${esc(п[1])}</p>
        ${кап_подсказка_9}
        ${cap('Вводное слово можно заменить другим из той же группы, и смысл оттенка сохранится.')}`;
    },

    ()=>`${kick('10 · Знаки при вводных словах')}
      ${h2('Запятые с двух сторон')}
      ${lead('Если вводное слово стоит в середине предложения, запятые ставятся и перед ним, и после него.')}
      ${двa(пр('Мы, конечно, успеем.'),пр('Мы, к сожалению, опоздали.'))}
      ${cards([['в начале','Конечно, мы успеем.'],['в середине','Мы, конечно, успеем.'],['в конце','Мы успеем, конечно.']])}
      ${cap('Вводное слово никогда не бывает членом предложения — к нему нельзя задать вопрос.')}`,

    ()=>`${kick('11 · Обращение и вводное слово вместе')}
      ${h2('Два знака в одном предложении')}
      ${lead('В предложении могут встретиться и обращение, и вводное слово — тогда запятых несколько.')}
      ${пр('Маша, конечно, помоги мне.')}
      ${cards([['Маша','обращение'],['конечно','вводное слово'],['обе группы','выделены запятыми']])}
      ${cap('Считай знаки по группам: отдельно обращение, отдельно вводное слово.')}`,

    ()=>`${kick('12 · Как различить')}
      ${h2('Порядок действий')}
      ${шаги([['1 найди того, к кому обращаются','это обращение'],['2 проверь остальные слова','есть ли среди них вводные'],['3 убери слово','предложение не разрушилось — не член предложения'],['4 поставь знаки','запятыми справа и слева, где нужно']])}
      ${пр('Маша, конечно, помоги мне. → обращение «Маша», вводное «конечно»')}
      ${cap('Оба вида слов не являются членами предложения.')}`,

    (st)=>{
      const к = (st.о13||0);
      const п = РАЗБОР[к];
      const кн = РАЗБОР.map((х,i)=>кноп('разбор '+(i+1),`ru1043Разбор(${i})`)).join('');
      return `${kick('13 · Интерактив: разбор по клику')}
        ${h2('Нажми — увидишь, где знаки и почему')}
        ${кнопки(кн)}
        ${пр(п[0])}
        ${cards([['что выделено',п[1]],['почему',п[2]]])}
        ${cap('Сравни разборы: знак зависит от места слова в предложении.')}`;
    },

    ()=>`${kick('14 · Ловушка')}
      ${h2('Имя в начале предложения — ещё не обращение')}
      ${lead('Если слово отвечает на вопрос кто? что?, это подлежащее, и запятых не нужно. Обращение же называет того, к кому обращаются.')}
      ${двa(пр('Маша помогла маме. → подлежащее, запятых нет'),пр('Маша, помоги маме. → обращение, запятая есть'))}
      ${cards([['подлежащее','отвечает кто? что?'],['обращение','к кому обращаются; звательная интонация'],['местоимения','я, ты, вы обращениями не бывают']])}
      ${cap('Ещё одна ошибка — кавычки при обращении: они не нужны.')}`,

    ()=>`${kick('15 · В карман')}
      ${h2('Три фразы, которые стоит унести')}
      ${cards([['обращение выделяем запятыми','Маша, помоги мне.'],['вводное слово выделяем запятыми','Мы, конечно, успеем.'],['оба не члены предложения','к ним нельзя задать вопрос']])}
      ${рельс('flex-wrap:wrap',chip('обращение'),chip('вводное слово'),chip('запятые'),chip('не член предложения'))}
      ${cap('Маша! Помоги мне. — восклицание тоже знак при обращении.')}`,

    (st)=>тренер(st)
  ]});

  const кап_подсказка_3 = cap('Если обращение в середине, запятые нужны с двух сторон.');
  const кап_подсказка_9 = cap('Группа вводного слова подсказывает оттенок: уверенность, сомнение, порядок, чувство.');

  /* ── карточка каталога ── */
  const L1043={
    id:ID, title:'Обращение и вводные слова', ico:'🙋',
    src:'Русский язык · 5 класс · Синтаксис', subj:'rus', group:null,
    explain:[
      'Обращение называет того, к кому обращена речь: «Маша, помоги мне». Оно не является членом предложения и выделяется запятыми.',
      'Сравним: «Маша помогла маме» — здесь «Маша» подлежащее (кто?), запятых нет. «Маша, помоги маме» — обращение (к кому?), запятая есть.',
      'Обращение может стоять в начале, в конце и в середине предложения. В середине запятые ставятся с двух сторон: «Помоги, Маша, мне».',
      'Если обращение произносят с сильным чувством, после него ставят восклицательный знак: «Маша! Помоги мне». Кавычки при обращении не нужны.',
      'Обращение бывает распространённым, то есть с определениями: «Дорогая бабушка, здравствуй!». Запятая ставится после всего обращения целиком.',
      'Личные местоимения я, ты, вы обращениями не бывают: «Ты помог маме» — «ты» подлежащее.',
      'Вводные слова выражают отношение говорящего к сказанному и тоже не являются членами предложения: конечно, вероятно, кажется, во-первых, к счастью.',
      'Вводные слова делятся на группы: уверенность (конечно, разумеется), неуверенность (кажется, вероятно), порядок мыслей (во-первых, наконец), чувства (к счастью, к сожалению).',
      'Вводное слово в середине предложения выделяется запятыми с двух сторон: «Мы, конечно, успеем».',
      'Проверка вводного слова: убери его — предложение не разрушится, изменится только оттенок; к вводному слову нельзя задать вопрос.',
      'В одном предложении могут встретиться и обращение, и вводное слово: «Маша, конечно, помоги мне» — здесь две группы знаков.',
      'Порядок разбора: найти того, к кому обращаются; проверить, есть ли вводные слова; убрать слово и посмотреть, не разрушилось ли предложение; расставить знаки.',
      'Ловушка первая: имя в начале предложения принимают за обращение, хотя это подлежащее: «Маша помогла» — без запятых.',
      'Ловушка вторая: ставят кавычки при обращении. Кавычки нужны для прямой речи, а не для обращения.',
      'В уроке есть интерактив: найти обращение, выбрать его место, сравнить обращение с подлежащим, выбрать группу вводного слова и разобрать предложение по клику.',
      'Тренажёр: десять заданий по кругу. Определи, обращение это, подлежащее или вводное слово, и вспомни знаки.'
    ],
    tasks:[
      { q:'«Маша, помоги мне». Что такое «Маша»?', kind:'choice',
        choices:['обращение','подлежащее'], ans:0,
        hints:['К кому обращаются?','Есть запятая после слова.'],
        sol:'Обращение.' },
      { q:'«Маша помогла маме». Что такое «Маша»?', kind:'choice',
        choices:['подлежащее','обращение'], ans:0,
        hints:['Какой вопрос?','Кто помогла?'],
        sol:'Подлежащее.' },
      { q:'«Мы, конечно, успеем». Что такое «конечно»?', kind:'choice',
        choices:['вводное слово','обращение'], ans:0,
        hints:['К кому обращаются?','Слово выражает уверенность.'],
        sol:'Вводное слово.' },
      { q:'Является ли обращение членом предложения?', kind:'choice',
        choices:['нет','да'], ans:0,
        hints:['Можно ли задать вопрос?','К обращению вопрос не задаётся.'],
        sol:'Нет, не является.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){
      const м=arr.findIndex(L=>L && L.id===ID);
      if(м>=0) arr[м]=L1043; else arr.push(L1043);
    }
  }catch(e){}

  /* ── состояние и обработчики ── */
  const С = ()=>{
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const перерисовать = ()=>{ if(typeof chRender==='function') chRender(0); };

  window.ru1043Найти=function(){ const s=С(); s.о1=1; перерисовать(); };
  window.ru1043Место=function(i){ const s=С(); s.о3=i; перерисовать(); };
  window.ru1043Сравни=function(i){ const s=С(); s.о6=i; перерисовать(); };
  window.ru1043Группа=function(i){ const s=С(); s.о9=i; перерисовать(); };
  window.ru1043Разбор=function(i){ const s=С(); s.о13=i; перерисовать(); };

  window.ru1043Game=function(ответ){
    const st=С();
    if(st.gRes!=null){
      st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null;
    }else{
      const it=GAME[(st.gIdx||0)%GAME.length];
      st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1;
    }
    перерисовать();
  };
})();
