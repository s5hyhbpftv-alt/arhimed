/* ====== МАТЕМАТИКА · УРОК 396 · «УСИЛЕННЫЙ ПРИНЦИП ДИРИХЛЕ» ==========================
   5–6 класс, олимпиадная тема. Переделан с нуля по эталону 1022
   (deploy/ЭТАЛОН_УРОКА.md), рисунки — сцена из сюжета. Прежние версии
   (vis_wk.js visW396, vis_bw.js) остаются в общих файлах; этот файл
   регистрируется поверх.

   ГЛАВНАЯ МЫСЛЬ. Если n «кроликов» рассадить по k «клеткам», то в какой-то
   клетке их не меньше ⌈n/k⌉ — округление ВВЕРХ. Задачи «сколько взять
   наверняка» решают через САМЫЙ НЕУДАЧНЫЙ случай: сначала противник
   (темнота, случай) делает всё, чтобы нам не повезло, и только следующий
   предмет гарантирует успех.

   СЮЖЕТ. «Голуби осаждённой крепости». Римляне перекрыли все дороги.
   Единственная связь Сиракуз с Карфагеном — голубиная почта Архимеда.
   Ученик рассаживает голубей, делит донесения, ночью на ощупь достаёт
   туники разведчиков и проверяет, куда легли камни римских катапульт.

   РУКАМИ: 25 голубей в 6 голубятен — касание голубятни сажает туда голубя;
   как ни старайся, в какой-то окажется 5.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ:
     10 голубей в 9 голубятен — где-то двое;
     25 в 6: 25 = 6 · 4 + 1 → ⌈25/6⌉ = 5 (пример 5,4,4,4,4,4 — больше нельзя
       уменьшить);
     100 донесений в 9 сумок: 100 = 9 · 11 + 1 → 12;
     туники 3 цветов: пара — 3 + 1 = 4; три одного цвета — 2 · 3 + 1 = 7;
     5 красных и 7 синих, наверняка 2 красные — 7 + 2 = 9;
     дни рождения: пятеро в одном месяце — 12 · 4 + 1 = 49;
     51 камень на 25 участков: 51 = 25 · 2 + 1 → участок с 3 камнями;
     9 голубятен, не больше 11 в каждой — максимум 9 · 11 = 99.

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md; в каждом кадре есть <animate>. */
(function(){
  'use strict';

  const ID = 396;
  const GOLD='#ffd76a', GREEN='#8fe0b0', RED='#ff8a78', ЛАЗУРЬ='#8fd0f0', НЕБО='#9fc4f0';
  const ИНК='#f5efe2', МУТ='#b8bfcf', ЛИНИЯ='#4d5a80', ОБВОД='#0c0a08';

  const ДЕЛА = [
    {ключ:'голуби', имя:'Рассадить почтовых голубей', итог:'5'},
    {ключ:'туники', имя:'Снарядить разведчиков вслепую', итог:'9'},
    {ключ:'мишень', имя:'Разгадать залп катапульт',   итог:'3'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];
  const ГОЛУБЕЙ = 25, ГОЛУБЯТЕН = 6;

  const CSS=`
  #lvis .s6.l396{gap:14px}
  #lvis .s6.l396 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l396 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l396 .pic svg .цель{cursor:pointer}
  #lvis .s6.l396 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#1f2a44,#131a2e);border:1.5px solid var(--line)}
  #lvis .s6.l396 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l396 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l396 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l396 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l396 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l396 .карт .текст b{color:${GOLD}}
  #lvis .s6.l396 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l396 .правило b{color:${GOLD}}
  #lvis .s6.l396 .лист{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(159,196,240,.16),rgba(159,196,240,.04));
    border:1.5px solid rgba(159,196,240,.5);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l396 .лист .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l396 .лист .шапка b{font-size:20px;letter-spacing:0;text-transform:none;color:${НЕБО}}
  #lvis .s6.l396 .лист .шапка b.готово{color:${GREEN}}
  #lvis .s6.l396 .лист ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l396 .лист li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l396 .лист li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;
    border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l396 .лист li.есть{color:${ИНК}}
  #lvis .s6.l396 .лист li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,224,176,.14)}
  #lvis .s6.l396 .лист li span{margin-left:auto;white-space:nowrap;font-variant-numeric:tabular-nums;color:var(--mut);font-size:14px}
  #lvis .s6.l396 .лист li.есть span{color:${GREEN}}
  #lvis .s6.l396 .сброс{display:flex;width:100%}
  #lvis .s6.l396 .сброс button{flex:1 1 100%;min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:16px;font-weight:700;
    border:1.5px solid rgba(255,255,255,.2);background:rgba(255,255,255,.05);color:${ИНК};
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l396 .сброс button:active{transform:translateY(2px)}
  #lvis .s6.l396 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l396 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l396 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l396 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l396 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l396 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-variant-numeric:tabular-nums;font-size:18px}
  #lvis .s6.l396 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-variant-numeric:tabular-nums;font-size:17px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l396 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l396 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l396 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l396 .уровни .точка.сейчас{background:${GOLD};animation:l396dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l396dot{0%,100%{opacity:1}50%{opacity:.6}}
  #lvis .s6.l396 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l396{-webkit-text-size-adjust:100%}
  #lvis .s6.l396 [data-anim]{animation:l396rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l396rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l396 [data-anim]{animation:none!important}
    #lvis .s6.l396 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l396 button{transition:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l396-style');
      if(!s){ s=document.createElement('style'); s.id='l396-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,выкл) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}"${выкл?' disabled':''}>${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Голуби крепости</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,обр) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, обр+"("+к+")")).join('')}</div>`;
  const ЛИСТ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'лист',
      `<div class="шапка"><span>Связь с Карфагеном</span><b class="${всё?'готово':''}">${
        всё?'голуби в пути':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}
           <span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const анСдвиг = (значения,длит,keyTimes) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}"
              repeatCount="indefinite" calcMode="spline" keyTimes="${keyTimes}"
              keySplines="${сплайны(keyTimes.split(';').length-1)}"/>${ЗАВОД}` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const вырасти = (длит,доля) => анК('opacity','0.15;0.15;1;1',длит,'0;'+кт(доля)+';'+кт(доля+0.06)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c396-рассвет" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2a3a70"/><stop offset="0.45" stop-color="#7a78b0"/><stop offset="0.8" stop-color="#f0b088"/><stop offset="1" stop-color="#ffd8a0"/>
      </linearGradient>
      <linearGradient id="c396-день" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#3a6ab0"/><stop offset="0.7" stop-color="#9cc4e8"/><stop offset="1" stop-color="#e8dcc0"/>
      </linearGradient>
      <linearGradient id="c396-кладовая" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#0c0a10"/><stop offset="1" stop-color="#241a14"/>
      </linearGradient>
      <radialGradient id="c396-солнце" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#fff8e0"/><stop offset="0.35" stop-color="#ffd090" stop-opacity=".8"/><stop offset="1" stop-color="#ffb070" stop-opacity="0"/>
      </radialGradient>
      <radialGradient id="c396-фонарь" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#fff0b0" stop-opacity=".8"/><stop offset="0.4" stop-color="#ffa040" stop-opacity=".35"/><stop offset="1" stop-color="#ff7020" stop-opacity="0"/>
      </radialGradient>
      <linearGradient id="c396-дерево" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#5a3418"/><stop offset="0.4" stop-color="#a06a3a"/><stop offset="1" stop-color="#4a2a12"/>
      </linearGradient>
      <linearGradient id="c396-крыша" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c05a3a"/><stop offset="1" stop-color="#7a2a1a"/>
      </linearGradient>
      <linearGradient id="c396-камень" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#d8c8a4"/><stop offset="1" stop-color="#8a7858"/>
      </linearGradient>
      <radialGradient id="c396-перо" cx="0.4" cy="0.35" r="0.7">
        <stop offset="0" stop-color="#ffffff"/><stop offset="0.6" stop-color="#e4e8f0"/><stop offset="1" stop-color="#a8b0c0"/>
      </radialGradient>
      <linearGradient id="c396-крыло" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0" stop-color="#c8d0dc"/><stop offset="1" stop-color="#6a7488"/>
      </linearGradient>
      <linearGradient id="c396-сумка" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#b07a44"/><stop offset="1" stop-color="#5a3818"/>
      </linearGradient>
      <linearGradient id="c396-пергамент" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f6ead0"/><stop offset="1" stop-color="#d8bf8a"/>
      </linearGradient>
      <linearGradient id="c396-поле" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0" stop-color="#a89060"/><stop offset="1" stop-color="#6a5838"/>
      </linearGradient>
      <radialGradient id="c396-ядро" cx="0.35" cy="0.35" r="0.7">
        <stop offset="0" stop-color="#e0d8c8"/><stop offset="1" stop-color="#4a4238"/>
      </radialGradient>
      <filter id="c396-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="0" dy="2" stdDeviation="1.8" flood-color="#000" flood-opacity=".5"/>
      </filter>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.66+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c396-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(12,10,8,.9)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  const доска = (x,y,w,h) => `<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="12" fill="rgba(10,12,28,.8)" stroke="rgba(159,196,240,.28)"/>`;

  /* голубь: тело, крыло, голова с клювом и глазом; летящий машет крыльями */
  const голубь = (x,y,м,летит,задерж) => `<g transform="translate(${x} ${y}) scale(${м||1})">
    <ellipse cx="0" cy="0" rx="9" ry="5.5" fill="url(#c396-перо)" stroke="#8a92a4" stroke-width=".5"/>
    <path d="M-8 -1 l-7 -3 l1 6 z" fill="#b8c0cc"/>
    <circle cx="8" cy="-4" r="3.6" fill="url(#c396-перо)" stroke="#8a92a4" stroke-width=".4"/>
    <path d="M11 -4 l3 1 l-3 1 z" fill="#e0a060"/><circle cx="9" cy="-4.8" r=".9" fill="#1a1010"/>
    <path d="M6.5 -1.5 q1.5 1 3 0" stroke="#7ab0a0" stroke-width="1" fill="none"/>
    ${летит
      ? `<path d="M-2 -3 q-4 -12 6 -14 q-2 8 2 12 z" fill="url(#c396-крыло)">${анЛин('d','M-2 -3 q-4 -12 6 -14 q-2 8 2 12 z;M-2 -1 q-6 8 4 10 q-2 -6 4 -9 z;M-2 -3 q-4 -12 6 -14 q-2 8 2 12 z','0.5s','begin="'+(задерж||0)+'s"')}</path>`
      : `<path d="M-5 -2 q6 -4 11 0 q-5 4 -11 0 z" fill="url(#c396-крыло)"/>
         <line x1="-1" y1="5" x2="-2" y2="9" stroke="#d08050" stroke-width="1"/><line x1="2" y1="5" x2="2" y2="9" stroke="#d08050" stroke-width="1"/>`}</g>`;
  /* голубятня: деревянная башенка с черепичной крышей и лётками */
  const голубятня = (x,y,w,h,подсветка,лётки) => `<g filter="url(#c396-тень)">
    <rect x="${x}" y="${y}" width="${w}" height="${h}" rx="3" fill="url(#c396-дерево)" stroke="${подсветка||'#2a1606'}" stroke-width="${подсветка?2.4:1}"/>
    ${Array.from({length:Math.floor(w/9)},(_,i)=>`<line x1="${x+4+i*9}" y1="${y+2}" x2="${x+4+i*9}" y2="${y+h-2}" stroke="#3a2010" stroke-width=".6" opacity=".6"/>`).join('')}
    <path d="M${x-6} ${y+2} L${x+w/2} ${y-20} L${x+w+6} ${y+2} Z" fill="url(#c396-крыша)" stroke="#4a1a0a" stroke-width="1"/>
    ${[0,1,2].map(i=>`<path d="M${x-6+i*8} ${y+2-i*7} L${x+w+6-i*8} ${y+2-i*7}" stroke="#5a1a0a" stroke-width=".7" opacity=".6"/>`).join('')}
    ${лётки?[0.3,0.7].map(k=>`<path d="M${x+w*k-7} ${y+22} v-6 a7 7 0 0 1 14 0 v6 z" fill="#1a0e06"/><rect x="${x+w*k-10}" y="${y+22}" width="20" height="3" fill="#6a4020"/>`).join('')+`<path d="M${x+w/2-7} ${y+46} v-6 a7 7 0 0 1 14 0 v6 z" fill="#1a0e06"/><rect x="${x+w/2-10}" y="${y+46}" width="20" height="3" fill="#6a4020"/>`:''}
    <rect x="${x-3}" y="${y+h-4}" width="${w+6}" height="5" fill="#3a2010"/></g>`;
  /* стопка голубей внутри голубятни */
  const стая = (x,y,w,h,n) => {
    const видно=Math.min(n,9), колонки=3, шx=w/колонки, шy=Math.min(18,(h-20)/3);
    return Array.from({length:видно},(_,i)=>голубь(x+шx*(i%колонки)+шx/2-2, y+h-10-Math.floor(i/колонки)*шy, 0.72,false)).join('');
  };

  /* ================= КАДРЫ ================= */

  /* 1. Голуби над Ортигией */
  function F1(s){
    const Н=300, в=s.ответ1;
    return ЛИСТ(s) +
      ЗАДАЧА('Римляне перекрыли все дороги и море. Единственная ниточка Сиракуз к Карфагену — <b>голубиная почта</b> Архимеда на крыше храма. Утром из Карфагена прилетели <b>10 голубей</b>, а голубятен на крыше <b>9</b>. Смотритель ворчит: «Кому-то придётся жить вдвоём».') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c396-рассвет)"/>
        <circle cx="250" cy="${Н-110}" r="70" fill="url(#c396-солнце)"/>
        <path d="M0 ${Н-80} q60 -18 120 -6 q60 -20 120 -4 q50 -10 96 0 V${Н} H0 Z" fill="#5a4a6a" opacity=".7"/>
        <rect x="0" y="${Н-58}" width="336" height="58" fill="url(#c396-камень)"/>
        ${Array.from({length:14},(_,i)=>`<rect x="${i*24}" y="${Н-64}" width="18" height="8" fill="url(#c396-камень)"/>`).join('')}
        ${[26,110,194,278].map((x,i)=>голубятня(x,Н-126+(i%2)*10,48,62,null,true)).join('')}
        ${[[50,Н-132],[216,Н-122]].map(([x,y])=>голубь(x,y-2,0.9,false)).join('')}
        ${[[60,86,0],[110,70,0.2],[170,96,0.35],[226,66,0.1],[292,84,0.25],[140,124,0.4]].map(([x,y,д],i)=>
          `<g>${анСдвиг(`0 0;${10+i*3} -6;0 0`,(4+i*0.5).toFixed(1)+'s','0;0.5;1')}${голубь(x,y,1.2,true,д)}</g>`).join('')}
        ${подпись(168,30, в===1?'10 в 9: хотя бы двое вместе':'10 голубей, 9 голубятен', в===1?GREEN:GOLD,13)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Не обязательно — можно рассадить по одному','Обязательно: в какой-то голубятне окажутся двое','Только если голуби сами захотят'],1,в,'r396Отв1') +
      (в==null ? СКАЗ('Вопрос','Прав ли смотритель?') : РАЗБОР(в===1, ['По одному помещается только 9 голубей — десятому места нет. <b>Где-то двое</b> — обязательно.','9 голубятен по одному — это 9 голубей, <b>десятый</b> неизбежно подсядет к кому-то. Это <b>принцип Дирихле</b>.','Желание голубей тут ни при чём: 10 > 9, значит, двое обязательно окажутся вместе.'][в])) +
      (в===1 ? ПРАВИЛО('<b>Принцип Дирихле:</b> если «кроликов» больше, чем «клеток», в какой-то клетке их хотя бы два.') : '');
  }

  /* 2. 25 голубей в 6 голубятен */
  function F2(s){
    const Н=362, в=s.ответ2;
    const г = Array.isArray(s.г2)?s.г2:[0,0,0,0,0,0];
    const посажено = г.reduce((a,b)=>a+b,0), осталось = ГОЛУБЕЙ-посажено, макс=Math.max(...г);
    const W=86, H=92, места = г.map((_,i)=>[18+(i%3)*106, 70+Math.floor(i/3)*138]);
    return ЛИСТ(s) +
      ЗАДАЧА('Главный вылет: <b>25 голубей</b> с донесениями надо рассадить по <b>6 голубятням</b>. Чем теснее, тем хуже голуби спят — а завтра им лететь над римским флотом. Касайся голубятен: каждая получает следующего голубя. Постарайся, чтобы в самой полной было <b>как можно меньше</b>.') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c396-день)"/>
        <rect x="0" y="${Н-40}" width="336" height="40" fill="url(#c396-камень)"/>
        ${г.map((n,i)=>{ const [x,y]=места[i], полная = посажено>0 && n===макс;
          return голубятня(x,y,W,H,полная?GOLD:null) + стая(x,y,W,H,n) +
            (n>9?т(x+W-8,y+16,'+'+(n-9),12,GOLD,true,'end','#2a1606'):''); }).join('')}
        ${г.map((n,i)=>{ const [x,y]=места[i], полная = посажено>0 && n===макс;
          return т(x+W/2,y+H+22,String(n),18,полная?GOLD:ИНК,true,undefined,'#1a1a2a'); }).join('')}
        ${т(168,38,'в корзине: '+осталось,15,осталось?ИНК:GREEN,true,undefined,'#1a2a4a')}
        ${места.map(([x,y],i)=>`<rect class="цель" x="${x-4}" y="${y-22}" width="${W+8}" height="${H+44}" fill="rgba(255,255,255,.001)" onclick="r396Голубь(${i})"/>`).join('')}
        ${подпись(168,Н-10, осталось ? 'сажай в самую пустую' : 'самая полная: '+макс+(макс===5?' — меньше нельзя':''), осталось?ИНК:(макс===5?GREEN:GOLD),13)}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="сброс">${BTN(4,'','↺ выпустить и рассадить заново',"r396Заново()")}</div>` +
      (осталось ? СКАЗ('Подсказка','Сажай каждого голубя в самую пустую голубятню — так «полнота» растёт медленнее всего.') :
        ОТВЕТЫ('три',['4','5','6'],1,в,'r396Отв2') +
        (в==null ? СКАЗ('Вопрос','Как ни сажай 25 голубей в 6 голубятен — в какой-то их будет не меньше…') :
          РАЗБОР(в===1, ['По 4 в шести — это только 24 голубя. <b>25-й</b> делает где-то пятерых: не меньше <b>5</b>.','25 = 6 · 4 + 1. Даже при самой ровной рассадке (5, 4, 4, 4, 4, 4) где-то <b>5</b>. А 5 — достижимо, значит, это точный ответ.','6 бывает, если сажать неровно. Но гарантировать можно только <b>5</b>: ровная рассадка 5, 4, 4, 4, 4, 4.'][в]))) +
      (в===1 ? ПРАВИЛО('<b>Усиленный Дирихле:</b> n предметов в k ящиков — в каком-то ящике не меньше <b>⌈n : k⌉</b> (делим и округляем <b>вверх</b>).') : '');
  }

  /* 3. 100 донесений */
  function F3(s){
    const Н=260, в=s.ответ3;
    const сумка = (x,y,n,лишний) => `<g filter="url(#c396-тень)">
      <path d="M${x-16} ${y-14} q16 -8 32 0 l4 30 q-20 8 -40 0 z" fill="url(#c396-сумка)" stroke="#3a2008" stroke-width=".8"/>
      <path d="M${x-12} ${y-14} q12 -18 24 0" fill="none" stroke="#3a2008" stroke-width="2"/>
      ${т(x,y+8,String(n),13,лишний?GOLD:ИНК,true,undefined,'#2a1606')}</g>`;
    return ЛИСТ(s) +
      ЗАДАЧА('Архимед написал <b>100 донесений</b> на крошечных свитках. Их раздадут <b>9 гонцам</b>-пловцам, которые ночью поплывут мимо римских кораблей. Сколько свитков <b>наверняка</b> окажется у самого нагруженного гонца, как ни дели?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c396-кладовая)"/>
        <circle cx="168" cy="60" r="80" fill="url(#c396-фонарь)" data-декор="1">${анЛин('r','74;86;74','2.4s')}</circle>
        ${Array.from({length:9},(_,i)=>{ const x=40+(i%5)*64, y=i<5?100:170, ещё=i===4&&в===2;
          return сумка(x+(i>=5?32:0),y,ещё?12:11,ещё); }).join('')}
        <g>${ДВИЖ?анСдвиг('0 -40;0 -40;0 0;0 0','3s','0;0.3;0.6;1'):''}<g transform="translate(296 84)">
          <rect x="-8" y="-4" width="16" height="8" rx="4" fill="url(#c396-пергамент)" stroke="#8a6a3a"/></g></g>
        ${т(168,36,'9 · 11 = 99, и ещё один свиток',14,GOLD,true,undefined,'#0c0a10')}
        ${подпись(168,Н-10, в===2?'100 = 9 · 11 + 1 → 12':'100 : 9 = 11 и остаток 1', в===2?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['11','10','12'],2,в,'r396Отв3') +
      (в==null ? СКАЗ('Вопрос','Сколько свитков наверняка у кого-то?') : РАЗБОР(в===2, ['11 · 9 = 99 — это на один свиток меньше. Сотый попадёт к кому-то двенадцатым: <b>12</b>.','10 · 9 = 90, а свитков 100 — осталось бы ещё 10. Даже по 11 не хватает места: <b>12</b>.','100 : 9 = 11 (ост. 1). Остаток не пропадает — он добавляет <b>ещё один</b>: 11 + 1 = <b>12</b>.'][в])) +
      (в===2 ? ПРАВИЛО('Если n : k даёт остаток, ответ — <b>частное + 1</b>. Если делится нацело — ровно частное.') : '');
  }

  /* туника для кадров «вслепую» */
  const туника = (x,y,цвет,м) => `<g transform="translate(${x} ${y}) scale(${м||1})" filter="url(#c396-тень)">
    <path d="M-14 -16 l-10 8 l6 8 l6 -4 v22 h24 v-22 l6 4 l6 -8 l-10 -8 q-4 6 -14 6 q-10 0 -14 -6 z" fill="${цвет}" stroke="rgba(0,0,0,.4)" stroke-width=".8"/>
    <path d="M-8 -10 q8 4 16 0" stroke="rgba(255,255,255,.35)" stroke-width="1" fill="none"/></g>`;
  const ЦВЕТА = {к:'#c0392b', с:'#2e5aa8', б:'#e8e2d4'};
  const кладовая = (Н) => `<rect x="0" y="0" width="336" height="${Н}" fill="url(#c396-кладовая)"/>
    <circle cx="40" cy="50" r="60" fill="url(#c396-фонарь)" data-декор="1">${анЛин('r','50;64;52;60;50','2.2s')}</circle>
    <path d="M36 40 q-6 8 -3 14 q3 3 6 0 q3 -6 -3 -14 z" fill="#ffd070">${анЛин('opacity','1;0.6;1','0.7s')}</path>
    <g filter="url(#c396-тень)"><rect x="210" y="${Н-110}" width="110" height="66" rx="6" fill="url(#c396-дерево)"/>
      <path d="M206 ${Н-110} q59 -30 118 0 z" fill="url(#c396-сумка)"/><rect x="258" y="${Н-96}" width="14" height="16" rx="2" fill="#d8b050"/></g>`;

  /* 4. Пара туник вслепую */
  function F4(s){
    const Н=260, в=s.ответ4;
    return ЛИСТ(s) +
      ЗАДАЧА('Ночь. Двоих разведчиков нужно одеть <b>одинаково</b> — чтобы римляне приняли их за своих. В тёмной кладовой сундук с туниками <b>трёх цветов</b>: красные, синие, белые. Фонарь зажигать нельзя. Сколько туник вынуть <b>наверняка</b>, чтобы среди них были две одного цвета?') +
      `<div class="pic">${свг(`
        ${кладовая(Н)}
        ${[['к',0],['с',1],['б',2]].map(([ц,i])=>`<g>${вырасти('7s',0.05+i*0.18)}${туника(64+i*52,150,ЦВЕТА[ц],1.1)}</g>`).join('')}
        <g>${вырасти('7s',0.6)}<g transform="translate(220 150)">
          <path d="M-14 -16 l-10 8 l6 8 l6 -4 v22 h24 v-22 l6 4 l6 -8 l-10 -8 q-4 6 -14 6 q-10 0 -14 -6 z" fill="none" stroke="${GOLD}" stroke-width="1.6" stroke-dasharray="4 3"/>
          ${т(0,6,'?',16,GOLD,true)}</g></g>
        ${т(116,100,'худший случай: все разные',13,МУТ,true)}
        ${подпись(168,Н-10, в===0?'4-я туника совпадёт с одной из трёх':'сначала не повезёт', в===0?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['4','3','6'],0,в,'r396Отв4') +
      (в==null ? СКАЗ('Вопрос','Сколько туник вынуть наверняка?') : РАЗБОР(в===0, ['Три первые могут оказаться разными — красная, синяя, белая. <b>Четвёртая</b> обязательно повторит цвет: цвета — «клетки», туники — «кролики».','3 могут быть все разные — пары нет. Нужна четвёртая: 3 + 1 = <b>4</b>.','6 — наверняка хватит, но это не наименьшее: уже <b>4</b> гарантируют пару.'][в])) +
      (в===0 ? ПРАВИЛО('«Наверняка» — значит, <b>в самом неудачном</b> случае. Сначала посчитай, сколько можно вынуть, не получив нужного, и прибавь 1.') : '');
  }

  /* 5. Три одного цвета */
  function F5(s){
    const Н=240, в=s.ответ5;
    const ряды=[['к','к'],['с','с'],['б','б']];
    return ЛИСТ(s) +
      ЗАДАЧА('Ночная вылазка больше: нужны <b>три</b> разведчика в одинаковых туниках. Тот же сундук, три цвета, темнота. Сколько туник вынуть наверняка?') +
      `<div class="pic">${свг(`
        ${кладовая(Н)}
        ${ряды.map((р,i)=>р.map((ц,j)=>`<g>${вырасти('8s',0.04+(i*2+j)*0.09)}${туника(52+i*62+j*26,120+j*6,ЦВЕТА[ц],0.8)}</g>`).join('')).join('')}
        <g>${вырасти('8s',0.62)}<g transform="translate(250 126)"><circle r="16" fill="none" stroke="${GOLD}" stroke-width="1.6" stroke-dasharray="4 3"/>${т(0,6,'7-я',12,GOLD,true)}</g></g>
        ${т(128,86,'по 2 каждого цвета — ещё не три',12,МУТ,true)}
        ${подпись(168,Н-10, в===2?'2 · 3 + 1 = 7':'по 2 каждого — и ни одной тройки', в===2?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['9','4','7'],2,в,'r396Отв5') +
      (в==null ? СКАЗ('Вопрос','Сколько туник для трёх одинаковых?') : РАЗБОР(в===2, ['9 хватит, но можно меньше. В худшем случае по 2 каждого цвета — 6 туник, <b>седьмая</b> даст тройку.','4 гарантируют только пару. Для тройки худший случай — по 2 каждого цвета: 6, и ещё одна — <b>7</b>.','Худший случай — по 2 каждого из 3 цветов: 6 туник без тройки. Седьмая: 2 · 3 + 1 = <b>7</b>.'][в])) +
      (в===2 ? ПРАВИЛО('Чтобы наверняка было <b>m</b> одного из <b>k</b> цветов, бери <b>(m − 1) · k + 1</b>.') : '');
  }

  /* 6. Две красные */
  function F6(s){
    const Н=260, в=s.ответ6;
    return ЛИСТ(s) +
      ЗАДАЧА('Командир требует: оба разведчика — в <b>красных</b> туниках (римский цвет!). В другом сундуке <b>5 красных</b> и <b>7 синих</b>. Темно. Сколько туник вынуть, чтобы <b>наверняка</b> было две красные?') +
      `<div class="pic">${свг(`
        ${кладовая(Н)}
        ${Array.from({length:7},(_,i)=>`<g>${вырасти('8s',0.03+i*0.07)}${туника(40+i*26,120+(i%2)*8,ЦВЕТА.с,0.72)}</g>`).join('')}
        ${[0,1].map(i=>`<g>${вырасти('8s',0.6+i*0.1)}${туника(110+i*40,190,ЦВЕТА.к,0.9)}</g>`).join('')}
        ${т(128,92,'сначала вся синяя «неудача»',13,МУТ,true)}
        ${подпись(168,Н-10, в===1?'7 синих + 2 красные = 9':'что, если сначала пойдут синие?', в===1?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['3','9','12'],1,в,'r396Отв6') +
      (в==null ? СКАЗ('Вопрос','Сколько туник наверняка?') : РАЗБОР(в===1, ['3 хватит, только если повезёт. А в худшем случае первыми выйдут <b>все 7 синих</b> — и лишь потом 2 красные: <b>9</b>.','Худший случай: все 7 синих, потом две красные. 7 + 2 = <b>9</b> — и меньшим числом не обойтись.','12 — это весь сундук. Хватит и <b>9</b>: даже если первые 7 синие, следующие две — красные.'][в])) +
      (в===1 ? ПРАВИЛО('Когда нужен <b>конкретный</b> цвет, худший случай — сначала вынуть <b>все остальные</b>.') : '');
  }

  /* 7. Дни рождения */
  function F7(s){
    const Н=250, в=s.ответ7;
    const МЕС=['янв','фев','мар','апр','май','июн','июл','авг','сен','окт','ноя','дек'];
    return ЛИСТ(s) +
      ЗАДАЧА('Гонцы — народ суеверный: пятеро, рождённые в одном месяце, считаются «счастливым отрядом». Сколько гонцов нужно собрать, чтобы <b>наверняка</b> нашлись пятеро, родившиеся в одном месяце?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c396-день)"/>
        ${доска(14,40,308,Н-84)}
        ${МЕС.map((м,i)=>{ const x=36+(i%6)*52, y=i<6?70:136;
          return `<g>${вырасти('9s',0.03+i*0.05)}<rect x="${x-20}" y="${y-18}" width="40" height="60" rx="6" fill="rgba(255,255,255,.06)" stroke="#5a6a90"/>
            ${т(x,y-4,м,11,МУТ,true)}
            ${[0,1,2,3].map(j=>`<circle cx="${x-10+(j%2)*20}" cy="${y+12+Math.floor(j/2)*14}" r="5" fill="#d8a878"/>`).join('')}</g>`; }).join('')}
        ${т(168,Н-52,'по 4 в каждом месяце — 48 гонцов',13,GOLD,true)}
        ${подпись(168,Н-10, в===2?'12 · 4 + 1 = 49':'сначала не везёт: по 4 в месяце', в===2?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['60','48','49'],2,в,'r396Отв7') +
      (в==null ? СКАЗ('Вопрос','Сколько гонцов нужно?') : РАЗБОР(в===2, ['60 = 12 · 5 — с запасом, но можно меньше: <b>49</b>.','48 — это по 4 в каждом месяце, и пятёрки ещё нет. Нужен <b>49-й</b>.','Худший случай — по 4 на месяц: 48. <b>49-й</b> делает где-то пятёрку: 12 · 4 + 1 = 49.'][в])) +
      (в===2 ? ПРАВИЛО('«Клетки» — это <b>месяцы</b>, «кролики» — люди. Формула та же: (m − 1) · k + 1.') : '');
  }

  /* 8. Залп катапульт */
  function F8(s){
    const Н=330, в=s.ответ8;
    const x0=48, y0=46, шаг=48;
    const камни=[];
    for(let r=0;r<5;r++) for(let c=0;c<5;c++){
      const n = (r===2&&c===3)?3:2;
      for(let k=0;k<n;k++){ const u=((r*7+c*13+k*5)%10)/10, v=((r*11+c*3+k*7)%10)/10;
        камни.push([x0+c*шаг+8+u*32, y0+r*шаг+8+v*32, r===2&&c===3]); }
    }
    return ЛИСТ(s) +
      ЗАДАЧА('Римляне дали залп: <b>51 камень</b> упал на квадратную площадь перед воротами. Архимед делит площадь на <b>25</b> равных участков 5 × 5 и говорит: «Где-то камни легли кучно — там у римлян пристрелянная катапульта». Сколько камней <b>наверняка</b> лежит на каком-то участке?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c396-день)"/>
        <rect x="${x0}" y="${y0}" width="${5*шаг}" height="${5*шаг}" rx="4" fill="url(#c396-поле)" filter="url(#c396-тень)"/>
        ${Array.from({length:6},(_,i)=>`<line x1="${x0+i*шаг}" y1="${y0}" x2="${x0+i*шаг}" y2="${y0+5*шаг}" stroke="#4a3a20" stroke-width="1"/><line x1="${x0}" y1="${y0+i*шаг}" x2="${x0+5*шаг}" y2="${y0+i*шаг}" stroke="#4a3a20" stroke-width="1"/>`).join('')}
        ${в===0?`<rect x="${x0+3*шаг}" y="${y0+2*шаг}" width="${шаг}" height="${шаг}" fill="rgba(255,215,106,.3)" stroke="${GOLD}" stroke-width="2.4">${анЛин('opacity','1;0.5;1','1.2s')}</rect>`:''}
        ${камни.map(([x,y,особ],i)=>`<circle cx="${x.toFixed(1)}" cy="${y.toFixed(1)}" r="4.2" fill="url(#c396-ядро)" stroke="${особ&&в===0?GOLD:'#2a2218'}" stroke-width="${особ&&в===0?1.4:0.6}"/>`).join('')}
        ${т(168,34,'51 камень, 25 участков',14,'#1a2a4a',true,undefined,'#e8dcc0')}
        ${подпись(168,Н-10, в===0?'51 = 25 · 2 + 1 → где-то 3':'по 2 на участок — это только 50', в===0?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['3','2','5'],0,в,'r396Отв8') +
      (в==null ? СКАЗ('Вопрос','Сколько камней наверняка на одном участке?') : РАЗБОР(в===0, ['По 2 на каждый участок — 50 камней. <b>51-й</b> делает где-то тройку: ⌈51 : 25⌉ = <b>3</b>.','2 гарантирует уже 26 камней. У нас 51 = 25 · 2 + 1 — значит, где-то <b>3</b>.','5 — это если бы все участки заполнили до пяти. Гарантировать можно только ⌈51 : 25⌉ = <b>3</b>.'][в])) +
      (в===0 ? ПРАВИЛО('Дирихле работает и в <b>геометрии</b>: «клетки» — части фигуры, «кролики» — точки.') : '');
  }

  /* 9. Точность оценки */
  function F9(s){
    const Н=250, в=s.ответ9;
    return ЛИСТ(s) +
      ЗАДАЧА('Смотритель спрашивает: «А если я хочу, чтобы <b>ни в одной</b> из 9 голубятен не было больше <b>11</b> голубей — сколько голубей я могу принять самое большее?»') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c396-рассвет)"/>
        <rect x="0" y="${Н-44}" width="336" height="44" fill="url(#c396-камень)"/>
        ${Array.from({length:9},(_,i)=>{ const x=14+(i%5)*64+(i>=5?32:0), y=i<5?64:142;
          return голубятня(x,y,42,50) + т(x+21,y+32,'11',15,GOLD,true,undefined,'#2a1606'); }).join('')}
        <g>${анСдвиг('0 0;-20 -8;0 0','3s','0;0.5;1')}${голубь(300,40,1.2,true,0)}</g>
        ${подпись(168,Н-10, в===2?'9 · 11 = 99 — а сотому места нет':'по 11 в каждой — сколько всего?', в===2?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['100','108','99'],2,в,'r396Отв9') +
      (в==null ? СКАЗ('Вопрос','Сколько голубей самое большее?') : РАЗБОР(в===2, ['100 голубей — это ⌈100 : 9⌉ = 12, где-то будет двенадцать. Самое большее — <b>99</b>.','108 = 9 · 12 — тут в каждой по 12. При пределе 11 — <b>9 · 11 = 99</b>.','По 11 в каждой из 9 — <b>99</b>. Сотый уже обязательно сделает где-то 12. Оценка Дирихле <b>точная</b>.'][в])) +
      (в===2 ? ПРАВИЛО('Оценка Дирихле <b>точная</b>: есть пример, где ни в одной клетке не больше ⌈n : k⌉.') : '');
  }

  /* 10. Голуби в пути */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=300;
    return ЛИСТ(s) +
      ЗАДАЧА(всё
        ? 'На рассвете 25 голубей взмывают над гаванью. Римские лучники стреляют — но голубей слишком много, и донесения разлетелись по всем «клеткам» неба. К вечеру в Карфагене читают: «Сиракузы держатся. Присылайте зерно». Разведчики в красных туниках уже у римской катапульты.'
        : 'Почта ещё не готова — вернись к делам в списке. Вот главное.') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c396-рассвет)"/>
        <circle cx="250" cy="${Н-110}" r="70" fill="url(#c396-солнце)"/>
        <rect x="0" y="${Н-70}" width="336" height="70" fill="#3a5a88"/>
        ${[0,1,2].map(i=>`<path d="M${-20+i*14} ${Н-60+i*16} q30 -3 60 0 t60 0 t60 0 t60 0 t60 0 t60 0" fill="none" stroke="#ffd8a0" stroke-width="1" opacity=".4" stroke-dasharray="14 18">${анЛин('stroke-dashoffset','0;-32',(3+i*0.5).toFixed(1)+'s')}</path>`).join('')}
        ${Array.from({length:12},(_,i)=>{ const x=30+(i*47)%280, y=90+((i*37)%110);
          return `<g>${анСдвиг(`0 0;${30+i%4*10} -${20+i%3*10};0 0`,(5+i*0.4).toFixed(1)+'s','0;0.5;1')}${голубь(x,y,0.9+(i%3)*0.15,true,(i*0.07).toFixed(2))}</g>`; }).join('')}
        <g>${проявить('9s',0.1,0.2)}${подпись(168,30,'n в k ящиков → где-то ⌈n : k⌉',GOLD,12)}</g>
        <g>${проявить('9s',0.3,0.4)}${подпись(168,58,'«наверняка» — считай худший случай',GOLD,12)}</g>
        <g>${проявить('9s',0.5,0.6)}${подпись(168,Н-12,'(m − 1) · k + 1',GREEN,13)}</g>
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','Если n предметов разложить по k ящикам, в каком-то будет не меньше <b>⌈n : k⌉</b> (округляем вверх). Чтобы наверняка получить m одного из k видов, бери <b>(m − 1) · k + 1</b>. Если нужен конкретный вид — сначала вынь все остальные. Оценка точная: есть пример, где больше и не бывает.') +
      ПРАВИЛО('<b>Против случая играй за худший случай.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'13 учеников. Обязательно ли двое родились в одном месяце?', варианты:[{т:'да',ок:true},{т:'нет',ок:false}], разбор:'13 > 12 месяцев.' },
    { вопрос:'30 яблок в 7 корзинах. В какой-то корзине не меньше…', варианты:[{т:'4',ок:false},{т:'5',ок:true}], разбор:'30 = 7 · 4 + 2 → 5.' },
    { вопрос:'Носки 2 цветов в темноте. Сколько взять, чтобы была пара?', варианты:[{т:'3',ок:true},{т:'2',ок:false}], разбор:'2 могут быть разными.' },
    { вопрос:'4 белых и 6 чёрных шаров. Сколько взять, чтобы наверняка был белый?', варианты:[{т:'7',ок:true},{т:'5',ок:false}], разбор:'Сначала все 6 чёрных, потом белый.' },
    { вопрос:'Какое наибольшее число шаров можно разложить в 5 коробок, чтобы нигде не было больше 3?', варианты:[{т:'16',ок:false},{т:'15',ок:true}], разбор:'5 · 3 = 15.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r396Reset()")}</div>` +
        ПРАВИЛО('<b>⌈n : k⌉ — округляй вверх.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r396Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Сколько наверняка', задания:[
    {q:'50 писем в 7 ящиков. В каком-то не меньше…', в:1, варианты:['7','8'], раз:'50 = 7 · 7 + 1.'},
    {q:'40 гостей, 8 столов. За каким-то не меньше…', в:0, варианты:['5','6'], раз:'40 : 8 = 5 ровно.'},
    {q:'21 шар в 4 коробки. В какой-то не меньше…', в:1, варианты:['5','6'], раз:'21 = 4 · 5 + 1.'},
    {q:'367 человек. Двое родились в один день года?', в:0, варианты:['обязательно','не обязательно'], раз:'Дней не больше 366.'}
  ]};
  const Т2 = { имя:'Худший случай', задания:[
    {q:'Перчатки 3 цветов в темноте. Сколько взять для пары?', в:1, варианты:['3','4'], раз:'3 разных, четвёртая — пара.'},
    {q:'4 цвета. Сколько взять, чтобы 3 одного цвета?', в:0, варианты:['9','12'], раз:'2 · 4 + 1.'},
    {q:'3 красных и 10 синих. Наверняка 1 красный?', в:1, варианты:['4','11'], раз:'Все 10 синих + 1.'},
    {q:'5 красных и 5 синих. Наверняка по одному каждого цвета?', в:0, варианты:['6','2'], раз:'5 одного цвета + 1.'}
  ]};
  const Т3 = { имя:'Олимпиадные', задания:[
    {q:'В классе 30 человек. Можно ли утверждать, что трое родились в одном месяце?', в:1, варианты:['нет','да'], раз:'30 = 12 · 2 + 6 → ⌈30 : 12⌉ = 3.'},
    {q:'Из чисел 1…10 выбрали 6. Наверняка два из них дают в сумме 11?', в:0, варианты:['да','нет'], раз:'Пары (1,10) (2,9) (3,8) (4,7) (5,6) — 5 «клеток».'},
    {q:'5 точек в квадрате 2 × 2. Найдутся ли две в одном квадратике 1 × 1?', в:0, варианты:['да','нет'], раз:'4 квадратика, 5 точек.'},
    {q:'Наибольшее число монет в 6 кошельках, чтобы нигде не было 4?', в:1, варианты:['24','18'], раз:'Не больше 3 в каждом: 6 · 3.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r396T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r396TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L396 = {
    id: ID,
    title: 'Усиленный принцип Дирихле',
    ico: '🕊️',
    src: 'Математика · 5–6 класс · Олимп-6: Дирихле',
    subj: 'math',
    explain: [
      'Осада: связь с Карфагеном — только голубиная почта. 10 голубей в 9 голубятнях — где-то двое. Это принцип Дирихле.',
      '25 голубей в 6 голубятен: 25 = 6 · 4 + 1, поэтому в какой-то не меньше 5. Усиленный Дирихле: n в k ящиков — где-то не меньше ⌈n : k⌉.',
      '100 донесений 9 гонцам: 100 = 9 · 11 + 1, у кого-то 12. Остаток добавляет ещё один.',
      'Туники трёх цветов в темноте: для пары нужно 4 — худший случай три разные.',
      'Для трёх одинаковых из трёх цветов — (3 − 1) · 3 + 1 = 7.',
      '5 красных и 7 синих: чтобы наверняка было две красные, берут 7 + 2 = 9 — сначала могут выйти все синие.',
      'Пятеро в одном месяце — 12 · 4 + 1 = 49 человек.',
      '51 камень на 25 участков: 51 = 25 · 2 + 1, где-то 3 камня. Дирихле работает и в геометрии.',
      'Оценка точная: в 9 голубятнях по 11 — 99 голубей, сотый даст двенадцать.',
      'Итог: ⌈n : k⌉, худший случай, (m − 1) · k + 1 и точность оценки.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: сколько наверняка.',
      'Тренажёр 2: худший случай.',
      'Тренажёр 3: олимпиадные задачи.'
    ],
    check: {
      q: '100 шаров разложили в 9 коробок. Сколько шаров минимум в какой-то коробке?',
      choices: ['11','12','10','9'],
      ans: 1,
      exp: '100 = 9 · 11 + 1 → есть коробка с 12 шарами.'
    },
    tasks: [
      { q:'Сколько шаров максимум можно разложить в 9 коробок, чтобы нигде не было больше 11?', kind:'unit', ans:99, tol:0,
        hints:['9 · 11.','Сотый даст двенадцать.'], sol:'99' },
      { q:'Сколько носков нужно взять (2 цвета), чтобы гарантированно достать пару одного цвета?', kind:'choice', choices:['2','3','4','1'], ans:1,
        hints:['Цветов 2.','Худший случай — два разных.'], sol:'3' },
      { q:'5 красных и 7 синих туник. Сколько вынуть в темноте, чтобы наверняка было две красные?', kind:'unit', ans:9, tol:0,
        hints:['Худший случай — сначала все синие.','7 + 2.'], sol:'9' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L396.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    const ЗАГОЛОВКИ={1:'Голубиная почта',2:'Главный вылет',3:'Сто донесений',4:'Пара вслепую',5:'Трое одинаковых',
      6:'Две красные',7:'Счастливый отряд',8:'Залп катапульт',9:'Сколько самое большее',10:'Голуби в пути',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l396" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Дирихле'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r396Отв1=(к)=>{ S().ответ1=к; chRender(0); };
  window.r396Голубь=(i)=>{ const s=S(); const г=Array.isArray(s.г2)?s.г2.slice():[0,0,0,0,0,0];
    if(г.reduce((a,b)=>a+b,0)>=ГОЛУБЕЙ) return; г[i]++; s.г2=г; chRender(0); };
  window.r396Заново=()=>{ const s=S(); s.г2=[0,0,0,0,0,0]; s.ответ2=null; chRender(0); };
  window.r396Отв2=(к)=>{ const s=S(); s.ответ2=к; if(к===1) s.дело_голуби=true; chRender(0); };
  window.r396Отв3=(к)=>{ S().ответ3=к; chRender(0); };
  window.r396Отв4=(к)=>{ S().ответ4=к; chRender(0); };
  window.r396Отв5=(к)=>{ S().ответ5=к; chRender(0); };
  window.r396Отв6=(к)=>{ const s=S(); s.ответ6=к; if(к===1) s.дело_туники=true; chRender(0); };
  window.r396Отв7=(к)=>{ S().ответ7=к; chRender(0); };
  window.r396Отв8=(к)=>{ const s=S(); s.ответ8=к; if(к===0) s.дело_мишень=true; chRender(0); };
  window.r396Отв9=(к)=>{ S().ответ9=к; chRender(0); };
  window.r396Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r396Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r396T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r396TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L396; else arr.push(L396); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA396={render:render, L:L396};
})();
