/* ============ ТУР 703 · МАТЕМАТИКА · 5 КЛАСС · «СНЕЖНЫЙ ГОРОДОК» ============

   ТУР, а не урок: свой сюжет, свои рисунки на каждом шаге, свои действия
   руками и разбор каждого задания.

   СЮЖЕТ. Зимой в школьном дворе строят Снежный городок: ледяная горка,
   снеговики, ёлки в гирляндах. Пять затей — пять заданий: флажок на
   календаре, бюро находок, круглый стол в штабе, клад четырёх пиратов
   и вороны на проводе.

   УСТРОЙСТВО ТУРА (12 шагов):
     1. городок            — знакомство с сюжетом
     2, 3. календарь        — теория и задача о 77-м дне зимы
     4, 5. ложные надписи   — теория и бюро находок
     6, 7. счёт по кругу    — теория и круглый стол
     8, 9. дележи           — теория и клад пиратов
    10, 11. расстояния      — теория и вороны на проводе
    12. итог               — балл и что повторить
   ============================================================================ */
(function(){
  'use strict';

  const ID = 703;
  const ДОМ = 't703';

  const ЗОЛОТО = '#ffd76a', ЛАМПА = '#ffe9a8', ПЕРО = '#f4e9c8', ТИХО = '#8fa08f',
        ЗЕЛЁН = '#8fd1a8', КРАСН = '#e86a5a', СИНЬ = '#7fd1ff', ЛИНИЯ = '#3f7a5f';

  /* ── данные тура: пять затей официального комплекта ── */
  const ЗАДАЧИ = [
    { id:'tour-math5-1', балл:2, тема:'Календарь · счёт дней и даты',
      название:'Флажок на календаре',
      условие:'Праздник открытия Снежного городка решили провести в 77-й день зимы, а первым зимним днём считать 1 декабря.',
      вопрос:'Какого числа будет 77-й день зимы?',
      ответ:'15 февраля',
      разбор:'Зима начинается 1 декабря. В декабре 31 день, в январе тоже 31 — вместе 62 дня. Значит, 77-й день зимы наступает после декабря и января: 77 − 62 = 15. Это 15 февраля. Проверка: 31 (декабрь) + 31 (январь) + 15 = 77.',
      ошибка:'Считать, что 77-й день зимы — это февраль без вычета. Сначала надо отнять целиком прошедшие месяцы: 31 + 31 = 62.' },

    { id:'tour-math5-2', балл:2, тема:'Логика · ложные надписи',
      название:'Бюро находок',
      условие:'Золотой ключик, перстень, алмаз и жемчужину спрятали в две шкатулки. На каждой по две надписи, и все четыре надписи неверные. Шкатулка 1: «Здесь алмаза нет» и «Перстень здесь». Шкатулка 2: «Золотой ключик в соседней шкатулке» и «В этой шкатулке ровно два предмета».',
      вопрос:'Что лежит в каждой шкатулке?',
      ответ:{ первая:['алмаз'], вторая:['золотой ключик','перстень','жемчужина'] },
      предметы:['золотой ключик','перстень','алмаз','жемчужина'],
      разбор:'Все надписи неверные, значит каждая говорит ровно противоположное. «Здесь алмаза нет» неверно — алмаз в первой шкатулке. «Перстень здесь» неверно — перстня в первой нет. «Ключик в соседней шкатулке» неверно — ключик не во второй, а в первой? Нет: надпись стоит на второй шкатулке и говорит про соседнюю, то есть про первую; раз она неверна, ключика в первой нет. «Ровно два предмета неверно» — во второй шкатулке не два предмета. Собираем: в первой только алмаз, во второй остальные три — ключик, перстень и жемчужина.',
      ошибка:'Читать надписи как правдивые. Здесь наоборот: неверны все четыре, поэтому каждую надо перевернуть.' },

    { id:'tour-math5-3', балл:2, тема:'Счёт по кругу · связь номера и числа участников',
      название:'Круглый стол в штабе',
      условие:'Ребята расселись за круглым столом и считаются по кругу: первый назвал 1, его правый сосед — 2, следующий — 3, и так дальше, уже второй круг пошёл.',
      вопрос:'Тот, кто в первом круге был под номером 5, во втором круге получил номер 17. Сколько всего ребят за столом?',
      ответ:12,
      разбор:'Каждый новый круг прибавляет к номеру ровно число ребят за столом. Значит, разница между номерами одного и того же человека в двух кругах и есть число участников: 17 − 5 = 12. Проверка: за столом 12 человек, второй круг начинается с 13-го номера, и пятый по счёту получит 12 + 5 = 17.',
      ошибка:'Складывать номера (5 + 17) или делить. Работает именно разность: второй круг длиннее первого ровно на число ребят.' },

    { id:'tour-math5-4', балл:2, тема:'Дележи · разницы и доли',
      название:'Клад четырёх пиратов',
      условие:'Четыре сундука с одинаковыми монетами разобрали Хитрый, Тёртый, Вёрткий и Хваткий. У Тёртого на 140 монет больше, чем у Хитрого, у Хваткого на 28 больше, чем у Хитрого, а у Вёрткого столько же, сколько у Хитрого. Потом они переделили добычу: Хваткий выложил 28 монет, Тёртый — 140 монет, всего 168 монет.',
      вопрос:'Сколько монет из этой кучки надо дать каждому, чтобы у всех стало поровну?',
      ответ:{ Хитрому:84, Тёртому:84, Вёрткому:0, Хваткому:0 },
      разбор:'Сначала уравняем: раз у Тёртого на 140 больше, чем у Хитрого, а у Хваткого на 28 больше, эти излишки и выложили — 140 + 28 = 168 монет. Но поровну должно стать у всех четверых, а не у двоих: в кучке 168 монет, значит каждому нужно добавить по 168 : 2 = 84? Проверим иначе: выложившие получили своё назад, а тем, у кого было меньше, надо добавить столько же, сколько у них уже есть. У Хитрого и Вёрткого поровну и меньше всех, значит 168 делим на двоих: по 84 каждому. У Тёртого и Хваткого после возврата уже столько же.',
      ошибка:'Делить 168 на четверых. Излишки выложили только двое, значит и досыпать надо только тем двоим, у кого монет меньше.' },

    { id:'tour-math5-5', балл:2, тема:'Расстояния · отношения и перебор',
      название:'Вороны на проводе',
      условие:'На проводе через равные промежутки сидят 10 ворон. Клара — четвёртая слева. Для вороны Усти Клара в 4 раза ближе, чем Варвара. Для вороны Сары Клара в 5 раз ближе, чем Варвара.',
      вопрос:'На каких местах, считая слева, сидят Устя, Сара и Варвара?',
      ответ:{ Устя:2, Сара:5, Варвара:10 },
      разбор:'Клара на 4-м месте. Если Клара в 4 раза ближе, чем Варвара, то расстояния до них относятся как 1 : 4. Для Усти это выполняется на 2-м месте: до Клары 2 промежутка, до Варвары 8. Для Сары отношение 1 : 5 — это 5-е место: до Клары 1 промежуток, до Варвары 5. Значит Варвара сидит на 10-м месте: 5 + 5 = 10.',
      ошибка:'Искать ответ перебором без правила. Сначала запиши отношение расстояний, а потом ищи место, где оно выполняется.' }
  ];

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="${ДОМ}-небо" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#0a1622"/><stop offset="1" stop-color="#1d3a44"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f0e2c4" stop-opacity="0.30"/>
        <stop offset="0.6" stop-color="#c9b591" stop-opacity="0.20"/>
        <stop offset="1" stop-color="#8a7a5c" stop-opacity="0.16"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стойка" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c98a45"/><stop offset="1" stop-color="#6f4420"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стекло" x1="0" y1="0" x2="0.4" y2="1">
        <stop offset="0" stop-color="#fff6d0" stop-opacity="0.9"/>
        <stop offset="1" stop-color="#a9762a" stop-opacity="0.8"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-банан" x1="0.2" y1="0" x2="0.8" y2="1">
        <stop offset="0" stop-color="#ffe08a"/><stop offset="0.5" stop-color="#f0c04a"/>
        <stop offset="1" stop-color="#b8862a"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-пол" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#243a30"/><stop offset="1" stop-color="#0d1a15"/>
      </linearGradient>
      <radialGradient id="${ДОМ}-кожа" cx="0.38" cy="0.32" r="0.75">
        <stop offset="0" stop-color="#f7d9b8"/><stop offset="0.6" stop-color="#eec5a0"/>
        <stop offset="1" stop-color="#d6a67c"/>
      </radialGradient>
      <radialGradient id="${ДОМ}-свет" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#ffdf95" stop-opacity="0.55"/>
        <stop offset="1" stop-color="#ffdf95" stop-opacity="0"/>
      </radialGradient>
      <linearGradient id="${ДОМ}-снег" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#cfe4f2" stop-opacity="0.30"/>
        <stop offset="1" stop-color="#7fa8c0" stop-opacity="0.16"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-хвоя" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2f6b4a"/><stop offset="1" stop-color="#173e2a"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-лёд" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0" stop-color="#dff1fb" stop-opacity="0.55"/>
        <stop offset="1" stop-color="#7fb8d8" stop-opacity="0.35"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-дерево" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#b07a42"/><stop offset="1" stop-color="#6f4420"/>
      </linearGradient>
      <filter id="${ДОМ}-мягко" x="-40%" y="-40%" width="180%" height="180%">
        <feGaussianBlur stdDeviation="4"/>
      </filter>
      <filter id="${ДОМ}-мягко2" x="-40%" y="-40%" width="180%" height="180%">
        <feGaussianBlur stdDeviation="1.8"/>
      </filter>
      <filter id="${ДОМ}-зерно">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="3" result="шум"/>
        <feColorMatrix in="шум" type="saturate" values="0"/>
        <feComponentTransfer><feFuncA type="linear" slope="0.14"/></feComponentTransfer>
        <feComposite operator="in" in2="SourceGraphic"/>
      </filter>
    </defs>`;


  const svg = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||170}" preserveAspectRatio="xMidYMid meet"
       style="display:block;width:100%;height:auto" aria-hidden="true">
       <defs>
         <marker id="${ДОМ}-стрелка" viewBox="0 0 10 10" refX="8" refY="5"
                 markerWidth="6" markerHeight="6" orient="auto">
           <path d="M0 0 L10 5 L0 10 z" fill="rgba(255,255,255,.5)"/>
         </marker>
       </defs>${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;


  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||12}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ПЕРО}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;


  const рамка = (x,y,ш,в,заливка,обводка,скругл,толщ) =>
    `<rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="${скругл==null?10:скругл}"
       fill="${заливка||'rgba(255,255,255,.05)'}" stroke="${обводка||ЛИНИЯ}"
       stroke-width="${толщ==null?1.4:толщ}"/>`;


  const круг = (cx,cy,r,заливка,обводка) =>
    `<circle cx="${cx}" cy="${cy}" r="${r}" fill="${заливка||'rgba(255,255,255,.06)'}"
       ${обводка?`stroke="${обводка}" stroke-width="1.5"`:''}/>`;


  /* Линия, которая прорисовывается от начала к концу: движение показывает
     связь, а не украшает. */
  const линияРисуется = (x1,y1,x2,y2,цвет,толщ,задержка) => {
    const д = Math.round(Math.hypot(x2-x1, y2-y1));
    return `<line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${цвет||ЛИНИЯ}"
      stroke-width="${толщ||1.6}" stroke-linecap="round" stroke-dasharray="${д}" stroke-dashoffset="${д}">
      <animate attributeName="stroke-dashoffset" from="${д}" to="0"
        dur="380ms" begin="${Number(задержка||0).toFixed(2)}s" fill="freeze"/></line>`;
  };


  /* ── РЕБЯТА ──
     Девочки — в платье, с косами или хвостом; мальчики — в тунике и штанах.
     Все разные: волосы, причёска, цвет одежды, рост. */
  const ДЕТИ = {
    петя:   {пол:'м', волосы:'#4a3524', причёска:'вихор',    одежда:'#5b8fd0'},
    катя:   {пол:'д', волосы:'#8a5a2a', причёска:'косы',     одежда:'#d9705f', бант:'#e8c46a'},
    вика:   {пол:'д', волосы:'#e8c46a', причёска:'хвост',    одежда:'#8a6ec0', бант:'#e86a5a'},
    оля:    {пол:'д', волосы:'#2f2a26', причёска:'косы',     одежда:'#5f9e6a', бант:'#ffd76a'},
    маша:   {пол:'д', волосы:'#b5632a', причёска:'хвост',    одежда:'#c98a3a', бант:'#7fd1ff'},
    мальчик:{пол:'м', волосы:'#3a2f28', причёска:'короткие', одежда:'#3f8f8a'},
    девочка:{пол:'д', волосы:'#6a4a2a', причёска:'косы',     одежда:'#c0607a', бант:'#ffd76a'},
    нейтр:  {пол:'',  волосы:'#5a4a3a', причёска:'короткие', одежда:'#6b7a86'}
  };

  const дитя = (x, y, рост, вид) => {
    const в = (typeof вид === 'string' ? ДЕТИ[вид] : вид) || ДЕТИ.нейтр;
    const р = рост || 30, г = р*0.26;
    const головаY = y - р + г*0.9;
    const косы = в.причёска === 'косы', хвост = в.причёска === 'хвост';
    return `
      <g>
        <ellipse cx="${x}" cy="${y+1.5}" rx="${р*0.30}" ry="${р*0.09}"
          fill="rgba(0,0,0,.42)" filter="url(#${ДОМ}-мягко2)"/>
        <ellipse cx="${x-р*0.11}" cy="${y}" rx="${р*0.09}" ry="${р*0.035}" fill="#3a2c22"/>
        <ellipse cx="${x+р*0.11}" cy="${y}" rx="${р*0.09}" ry="${р*0.035}" fill="#3a2c22"/>
        ${в.пол === 'д'
          ? `<path d="M${x-р*0.12} ${y-р*0.34} L${x+р*0.12} ${y-р*0.34}
                    L${x+р*0.30} ${y} L${x-р*0.30} ${y} Z"
               fill="${в.одежда}" stroke="rgba(0,0,0,.35)" stroke-width="1"/>
             <path d="M${x-р*0.14} ${y-р*0.60} C${x-р*0.16} ${y-р*0.42} ${x+р*0.16} ${y-р*0.42} ${x+р*0.14} ${y-р*0.60} Z"
               fill="${в.одежда}" stroke="rgba(0,0,0,.3)" stroke-width="1"/>`
          : `<path d="M${x-р*0.14} ${y-р*0.12} L${x-р*0.11} ${y-р*0.56} L${x+р*0.11} ${y-р*0.56} L${x+р*0.14} ${y-р*0.12} Z"
               fill="${в.одежда}" stroke="rgba(0,0,0,.35)" stroke-width="1"/>
             <path d="M${x-р*0.13} ${y-р*0.30} L${x+р*0.13} ${y-р*0.30}" stroke="#3a2c22"
               stroke-width="${(р*0.10).toFixed(1)}" stroke-linecap="round" opacity=".9"/>`}
        <path d="M${x-р*0.13} ${y-р*0.50} L${x-р*0.26} ${y-р*0.30}" stroke="${в.пол==='д'?в.одежда:'#e0b48f'}"
          stroke-width="${(р*0.075).toFixed(1)}" stroke-linecap="round"/>
        <path d="M${x+р*0.13} ${y-р*0.50} L${x+р*0.26} ${y-р*0.30}" stroke="${в.пол==='д'?в.одежда:'#e0b48f'}"
          stroke-width="${(р*0.075).toFixed(1)}" stroke-linecap="round"/>
        <path d="M${x} ${y-р*0.58} L${x} ${y-р*0.66}" stroke="#e0b48f" stroke-width="${(р*0.08).toFixed(1)}"/>
        <circle cx="${x}" cy="${головаY}" r="${г.toFixed(1)}" fill="url(#${ДОМ}-кожа)"
          stroke="rgba(0,0,0,.4)" stroke-width="1"/>
        <ellipse cx="${x-г*0.55}" cy="${головаY+г*0.35}" rx="${(г*0.28).toFixed(1)}" ry="${(г*0.18).toFixed(1)}"
          fill="#e08a7a" opacity=".32"/>
        <ellipse cx="${x+г*0.55}" cy="${головаY+г*0.35}" rx="${(г*0.28).toFixed(1)}" ry="${(г*0.18).toFixed(1)}"
          fill="#e08a7a" opacity=".32"/>
        <ellipse cx="${x-г*0.34}" cy="${головаY+г*0.05}" rx="${(г*0.17).toFixed(1)}" ry="${(г*0.20).toFixed(1)}" fill="#fdfaf3"/>
        <ellipse cx="${x+г*0.34}" cy="${головаY+г*0.05}" rx="${(г*0.17).toFixed(1)}" ry="${(г*0.20).toFixed(1)}" fill="#fdfaf3"/>
        <circle cx="${x-г*0.32}" cy="${головаY+г*0.07}" r="${(г*0.10).toFixed(1)}" fill="#3b2a1e"/>
        <circle cx="${x+г*0.36}" cy="${головаY+г*0.07}" r="${(г*0.10).toFixed(1)}" fill="#3b2a1e"/>
        <path d="M${x-г*0.22} ${головаY+г*0.52} Q${x} ${головаY+г*0.70} ${x+г*0.22} ${головаY+г*0.52}"
          fill="none" stroke="#a0504a" stroke-width="1.1" stroke-linecap="round"/>
        <path d="M${x-г} ${головаY-г*0.10} A${г} ${г} 0 0 1 ${x+г} ${головаY-г*0.10}
                L${x+г*0.86} ${головаY-г*0.62} L${x-г*0.86} ${головаY-г*0.62} Z"
          fill="${в.волосы}" opacity=".97"/>
        ${косы?`
          <path d="M${x-г*0.92} ${головаY+г*0.1} C${x-г*1.2} ${головаY+г*0.5} ${x-г*1.12} ${головаY+г*0.95} ${x-г*0.98} ${головаY+г*1.25}"
            stroke="${в.волосы}" stroke-width="${(г*0.30).toFixed(1)}" fill="none" stroke-linecap="round"/>
          <path d="M${x+г*0.92} ${головаY+г*0.1} C${x+г*1.2} ${головаY+г*0.5} ${x+г*1.12} ${головаY+г*0.95} ${x+г*0.98} ${головаY+г*1.25}"
            stroke="${в.волосы}" stroke-width="${(г*0.30).toFixed(1)}" fill="none" stroke-linecap="round"/>
          ${в.бант?`<circle cx="${x-г*0.98}" cy="${головаY+г*1.3}" r="${(г*0.22).toFixed(1)}" fill="${в.бант}"/>
                     <circle cx="${x+г*0.98}" cy="${головаY+г*1.3}" r="${(г*0.22).toFixed(1)}" fill="${в.бант}"/>`:''}`
        : хвост?`
          <path d="M${x+г*0.85} ${головаY-г*0.5} C${x+г*1.5} ${головаY-г*0.1} ${x+г*1.4} ${головаY+г*0.9} ${x+г*1.0} ${головаY+г*1.2}"
            stroke="${в.волосы}" stroke-width="${(г*0.34).toFixed(1)}" fill="none" stroke-linecap="round"/>
          ${в.бант?`<circle cx="${x+г*1.1}" cy="${головаY-г*0.35}" r="${(г*0.26).toFixed(1)}" fill="${в.бант}"/>`:''}`
        : `<path d="M${x-г*0.9} ${головаY-г*0.35} L${x-г*0.2} ${головаY-г*1.05} L${x+г*0.5} ${головаY-г*0.65}"
             fill="${в.волосы}" opacity=".95"/>`}
      </g>`;
  };


  /* Разбор: подробный ответ и частая ошибка. */
  const разбор = (текст, ошибка) =>
    `<div class="card" style="border-color:${ЗЕЛЁН};padding:12px 14px">
       <div style="font-size:16px;font-weight:bold;color:${ЗЕЛЁН};margin-bottom:6px">✅ Разбор</div>
       <div style="font-size:16px;line-height:1.55">${esc(текст)}</div>
       ${ошибка?`<div style="font-size:14px;line-height:1.5;color:${КРАСН};margin-top:8px">⚠ ${esc(ошибка)}</div>`:''}
     </div>`;


  /* Жетон с числом: табличка с рамкой, тенью и уголками. */
  const жетон = (x,y,текст,цвет,размер) => {
    const к = размер||20, ш=к*1.9, в=к*1.25;
    return `
      <g>
        <rect x="${x-ш/2+1.5}" y="${y-в/2+2.5}" width="${ш}" height="${в}" rx="5"
          fill="rgba(0,0,0,.45)" filter="url(#${ДОМ}-мягко2)"/>
        <rect x="${x-ш/2}" y="${y-в/2}" width="${ш}" height="${в}" rx="5"
          fill="url(#${ДОМ}-стена)" stroke="${цвет||ЛИНИЯ}" stroke-width="1.6"/>
        <path d="M${x-ш/2+3} ${y-в/2+1} L${x-ш/2+1} ${y-в/2+3}" stroke="${цвет||ЛИНИЯ}" stroke-width="1.3"/>
        <path d="M${x+ш/2-3} ${y+в/2-1} L${x+ш/2-1} ${y+в/2-3}" stroke="${цвет||ЛИНИЯ}" stroke-width="1.3"/>
        <text x="${x}" y="${y+к*0.36}" text-anchor="middle" font-size="${к}"
          font-weight="bold" fill="${цвет||ПЕРО}" font-family="Georgia,serif"
          style="font-variant-numeric:tabular-nums">${esc(текст)}</text>
      </g>`;
  };


  /* Знак операции в кружке: ×2, +3, −4, ÷5. */
  const операция = (x,y,знак,подпись,цвет,жирная) => `
    <g>
      <circle cx="${x}" cy="${y}" r="17" fill="rgba(255,255,255,.06)"
        stroke="${цвет||ЗОЛОТО}" stroke-width="${жирная?2:1.6}"/>
      ${т(x,y+5,знак,16,цвет||ЗОЛОТО,true)}
      ${подпись?т(x,y+30,подпись,12,ТИХО):''}
    </g>`;



  /* ── ПРЕДМЕТЫ ЗИМНЕГО ГОРОДКА ── */

  /* Снеговик: три шара, ведро, морковка, руки-ветки, пуговицы. */
  const снеговик = (x,y,рост) => {
    const р=рост||54, н=р*0.19, с=р*0.15, в=р*0.12;
    return `
      <g>
        <ellipse cx="${x}" cy="${y+1.5}" rx="${р*0.30}" ry="${р*0.07}" fill="rgba(0,0,0,.35)"
          filter="url(#${ДОМ}-мягко2)"/>
        <circle cx="${x}" cy="${y-н}" r="${н}" fill="#eef4f8" stroke="#b9c8d4" stroke-width="1.2"/>
        <circle cx="${x}" cy="${y-н*2-с*0.4}" r="${с}" fill="#f4f9fc" stroke="#b9c8d4" stroke-width="1.2"/>
        <circle cx="${x}" cy="${y-н*2-с*2.4-в*0.3}" r="${в}" fill="#f8fcff" stroke="#b9c8d4" stroke-width="1.1"/>
        <path d="M${x-с*1.6} ${y-н*2-с*0.6} L${x-с*2.6} ${y-н*2-с*1.6}" stroke="#7a5a3a" stroke-width="1.6" stroke-linecap="round"/>
        <path d="M${x+с*1.6} ${y-н*2-с*0.6} L${x+с*2.6} ${y-н*2-с*1.6}" stroke="#7a5a3a" stroke-width="1.6" stroke-linecap="round"/>
        <circle cx="${x-в*0.35}" cy="${y-н*2-с*2.4-в*0.4}" r="${(в*0.14).toFixed(1)}" fill="#2b2118"/>
        <circle cx="${x+в*0.35}" cy="${y-н*2-с*2.4-в*0.4}" r="${(в*0.14).toFixed(1)}" fill="#2b2118"/>
        <path d="M${x} ${y-н*2-с*2.4-в*0.1} L${x+в*0.9} ${y-н*2-с*2.4+в*0.15}" stroke="#e08a2e" stroke-width="1.6" stroke-linecap="round"/>
        <path d="M${x-в*1.1} ${y-н*2-с*2.4-в*0.9} L${x+в*1.1} ${y-н*2-с*2.4-в*0.9} L${x+в*0.7} ${y-н*2-с*2.4-в*1.9} L${x-в*0.7} ${y-н*2-с*2.4-в*1.9} Z"
          fill="#8a8f96" stroke="#5f666e" stroke-width="1"/>
        <circle cx="${x}" cy="${y-н*2-с*0.4}" r="${(с*0.16).toFixed(1)}" fill="#8a8f96"/>
        <circle cx="${x}" cy="${y-н*1.5}" r="${(н*0.16).toFixed(1)}" fill="#8a8f96"/>
      </g>`;
  };

  /* Ёлка в снегу: три яруса, ствол, гирлянда. */
  const ёлка = (x,y,высота) => {
    const в=высота||70;
    let ярусы='';
    for(let i=0;i<3;i++){
      const ш=(1-i*0.26)*в*0.42, верх=y-в*(0.35+i*0.28), низ=y-в*(0.05+i*0.28);
      ярусы += `<path d="M${x-ш} ${низ} L${x} ${верх} L${x+ш} ${низ} Z"
        fill="url(#${ДОМ}-хвоя)" stroke="#1f4a34" stroke-width="1.2"/>`;
      ярусы += `<path d="M${x-ш*0.85} ${низ-2} L${x} ${верх+6} L${x+ш*0.2} ${низ-2} Z"
        fill="#fff" opacity=".22"/>`;
    }
    return `
      <g>
        <ellipse cx="${x}" cy="${y+2}" rx="${в*0.22}" ry="${в*0.05}" fill="rgba(0,0,0,.3)"
          filter="url(#${ДОМ}-мягко2)"/>
        <rect x="${x-2.5}" y="${y-6}" width="5" height="8" fill="#6f4420"/>
        ${ярусы}
        <circle cx="${x}" cy="${y-в*1.02}" r="3" fill="${ЗОЛОТО}">
          <animate attributeName="opacity" values=".5;1;.5" dur="2.6s" repeatCount="indefinite"/>
        </circle>
      </g>`;
  };

  /* Ледяная горка: скат, ступеньки, флажок. */
  const горка = (x,y,ш,в) => `
    <g>
      <path d="M${x} ${y} L${x+ш} ${y} L${x+ш} ${y-в} Z" fill="url(#${ДОМ}-лёд)"
        stroke="#7fb8d8" stroke-width="1.4"/>
      <path d="M${x+ш*0.15} ${y} L${x+ш} ${y-в*0.75}" stroke="#fff" stroke-width="1.2" opacity=".35"/>
      ${[0.25,0.5,0.75].map(к=>`<path d="M${x+ш*(1-к)} ${y-в*к*0.9} L${x+ш*(1-к)+8} ${y-в*к*0.9}"
        stroke="#7fb8d8" stroke-width="1" opacity=".6"/>`).join('')}
      <path d="M${x+ш} ${y-в} L${x+ш} ${y-в-14}" stroke="#6f4420" stroke-width="1.6"/>
      <path d="M${x+ш} ${y-в-14} L${x+ш+12} ${y-в-10} L${x+ш} ${y-в-6} Z" fill="${КРАСН}"/>
    </g>`;

  /* Шкатулка с двумя надписями. */
  const шкатулка = (x,y,ш,в,надписи,цвет) => {
    const строки = (надписи||[]).map((н,i)=>
      `<text x="${x+ш/2}" y="${y+в+16+i*14}" text-anchor="middle" font-size="12"
         fill="${ПЕРО}" font-family="Georgia,serif">«${esc(н)}»</text>`).join('');
    return `
      <g>
        <ellipse cx="${x+ш/2}" cy="${y+в+2}" rx="${ш*0.5}" ry="4" fill="rgba(0,0,0,.4)"
          filter="url(#${ДОМ}-мягко2)"/>
        <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="4" fill="url(#${ДОМ}-дерево)"
          stroke="#4a2a10" stroke-width="1.5"/>
        <path d="M${x} ${y+в*0.35} L${x+ш} ${y+в*0.35}" stroke="#4a2a10" stroke-width="1.2"/>
        <rect x="${x+ш*0.42}" y="${y+в*0.3}" width="${ш*0.16}" height="${в*0.12}" rx="2"
          fill="${ЗОЛОТО}" stroke="#8a6420" stroke-width="1"/>
        <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="4" fill="#000" opacity=".10"
          filter="url(#${ДОМ}-зерно)"/>
        ${строки}
      </g>`;
  };

  /* Предметы бюро находок. */
  const ключик = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <circle r="5" fill="none" stroke="${ЗОЛОТО}" stroke-width="2"/>
      <path d="M0 5 L0 14 M0 10 L5 10 M0 13 L4 13" stroke="${ЗОЛОТО}" stroke-width="2" stroke-linecap="round"/>
    </g>`;
  const перстень = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <circle r="6" fill="none" stroke="#c9b591" stroke-width="2.2"/>
      <circle cy="-7" r="2.6" fill="${СИНЬ}"/>
    </g>`;
  const алмаз = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <path d="M0 -8 L8 -2 L0 9 L-8 -2 Z" fill="#bfe6ff" stroke="#7fb8d8" stroke-width="1.2"/>
      <path d="M-8 -2 L8 -2 M0 -8 L0 9" stroke="#7fb8d8" stroke-width="0.8" opacity=".7"/>
    </g>`;
  const жемчужина = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <circle r="6" fill="#f6efe2" stroke="#c9b591" stroke-width="1.2"/>
      <circle cx="-2" cy="-2" r="1.6" fill="#fff"/>
    </g>`;

  /* Круглый стол со стульями. */
  const столКруглый = (x,y,r) => `
    <g>
      <ellipse cx="${x}" cy="${y+6}" rx="${r*1.05}" ry="${r*0.28}" fill="rgba(0,0,0,.35)"
        filter="url(#${ДОМ}-мягко)"/>
      <circle cx="${x}" cy="${y}" r="${r}" fill="url(#${ДОМ}-дерево)" stroke="#4a2a10" stroke-width="1.6"/>
      <circle cx="${x}" cy="${y}" r="${r*0.72}" fill="none" stroke="#8a5f3a" stroke-width="1" opacity=".7"/>
    </g>`;

  /* Сундук с монетами. */
  const сундук = (x,y,ш,в,открыт) => `
    <g>
      <ellipse cx="${x+ш/2}" cy="${y+в+2}" rx="${ш*0.52}" ry="4" fill="rgba(0,0,0,.4)"
        filter="url(#${ДОМ}-мягко2)"/>
      <rect x="${x}" y="${y+в*0.3}" width="${ш}" height="${в*0.7}" rx="3"
        fill="url(#${ДОМ}-дерево)" stroke="#4a2a10" stroke-width="1.5"/>
      <path d="M${x} ${y+в*0.3} Q${x+ш/2} ${y-(открыт?в*0.5:0.0)} ${x+ш} ${y+в*0.3} Z"
        fill="url(#${ДОМ}-дерево)" stroke="#4a2a10" stroke-width="1.5"/>
      <rect x="${x+ш*0.42}" y="${y+в*0.55}" width="${ш*0.16}" height="${в*0.22}" rx="2"
        fill="${ЗОЛОТО}" stroke="#8a6420" stroke-width="1"/>
    </g>`;

  const монета = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})">
      <circle r="5" fill="${ЗОЛОТО}" stroke="#8a6420" stroke-width="1"/>
      <circle r="2.6" fill="none" stroke="#8a6420" stroke-width="0.8"/>
    </g>`;

  /* Ворона на проводе. */
  const ворона = (x,y,м) => {
    const к=м||1;
    return `<g transform="translate(${x} ${y}) scale(${к})">
      <ellipse cy="3" rx="7" ry="4.6" fill="#2b2f36" stroke="#12161b" stroke-width="0.9"/>
      <circle cx="-5.5" cy="-4" r="3.6" fill="#2b2f36" stroke="#12161b" stroke-width="0.9"/>
      <path d="M-9 -4 L-13 -3 L-9 -1.6 Z" fill="#c98a3a"/>
      <circle cx="-6.2" cy="-4.6" r="0.8" fill="#fff"/>
      <path d="M4 3 L11 -2 L6 5 Z" fill="#12161b"/>
    </g>`;
  };

  /* ══════════════ ШАГИ ══════════════ */
  const ШАГИ = [];

  /* ── 1. ГОРОДОК ── */
  ШАГИ.push({
    т:'Снежный городок',
    рисунок(){
      let s = `<rect x="8" y="8" width="320" height="168" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="168" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,26,'Зимой во дворе строят городок',14,ЛАМПА,true)}`;
      /* снег и небо */
      s += `<rect x="8" y="112" width="320" height="64" fill="url(#${ДОМ}-снег)"/>`;
      s += `<path d="M8 112 L328 112" stroke="#cfe4f2" stroke-width="1.2" opacity=".5"/>`;
      /* снежинки падают */
      for(let i=0;i<12;i++){
        const x=22+i*26, задержка=(i*0.45).toFixed(2);
        s += `<circle cx="${x}" cy="24" r="${1.4+(i%3)*0.4}" fill="#eaf4fb" opacity=".7">
                <animate attributeName="cy" values="24;124" dur="${(7+i%4).toFixed(1)}s"
                  begin="${задержка}s" repeatCount="indefinite"/>
                <animate attributeName="opacity" values=".8;.1" dur="${(7+i%4).toFixed(1)}s"
                  begin="${задержка}s" repeatCount="indefinite"/>
              </circle>`;
      }
      /* горка, ёлка, снеговики */
      s += горка(196,110,96,60);
      s += ёлка(60,110,66);
      s += `<g>${снеговик(150,110,52)}
              <animateTransform attributeName="transform" type="translate"
                values="0 0; 0 -2; 0 0" dur="3.2s" repeatCount="indefinite"/>
            </g>`;
      s += `<g>${снеговик(300,110,40)}
              <animateTransform attributeName="transform" type="translate"
                values="0 0; 0 -1.6; 0 0" dur="2.8s" begin="0.6s" repeatCount="indefinite"/>
            </g>`;
      /* ребята */
      s += `<g>${дитя(104,110,40,'мальчик')}
              <animateTransform attributeName="transform" type="translate"
                values="0 0; 0 -1.8; 0 0" dur="2.6s" begin="0.3s" repeatCount="indefinite"/>
            </g>`;
      s += `${т(168,164,'пять затей — пять задач тура',12,'#9fb7c4')}`;
      return svg(s,184);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Зимой ребята строят во дворе Снежный городок: ледяную горку, снеговиков и ёлки в гирляндах. Вместе с постройкой идут пять затей — пять задач этого тура.</div>
          <div style="font-size:16px;line-height:1.5;margin-top:8px">Задач пять, за каждую можно получить 2 балла — всего 10. В конце будет итог: балл и что стоит повторить.</div>
        </div>`;
    }
  });

  /* ── 2. КАЛЕНДАРЬ: теория ── */
  ШАГИ.push({
    т:'Счёт дней зимы',
    рисунок(){
      /* Зима начинается 1 декабря. Отмечаем дни по месяцам и находим 77-й. */
      const месяцы=[['декабрь',31],['январь',31],['февраль',28]];
      const КРУГ=12, ДОЛЯ=КРУГ/3;
      const мысли=['считаем от 1 декабря','в декабре 31 день и в январе 31 — вместе 62','77 − 62 = 15: это 15 февраля'];
      let s = `<rect x="8" y="8" width="320" height="186" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="186" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Первый зимний день — 1 декабря',14,ЛАМПА,true)}`;
      мысли.forEach((м,i)=>{
        s += `<text class="${ДОМ}-речь${i===0?' '+ДОМ+'-речь-первая':''}" x="168" y="46"
                text-anchor="middle" font-size="14" font-weight="bold" fill="${ЗОЛОТО}"
                font-family="Georgia,serif" opacity="0"
                style="animation:${ДОМ}-строка ${КРУГ}s linear ${(i*ДОЛЯ).toFixed(2)}s infinite">${esc(м)}</text>`;
      });
      /* три полосы-месяца: длина полосы — число дней */
      let накоплено=0;
      месяцы.forEach(([имя,дней],i)=>{
        const y=78+i*34, ш=дней*5.6;
        s += `${т(24,y+4,имя,12,ПЕРО,false,'start')}`;
        s += `<rect x="86" y="${y-9}" width="${ш}" height="18" rx="4" fill="url(#${ДОМ}-снег)"
                stroke="#9fb7c4" stroke-width="1.1" opacity=".85"/>
              <rect x="86" y="${y-9}" width="${ш}" height="18" rx="4" fill="none" stroke="${ЗОЛОТО}"
                stroke-width="1.4" opacity="0">
                <animate attributeName="opacity" values="0;.9;0" dur="${КРУГ}s"
                  begin="${(i*ДОЛЯ).toFixed(2)}s" repeatCount="indefinite" calcMode="discrete"
                  keyTimes="0;0.05;0.2"/>
              </rect>`;
        s += `${т(86+ш+6,y+4,String(дней),12,ТИХО,false,'start')}`;
        накоплено+=дней;
        s += `${т(300,y+4,String(накоплено),12,ЗОЛОТО,false,'end')}`;
      });
      s += `<path d="M292 62 L292 160" stroke="${ЛИНИЯ}" stroke-width="1" stroke-dasharray="3 3" opacity=".6"/>`;
      s += `${т(168,178,'справа — сколько дней уже прошло к концу месяца',12,ТИХО)}`;
      return svg(s,194);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Зима начинается 1 декабря, значит декабрь — это дни с 1-го по 31-й. В январе ещё 31 день, вместе получается 62. Остаётся отсчитать от 63-го дня: 77 − 62 = 15, то есть 15 февраля.</div>
        </div>`;
    }
  });

  /* ── 3. ФЛАЖОК: вводим дату ── */
  ШАГИ.push({
    т:'Флажок на календаре',
    рисунок(){
      const п=память();
      const верно = датаВерна(п.ответы[0]);
      /* Полоса из 77 клеток: декабрь, январь и начало февраля. */
      let s = `<rect x="8" y="8" width="320" height="176" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="176" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Семьдесят седьмой день зимы',14,ЛАМПА,true)}`;
      /* три строки по 26 клеток: 31, 31, 15 */
      const строки=[[31,'декабрь'],[31,'январь'],[15,'февраль']];
      let номер=1;
      строки.forEach(([сколько,имя],ряд)=>{
        const y=48+ряд*38;
        s += `${т(24,y+10,имя,12,ПЕРО,false,'start')}`;
        for(let i=0;i<сколько;i++){
          const x=84+i*7.6;
          const это77=(номер===77);
          s += `<rect x="${x}" y="${y}" width="6.4" height="14" rx="1.6"
            fill="${это77?'rgba(255,215,106,.95)':'rgba(207,228,242,.14)'}"
            stroke="${это77?ЗОЛОТО:'#7d97a6'}" stroke-width="${это77?1.4:0.7}"/>`;
          if(это77){
            s += `<path d="M${x+3.5} ${y} L${x+3.5} ${y-16}" stroke="#6f4420" stroke-width="1.4"/>
                  <path d="M${x+3.5} ${y-16} L${x+13} ${y-12} L${x+3.5} ${y-8} Z" fill="${КРАСН}">
                    <animate attributeName="opacity" values=".6;1;.6" dur="2s" repeatCount="indefinite"/>
                  </path>`;
          }
          номер++;
        }
      });
      s += `${т(168,168, верно ? 'это 15 февраля' : 'найди 77-ю клетку и запиши дату',12, верно?ЗЕЛЁН:ТИХО)}`;
      return svg(s,192);
    },
    низ(){
      const п=память(), проверено=п.проверено[0], верно=датаВерна(п.ответы[0]);
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[0].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[0].вопрос)}</div>
        </div>`;
      блок += `<div class="card ${проверено&&!верно?'':ДОМ+'-ждёт'}" style="padding:12px 14px">
          <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
            <input id="${ДОМ}-дата" type="text" value="${esc(п.ответы[0]||'')}"
              placeholder="например 15 февраля"
              style="min-height:48px;width:200px;font-size:18px;padding:0 12px;border-radius:10px;
                     border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8">
            <button class="btn" style="min-height:48px;margin:0" onclick="Т703.дата()">Проверить</button>
          </div>
        </div>`;
      if(проверено) блок += разбор(ЗАДАЧИ[0].разбор, ЗАДАЧИ[0].ошибка);
      if(верно) блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: шкатулки →</button></div>`;
      return блок;
    }
  });

  /* ── 4. ЛОЖНЫЕ НАДПИСИ: теория ── */
  ШАГИ.push({
    т:'Неверные надписи',
    рисунок(){
      /* Если надпись неверна, верно обратное. Показываем это стрелками. */
      let s = `<rect x="8" y="8" width="320" height="190" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="190" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Надпись неверна — значит верно обратное',14,ЛАМПА,true)}`;
      const пары=[
        ['«Здесь алмаза нет»','алмаз здесь есть',0],
        ['«Перстень здесь»','перстня здесь нет',1],
        ['«Ключик в соседней шкатулке»','ключика в соседней нет',2],
        ['«Здесь ровно два предмета»','здесь не два предмета',3]
      ];
      пары.forEach(([было,стало,i])=>{
        const y=58+i*32;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 380ms ease-out ${(0.2+i*0.5).toFixed(2)}s 1 forwards">
                ${рамка(24,y-14,140,26,'rgba(232,106,90,.10)',КРАСН,7,1.2)}
                ${т(94,y+4,было,12,ПЕРО)}
                ${линияРисуется(168,y-1,196,y-1,ЗОЛОТО,1.6,0.35+i*0.5)}
                ${рамка(200,y-14,112,26,'rgba(143,209,168,.10)',ЗЕЛЁН,7,1.2)}
                ${т(256,y+4,стало,12,ЗЕЛЁН)}
              </g>`;
      });
      s += `${т(168,182,'неверные надписи сами раскладывают предметы',12,ТИХО)}`;
      return svg(s,198);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">В этой затее все надписи неверные. Значит, каждая говорит ровно противоположное: «алмаза нет» превращается в «алмаз здесь», «ровно два предмета» — в «не два». Так надписи сами подсказывают, где что лежит.</div>
        </div>`;
    }
  });

  /* ── 5. БЮРО НАХОДОК: раскладываем ── */
  ШАГИ.push({
    т:'Бюро находок',
    рисунок(){
      const п=память(), где=п.ответы[1]||{};
      const предметы=ЗАДАЧИ[1].предметы;
      const верно=раскладкаВерна(где);
      let s = `<rect x="8" y="8" width="320" height="196" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="196" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, верно?'всё разложено верно':'разложи предметы по шкатулкам',14, верно?ЗЕЛЁН:ЛАМПА,true)}`;
      /* две шкатулки */
      const коробки=[
        {номер:1, надписи:['Здесь алмаза нет','Перстень здесь'], x:28},
        {номер:2, надписи:['Ключик в соседней','Здесь ровно два'], x:180}
      ];
      коробки.forEach((к,i)=>{
        s += шкатулка(к.x,52,128,44,к.надписи, i?СИНЬ:ЗОЛОТО);
        s += `${т(к.x+64,44,'Шкатулка '+к.номер,12, i?СИНЬ:ЗОЛОТО,true)}`;
        /* что внутри */
        const внутри=предметы.filter(им=>(где[им]||1)===к.номер);
        внутри.forEach((им,j)=>{
          const x=к.x+26+j*26, y=112;
          s += `<g opacity="0" style="animation:${ДОМ}-фрукт 320ms ease-out ${(0.1+j*0.1).toFixed(2)}s 1 forwards">
                  ${предметРисунок(им,x,y)}
                </g>`;
        });
      });
      /* предметы в руках: показываем, куда каждый положен */
      предметы.forEach((им,i)=>{
        const x=48+i*80, y=158;
        const куда=где[им]||1;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out ${(0.2+i*0.1).toFixed(2)}s 1 forwards">
                ${рамка(x-30,y-16,60,32, куда===1?'rgba(255,215,106,.10)':'rgba(127,209,255,.10)',
                        куда===1?ЗОЛОТО:СИНЬ,8,1.2)}
                ${предметРисунок(им,x-10,y)}
                ${т(x+14,y+4,'№'+куда,12,куда===1?ЗОЛОТО:СИНЬ,true)}
              </g>`;
      });
      s += `${т(168,190,'нажимай на предмет — он переезжает в другую шкатулку',12,ТИХО)}`;
      return svg(s,206);

      function предметРисунок(им,x,y){
        if(им==='золотой ключик') return ключик(x,y,0.9);
        if(им==='перстень') return перстень(x,y,0.9);
        if(им==='алмаз') return алмаз(x,y,0.9);
        return жемчужина(x,y,0.9);
      }
    },
    низ(){
      const п=память(), где=п.ответы[1]||{}, верно=раскладкаВерна(где);
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[1].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[1].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${ДОМ}-ждёт" style="padding:12px 14px">
            <div style="display:flex;gap:10px;flex-wrap:wrap">` +
          ЗАДАЧИ[1].предметы.map(им=>
            `<button class="btn" style="min-height:48px;margin:0"
               onclick="Т703.положить('${esc(им)}')">${esc(им)}: шкатулка ${(где[им]||1)}</button>`).join('') +
          `</div></div>`;
      } else {
        блок += разбор(ЗАДАЧИ[1].разбор, ЗАДАЧИ[1].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: круглый стол →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 6. СЧЁТ ПО КРУГУ: теория ── */
  ШАГИ.push({
    т:'Счёт по кругу',
    рисунок(){
      /* Круг: второй круг продолжает номера. Разница между номерами одного
         человека равна числу ребят. */
      const n=12;
      let s = `<rect x="8" y="8" width="320" height="194" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="194" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Второй круг продолжает номера',14,ЛАМПА,true)}`;
      s += столКруглый(168,102,42);
      for(let i=0;i<n;i++){
        const угол=(i/n)*Math.PI*2 - Math.PI/2;
        const x=168+Math.cos(угол)*64, y=102+Math.sin(угол)*52;
        const первый=(i===4);              /* пятый в первом круге */
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out ${(0.1+i*0.07).toFixed(2)}s 1 forwards">
                <circle cx="${x}" cy="${y}" r="11" fill="rgba(255,255,255,.06)"
                  stroke="${первый?ЗОЛОТО:ЛИНИЯ}" stroke-width="${первый?2:1.2}"/>
                ${т(x,y+4,String(i+1),12, первый?ЗОЛОТО:ПЕРО, первый)}
              </g>`;
        if(первый){
          s += `<g>${т(x,y-20,'5',14,ЗОЛОТО,true)}
                  <animate attributeName="opacity" values=".5;1;.5" dur="2.4s" repeatCount="indefinite"/>
                </g>`;
        }
      }
      s += `${т(168,186,'тот же человек во втором круге получил 17',12,ТИХО)}`;
      return svg(s,202);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Когда считаются по кругу, каждый новый круг прибавляет к номеру ровно число ребят за столом. Поэтому один и тот же человек в первом круге имел номер 5, а во втором — 17. Разница между этими номерами и есть число участников.</div>
        </div>`;
    }
  });

  /* ── 7. КРУГЛЫЙ СТОЛ: вводим число ── */
  ШАГИ.push({
    т:'Круглый стол в штабе',
    рисунок(){
      const п=память(), ответ=Number(п.ответы[2]), верно=ответ===ЗАДАЧИ[2].ответ;
      const n=верно?ЗАДАЧИ[2].ответ:12;
      let s = `<rect x="8" y="8" width="320" height="176" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="176" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, верно?'за столом 12 ребят':'сколько ребят за столом?',14, верно?ЗЕЛЁН:ЛАМПА,true)}`;
      s += столКруглый(168,102,40);
      /* показываем место 5 и место 17 на круге */
      [[4,'5',ЗОЛОТО],[4,'17',ЗЕЛЁН]].forEach(([индекс,подпись,ц],к)=>{
        const угол=(индекс/n)*Math.PI*2 - Math.PI/2 + к*0.55;
        const x=168+Math.cos(угол)*62, y=102+Math.sin(угол)*52;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out ${(0.2+к*0.3).toFixed(2)}s 1 forwards">
                <circle cx="${x}" cy="${y}" r="13" fill="rgba(255,255,255,.06)" stroke="${ц}" stroke-width="1.8"/>
                ${т(x,y+4,подпись,12,ц,true)}
              </g>`;
      });
      s += линияРисуется(120,60,216,60,ЗОЛОТО,1.6,0.6);
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out 0.7s 1 forwards">
              ${т(168,52,'17 − 5 = 12',14,ЗОЛОТО,true)}
            </g>`;
      s += `${т(168,168,'разница номеров одного человека = число ребят',12,ТИХО)}`;
      return svg(s,192);
    },
    низ(){
      const п=память(), проверено=п.проверено[2], верно=Number(п.ответы[2])===ЗАДАЧИ[2].ответ;
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[2].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[2].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${проверено?'':ДОМ+'-ждёт'}" style="padding:12px 14px">
            <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
              <input id="${ДОМ}-ребят" type="number" inputmode="numeric" value="${п.ответы[2]||''}"
                placeholder="сколько ребят"
                style="min-height:48px;width:170px;font-size:20px;padding:0 12px;border-radius:10px;
                       border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8">
              <button class="btn" style="min-height:48px;margin:0" onclick="Т703.ребят()">Проверить</button>
            </div>
            ${проверено?`<div style="font-size:16px;color:${КРАСН};margin-top:10px">Пока не сходится. Вспомни: разница номеров равна числу ребят.</div>`:''}
          </div>`;
      } else {
        блок += разбор(ЗАДАЧИ[2].разбор, ЗАДАЧИ[2].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: клад →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 8. ДЕЛЕЖИ: теория ── */
  ШАГИ.push({
    т:'Как делить поровну',
    рисунок(){
      /* Уравниваем: излишки выложили, их делим между теми, у кого меньше. */
      let s = `<rect x="8" y="8" width="320" height="190" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="190" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Лишнее выложили — его и делим',14,ЛАМПА,true)}`;
      const пираты=[['Хитрый',0,0],['Тёртый',140,0],['Вёрткий',0,0],['Хваткий',28,0]];
      пираты.forEach(([имя,больше],i)=>{
        const x=54+i*72;
        s += `${т(x,52,имя,12,ПЕРО,true)}`;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 320ms ease-out ${(0.1+i*0.15).toFixed(2)}s 1 forwards">
                ${сундук(x-24,66,48,34,false)}
              </g>`;
        s += `${т(x,118, больше?('+'+больше):'меньше всех',12, больше?КРАСН:ТИХО)}`;
      });
      s += линияРисуется(40,138,296,138,ЗОЛОТО,1.8,0.6);
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 320ms ease-out 0.7s 1 forwards">
              ${т(168,132,'выложили 140 + 28 = 168 монет',12,ЗОЛОТО,true)}
            </g>`;
      s += `<g opacity="0" style="animation:${ДОМ}-фрукт 320ms ease-out 1.1s 1 forwards">
              ${т(168,166,'делим на двоих: 168 : 2 = 84',14,ЗЕЛЁН,true)}
            </g>`;
      return svg(s,198);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Сначала посмотрим, у кого монет больше всех и на сколько. Излишки пираты выложили в общую кучку — 140 и 28, всего 168 монет. Поровну должно стать у всех, а меньше всех было у двоих, значит кучку делим между ними: 168 : 2 = 84.</div>
        </div>`;
    }
  });

  /* ── 9. КЛАД: вводим четыре числа ── */
  ШАГИ.push({
    т:'Клад четырёх пиратов',
    рисунок(){
      const п=память(), о=п.ответы[3]||{}, верно=кладВерен(о);
      let s = `<rect x="8" y="8" width="320" height="186" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="186" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, верно?'монеты поделены поровну':'сколько монет дать каждому?',14, верно?ЗЕЛЁН:ЛАМПА,true)}`;
      /* кучка монет в центре */
      for(let i=0;i<10;i++){
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 260ms ease-out ${(0.1+i*0.06).toFixed(2)}s 1 forwards">
                ${монета(146+(i%5)*18, 58+Math.floor(i/5)*14, 1)}
              </g>`;
      }
      s += `${т(168,96,'168 монет в кучке',12,ЗОЛОТО,true)}`;
      /* четыре пирата с ответами */
      const имена=['Хитрому','Тёртому','Вёрткому','Хваткому'];
      имена.forEach((имя,i)=>{
        const x=52+i*76;
        s += `${т(x,124,имя,12,ПЕРО,true)}`;
        const значение=о[имя];
        const показать = значение!==undefined && значение!=='';
        s += `<g>${жетон(x,148, показать?String(значение):'?', показать?ЗОЛОТО:ТИХО,16)}</g>`;
        /* монеты у пирата: их число совпадает с ответом */
        if(верно){
          const сколько=Math.min(Number(значение)||0,6);
          for(let j=0;j<сколько;j++)
            s += `<g opacity="0" style="animation:${ДОМ}-фрукт 260ms ease-out ${(0.2+j*0.08).toFixed(2)}s 1 forwards">
                    ${монета(x-20+j*8, 172, 0.8)}
                  </g>`;
        }
      });
      s += `${т(168,186,'кому давать нечего — напиши 0',12,ТИХО)}`;
      return svg(s,200);
    },
    низ(){
      const п=память(), о=п.ответы[3]||{}, проверено=п.проверено[3], верно=кладВерен(о);
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[3].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[3].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${проверено?'':ДОМ+'-ждёт'}" style="padding:12px 14px">
            <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px">` +
          ['Хитрому','Тёртому','Вёрткому','Хваткому'].map(имя=>
            `<label style="font-size:14px;color:var(--muted)">${имя}
               <input id="${ДОМ}-${имя}" type="number" inputmode="numeric" value="${esc(о[имя]??'')}"
                 style="min-height:48px;width:100%;margin-top:4px;font-size:18px;padding:0 10px;border-radius:10px;
                        border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8"></label>`).join('') +
          `</div>
            <div style="margin-top:12px"><button class="btn" style="min-height:48px;margin:0"
              onclick="Т703.клад()">Проверить</button></div>
            ${проверено?`<div style="font-size:16px;color:${КРАСН};margin-top:10px">Не сходится: у всех четырёх должно стать одинаково.</div>`:''}
          </div>`;
      } else {
        блок += разбор(ЗАДАЧИ[3].разбор, ЗАДАЧИ[3].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: вороны →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 10. РАССТОЯНИЯ: теория ── */
  ШАГИ.push({
    т:'Ближе и дальше',
    рисунок(){
      /* Провод с 10 воронами. Клара на 4-м месте. Показываем отношения. */
      let s = `<rect x="8" y="8" width="320" height="196" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="196" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Клара на четвёртом месте слева',14,ЛАМПА,true)}`;
      /* провод и вороны */
      s += `<path d="M20 70 Q168 80 316 70" fill="none" stroke="#9fb7c4" stroke-width="1.6"/>`;
      for(let i=0;i<10;i++){
        const x=44+i*28, y=72+Math.sin((i/9)*Math.PI)*3;
        const клара=(i===3);
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 280ms ease-out ${(0.1+i*0.08).toFixed(2)}s 1 forwards">
                ${ворона(x,y,0.9)}
                ${т(x,y+22,String(i+1),12, клара?ЗОЛОТО:ТИХО, клара)}
              </g>`;
        if(клара) s += `${т(x,y+36,'Клара',12,ЗОЛОТО,true)}`;
      }
      /* два отношения */
      const строки=[
        ['Устя: до Клары 2, до Варвары 8','в 4 раза',ЗЕЛЁН],
        ['Сара: до Клары 1, до Варвары 5','в 5 раз',СИНЬ]
      ];
      строки.forEach(([текст,счёт,ц],i)=>{
        const y=142+i*24;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 360ms ease-out ${(0.9+i*0.5).toFixed(2)}s 1 forwards">
                ${т(28,y,текст,12,ПЕРО,false,'start')}
                ${т(308,y,счёт,12,ц,true,'end')}
              </g>`;
      });
      s += `${т(168,188,'Варвара — на 10-м месте: 5 + 5',12,ТИХО)}`;
      return svg(s,204);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Клара сидит на 4-м месте. Если до Клары в 4 раза ближе, чем до Варвары, то расстояния относятся как 1 : 4 — это выполняется для 2-го места. Отношение 1 : 5 выполняется для 5-го места. Значит, от 5-го места до Варвары пять промежутков, и Варвара сидит на 10-м.</div>
        </div>`;
    }
  });

  /* ── 11. ВОРОНЫ: вводим три места ── */
  ШАГИ.push({
    т:'Вороны на проводе',
    рисунок(){
      const п=память(), о=п.ответы[4]||{}, верно=вороныВерны(о);
      let s = `<rect x="8" y="8" width="320" height="186" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="186" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, верно?'все трое на своих местах':'на каких местах сидят вороны?',14, верно?ЗЕЛЁН:ЛАМПА,true)}`;
      s += `<path d="M20 92 Q168 102 316 92" fill="none" stroke="#9fb7c4" stroke-width="1.6"/>`;
      const подписи={ Устя:Number(о.Устя), Сара:Number(о.Сара), Варвара:Number(о.Варвара) };
      for(let i=0;i<10;i++){
        const x=44+i*28, y=94+Math.sin((i/9)*Math.PI)*3;
        const место=i+1;
        const кто=Object.keys(подписи).find(им=>подписи[им]===место);
        const клара=(место===4);
        s += `<g>${ворона(x,y,0.9)}${т(x,y+22,String(место),12, кто?ЗОЛОТО:(клара?СИНЬ:ТИХО), !!(кто||клара))}</g>`;
        if(кто) s += `${т(x,y+36,кто,12,ЗОЛОТО,true)}`;
        if(клара) s += `${т(x,y+50,'Клара',12,СИНЬ,true)}`;
      }
      s += `${т(168,168,'от Клары в 4 раза ближе — это про Устю, в 5 раз — про Сару',12,ТИХО)}`;
      s += `${т(168,184,'Варвара дальше всех',12,ТИХО)}`;
      return svg(s,198);
    },
    низ(){
      const п=память(), о=п.ответы[4]||{}, проверено=п.проверено[4], верно=вороныВерны(о);
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[4].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[4].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${проверено?'':ДОМ+'-ждёт'}" style="padding:12px 14px">
            <div style="display:flex;gap:10px;flex-wrap:wrap">` +
          ['Устя','Сара','Варвара'].map(имя=>
            `<label style="font-size:14px;color:var(--muted)">${имя}
               <input id="${ДОМ}-${имя}" type="number" inputmode="numeric" value="${esc(о[имя]??'')}"
                 style="min-height:48px;width:96px;margin-left:8px;font-size:18px;padding:0 10px;border-radius:10px;
                        border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8"></label>`).join('') +
          `</div>
            <div style="margin-top:12px"><button class="btn" style="min-height:48px;margin:0"
              onclick="Т703.вороны()">Проверить</button></div>
            ${проверено?`<div style="font-size:16px;color:${КРАСН};margin-top:10px">Не сходится. Проверь отношение расстояний: 1 : 4 и 1 : 5.</div>`:''}
          </div>`;
      } else {
        блок += разбор(ЗАДАЧИ[4].разбор, ЗАДАЧИ[4].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Смотреть итог →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 12. ИТОГ ── */
  ШАГИ.push({
    т:'Итог',
    рисунок(){
      const балл=баллТура(), процент=Math.round(балл/10*100);
      const ц=балл>=8?ЗЕЛЁН:(балл>=5?ЗОЛОТО:КРАСН);
      const дела=[
        {имя:'дата', ок:датаВерна(память().ответы[0])},
        {имя:'шкатулки', ок:раскладкаВерна(память().ответы[1]||{})},
        {имя:'стол', ок:Number(память().ответы[2])===ЗАДАЧИ[2].ответ},
        {имя:'клад', ок:кладВерен(память().ответы[3]||{})},
        {имя:'вороны', ок:вороныВерны(память().ответы[4]||{})}
      ];
      let s = `<rect x="8" y="8" width="320" height="186" rx="12" fill="#0b1a26"/>`;
      s += `<rect x="8" y="8" width="320" height="186" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g>
              <circle cx="168" cy="62" r="38" fill="rgba(255,255,255,.04)" stroke="${ц}" stroke-width="1.6"/>
              <circle cx="168" cy="62" r="44" fill="none" stroke="${ц}" stroke-width="1.2"
                stroke-dasharray="9 7" opacity=".5">
                <animateTransform attributeName="transform" type="rotate"
                  from="0 168 62" to="360 168 62" dur="18s" repeatCount="indefinite"/>
              </circle>
              <text id="${ДОМ}-балл" x="168" y="70" text-anchor="middle" font-size="32"
                font-weight="bold" fill="${ц}" font-family="Georgia,serif"
                style="font-variant-numeric:tabular-nums">0</text>
            </g>`;
      s += `${т(168,122,'выполнено '+процент+' %',14,ПЕРО)}`;
      дела.forEach((д,i)=>{
        const x=56+i*56;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 320ms ease-out ${(0.2+i*0.12).toFixed(2)}s 1 forwards">
                ${рамка(x-24,140,48,26, д.ок?'rgba(143,209,168,.14)':'rgba(232,106,90,.12)',
                        д.ок?ЗЕЛЁН:КРАСН,8,1.4)}
                ${т(x,157,д.ок?'✓':'✕',14,д.ок?ЗЕЛЁН:КРАСН,true)}
                ${т(x,178,д.имя,12,ТИХО)}
              </g>`;
      });
      s += `<rect x="48" y="186" width="240" height="8" rx="4" fill="rgba(255,255,255,.06)"/>`;
      s += `<rect x="48" y="186" width="0" height="8" rx="4" fill="${ц}">
              <animate attributeName="width" from="0" to="${Math.round(240*балл/10)}" dur="900ms"
                begin="0.1s" fill="freeze"/></rect>`;
      return svg(s,202);
    },
    низ(){
      const балл=баллТура();
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Тур пройден. Что мы делали: считали день зимы по календарю, переворачивали неверные надписи, находили число ребят по номерам в круге, делили клад на четвёртых и искали места ворон по отношению расстояний.</div>
          <div style="font-size:16px;line-height:1.5;margin-top:8px">${балл>=8?'Отличный результат.':(балл>=5?'Хорошо, но есть что разобрать.':'Вернись к разборам и попробуй снова.')}</div>
        </div>
        <div class="btnrow"><button class="btn ghost" onclick="go('olymp')">← К списку туров</button>
        <button class="btn ok2" onclick="go('tour')">В раздел «Тур»</button></div>`;
    }
  });

  /* ── проверки ответов ── */
  function нормализовать(s){
    return String(s||'').toLowerCase().replace(/ё/g,'е').replace(/\s+/g,'').replace(/[./-]/g,'.');
  }
  function датаВерна(ответ){
    const н=нормализовать(ответ);
    return н==='15февраля' || н==='15.02' || н==='15.2' || н==='15фев';
  }
  function раскладкаВерна(где){
    const о=ЗАДАЧИ[1].ответ;
    const первая=ЗАДАЧИ[1].предметы.filter(им=>(где&&где[им]||1)===1);
    const вторая=ЗАДАЧИ[1].предметы.filter(им=>(где&&где[им]||1)===2);
    return первая.length===о.первая.length && первая.every(им=>о.первая.indexOf(им)>=0) &&
           вторая.length===о.вторая.length && вторая.every(им=>о.вторая.indexOf(им)>=0);
  }
  function кладВерен(о){
    const а=ЗАДАЧИ[3].ответ;
    return ['Хитрому','Тёртому','Вёрткому','Хваткому'].every(им=>Number(о[им])===а[им]);
  }
  function вороныВерны(о){
    const а=ЗАДАЧИ[4].ответ;
    return ['Устя','Сара','Варвара'].every(им=>Number(о[им])===а[им]);
  }

  /* ── состояние ответов ── */
  function память(){
    const к = (typeof lidKey === 'function') ? lidKey(ID) : String(ID);
    if(typeof CHS === 'undefined') window.CHS = {};
    if(!CHS[к]) CHS[к] = {};
    const п = CHS[к];
    if(!п.ответы) п.ответы = {};
    if(!п.проверено) п.проверено = {};
    return п;
  }
  const перерисовать = () => { if(typeof chRender === 'function') chRender(0); };

  function баллТура(){
    const п=память(); let б=0;
    if(датаВерна(п.ответы[0])) б+=2;
    if(раскладкаВерна(п.ответы[1]||{})) б+=2;
    if(Number(п.ответы[2])===ЗАДАЧИ[2].ответ) б+=2;
    if(кладВерен(п.ответы[3]||{})) б+=2;
    if(вороныВерны(п.ответы[4]||{})) б+=2;
    return б;
  }

  function счётДо(ид, до, мс){
    try{
      const el=document.getElementById(ид); if(!el) return;
      const t0=performance.now(), длит=мс||700;
      const шаг=()=>{
        const k=Math.min(1,(performance.now()-t0)/длит);
        el.textContent=Math.round(до*(1-Math.pow(1-k,3)));
        if(k<1) requestAnimationFrame(шаг); else el.textContent=до;
      };
      requestAnimationFrame(шаг);
    }catch(e){}
  }

  /* ── действия руками ── */
  window.Т703 = {
    дата(){
      const п=память(), el=document.getElementById(ДОМ+'-дата');
      п.ответы[0] = el ? el.value.trim() : '';
      п.проверено[0] = true;
      перерисовать();
    },
    положить(имя){
      const п=память();
      const где = п.ответы[1] = п.ответы[1] || {};
      /* По умолчанию предмет лежит в первой шкатулке, поэтому первое
         нажатие переносит его во вторую, а не «оставляет на месте». */
      где[имя] = (где[имя]===2) ? 1 : 2;
      перерисовать();
    },
    ребят(){
      const п=память(), el=document.getElementById(ДОМ+'-ребят');
      п.ответы[2] = el ? el.value.trim() : '';
      п.проверено[2] = true;
      перерисовать();
    },
    клад(){
      const п=память(), о={};
      ['Хитрому','Тёртому','Вёрткому','Хваткому'].forEach(имя=>{
        const el=document.getElementById(ДОМ+'-'+имя);
        о[имя] = el ? el.value.trim() : '';
      });
      п.ответы[3]=о; п.проверено[3]=true;
      перерисовать();
    },
    вороны(){
      const п=память(), о={};
      ['Устя','Сара','Варвара'].forEach(имя=>{
        const el=document.getElementById(ДОМ+'-'+имя);
        о[имя] = el ? el.value.trim() : '';
      });
      п.ответы[4]=о; п.проверено[4]=true;
      перерисовать();
    }
  };

  /* ══════════════════════ РЕГИСТРАЦИЯ ══════════════════════ */
  const ТЕКУЩИЙ = () => Math.max(0, Math.min(ШАГИ.length-1,
                     (typeof LV !== 'undefined' && LV && LV.step) || 0));

  function стиль(){
    if(document.getElementById(ДОМ+'-стиль')) return;
    const s=document.createElement('style');
    s.id=ДОМ+'-стиль';
    s.textContent = `
      @keyframes ${ДОМ}-появление{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
      @keyframes ${ДОМ}-пульс{0%,100%{opacity:.45}50%{opacity:1}}
      @keyframes ${ДОМ}-строка{0%{opacity:1}18%{opacity:1}21%{opacity:0}100%{opacity:0}}
      @keyframes ${ДОМ}-фрукт{from{opacity:0}to{opacity:1}}
      .${ДОМ}{display:flex;flex-direction:column;align-items:stretch;gap:12px;
        font-family:Georgia,'Times New Roman',serif;-webkit-font-smoothing:antialiased}
      .${ДОМ}-шапка{text-align:center;font-size:14px;color:var(--muted,#cbb89a);
        letter-spacing:.02em;font-variant-numeric:tabular-nums}
      .${ДОМ}-шапка b{color:#ffd76a;font-weight:600}
      .${ДОМ}-сцена{display:flex;justify-content:center;width:100%}
      .${ДОМ}-сцена svg{display:block;width:100%;max-width:420px;height:auto;margin:0 auto}
      .${ДОМ}-низ{display:flex;flex-direction:column;gap:12px;
        animation:${ДОМ}-появление 320ms cubic-bezier(.23,1,.32,1) both}
      .${ДОМ} .card{text-align:left}
      .${ДОМ}-ждёт{animation:${ДОМ}-пульс 1.6s ease-in-out infinite}
      .${ДОМ} button{transition:transform 140ms cubic-bezier(.23,1,.32,1),
        border-color 140ms ease,background-color 140ms ease}
      .${ДОМ} button:active{transform:translateY(2px) scale(.985)}
      .${ДОМ} button:focus-visible{outline:3px solid #ffd76a;outline-offset:2px}
      @media (min-width: 700px){ .${ДОМ}{max-width:560px;margin:0 auto} }
      @media (prefers-reduced-motion: reduce){
        .${ДОМ} *, #lvis svg *{animation:none!important;transition:none!important}
        .${ДОМ}-речь{opacity:0}
        .${ДОМ}-речь-первая{opacity:1}
      }`;
    document.head.appendChild(s);
  }

  function рисовать(el){
    if(!el) return;
    стиль();
    const i = ТЕКУЩИЙ(), шаг = ШАГИ[i];
    if(!шаг){ el.innerHTML=''; return; }
    try{
      el.innerHTML = `<div class="${ДОМ}" style="animation:${ДОМ}-появление 280ms cubic-bezier(.23,1,.32,1) both">
          <div class="${ДОМ}-шапка">Шаг <b>${i+1}</b> из ${ШАГИ.length} · ${esc(шаг.т)}</div>
          ${шаг.рисунок ? `<div class="${ДОМ}-сцена">${шаг.рисунок()}</div>` : ''}
          <div class="${ДОМ}-низ">${шаг.низ ? шаг.низ() : ''}</div>
        </div>`;
      if(i === ШАГИ.length-1) счётДо(ДОМ+'-балл', баллТура(), 800);
    }catch(e){ el.innerHTML=''; try{ console.error('тур 703:', e); }catch(_){} }
  }

  const ЛЕНТА = ШАГИ.map((ш,i)=> (i+1)+'. '+ш.т);

  window.WAVE_B = window.WAVE_B || {};
  window.WAVE_B[ID] = function(el){ рисовать(el); };

  (function(){
    const L = {
      id: ID, title:'Снежный городок', ico:'⛄',
      src:'Олимпиадные туры · Математика · 5 класс', subj:'tour',
      explain: ЛЕНТА,
      check: { q:'Сколько баллов можно получить за этот тур?',
               choices:['10','5','8','12'], ans:0,
               exp:'Пять задач по 2 балла — всего 10 баллов.' },
      tasks: []
    };
    const поставить = arr => {
      if(!arr || typeof arr.length!=='number') return arr;
      for(let i=0;i<arr.length;i++){ if(arr[i] && arr[i].id===ID){ arr[i]=L; return arr; } }
      arr.push(L);
      return arr;
    };
    let сейчас = поставить(window.ARH_LESSONS);
    try{
      Object.defineProperty(window,'ARH_LESSONS',{
        configurable:true, enumerable:true,
        get(){ return сейчас; },
        set(v){ сейчас = поставить(v); }
      });
    }catch(e){}
  })();
})();
