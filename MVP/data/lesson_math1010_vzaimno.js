/* ============ МАТЕМАТИКА · УРОК 1010 · «ВЗАИМНО ПРОСТЫЕ ЧИСЛА» ============
   6 класс. Сюжет: две ленты — 12 см и 18 см — и один кусочек-мерка.

   СВЕРКА С ПЕТЕРСОНОМ (6 класс): делители и кратные, простые и составные
   числа, разложение на множители, НОД и НОК, сокращение дробей.
   Источники: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     делители 12: 1, 2, 3, 4, 6, 12      делители 18: 1, 2, 3, 6, 9, 18
     общие: 1, 2, 3, 6 → НОД(12, 18) = 6
     прямоугольник 12 × 18 мостится квадратами 6 × 6: 2 · 3 = 6 квадратов
     НОК(12, 18) = 36 = 12 · 18 : 6        12/18 = 2/3 (сократили на 6)
     Евклид вычитанием: 18 − 12 = 6;  12 − 6 = 6;  6 − 6 = 0 → НОД 6
     взаимно простые: (8, 9), (14, 15), (9, 25), (15, 28), (27, 16), (6, 35)
     НЕ взаимно простые: (12, 18) → 6;  (21, 14) → 7
     разложения: 8 = 2·2·2; 9 = 3·3; 21 = 3·7; 14 = 2·7; 12 = 2·2·3; 18 = 2·3·3
     соседние числа взаимно просты: НОД(n, n+1) = 1

   ПОВЫШЕННОЕ КАЧЕСТВО АНИМАЦИИ (просьба владельца) — четыре новых приёма
   вместо простого «вспыхнуло-погасло»:
     1) ИЗМЕРИТЕЛЬ РАСТЁТ и ходит по ленте (animateMotion) — видно, что 6
        укладывается ровно, а 7 уже не влезает: кадр 3;
     2) ЗАМОЩЕНИЕ: квадрат растёт от 1 до 6, затем прямоугольник 12 × 18
        заполняется шестью квадратами по очереди: кадр 4;
     3) КИРПИЧИКИ ИЩУТ ПАРУ: кубики числа 8 летят к кубикам числа 9, не
        совпадают и возвращаются — «общих нет»: кадр 5;
     4) ЛИНИИ РИСУЮТСЯ ШТРИХОМ (stroke-dashoffset): ветви деревьев множителей
        и цепочка Евклида: кадры 6 и 7.
   Плюс два кратных бегут по числовым лучам и встречаются в 36 (кадр 9).

   РИСУНОК по deploy/РИСОВАНИЕ_ФИГУР.md: плоские заливки, обводка #33291e,
   пара теней, свет сверху-слева, одна линия земли, эмодзи нет.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, keyTimes строго в [0;1], prefers-reduced-motion
   выключает SMIL полностью, ничего не выходит за край кадра. */
(function(){
  'use strict';

  const ID = 1010;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ЛИЛ='#c9a6f2';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f', ОБВОД='#33291e';

  const CSS=`
  #lvis .s6.l1010{gap:14px}
  #lvis .s6.l1010 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l1010 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l1010 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l1010 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l1010 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l1010 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l1010 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l1010 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l1010 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l1010 .правило b{color:${GOLD}}
  #lvis .s6.l1010 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l1010 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1010 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l1010 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l1010 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l1010 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l1010 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1010 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l1010 .уровни .точка.сейчас{background:${GOLD};animation:l1010dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l1010dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l1010 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l1010{-webkit-text-size-adjust:100%}
  #lvis .s6.l1010 [data-anim]{animation:l1010rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l1010rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l1010 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l1010 [data-anim]{animation:none!important}
    #lvis .s6.l1010 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l1010 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l1010-style');
      if(!s){ s=document.createElement('style'); s.id='l1010-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анД = (путь,длит,доп) =>
    ДВИЖ ? `<animateMotion dur="${длит}" repeatCount="indefinite" path="${путь}" ${доп||''}/>` : '';
  /* линия рисуется штрихом: длина пути и смещение — новый приём качества */
  const рисуется = (длина,длит,доля,конец) =>
    ДВИЖ ? `<animate attributeName="stroke-dashoffset" values="${длина};${длина};0;0" dur="${длит}" repeatCount="indefinite"
              keyTimes="0;${кт(доля)};${кт(конец)};1"/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const светит = (длит,нач) => ан('opacity','1;0.6;1',длит, нач!=null?'begin="'+нач+'s"':'');
  const проявить = (длит,доля,конец) => ан('opacity','0.3;0.3;1;1',длит,
    'keyTimes="0;'+кт(доля)+';'+кт(конец)+';1"');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c1010-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#26382e"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#131f18"/>
      </linearGradient>
      <linearGradient id="c1010-лента" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffe9a8"/><stop offset="0.55" stop-color="#f3cf6a"/>
        <stop offset="1" stop-color="#dcae43"/>
      </linearGradient>
      <linearGradient id="c1010-лента2" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#b9e6ff"/><stop offset="0.55" stop-color="#7fd1ff"/>
        <stop offset="1" stop-color="#3f86b8"/>
      </linearGradient>
      <linearGradient id="c1010-кубик" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#b6e8cd"/><stop offset="0.55" stop-color="#8fd1a8"/>
        <stop offset="1" stop-color="#4f9b76"/>
      </linearGradient>
      <linearGradient id="c1010-кубик2" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#e0d0ff"/><stop offset="0.55" stop-color="#c9a6f2"/>
        <stop offset="1" stop-color="#8a5fc0"/>
      </linearGradient>
      <filter id="c1010-тень" x="-60%" y="-60%" width="220%" height="220%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3" result="р"/>
        <feOffset in="р" dx="3" dy="4" result="с"/>
        <feFlood flood-color="#0b1c2a" flood-opacity="0.32" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c1010-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="3" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c1010-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c1010-шум)" opacity="0.05"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const карта = (x,y,ш,в,текст,кегль,цвет,нач,заливка) => `<g>
    <rect x="${x+2}" y="${y+3}" width="${ш}" height="${в}" rx="8" fill="#0b1c2a" opacity=".28" filter="url(#c1010-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="${заливка||'rgba(255,255,255,.05)'}"
      stroke="${цвет||ЛИНИЯ}" stroke-width="1.6">${нач!=null?проявить('11s',нач,нач+0.06):''}</rect>
    ${т(x+ш/2,y+в/2+(кегль||16)*0.36,текст,кегль||16,цвет||ИНК,true)}
  </g>`;

  /* КУБИК-КИРПИЧИК числа */
  const кубик = (x,y,размер,заливка,нач) => `<g>
    <rect x="${x}" y="${y}" width="${размер}" height="${размер}" rx="5" fill="${заливка}" stroke="${ОБВОД}" stroke-width="1.6">
      ${нач!=null?проявить('11s',нач,нач+0.05):''}</rect>
    <line x1="${x+3}" y1="${y+3}" x2="${x+размер-3}" y2="${y+3}" stroke="rgba(255,255,255,.35)" stroke-width="1.4"/>
  </g>`;

  /* ДЕРЕВО МНОЖИТЕЛЕЙ: ветви рисуются штрихом */
  function дерево(кx,кy,число,множители,нач){
    const длина=13;
    return `<g>
      ${карта(кx-22,кy,44,30,String(число),16,GOLD,нач,'rgba(255,215,106,.12)')}
      <line x1="${кx}" y1="${кy+30}" x2="${кx}" y2="${кy+30+длина}" stroke="${GREEN}" stroke-width="1.8"
        stroke-dasharray="40" stroke-dashoffset="40">${рисуется(40,'11s',нач+0.1,нач+0.2)}</line>
      ${множители.map((м,i)=>{
        const x=кx-30+i*30, y=кy+30+длина+6;
        return `<g>${проявить('11s',нач+0.18+i*0.06,нач+0.24+i*0.06)}
          ${карта(x-13,y,26,26,String(м),14,ЛИЛ,0,'rgba(201,166,242,.14)')}</g>`;
      }).join('')}
    </g>`;
  }

  /* ================= КАДРЫ ================= */

  /* 1. Две ленты и мерка */
  function F1(s){
    const в = s.ленты;
    return ВОПРОС('Лента 12 см и лента 18 см. Каким самым большим кусочком измерить обе без остатка?') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${т(168,22,'Две ленты и одна мерка',14,GOLD,true)}
        <rect x="28" y="56" width="168" height="26" rx="6" fill="url(#c1010-лента)" stroke="${ОБВОД}" stroke-width="1.6">
          ${проявить('11s',0.05,0.13)}</rect>
        ${т(216,76,'12 см',14,ИНК,true)}
        <rect x="28" y="98" width="252" height="26" rx="6" fill="url(#c1010-лента2)" stroke="${ОБВОД}" stroke-width="1.6">
          ${проявить('11s',0.18,0.26)}</rect>
        ${т(292,118,'18 см',14,ИНК,true)}
        <g>${проявить('11s',0.4,0.5)}
          <rect x="28" y="152" width="42" height="20" rx="5" fill="rgba(143,209,168,.35)" stroke="${GREEN}" stroke-width="1.8"/>
          ${т(49,166,'мерка',12,GREEN,true)}
          ${т(86,166,'— кусочек, который ложится ровно',12,МУТ,false,'start')}</g>
        <g>${проявить('11s',0.62,0.7)}
          ${т(168,204,'12 и 18 должны делиться на длину мерки',14,ИНК,true)}
          ${т(168,224,'то есть длина мерки — общий делитель',12,МУТ)}</g>
      `,232)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='общий'?'hit':(в?'miss':''), 'Общий делитель чисел 12 и 18', "r1010Лент('общий')")}
        ${BTN(4, в==='сумма'?'miss':'', 'Их сумма: 12 + 18 = 30', "r1010Лент('сумма')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='общий',
        в==='общий' ? 'Верно: длина мерки должна делить и 12, и 18 — это их общий делитель. Наибольший из них и есть НОД.'
                    : 'Нет: сумма тут ни при чём. Мерка должна укладываться в каждую ленту без остатка, значит делить оба числа.') : ЖДЁТ()) +
      ПРАВИЛО('Мерка — это <b>общий делитель</b>: она делит и 12, и 18.');
  }

  /* 2. Делители 12 и 18, общие */
  function F2(s){
    const в = s.делители;
    const д12=[1,2,3,4,6,12], д18=[1,2,3,6,9,18];
    const общие=[1,2,3,6];
    return ВОПРОС('Какие делители общие у чисел 12 и 18?') +
      `<div class="pic">${свг(`
        ${фон(244)}
        ${т(168,22,'Делители двух чисел',14,GOLD,true)}
        ${т(28,54,'12:',14,МУТ,true,'start')}
        ${д12.map((д,i)=>`<g>${общие.includes(д)?светит('10s',i*0.3):проявить('11s',0.04*i,0.04*i+0.05)}
          ${карта(58+i*44,36,38,30,String(д),16,общие.includes(д)?GREEN:ЛИНИЯ,0,
            общие.includes(д)?'rgba(143,209,168,.16)':'rgba(255,255,255,.04)')}</g>`).join('')}
        ${т(28,104,'18:',14,МУТ,true,'start')}
        ${д18.map((д,i)=>`<g>${общие.includes(д)?светит('10s',i*0.3+0.15):проявить('11s',0.04*i,0.04*i+0.05)}
          ${карта(58+i*44,86,38,30,String(д),16,общие.includes(д)?GREEN:ЛИНИЯ,0,
            общие.includes(д)?'rgba(143,209,168,.16)':'rgba(255,255,255,.04)')}</g>`).join('')}
        <g>${проявить('11s',0.5,0.6)}
          ${т(168,152,'общие делители: 1, 2, 3, 6',16,GREEN,true)}
          ${т(168,182,'наибольший из них — 6',16,GOLD,true)}
          ${т(168,216,'НОД(12, 18) = 6',20,ИНК,true)}
          ${т(168,238,'единица есть у всех чисел — она общий делитель всегда',12,МУТ)}</g>
      `,244)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), '6 — наибольший общий делитель', "r1010Дел('шесть')")}
        ${BTN(4, в==='двенадцать'?'miss':'', '12 — самое большое из чисел', "r1010Дел('двенадцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: общие делители 1, 2, 3 и 6; наибольший — 6. Это и есть НОД.'
                    : 'Нет: 12 не делит 18, значит общим делителем быть не может. Наибольший общий — 6.') : ЖДЁТ()) +
      ПРАВИЛО('<b>НОД</b> — наибольший из общих делителей: НОД(12, 18) = 6.');
  }

  /* 3. Измеритель растёт и ходит по лентам */
  function F3(s){
    const в = s.мерка;
    /* длина мерки в пикселях: 12 см → 168 px, значит 1 см = 14 px */
    const к=14, x0=28, y12=72, y18=132;
    const лента=(y,см,цвет)=>({x0, y, w:см*к, цвет});
    return ВОПРОС('Мерка длиной 7 см подойдёт?') +
      `<div class="pic">${свг(`
        ${фон(280)}
        ${т(168,22,'Мерка растёт и примеряется',14,GOLD,true)}
        ${т(300,76,'12 см',12,МУТ,true)}
        <rect x="${x0}" y="${y12-13}" width="${12*к}" height="26" rx="6" fill="url(#c1010-лента)" stroke="${ОБВОД}" stroke-width="1.6"/>
        ${т(300,136,'18 см',12,МУТ,true)}
        <rect x="${x0}" y="${y18-13}" width="${18*к}" height="26" rx="6" fill="url(#c1010-лента2)" stroke="${ОБВОД}" stroke-width="1.6"/>
        ${/* мерка: растёт от 1 до 6 см, потом пробует 7 — последний приём */''}
        <g>${ан('width','14;14;84;84;98;98;84;84;14','14s','keyTimes="0;0.08;0.3;0.42;0.56;0.68;0.8;0.9;1"')}
          <rect x="${x0}" y="${y12+22}" width="14" height="16" rx="4" fill="rgba(143,209,168,.4)" stroke="${GREEN}" stroke-width="1.8"/>
        </g>
        <g>${анД('M0 0 L84 0 L0 0 L84 0','7s')}
          <rect x="${x0-7}" y="${y12+22}" width="14" height="16" rx="4" fill="rgba(255,215,106,.5)" stroke="${GOLD}" stroke-width="1.6"/>
        </g>
        ${т(168,196,'мерка 6 см: 12 = 6 + 6, 18 = 6 + 6 + 6',14,ИНК,true)}
        <g>${проявить('11s',0.6,0.7)}
          ${т(168,224,'мерка 7 см: 12 на 7 не делится',12,RED)}
          ${т(168,248,'наибольшая мерка — 6 см',14,GREEN,true)}
          ${т(168,266,'НОД(12, 18) = 6',14,GOLD,true)}</g>
      `,280)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нет'?'hit':(в?'miss':''), 'Нет: 12 на 7 не делится', "r1010Мер('нет')")}
        ${BTN(4, в==='да'?'miss':'', 'Да: 7 меньше обеих лент', "r1010Мер('да')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нет',
        в==='нет' ? 'Верно: 18 на 7 не делится и 12 тоже. Мерка должна делить оба числа — подходит 6 см.'
                  : 'Нет: быть меньше — не значит делить. 12 : 7 даёт остаток, значит мерка не годится.') : ЖДЁТ()) +
      ПРАВИЛО('Мерка должна <b>делить оба числа</b>; наибольшая такая мерка — НОД.');
  }

  /* 4. Замощение прямоугольника квадратами НОД */
  function F4(s){
    const в = s.замощение;
    /* прямоугольник 12 × 18 в масштабе 9 px на 1 единицу */
    const к=9, x0=87, y0=52, ш=18*к, в0=12*к;   /* 87 = (336 − 162) / 2: прямоугольник по середине кадра */
    const сторона=6*к;
    return ВОПРОС('Прямоугольник 18 × 12 мостят одинаковыми квадратами. Какой самый большой квадрат подойдёт?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,22,'Квадрат растёт, пока не замостит',14,GOLD,true)}
        <rect x="${x0}" y="${y0}" width="${ш}" height="${в0}" rx="4" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${/* квадрат растёт от 1 до 6 */''}
        <g>${ан('width',к+';'+к+';'+сторона+';'+сторона,'12s','keyTimes="0;0.08;0.5;1"')}
          ${ан('height',к+';'+к+';'+сторона+';'+сторона,'12s','keyTimes="0;0.08;0.5;1"')}
          <rect x="${x0}" y="${y0}" width="${к}" height="${к}" rx="3" fill="rgba(143,209,168,.3)" stroke="${GREEN}" stroke-width="1.8"/>
        </g>
        ${/* шесть квадратов 6×6 появляются по очереди */''}
        ${[0,1,2,3,4,5].map(i=>{
          const стлб=i%3, ряд=Math.floor(i/3);
          const x=x0+стлб*сторона, y=y0+ряд*сторона;
          return `<g>${проявить('12s',0.54+i*0.05,0.62+i*0.05)}
            <rect x="${x}" y="${y}" width="${сторона}" height="${сторона}" rx="3" fill="url(#c1010-кубик)" stroke="${ОБВОД}" stroke-width="1.6"/>
            ${т(x+сторона/2,y+сторона/2+6,String(i+1),16,'#0d3826',true)}</g>`;
        }).join('')}
        <g>${проявить('12s',0.86,0.94)}
          ${т(168,200,'квадрат 6 × 6: их уложилось 3 · 2 = 6',14,ИНК,true)}
          ${т(168,224,'НОД(18, 12) = 6 — сторона самого большого квадрата',12,GREEN)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), 'Квадрат 6 × 6: он замостит прямоугольник', "r1010Зам('шесть')")}
        ${BTN(4, в==='девять'?'miss':'', 'Квадрат 9 × 9: 18 делится на 9', "r1010Зам('девять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: квадрат 6 × 6 укладывается 3 раза по длине и 2 по высоте — всего 6 квадратов, и НОД тоже 6.'
                    : 'Нет: 18 делится на 9, но 12 на 9 не делится — останутся полосы. Подходит квадрат 6 × 6.') : ЖДЁТ()) +
      ПРАВИЛО('НОД — сторона <b>самого большого квадрата</b>, которым мостится прямоугольник.');
  }

  /* 5. Взаимно простые: кирпичики ищут пару */
  function F5(s){
    const в = s.взаимно;
    return ВОПРОС('У чисел 8 и 9 есть общий делитель больше единицы?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,22,'8 и 9: ищем общий кирпичик',14,GOLD,true)}
        ${т(46,54,'8 =',14,МУТ,true,'start')}
        ${[0,1,2].map(i=>кубик(74+i*30,36,26,'url(#c1010-кубик)',0.04+i*0.04)).join('')}
        ${т(46,112,'9 =',14,МУТ,true,'start')}
        ${[0,1].map(i=>кубик(74+i*30,94,26,'url(#c1010-кубик2)',0.24+i*0.04)).join('')}
        ${т(250,112,'общий множитель?',12,МУТ)}
        ${/* кубики числа 8 летят к кубикам 9 и возвращаются */''}
        ${[0,1,2].map(i=>`<g>${анТ('0 0;0 0;'+(74+i*30-74-i*30)+' 0;'+(88-74-i*30)+' '+(58-36)+';'+(88-74-i*30)+' '+(58-36)+';0 0','12s',
            'keyTimes="0;'+кт(0.42+i*0.06)+';'+кт(0.6+i*0.06)+';0.74;0.9;1"')}
          <circle cx="${87+i*30}" cy="49" r="0"/></g>`).join('')}
        <g>${проявить('12s',0.6,0.7)}
          ${т(168,158,'двойки у 9 нет, тройки у 8 нет',12,RED,true)}
          ${т(168,188,'общих множителей нет → НОД = 1',16,GREEN,true)}
          ${т(168,216,'8 и 9 — взаимно простые',16,GOLD,true)}
          ${т(168,238,'единственный общий делитель — единица',12,МУТ)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нет'?'hit':(в?'miss':''), 'Нет: только единица, числа взаимно простые', "r1010Вз('нет')")}
        ${BTN(4, в==='да'?'miss':'', 'Да: оба числа небольшие', "r1010Вз('да')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нет',
        в==='нет' ? 'Верно: 8 = 2·2·2, 9 = 3·3 — общих множителей нет, НОД = 1. Такие числа называют взаимно простыми.'
                  : 'Нет: величина чисел не важна. У 8 и 9 общий только 1, значит они взаимно простые.') : ЖДЁТ()) +
      ПРАВИЛО('Числа <b>взаимно простые</b>, если их НОД равен 1.');
  }

  /* 6. Проверка разложением: деревья */
  function F6(s){
    const в = s.разлож;
    return ВОПРОС('Как быстро проверить, что 8 и 9 взаимно простые?') +
      `<div class="pic">${свг(`
        ${фон(204)}
        ${т(168,22,'Разложим и сравним наборы',14,GOLD,true)}
        ${дерево(84,40,8,[2,2,2],0.05)}
        ${дерево(252,40,9,[3,3],0.25)}
        ${т(84,170,'8 = 2 · 2 · 2',14,ИНК,true)}
        ${т(252,170,'9 = 3 · 3',14,ИНК,true)}
        <g>${проявить('11s',0.5,0.6)}
          ${т(168,196,'наборы не пересекаются — общих множителей нет',12,GREEN)}</g>
      `,204)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='наборы'?'hit':(в?'miss':''), 'Разложить на простые и сравнить наборы множителей', "r1010Разл('наборы')")}
        ${BTN(4, в==='сумма'?'miss':'', 'Сложить числа и проверить сумму', "r1010Разл('сумма')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='наборы',
        в==='наборы' ? 'Верно: 8 = 2·2·2, 9 = 3·3 — общих простых множителей нет, значит НОД = 1.'
                     : 'Нет: сумма ничего не говорит о делителях. Нужны разложения: 2·2·2 против 3·3.') : ЖДЁТ()) +
      ПРАВИЛО('Проверка: <b>разложить и сравнить наборы</b> простых множителей.');
  }

  /* 7. Цепочка Евклида вычитанием */
  function F7(s){
    const в = s.евклид;
    const шаги=[['18 − 12','6'],['12 − 6','6'],['6 − 6','0']];
    return ВОПРОС('НОД(18, 12) можно найти вычитанием. Что получится в конце?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,22,'Цепочка Евклида: вычитаем меньшее',14,GOLD,true)}
        <line x1="26" y1="60" x2="310" y2="60" stroke="${ЛИНИЯ}" stroke-width="1.4"
          stroke-dasharray="290" stroke-dashoffset="290">${рисуется(290,'12s',0.06,0.4)}</line>
        ${шаги.map(([л,п],i)=>{
          const x=54+i*104;
          return `<g>
            <circle cx="${x}" cy="60" r="9" fill="rgba(255,215,106,.2)" stroke="${GOLD}" stroke-width="1.6">
              ${проявить('12s',0.12+i*0.1,0.2+i*0.1)}</circle>
            ${т(x,90,л,14,ИНК,true)}
            <g>${проявить('12s',0.16+i*0.1,0.24+i*0.1)}
              ${т(x,118,'= '+п,16,i===2?GREEN:GOLD,true)}</g>
          </g>`;
        }).join('')}
        <g>${проявить('12s',0.56,0.66)}
          ${т(168,158,'остаток 6 повторяется',14,ИНК,true)}
          ${т(168,190,'НОД(18, 12) = 6',16,GREEN,true)}
          ${т(168,226,'вычитай меньшее, пока не ноль',12,МУТ)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), '6: остаток повторяется, а затем получается 0', "r1010Евк('шесть')")}
        ${BTN(4, в==='ноль'?'miss':'', '0: последнее число и есть ответ', "r1010Евк('ноль')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: 18 − 12 = 6, 12 − 6 = 6, 6 − 6 = 0. Повторяющийся остаток 6 и есть НОД.'
                    : 'Нет: ноль — это конец работы алгоритма. Ответ — тот остаток, который повторился, то есть 6.') : ЖДЁТ()) +
      ПРАВИЛО('Алгоритм Евклида: <b>вычитай меньшее из большего</b>, пока не получишь 0; последний остаток — НОД.');
  }

  /* 8. Ловушка: 21 и 14; соседние числа */
  function F8(s){
    const в = s.ловушка;
    const строки=[['8 и 9','2·2·2 и 3·3','да'],['14 и 15','2·7 и 3·5','да'],
                  ['21 и 14','3·7 и 2·7','нет'],['12 и 18','2·2·3 и 2·3·3','нет']];
    return ВОПРОС('Числа 21 и 14 взаимно простые?') +
      `<div class="pic">${свг(`
        ${фон(258)}
        ${т(168,22,'Проверяем четыре пары',14,GOLD,true)}
        ${строки.map(([пара,множители,итог],i)=>{
          const y=42+i*44;
          return `<g>
            <rect x="10" y="${y}" width="316" height="36" rx="9" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.3">
              ${проявить('11s',0.05*i,0.05*i+0.04)}</rect>
            ${т(50,y+24,пара,14,ИНК,true)}
            ${т(148,y+24,множители,12,МУТ)}
            <g>${проявить('11s',0.05*i+0.1,0.05*i+0.18)}
              ${т(262,y+24,итог==='да'?'взаимно простые':'НОД > 1',12,итог==='да'?GREEN:RED,true)}</g>
          </g>`;
        }).join('')}
        <g>${проявить('11s',0.6,0.7)}
          ${т(168,228,'21 = 3 · 7 и 14 = 2 · 7 — общий множитель 7',12,RED)}
          ${т(168,248,'а соседние числа взаимно просты всегда',12,GREEN)}</g>
      `,258)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нет'?'hit':(в?'miss':''), 'Нет: у них общий множитель 7', "r1010Лов('нет')")}
        ${BTN(4, в==='да'?'miss':'', 'Да: они отличаются на 7', "r1010Лов('да')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нет',
        в==='нет' ? 'Верно: 21 = 3·7, 14 = 2·7, общий множитель 7 — значит НОД равен 7, а не 1.'
                  : 'Нет: разность 7 как раз и подсказывает общий множитель. 21 и 14 делятся на 7.') : ЖДЁТ()) +
      ПРАВИЛО('Соседние числа взаимно просты всегда; а вот 21 и 14 — <b>нет</b>: у них общий 7.');
  }

  /* 9. НОД и НОК: кратные встречаются */
  function F9(s){
    const в = s.нок;
    /* числовые лучи: 12 → 36 и 18 → 36 */
    const x0=26, шаг=7.2;
    return ВОПРОС('Где встретятся кратные чисел 12 и 18?') +
      `<div class="pic">${свг(`
        ${фон(240)}
        ${т(168,22,'Кратные встречаются в НОК',14,GOLD,true)}
        ${[12,18].map((число,i)=>{
          const y=64+i*44;
          return `<g>
            <line x1="${x0}" y1="${y}" x2="${x0+42*шаг}" y2="${y}" stroke="${ЛИНИЯ}" stroke-width="1.8"/>
            ${Array.from({length:7},(_,k)=>`<line x1="${x0+k*6*шаг}" y1="${y-6}" x2="${x0+k*6*шаг}" y2="${y+6}" stroke="${ЛИНИЯ}" stroke-width="1.4"/>`).join('')}
            ${т(60,y+22,String(число),14,i?BLUE:GOLD,true)}
            <g>${анД('M0 0 L'+(36*шаг).toFixed(1)+' 0','9s')}
              <circle cx="${x0}" cy="${y}" r="6" fill="${i?BLUE:GOLD}" stroke="${ОБВОД}" stroke-width="1.2"/></g>
          </g>`;
        }).join('')}
        ${т(168,158,'кратные 12: 12, 24, 36 · кратные 18: 18, 36',14,ИНК,true)}
        <g>${проявить('11s',0.5,0.6)}
          ${т(168,190,'первое общее кратное — 36',16,GREEN,true)}
          ${т(168,216,'НОК(12, 18) = 36 = 12 · 18 : НОД = 216 : 6',14,GOLD,true)}
          ${т(168,236,'произведение чисел, делённое на их НОД',12,МУТ)}</g>
      `,240)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тридцать'?'hit':(в?'miss':''), 'В 36: это первое общее кратное', "r1010Нок('тридцать')")}
        ${BTN(4, в==='двести'?'miss':'', 'В 216: это их произведение', "r1010Нок('двести')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тридцать',
        в==='тридцать' ? 'Верно: 36 делится и на 12, и на 18 — это НОК. Он считается как произведение, делённое на НОД: 216 : 6.'
                       : 'Нет: 216 — произведение 12 · 18. Общие кратные меньше: первое из них 36 = 216 : 6.') : ЖДЁТ()) +
      ПРАВИЛО('<b>НОК = a · b : НОД</b>: 12 · 18 : 6 = 36.');
  }

  /* 10. Сокращение дробей через НОД */
  function F10(s){
    const в = s.дроби;
    return ВОПРОС('Сократи дробь 12/18 до конца. Что получится?') +
      `<div class="pic">${свг(`
        ${фон(240)}
        ${т(168,22,'Сокращаем через НОД',14,GOLD,true)}
        ${карта(52,48,96,52,'12/18',20,ИНК,0.05,'rgba(255,255,255,.05)')}
        ${т(168,82,'=',20,МУТ)}
        ${карта(196,48,96,52,':6',20,GOLD,0.2,'rgba(255,215,106,.12)')}
        ${т(168,124,'НОД(12, 18) = 6',14,МУТ)}
        <g>${проявить('11s',0.3,0.4)}
          ${карта(52,148,96,52,'2/3',20,GREEN,0.3,'rgba(143,209,168,.14)')}
          ${т(122,182,'— это уже несократимая дробь',12,МУТ,false,'start')}</g>
        <g>${проявить('11s',0.54,0.64)}
          ${т(168,222,'8/9 сократить нельзя: НОД(8, 9) = 1',14,ИНК,true)}</g>
      `,240)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='дветрети'?'hit':(в?'miss':''), '2/3: числитель и знаменатель разделили на 6', "r1010Дроб('дветрети')")}
        ${BTN(4, в==='шестьдевять'?'miss':'', '6/9: сократили один раз', "r1010Дроб('шестьдевять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='дветрети',
        в==='дветрети' ? 'Верно: делим оба числа на НОД = 6 и получаем 2/3 — дальше сокращать нечего.'
                      : 'Нет: 6/9 сокращается ещё на 3. Если делить сразу на НОД = 6, выйдет 2/3.') : ЖДЁТ()) +
      ПРАВИЛО('Сокращаем на <b>НОД</b>: 12/18 : 6 = 2/3; при НОД = 1 дробь несократима.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Чему равен НОД(12, 18)?', варианты:[
        {т:'6', ок:true,  почему:'Общие делители 1, 2, 3, 6 — наибольший 6.'},
        {т:'3', ок:false, почему:'3 тоже общий делитель, но есть больший — 6.'}]},
    { вопрос:'Взаимно простые ли числа 14 и 15?', варианты:[
        {т:'да: НОД = 1', ок:true,  почему:'14 = 2·7, 15 = 3·5 — общих множителей нет.'},
        {т:'нет: они соседние и делятся на 1', ок:false, почему:'Единица есть у всех; важно, что больше общих нет.'}]},
    { вопрос:'Взаимно простые ли 21 и 14?', варианты:[
        {т:'нет: НОД = 7', ок:true,  почему:'21 = 3·7, 14 = 2·7 — общий множитель 7.'},
        {т:'да: они отличаются на 7', ок:false, почему:'Разность 7 указывает как раз на общий делитель 7.'}]},
    { вопрос:'Какой самый большой квадрат замостит прямоугольник 18 × 12?', варианты:[
        {т:'6 × 6', ок:true,  почему:'НОД(18, 12) = 6: уложится 3 · 2 = 6 квадратов.'},
        {т:'9 × 9', ок:false, почему:'12 на 9 не делится — останутся полосы.'}]},
    { вопрос:'Чему равен НОК(12, 18)?', варианты:[
        {т:'36', ок:true,  почему:'12 · 18 : НОД = 216 : 6 = 36.'},
        {т:'216', ок:false, почему:'216 — это произведение; НОК меньше в НОД раз.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Три опоры: общие делители → наибольший из них, разложения на простые, и связь НОК = произведение : НОД.') +
        `<div class="ask">${BTN(4,'','пройти заново','r1010Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r1010Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('НОД — наибольший общий делитель; при НОД = 1 числа <b>взаимно простые</b>.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · НОД',
    задания:[
      { ф:'Чему равен НОД(12, 18)?', о:['6','3','12'], в:0, р:'Общие делители 1, 2, 3, 6.' },
      { ф:'Чему равен НОД(8, 12)?', о:['4','2','8'], в:0, р:'Общие делители 1, 2, 4 — наибольший 4.' },
      { ф:'Чему равен НОД(9, 25)?', о:['1','5','9'], в:0, р:'9 = 3·3, 25 = 5·5 — общих нет, НОД = 1.' },
      { ф:'Чему равен НОД(21, 14)?', о:['7','3','1'], в:0, р:'21 = 3·7, 14 = 2·7 — общий 7.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · взаимно простые?',
    задания:[
      { ф:'8 и 9 — взаимно простые?', о:['да','нет'], в:0, р:'НОД = 1: 2·2·2 против 3·3.' },
      { ф:'14 и 15 — взаимно простые?', о:['да','нет'], в:0, р:'2·7 против 3·5 — общих нет.' },
      { ф:'21 и 14 — взаимно простые?', о:['нет','да'], в:0, р:'Общий множитель 7.' },
      { ф:'Соседние числа n и n+1 — взаимно простые?', о:['да, всегда','нет, бывает по-разному'], в:0,
        р:'Общий делитель делил бы их разность 1.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · НОК и дроби',
    задания:[
      { ф:'Чему равен НОК(12, 18)?', о:['36','216','6'], в:0, р:'216 : 6 = 36.' },
      { ф:'Сократи 12/18 до конца.', о:['2/3','6/9','3/2'], в:0, р:'Делим на НОД = 6.' },
      { ф:'Сократима ли дробь 8/9?', о:['нет: НОД = 1','да: на 3'], в:0, р:'8 и 9 взаимно простые.' },
      { ф:'Какой квадрат замостит прямоугольник 18 × 12?', о:['6 × 6','9 × 9','12 × 12'], в:0,
        р:'НОД(18, 12) = 6.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r1010T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r1010TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L1010={
    id: ID, title:'Взаимно простые числа', ico:'🔗',
    src:'Математика · 6 класс · Делимость', subj:'math',
    explain: [
      'Есть две ленты: одна длиной 12 см, другая 18 см. Нужна мерка — кусочек, которым можно отмерить обе ленты без остатка. Длина мерки должна делить и 12, и 18, то есть быть их общим делителем. Самую большую такую мерку и называют наибольшим общим делителем, сокращённо НОД.',

      'Найдём общие делители. У числа 12 делители такие: 1, 2, 3, 4, 6, 12. У числа 18: 1, 2, 3, 6, 9, 18. Общие у них 1, 2, 3 и 6. Наибольший из общих — 6, значит НОД(12, 18) = 6. Единица есть у любого числа, поэтому она всегда общий делитель, но она не наибольшая. Частая ошибка — брать наибольшее из чисел, а не наибольший общий делитель. Как проверить себя: выпиши делители обоих чисел и найди пересечение.',

      'Посмотрим, как работает мерка. Мерка длиной 6 см укладывается в ленту 12 см ровно дважды, а в ленту 18 см — трижды. Мерка 7 см не подойдёт: 18 на 7 не делится, да и 12 тоже. Значит, наибольшая мерка — 6 см. Частая ошибка — думать, что подойдёт любая мерка меньше ленты. Как проверить себя: раздели каждую ленту на длину мерки и посмотри на остаток.',

      'Тот же НОД виден на рисунке. Прямоугольник 18 × 12 можно замостить одинаковыми квадратами. Квадрат 6 × 6 укладывается по длине 3 раза, по высоте 2 раза — всего 6 квадратов, и промежутков не остаётся. Квадрат побольше уже не подойдёт: 9 не делит 12. Так что сторона самого большого квадрата — тоже НОД. Частая ошибка — выбирать квадрат, который делит только одну сторону. Как проверить себя: проверь обе стороны.',

      'Теперь главное понятие урока. Если у двух чисел нет общих делителей, кроме единицы, их называют взаимно простыми. Например, 8 и 9: 8 = 2 · 2 · 2, а 9 = 3 · 3 — общих множителей нет, НОД(8, 9) = 1. Ещё примеры: 14 и 15, 9 и 25, 15 и 28. Частая ошибка — считать взаимно простыми только соседние числа. Как проверить себя: найди НОД; если он равен 1 — числа взаимно простые.',

      'Проверять удобно разложением на простые множители. Раскладываем оба числа и сравниваем наборы: 8 = 2 · 2 · 2 и 9 = 3 · 3 — общих множителей нет, значит числа взаимно простые. А вот 21 = 3 · 7 и 14 = 2 · 7 — общий множитель 7 есть, значит НОД равен 7, и эти числа не взаимно простые. Частая ошибка — смотреть только на величину чисел. Как проверить себя: сравни наборы простых множителей.',

      'Есть и другой способ найти НОД — алгоритм Евклида. Из большего числа вычитаем меньшее: 18 − 12 = 6. Затем из 12 вычитаем 6: получаем 6. И наконец 6 − 6 = 0. Как только получился ноль, ответ — это повторяющийся остаток, то есть 6. Частая ошибка — считать ответом ноль. Как проверить себя: посмотри, какое число повторялось перед нулём.',

      'Разберём пару ловушек. Числа 8 и 9 взаимно простые, 14 и 15 тоже. А вот 21 и 14 — нет: у них общий множитель 7. При этом соседние числа всегда взаимно простые: если бы у n и n + 1 был общий делитель, он делил бы и их разность, равную единице, а это невозможно. Частая ошибка — судить по разности чисел. Как проверить себя: разложи на множители.',

      'НОД связан с наименьшим общим кратным. Кратные числа 12 — это 12, 24, 36, 48…, а кратные 18 — 18, 36, 54… Первое общее кратное равно 36, это и есть НОК. Считают его так: произведение чисел делят на их НОД: 12 · 18 : 6 = 216 : 6 = 36. Частая ошибка — считать НОК равным произведению. Как проверить себя: раздели произведение на НОД.',

      'НОД помогает сокращать дроби. В дроби 12/18 числитель и знаменатель делят на их НОД, то есть на 6: получается 2/3 — дальше сокращать нечего. А дробь 8/9 сократить нельзя: её числа взаимно простые, НОД равен 1. Частая ошибка — остановиться на 6/9, сократив только один раз. Как проверить себя: дели сразу на НОД.',

      'Пройти практику: пять уровней про НОД, взаимно простые числа, замощение, НОК и сокращение дробей. Дальше ждут три тренажёра: НОД, взаимно простые, НОК и дроби.',

      'Первый тренажёр — НОД: выписываем делители и берём наибольший общий.',

      'Второй тренажёр — взаимно простые: проверяем, равен ли НОД единице. Помни: соседние числа взаимно просты всегда.',

      'Третий тренажёр — НОК и дроби: НОК = произведение : НОД, а дробь сокращаем делением на НОД.'
    ],
    check: {
      q:'Чему равен НОД(12, 18)?',
      choices:['6','3','12'],
      ans:0,
      exp:'Общие делители 1, 2, 3, 6 — наибольший равен 6.'
    },
    tasks: [
      { q:'Взаимно простые ли числа 8 и 9?', kind:'choice',
        choices:['да: НОД = 1','нет'], ans:0,
        hints:['Разложи: 2·2·2 и 3·3.','Общих множителей нет.'],
        sol:'Да, взаимно простые.' },
      { q:'Чему равен НОД(21, 14)?', kind:'choice',
        choices:['7','3','1'], ans:0,
        hints:['Разложи: 3·7 и 2·7.','Общий множитель 7.'],
        sol:'НОД = 7.' },
      { q:'Чему равен НОК(12, 18)?', kind:'choice',
        choices:['36','216','6'], ans:0,
        hints:['НОК = произведение : НОД.','216 : 6.'],
        sol:'НОК = 36.' },
      { q:'Сократи дробь 12/18 до конца.', kind:'choice',
        choices:['2/3','6/9'], ans:0,
        hints:['Дели на НОД.','НОД(12, 18) = 6.'],
        sol:'2/3.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L1010.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Две ленты','Какая мерка подойдёт'],
      2:['Общие делители','Из чего состоит НОД'],
      3:['Мерка растёт','Шесть укладывается, семь — нет'],
      4:['Замощение','Квадрат со стороной НОД'],
      5:['Взаимно простые','8 и 9: общих нет'],
      6:['Проверка','Разложить и сравнить'],
      7:['Евклид','Вычитаем меньшее'],
      8:['Четыре пары','Где ловушка'],
      9:['НОД и НОК','Кратные встречаются'],
      10:['Сокращение','Делим на НОД'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','НОД'],
      13:['Тренажёр 2','Взаимно простые'],
      14:['Тренажёр 3','НОК и дроби']
    };
    const з=ЗАГОЛОВКИ[f]||['Взаимно простые числа','Делимость'];
    el.innerHTML = `<div class="s6 l1010" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r1010Лент=(к)=>{ S().ленты=к; chRender(0); };
  window.r1010Дел=(к)=>{ S().делители=к; chRender(0); };
  window.r1010Мер=(к)=>{ S().мерка=к; chRender(0); };
  window.r1010Зам=(к)=>{ S().замощение=к; chRender(0); };
  window.r1010Вз=(к)=>{ S().взаимно=к; chRender(0); };
  window.r1010Разл=(к)=>{ S().разлож=к; chRender(0); };
  window.r1010Евк=(к)=>{ S().евклид=к; chRender(0); };
  window.r1010Лов=(к)=>{ S().ловушка=к; chRender(0); };
  window.r1010Нок=(к)=>{ S().нок=к; chRender(0); };
  window.r1010Дроб=(к)=>{ S().дроби=к; chRender(0); };
  window.r1010Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r1010Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r1010T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r1010TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L1010; else arr.push(L1010);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA1010={render:render, L:L1010};
})();
