/* ====== МАТЕМАТИКА · УРОК 821 · «ВРЕМЯ: РАСПИСАНИЯ И ПОЯСА» ===============
   4 класс. Тема была заглушкой «готовится» в MVP/data/soon_lessons.js.

   ПОЧЕМУ КРУГ. Вся трудность темы в одном месте: в полуночи. Ребёнок пишет
   «21:40 + 8 = 29:40», потому что складывает числа, а не время. Значит, сутки
   должны перестать быть числом и стать КРУГОМ: двадцать четыре часа — полный
   оборот, полночь — одна точка наверху, и стрелка через неё проходит, а не
   упирается. Тогда «перевалить за полночь» — это видимое действие, и дуга
   сама делится швом на две части: 2 ч 20 мин до полуночи и 5 ч 40 мин после.

   Цвет дуги меняется ровно на шве: до полуночи она ночная, после — рассветная.
   Это не украшение: именно по цвету видно, где кончились одни сутки.

   Второй предмет урока — ЛЕНТА ВАХТ: те же часы, вытянутые в прямую, потому
   что расписание читают строкой, а не кругом. Третий — ПОЯСА: полоса столбцов,
   где солнце стоит над полуднем, а к востоку часы уходят вперёд.

   СЮЖЕТ ПОД МИР 4 КЛАССА В «ПУТИ» («Открытое море»): ночной переход до
   Сиракуз. Вахтенный журнал висит над каждым кадром: три записи с отметками.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     приход:   21:40 + 8 ч = 05:40 следующих суток
               разрез у полуночи: 2 ч 20 мин + 5 ч 40 мин = 8 ч ✓
     проверка: 05:40 − 8 ч = 21:40 предыдущих суток ✓
     сколько:  с 22:15 до 03:05 → 1 ч 45 мин + 3 ч 05 мин = 4 ч 50 мин ✓
     вахты:    по 4 часа от 20:00 → 20–24, 00–04, 04–08 ✓
     пояса:    21:40 + 2 пояса к востоку = 23:40; + 3 пояса = 00:40 ✓
     итог:     05:40 + 2 пояса = 07:40 по местному времени ✓

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md: одно главное движение на кадр и одна
   вспышка результата. prefers-reduced-motion выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 821;
  const ЯНТ='#ffc861', GREEN='#8fd1a8', RED='#e86a5a', НОЧЬ='#7e8fe0', ЗАРЯ='#ff9f6b',
        ЛУНА='#dfe6ff', СОЛНЦЕ='#ffd76a';
  const ИНК='#f2f4ff', МУТ='#b3bbdd', ЛИНИЯ='#4d5a93', ОБВОД='#141830';

  /* ---------- ВРЕМЯ ---------- */
  const СТАРТ = 21*60+40;              /* 21:40 */
  const всутках = (м) => ((м % 1440) + 1440) % 1440;
  const чч = (м) => {
    const t = всутках(м);
    return String(Math.floor(t/60)).padStart(2,'0') + ':' + String(t%60).padStart(2,'0');
  };
  const суток = (м) => Math.floor(м/1440);
  const длит = (м) => { const ч=Math.floor(м/60), мн=м%60;
    return (ч?ч+' ч':'') + (ч&&мн?' ':'') + (мн?мн+' мин':(ч?'':'0 мин')); };

  /* ---------- ВАХТЕННЫЙ ЖУРНАЛ: ход квеста ---------- */
  const ЗАПИСИ = [
    {ключ:'приход', имя:'Приход в Сиракузы',  итог:'05:40'},
    {ключ:'вахта',  имя:'Смена дежурств',     итог:'04:00'},
    {ключ:'пояс',   имя:'Перевод часов',      итог:'+2 часа'}
  ];
  const записано = (s,к) => !!s['зап_'+к];

  const CSS=`
  #lvis .s6.l821{gap:14px}
  #lvis .s6.l821 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l821 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l821 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#232a4a,#161a2e);border:1.5px solid var(--line)}
  #lvis .s6.l821 .карт.задача{border-color:${ЯНТ}}
  #lvis .s6.l821 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l821 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l821 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l821 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l821 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${ЯНТ};
    background:linear-gradient(180deg,rgba(255,200,97,.14),rgba(255,200,97,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l821 .правило b{color:${ЯНТ}}

  #lvis .s6.l821 .журнал{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(126,143,224,.12),rgba(126,143,224,.03));
    border:1.5px solid rgba(126,143,224,.45);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l821 .журнал .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l821 .журнал .шапка b{font-size:24px;letter-spacing:0;text-transform:none;color:${ЯНТ};
    font-variant-numeric:tabular-nums}
  #lvis .s6.l821 .журнал .шапка b.готово{color:${GREEN}}
  #lvis .s6.l821 .журнал ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l821 .журнал li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l821 .журнал li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;
    border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l821 .журнал li.есть{color:${ИНК}}
  #lvis .s6.l821 .журнал li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,209,168,.14)}
  #lvis .s6.l821 .журнал li span{margin-left:auto;font-variant-numeric:tabular-nums;color:var(--mut);font-size:16px}
  #lvis .s6.l821 .журнал li.есть span{color:${GREEN}}

  #lvis .s6.l821 .рычаг{display:flex;align-items:center;gap:10px;width:100%;padding:12px;border-radius:16px;
    background:rgba(255,200,97,.08);border:1.5px solid rgba(255,200,97,.34)}
  #lvis .s6.l821 .рычаг .сколько{flex:1;min-width:0;text-align:center;font-size:16px;line-height:1.25;color:${ИНК}}
  #lvis .s6.l821 .рычаг .сколько b{display:block;font-size:32px;color:${ЯНТ};font-variant-numeric:tabular-nums}
  #lvis .s6.l821 .рычаг button{width:56px;height:56px;flex:0 0 56px;border-radius:14px;cursor:pointer;
    border:1px solid rgba(255,200,97,.5);background:rgba(255,200,97,.14);color:${ЯНТ};
    font:inherit;font-size:24px;line-height:1;touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l821 .рычаг button:active{transform:translateY(2px);background:rgba(255,200,97,.26)}
  #lvis .s6.l821 .рычаг button:disabled{opacity:.35;cursor:not-allowed}

  /* ── строки расписания вахт ── */
  #lvis .s6.l821 .вахты{display:flex;flex-direction:column;gap:8px;width:100%}
  #lvis .s6.l821 .вахты button{display:flex;align-items:center;gap:12px;width:100%;min-height:52px;
    padding:11px 14px;border-radius:14px;cursor:pointer;font:inherit;font-size:16px;text-align:left;
    color:${ИНК};background:rgba(126,143,224,.1);border:1.5px solid rgba(126,143,224,.38);
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),border-color 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l821 .вахты button:active{transform:translateY(2px)}
  #lvis .s6.l821 .вахты button.выбрана{border-color:${ЯНТ};background:rgba(255,200,97,.14)}
  #lvis .s6.l821 .вахты button b{font-variant-numeric:tabular-nums;font-size:20px;color:${ЯНТ};flex:0 0 auto}
  #lvis .s6.l821 .вахты button.выбрана b{color:${ЯНТ}}
  #lvis .s6.l821 .вахты button em{margin-left:auto;font-style:normal;font-size:14px;color:var(--mut)}

  #lvis .s6.l821 .дело{width:100%;min-height:60px;padding:16px;border-radius:16px;cursor:pointer;
    font:inherit;font-size:20px;line-height:1.25;text-align:center;color:#1b1608;
    border:0;background:linear-gradient(180deg,${ЯНТ},#c8912f);
    box-shadow:0 3px 0 rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.4);
    transition:transform 120ms ease, box-shadow 180ms ease}
  #lvis .s6.l821 .дело:active{transform:translateY(2px);box-shadow:0 1px 0 rgba(0,0,0,.42)}
  #lvis .s6.l821 .дело.сделано{background:rgba(143,209,168,.16);color:${GREEN};
    box-shadow:none;border:1.5px solid ${GREEN}}
  #lvis .s6.l821 .дело:disabled{background:rgba(255,255,255,.07);color:var(--mut);
    box-shadow:none;border:1.5px solid rgba(255,255,255,.16);cursor:not-allowed}

  #lvis .s6.l821 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l821 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l821 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l821 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l821 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l821 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center}

  #lvis .s6.l821 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l821 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l821 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l821 .уровни .точка.сейчас{background:${ЯНТ};animation:l821dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l821dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l821 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l821{-webkit-text-size-adjust:100%}
  #lvis .s6.l821 [data-anim]{animation:l821rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l821rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l821 [data-anim]{animation:none!important}
    #lvis .s6.l821 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l821 .ask button,#lvis .s6.l821 .рычаг button,#lvis .s6.l821 .дело,
    #lvis .s6.l821 .вахты button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l821-style');
      if(!s){ s=document.createElement('style'); s.id='l821-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Вахта</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ДЕЛО = (текст,on,сделано,можно) =>
    `<button type="button" data-anim style="--i:3" class="дело${сделано?' сделано':''}"
       ${можно===false&&!сделано?'disabled':''} onclick="${on}">${текст}</button>`;

  const ЖУРНАЛ = (s) => {
    const всё = ЗАПИСИ.every(з=>записано(s,з.ключ));
    return A(0,'журнал',
      `<div class="шапка"><span>Журнал</span><b class="${всё?'готово':''}">${
        всё?'переход записан':'ночной переход'}</b></div>
       <ul>${ЗАПИСИ.map(з=>{
         const есть = записано(s,з.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${з.имя}
           <span>${есть?з.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длительность,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длительность}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анРаз = (имя,значения,длительность,задержка) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длительность}" fill="freeze"
              ${задержка?'begin="'+задержка+'"':''}
              calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длительность,доля,конец) => анК('opacity','0.55;0.55;1;1',длительность,'0;'+кт(доля)+';'+кт(конец)+';1');
  const очередь = (номер,всего,длительность) => {
    if(!ДВИЖ) return '';
    const д = 1/(всего+1), a = кт(номер*д), b = кт(номер*д+д*0.5), c = кт(номер*д+д*0.9);
    return анК('opacity','0.6;0.6;1;1;0.6',длительность,'0;'+a+';'+b+';'+c+';1');
  };
  const рамкаВсп = (x0,y0,x1,y1,цвет,длительность,доля) => ДВИЖ ? `<rect x="${x0}" y="${y0}"
      width="${x1-x0}" height="${y1-y0}" rx="10" fill="none" stroke="${цвет||ЯНТ}"
      stroke-width="2.2" opacity="0">
      ${анК('opacity','0;0;0.95;0',длительность,'0;'+кт(доля)+';'+кт(доля+0.1)+';1')}
    </rect>` : '';
  const кольцо = (x,y,r,цвет,длительность,доля) => ДВИЖ ? `<circle cx="${x}" cy="${y}" r="${r}" fill="none"
      stroke="${цвет||ЯНТ}" stroke-width="2.2" opacity="0">
      ${анК('opacity','0;0;0.95;0',длительность,'0;'+кт(доля)+';'+кт(доля+0.1)+';1')}
      ${анК('r',r+';'+r+';'+(r+7)+';'+(r+7),длительность,'0;'+кт(доля)+';'+кт(доля+0.16)+';1')}
    </circle>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c821-фон" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2a3158"/><stop offset="0.55" stop-color="#1b2040"/>
        <stop offset="1" stop-color="#101328"/>
      </linearGradient>
      <radialGradient id="c821-диск" cx="0.5" cy="0.35" r="0.72">
        <stop offset="0" stop-color="#2c3564"/><stop offset="1" stop-color="#171b35"/>
      </radialGradient>
      <linearGradient id="c821-заря" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#ff9f6b"/><stop offset="1" stop-color="#ffd76a"/>
      </linearGradient>
      <linearGradient id="c821-море" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#5f7fb0"/><stop offset="1" stop-color="#1a2c50"/>
      </linearGradient>
      <linearGradient id="c821-дуб" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c79a5a"/><stop offset="1" stop-color="#6d4a22"/>
      </linearGradient>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||250}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c821-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+16, в=к+10;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g>
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,12,26,.9)" stroke="${цвет||ЯНТ}" stroke-width="1.2"/>
      ${т(cx,y-2,текст,к,цвет||ЯНТ,true)}
    </g>`;
  };

  /* ---------------- СУТОЧНЫЙ КРУГ -------------------------------------------
     Двадцать четыре часа — полный оборот. Полночь наверху одной точкой: через
     неё стрелка ПРОХОДИТ, а не упирается. Отсюда и весь приём темы. */
  const точкаЧ = (cx,cy,r,часы) => {
    const a = (часы/24)*2*Math.PI;
    return [cx + r*Math.sin(a), cy - r*Math.cos(a)];
  };
  const дуга = (cx,cy,r,ч0,ч1,цвет,ширина) => {
    if(ч1-ч0 <= 0.001) return '';
    const [x0,y0] = точкаЧ(cx,cy,r,ч0), [x1,y1] = точкаЧ(cx,cy,r,ч1);
    const большая = (ч1-ч0) > 12 ? 1 : 0;
    return `<path d="M${x0.toFixed(1)} ${y0.toFixed(1)} A${r} ${r} 0 ${большая} 1 ${x1.toFixed(1)} ${y1.toFixed(1)}"
      fill="none" stroke="${цвет}" stroke-width="${ширина||9}" stroke-linecap="round"/>`;
  };
  const стрелкаЧ = (cx,cy,r,часы,цвет,ширина) => {
    const [x,y] = точкаЧ(cx,cy,r,часы);
    return `<g>
      <line x1="${cx}" y1="${cy}" x2="${x.toFixed(1)}" y2="${y.toFixed(1)}"
        stroke="${цвет||ЯНТ}" stroke-width="${ширина||2.4}" stroke-linecap="round"/>
      <circle cx="${x.toFixed(1)}" cy="${y.toFixed(1)}" r="3.4" fill="${цвет||ЯНТ}"/>
    </g>`;
  };
  /* Циферблат: 24 деления, подписи через три часа, полночь помечена луной,
     полдень — солнцем. Без них круг читается как обычные часы на 12. */
  const циферблат = (cx,cy,r) => {
    const метки = Array.from({length:24},(_,ч)=>{
      const [x1,y1] = точкаЧ(cx,cy,r,ч), [x2,y2] = точкаЧ(cx,cy,r-(ч%3?4:8),ч);
      return `<line x1="${x1.toFixed(1)}" y1="${y1.toFixed(1)}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}"
        stroke="${ч%3?'rgba(255,255,255,.3)':'rgba(255,255,255,.6)'}" stroke-width="${ч%3?1:1.6}"/>`;
    }).join('');
    const числа = [3,6,9,15,18,21].map(ч=>{
      const [x,y] = точкаЧ(cx,cy,r+13,ч);
      return т(x,y+5,String(ч),14,МУТ);
    }).join('')
    + т(cx, cy-r+34, '24', 14, ЛУНА, true)
    + т(cx, cy+r-26, '12', 14, СОЛНЦЕ, true);
    const [лx,лy] = точкаЧ(cx,cy,r-17,0), [сx,сy] = точкаЧ(cx,cy,r-17,12);
    return `<g>
      <circle cx="${cx}" cy="${cy}" r="${r}" fill="url(#c821-диск)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${метки}${числа}
      <path d="M${(лx+4).toFixed(1)} ${(лy-6).toFixed(1)} a7 7 0 1 0 0 12 a5.6 5.6 0 1 1 0 -12 z" fill="${ЛУНА}" opacity=".9"/>
      <circle cx="${сx.toFixed(1)}" cy="${сy.toFixed(1)}" r="5" fill="${СОЛНЦЕ}"/>
      ${[0,1,2,3,4,5].map(i=>{const a=i*Math.PI/3;
        return `<line x1="${(сx+Math.cos(a)*7).toFixed(1)}" y1="${(сy+Math.sin(a)*7).toFixed(1)}"
          x2="${(сx+Math.cos(a)*10).toFixed(1)}" y2="${(сy+Math.sin(a)*10).toFixed(1)}"
          stroke="${СОЛНЦЕ}" stroke-width="1.4"/>`;}).join('')}
      <circle cx="${cx}" cy="${cy}" r="3" fill="${МУТ}"/>
    </g>`;
  };
  /* Шов полуночи: заметная зарубка наверху круга — здесь кончаются сутки */
  const шов = (cx,cy,r) => `<g>
    <line x1="${cx}" y1="${(cy-r-9).toFixed(1)}" x2="${cx}" y2="${(cy-r+11).toFixed(1)}"
      stroke="${ЛУНА}" stroke-width="2.4" stroke-linecap="round"/>
  </g>`;

  /* ---------------- ЛЕНТА ЧАСОВ ---------------------------------------------
     Те же сутки, вытянутые в строку: расписание читают строкой, а не кругом. */
  const лента = (x,y,ш,отЧ,доЧ) => {
    const н = доЧ-отЧ, ед = ш/н;
    return `<g>
      <rect x="${x}" y="${y}" width="${ш}" height="26" rx="4"
        fill="rgba(126,143,224,.12)" stroke="${ЛИНИЯ}" stroke-width="1.2"/>
      ${Array.from({length:н+1},(_,i)=>{
        const ч=отЧ+i, круглый = ч%24===0;
        return `<line x1="${(x+i*ед).toFixed(1)}" y1="${y}" x2="${(x+i*ед).toFixed(1)}" y2="${y+26}"
          stroke="${круглый?ЛУНА:'rgba(255,255,255,.22)'}" stroke-width="${круглый?2.2:0.9}"/>`;
      }).join('')}
      ${Array.from({length:н+1},(_,i)=>{
        const ч=отЧ+i; if(ч%2) return '';
        return т(x+i*ед, y+42, String(((ч%24)+24)%24), 14, ч%24===0?ЛУНА:МУТ, ч%24===0);
      }).join('')}
    </g>`;
  };
  const блокЛенты = (x,y,ш,отЧ,доЧ,ч0,ч1,цвет,текст) => {
    const ед = ш/(доЧ-отЧ);
    const x0 = x+(ч0-отЧ)*ед, ширина = (ч1-ч0)*ед;
    return `<g>
      <rect x="${x0.toFixed(1)}" y="${y+3}" width="${ширина.toFixed(1)}" height="20" rx="3"
        fill="${цвет}" opacity=".85"/>
      ${текст?т(x0+ширина/2, y+18, текст, 14, '#15182c', true):''}
    </g>`;
  };

  /* ---------------- ПОЯСА ----------------------------------------------------
     Столбцы поясов, солнце над полуднем: к востоку часы уходят вперёд. */
  const поясы = (x,y,ш,часы,подсвечен,живой) => {
    const н = часы.length, шк = ш/н;
    return `<g>
      ${часы.map((ч,i)=>{
        const cx = x+i*шк+шк/2, свой = i===подсвечен;
        return `<g>${живой?очередь(i,часы.length,'8s'):''}
          <rect x="${(x+i*шк+1.6).toFixed(1)}" y="${y}" width="${(шк-3.2).toFixed(1)}" height="62" rx="5"
            fill="${свой?'rgba(255,200,97,.16)':'rgba(126,143,224,.1)'}"
            stroke="${свой?ЯНТ:ЛИНИЯ}" stroke-width="${свой?1.8:1.1}"/>
          <circle cx="${cx.toFixed(1)}" cy="${y+22}" r="12" fill="url(#c821-диск)"
            stroke="${свой?ЯНТ:МУТ}" stroke-width="1.2"/>
          ${(()=>{const [hx,hy]=точкаЧ(cx,y+22,8,(всутках(ч)/60));
            return `<line x1="${cx.toFixed(1)}" y1="${y+22}" x2="${hx.toFixed(1)}" y2="${hy.toFixed(1)}"
              stroke="${свой?ЯНТ:ИНК}" stroke-width="1.8" stroke-linecap="round"/>`;})()}
          ${т(cx, y+54, чч(ч), 14, свой?ЯНТ:МУТ, свой)}
        </g>`;
      }).join('')}
    </g>`;
  };

  const судно = (x,y,м) => {
    const мм=м||1;
    return `<g transform="translate(${x} ${y}) scale(${мм})">
      <path d="M-30 0 q5 13 30 13 q25 0 30 -13 z" fill="url(#c821-дуб)" stroke="${ОБВОД}" stroke-width="${(1.3/мм).toFixed(2)}"/>
      <rect x="-31" y="-5" width="62" height="5" rx="2" fill="#8a5f30" stroke="${ОБВОД}" stroke-width="${(1/мм).toFixed(2)}"/>
      <line x1="0" y1="-5" x2="0" y2="-44" stroke="${ОБВОД}" stroke-width="${(2.2/мм).toFixed(2)}"/>
      <path d="M3 -42 q18 14 0 28 z" fill="#f4f0e2" stroke="${ОБВОД}" stroke-width="${(1/мм).toFixed(2)}"/>
      <path d="M-3 -39 q-14 12 0 23 z" fill="#dcd4c0" stroke="${ОБВОД}" stroke-width="${(1/мм).toFixed(2)}"/>
      <circle cx="0" cy="-48" r="2.6" fill="${ЯНТ}"/>
    </g>`;
  };

  /* ================= КАДРЫ ================= */
  const ХОД = 8*60;                 /* идти 8 часов */
  const ПРИХОД = СТАРТ + ХОД;       /* 05:40 следующих суток */
  const ДО_ПОЛУНОЧИ = 1440 - СТАРТ; /* 2 ч 20 мин */
  const ПОСЛЕ = ХОД - ДО_ПОЛУНОЧИ;  /* 5 ч 40 мин */

  /* дуга перехода: до полуночи ночная, после — рассветная */
  const дугаПерехода = (cx,cy,r,от,сколько,ш) => {
    const ч0 = от/60, ч1 = (от+сколько)/60;
    if(ч1 <= 24) return дуга(cx,cy,r,ч0,ч1,НОЧЬ,ш);
    return дуга(cx,cy,r,ч0,24,НОЧЬ,ш) + дуга(cx,cy,r,0,ч1-24,'url(#c821-заря)',ш);
  };
  const звёзды = (в) => [[38,44],[92,30],[276,38],[308,62],[56,84],[300,120],[26,140],[312,180]]
    .filter(п=>п[1]<в-20)
    .map((п,i)=>`<circle cx="${п[0]}" cy="${п[1]}" r="${1.4+(i%3)*0.5}" fill="${ЛУНА}" opacity=".7">
      ${ДВИЖ?анК('opacity','0.7;0.25;0.7','4.5s','0;0.5;1','begin="'+(i*0.4).toFixed(1)+'s"'):''}
    </circle>`).join('');

  /* 1. Ночной переход: сутки — это круг */
  function F1(s){
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Корабль отправляется в <b>21:40</b> и идёт до Сиракуз <b>8 часов</b>. Во сколько он придёт? Просто сложить «21:40 + 8» тут не получится: в сутках всего 24 часа.') +
      `<div class="pic">${свг(`
        ${фон(246)}
        ${звёзды(246)}
        ${циферблат(168,136,62)}
        ${шов(168,136,62)}
        ${стрелкаЧ(168,136,52,СТАРТ/60,ЯНТ,2.6)}
        ${кольцо(...точкаЧ(168,136,52,СТАРТ/60),9,ЯНТ,'7s',0.25)}
        ${подпись(168,26,'Сутки — это круг',ЯНТ,16)}
        ${подпись(168,58,'вышли в 21:40',ЯНТ,14)}
        ${подпись(168,232,'идти 8 часов — куда придёт стрелка?',МУТ,14)}
      `,246)}</div>` +
      СКАЗ('Смотри','Двадцать четыре часа — <b>полный оборот</b>. Наверху, под луной, — полночь: там кончаются одни сутки и начинаются другие. Стрелка через эту точку <b>проходит</b>, а не упирается.') +
      ПРАВИЛО('Часа с номером больше 24 не бывает. Перевалили за полночь — <b>начались новые сутки</b>.');
  }

  /* 2. Песочница: крути стрелку через полночь */
  function F2(s){
    const д = Math.max(1, Math.min(12, s.часов==null?3:s.часов));
    const конец = СТАРТ + д*60, перешли = конец >= 1440;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Выставь, сколько часов идти, и смотри на дугу. Она сама меняет цвет там, где кончаются сутки.') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${звёзды(250)}
        ${циферблат(168,136,62)}
        ${дугаПерехода(168,136,62,СТАРТ,д*60,9)}
        ${шов(168,136,62)}
        ${стрелкаЧ(168,136,50,СТАРТ/60,МУТ,2)}
        ${стрелкаЧ(168,136,50,(конец%1440)/60,ЯНТ,2.6)}
        ${перешли?кольцо(168,74,11,ЛУНА,'6s',0.2):''}
        ${подпись(168,26,'Крути стрелку через полночь',ЯНТ,14)}
        ${подпись(168,58,'с 21:40 · '+д+' ч',МУТ,14)}
        ${подпись(168,232, чч(конец)+(перешли?' · следующие сутки':' · те же сутки'),
          перешли?ЗАРЯ:НОЧЬ, 14)}
      `,250)}</div>` +
      A(3,'рычаг',
        `<button type="button" onclick="r821Часов(-1)" ${д>1?'':'disabled'} aria-label="убрать час">−</button>
         <span class="сколько">идти часов<b>${д}</b></span>
         <button type="button" onclick="r821Часов(1)" ${д<12?'':'disabled'} aria-label="добавить час">+</button>`) +
      СКАЗ('Смотри', перешли
        ? 'Дуга перевалила через шов: синяя часть — остаток старых суток, рыжая — уже новые. Приход <b>'+чч(конец)+'</b>, и это <b>следующие сутки</b>.'
        : 'Дуга пока не дошла до шва — всё в тех же сутках. Приход <b>'+чч(конец)+'</b>. Добавь ещё часов и посмотри, что будет на полуночи.') +
      ПРАВИЛО('Цвет меняется ровно на полуночи: по нему и видно, <b>где сменились сутки</b>.');
  }

  /* 3. Разрез у полуночи. Главный кадр урока. */
  function F3(s){
    const в = s.о1, верно = в==='ok';
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Приём простой: <b>режем восемь часов на полуночи</b>. Сколько остаётся до неё — и сколько уйдёт после.') +
      `<div class="pic">${свг(`
        ${фон(258)}
        ${звёзды(258)}
        ${циферблат(168,128,58)}
        <g>${очередь(0,2,'7s')}${дуга(168,128,58,СТАРТ/60,24,НОЧЬ,9)}</g>
        <g>${очередь(1,2,'7s')}${дуга(168,128,58,0,ПОСЛЕ/60,'url(#c821-заря)',9)}</g>
        ${шов(168,128,58)}
        ${стрелкаЧ(168,128,48,СТАРТ/60,МУТ,2)}
        ${стрелкаЧ(168,128,48,(ПРИХОД%1440)/60,верно?GREEN:ЯНТ,2.6)}
        ${верно?кольцо(...точкаЧ(168,128,48,(ПРИХОД%1440)/60),9,GREEN,'7s',0.3):''}
        ${подпись(168,26,'Разрез у полуночи',ЯНТ,16)}
        ${подпись(168,56,'21:40 + 8 часов',МУТ,14)}
        ${подпись(88,222,'до полуночи 2:20',НОЧЬ,14)}
        ${подпись(248,222,'после 5:40',ЗАРЯ,14)}
        ${подпись(168,248, верно?'2:20 + 5:40 = 8 часов · приход 05:40':'сложи обе части',
          верно?GREEN:МУТ, 14)}
      `,258)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), '05:40 следующих суток', "r821О1('ok')")}
        ${BTN(5, в==='no'?'miss':'', '29:40 тех же суток', "r821О1('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. До полуночи оставалось 2 ч 20 мин: 21:40 + 2:20 = 24:00. Остальные 5 ч 40 мин легли уже на новые сутки: 00:00 + 5:40 = 05:40. И 2:20 + 5:40 = 8 часов — всё сошлось.'
              : 'Нет: часа с номером 29 не бывает. Дугу режут на полуночи: 2 ч 20 мин до неё и 5 ч 40 мин после — получается 05:40 следующих суток.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('<b>Режь время на полуночи</b>: сколько до неё и сколько после.');
  }

  /* 4. Ловушка: часа номер 29 не бывает */
  function F4(s){
    const в = s.о2, верно = в==='ok';
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Так ошибаются чаще всего: складывают в столбик и пишут <b>29:40</b>. Почему такой записи быть не может?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${звёзды(248)}
        <g>${проявить('6s',0.42,0.6)}${подпись(168,52,'21:40 + 8 = 29:40',RED,16)}</g>
        <path d="M84 30 L252 60" stroke="${RED}" stroke-width="2.2" opacity=".75"/>
        <path d="M84 60 L252 30" stroke="${RED}" stroke-width="2.2" opacity=".75"/>
        ${циферблат(168,140,56)}
        ${дугаПерехода(168,140,56,СТАРТ,ХОД,9)}
        ${шов(168,140,56)}
        ${стрелкаЧ(168,140,46,(ПРИХОД%1440)/60,верно?GREEN:ЯНТ,2.6)}
        ${верно?кольцо(168,84,11,ЛУНА,'6s',0.22):''}
        ${подпись(168,26,'Ловушка',ЯНТ,16)}
        ${подпись(168,236, верно?'29:40 − 24 = 05:40, и сутки новые':'на круге нет числа 29',
          верно?GREEN:МУТ, 14)}
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), 'В сутках 24 часа — лишние 24 сбрасывают', "r821О2('ok')")}
        ${BTN(5, в==='no'?'miss':'', 'Потому что 40 минут — это меньше часа', "r821О2('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Стрелка прошла полный круг и пошла по новому: 29:40 − 24:00 = 05:40, и сутки уже другие. Число 29 просто некуда поставить — на круге его нет.'
              : 'Нет, минуты тут ни при чём — 40 минут бывает. Дело в часах: их в сутках 24, и всё, что сверх, отсчитывается заново: 29:40 − 24 = 05:40.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Перевалило за 24 — <b>вычти 24 и прибавь сутки</b>.');
  }

  /* 5. Проверка обратным действием */
  function F5(s){
    const отмотал = !!s.отмотал;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Проверим приход. Если от <b>05:40</b> отмотать те же восемь часов назад, должно получиться 21:40 — и уже прошедших суток.') +
      `<div class="pic">${свг(`
        ${фон(246)}
        ${звёзды(246)}
        ${циферблат(168,132,58)}
        ${отмотал?дугаПерехода(168,132,58,СТАРТ,ХОД,9):''}
        ${шов(168,132,58)}
        ${стрелкаЧ(168,132,48,(ПРИХОД%1440)/60, отмотал?МУТ:ЯНТ,2.4)}
        ${отмотал?стрелкаЧ(168,132,48,СТАРТ/60,GREEN,2.6):''}
        ${отмотал?кольцо(...точкаЧ(168,132,48,СТАРТ/60),9,GREEN,'6s',0.24):''}
        ${подпись(168,26,'Проверка обратным ходом',ЯНТ,14)}
        ${подпись(168,58, отмотал?'05:40 − 8 ч':'стоим на 05:40', отмотал?МУТ:ЯНТ, 14)}
        ${подпись(168,226, отмотал?'21:40 прошедших суток ✓':'отмотай восемь часов назад',
          отмотал?GREEN:МУТ, 14)}
      `,246)}</div>` +
      ДЕЛО(отмотал ? '✓ Отмотано: вышло 21:40' : 'Отмотать 8 часов назад', "r821Отмотать()", отмотал, true) +
      СКАЗ('Зачем', отмотал
        ? 'Сошлось: стрелка вернулась ровно туда, откуда шла. Обратный ход через полночь работает так же — только шов проходят в другую сторону.'
        : 'Умножал — подели, прибавлял — отними. Со временем то же самое: прибавил 8 часов — отними 8 и посмотри, вернулся ли.') +
      ПРАВИЛО('Обратный ход через полночь считают так же: <b>сначала до шва, потом за ним</b>.');
  }

  /* 6. Веха 2: лента вахт */
  const ВАХТЫ = [
    {имя:'Первая',  от:20, до:24, под:'вечерняя'},
    {имя:'Вторая',  от:24, до:28, под:'через полночь'},
    {имя:'Третья',  от:28, до:32, под:'предрассветная'}
  ];
  function F6(s){
    const в = s.о3, верно = в==='ok';
    const выбрана = s.вахта==null ? -1 : s.вахта;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Ночью на корабле дежурят по очереди: пока одни спят, другие стоят у руля. Такое дежурство называют <b>вахтой</b>, и длится оно <b>четыре часа</b>. Первая вахта заступает в 20:00. Расписание удобнее читать строкой, поэтому те же сутки вытянуты в ленту — нажимай вахты и смотри, куда они ложатся.') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${подпись(168,26,'Лента вахт',ЯНТ,16)}
        ${лента(28,96,280,20,32)}
        ${ВАХТЫ.map((в_,i)=>`<g>${выбрана<0?очередь(i,3,'7s'):''}${блокЛенты(28,96,280,20,32,в_.от,в_.до,
          i===выбрана ? ЯНТ : (i===1?ЗАРЯ:НОЧЬ), в_.имя)}</g>`).join('')}
        ${т(168,70,'от 20:00 до 08:00',14,МУТ)}
        ${выбрана>=0?рамкаВсп(28+(ВАХТЫ[выбрана].от-20)*(280/12)-3, 93,
          28+(ВАХТЫ[выбрана].до-20)*(280/12)+3, 125, ЯНТ,'6s',0.16):''}
        ${подпись(168,176, выбрана<0 ? 'выбери вахту под рисунком'
          : (ВАХТЫ[выбрана].имя+' вахта: '+чч(ВАХТЫ[выбрана].от*60)+' – '+чч(ВАХТЫ[выбрана].до*60)),
          выбрана<0?МУТ:ЯНТ, 14)}
        ${подпись(168,210, верно?'в 03:00 стоит вторая вахта':'а кто стоит в 03:00?',
          верно?GREEN:МУТ, 14)}
      `,232)}</div>` +
      A(3,'вахты', ВАХТЫ.map((в_,i)=>
        `<button type="button" class="${i===выбрана?'выбрана':''}" onclick="r821Вахта(${i})">
           <b>${чч(в_.от*60)} – ${чч(в_.до*60)}</b>${в_.имя}<em>${в_.под}</em></button>`).join('')) +
      `<div class="ask пара">
        ${BTN(6, верно?'hit':(в?'miss':''), 'Вторая', "r821О3('ok')")}
        ${BTN(7, в==='no'?'miss':'', 'Третья', "r821О3('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Вторая вахта идёт с 00:00 до 04:00 — она одна из трёх перешагивает полночь, поэтому её и записывают двумя числами: 24:00 и 00:00 — это одна и та же точка.'
              : 'Нет: третья заступает только в 04:00. В 03:00 ещё стоит вторая — та самая, что перешагнула полночь.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('<b>24:00 и 00:00 — одна точка.</b> Слева она конец суток, справа — начало.');
  }

  /* 7. Обратная задача: сколько прошло */
  function F7(s){
    const в = s.о4, верно = в==='ok';
    const ОТ = 22*60+15, ДО = 3*60+5;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Обратная задача. Дежурный записал: заступил в <b>22:15</b>, сменился в <b>03:05</b>. Сколько времени он простоял у руля?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${звёзды(252)}
        ${циферблат(168,128,58)}
        <g>${очередь(0,2,'7s')}${дуга(168,128,58,ОТ/60,24,НОЧЬ,9)}</g>
        <g>${очередь(1,2,'7s')}${дуга(168,128,58,0,ДО/60,'url(#c821-заря)',9)}</g>
        ${шов(168,128,58)}
        ${стрелкаЧ(168,128,48,ОТ/60,МУТ,2)}
        ${стрелкаЧ(168,128,48,ДО/60, верно?GREEN:ЯНТ,2.6)}
        ${подпись(168,26,'Сколько простоял',ЯНТ,16)}
        ${подпись(168,56,'с 22:15 до 03:05',МУТ,14)}
        ${подпись(88,218,'до полуночи 1:45',НОЧЬ,14)}
        ${подпись(250,218,'после 3:05',ЗАРЯ,14)}
        ${подпись(168,242, верно?'1 ч 45 мин + 3 ч 05 мин = 4 ч 50 мин':'сложи обе части',
          верно?GREEN:МУТ, 14)}
      `,252)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '4 ч 50 мин', "r821О4('ok')")}
        ${BTN(5, в==='no'?'miss':'', '5 ч 10 мин', "r821О4('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно, и режут тут так же. До полуночи: 24:00 − 22:15 = 1 ч 45 мин. После: 03:05. Вместе 1:45 + 3:05 = 4 ч 50 мин.'
              : 'Нет: 5 ч 10 мин — это если отнять 22:15 от 03:05 «по цифрам». До полуночи 1 ч 45 мин, после неё 3 ч 05 мин, вместе 4 ч 50 мин.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Считаешь, сколько прошло, — <b>режь на полуночи точно так же</b>.');
  }

  /* 8. Веха 3: часовые пояса */
  function F8(s){
    const в = s.о5, верно = в==='ok';
    const ряд = [СТАРТ-60, СТАРТ, СТАРТ+60, СТАРТ+120, СТАРТ+180];
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Земля круглая, и солнце встаёт не везде разом. Поэтому её поделили на <b>часовые пояса</b>: в соседнем поясе к востоку часы показывают на час больше. Корабль за ночь пересекает <b>два пояса</b>. Который час будет на корабельных часах, когда в порту, откуда вышли, 21:40?') +
      `<div class="pic">${свг(`
        ${фон(240)}
        ${подпись(168,26,'Часовые пояса',ЯНТ,16)}
        ${т(168,52,'к востоку часы уходят вперёд',14,МУТ)}
        <path d="M60 80 L272 80" stroke="${ЯНТ}" stroke-width="1.6" opacity=".7"/>
        <path d="M280 80 L266 75 L266 85 z" fill="${ЯНТ}" opacity=".85"/>
        ${[0,1,2,3].map(i=>т(88+i*57,76,'+1 ч',14,ЯНТ)).join('')}
        ${поясы(24,92,288,ряд, верно?3:1, !верно)}
        ${верно?рамкаВсп(24+3*57.6-1, 89, 24+4*57.6+1, 157, GREEN,'6s',0.2):''}
        ${подпись(168,186,'ушли на 2 пояса к востоку',ЯНТ,14)}
        ${подпись(168,222, верно?'21:40 + 2 часа = 23:40':'который час на корабельных часах?',
          верно?GREEN:МУТ, 14)}
      `,240)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '23:40', "r821О5('ok')")}
        ${BTN(5, в==='no'?'miss':'', '19:40', "r821О5('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно: на восток — вперёд. Два пояса, значит плюс два часа: 21:40 + 2 = 23:40. А ушли бы на три — было бы 00:40, и уже следующих суток: пояса тоже умеют перешагивать полночь.'
              : 'Нет: назад часы переводят, когда идут на запад. На восток — вперёд: 21:40 + 2 = 23:40.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('<b>К востоку — прибавляй, к западу — отнимай</b>, по часу на пояс.');
  }

  /* 9. Два действия подряд */
  function F9(s){
    const в = s.о6, верно = в==='ok';
    const малый = (cx,время,цвет) => `<g>
      <circle cx="${cx}" cy="138" r="28" fill="url(#c821-диск)" stroke="${цвет}" stroke-width="1.6"/>
      ${[0,6,12,18].map(ч=>{const [x1,y1]=точкаЧ(cx,138,28,ч),[x2,y2]=точкаЧ(cx,138,23,ч);
        return `<line x1="${x1.toFixed(1)}" y1="${y1.toFixed(1)}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}"
          stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>`;}).join('')}
      ${(()=>{const [x,y]=точкаЧ(cx,138,20,всутках(время)/60);
        return `<line x1="${cx}" y1="138" x2="${x.toFixed(1)}" y2="${y.toFixed(1)}"
          stroke="${цвет}" stroke-width="2.2" stroke-linecap="round"/>`;})()}
      <circle cx="${cx}" cy="138" r="2.4" fill="${цвет}"/>
    </g>`;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Соберём всё вместе. Вышли в <b>21:40</b>, шли <b>8 часов</b>, и по дороге пересекли <b>два пояса к востоку</b>. Который час будет на часах в самих Сиракузах?') +
      `<div class="pic">${свг(`
        ${фон(246)}
        ${подпись(168,26,'Два действия подряд',ЯНТ,16)}
        <g>${очередь(0,3,'8s')}${малый(64,СТАРТ,МУТ)}</g>
        <g>${очередь(1,3,'8s')}${малый(168,ПРИХОД,ЗАРЯ)}</g>
        <g>${очередь(2,3,'8s')}${малый(272,ПРИХОД+120, верно?GREEN:МУТ)}</g>
        <path d="M96 138 L134 138" stroke="${ЯНТ}" stroke-width="1.6"/>
        <path d="M140 138 L128 133 L128 143 z" fill="${ЯНТ}"/>
        <path d="M200 138 L238 138" stroke="${ЯНТ}" stroke-width="1.6"/>
        <path d="M244 138 L232 133 L232 143 z" fill="${ЯНТ}"/>
        ${подпись(116,104,'+8 ч',ЯНТ,14)}
        ${подпись(220,104,'+2 пояса',ЯНТ,14)}
        ${т(64,186,'21:40',16,МУТ,true)}
        ${т(168,186,'05:40',16,ЗАРЯ,true)}
        ${т(272,186, верно?'07:40':'?',16, верно?GREEN:МУТ,true)}
        ${т(64,206,'вышли',14,МУТ)}
        ${т(168,206,'пришли',14,МУТ)}
        ${т(272,206,'в порту',14,МУТ)}
        ${верно?кольцо(272,138,34,GREEN,'7s',0.24):''}
        ${подпись(168,238, верно?'05:40 + 2 = 07:40':'сначала ход, потом пояса',
          верно?GREEN:МУТ, 14)}
      `,246)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '07:40', "r821О6('ok')")}
        ${BTN(5, в==='no'?'miss':'', '05:40', "r821О6('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно, и порядок важен. Сначала сколько шли: 21:40 + 8 ч = 05:40 следующих суток. Потом пояса: 05:40 + 2 = 07:40. Столько и покажут часы на берегу.'
              : 'Нет: 05:40 — это по часам, которые не переводили. А корабль ушёл на два пояса к востоку, значит часы двигают вперёд: 05:40 + 2 = 07:40.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Сначала <b>сколько шли</b>, потом <b>сколько поясов прошли</b>.');
  }

  /* 10. Итог перехода */
  function F10(s){
    const всё = ЗАПИСИ.every(з=>записано(s,з.ключ));
    return ЖУРНАЛ(s) +
      ЗАДАЧА(всё
        ? 'Журнал закрыт: приход, вахты и перевод часов записаны. Корабль у причала в 07:40 по местному времени.'
        : 'В журнале записано не всё — вернись и добей записи. А пока посмотри, чем кончается ночной переход.') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="128" fill="url(#c821-заря)" opacity=".45"/>
        <rect x="0" y="0" width="336" height="70" fill="url(#c821-фон)" opacity=".7"/>
        <circle cx="272" cy="118" r="18" fill="${СОЛНЦЕ}" opacity=".9"/>
        <rect x="0" y="128" width="336" height="118" fill="url(#c821-море)"/>
        ${судно(120,120,1.2)}
        <rect x="0" y="0" width="336" height="246" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${рамкаВсп(70,58,172,142,ЯНТ,'8s',0.2)}
        ${подпись(168,26,'Пришли на рассвете',ЯНТ,16)}
        ${подпись(168,182,'21:40 + 8 ч = 05:40',ЗАРЯ,14)}
        ${подпись(168,214,'+ 2 пояса = 07:40 в Сиракузах',GREEN,14)}
        ${т(168,234,'сутки сменились один раз',14,МУТ)}
      `,246)}</div>` +
      СКАЗ('Итог перехода','Три записи — один приём. Время режут на полуночи: до шва и после. Пояса прибавляют после того, как посчитан ход. А 24:00 и 00:00 — одна и та же точка круга.') +
      ПРАВИЛО('<b>Перевалило за 24 — вычти 24 и прибавь сутки.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Поезд отправился в 23:20 и шёл 5 часов. Когда прибыл?',
      варианты:[{т:'04:20 следующих суток',ок:true},{т:'28:20 тех же суток',ок:false}],
      разбор:'До полуночи 40 минут, после неё ещё 4 ч 20 мин. 23:20 + 5 ч = 04:20 следующих суток.' },
    { вопрос:'Дежурство шло с 21:50 до 01:30. Сколько времени это заняло?',
      варианты:[{т:'3 ч 40 мин',ок:true},{т:'20 ч 20 мин',ок:false}],
      разбор:'До полуночи 2 ч 10 мин, после — 1 ч 30 мин. Вместе 3 ч 40 мин.' },
    { вопрос:'Дежурства по 4 часа, первое с 20:00. Когда начнётся третье?',
      варианты:[{т:'В 04:00',ок:true},{т:'В 08:00',ок:false}],
      разбор:'Первая 20:00–24:00, вторая 00:00–04:00, третья начинается в 04:00.' },
    { вопрос:'В Москве 15:00. Город лежит на 3 пояса восточнее. Который час там?',
      варианты:[{т:'18:00',ок:true},{т:'12:00',ок:false}],
      разбор:'К востоку часы уходят вперёд: 15:00 + 3 = 18:00.' },
    { вопрос:'Можно ли записать время прибытия как 26:30?',
      варианты:[{т:'Нельзя: это 02:30 следующих суток',ок:true},{т:'Можно, если поезд идёт ночью',ок:false}],
      разбор:'В сутках 24 часа. 26:30 − 24:00 = 02:30, и сутки уже новые.' }
  ];

  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r821Reset()")}</div>` +
        ПРАВИЛО('Режь время <b>на полуночи</b>.');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к,
        выбран===к ? (в.ок?'hit':'miss') : '',
        в.т, "r821Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Через полночь', задания:[
    {q:'22:00 + 5 часов = ?', в:0, варианты:['03:00','27:00'], раз:'2 часа до полуночи и 3 после: 03:00 следующих суток.'},
    {q:'23:15 + 2 часа = ?', в:1, варианты:['25:15','01:15'], раз:'45 минут до полуночи, потом ещё 1 ч 15 мин: 01:15.'},
    {q:'20:45 + 6 часов = ?', в:0, варианты:['02:45','26:45'], раз:'3 ч 15 мин до полуночи, 2 ч 45 мин после.'},
    {q:'19:30 + 9 часов = ?', в:1, варианты:['28:30','04:30'], раз:'4 ч 30 мин до полуночи и столько же после.'}
  ]};
  const Т2 = { имя:'Сколько прошло', задания:[
    {q:'С 23:00 до 02:00 — сколько времени?', в:0, варианты:['3 часа','21 час'], раз:'1 час до полуночи и 2 после.'},
    {q:'С 21:30 до 00:15 — сколько?', в:1, варианты:['21 ч 15 мин','2 ч 45 мин'], раз:'2 ч 30 мин до полуночи и 15 минут после.'},
    {q:'С 22:40 до 03:10 — сколько?', в:0, варианты:['4 ч 30 мин','5 ч 30 мин'], раз:'1 ч 20 мин до полуночи и 3 ч 10 мин после.'},
    {q:'С 18:00 до 06:00 — сколько?', в:1, варианты:['6 часов','12 часов'], раз:'6 часов до полуночи и 6 после — полкруга.'}
  ]};
  const Т3 = { имя:'Пояса и вахты', задания:[
    {q:'14:00, город на 3 пояса восточнее. Который час там?', в:0, варианты:['17:00','11:00'],
     раз:'К востоку прибавляют: 14:00 + 3 = 17:00.'},
    {q:'10:00, город на 2 пояса западнее. Который час там?', в:1, варианты:['12:00','08:00'],
     раз:'К западу отнимают: 10:00 − 2 = 08:00.'},
    {q:'23:00, ушли на 2 пояса к востоку. Который час?', в:0, варианты:['01:00 следующих суток','21:00'],
     раз:'23:00 + 2 = 25:00, а 25:00 − 24 = 01:00. Пояса тоже перешагивают полночь.'},
    {q:'Дежурства по 4 часа с 16:00. Какое по счёту идёт в 03:00?', в:1, варианты:['Вторая','Третья'],
     раз:'16–20 первое, 20–24 второе, 00–04 третье. В 03:00 идёт третье.'}
  ]};

  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r821T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r821TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L821 = {
    id: ID,
    title: 'Время: расписания и пояса',
    ico: '🕛',
    src: 'Начальная школа · 4 класс · Время, расписания и пояса',
    subj: 'jun',
    explain: [
      'Корабль выходит в 21:40 и идёт 8 часов. Сутки — это круг: двадцать четыре часа — полный оборот, а полночь наверху одна точка, через которую стрелка проходит.',
      'Крути стрелку сам: дуга меняет цвет ровно на полуночи — синяя часть от старых суток, рыжая от новых.',
      'Разрез у полуночи: 2 ч 20 мин до неё и 5 ч 40 мин после. Приход 05:40 следующих суток, и 2:20 + 5:40 = 8 часов.',
      'Ловушка: «21:40 + 8 = 29:40». Часа с номером 29 нет: вычитают 24 и прибавляют сутки.',
      'Проверка обратным ходом: 05:40 − 8 часов = 21:40 прошедших суток.',
      'Лента дежурств: вахты по четыре часа с 20:00. Вторая перешагивает полночь, и 24:00 с 00:00 — одна и та же точка.',
      'Обратная задача: с 22:15 до 03:05 — это 1 ч 45 мин до полуночи и 3 ч 05 мин после, всего 4 ч 50 мин.',
      'Часовые пояса: земля поделена на пояса, и к востоку часы уходят вперёд по часу на пояс. 21:40 плюс два пояса — 23:40.',
      'Два действия подряд: сначала сколько шли, 21:40 + 8 ч = 05:40, потом пояса, 05:40 + 2 = 07:40.',
      'Итог: время режут на полуночи, пояса прибавляют после хода, а 24:00 и 00:00 — одна точка круга.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: через полночь.',
      'Тренажёр 2: сколько прошло.',
      'Тренажёр 3: пояса и вахты.'
    ],
    check: {
      q: 'Корабль вышел в 21:40 и шёл 8 часов. Когда он пришёл?',
      choices: ['05:40 следующих суток','29:40 тех же суток','13:40 тех же суток'],
      ans: 0,
      exp: 'До полуночи 2 ч 20 мин, после неё ещё 5 ч 40 мин. Выходит 05:40, и сутки уже новые.'
    },
    tasks: [
      { q:'Корабль вышел в 21:40 и шёл 8 часов. Когда он пришёл?', kind:'choice',
        choices:['05:40 следующих суток','29:40'], ans:0,
        hints:['Режь восемь часов на полуночи.','21:40 + 2:20 = 24:00, осталось 5 ч 40 мин.'],
        sol:'05:40 следующих суток.' },
      { q:'Дежурство шло с 22:15 до 03:05. Сколько времени это заняло?', kind:'choice',
        choices:['4 ч 50 мин','5 ч 10 мин'], ans:0,
        hints:['До полуночи 1 ч 45 мин.','Прибавь то, что после неё.'],
        sol:'1 ч 45 мин + 3 ч 05 мин = 4 ч 50 мин.' },
      { q:'В порту 21:40. Корабль ушёл на 2 пояса к востоку. Который час у него на часах?', kind:'choice',
        choices:['23:40','19:40'], ans:0,
        hints:['К востоку часы переводят вперёд.','По часу на пояс.'],
        sol:'21:40 + 2 = 23:40.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L821.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1);
    else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);

    const ЗАГОЛОВКИ={
      1:'Сутки — это круг', 2:'Стрелка через полночь', 3:'Разрез у полуночи',
      4:'Ловушка', 5:'Проверка', 6:'Лента вахт', 7:'Сколько простоял',
      8:'Часовые пояса', 9:'Два действия подряд', 10:'Пришли на рассвете',
      11:'Практика', 12:'Тренажёр 1', 13:'Тренажёр 2', 14:'Тренажёр 3'
    };
    el.innerHTML = `<div class="s6 l821" data-frame="${f}">
        <h2>${ЗАГОЛОВКИ[f]||'Время и сутки'}</h2>
        ${сцена}
      </div>`;
  }

  window.r821Часов =(д)=>{ const s=S();
    s.часов=Math.max(1,Math.min(12,(s.часов==null?3:s.часов)+д)); chRender(0); };
  window.r821О1    =(к)=>{ const s=S(); s.о1=к; if(к==='ok') s.зап_приход=true; chRender(0); };
  window.r821О2    =(к)=>{ S().о2=к; chRender(0); };
  window.r821О3    =(к)=>{ const s=S(); s.о3=к; if(к==='ok') s.зап_вахта=true;  chRender(0); };
  window.r821О4    =(к)=>{ S().о4=к; chRender(0); };
  window.r821О5    =(к)=>{ const s=S(); s.о5=к; if(к==='ok') s.зап_пояс=true;   chRender(0); };
  window.r821О6    =(к)=>{ S().о6=к; chRender(0); };
  window.r821Вахта =(i)=>{ const s=S(); s.вахта=(s.вахта===i?null:i); chRender(0); };
  window.r821Отмотать=()=>{ const s=S(); s.отмотал=!s.отмотал; chRender(0); };
  window.r821Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r821Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r821T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=(s[ключ+'Шаг']||0)%набор.задания.length;
    const з=набор.задания[шаг];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r821TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L821; else arr.push(L821);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA821={render:render, L:L821};
})();
