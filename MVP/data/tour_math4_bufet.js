/* ============ ТУР 702 · МАТЕМАТИКА · 4 КЛАСС · «ОЧЕРЕДЬ В БУФЕТ» ============

   Это ТУР, а не урок: у него свой сюжет, свои рисунки на каждом шаге, свои
   действия руками и разбор каждого задания.

   СЮЖЕТ. Школьный буфет перед большой переменой. У стойки выстроилась
   очередь, на подносах — стаканы и булочки, на стене висит календарь,
   а за окном зооуголок. Пять карточек-заданий лежат на подносе: каждую
   надо решить, пока подходит твоя очередь.

   УСТРОЙСТВО ТУРА (12 шагов):
     1. буфет          — знакомство с сюжетом
     2, 3. обратный ход      — теория и задача
     4, 5. расстановка       — теория и задача
     6, 7. календарь         — теория и задача
     8, 9. сравнение         — теория и задача
    10, 11. знаки и порядок  — теория и задача
    12. итог           — балл и что повторить

   ЧТО ЗДЕСЬ ВАЖНО (уроки тура 701):
     • подписи не налезают на фигуры, кадр не обрезан — проверяется замером;
     • у каждой сцены свои детали, а не кружки-заглушки;
     • текст — живой речью, без «Как это работает» и «Приём: …»;
     • у каждого шага не меньше двух осмысленных анимаций;
     • `prefers-reduced-motion` выключает движение, но не ломает кадр.
   ============================================================================ */
(function(){
  'use strict';

  const ID = 702;
  const ДОМ = 't702';

  /* ── палитра проекта ── */
  const ЗОЛОТО = '#ffd76a', ЛАМПА = '#ffe9a8', ПЕРО = '#f4e9c8', ТИХО = '#8fa08f',
        ЗЕЛЁН = '#8fd1a8', КРАСН = '#e86a5a', СИНЬ = '#7fd1ff', ЛИНИЯ = '#3f7a5f';

  /* ── данные тура: пять заданий официального комплекта ── */
  const ЗАДАЧИ = [
    { id:'tour-math4-1', балл:2, тема:'Арифметические действия · обратный ход',
      название:'Потерянное число',
      условие:'Петя загадал число, умножил его на 2, прибавил 3, вычел 4 и разделил на 5. Получилось 11.',
      вопрос:'Какое число загадал Петя?',
      ответ:28,
      шаги:[['×2','умножил на 2'],['+3','прибавил 3'],['−4','вычел 4'],['÷5','разделил на 5']],
      разбор:'Идём с конца и каждое действие заменяем на противоположное. Было деление на 5 — умножаем: 11 · 5 = 55. Было «вычел 4» — прибавляем: 55 + 4 = 59. Было «прибавил 3» — отнимаем: 59 − 3 = 56. Было умножение на 2 — делим: 56 : 2 = 28. Проверка вперёд: 28 · 2 = 56, 56 + 3 = 59, 59 − 4 = 55, 55 : 5 = 11 — сходится.',
      ошибка:'Пойти вперёд и повторить те же действия с числом 11. Обратный ход снимает действия с конца и меняет каждое на противоположное.' },

    { id:'tour-math4-2', балл:2, тема:'Логика · расстановка по ограничениям',
      название:'Кто за кем',
      условие:'У буфета друг за другом стоят Катя, Вика, Оля и Маша. Вика стоит сразу за Олей. Маша и Оля не стоят рядом. Маша не последняя, а Оля не первая.',
      вопрос:'Расставь девочек по порядку — от первой до последней.',
      ответ:['Маша','Катя','Оля','Вика'],
      имена:['Маша','Катя','Оля','Вика'],
      разбор:'Вика стоит сразу за Олей — значит они пара, и Оля не первая. Пара не может стоять на местах 3–4: тогда впереди оказались бы Маша и Катя, и кто-то из них встал бы рядом с Олей, а это запрещено. Значит, пара на местах 2–3: Оля вторая, Вика третья. По краям остались Маша и Катя, и Маша не последняя — значит Маша первая, Катя четвёртая. Проверяем все условия: Вика сразу за Олей — да; Маша и Оля на местах 1 и 3 — не рядом; Маша не последняя; Оля не первая.',
      ошибка:'Поставить пару «Оля, Вика» на места 3–4. Тогда кто-то из оставшихся оказывается соседом Оли, а это условием запрещено.' },

    { id:'tour-math4-3', балл:2, тема:'Календарь · деление с остатком',
      название:'Календарь на стене',
      условие:'Год високосный, в феврале 29 дней. В этом феврале воскресенье выпало пять раз.',
      вопрос:'На какой день недели пришлось 23 февраля?',
      ответ:'понедельник',
      дни:['понедельник','вторник','среда','четверг','пятница','суббота','воскресенье'],
      разбор:'29 дней — это четыре полные недели и ещё один день: 29 = 4 · 7 + 1. Значит, пятым днём недели может быть только тот, с которого месяц начался. Пять раз было воскресенье — значит 1 февраля воскресенье. До 23 февраля проходит 22 дня: 22 = 3 · 7 + 1, то есть три недели и один день. Три недели возвращают тот же день недели, а один день сдвигает на следующий: воскресенье и ещё день — понедельник.',
      ошибка:'Решить, что 23 февраля тоже воскресенье. Воскресенья в феврале приходятся на 1, 8, 15 и 22 числа, а 23-е — уже следующий день.' },

    { id:'tour-math4-4', балл:2, тема:'Текстовые задачи · метод сравнения',
      название:'Бананы для зооуголка',
      условие:'В зооуголке 25 зверей — мартышки и шимпанзе, других нет. Каждая мартышка съела 3 банана, каждый шимпанзе — 5 бананов, а всего ушло 95 бананов.',
      вопрос:'Сколько в зооуголке мартышек и сколько шимпанзе?',
      ответ:{ мартышек:15, шимпанзе:10 },
      разбор:'Сравним с ровным предположением. Если бы все 25 зверей съели по 3 банана, ушло бы 25 · 3 = 75 бананов. На самом деле ушло 95 — на 20 бананов больше. Каждый шимпанзе съедает на 5 − 3 = 2 банана больше мартышки, значит шимпанзе 20 : 2 = 10, а мартышек 25 − 10 = 15. Проверка: 15 · 3 = 45 и 10 · 5 = 50, вместе 95 бананов при 25 зверях.',
      ошибка:'Разделить 95 на 5 или на 3. Бананы ели и мартышки, и шимпанзе, поэтому сначала надо сравнить с ровным предположением.' },

    { id:'tour-math4-5', балл:2, тема:'Выражения · порядок действий и перебор',
      название:'Пять цифр и знаки',
      условие:'На карточке цифры 5, 4, 3, 2, 1 строго в этом порядке, между ними — места для знаков «+», «−», «·», «:». Знаки могут повторяться, скобки ставить нельзя.',
      вопрос:'Какие знаки вставить, чтобы получилось ровно 27?',
      ответ:['·','+','·','+'],
      цифры:[5,4,3,2,1],
      разбор:'Умножение выполняется раньше сложения, поэтому 5 · 4 + 3 · 2 + 1 считается так: 5 · 4 = 20, 3 · 2 = 6, а потом 20 + 6 + 1 = 27. Между пятью цифрами четыре промежутка, в каждый можно поставить один из четырёх знаков — всего 4 · 4 · 4 · 4 = 256 вариантов. Перебор показывает, что ровно 27 даёт единственный вариант.',
      ошибка:'Считать слева направо, как читаем. Тогда ни один из 256 вариантов не даст 27: умножение обязано идти раньше сложения.' }
  ];

  const esc = s => String(s==null?'':s).replace(/[&<>"']/g, c =>
    ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  /* ══════════════ РИСОВАНИЕ ══════════════
     Сцена — svg 336 в ширину. Высоту задаёт сам шаг. Определения градиентов
     и фильтров лежат внутри каждого кадра: так сцена живёт без внешних файлов. */
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="${ДОМ}-небо" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#0a1622"/><stop offset="1" stop-color="#1d3a44"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f0e2c4" stop-opacity="0.30"/>
        <stop offset="0.6" stop-color="#c9b591" stop-opacity="0.20"/>
        <stop offset="1" stop-color="#8a7a5c" stop-opacity="0.16"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стойка" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c98a45"/><stop offset="1" stop-color="#6f4420"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стекло" x1="0" y1="0" x2="0.4" y2="1">
        <stop offset="0" stop-color="#fff6d0" stop-opacity="0.9"/>
        <stop offset="1" stop-color="#a9762a" stop-opacity="0.8"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-банан" x1="0.2" y1="0" x2="0.8" y2="1">
        <stop offset="0" stop-color="#ffe08a"/><stop offset="0.5" stop-color="#f0c04a"/>
        <stop offset="1" stop-color="#b8862a"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-пол" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#243a30"/><stop offset="1" stop-color="#0d1a15"/>
      </linearGradient>
      <radialGradient id="${ДОМ}-кожа" cx="0.38" cy="0.32" r="0.75">
        <stop offset="0" stop-color="#f7d9b8"/><stop offset="0.6" stop-color="#eec5a0"/>
        <stop offset="1" stop-color="#d6a67c"/>
      </radialGradient>
      <radialGradient id="${ДОМ}-свет" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#ffdf95" stop-opacity="0.55"/>
        <stop offset="1" stop-color="#ffdf95" stop-opacity="0"/>
      </radialGradient>
      <filter id="${ДОМ}-мягко" x="-40%" y="-40%" width="180%" height="180%">
        <feGaussianBlur stdDeviation="4"/>
      </filter>
      <filter id="${ДОМ}-мягко2" x="-40%" y="-40%" width="180%" height="180%">
        <feGaussianBlur stdDeviation="1.8"/>
      </filter>
      <filter id="${ДОМ}-зерно">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="3" result="шум"/>
        <feColorMatrix in="шум" type="saturate" values="0"/>
        <feComponentTransfer><feFuncA type="linear" slope="0.14"/></feComponentTransfer>
        <feComposite operator="in" in2="SourceGraphic"/>
      </filter>
    </defs>`;

  const svg = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||170}" preserveAspectRatio="xMidYMid meet"
       style="display:block;width:100%;height:auto" aria-hidden="true">
       <defs>
         <marker id="${ДОМ}-стрелка" viewBox="0 0 10 10" refX="8" refY="5"
                 markerWidth="6" markerHeight="6" orient="auto">
           <path d="M0 0 L10 5 L0 10 z" fill="rgba(255,255,255,.5)"/>
         </marker>
       </defs>${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||12}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ПЕРО}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const рамка = (x,y,ш,в,заливка,обводка,скругл,толщ) =>
    `<rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="${скругл==null?10:скругл}"
       fill="${заливка||'rgba(255,255,255,.05)'}" stroke="${обводка||ЛИНИЯ}"
       stroke-width="${толщ==null?1.4:толщ}"/>`;

  const круг = (cx,cy,r,заливка,обводка) =>
    `<circle cx="${cx}" cy="${cy}" r="${r}" fill="${заливка||'rgba(255,255,255,.06)'}"
       ${обводка?`stroke="${обводка}" stroke-width="1.5"`:''}/>`;

  /* Линия, которая прорисовывается от начала к концу: движение показывает
     связь, а не украшает. */
  const линияРисуется = (x1,y1,x2,y2,цвет,толщ,задержка) => {
    const д = Math.round(Math.hypot(x2-x1, y2-y1));
    return `<line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${цвет||ЛИНИЯ}"
      stroke-width="${толщ||1.6}" stroke-linecap="round" stroke-dasharray="${д}" stroke-dashoffset="${д}">
      <animate attributeName="stroke-dashoffset" from="${д}" to="0"
        dur="380ms" begin="${Number(задержка||0).toFixed(2)}s" fill="freeze"/></line>`;
  };

  /* ── РЕБЯТА ──
     Девочки — в платье, с косами или хвостом; мальчики — в тунике и штанах.
     Все разные: волосы, причёска, цвет одежды, рост. */
  const ДЕТИ = {
    петя:   {пол:'м', волосы:'#4a3524', причёска:'вихор',    одежда:'#5b8fd0'},
    катя:   {пол:'д', волосы:'#8a5a2a', причёска:'косы',     одежда:'#d9705f', бант:'#e8c46a'},
    вика:   {пол:'д', волосы:'#e8c46a', причёска:'хвост',    одежда:'#8a6ec0', бант:'#e86a5a'},
    оля:    {пол:'д', волосы:'#2f2a26', причёска:'косы',     одежда:'#5f9e6a', бант:'#ffd76a'},
    маша:   {пол:'д', волосы:'#b5632a', причёска:'хвост',    одежда:'#c98a3a', бант:'#7fd1ff'},
    мальчик:{пол:'м', волосы:'#3a2f28', причёска:'короткие', одежда:'#3f8f8a'},
    девочка:{пол:'д', волосы:'#6a4a2a', причёска:'косы',     одежда:'#c0607a', бант:'#ffd76a'},
    нейтр:  {пол:'',  волосы:'#5a4a3a', причёска:'короткие', одежда:'#6b7a86'}
  };

  const дитя = (x, y, рост, вид) => {
    const в = (typeof вид === 'string' ? ДЕТИ[вид] : вид) || ДЕТИ.нейтр;
    const р = рост || 30, г = р*0.26;
    const головаY = y - р + г*0.9;
    const косы = в.причёска === 'косы', хвост = в.причёска === 'хвост';
    return `
      <g>
        <ellipse cx="${x}" cy="${y+1.5}" rx="${р*0.30}" ry="${р*0.09}"
          fill="rgba(0,0,0,.42)" filter="url(#${ДОМ}-мягко2)"/>
        <ellipse cx="${x-р*0.11}" cy="${y}" rx="${р*0.09}" ry="${р*0.035}" fill="#3a2c22"/>
        <ellipse cx="${x+р*0.11}" cy="${y}" rx="${р*0.09}" ry="${р*0.035}" fill="#3a2c22"/>
        ${в.пол === 'д'
          ? `<path d="M${x-р*0.12} ${y-р*0.34} L${x+р*0.12} ${y-р*0.34}
                    L${x+р*0.30} ${y} L${x-р*0.30} ${y} Z"
               fill="${в.одежда}" stroke="rgba(0,0,0,.35)" stroke-width="1"/>
             <path d="M${x-р*0.14} ${y-р*0.60} C${x-р*0.16} ${y-р*0.42} ${x+р*0.16} ${y-р*0.42} ${x+р*0.14} ${y-р*0.60} Z"
               fill="${в.одежда}" stroke="rgba(0,0,0,.3)" stroke-width="1"/>`
          : `<path d="M${x-р*0.14} ${y-р*0.12} L${x-р*0.11} ${y-р*0.56} L${x+р*0.11} ${y-р*0.56} L${x+р*0.14} ${y-р*0.12} Z"
               fill="${в.одежда}" stroke="rgba(0,0,0,.35)" stroke-width="1"/>
             <path d="M${x-р*0.13} ${y-р*0.30} L${x+р*0.13} ${y-р*0.30}" stroke="#3a2c22"
               stroke-width="${(р*0.10).toFixed(1)}" stroke-linecap="round" opacity=".9"/>`}
        <path d="M${x-р*0.13} ${y-р*0.50} L${x-р*0.26} ${y-р*0.30}" stroke="${в.пол==='д'?в.одежда:'#e0b48f'}"
          stroke-width="${(р*0.075).toFixed(1)}" stroke-linecap="round"/>
        <path d="M${x+р*0.13} ${y-р*0.50} L${x+р*0.26} ${y-р*0.30}" stroke="${в.пол==='д'?в.одежда:'#e0b48f'}"
          stroke-width="${(р*0.075).toFixed(1)}" stroke-linecap="round"/>
        <path d="M${x} ${y-р*0.58} L${x} ${y-р*0.66}" stroke="#e0b48f" stroke-width="${(р*0.08).toFixed(1)}"/>
        <circle cx="${x}" cy="${головаY}" r="${г.toFixed(1)}" fill="url(#${ДОМ}-кожа)"
          stroke="rgba(0,0,0,.4)" stroke-width="1"/>
        <ellipse cx="${x-г*0.55}" cy="${головаY+г*0.35}" rx="${(г*0.28).toFixed(1)}" ry="${(г*0.18).toFixed(1)}"
          fill="#e08a7a" opacity=".32"/>
        <ellipse cx="${x+г*0.55}" cy="${головаY+г*0.35}" rx="${(г*0.28).toFixed(1)}" ry="${(г*0.18).toFixed(1)}"
          fill="#e08a7a" opacity=".32"/>
        <ellipse cx="${x-г*0.34}" cy="${головаY+г*0.05}" rx="${(г*0.17).toFixed(1)}" ry="${(г*0.20).toFixed(1)}" fill="#fdfaf3"/>
        <ellipse cx="${x+г*0.34}" cy="${головаY+г*0.05}" rx="${(г*0.17).toFixed(1)}" ry="${(г*0.20).toFixed(1)}" fill="#fdfaf3"/>
        <circle cx="${x-г*0.32}" cy="${головаY+г*0.07}" r="${(г*0.10).toFixed(1)}" fill="#3b2a1e"/>
        <circle cx="${x+г*0.36}" cy="${головаY+г*0.07}" r="${(г*0.10).toFixed(1)}" fill="#3b2a1e"/>
        <path d="M${x-г*0.22} ${головаY+г*0.52} Q${x} ${головаY+г*0.70} ${x+г*0.22} ${головаY+г*0.52}"
          fill="none" stroke="#a0504a" stroke-width="1.1" stroke-linecap="round"/>
        <path d="M${x-г} ${головаY-г*0.10} A${г} ${г} 0 0 1 ${x+г} ${головаY-г*0.10}
                L${x+г*0.86} ${головаY-г*0.62} L${x-г*0.86} ${головаY-г*0.62} Z"
          fill="${в.волосы}" opacity=".97"/>
        ${косы?`
          <path d="M${x-г*0.92} ${головаY+г*0.1} C${x-г*1.2} ${головаY+г*0.5} ${x-г*1.12} ${головаY+г*0.95} ${x-г*0.98} ${головаY+г*1.25}"
            stroke="${в.волосы}" stroke-width="${(г*0.30).toFixed(1)}" fill="none" stroke-linecap="round"/>
          <path d="M${x+г*0.92} ${головаY+г*0.1} C${x+г*1.2} ${головаY+г*0.5} ${x+г*1.12} ${головаY+г*0.95} ${x+г*0.98} ${головаY+г*1.25}"
            stroke="${в.волосы}" stroke-width="${(г*0.30).toFixed(1)}" fill="none" stroke-linecap="round"/>
          ${в.бант?`<circle cx="${x-г*0.98}" cy="${головаY+г*1.3}" r="${(г*0.22).toFixed(1)}" fill="${в.бант}"/>
                     <circle cx="${x+г*0.98}" cy="${головаY+г*1.3}" r="${(г*0.22).toFixed(1)}" fill="${в.бант}"/>`:''}`
        : хвост?`
          <path d="M${x+г*0.85} ${головаY-г*0.5} C${x+г*1.5} ${головаY-г*0.1} ${x+г*1.4} ${головаY+г*0.9} ${x+г*1.0} ${головаY+г*1.2}"
            stroke="${в.волосы}" stroke-width="${(г*0.34).toFixed(1)}" fill="none" stroke-linecap="round"/>
          ${в.бант?`<circle cx="${x+г*1.1}" cy="${головаY-г*0.35}" r="${(г*0.26).toFixed(1)}" fill="${в.бант}"/>`:''}`
        : `<path d="M${x-г*0.9} ${головаY-г*0.35} L${x-г*0.2} ${головаY-г*1.05} L${x+г*0.5} ${головаY-г*0.65}"
             fill="${в.волосы}" opacity=".95"/>`}
      </g>`;
  };

  /* ── ПРЕДМЕТЫ БУФЕТА ── */

  /* Стойка буфета: полка, витрина под стеклом, поднос. */
  const стойка = (x,y,ш,в) => `
    <g>
      <ellipse cx="${x+ш/2}" cy="${y+в+2}" rx="${ш*0.55}" ry="5" fill="rgba(0,0,0,.4)"
        filter="url(#${ДОМ}-мягко)"/>
      <rect x="${x}" y="${y+в*0.55}" width="${ш}" height="${в*0.45}" rx="3"
        fill="url(#${ДОМ}-стойка)" stroke="#3a2210" stroke-width="1.6"/>
      <rect x="${x}" y="${y+в*0.55}" width="${ш}" height="${в*0.45}" rx="3" fill="#000" opacity=".12"
        filter="url(#${ДОМ}-зерно)"/>
      <rect x="${x+4}" y="${y}" width="${ш-8}" height="${в*0.55}" rx="4"
        fill="rgba(255,246,208,.10)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
      <path d="M${x+10} ${y+6} L${x+ш*0.4} ${y+в*0.5-6}" stroke="#fff" stroke-width="1.2"
        opacity=".18" stroke-linecap="round"/>
      <rect x="${x-4}" y="${y+в*0.5}" width="${ш+8}" height="5" rx="2"
        fill="url(#${ДОМ}-стойка)" stroke="#3a2210" stroke-width="1"/>
    </g>`;

  /* Поднос с бортиком. */
  const поднос = (x,y,ш,в) => `
    <g>
      <ellipse cx="${x+ш/2}" cy="${y+в+1}" rx="${ш*0.5}" ry="3.5" fill="rgba(0,0,0,.35)"
        filter="url(#${ДОМ}-мягко2)"/>
      <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="${в/2}"
        fill="url(#${ДОМ}-стойка)" stroke="#3a2210" stroke-width="1.5"/>
      <rect x="${x+4}" y="${y+3}" width="${ш-8}" height="${в-6}" rx="${(в-6)/2}"
        fill="rgba(255,255,255,.06)"/>
    </g>`;

  /* Стакан с компотом: стекло, жидкость, блик. */
  const стакан = (x,y,м) => {
    const к = м||1;
    return `
      <g transform="translate(${x} ${y}) scale(${к})">
        <path d="M-7 -14 L7 -14 L5.6 0 L-5.6 0 Z" fill="rgba(255,255,255,.10)"
          stroke="rgba(255,255,255,.45)" stroke-width="1.1"/>
        <path d="M-6.4 -6 L6.4 -6 L5.6 0 L-5.6 0 Z" fill="url(#${ДОМ}-стекло)" opacity=".85"/>
        <path d="M-5 -12 L-3.6 -3" stroke="#fff" stroke-width="1" opacity=".35"/>
      </g>`;
  };

  /* Булочка: тело, надрез, крошки. */
  const булочка = (x,y,м) => {
    const к = м||1;
    return `
      <g transform="translate(${x} ${y}) scale(${к})">
        <ellipse cx="0" cy="-4" rx="9" ry="6" fill="#d9a05a" stroke="#8a5a2a" stroke-width="1.1"/>
        <path d="M-6 -5 Q0 -9 6 -5" fill="none" stroke="#8a5a2a" stroke-width="1"/>
        <circle cx="-3" cy="-7" r="1" fill="#fff3d0" opacity=".8"/>
        <circle cx="2" cy="-8" r="0.9" fill="#fff3d0" opacity=".8"/>
      </g>`;
  };

  /* Банан: дуга, кончики, блик. */
  const банан = (x,y,м,поворот) => {
    const к = м||1;
    return `
      <g transform="translate(${x} ${y}) rotate(${поворот||0}) scale(${к})">
        <path d="M-9 3 C-6 -6 6 -6 9 3 C6 0 -6 0 -9 3 Z"
          fill="url(#${ДОМ}-банан)" stroke="#8a6420" stroke-width="1"/>
        <path d="M-7 1 C-4 -4 4 -4 7 1" fill="none" stroke="#fff3c4" stroke-width="0.9" opacity=".5"/>
      </g>`;
  };

  /* Мартышка и шимпанзе: у мартышки светлая шерсть и тонкий хвост,
     у шимпанзе тёмная и крупнее. */
  const обезьяна = (x,y,рост,вид) => {
    const р = рост||28, г = р*0.30;
    const шимп = вид==='шимпанзе';
    const шерсть = шимп ? '#3a2a22' : '#b98a52';
    const морда  = шимп ? '#c99a72' : '#e8c9a0';
    const головаY = y - р + г*0.85;
    return `
      <g>
        <ellipse cx="${x}" cy="${y+1.5}" rx="${р*0.30}" ry="${р*0.09}" fill="rgba(0,0,0,.42)"
          filter="url(#${ДОМ}-мягко2)"/>
        ${шимп?'':'<path d="M'+x+' '+(y-р*0.45)+' C'+(x+р*0.7)+' '+(y-р*0.5)+' '+(x+р*0.75)+' '+(y-р*1.1)+' '+(x+р*0.25)+' '+(y-р*1.15)+'" fill="none" stroke="'+шерсть+'" stroke-width="2.4" stroke-linecap="round"/>'}
        <path d="M${x-р*0.18} ${y-р*0.10} L${x-р*0.14} ${y-р*0.55} L${x+р*0.14} ${y-р*0.55} L${x+р*0.18} ${y-р*0.10} Z"
          fill="${шерсть}" stroke="rgba(0,0,0,.4)" stroke-width="1"/>
        <ellipse cx="${x}" cy="${y-р*0.32}" rx="${р*0.20}" ry="${р*0.16}" fill="${морда}" opacity=".55"/>
        <path d="M${x-р*0.16} ${y-р*0.48} L${x-р*0.32} ${y-р*0.22}" stroke="${шерсть}" stroke-width="${(р*0.10).toFixed(1)}" stroke-linecap="round"/>
        <path d="M${x+р*0.16} ${y-р*0.48} L${x+р*0.32} ${y-р*0.22}" stroke="${шерсть}" stroke-width="${(р*0.10).toFixed(1)}" stroke-linecap="round"/>
        <circle cx="${x}" cy="${головаY}" r="${г.toFixed(1)}" fill="${шерсть}" stroke="rgba(0,0,0,.4)" stroke-width="1"/>
        <ellipse cx="${x}" cy="${головаY+г*0.15}" rx="${(г*0.62).toFixed(1)}" ry="${(г*0.55).toFixed(1)}"
          fill="${морда}" stroke="rgba(0,0,0,.25)" stroke-width="0.8"/>
        <circle cx="${x-г*0.24}" cy="${головаY-г*0.05}" r="${(г*0.11).toFixed(1)}" fill="#2b2118"/>
        <circle cx="${x+г*0.24}" cy="${головаY-г*0.05}" r="${(г*0.11).toFixed(1)}" fill="#2b2118"/>
        <path d="M${x-г*0.22} ${головаY+г*0.35} Q${x} ${головаY+г*0.52} ${x+г*0.22} ${головаY+г*0.35}"
          fill="none" stroke="#5a3a2a" stroke-width="1" stroke-linecap="round"/>
        <ellipse cx="${x-г*0.85}" cy="${головаY-г*0.05}" rx="${(г*0.26).toFixed(1)}" ry="${(г*0.32).toFixed(1)}" fill="${шерсть}"/>
        <ellipse cx="${x+г*0.85}" cy="${головаY-г*0.05}" rx="${(г*0.26).toFixed(1)}" ry="${(г*0.32).toFixed(1)}" fill="${шерсть}"/>
      </g>`;
  };

  /* Карточка-задание на подносе: листок с загнутым уголком. */
  const карточка = (x,y,ш,в,строка) => `
    <g>
      <path d="M${x} ${y} L${x+ш-10} ${y} L${x+ш} ${y+10} L${x+ш} ${y+в} L${x} ${y+в} Z"
        fill="rgba(255,246,208,.92)" stroke="#b8a06a" stroke-width="1.2"/>
      <path d="M${x+ш-10} ${y} L${x+ш-10} ${y+10} L${x+ш} ${y+10} Z" fill="rgba(0,0,0,.12)"/>
      ${строка?т(x+ш/2,y+в*0.62,строка,12,'#4a3a20',true):''}
    </g>`;

  /* Разбор: подробный ответ и частая ошибка. */
  const разбор = (текст, ошибка) =>
    `<div class="card" style="border-color:${ЗЕЛЁН};padding:12px 14px">
       <div style="font-size:16px;font-weight:bold;color:${ЗЕЛЁН};margin-bottom:6px">✅ Разбор</div>
       <div style="font-size:16px;line-height:1.55">${esc(текст)}</div>
       ${ошибка?`<div style="font-size:14px;line-height:1.5;color:${КРАСН};margin-top:8px">⚠ ${esc(ошибка)}</div>`:''}
     </div>`;

  /* Жетон с числом: табличка с рамкой, тенью и уголками. */
  const жетон = (x,y,текст,цвет,размер) => {
    const к = размер||20, ш=к*1.9, в=к*1.25;
    return `
      <g>
        <rect x="${x-ш/2+1.5}" y="${y-в/2+2.5}" width="${ш}" height="${в}" rx="5"
          fill="rgba(0,0,0,.45)" filter="url(#${ДОМ}-мягко2)"/>
        <rect x="${x-ш/2}" y="${y-в/2}" width="${ш}" height="${в}" rx="5"
          fill="url(#${ДОМ}-стена)" stroke="${цвет||ЛИНИЯ}" stroke-width="1.6"/>
        <path d="M${x-ш/2+3} ${y-в/2+1} L${x-ш/2+1} ${y-в/2+3}" stroke="${цвет||ЛИНИЯ}" stroke-width="1.3"/>
        <path d="M${x+ш/2-3} ${y+в/2-1} L${x+ш/2-1} ${y+в/2-3}" stroke="${цвет||ЛИНИЯ}" stroke-width="1.3"/>
        <text x="${x}" y="${y+к*0.36}" text-anchor="middle" font-size="${к}"
          font-weight="bold" fill="${цвет||ПЕРО}" font-family="Georgia,serif"
          style="font-variant-numeric:tabular-nums">${esc(текст)}</text>
      </g>`;
  };

  /* Знак операции в кружке: ×2, +3, −4, ÷5. */
  const операция = (x,y,знак,подпись,цвет,жирная) => `
    <g>
      <circle cx="${x}" cy="${y}" r="17" fill="rgba(255,255,255,.06)"
        stroke="${цвет||ЗОЛОТО}" stroke-width="${жирная?2:1.6}"/>
      ${т(x,y+5,знак,16,цвет||ЗОЛОТО,true)}
      ${подпись?т(x,y+30,подпись,12,ТИХО):''}
    </g>`;

  /* ══════════════ ШАГИ ══════════════ */
  const ШАГИ = [];

  /* ── 1. БУФЕТ: знакомство ── */
  ШАГИ.push({
    т:'Буфет',
    рисунок(){
      let s = `<rect x="8" y="8" width="320" height="168" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="168" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,26,'Скоро большая перемена',14,ЗОЛОТО,true)}`;

      /* пол и стойка */
      s += `<rect x="8" y="120" width="320" height="56" fill="url(#${ДОМ}-пол)" opacity=".8"/>`;
      s += `<path d="M8 120 L328 120" stroke="${ЛИНИЯ}" stroke-width="1.4"/>`;
      s += стойка(190,72,124,48);

      /* на витрине: поднос, стаканы, булочки */
      s += поднос(202,86,66,12);
      s += `${стакан(214,92,1.1)}${стакан(228,92,1.1)}`;
      s += `<g>${булочка(262,96,1.3)}
              <animateTransform attributeName="transform" type="translate"
                values="0 0; 0 -1.5; 0 0" dur="3s" repeatCount="indefinite"/>
            </g>`;
      /* пар над булочками: движение показывает, что еда горячая */
      [258,266,274].forEach((x,i)=>{
        s += `<circle cx="${x}" cy="${82}" r="2.2" fill="#dfe7ee" opacity=".3">
                <animate attributeName="cy" values="86;68" dur="${(3.4+i*0.5).toFixed(1)}s"
                  begin="${(i*0.7).toFixed(1)}s" repeatCount="indefinite"/>
                <animate attributeName="opacity" values=".35;0" dur="${(3.4+i*0.5).toFixed(1)}s"
                  begin="${(i*0.7).toFixed(1)}s" repeatCount="indefinite"/>
              </circle>`;
      });

      /* очередь ребят: стоят и ждут, каждый «дышит» */
      const очередь=[['петя',60],['катя',96],['вика',132]];
      очередь.forEach(([кто,x],i)=>{
        s += `<g>${дитя(x,120,44,кто)}
                <animateTransform attributeName="transform" type="translate"
                  values="0 0; 0 -2; 0 0" dur="${(2.4+i*0.3).toFixed(2)}s"
                  begin="${(i*0.25).toFixed(2)}s" repeatCount="indefinite"/>
              </g>`;
      });

      /* Карточки-задания стоят на стойке справа: слева очередь, и карточки
         её не загораживают. Верхняя чуть качается — видно, что это бумага. */
      s += `<g>${карточка(198,42,46,30,'№ 1')}
              <animateTransform attributeName="transform" type="rotate"
                values="-2 221 57; 2 221 57; -2 221 57" dur="5s" repeatCount="indefinite"/>
            </g>`;
      s += карточка(210,34,46,30,'');
      s += `${т(168,162,'пять карточек — пять задач тура',12,ТИХО)}`;
      return svg(s,184);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Перед большой переменой в школьном буфете всегда очередь. На подносе у стойки лежат пять карточек с задачами: пока подходит твоя очередь, их можно решить.</div>
          <div style="font-size:16px;line-height:1.5;margin-top:8px">Задач пять, за каждую можно получить 2 балла — всего 10. В конце будет итог: балл и что стоит повторить.</div>
        </div>`;
    }
  });

  /* ── 2. ОБРАТНЫЙ ХОД: теория ──
     Показываем цепочку действий Пети и проход по ней с конца: каждое
     действие снимается противоположным. Строка рассуждения меняется по
     шагам, как в туре 701, но здесь ещё и числа пересчитываются на глазах. */
  ШАГИ.push({
    т:'Обратный ход',
    рисунок(){
      /* Цепочка Пети — вертикально, по строкам: число, действие, что вышло.
         Справа у каждой строки написано, как это действие снимается назад.
         Разбор идёт с НИЗУ ВВЕРХ: сначала снимаем деление, потом вычитание,
         потом прибавление, потом умножение — по строке за раз. */
      const строки=[
        {было:28, знак:'×2', стало:56, назад:'назад: делим на 2'},
        {было:56, знак:'+3', стало:59, назад:'назад: минус 3'},
        {было:59, знак:'−4', стало:55, назад:'назад: плюс 4'},
        {было:55, знак:'÷5', стало:11, назад:'назад: умножаем на 5'}
      ];
      const КРУГ = 16, ДОЛЯ = КРУГ/строки.length;

      let s = `<rect x="8" y="8" width="320" height="200" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="200" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Петя загадал число',14,ЗОЛОТО,true)}`;

      /* строка рассуждения: идёт снизу вверх, поэтому задержки в обратном порядке */
      строки.forEach((стр,i)=>{
        const обратный = строки.length-1-i;
        s += `<text class="${ДОМ}-речь${i===0?' '+ДОМ+'-речь-первая':''}" x="168" y="44"
                text-anchor="middle" font-size="14" font-weight="bold" fill="${ЛАМПА}"
                font-family="Georgia,serif" opacity="0"
                style="animation:${ДОМ}-строка ${КРУГ}s linear ${(обратный*ДОЛЯ).toFixed(2)}s infinite">${esc('снимаем «'+стр.знак+'»: '+стр.назад.replace('назад: ',''))}</text>`;
      });

      /* строки цепочки */
      строки.forEach((стр,i)=>{
        const y=76+i*32, обратный = строки.length-1-i;
        /* подсветка строки: обходит строки снизу вверх */
        s += `<rect x="20" y="${y-15}" width="296" height="28" rx="9" fill="rgba(255,215,106,.10)"
                stroke="${ЗОЛОТО}" stroke-width="1.4" opacity="0">
                <animate attributeName="opacity" values="0;.9;.9;0" dur="${КРУГ}s"
                  begin="${(обратный*ДОЛЯ).toFixed(2)}s" repeatCount="indefinite"
                  calcMode="discrete" keyTimes="0;0.02;0.19;0.21"/>
              </rect>`;
        s += `${жетон(66,y,String(стр.было),ЛИНИЯ,16)}`;
        s += `${операция(126,y,стр.знак,'',ЗОЛОТО)}`;
        s += `${жетон(190,y,String(стр.стало),i===строки.length-1?ЗЕЛЁН:ЗОЛОТО,16)}`;
        s += `${т(262,y+4,стр.назад,12,ТИХО)}`;
      });

      s += `${т(168,196,'снимаем действия с конца',12,ТИХО)}`;
      return svg(s,212);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Петя умножал и прибавлял, а мы знаем только, что вышло 11. Значит, идём с конца и каждое действие снимаем противоположным: было деление — умножаем, было «вычел» — прибавляем.</div>
        </div>`;
    }
  });


  /* ── состояние ответов: живёт в CHS, как в туре 701 ── */
  function память(){
    const к = (typeof lidKey === 'function') ? lidKey(ID) : String(ID);
    if(typeof CHS === 'undefined') window.CHS = {};
    if(!CHS[к]) CHS[к] = {};
    const п = CHS[к];
    if(!п.ответы) п.ответы = {};
    if(!п.проверено) п.проверено = {};
    return п;
  }
  const перерисовать = () => { if(typeof chRender === 'function') chRender(0); };

  /* Балл тура: по задачам, по 2 балла за каждую. */
  function баллТура(){
    const п=память(); let б=0;
    if(Number(п.ответы[0])===ЗАДАЧИ[0].ответ) б+=2;
    const о2=п.ответы[1];
    if(Array.isArray(о2) && о2.join('|')===ЗАДАЧИ[1].ответ.join('|')) б+=2;
    if(п.ответы[2]===ЗАДАЧИ[2].ответ) б+=2;
    const о4=п.ответы[3]||{};
    if(Number(о4.мартышек)===ЗАДАЧИ[3].ответ.мартышек && Number(о4.шимпанзе)===ЗАДАЧИ[3].ответ.шимпанзе) б+=2;
    const о5=п.ответы[4];
    if(Array.isArray(о5) && о5.join('')===ЗАДАЧИ[4].ответ.join('')) б+=2;
    return б;
  }

  /* Счёт числа в DOM: в svg текстом управлять нельзя. */
  function счётДо(ид, до, мс){
    try{
      const el=document.getElementById(ид); if(!el) return;
      const t0=performance.now(), длит=мс||700;
      const шаг=()=>{
        const k=Math.min(1,(performance.now()-t0)/длит);
        el.textContent=Math.round(до*(1-Math.pow(1-k,3)));
        if(k<1) requestAnimationFrame(шаг); else el.textContent=до;
      };
      requestAnimationFrame(шаг);
    }catch(e){}
  }

  /* ── 3. ПОТЕРЯННОЕ ЧИСЛО: считаем сами ── */
  ШАГИ.push({
    т:'Потерянное число',
    рисунок(){
      const п=память(), дан=Number(п.ответы[0])===ЗАДАЧИ[0].ответ;
      /* Обратная цепочка: 11 → ×5 → 55 → +4 → 59 → −3 → 56 → ÷2 → 28.
         Пока не решено, на месте чисел стоят вопросы — их и надо найти. */
      const звенья=[
        {оп:'×5', знач:55},
        {оп:'+4', знач:59},
        {оп:'−3', знач:56},
        {оп:'÷2', знач:28}
      ];
      let s = `<rect x="8" y="8" width="320" height="176" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="176" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,26,'Идём с конца',14,ЗОЛОТО,true)}`;
      s += `${т(168,46,'было 11 — какое число получится на каждом шаге назад?',12,ТИХО)}`;

      const y=96, шз=(320-4*28)/5;      /* 5 чисел и 4 знака между ними */
      let x=8+шз/2;
      s += жетон(x,y,'11',ЗЕЛЁН,16);
      звенья.forEach((з,i)=>{
        x += шз/2 + 28/2;
        s += `<g>${операция(x,y,з.оп,'',ЗОЛОТО)}
                <animate attributeName="opacity" values=".4;1;.4" dur="2.4s"
                  begin="${(i*0.4).toFixed(2)}s" repeatCount="indefinite"/>
              </g>`;
        x += 28/2 + шз/2;
        const показать = дан || п.проверено[0];
        s += жетон(x,y, показать?String(з.знач):'?', показать?ЗОЛОТО:ТИХО,16);
      });

      s += `${т(168,140, дан ? 'число найдено' : 'введи число в ответе ниже',12, дан?ЗЕЛЁН:ТИХО)}`;
      s += `${т(168,166,'назад каждое действие меняется на противоположное',12,ТИХО)}`;
      return svg(s,192);
    },
    низ(){
      const п=память(), проверено=п.проверено[0];
      const верно=Number(п.ответы[0])===ЗАДАЧИ[0].ответ;
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[0].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[0].вопрос)}</div>
        </div>`;
      блок += `<div class="card ${проверено&&!верно?'':ДОМ+'-ждёт'}" style="padding:12px 14px">
          <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
            <input id="${ДОМ}-число" type="number" inputmode="numeric" value="${п.ответы[0]||''}"
              style="min-height:48px;width:120px;font-size:20px;padding:0 12px;border-radius:10px;
                     border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8"
              placeholder="число">
            <button class="btn" style="min-height:48px;margin:0" onclick="Т702.число()">Проверить</button>
          </div>
        </div>`;
      if(проверено) блок += разбор(ЗАДАЧИ[0].разбор, ЗАДАЧИ[0].ошибка);
      if(верно) блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: очередь →</button></div>`;
      return блок;
    }
  });

  /* ── 4. РАССТАНОВКА: теория ── */
  ШАГИ.push({
    т:'Кто за кем',
    рисунок(){
      /* Четыре места и три условия. Разбор идёт по шагам: сначала пара
         «Оля — Вика», потом проверка мест, потом края. Рамка показывает,
         куда пара может встать. */
      const КРУГ=12, ДОЛЯ=КРУГ/4;
      let s = `<rect x="8" y="8" width="320" height="196" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="196" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Вика стоит сразу за Олей',14,ЗОЛОТО,true)}`;

      const мысли=[
        'Оля и Вика — пара, и Оля не первая',
        'пара не на 3–4: рядом с Олей сядет сосед',
        'значит Оля вторая, Вика третья',
        'по краям Маша и Катя: Маша не последняя'
      ];
      мысли.forEach((м,i)=>{
        s += `<text class="${ДОМ}-речь${i===0?' '+ДОМ+'-речь-первая':''}" x="168" y="46"
                text-anchor="middle" font-size="14" font-weight="bold" fill="${ЛАМПА}"
                font-family="Georgia,serif" opacity="0"
                style="animation:${ДОМ}-строка ${КРУГ}s linear ${(i*ДОЛЯ).toFixed(2)}s infinite">${esc(м)}</text>`;
      });

      /* четыре места */
      const места=['1','2','3','4'];
      места.forEach((м,i)=>{
        const x=56+i*76;
        s += рамка(x-32,70,64,56,'rgba(255,255,255,.05)',ЛИНИЯ,10,1.4);
        s += `${т(x,86,м,14,ЗОЛОТО,true)}`;
        s += `${т(x,116,'место',12,ТИХО)}`;
      });
      /* рамка ходит по парам мест: 1–2, 2–3, 3–4 */
      s += `<rect x="24" y="66" width="140" height="64" rx="12" fill="none" stroke="${ЗОЛОТО}" stroke-width="2">
              <animate attributeName="x" values="24;100;176;24" dur="${КРУГ}s"
                repeatCount="indefinite" calcMode="discrete"/>
              <animate attributeName="opacity" values=".9;.5;.9" dur="${КРУГ}s"
                repeatCount="indefinite" calcMode="discrete"/>
            </rect>`;

      /* условия списком, каждое со своей отметкой */
      const условия=['Вика сразу за Олей','Маша и Оля не рядом','Маша не последняя, Оля не первая'];
      условия.forEach((у,i)=>{
        s += `<circle cx="28" cy="${152+i*18}" r="4" fill="${ЗОЛОТО}" opacity=".8"/>`;
        s += т(40, 156+i*18, у, 12, ПЕРО, false, 'start');
      });
      return svg(s,212);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Каждое условие отсекает часть вариантов. Пара «Оля — Вика» не может стоять в конце, потому что тогда кто-то из оставшихся окажется соседом Оли, а это запрещено. Остаётся одно место для пары — и дальше края определяются сами.</div>
        </div>`;
    }
  });

  /* ── 5. ОЧЕРЕДЬ: расставляем сами ── */
  ШАГИ.push({
    т:'Очередь',
    рисунок(){
      const п=память(), очередь=п.ответы[1]||[];
      const верно = очередь.join('|')===ЗАДАЧИ[1].ответ.join('|');
      const вид={'Маша':'маша','Катя':'катя','Оля':'оля','Вика':'вика'};
      let s = `<rect x="8" y="8" width="320" height="198" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="198" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24, очередь.length<4 ? 'Расставь девочек по порядку' : (верно?'порядок верный':'проверь условия ещё раз'),14, очередь.length<4?ЗОЛОТО:(верно?ЗЕЛЁН:КРАСН),true)}`;

      /* пол и четыре места */
      s += `<rect x="8" y="128" width="320" height="66" fill="url(#${ДОМ}-пол)" opacity=".7"/>`;
      s += `<path d="M8 128 L328 128" stroke="${ЛИНИЯ}" stroke-width="1.4"/>`;
      s += поднос(30,146,276,14);
      [0,1,2,3].forEach(i=>{
        const x=74+i*62;
        s += `${т(x,124,'место '+(i+1),12,ТИХО)}`;
        const имя=очередь[i];
        if(имя){
          s += `<g>${дитя(x,128,42,вид[имя]||'нейтр')}
                  <animateTransform attributeName="transform" type="translate"
                    values="0 0; 0 -2; 0 0" dur="${(2.4+i*0.3).toFixed(2)}s"
                    begin="${(i*0.2).toFixed(2)}s" repeatCount="indefinite"/>
                </g>`;
          s += `${т(x,120,имя,12,ПЕРО,true)}`;
        } else {
          s += `<g>${т(x,100,'?',20,ТИХО,true)}
                  <animate attributeName="opacity" values=".35;1;.35" dur="2s"
                    begin="${(i*0.3).toFixed(2)}s" repeatCount="indefinite"/>
                </g>`;
        }
      });
      s += `${т(168,172,'Вика сразу за Олей · Маша и Оля не рядом',12,ТИХО)}`;
      s += `${т(168,188,'Маша не последняя · Оля не первая',12,ТИХО)}`;
      return svg(s,214);
    },
    низ(){
      const п=память(), очередь=п.ответы[1]||[];
      const верно = очередь.join('|')===ЗАДАЧИ[1].ответ.join('|');
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[1].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[1].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${ДОМ}-ждёт" style="padding:12px 14px">
            <div style="font-size:14px;color:var(--muted);font-variant-numeric:tabular-nums">поставлено ${очередь.length} из 4</div>
            <div style="display:flex;gap:10px;flex-wrap:wrap;margin-top:10px">` +
          ЗАДАЧИ[1].имена.map(им=>
            `<button class="btn" style="min-height:48px;min-width:104px;margin:0"
               onclick="Т702.очередь('${esc(им)}')">${esc(им)}</button>`).join('') +
          `</div></div>`;
        if(очередь.length)
          блок += `<div class="btnrow"><button class="btn ghost" onclick="Т702.очередьСброс()">Сбросить</button></div>`;
      } else {
        блок += разбор(ЗАДАЧИ[1].разбор, ЗАДАЧИ[1].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: календарь →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 6. КАЛЕНДАРЬ: теория ── */
  ШАГИ.push({
    т:'Календарь на стене',
    рисунок(){
      /* Февраль високосного года: 29 дней. 1 февраля — воскресенье, значит
         воскресенья выпадают на 1, 8, 15 и 22. Двадцать третье — уже
         понедельник, его и надо найти. */
      const дни=['вс','пн','вт','ср','чт','пт','сб'];
      let s = `<rect x="8" y="8" width="320" height="196" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="196" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Февраль: 29 дней',14,ЗОЛОТО,true)}`;

      /* шапка дней недели */
      дни.forEach((д,i)=>{
        const x=36+i*40;
        s += `${т(x,44,д,12, д==='вс'?ЗЕЛЁН:ТИХО, д==='вс')}`;
      });
      /* сетка: 1 февраля — воскресенье, поэтому первый столбец воскресный */
      for(let н=1;н<=29;н++){
        const столбец=(н-1)%7, ряд=Math.floor((н-1)/7);
        const x=36+столбец*40, y=64+ряд*24;
        const воскр=(столбец===0);
        const это23=(н===23);
        s += рамка(x-16,y-15,32,20, воскр?'rgba(143,209,168,.14)':'rgba(255,255,255,.04)',
                   воскр?ЗЕЛЁН:ЛИНИЯ, 6, это23?2:1);
        s += `${т(x,y,н,12, это23?ЗОЛОТО:(воскр?ЗЕЛЁН:ПЕРО), это23||воскр)}`;
        if(воскр){
          s += `<circle cx="${x}" cy="${y-6}" r="14" fill="none" stroke="${ЗЕЛЁН}" stroke-width="1.3" opacity="0">
                  <animate attributeName="opacity" values="0;.7;0" dur="3s"
                    begin="${(ряд*0.4).toFixed(2)}s" repeatCount="indefinite"/>
                </circle>`;
        }
        if(это23){
          s += `<circle cx="${x}" cy="${y-6}" r="16" fill="none" stroke="${ЗОЛОТО}" stroke-width="2">
                  <animate attributeName="r" values="13;19;13" dur="2.4s" repeatCount="indefinite"/>
                  <animate attributeName="opacity" values=".8;.2;.8" dur="2.4s" repeatCount="indefinite"/>
                </circle>`;
        }
      }
      s += `${т(168,190,'29 = 4 недели и ещё один день',12,ТИХО)}`;
      return svg(s,204);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">В феврале 29 дней — это четыре полные недели и ещё один день. Пятым может стать только тот день недели, с которого месяц начался. Раз воскресенье выпало пять раз, значит 1 февраля — воскресенье. От 1-го до 23-го проходит 22 дня: три недели и один день.</div>
        </div>`;
    }
  });

  /* ── 7. ДЕНЬ НЕДЕЛИ: выбираем ── */
  ШАГИ.push({
    т:'День недели',
    рисунок(){
      const п=память(), выбран=п.ответы[2];
      const верно = выбран===ЗАДАЧИ[2].ответ;
      const дни=['вс','пн','вт','ср','чт','пт','сб'];
      let s = `<rect x="8" y="8" width="320" height="176" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="176" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Каким днём было 23 февраля?',14,ЗОЛОТО,true)}`;

      /* полоска дней недели: 22-е воскресенье, 23-е — искомый */
      const y=86;
      s += `${т(168,50,'воскресенья: 1, 8, 15, 22',12,ЗЕЛЁН)}`;
      [['22 февраля','вс',ЗЕЛЁН],['23 февраля','?',ЗОЛОТО]].forEach(([подпись,день,ц],i)=>{
        const x=104+i*128;
        s += рамка(x-52,y-24,104,48,'rgba(255,255,255,.05)',ц,10,1.6);
        s += `${т(x,y-4,подпись,12,ПЕРО)}`;
        s += `${т(x,y+16, день==='?' ? (выбранApprox(выбран,дни)||'?') : 'воскресенье', 12, ц, true)}`;
      });
      function выбранApprox(в,дни){ return в?дни[(дни.indexOf(в)+1)%7]:null; }
      /* стрелка «+1 день» между ними */
      s += линияРисуется(160,y-6,196,y-6,ЗОЛОТО,1.6,0.3);
      s += `${т(178,y-14,'+1 день',12,ТИХО)}`;

      s += `${т(168,140, выбран ? (верно?'верно: понедельник':'это не следующий день') : 'выбери день недели в ответе ниже',12, выбран?(верно?ЗЕЛЁН:КРАСН):ТИХО)}`;
      s += `${т(168,162,'четыре недели — тот же день, лишний день сдвигает',12,ТИХО)}`;
      return svg(s,192);
    },
    низ(){
      const п=память(), выбран=п.ответы[2];
      const верно = выбран===ЗАДАЧИ[2].ответ;
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[2].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[2].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${ДОМ}-ждёт" style="padding:12px 14px">
            <div style="display:flex;gap:8px;flex-wrap:wrap">` +
          ЗАДАЧИ[2].дни.map(д=>
            `<button class="btn" style="min-height:48px;margin:0;${выбран===д?'border-color:'+КРАСН:''}"
               onclick="Т702.день('${esc(д)}')">${esc(д)}</button>`).join('') +
          `</div></div>`;
        if(выбран) блок += `<div class="card" style="border-color:${КРАСН};padding:12px 14px;font-size:16px">Не сходится: 23-е — это следующий день после 22-го. Попробуй другой.</div>`;
      } else {
        блок += разбор(ЗАДАЧИ[2].разбор, ЗАДАЧИ[2].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: зооуголок →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 8. СРАВНЕНИЕ: теория ── */
  ШАГИ.push({
    т:'Бананы для зооуголка',
    рисунок(){
      /* Метод сравнения: сначала ровное предположение (все по 3 банана),
         потом лишние бананы делим на добавку каждого шимпанзе. */
      let s = `<rect x="8" y="8" width="320" height="200" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="200" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Сравниваем с ровным предположением',14,ЗОЛОТО,true)}`;

      const строки=[
        {л:'если бы все ели по 3 банана', ц:'25 · 3 = 75', балл:75, цвет:ЛИНИЯ},
        {л:'на самом деле ушло', ц:'95', балл:95, цвет:ПЕРО},
        {л:'лишних бананов', ц:'95 − 75 = 20', балл:20, цвет:ЗОЛОТО},
        {л:'на столько больше съедает шимпанзе', ц:'5 − 3 = 2', балл:2, цвет:СИНЬ},
        {л:'значит, шимпанзе', ц:'20 : 2 = 10', балл:10, цвет:ЗЕЛЁН}
      ];
      строки.forEach((стр,i)=>{
        const y=54+i*26;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 400ms ease-out ${(0.15+i*0.35).toFixed(2)}s 1 forwards">
                ${рамка(24,y-13,288,24,'rgba(255,255,255,.04)',стр.цвет,7,1.2)}
                ${т(36,y+4,стр.л,12,ПЕРО,false,'start')}
                ${т(300,y+4,стр.ц,12,стр.цвет,true,'end')}
              </g>`;
      });

      /* бананы: двадцать пять порций и лишние двадцать */
      for(let i=0;i<10;i++){
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 260ms ease-out ${(0.2+i*0.05).toFixed(2)}s 1 forwards">
                ${банан(40+i*24,196,0.9,-12+((i%3)*12))}
              </g>`;
      }
      s += `${т(300,194,'поровну по 3',12,ТИХО,false,'end')}`;
      return svg(s,212);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Сначала представим, что все звери съели поровну — по 3 банана. Тогда ушло бы 75 бананов. На самом деле ушло 95, то есть на 20 больше. Каждый шимпанзе съедает на 2 банана больше мартышки, значит этих шимпанзе 20 : 2 = 10, а мартышек остаётся 15.</div>
        </div>`;
    }
  });

  /* ── 9. ЗООУГОЛОК: вводим числа ── */
  ШАГИ.push({
    т:'Зооуголок',
    рисунок(){
      const п=память(), о=п.ответы[3]||{};
      const верно = Number(о.мартышек)===ЗАДАЧИ[3].ответ.мартышек &&
                    Number(о.шимпанзе)===ЗАДАЧИ[3].ответ.шимпанзе;
      const проверено = п.проверено[3];
      const м = проверено ? Number(о.мартышек)||0 : 0;
      const ш = проверено ? Number(о.шимпанзе)||0 : 0;

      let s = `<rect x="8" y="8" width="320" height="186" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="186" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Двадцать пять зверей и 95 бананов',14,ЗОЛОТО,true)}`;

      /* вольер: слева мартышки, справа шимпанзе */
      [['мартышки',м,3,ЗЕЛЁН,88],['шимпанзе',ш,5,СИНЬ,248]].forEach(([подпись,количество,порция,ц,x])=>{
        s += рамка(x-72,44,144,116,'rgba(255,255,255,.04)',ц,10,1.4);
        s += `${т(x,64,подпись,12,ц,true)}`;
        s += `${т(x,80,'по '+порция+' банана',12,ТИХО)}`;
        /* показываем до десяти зверей, чтобы не мельчить */
        const видно=Math.min(количество,10);
        for(let i=0;i<видно;i++){
          const кx=x-60+(i%5)*30, ку=106+Math.floor(i/5)*30;
          s += `<g opacity="0" style="animation:${ДОМ}-фрукт 300ms ease-out ${(0.1+i*0.06).toFixed(2)}s 1 forwards">
                  <g>${обезьяна(кx,ку,24, подпись==='шимпанзе'?'шимпанзе':'мартышка')}
                    <animateTransform attributeName="transform" type="translate"
                      values="0 0; 0 -1.6; 0 0" dur="${(2.3+i*0.15).toFixed(2)}s"
                      begin="${(i*0.2).toFixed(2)}s" repeatCount="indefinite"/>
                  </g>
                </g>`;
        }
        if(количество){
          s += `${т(x,152, количество+(количество>10?' (показаны не все)':''), 14, ц, true)}`;
        } else {
          s += `<g>${т(x,152,'?',16,ТИХО,true)}
                  <animate attributeName="opacity" values=".35;1;.35" dur="2s" repeatCount="indefinite"/>
                </g>`;
        }
      });

      s += `${т(168,178, верно?'сходится: 15 · 3 + 10 · 5 = 95':(проверено?'проверь: 3 · мартышки + 5 · шимпанзе = 95':'введи числа в ответе ниже'),12, верно?ЗЕЛЁН:ТИХО)}`;
      return svg(s,202);
    },
    низ(){
      const п=память(), о=п.ответы[3]||{};
      const верно = Number(о.мартышек)===ЗАДАЧИ[3].ответ.мартышек &&
                    Number(о.шимпанзе)===ЗАДАЧИ[3].ответ.шимпанзе;
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[3].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[3].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${ДОМ}-ждёт" style="padding:12px 14px">
            <div style="display:flex;gap:12px;flex-wrap:wrap;align-items:center">
              <label style="font-size:14px;color:var(--muted)">мартышек
                <input id="${ДОМ}-мартышек" type="number" inputmode="numeric" value="${о.мартышек||''}"
                  style="min-height:48px;width:96px;margin-left:8px;font-size:20px;padding:0 10px;border-radius:10px;
                         border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8"></label>
              <label style="font-size:14px;color:var(--muted)">шимпанзе
                <input id="${ДОМ}-шимпанзе" type="number" inputmode="numeric" value="${о.шимпанзе||''}"
                  style="min-height:48px;width:96px;margin-left:8px;font-size:20px;padding:0 10px;border-radius:10px;
                         border:1.5px solid ${ЛИНИЯ};background:rgba(255,255,255,.06);color:#f4e9c8"></label>
            </div>
            <div style="margin-top:12px"><button class="btn" style="min-height:48px;margin:0"
              onclick="Т702.звери()">Проверить</button></div>
          </div>`;
      } else {
        блок += разбор(ЗАДАЧИ[3].разбор, ЗАДАЧИ[3].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: знаки →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 10. ПОРЯДОК ДЕЙСТВИЙ: теория ── */
  ШАГИ.push({
    т:'Сначала умножение',
    рисунок(){
      /* Показываем, почему «как читаем» не работает: сначала считаются
         умножения, и только потом сложения. */
      let s = `<rect x="8" y="8" width="320" height="186" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="186" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Умножение идёт раньше сложения',14,ЗОЛОТО,true)}`;

      /* Выражение 5 · 4 + 3 · 2 + 1: цифры и знаки между ними. Раньше цифры
         рисовались дважды и налезали сами на себя. */
      const цифрыX=[40,96,152,208,264];
      const знакиX=[68,124,180,236];
      const знаки=['·','+','·','+'];
      цифрыX.forEach((x,i)=> s += `${т(x,80,['5','4','3','2','1'][i],24,ПЕРО,true)}`);
      знакиX.forEach((x,i)=>{
        const умножение = знаки[i]==='·';
        s += `<g>${т(x,80,знаки[i],24, умножение?ЗЕЛЁН:ЗОЛОТО,true)}
                <animate attributeName="opacity" values=".45;1;.45" dur="2.6s"
                  begin="${(i*0.4).toFixed(2)}s" repeatCount="indefinite"/>
              </g>`;
      });

      /* порядок: сначала умножения, потом сложения */
      const порядок=[
        {текст:'сначала 5 · 4 = 20', ц:ЗЕЛЁН, y:112},
        {текст:'и 3 · 2 = 6', ц:ЗЕЛЁН, y:134},
        {текст:'потом 20 + 6 + 1 = 27', ц:ЗОЛОТО, y:164}
      ];
      порядок.forEach((п,i)=>{
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 420ms ease-out ${(0.3+i*0.6).toFixed(2)}s 1 forwards">
                ${т(168,п.y,п.текст,14,п.ц,true)}
              </g>`;
        s += `<circle cx="60" cy="${п.y-5}" r="4" fill="${п.ц}">
                <animate attributeName="opacity" values=".3;1;.3" dur="2.4s"
                  begin="${(i*0.5).toFixed(2)}s" repeatCount="indefinite"/>
              </circle>`;
      });
      return svg(s,196);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">В выражении без скобок умножение и деление выполняются раньше сложения и вычитания. Поэтому 5 · 4 + 3 · 2 + 1 считается так: сначала 5 · 4 = 20 и 3 · 2 = 6, а потом 20 + 6 + 1 = 27.</div>
        </div>`;
    }
  });

  /* ── 11. ПЯТЬ ЦИФР: расставляем знаки ── */
  ШАГИ.push({
    т:'Пять цифр и знаки',
    рисунок(){
      const п=память(), знаки=п.ответы[4]||[];
      const цифры=ЗАДАЧИ[4].цифры;
      const верно = знаки.join('')===ЗАДАЧИ[4].ответ.join('');
      const результат = посчитать(цифры, знаки);

      let s = `<rect x="8" y="8" width="320" height="176" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="176" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `${т(168,24,'Собери выражение, равное 27',14,ЗОЛОТО,true)}`;

      /* цифры и места под знаки */
      const x0=40, шаг=64;
      цифры.forEach((ц,i)=>{
        const x=x0+i*шаг;
        s += жетон(x,86,String(ц),ПЕРО,20);
        if(i<знаки.length && знаки[i]!==undefined)
          s += `${т(x+шаг/2,94,знаки[i],24, верно?ЗЕЛЁН:ЗОЛОТО, true)}`;
        else if(i<4){
          s += рамка(x+шаг/2-11,74,22,24, результат===null?'rgba(255,255,255,.04)':'rgba(255,215,106,.10)',ЛИНИЯ,5,1.2);
          s += `<g>${т(x+шаг/2,90,'?',16,ТИХО,true)}
                  <animate attributeName="opacity" values=".35;1;.35" dur="2.2s"
                    begin="${(i*0.35).toFixed(2)}s" repeatCount="indefinite"/>
                </g>`;
        }
      });

      /* что получается сейчас */
      s += `${т(168,132, результат===null ? 'поставь все четыре знака' : ('сейчас получается ' + результат),12,
             результат===27?ЗЕЛЁН:ТИХО)}`;
      s += `${т(168,156, верно ? 'верно: 5 · 4 + 3 · 2 + 1 = 27' : 'помни: умножение считается раньше сложения',12,ТИХО)}`;
      return svg(s,192);

      function посчитать(цифры, знаки){
        if(знаки.length<4 || знаки.some(з=>!з)) return null;
        /* считаем с учётом порядка действий: сначала «·» и «:», потом «+» и «−» */
        const числа=[цифры[0]], операции=[];
        for(let i=0;i<знаки.length;i++){
          const з=знаки[i];
          if(з==='·' || з===':'){ 
            const прошлое=числа.pop();
            const знач = з==='·' ? прошлое*цифры[i+1] : (цифры[i+1]? прошлое/цифры[i+1] : NaN);
            числа.push(знач);
          } else { операции.push(з); числа.push(цифры[i+1]); }
        }
        let итог=числа[0];
        for(let i=0;i<операции.length;i++){
          итог = операции[i]==='+' ? итог+числа[i+1] : итог-числа[i+1];
        }
        return Number.isFinite(итог) ? итог : null;
      }
    },
    низ(){
      const п=память(), знаки=п.ответы[4]||[];
      const верно = знаки.join('')===ЗАДАЧИ[4].ответ.join('');
      let блок = `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[4].условие)}</div>
          <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[4].вопрос)}</div>
        </div>`;
      if(!верно){
        блок += `<div class="card ${ДОМ}-ждёт" style="padding:12px 14px">
            <div style="font-size:14px;color:var(--muted);font-variant-numeric:tabular-nums">поставлено знаков: ${знаки.length} из 4</div>
            <div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:10px">` +
          ['+','−','·',':'].map(з=>
            `<button class="btn" style="min-height:48px;min-width:56px;margin:0;font-size:20px"
               onclick="Т702.знак('${з}')">${з}</button>`).join('') +
          `</div></div>`;
        if(знаки.length)
          блок += `<div class="btnrow"><button class="btn ghost" onclick="Т702.знакиСтереть()">Стереть</button></div>`;
      } else {
        блок += разбор(ЗАДАЧИ[4].разбор, ЗАДАЧИ[4].ошибка);
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Смотреть итог →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 12. ИТОГ ── */
  ШАГИ.push({
    т:'Итог',
    рисунок(){
      const балл=баллТура(), процент=Math.round(балл/10*100);
      const ц=балл>=8?ЗЕЛЁН:(балл>=5?ЗОЛОТО:КРАСН);
      const дела=[
        {имя:'число', ок:Number(память().ответы[0])===ЗАДАЧИ[0].ответ},
        {имя:'очередь', ок:(память().ответы[1]||[]).join('|')===ЗАДАЧИ[1].ответ.join('|')},
        {имя:'день', ок:память().ответы[2]===ЗАДАЧИ[2].ответ},
        {имя:'звери', ок:(()=>{const о=память().ответы[3]||{};
          return Number(о.мартышек)===ЗАДАЧИ[3].ответ.мартышек && Number(о.шимпанзе)===ЗАДАЧИ[3].ответ.шимпанзе;})()},
        {имя:'знаки', ок:(память().ответы[4]||[]).join('')===ЗАДАЧИ[4].ответ.join('')}
      ];
      let s = `<rect x="8" y="8" width="320" height="186" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="186" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g>
              <circle cx="168" cy="62" r="38" fill="rgba(255,255,255,.04)" stroke="${ц}" stroke-width="1.6"/>
              <circle cx="168" cy="62" r="44" fill="none" stroke="${ц}" stroke-width="1.2"
                stroke-dasharray="9 7" opacity=".5">
                <animateTransform attributeName="transform" type="rotate"
                  from="0 168 62" to="360 168 62" dur="18s" repeatCount="indefinite"/>
              </circle>
              <text id="${ДОМ}-балл" x="168" y="70" text-anchor="middle" font-size="32"
                font-weight="bold" fill="${ц}" font-family="Georgia,serif"
                style="font-variant-numeric:tabular-nums">0</text>
            </g>`;
      s += `${т(168,122,'выполнено '+процент+' %',14,ПЕРО)}`;

      дела.forEach((д,i)=>{
        const x=56+i*56;
        s += `<g opacity="0" style="animation:${ДОМ}-фрукт 320ms ease-out ${(0.2+i*0.12).toFixed(2)}s 1 forwards">
                ${рамка(x-24,140,48,26, д.ок?'rgba(143,209,168,.14)':'rgba(232,106,90,.12)',
                        д.ок?ЗЕЛЁН:КРАСН,8,1.4)}
                ${т(x,157,д.ок?'✓':'✕',14,д.ок?ЗЕЛЁН:КРАСН,true)}
                ${т(x,178,д.имя,12,ТИХО)}
              </g>`;
      });
      s += `<rect x="48" y="186" width="240" height="8" rx="4" fill="rgba(255,255,255,.06)"/>`;
      s += `<rect x="48" y="186" width="0" height="8" rx="4" fill="${ц}">
              <animate attributeName="width" from="0" to="${Math.round(240*балл/10)}" dur="900ms"
                begin="0.1s" fill="freeze"/></rect>`;
      return svg(s,202);
    },
    низ(){
      const балл=баллТура();
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">Тур пройден. Что мы делали: находили число обратным ходом, расставляли очередь по ограничениям, считали день недели по календарю, сравнивали с ровным предположением и собирали выражение с правильным порядком действий.</div>
          <div style="font-size:16px;line-height:1.5;margin-top:8px">${балл>=8?'Отличный результат.':(балл>=5?'Хорошо, но есть что разобрать.':'Вернись к разборам и попробуй снова.')}</div>
        </div>
        <div class="btnrow"><button class="btn ghost" onclick="go('olymp')">← К списку туров</button>
        <button class="btn ok2" onclick="go('tour')">В раздел «Тур»</button></div>`;
    }
  });

  /* ── действия руками ── */
  window.Т702 = {
    число(){
      const п=память(), el=document.getElementById(ДОМ+'-число');
      п.ответы[0] = el ? el.value.trim() : '';
      п.проверено[0] = true;
      перерисовать();
    },
    очередь(имя){
      const п=память();
      const о=п.ответы[1] = п.ответы[1] || [];
      if(о.length>=4 || о.indexOf(имя)>=0) return;
      о.push(имя);
      перерисовать();
    },
    очередьСброс(){ const п=память(); п.ответы[1]=[]; перерисовать(); },
    день(д){ const п=память(); п.ответы[2]=д; перерисовать(); },
    звери(){
      const п=память();
      const м=document.getElementById(ДОМ+'-мартышек'), ш=document.getElementById(ДОМ+'-шимпанзе');
      п.ответы[3] = { мартышек: м?м.value.trim():'', шимпанзе: ш?ш.value.trim():'' };
      п.проверено[3] = true;
      перерисовать();
    },
    знак(з){
      const п=память();
      const знаки=п.ответы[4] = п.ответы[4] || [];
      if(знаки.length>=4) return;
      знаки.push(з);
      перерисовать();
    },
    знакиСтереть(){ const п=память(); п.ответы[4]=[]; перерисовать(); }
  };

  window.WAVE_B = window.WAVE_B || {};
  window.WAVE_B[ID] = function(el){
    try{ рисовать(el); }
    catch(e){ el.innerHTML=''; try{ console.error('тур 702:', e); }catch(_){} }
  };

  /* ══════════════════════ РЕГИСТРАЦИЯ ══════════════════════ */
  const ТЕКУЩИЙ = () => Math.max(0, Math.min(ШАГИ.length-1,
                     (typeof LV !== 'undefined' && LV && LV.step) || 0));

  function стиль(){
    if(document.getElementById(ДОМ+'-стиль')) return;
    const s=document.createElement('style');
    s.id=ДОМ+'-стиль';
    s.textContent = `
      @keyframes ${ДОМ}-появление{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
      @keyframes ${ДОМ}-пульс{0%,100%{opacity:.45}50%{opacity:1}}
      @keyframes ${ДОМ}-строка{0%{opacity:1}18%{opacity:1}21%{opacity:0}100%{opacity:0}}
      @keyframes ${ДОМ}-фрукт{from{opacity:0}to{opacity:1}}
      .${ДОМ}{display:flex;flex-direction:column;align-items:stretch;gap:12px;
        font-family:Georgia,'Times New Roman',serif;-webkit-font-smoothing:antialiased}
      .${ДОМ}-шапка{text-align:center;font-size:14px;color:var(--muted,#cbb89a);
        letter-spacing:.02em;font-variant-numeric:tabular-nums}
      .${ДОМ}-шапка b{color:#ffd76a;font-weight:600}
      .${ДОМ}-сцена{display:flex;justify-content:center;width:100%}
      .${ДОМ}-сцена svg{display:block;width:100%;max-width:420px;height:auto;margin:0 auto}
      .${ДОМ}-низ{display:flex;flex-direction:column;gap:12px;
        animation:${ДОМ}-появление 320ms cubic-bezier(.23,1,.32,1) both}
      .${ДОМ} .card{text-align:left}
      .${ДОМ}-ждёт{animation:${ДОМ}-пульс 1.6s ease-in-out infinite}
      .${ДОМ} button{transition:transform 140ms cubic-bezier(.23,1,.32,1),
        border-color 140ms ease,background-color 140ms ease}
      .${ДОМ} button:active{transform:translateY(2px) scale(.985)}
      .${ДОМ} button:focus-visible{outline:3px solid #ffd76a;outline-offset:2px}
      @media (min-width: 700px){ .${ДОМ}{max-width:560px;margin:0 auto} }
      @media (prefers-reduced-motion: reduce){
        .${ДОМ} *, #lvis svg *{animation:none!important;transition:none!important}
        .${ДОМ}-речь{opacity:0}
        .${ДОМ}-речь-первая{opacity:1}
      }`;
    document.head.appendChild(s);
  }

  function рисовать(el){
    if(!el) return;
    стиль();
    const i = ТЕКУЩИЙ(), шаг = ШАГИ[i];
    if(!шаг){ el.innerHTML=''; return; }
    try{
      el.innerHTML = `<div class="${ДОМ}" style="animation:${ДОМ}-появление 280ms cubic-bezier(.23,1,.32,1) both">
          <div class="${ДОМ}-шапка">Шаг <b>${i+1}</b> из ${ШАГИ.length} · ${esc(шаг.т)}</div>
          ${шаг.рисунок ? `<div class="${ДОМ}-сцена">${шаг.рисунок()}</div>` : ''}
          <div class="${ДОМ}-низ">${шаг.низ ? шаг.низ() : ''}</div>
        </div>`;
      /* Счёт балла: в svg цифрой управлять нельзя, поэтому досчитываем в DOM. */
      if(i === ШАГИ.length-1) счётДо(ДОМ+'-балл', баллТура(), 800);
    }catch(e){ el.innerHTML=''; try{ console.error('тур 702:', e); }catch(_){} }
  }

  const ЛЕНТА = ШАГИ.map((ш,i)=> (i+1)+'. '+ш.т);


  /* Раздел «Тур» строит список из ARH_TOURS. Без этой записи тур был бы
     в каталоге, но в списке туров его не видно — именно так три тура
     и «пропали». */
  (function(){
    window.ARH_TOURS = window.ARH_TOURS || [];
    if(window.ARH_TOURS.some(t=>t && t.id===ID)) return;
    window.ARH_TOURS.push({
      id: ID, klass: 4, subject: 'math',
      title: 'Математика · 4 класс · «Очередь в буфет»',
      макс: 10, время: 45,
      items: [1,2,3,4,5].map(i=>({ n:i })),
      hand: true
    });
  })();

  (function(){
    const L = {
      id: ID, title:'Очередь в буфет', ico:'🥐',
      src:'Олимпиадные туры · Математика · 4 класс', subj:'tour',
      explain: ЛЕНТА,
      check: { q:'Сколько баллов можно получить за этот тур?',
               choices:['10','5','8','12'], ans:0,
               exp:'Пять задач по 2 балла — всего 10 баллов.' },
      tasks: []
    };
    const поставить = arr => {
      if(!arr || typeof arr.length!=='number') return arr;
      for(let i=0;i<arr.length;i++){ if(arr[i] && arr[i].id===ID){ arr[i]=L; return arr; } }
      arr.push(L);
      return arr;
    };
    let сейчас = поставить(window.ARH_LESSONS);
    try{
      Object.defineProperty(window,'ARH_LESSONS',{
        configurable:true, enumerable:true,
        get(){ return сейчас; },
        set(v){ сейчас = поставить(v); }
      });
    }catch(e){}
  })();
})();
