/* ============ РУССКИЙ ЯЗЫК · УРОК 883 · «О И Ё ПОСЛЕ ШИПЯЩИХ» ============
   5 класс, II четверть, раздел «Морфемика. Орфография».

   СВЕРКА С УЧЕБНИКОМ (5 класс, рабочая программа):
     • «Буквы ё и о после шипящих в корне»;
     • «Буквы о и е после шипящих и ц в окончаниях существительных»;
     • «Правописание букв и, а, у после шипящих» (повторение);
     • «Буква Ь на конце существительных после шипящих» (повторение).

   СЮЖЕТ: ярмарка. Ребёнок помогает писарю подписывать ценники: в словах
   после шипящих стёрлась буква, и надо решить, о или ё (е).

   УСТРОЙСТВО ЭКРАНА — единое во всех уроках направления:
     карточка «Вопрос» → рисунок → варианты → карточка «Верно» /
     «Разбор ошибки». В практике — многоуровневые карточки.

   СТАНДАРТ: заголовок 24, текст кадра 20, подпись 16, служебное 14; в SVG
   кегли только 12/14/16/20/24/32; цели касания от 48 px; движение только
   вертикальное; задержка появления ограничена; есть prefers-reduced-motion. */
(function(){
  'use strict';

  const ID = 883;

  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';

  const CSS=`
  #lvis .s6.l883{gap:14px}
  #lvis .s6.l883 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l883 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l883 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l883 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l883 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l883 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l883 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l883 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l883 .герой .лицо{flex:none;width:64px;height:64px}
  #lvis .s6.l883 .герой .лицо svg{width:100%;height:100%;display:block}
  #lvis .s6.l883 .герой .речь{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l883 .герой .приём{margin-top:6px;font-size:16px;line-height:1.4;color:var(--mut)}
  #lvis .s6.l883 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l883 .правило b{color:${GOLD}}
  #lvis .s6.l883 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l883 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left}
  #lvis .s6.l883 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l883 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l883 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l883 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l883 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l883 .уровни .точка.сейчас{background:${GOLD}}
  #lvis .s6.l883 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l883 [data-anim]{animation:l883rise .42s cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*60ms)}
  #lvis .s6.l883[data-frame="3"] [data-anim],#lvis .s6.l883[data-frame="6"] [data-anim]{animation-name:l883pop}
  #lvis .s6.l883[data-frame="10"] [data-anim]{animation-name:l883pop}
  @keyframes l883rise{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:none}}
  @keyframes l883pop{0%{opacity:0;transform:scale(.95)}70%{transform:scale(1.03)}100%{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l883 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){ #lvis .s6.l883 [data-anim]{animation:none!important} }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l883-style');
      if(!s){ s=document.createElement('style'); s.id='l883-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c883-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22362c"/><stop offset="1" stop-color="#152219"/>
      </linearGradient>
      <linearGradient id="c883-ярлык" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#fdf6e0"/><stop offset="1" stop-color="#e2d3ad"/>
      </linearGradient>
      <linearGradient id="c883-прилавок" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c98a45"/><stop offset="1" stop-color="#6f4420"/>
      </linearGradient>
      <radialGradient id="c883-свет" cx="0.5" cy="0.3" r="0.7">
        <stop offset="0" stop-color="#ffe9a8" stop-opacity="0.5"/>
        <stop offset="1" stop-color="#ffe9a8" stop-opacity="0"/>
      </radialGradient>
      <filter id="c883-тень" x="-30%" y="-30%" width="160%" height="160%">
        <feGaussianBlur stdDeviation="2.4"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||176}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* ценник с пропуском */
  const ценник = (x,y,ш,слово,пропуск,цвет) => {
    const буквы=[...слово];
    /* шаг между буквами задаёт ширина ярлыка, а кегль выбираем так,
       чтобы буквы не налезали друг на друга (ширина буквы ≈ 0,62 кегля) */
    const шаг = (ш-12)/Math.max(1,буквы.length);
    const кегль = [20,16,14,12].find(к=>к*0.95<=шаг) || 12;
    let cx=x+6+шаг*0.5;
    const ч=буквы.map((б,i)=>{
      const пусто = пропуск===i;
      const рамкаШ = Math.max(10, Math.min(24, шаг-3));
      const г = пусто
        ? `<rect x="${(cx-рамкаШ/2).toFixed(1)}" y="${y-16}" width="${рамкаШ.toFixed(1)}" height="30" rx="5" fill="rgba(255,215,106,.18)"
             stroke="${GOLD}" stroke-width="2"/>
           ${т(cx.toFixed(1),y+7,'_',кегль,GOLD,true)}`
        : т(cx.toFixed(1),y+7,б,кегль,'#2a2118',true);
      cx += шаг;
      return г;
    }).join('');
    return `<g>
      <rect x="${x+3}" y="${y-21}" width="${ш}" height="40" rx="8" fill="rgba(0,0,0,.4)" filter="url(#c883-тень)"/>
      <rect x="${x}" y="${y-24}" width="${ш}" height="40" rx="8" fill="url(#c883-ярлык)"
        stroke="${цвет||'#b8a06a'}" stroke-width="1.6"/>
      ${ч}
    </g>`;
  };

  /* прилавок */
  const прилавок = (x,y,ш) => `
    <g>
      <rect x="${x}" y="${y}" width="${ш}" height="12" rx="4" fill="url(#c883-прилавок)" stroke="#3a2412" stroke-width="1.2"/>
      <rect x="${x+8}" y="${y+12}" width="10" height="18" fill="url(#c883-прилавок)" stroke="#3a2412" stroke-width="1"/>
      <rect x="${x+ш-18}" y="${y+12}" width="10" height="18" fill="url(#c883-прилавок)" stroke="#3a2412" stroke-width="1"/>
    </g>`;

  /* шипящая буква-плитка */
  const шипящая = (x,y,б) => `
    <g>
      <rect x="${x-18}" y="${y-24}" width="36" height="46" rx="9" fill="rgba(255,255,255,.05)"
        stroke="${BLUE}" stroke-width="2"/>
      ${т(x,y+8,б,24,BLUE,true)}
      <path d="M${x-10} ${y-34} q6 -6 0 -12 M${x+10} ${y-34} q6 -6 0 -12" fill="none"
        stroke="${BLUE}" stroke-width="1.6" opacity=".6"/>
    </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Сюжет */
  function F1(s){
    const в = s.ярмарка;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c883-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      <circle cx="90" cy="60" r="54" fill="url(#c883-свет)"/>
      ${прилавок(20,112,140)}
      ${прилавок(176,112,140)}
      ${ценник(16,60,92,'ш_лк',1,GOLD)}
      ${ценник(118,60,202,'крыж_вник',4,GOLD)}
      ${т(168,26,'Писарь подписывает ценники',14,GOLD,true)}
      ${т(168,160,'после шипящих стёрлась буква',12,МУТ)}
    `,176);
    return ВОПРОС('На ярмарке в ценниках стёрлась буква после шипящей. Кто решает, что писать: о или ё?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='правило'?'hit':(в?'miss':''), 'Решает правило: о или ё зависит от проверки', "r883Fair('правило')")}
        ${BTN(4, в==='ухо'?'miss':'', 'Решает слух: как слышится, так и пишется', "r883Fair('ухо')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='правило',
        в==='правило' ? 'Верно: под ударением после шипящих в корне пишем ё, если есть проверочное слово с е. Слух обманывает: и «шёлк», и «шов» слышатся одинаково.'
                     : 'Нет: на слух о и ё после шипящих не различить. Нужна проверка родственным словом.')
               : ЖДЁТ()) +
      ПРАВИЛО('В корне после шипящих под ударением пишем <b>ё</b>, если можно подобрать проверочное слово с <b>е</b>.');
  }

  /* 2. Шипящие буквы */
  function F2(s){
    const в = s.шипящие;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="164" fill="url(#c883-стена)"/>
      <rect x="0" y="0" width="336" height="164" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${шипящая(60,70,'ж')}${шипящая(140,70,'ш')}${шипящая(212,70,'ч')}${шипящая(284,70,'щ')}
      ${т(168,140,'буквы, которые шипят',12,МУТ)}
      ${т(168,24,'Четыре шипящие буквы',14,GOLD,true)}
    `,164);
    return ВОПРОС('Сколько шипящих букв в русском языке?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='четыре'?'hit':(в?'miss':''), 'Четыре: ж, ш, ч, щ', "r883Hiss('четыре')")}
        ${BTN(4, в==='две'?'miss':'', 'Две: ж и ш', "r883Hiss('две')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='четыре',
        в==='четыре' ? 'Верно: шипящих четыре — ж, ш, ч, щ. Правило о и ё работает после всех четырёх.'
                    : 'Нет: шипящих четыре. Ч и щ тоже шипят, и после них правило действует так же.')
               : ЖДЁТ()) +
      ПРАВИЛО('Шипящие: <b>ж, ш, ч, щ</b>. После них под ударением выбираем между о и ё (или е).');
  }

  /* 3. Проверка с е → ё */
  const ПАРЫ=[
    { слово:'ж_лтый', буква:'ё', проверка:'желтеть', пропуск:1 },
    { слово:'ч_рный', буква:'ё', проверка:'чернеть', пропуск:1 },
    { слово:'ш_лк',   буква:'ё', проверка:'шелковистый', пропуск:1 },
    { слово:'щ_тка',  буква:'ё', проверка:'щетина', пропуск:1 }
  ];
  function F3(s){
    const и=(s.проверка||0)%ПАРЫ.length, з=ПАРЫ[и];
    const отв=s.прОтв, готово=отв!=null;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c883-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Проверяем родственным словом',14,GOLD,true)}
      ${ценник(34,64,118,з.слово,з.пропуск,GOLD)}
      ${т(168,72,'→',20,МУТ)}
      ${ценник(184,64,126,з.проверка,null,GREEN)}
      ${т(168,132,'в проверке слышится е — значит, пишем ё',12,ИНК)}
      ${т(168,156,'проверочное слово — родственное, с е',12,МУТ)}
    `,176);
    return ВОПРОС('Как проверить букву в слове «'+з.слово.replace('_','…')+'»?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, готово?(отв===0?'hit':'miss'):'', 'Подобрать родственное слово с е: '+з.проверка, 'r883Chk(0)')}
        ${BTN(4, готово?(отв===1?'hit':'miss'):'', 'Запомнить на слух', 'r883Chk(1)')}
      </div>` +
      (готово ? РАЗБОР(отв===0, отв===0
        ? 'Верно: '+з.проверка+' — здесь ясно слышится е, поэтому в слове «'+з.слово.replace('_','ё')+'» пишем ё.'
        : 'Нет: на слух не различить. Нужно родственное слово: '+з.проверка+' — тогда ясно, что писать ё.') : ЖДЁТ()) +
      (готово ? `<div class="ask">${BTN(5,'','следующее слово','r883ChkNext()')}</div>` : '') +
      ПРАВИЛО('Есть проверка с <b>е</b> — пишем <b>ё</b>: жёлтый — желтеть, чёрный — чернеть, шёлк — шелковистый.');
  }

  /* 4. Нет проверки → о */
  const БЕЗПРОВЕРКИ=[
    { слово:'ш_в', буква:'о', пояснение:'шов — проверки с е нет, пишем о' },
    { слово:'ш_рох', буква:'о', пояснение:'шорох — проверки нет, пишем о' },
    { слово:'крыж_вник', буква:'о', пояснение:'крыжовник — проверки нет, пишем о' },
    { слово:'капюш_н', буква:'о', пояснение:'капюшон — проверки нет, пишем о' }
  ];
  function F4(s){
    const в = s.безПроверки;
    const отв = s.бпОтв, готово = отв!=null;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c883-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Проверки нет — пишем о',14,GOLD,true)}
      ${БЕЗПРОВЕРКИ.map((х,i)=>{
        const кол=i%2, ряд=Math.floor(i/2);
        const x=16+кол*164, y=60+ряд*50;
        return ценник(x,y,150,х.слово,х.слово.indexOf('_'),GOLD);
      }).join('')}
      ${т(168,166,'такие слова приходится запоминать',12,МУТ)}
    `,176);
    return ВОПРОС('Проверочного слова с е нет. Какую букву писать?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, готово?(отв===0?'hit':'miss'):'', 'Писать о: шов, шорох, крыжовник, капюшон', 'r883No(0)')}
        ${BTN(4, готово?(отв===1?'hit':'miss'):'', 'Писать ё: шёв, шёрох, крыжёвник', 'r883No(1)')}
      </div>` +
      (готово ? РАЗБОР(отв===0, отв===0
        ? 'Верно: если проверки с е нет, в корне после шипящих пишем о. Эти слова надо запомнить.'
        : 'Нет: без проверки пишем о. «Шёв» и «шёрох» — неверно.') : ЖДЁТ()) +
      ПРАВИЛО('Проверки с <b>е</b> нет — пишем <b>о</b>: шов, шорох, крыжовник, капюшон.');
  }

  /* 5. Сундук исключений */
  function F5(s){
    const в = s.исключения;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="180" fill="url(#c883-стена)"/>
      <rect x="0" y="0" width="336" height="180" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Сундук слов, которые надо запомнить',14,GOLD,true)}
      <rect x="44" y="46" width="248" height="86" rx="10" fill="url(#c883-прилавок)" stroke="#3a2412" stroke-width="1.6"/>
      <rect x="44" y="80" width="248" height="8" fill="rgba(0,0,0,.35)"/>
      <circle cx="168" cy="84" r="9" fill="${GOLD}" stroke="#8a5f05" stroke-width="1.4"/>
      ${ценник(46,60,122,'шов',null)}
      ${ценник(172,60,122,'шорох',null)}
      ${ценник(46,110,122,'крыжовник',null)}
      ${ценник(172,110,122,'капюшон',null)}
      ${т(168,158,'проверки с е у них нет',12,МУТ)}
    `,180);
    return ВОПРОС('Что общего у слов шов, шорох, крыжовник, капюшон?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='запомнить'?'hit':(в?'miss':''), 'У них нет проверки с е — их надо запомнить', "r883Box('запомнить')")}
        ${BTN(4, в==='проверка'?'miss':'', 'У них есть проверка с е', "r883Box('проверка')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='запомнить',
        в==='запомнить' ? 'Верно: проверочного слова с е нет, поэтому пишем о. Такие слова запоминают.'
                       : 'Нет: проверки с е у них нет — иначе писались бы с ё.')
               : ЖДЁТ()) +
      ПРАВИЛО('Слова с <b>о</b> без проверки: шов, шорох, крыжовник, капюшон. Запомни их — они встречаются часто.');
  }

  /* 6. Окончания существительных */
  function F6(s){
    const в = s.окончания;
    const отв=s.окОтв, готово=отв!=null;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="180" fill="url(#c883-стена)"/>
      <rect x="0" y="0" width="336" height="180" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Окончания существительных',14,GOLD,true)}
      <rect x="20" y="42" width="140" height="112" rx="12" fill="url(#c883-прилавок)" opacity=".25" stroke="${GOLD}" stroke-width="1.8"/>
      ${т(90,66,'под ударением — о',12,GOLD,true)}
      ${т(90,96,'врачом',16,ИНК)}
      ${т(90,120,'плащом',16,ИНК)}
      ${т(90,144,'свечой',16,ИНК)}
      <rect x="176" y="42" width="140" height="112" rx="12" fill="url(#c883-прилавок)" opacity=".18" stroke="${BLUE}" stroke-width="1.8"/>
      ${т(246,66,'без ударения — е',12,BLUE,true)}
      ${т(246,96,'товарищем',16,ИНК)}
      ${т(246,120,'задачей',16,ИНК)}
      ${т(246,144,'птицей',16,ИНК)}
    `,180);
    return ВОПРОС('В слове «плащ…м» какую букву писать в окончании?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, готово?(отв===0?'hit':'miss'):'', 'Под ударением — о: плащом', 'r883End(0)')}
        ${BTN(4, готово?(отв===1?'hit':'miss'):'', 'Без ударения — е: плащем', 'r883End(1)')}
      </div>` +
      (готово ? РАЗБОР(отв===0, отв===0
        ? 'Верно: в окончании под ударением пишем о — плащом. Сравни: товарищем — здесь ударение не на окончании, поэтому е.'
        : 'Нет: ударение падает на окончание, значит пишем о — плащом.') : ЖДЁТ()) +
      ПРАВИЛО('В окончаниях существительных: под ударением — <b>о</b> (врачом, плащом), без ударения — <b>е</b> (товарищем, задачей).');
  }

  /* 7. Глаголы и причастия */
  function F7(s){
    const в = s.глаголы;
    const отв=s.глОтв, готово=отв!=null;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c883-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'В глаголах после шипящих — ё',14,GOLD,true)}
      <path d="M96 56 q72 -22 144 0 L232 136 q-64 20 -128 0 Z" fill="url(#c883-прилавок)" opacity=".35"
        stroke="#8a5f05" stroke-width="1.6"/>
      <path d="M118 74 q50 -12 100 0" fill="none" stroke="${GOLD}" stroke-width="3" stroke-linecap="round"/>
      <circle cx="150" cy="104" r="12" fill="${GOLD}" opacity=".85"/>
      <circle cx="186" cy="104" r="12" fill="${GOLD}" opacity=".85"/>
      <path d="M110 96 q6 -10 0 -18 M226 96 q-6 -10 0 -18" fill="none" stroke="${МУТ}" stroke-width="2" opacity=".6"/>
      ${т(168,158,'печёт, течёт, жжёный',16,ИНК,true)}
    `,176);
    return ВОПРОС('В слове «печ…т» какую букву писать?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, готово?(отв===0?'hit':'miss'):'', 'ё: печёт', 'r883Verb(0)')}
        ${BTN(4, готово?(отв===1?'hit':'miss'):'', 'о: печот', 'r883Verb(1)')}
      </div>` +
      (готово ? РАЗБОР(отв===0, отв===0
        ? 'Верно: в глаголах после шипящих пишем ё — печёт, течёт, жжёный. Здесь работает не проверка, а часть речи.'
        : 'Нет: в глаголах под ударением после шипящих пишется ё — печёт.') : ЖДЁТ()) +
      ПРАВИЛО('В глаголах и причастиях после шипящих под ударением — <b>ё</b>: печёт, течёт, жжёный.');
  }

  /* 8. Практика: многоуровневые карточки */
  const УРОВНИ=[
    { вопрос:'В слове «ш_лк» какую букву писать?', варианты:[
        {т:'ё — есть проверка «шелковистый»', ок:true,  почему:'шелковистый — слышится е, значит в корне пишем ё.'},
        {т:'о — нет проверки', ок:false, почему:'Проверка есть: шелковистый, шёлковый. Пишем ё.'}]},
    { вопрос:'В слове «крыж_вник» какую букву писать?', варианты:[
        {т:'о — проверки с е нет', ок:true,  почему:'крыжовник — проверочного слова с е нет, пишем о и запоминаем.'},
        {т:'ё — проверю словом «крыжовник»', ок:false, почему:'Это то же слово, а не проверка. Проверки с е нет — пишем о.'}]},
    { вопрос:'В слове «ч_рный» какую букву писать?', варианты:[
        {т:'ё — есть проверка «чернеть»', ок:true,  почему:'чернеть — слышится е, значит пишем ё.'},
        {т:'о — слышится о', ок:false, почему:'На слух о и ё не различить. Проверка подсказывает ё.'}]},
    { вопрос:'В слове «врач_м» какую букву писать?', варианты:[
        {т:'о — окончание под ударением', ок:true,  почему:'Ударение на окончании, поэтому о: врачом.'},
        {т:'е — без ударения', ок:false, почему:'Ударение как раз на окончании, значит о.'}]},
    { вопрос:'В слове «товарищ_м» какую букву писать?', варианты:[
        {т:'е — окончание без ударения', ок:true,  почему:'Ударение не на окончании, поэтому е: товарищем.'},
        {т:'о — после шипящих всегда о', ок:false, почему:'о пишем только под ударением. Здесь ударение на «а» — пишем е.'}]}
  ];
  function F8(s){
    const и = Math.min(s.проги||0, УРОВНИ.length-1);
    const пройдено = s.проги||0;
    const готово = пройдено>=УРОВНИ.length;
    const точки=Array.from({length:УРОВНИ.length},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===и&&!готово?'сейчас':'')}"></span>`).join('');
    if(готово){
      return ВОПРОС('Все пять ценников подписаны. Что помогло?') +
        A(3,'уровни',точки) +
        РАЗБОР(true,'Правило работает в два вопроса: есть проверка с е — пишем ё; нет — пишем о. В окончаниях смотрим на ударение.') +
        `<div class="ask">${BTN(4,'','подписать заново','r883Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.прогиУровень===и) ? s.прогиВыбор : null;
    return A(1,'уровни',точки) + A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r883Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Сначала ищем проверку с <b>е</b>. Нашли — пишем <b>ё</b>. Не нашли — пишем <b>о</b> и запоминаем слово.');
  }

  /* 9. Сводка правила */
  function F9(s){
    const в = s.сводка;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="180" fill="url(#c883-стена)"/>
      <rect x="0" y="0" width="336" height="180" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Три случая — три решения',14,GOLD,true)}
      <rect x="20" y="42" width="296" height="34" rx="9" fill="rgba(255,215,106,.12)" stroke="${GOLD}" stroke-width="1.5"/>
      ${т(30,64,'корень: есть проверка с е',12,GOLD,true,'start')}${т(292,64,'ё',20,GOLD,true,'end')}
      <rect x="20" y="84" width="296" height="34" rx="9" fill="rgba(232,106,90,.12)" stroke="${RED}" stroke-width="1.5"/>
      ${т(30,106,'корень: проверки нет',12,RED,true,'start')}${т(292,106,'о',20,RED,true,'end')}
      <rect x="20" y="126" width="296" height="34" rx="9" fill="rgba(127,209,255,.12)" stroke="${BLUE}" stroke-width="1.5"/>
      ${т(30,148,'окончание: смотрим ударение',12,BLUE,true,'start')}${т(292,148,'о / е',16,BLUE,true,'end')}
    `,180);
    return ВОПРОС('Сколько случаев надо различать, чтобы верно выбрать букву?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), 'Три: корень с проверкой, корень без проверки, окончание', "r883Sum('три')")}
        ${BTN(4, в==='один'?'miss':'', 'Один: после шипящих всегда о', "r883Sum('один')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: три случая — ё в корне с проверкой, о в корне без проверки, о или е в окончании по ударению.'
                  : 'Нет: правило зависит от случая. После шипящих пишут и о, и ё, и е.')
               : ЖДЁТ()) +
      ПРАВИЛО('Корень с проверкой — <b>ё</b>. Корень без проверки — <b>о</b>. Окончание — <b>о</b> под ударением, <b>е</b> без ударения.');
  }

  /* 10. Тренажёр */
  const ТРЕНАЖЁР=[
    { ф:'В слове «щ_тка» какую букву писать?', о:['ё — есть проверка «щетина»','о — слышится о','е — без ударения'], в:0,
      р:'щетина — проверка с е, значит пишем ё.' },
    { ф:'В слове «ш_рох» какую букву писать?', о:['о — проверки с е нет','ё — проверю словом «шершавый»','е — без ударения'], в:0,
      р:'шорох — проверки нет, пишем о; слово надо запомнить.' },
    { ф:'В слове «пч_лы» какую букву писать?', о:['ё — проверка «пчела»','о — слышится о','е — окончание'], в:0,
      р:'пчела — слышится е, значит в корне ё.' },
    { ф:'В слове «свеч_й» какую букву писать?', о:['о — окончание под ударением','е — без ударения','ё — корень'], в:0,
      р:'ударение на окончании, поэтому о: свечой.' }
  ];
  function F10(s){
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length, з=ТРЕНАЖЁР[и], отв=s.тОтвет, готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:ТРЕНАЖЁР.length},(_,к)=>
      `<span class="точка ${к<и?'пройдено':(к===и?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(1,'cap','Уровень '+(и+1)+' из '+ТРЕНАЖЁР.length) +
      ВОПРОС(з.ф) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(3+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r883Train(${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${s.тВерно||0} · ошибок: ${s.тОшибки||0} · всего: ${ТРЕНАЖЁР.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий уровень','r883NextQ()')}</div>` : '');
  }


  const L883={
    id: ID, title:'О и ё после шипящих', ico:'🔤',
    src:'Русский язык · 5 класс · Орфография корня', subj:'rus',
    explain: [
      'На ярмарке писарь подписывает ценники, и в словах после шипящих стёрлась буква: «ш_лк», «крыж_вник». На слух о и ё после шипящих не различить: и «шёлк», и «шов» звучат одинаково. Значит, слух здесь не помощник — решает правило. Сначала смотрим, в какой части слова стоит буква: в корне, в окончании или в суффиксе. От этого зависит выбор.',

      'Вспомним шипящие буквы. Их четыре: ж, ш, ч, щ. Правило о и ё работает после всех четырёх, поэтому важно их не забыть. После ч и щ слова встречаются реже, но правило то же: «чёрный», «щёлка», «шорох», «жёлтый». Частая ошибка — вспомнить только ж и ш и растеряться на словах с ч и щ. Как проверить себя: назови четыре шипящие буквы вслух и приведи по одному слову на каждую.',

      'Главное правило корня: под ударением после шипящей пишем ё, если можно подобрать родственное слово с буквой е. «Жёлтый» — «желтеть», «чёрный» — «чернеть», «шёлк» — «шелковистый», «щёлка» — «щель». В проверочном слове звук слышится ясно, и сразу понятно, что писать. Частая ошибка — проверять тем же словом или формой слова: «шёлк» — «шёлковый» не проверка, потому что в нём та же стёртая буква. Как проверить себя: в проверочном слове должно ясно слышаться е.',

      'Если проверочного слова с е нет, в корне после шипящих пишем о. Так появились «шов», «шорох», «крыжовник», «капюшон»: родственных слов с е у них нет. Частая ошибка — писать «шёв» или «крыжёвник» по привычке: «раз после шипящих чаще ё, значит и здесь ё». Как проверить себя: попробуй подобрать слово с е — не получается, значит пишем о.',

      'Слова с о без проверки надо запомнить, и их не так много: шов, шорох, крыжовник, капюшон. В старших классах к ним добавятся обжора, трущоба, чащоба, шорник, но в пятом классе достаточно этих четырёх. Сложи их в «сундук памяти» на кадре. Частая ошибка — забыть, что «шорох» пишется с о, хотя очень хочется написать ё. Как проверить себя: проговори эти четыре слова вслух по порядку.',

      'Отдельный случай — окончания существительных. Здесь работает не проверка, а ударение: под ударением пишем о, без ударения — е. «Врачом», «плащом», «свечой» — ударение на окончании, поэтому о. «Товарищем», «задачей», «птицей» — ударение не на окончании, поэтому е. Частая ошибка — писать о без ударения или е под ударением. Как проверить себя: произнеси слово и послушай, падает ли ударение на окончание.',

      'В глаголах и причастиях после шипящих под ударением всегда ё: «печёт», «течёт», «жжёный», «ночёвка». Здесь не нужна ни проверка, ни ударение на окончании — достаточно узнать часть речи. Частая ошибка — писать «печот» по аналогии с «врачом»: окончания существительных и глагольные окончания подчиняются разным правилам. Как проверить себя: определи часть речи и вспомни её правило.',

      'Теперь подпиши ценники сам. В практике пять уровней: корень с проверкой, корень без проверки, слова-исключения и два окончания. На каждом уровне выбирай букву и читай разбор — он объясняет, почему решение верное или нет. Частая ошибка — решать по первому впечатлению: сначала определи часть слова, потом выбирай букву. Как проверить себя: после ответа перескажи правило своими словами.',

      'Соберём три случая вместе. Первый: буква в корне, проверка с е есть — пишем ё. Второй: буква в корне, проверки нет — пишем о и запоминаем слово. Третий: буква в окончании — под ударением о, без ударения е. Глаголы и причастия всегда дают ё. Частая ошибка — смешивать случаи. Как проверить себя: перед ответом скажи, где стоит буква: в корне или в окончании.',

      'Проверим себя на четырёх словах: «щётка», «шорох», «пчёлы», «свечой». Дальше — тренажёр со счётом; на каждом уровне сначала читай вопрос, потом выбирай ответ и читай разбор. Если ошибся, вернись к правилу: корень с проверкой, корень без проверки, окончание. Так выбор буквы перестаёт быть догадкой.'
    ],
    check: {
      q:'В слове «ш_лк» какую букву писать?',
      choices:['ё: есть проверка «шелковистый»','о: проверки нет','е: без ударения'],
      ans:0,
      exp:'Шелковистый, шёлковый — в проверке слышится е, значит в корне пишем ё.'
    },
    tasks: [
      { q:'Сколько шипящих букв в русском языке?', kind:'unit', ans:4, tol:0,
        hints:['Они шипят.','ж, ш, ч, щ'],
        sol:'Четыре: ж, ш, ч, щ.' },
      { q:'В слове «крыж_вник» какую букву писать?', kind:'choice',
        choices:['о','ё','е'], ans:0,
        hints:['Попробуй подобрать проверку с е.','Проверки нет — пишем о, слово надо запомнить.'],
        sol:'о: крыжовник — проверки с е нет.' },
      { q:'В слове «печ_т» какую букву писать?', kind:'choice',
        choices:['ё','о','е'], ans:0,
        hints:['Определи часть речи.','В глаголах после шипящих под ударением пишем ё.'],
        sol:'ё: печёт — глагол, под ударением пишем ё.' },
      { q:'В слове «товарищ_м» какую букву писать?', kind:'choice',
        choices:['е','о','ё'], ans:0,
        hints:['Это окончание существительного.','Послушай, падает ли ударение на окончание.'],
        sol:'е: ударение не на окончании, поэтому е.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L883.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else сцена=F10(s);

    const ЗАГОЛОВКИ={
      1:['Ярмарка','Кто решает: слух или правило'],
      2:['Шипящие','Четыре буквы'],
      3:['Проверка','Родственное слово с е'],
      4:['Без проверки','Пишем о'],
      5:['Сундук памяти','Слова с о'],
      6:['Окончания','Ударение решает'],
      7:['Глаголы','Всегда ё'],
      8:['Практика','Подпиши ценники'],
      9:['Три случая','Сводка правила'],
      10:['Тренажёр','Проверь себя']
    };
    const з=ЗАГОЛОВКИ[f]||['Орфография','О и ё после шипящих'];
    el.innerHTML = `<div class="s6 l883" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r883Fair=(к)=>{ S().ярмарка=к; chRender(0); };
  window.r883Hiss=(к)=>{ S().шипящие=к; chRender(0); };
  window.r883Chk=(к)=>{ const s=S(); if(s.прОтв==null) s.прОтв=к; chRender(0); };
  window.r883ChkNext=()=>{ const s=S(); s.проверка=((s.проверка||0)+1)%ПАРЫ.length; s.прОтв=null; chRender(0); };
  window.r883No=(к)=>{ const s=S(); if(s.бпОтв==null) s.бпОтв=к; chRender(0); };
  window.r883Box=(к)=>{ S().исключения=к; chRender(0); };
  window.r883End=(к)=>{ const s=S(); if(s.окОтв==null) s.окОтв=к; chRender(0); };
  window.r883Verb=(к)=>{ const s=S(); if(s.глОтв==null) s.глОтв=к; chRender(0); };
  window.r883Sum=(к)=>{ S().сводка=к; chRender(0); };
  window.r883Pick=(уровень,вариант)=>{
    const s=S();
    s.прогиУровень=уровень; s.прогиВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.проги=уровень+1;
    chRender(0);
  };
  window.r883Reset=()=>{ const s=S(); s.проги=0; s.прогиВыбор=null; s.прогиУровень=null; chRender(0); };
  window.r883Train=(к)=>{
    const s=S();
    if(s.тОтвет!=null) return;
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length;
    s.тОтвет=к;
    if(к===ТРЕНАЖЁР[и].в) s.тВерно=(s.тВерно||0)+1; else s.тОшибки=(s.тОшибки||0)+1;
    chRender(0);
  };
  window.r883NextQ=()=>{ const s=S(); s.тНомер=((s.тНомер||0)+1)%ТРЕНАЖЁР.length; s.тОтвет=null; chRender(0); };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L883; else arr.push(L883);
  })();
  window.RU883={render:render, L:L883};
})();
