/* ============ МАТЕМАТИКА · УРОК 830 · «ШКАЛЫ И КООРДИНАТЫ» ============
   5 класс. Сюжет: мастерская измерений — на каждом приборе своя шкала.

   ПРИНЦИП ЭТАЛОНА (урок 995): движение в кадре — это САМА МАТЕМАТИКА.
     кадр 1  три прибора оживают по очереди: штрихи линейки, столбик
             термометра, стрелка круглой шкалы;
     кадр 2  штрихи линейки ПРОСТУПАЮТ по одному, под длинными встают числа;
     кадр 3  промежуток между 0 и 10 ДЕЛИТСЯ: у одной линейки на 10 частей
             (цена 1), у другой на 5 (цена 2);
     кадр 4  единичный отрезок ШАГАЕТ по лучу, откладывая равные шаги;
     кадр 5  бегунок ИДЁТ по лучу, и на табло меняется координата;
     кадр 6  отрезок от 0 до 1 ДЕЛИТСЯ на четыре части, точка садится
             на третье деление — это координата 3/4;
     кадр 7  столбик термометра ПОДНИМАЕТСЯ до 22;
     кадр 8  стрелка круглой шкалы ПОВОРАЧИВАЕТСЯ к делению 7;
     кадр 9  две точки РАЗЪЕЗЖАЮТСЯ, и появляется знак сравнения;
     кадр 10 между точками УКЛАДЫВАЮТСЯ единичные шаги — это расстояние.

   ЧТОБЫ БЫЛО ИНТЕРЕСНО: в уроке пять разных приборов (линейка, луч с шагами,
   бегунок, термометр, круглая шкала) и три вида движения — рост штрихов,
   шаги по лучу, поворот стрелки. Ни один кадр не повторяет предыдущий.

   СВЕРКА С ПЕТЕРСОНОМ (5 класс): «шкалы, координатный луч», «изображение
   чисел и дробей на координатном луче», «сравнение чисел». Источники:
   план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, движение только вертикальное или вдоль оси,
   ничего за край кадра, prefers-reduced-motion выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 830;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';

  const CSS=`
  #lvis .s6.l830{gap:14px}
  #lvis .s6.l830 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l830 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l830 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l830 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l830 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l830 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l830 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l830 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l830 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l830 .правило b{color:${GOLD}}
  #lvis .s6.l830 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l830 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l830 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l830 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l830 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l830 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l830 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l830 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l830 .уровни .точка.сейчас{background:${GOLD};animation:l830dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l830dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l830 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l830{-webkit-text-size-adjust:100%}
  #lvis .s6.l830 [data-anim]{animation:l830rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l830rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l830 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l830 [data-anim]{animation:none!important}
    #lvis .s6.l830 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l830 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l830-style');
      if(!s){ s=document.createElement('style'); s.id='l830-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анП = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  /* штрих растёт из середины — так проступает деление шкалы */
  const штрихРастёт = (x,y0,y1,цвет,нач,толщ) =>
    `<line x1="${x}" y1="${((y0+y1)/2).toFixed(1)}" x2="${x}" y2="${((y0+y1)/2).toFixed(1)}" stroke="${цвет}" stroke-width="${толщ||1.6}">`+
      ан('y1',((y0+y1)/2)+';'+((y0+y1)/2)+';'+y0+';'+y0,'6s','keyTimes="0;0.08;0.3;1" begin="'+нач+'s"')+
      ан('y2',((y0+y1)/2)+';'+((y0+y1)/2)+';'+y1+';'+y1,'6s','keyTimes="0;0.08;0.3;1" begin="'+нач+'s"')+
    `</line>`;
  const светит = (длит,нач) => ан('opacity','1;0.55;1',длит, нач!=null?'begin="'+нач+'s"':'');

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c830-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22362c"/><stop offset="1" stop-color="#152219"/>
      </linearGradient>
      <linearGradient id="c830-металл" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#e8eef4"/><stop offset="1" stop-color="#9fb2c4"/>
      </linearGradient>
      <linearGradient id="c830-стекло" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#cfe6f5" stop-opacity=".35"/>
        <stop offset="1" stop-color="#7fd1ff" stop-opacity=".25"/>
      </linearGradient>
      <linearGradient id="c830-ртуть" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#ff8f7a"/><stop offset="1" stop-color="#d8452f"/>
      </linearGradient>
      <filter id="c830-тень" x="-30%" y="-30%" width="160%" height="160%">
        <feGaussianBlur stdDeviation="2.2"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||190}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* ЛУЧ: линия со стрелкой, деления и подписи */
  function луч(x0,x1,y,шагов,подписи,цвет){
    const ш=(x1-x0)/шагов;
    let s=`<line x1="${x0}" y1="${y}" x2="${x1}" y2="${y}" stroke="${цвет||'url(#c830-металл)'}" stroke-width="2.6"/>
      <path d="M${x1} ${y} L${x1-11} ${y-6} L${x1-11} ${y+6} Z" fill="${цвет||'#e8eef4'}"/>`;
    for(let i=0;i<=шагов;i++){
      const x=x0+i*ш;
      s+=`<line x1="${x.toFixed(1)}" y1="${y-8}" x2="${x.toFixed(1)}" y2="${y+8}" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      if(подписи) s+=т(x.toFixed(1),y+28,String(i),14,i===0?GOLD:МУТ,i===0);
    }
    return s;
  }

  /* ЛИНЕЙКА: корпус со штрихами */
  function линейка(x,y,ш,интервалов,подписи,оживить){
    const шШаг=ш/интервалов;
    let s=`<rect x="${x}" y="${y}" width="${ш}" height="34" rx="6" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>`;
    for(let i=0;i<=интервалов;i++){
      const x1=x+i*шШаг, длинный=(i%5===0);
      const y2=y+(длинный?24:16);
      s+= оживить ? штрихРастёт(x1.toFixed(1),y,y2,ИНК,i*0.22,1.4)
                  : `<line x1="${x1.toFixed(1)}" y1="${y}" x2="${x1.toFixed(1)}" y2="${y2}" stroke="${ИНК}" stroke-width="1.4"/>`;
      if(подписи && длинный) s+=т(x1.toFixed(1),y+44,String(i),12,МУТ);
    }
    return s;
  }

  /* ТЕРМОМЕТР: трубка и столбик */
  function термометр(x,y,высота,значение,макс){
    const в=высота, столб=Math.max(6,в*значение/макс);
    return `<g>
      <rect x="${x}" y="${y}" width="26" height="${в}" rx="13" fill="url(#c830-стекло)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      <circle cx="${x+13}" cy="${y+в+10}" r="13" fill="url(#c830-ртуть)" stroke="#8a2b1c" stroke-width="1.4"/>
      <rect x="${x+8}" y="${y+в}" width="10" height="0" rx="5" fill="url(#c830-ртуть)">
        ${ан('y',(y+в)+';'+(y+в)+';'+(y+в-столб)+';'+(y+в-столб),'7s','keyTimes="0;0.15;0.5;1"')}
        ${ан('height','0;0;'+столб+';'+столб,'7s','keyTimes="0;0.15;0.5;1"')}
      </rect>
      ${[0,1,2,3,4].map(i=>`<line x1="${x+20}" y1="${(y+в-i*в/4).toFixed(1)}" x2="${x+26}" y2="${(y+в-i*в/4).toFixed(1)}" stroke="${ЛИНИЯ}" stroke-width="1.2"/>`).join('')}
      ${т(x+52,y+22,String(значение)+'°',16,GOLD,true)}
    </g>`;
  }

  /* КРУГЛАЯ ШКАЛА: циферблат и стрелка */
  function круглый(цx,цy,r,значение,макс,нач){
    const угол=-90+360*значение/макс;
    let s=`<circle cx="${цx}" cy="${цy}" r="${r}" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.8"/>
      <circle cx="${цx}" cy="${цy}" r="4" fill="${ИНК}"/>`;
    for(let i=0;i<=макс;i++){
      const а=(-90+360*i/макс)*Math.PI/180;
      const x1=цx+Math.cos(а)*(r-9), y1=цy+Math.sin(а)*(r-9);
      const x2=цx+Math.cos(а)*r, y2=цy+Math.sin(а)*r;
      s+=`<line x1="${x1.toFixed(1)}" y1="${y1.toFixed(1)}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}" stroke="${ЛИНИЯ}" stroke-width="${i%5===0?2:1.1}"/>`;
      if(i%2===0 && i<макс) s+=т((цx+Math.cos(а)*(r-20)).toFixed(1),(цy+Math.sin(а)*(r-20)+4).toFixed(1),String(i),12,МУТ);
    }
    s+=`<g>${анП('0 '+цx+' '+цy+';0 '+цx+' '+цy+';'+угол+' '+цx+' '+цy+';'+угол+' '+цx+' '+цy,'7s','keyTimes="0;0.15;0.55;1" begin="'+(нач||0)+'s"')}
      <line x1="${цx}" y1="${цy}" x2="${цx}" y2="${(цy-r+10).toFixed(1)}" stroke="${GOLD}" stroke-width="2.6" stroke-linecap="round"/>
    </g>`;
    return s;
  }

  /* ================= КАДРЫ ================= */

  /* 1. Три прибора оживают по очереди */
  function F1(s){
    const в = s.приборы;
    return ВОПРОС('Что общего у линейки, термометра и стрелочного прибора?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Мастерская измерений',14,GOLD,true)}
        ${линейка(20,52,110,5,false,true)}
        ${т(75,132,'линейка',12,МУТ)}
        ${термометр(168,44,86,22,30)}
        ${т(181,168,'термометр',12,МУТ)}
        ${круглый(272,96,36,7,10,0)}
        ${т(272,168,'прибор со стрелкой',12,МУТ)}
        ${т(168,186,'у каждого прибора есть шкала: деления и числа',12,ИНК)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шкала'?'hit':(в?'miss':''), 'У всех есть шкала: деления и числа', "r830Dev('шкала')")}
        ${BTN(4, в==='длина'?'miss':'', 'Все измеряют длину', "r830Dev('длина')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шкала',
        в==='шкала' ? 'Верно: деления и числа — это шкала. Линейка мерит длину, термометр — температуру, прибор со стрелкой — что угодно.'
                    : 'Нет: длину мерит только линейка. Общее у всех — шкала, то есть деления с числами.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Шкала</b> — деления и числа на приборе; по ней читают значение.');
  }

  /* 2. Штрихи линейки проступают по одному */
  function F2(s){
    const в = s.штрихи;
    return ВОПРОС('Что означает число, стоящее под штрихом линейки?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Штрихи встают по одному',14,GOLD,true)}
        ${линейка(24,58,288,10,true,true)}
        ${т(168,148,'длинные штрихи отмечены числами 0, 1, 2, … 10',12,ИНК)}
        ${т(168,172,'расстояние между соседними числами — один и тот же шаг',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='значение'?'hit':(в?'miss':''), 'Значение отметки: сколько шагов от нуля', "r830Marks('значение')")}
        ${BTN(4, в==='номер'?'miss':'', 'Номер штриха по порядку', "r830Marks('номер')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='значение',
        в==='значение' ? 'Верно: число показывает, сколько единичных шагов от нуля до этой отметки.'
                      : 'Нет: число — не номер, а значение: от нуля до отметки отложено столько шагов.') : ЖДЁТ()) +
      ПРАВИЛО('Число под штрихом — <b>значение</b> отметки, счёт идёт от нуля.');
  }

  /* 3. Цена деления: два прибора */
  function F3(s){
    const в = s.цена;
    return ВОПРОС('На нижней линейке между 0 и 10 пять промежутков. Чему равна цена деления?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Сколько единиц в промежутке',14,GOLD,true)}
        ${т(30,44,'десять промежутков',12,BLUE,true,'start')}
        ${линейка(30,50,276,10,false,true)}
        ${т(168,100,'цена деления 1',14,BLUE,true)}
        ${линейка(30,104,276,5,false,true)}
        ${т(168,154,'цена деления 2',14,GOLD,true)}
        ${т(168,180,'число на делении = цена деления · номер промежутка',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='два'?'hit':(в?'miss':''), 'Два: 10 : 5 = 2', "r830Price('два')")}
        ${BTN(4, в==='пять'?'miss':'', 'Пять: промежутков пять', "r830Price('пять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='два',
        в==='два' ? 'Верно: десять единиц разделили на пять промежутков — в каждом по две: цена деления 2.'
                  : 'Нет: пять — это число промежутков. Цена деления — сколько единиц в одном промежутке: 10 : 5 = 2.') : ЖДЁТ()) +
      ПРАВИЛО('Цена деления = <b>разница чисел : число промежутков</b>. Здесь 10 : 5 = 2.');
  }

  /* 4. Единичный отрезок шагает по лучу */
  function F4(s){
    const в = s.единичный;
    const x0=28, x1=312, y=96, ш=6;
    const шаг=(x1-x0)/ш;
    return ВОПРОС('Что такое единичный отрезок на координатном луче?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Единичный отрезок шагает по лучу',14,GOLD,true)}
        ${луч(x0,x1,y,ш,true)}
        ${Array.from({length:ш},(_,i)=>`<rect x="${(x0+i*шаг).toFixed(1)}" y="${y-5}" width="${шаг.toFixed(1)}" height="10" rx="3"
            fill="none" stroke="${GOLD}" stroke-width="1.8">
            ${ан('opacity','0.3;0.3;1;1','7s','keyTimes="0;'+(0.08+i*0.12).toFixed(2)+';'+(0.22+i*0.12).toFixed(2)+';1"')}
          </rect>`).join('')}
        <g>${анТ('0 0;0 0;'+(x1-x0-шаг).toFixed(1)+' 0;'+(x1-x0-шаг).toFixed(1)+' 0;0 0','7s','keyTimes="0;0.06;0.74;0.9;1"')}
          <rect x="${(x0+шаг/2-8).toFixed(1)}" y="${y-30}" width="16" height="12" rx="3" fill="${GOLD}"/>
          ${т((x0+шаг/2).toFixed(1),y-40,'1',12,GOLD,true)}
        </g>
        ${т(168,150,'от нуля откладывают равные отрезки длиной 1',12,ИНК)}
        ${т(168,176,'концы отрезков отмечают числами 1, 2, 3, …',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='между01'?'hit':(в?'miss':''), 'Расстояние между соседними числами 0 и 1', "r830Unit('между01')")}
        ${BTN(4, в==='любой'?'miss':'', 'Любой отрезок на луче', "r830Unit('любой')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='между01',
        в==='между01' ? 'Верно: единичный отрезок — расстояние от 0 до 1; им и отмеряют все остальные отметки.'
                      : 'Нет: любой отрезок не годится. Единичный — именно от 0 до 1, он задаёт шаг шкалы.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Единичный отрезок</b> — расстояние от 0 до 1; он задаёт шаг всего луча.');
  }

  /* 5. Бегунок идёт по лучу, координата меняется */
  function F5(s){
    const в = s.бегунок;
    const x0=28, x1=312, y=100, ш=6;
    const шаг=(x1-x0)/ш;
    return ВОПРОС('Бегунок остановился на четвёртом делении. Какая у него координата?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Бегунок идёт, координата растёт',14,GOLD,true)}
        ${луч(x0,x1,y,ш,true)}
        <g>${анТ('0 0;0 0;'+(4*шаг).toFixed(1)+' 0;'+(4*шаг).toFixed(1)+' 0;0 0','8s','keyTimes="0;0.06;0.62;0.86;1"')}
          <circle cx="${x0}" cy="${y-26}" r="9" fill="url(#c830-металл)" stroke="#3a4750" stroke-width="1.6"/>
          <path d="M${x0-7} ${y-14} L${x0+7} ${y-14} L${x0} ${y-6} Z" fill="${GOLD}"/>
          <g>${ан('opacity','0.4;0.4;1;1','8s','keyTimes="0;0.55;0.68;1"')}
            ${т((4*шаг+x0).toFixed(1),y-46,'4',20,GOLD,true)}
          </g>
        </g>
        ${[0,1,2,3,4].map(i=>`<g>${светит('8s',0.3+i*0.9)}${т(x0+i*шаг,y+46,String(i),12,i===4?GOLD:МУТ,true)}</g>`).join('')}
        ${т(168,166,'сколько шагов от нуля — такая и координата',12,ИНК)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='четыре'?'hit':(в?'miss':''), '4: от нуля отсчитано четыре шага', "r830Walk('четыре')")}
        ${BTN(4, в==='три'?'miss':'', '3: бегунок прошёл три отметки', "r830Walk('три')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='четыре',
        в==='четыре' ? 'Верно: бегунок на четвёртом делении — координата 4. Пишут так: A(4).'
                     : 'Нет: считаем шаги от нуля до деления, на котором он стоит: их четыре, координата 4.') : ЖДЁТ()) +
      ПРАВИЛО('Координата точки — <b>число шагов от нуля</b> до этой точки: A(4).');
  }

  /* 6. Дробная координата 3/4 */
  function F6(s){
    const в = s.дробная;
    const x0=34, x1=302, y=104;
    const ш=4, шаг=(x1-x0)/ш;
    return ВОПРОС('Точка стоит на третьем делении, если отрезок от 0 до 1 разделили на четыре части. Какая координата?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Отрезок от 0 до 1 делим на четыре части',14,GOLD,true)}
        <line x1="${x0}" y1="${y}" x2="${x1}" y2="${y}" stroke="url(#c830-металл)" stroke-width="2.6"/>
        <path d="M${x1} ${y} L${x1-11} ${y-6} L${x1-11} ${y+6} Z" fill="#e8eef4"/>
        ${Array.from({length:ш},(_,i)=>штрихРастёт((x0+(i+1)*шаг).toFixed(1),y-10,y+10,ИНК,i*0.3,1.6)).join('')}
        ${т(x0,y+30,'0',14,GOLD,true)}${т(x1,y+30,'1',14,GOLD,true)}
        <g>${анТ('0 0;0 0;0 0;0 0','7s')}
          <circle cx="${(x0+3*шаг).toFixed(1)}" cy="${y-26}" r="8" fill="${GOLD}">
            ${ан('opacity','0.25;0.25;1;1','7s','keyTimes="0;0.55;0.7;1"')}
          </circle>
          <path d="M${(x0+3*шаг-6).toFixed(1)} ${y-16} L${(x0+3*шаг+6).toFixed(1)} ${y-16} L${(x0+3*шаг).toFixed(1)} ${y-8} Z" fill="${GOLD}">
            ${ан('opacity','0.25;0.25;1;1','7s','keyTimes="0;0.55;0.7;1"')}
          </path>
        </g>
        ${т((x0+3*шаг).toFixed(1),y-42,'3/4',20,GOLD,true)}
        ${т(168,y+56,'каждая часть — одна четвёртая; три части — три четвёртых',12,ИНК)}
        ${т(168,y+78,'на луче дробь стоит между 0 и 1',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тричетвёртых'?'hit':(в?'miss':''), '3/4: три части из четырёх', "r830Frac('тричетвёртых')")}
        ${BTN(4, в==='четыретрети'?'miss':'', '4/3: четыре деления', "r830Frac('четыретрети')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тричетвёртых',
        в==='тричетвёртых' ? 'Верно: отрезок от 0 до 1 разделён на четыре части, точка на третьей — координата 3/4.'
                           : 'Нет: делений четыре, но взято три части: координата 3/4.') : ЖДЁТ()) +
      ПРАВИЛО('Дроби тоже живут на луче: точка на третьей части из четырёх — это <b>3/4</b>.');
  }

  /* 7. Термометр: столбик поднимается */
  function F7(s){
    const в = s.термометр;
    return ВОПРОС('Столбик термометра поднялся до деления 22. Что он показывает?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Термометр: своя шкала',14,GOLD,true)}
        ${термометр(70,44,104,22,30)}
        <g>${[0,10,20,30].map(i=>`<line x1="96" y1="${(148-i*104/3).toFixed(1)}" x2="120" y2="${(148-i*104/3).toFixed(1)}" stroke="${ЛИНИЯ}" stroke-width="1.4"/>${т(134,(148-i*104/3+4).toFixed(1),String(i),12,МУТ,false,'start')}`).join('')}</g>
        ${т(240,80,'деление 22',16,GOLD,true)}
        ${т(240,110,'цена деления 1',12,МУТ)}
        ${т(240,134,'столбик дошёл',12,ИНК)}
        ${т(240,158,'до отметки 22',12,ИНК)}
        ${т(168,186,'по шкале читают значение: 22 °C',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='двадцать два'?'hit':(в?'miss':''), '22 °C: столбик на отметке 22', "r830Thermo('двадцать два')")}
        ${BTN(4, в==='двадцать'?'miss':'', '20 °C: округляем до круглого', "r830Thermo('двадцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='двадцать два',
        в==='двадцать два' ? 'Верно: столбик стоит на отметке 22, значит 22 °C. По шкале читают точно, а не примерно.'
                          : 'Нет: округлять не нужно — шкала показывает ровно 22 °C.') : ЖДЁТ()) +
      ПРАВИЛО('У каждого прибора своя шкала: у термометра <b>цена деления 1</b>, читаем 22 °C.');
  }

  /* 8. Круглая шкала: стрелка поворачивается */
  function F8(s){
    const в = s.круглая;
    return ВОПРОС('Стрелка остановилась на делении 7. Какое значение показывает прибор?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Шкала бывает и круглой',14,GOLD,true)}
        ${круглый(120,102,64,7,10,0)}
        ${т(120,186,'деления идут по кругу',12,МУТ)}
        ${т(252,80,'стрелка на 7',16,GOLD,true)}
        ${т(252,110,'цена деления 1',12,МУТ)}
        ${т(252,138,'значение 7',14,GREEN,true)}
        ${т(252,164,'условных единиц',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='семь'?'hit':(в?'miss':''), '7: стрелка на делении 7', "r830Dial('семь')")}
        ${BTN(4, в==='три'?'miss':'', '3: стрелка повернулась на три', "r830Dial('три')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='семь',
        в==='семь' ? 'Верно: читаем деление, у которого стоит стрелка: 7. Форма шкалы не важна, важно деление.'
                   : 'Нет: смотрим, у какого деления остановилась стрелка: у седьмого.') : ЖДЁТ()) +
      ПРАВИЛО('Шкала может быть прямой или <b>круглой</b> — читают всегда деление под стрелкой.');
  }

  /* 9. Сравнение координат */
  function F9(s){
    const в = s.сравнение;
    const x0=30, x1=306, y=104, ш=8;
    const шаг=(x1-x0)/ш;
    return ВОПРОС('На луче отмечены A(3) и B(7). Какая координата больше?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Правее — значит больше',14,GOLD,true)}
        ${луч(x0,x1,y,ш,true)}
        <g>${анТ('0 0;0 0;-12 0;-12 0;0 0','7s','keyTimes="0;0.2;0.45;0.85;1"')}
          <circle cx="${(x0+3*шаг).toFixed(1)}" cy="${y}" r="7" fill="${BLUE}"/>
          ${т((x0+3*шаг).toFixed(1),y-18,'A(3)',12,BLUE,true)}
        </g>
        <g>${анТ('0 0;0 0;12 0;12 0;0 0','7s','keyTimes="0;0.2;0.45;0.85;1"')}
          <circle cx="${(x0+7*шаг).toFixed(1)}" cy="${y}" r="7" fill="${GOLD}"/>
          ${т((x0+7*шаг).toFixed(1),y-18,'B(7)',12,GOLD,true)}
        </g>
        <g>${светит('7s',2)}${т(168,y+52,'3 < 7',20,GREEN,true)}</g>
        ${т(168,y+78,'точка B стоит правее, поэтому её координата больше',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='семь'?'hit':(в?'miss':''), '7: точка B стоит правее', "r830Cmp('семь')")}
        ${BTN(4, в==='три'?'miss':'', '3: точка A ближе к нулю', "r830Cmp('три')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='семь',
        в==='семь' ? 'Верно: на луче правее стоит то число, которое больше: 7 > 3.'
                   : 'Нет: близость к нулю ничего не решает. Смотрим, кто правее: B(7) правее, значит 7 больше.') : ЖДЁТ()) +
      ПРАВИЛО('На координатном луче <b>правее — больше</b>: 3 < 7.');
  }

  /* 10. Расстояние между точками */
  function F10(s){
    const в = s.расстояние;
    const x0=30, x1=306, y=104, ш=8;
    const шаг=(x1-x0)/ш;
    return ВОПРОС('Каково расстояние между точками A(2) и B(7) на луче?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c830-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Между точками укладываются шаги',14,GOLD,true)}
        ${луч(x0,x1,y,ш,true)}
        ${Array.from({length:5},(_,i)=>`<rect x="${(x0+(2+i)*шаг).toFixed(1)}" y="${y-5}" width="${шаг.toFixed(1)}" height="10" rx="3"
            fill="none" stroke="${GREEN}" stroke-width="1.8">
            ${ан('opacity','0.3;0.3;1;1','7s','keyTimes="0;'+(0.1+i*0.16).toFixed(2)+';'+(0.24+i*0.16).toFixed(2)+';1"')}
          </rect>`).join('')}
        <circle cx="${(x0+2*шаг).toFixed(1)}" cy="${y}" r="7" fill="${BLUE}"/>
        ${т((x0+2*шаг).toFixed(1),y-18,'A(2)',12,BLUE,true)}
        <circle cx="${(x0+7*шаг).toFixed(1)}" cy="${y}" r="7" fill="${GOLD}"/>
        ${т((x0+7*шаг).toFixed(1),y-18,'B(7)',12,GOLD,true)}
        <g>${светит('7s',2.2)}${т(168,y+52,'7 − 2 = 5',20,GREEN,true)}</g>
        ${т(168,y+78,'пять единичных шагов между точками',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пять'?'hit':(в?'miss':''), '5: между точками пять шагов', "r830Dist('пять')")}
        ${BTN(4, в==='девять'?'miss':'', '9: сложили координаты', "r830Dist('девять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пять',
        в==='пять' ? 'Верно: расстояние — это разность координат: 7 − 2 = 5.'
                   : 'Нет: координаты складывать не нужно. Расстояние — разность: 7 − 2 = 5.') : ЖДЁТ()) +
      ПРАВИЛО('Расстояние между точками на луче — <b>разность координат</b>: 7 − 2 = 5.');
  }

  /* 11. Практика: пять уровней */
  const УРОВНИ=[
    { вопрос:'На линейке между 0 и 40 восемь промежутков. Чему равна цена деления?', варианты:[
        {т:'5', ок:true,  почему:'40 : 8 = 5 — в каждом промежутке пять единиц.'},
        {т:'8', ок:false, почему:'Восемь — это число промежутков, а не цена деления.'}]},
    { вопрос:'Точка стоит на шестом делении от нуля. Какая у неё координата?', варианты:[
        {т:'6', ок:true,  почему:'Сколько шагов от нуля — такая и координата.'},
        {т:'5', ок:false, почему:'Считаем шаги от нуля до самой точки: их шесть.'}]},
    { вопрос:'Отрезок от 0 до 1 разделили на пять частей. Какая координата у точки на второй части?', варианты:[
        {т:'2/5', ок:true,  почему:'Взято две части из пяти — это 2/5.'},
        {т:'5/2', ок:false, почему:'Частей всего пять, взято две: 2/5.'}]},
    { вопрос:'Что больше: A(4) или B(9)?', варианты:[
        {т:'B(9): она правее', ок:true,  почему:'На луче правее — больше.'},
        {т:'A(4): она ближе к нулю', ок:false, почему:'Близость к нулю не делает число больше.'}]},
    { вопрос:'Чему равно расстояние между A(3) и B(8)?', варианты:[
        {т:'5', ок:true,  почему:'Разность координат: 8 − 3 = 5.'},
        {т:'11', ок:false, почему:'Координаты складывать не нужно: расстояние — разность.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Шкала: находим цену деления, считаем шаги от нуля, читаем деление под отметкой или стрелкой.') +
        `<div class="ask">${BTN(4,'','пройти заново','r830Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r830Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Порядок один: <b>цена деления → шаги от нуля → координата</b>.');
  }

  /* 12. Тренажёр */
  const ТРЕНАЖЁР=[
    { ф:'На линейке между 0 и 30 шесть промежутков. Чему равна цена деления?', о:['5','6','30'], в:0,
      р:'30 : 6 = 5 — в каждом промежутке пять единиц.' },
    { ф:'Точка на седьмом делении от нуля. Какая координата?', о:['7','6','8'], в:0,
      р:'Считаем шаги от нуля: их семь.' },
    { ф:'Стрелка прибора остановилась на делении 4. Что показывает прибор?', о:['4','3','5'], в:0,
      р:'Читаем деление под стрелкой.' },
    { ф:'Чему равно расстояние между A(6) и B(10)?', о:['4','16','10'], в:0,
      р:'Расстояние — разность координат: 10 − 6 = 4.' }
  ];
  function F12(s){
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length, з=ТРЕНАЖЁР[и], отв=s.тОтвет, готово=отв!=null, верно=отв===з.в;
    return ТОЧКИ(ТРЕНАЖЁР.length, и, и) +
      A(2,'cap','Уровень '+(и+1)+' из '+ТРЕНАЖЁР.length) +
      ВОПРОС(з.ф) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(3+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r830Train(${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${s.тВерно||0} · ошибок: ${s.тОшибки||0} · всего: ${ТРЕНАЖЁР.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий уровень','r830NextQ()')}</div>` : '');
  }

  const L830={
    id: ID, title:'Шкалы и координаты', ico:'📏',
    src:'Математика · 5 класс · Шкалы и координатный луч', subj:'math',
    explain: [
      'В мастерской измерений у каждого прибора своя шкала: у линейки штрихи с числами, у термометра столбик и деления, у стрелочного прибора круг с делениями и стрелка. Шкала — это деления и числа, по которым читают значение. Линейка измеряет длину, термометр — температуру, стрелочный прибор — что угодно, но читают все одинаково: находят нужное деление и смотрят его значение.',

      'Разберём линейку. Штрихи стоят через равные промежутки, а под длинными штрихами написаны числа. Число под штрихом — это значение отметки: сколько единичных шагов отложено от нуля. Числа идут по порядку 0, 1, 2, … и расстояние между соседними числами всегда одно и то же. Частая ошибка — считать число «номером штриха». Как проверить себя: посчитай шаги от нуля до отметки — их столько же, сколько написано.',

      'Цена деления — сколько единиц в одном промежутке. Чтобы её найти, берут два соседних числа, вычитают меньшее из большего и делят на число промежутков между ними. На одной линейке между 0 и 10 десять промежутков: 10 : 10 = 1 — цена деления 1. На другой между 0 и 10 пять промежутков: 10 : 5 = 2 — цена деления 2. Частая ошибка — путать число промежутков с ценой деления. Как проверить себя: подставь цену в формулу и проверь, что произведение даёт разницу чисел.',

      'Координатный луч начинается в точке 0, идёт вправо и имеет стрелку. На нём от нуля откладывают равные отрезки длиной 1 — это единичные отрезки, их концы отмечают числами 1, 2, 3 и так далее. Единичный отрезок — это расстояние от 0 до 1, и он задаёт шаг всей шкалы луча. Частая ошибка — считать единичным «любой» отрезок. Как проверить себя: расстояние от нуля до отметки 1 и есть единичный отрезок.',

      'Координата точки — это число, которое показывает, сколько единичных отрезков отложено от нуля до этой точки. Если бегунок остановился на четвёртом делении, его координата 4; пишут коротко: A(4). Чем дальше вправо, тем больше координата. Частая ошибка — считать число отметок, мимо которых прошёл бегунок, а не шаги от нуля. Как проверить себя: считай шаги от нуля до самой точки.',

      'На координатном луче живут не только целые числа, но и дроби. Разделим отрезок от 0 до 1 на четыре равные части: каждая часть — это одна четвёртая. Точка на третьем делении имеет координату 3/4 — она стоит между 0 и 1. Частая ошибка — писать 4/3, путая число частей и число взятых частей. Как проверить себя: частей всего четыре — значит, знаменатель 4; взято три — числитель 3.',

      'Термометр — тоже прибор со шкалой. Столбик поднимается и останавливается у нужного деления, а мы читаем значение: если столбик на отметке 22, температура 22 °C. Цена деления здесь 1, поэтому читаем точно, а не примерно. Частая ошибка — округлять показание до круглого числа. Как проверить себя: посмотри, у какого деления стоит столбик, и назови это число.',

      'Шкала бывает и круглой: деления идут по кругу, а вместо столбика — стрелка. Читают так же: смотрят, у какого деления остановилась стрелка. Если стрелка на делении 7, прибор показывает 7 условных единиц. Форма шкалы не меняет правила чтения. Частая ошибка — считать, что на круглой шкале значения считают по-другому. Как проверить себя: найди деление под стрелкой и назови его.',

      'Координаты удобно сравнивать. На луче правее стоит то число, которое больше: точка B(7) правее точки A(3), значит 7 > 3. Сравнивать можно только по положению на луче: чем правее, тем больше. Частая ошибка — считать, что ближе к нулю значит больше. Как проверить себя: посмотри, какая точка правее.',

      'Расстояние между двумя точками на луче равно разности их координат. Между A(2) и B(7) укладывается пять единичных отрезков: 7 − 2 = 5. Складывать координаты не нужно — расстояние всегда разность. Частая ошибка — сложить координаты и получить 9. Как проверить себя: посчитай единичные шаги между точками.',

      'Пройти практику: пять уровней — цена деления, чтение координаты, дробная координата, сравнение и расстояние. Дальше — тренажёр со счётом.',

      'Соберём правило. Шкала — деления и числа. Цена деления — разница соседних чисел, делённая на число промежутков. На координатном луче от нуля откладывают единичные отрезки; координата точки — число шагов от нуля. На луче правее значит больше, а расстояние между точками — разность координат.'
    ],
    check: {
      q:'На линейке между 0 и 40 восемь промежутков. Чему равна цена деления?',
      choices:['5','8','40'],
      ans:0,
      exp:'40 : 8 = 5 — в каждом промежутке по пять единиц.'
    },
    tasks: [
      { q:'Точка стоит на девятом делении от нуля. Какая у неё координата?', kind:'choice',
        choices:['9','8','10'], ans:0,
        hints:['Считай шаги от нуля.','Сколько шагов — такая и координата.'],
        sol:'Координата 9: от нуля отложено девять единичных отрезков.' },
      { q:'Отрезок от 0 до 1 разделили на три части. Какая координата у точки на второй части?', kind:'choice',
        choices:['2/3','3/2','1/3'], ans:0,
        hints:['Частей три — знаменатель 3.','Взято две части — числитель 2.'],
        sol:'Координата 2/3.' },
      { q:'Чему равно расстояние между A(4) и B(9)?', kind:'choice',
        choices:['5','13','9'], ans:0,
        hints:['Расстояние — разность координат.','9 − 4.'],
        sol:'Расстояние равно 5.' },
      { q:'Что больше: A(6) или B(11)?', kind:'choice',
        choices:['B(11)','A(6)','они равны'], ans:0,
        hints:['Смотри, какая точка правее.','На луче правее — больше.'],
        sol:'B(11) больше: она стоит правее.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L830.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else сцена=F12(s);

    const ЗАГОЛОВКИ={
      1:['Приборы','У каждого своя шкала'],
      2:['Штрихи','Числа под делениями'],
      3:['Цена деления','Сколько в промежутке'],
      4:['Координатный луч','Единичный отрезок'],
      5:['Координата','Бегунок и число'],
      6:['Дробь на луче','Три четверти'],
      7:['Термометр','Столбик и деления'],
      8:['Круглая шкала','Стрелка и деления'],
      9:['Сравнение','Правее — больше'],
      10:['Расстояние','Разность координат'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр','Проверь себя']
    };
    const з=ЗАГОЛОВКИ[f]||['Шкалы','Координатный луч'];
    el.innerHTML = `<div class="s6 l830" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r830Dev=(к)=>{ S().приборы=к; chRender(0); };
  window.r830Marks=(к)=>{ S().штрихи=к; chRender(0); };
  window.r830Price=(к)=>{ S().цена=к; chRender(0); };
  window.r830Unit=(к)=>{ S().единичный=к; chRender(0); };
  window.r830Walk=(к)=>{ S().бегунок=к; chRender(0); };
  window.r830Frac=(к)=>{ S().дробная=к; chRender(0); };
  window.r830Thermo=(к)=>{ S().термометр=к; chRender(0); };
  window.r830Dial=(к)=>{ S().круглая=к; chRender(0); };
  window.r830Cmp=(к)=>{ S().сравнение=к; chRender(0); };
  window.r830Dist=(к)=>{ S().расстояние=к; chRender(0); };
  window.r830Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r830Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r830Train=(к)=>{
    const s=S();
    if(s.тОтвет!=null) return;
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length;
    s.тОтвет=к;
    if(к===ТРЕНАЖЁР[и].в) s.тВерно=(s.тВерно||0)+1; else s.тОшибки=(s.тОшибки||0)+1;
    chRender(0);
  };
  window.r830NextQ=()=>{ const s=S(); s.тНомер=((s.тНомер||0)+1)%ТРЕНАЖЁР.length; s.тОтвет=null; chRender(0); };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L830; else arr.push(L830);
  })();
  window.MA830={render:render, L:L830};
})();
