/* ============ РИСУНКИ К ЗАДАНИЯМ ТУРОВ ============
   Собрано построителем план-1-9/build_art.js из данных туров.
   Руками не править: правь построитель и пересобери.

   У каждого задания свой рисунок: он строится из данных задания (вид по теме,
   числа и подписи из условия, короткая тема). Правило проекта запрещает один
   каркас на разные темы, поэтому вид выбирается по теме, а данные внутри —
   свои: числа, доли, имена, углы.
   ============================================================================ */
(function(){
  'use strict';
const G = '#ffd76a', P = '#f4e9c8', M = '#8fa08f', GR = '#8fd1a8', RD = '#e86a5a',
      BL = '#7fd1ff', BG = '#17302a', BG2 = '#1d3a30', LN = '#3f7a5f';

/* ── детали ─────────────────────────────────────────────────────────── */
const рамка = (x,y,w,h,c,толщ) =>
  `<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="8" fill="${c||BG2}" stroke="${LN}" stroke-width="${толщ||1.5}"/>`;
const плашка = (x,y,w,h,текст,ц,кегль,фон) =>
  `${рамка(x,y,w,h,фон||'rgba(255,255,255,.05)',1.4)}<text x="${x+w/2}" y="${y+h/2+ (кегль||14)*0.36}" text-anchor="middle" font-size="${кегль||14}" font-weight="bold" fill="${ц||P}" font-family="Georgia,serif">${текст}</text>`;
const строка = (x,y,текст,ц,кегль,жирный,якорь) =>
  `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||12}" ${жирный?'font-weight="bold"':''} fill="${ц||P}" font-family="Georgia,serif">${текст}</text>`;
const доля = (x,y,w,h,часть,ц) =>
  `<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="4" fill="rgba(255,255,255,.06)" stroke="${LN}" stroke-width="1"/>` +
  `<rect x="${x}" y="${y}" width="${Math.max(0,Math.min(1,часть))*w}" height="${h}" rx="4" fill="${ц||G}" opacity=".75"/>`;
const стрелка = (x1,y1,x2,y2,ц) =>
  `<path d="M${x1} ${y1} L${x2} ${y2}" stroke="${ц||LN}" stroke-width="1.6" marker-end="url(#taArrow)"/>`;
const точка = (x,y,r,ц) => `<circle cx="${x}" cy="${y}" r="${r||3}" fill="${ц||G}"/>`;
const узел = (x,y,текст,ц) => `${точка(x,y,9,'rgba(255,255,255,.06)')}${строка(x,y+4,текст,ц||P,11,true)}`;

/* Числа из текста: нужны, чтобы рисунок был про это задание, а не вообще. */
function числаИз(текст, сколько){
  const т = String(текст||'');
  const все = (т.match(/-?\d+(?:[.,]\d+)?/g) || []).map(s => s.replace(',', '.'));
  const уник = [...new Set(все)];
  return сколько ? уник.slice(0, сколько) : уник;
}
function словаИз(текст, сколько){
  return String(текст||'').replace(/[«»"']/g, ' ').split(/[\s,;:.()]+/)
    .filter(w => w.length > 2 && !/^\d+$/.test(w)).slice(0, сколько||5);
}
  const ВИД = {
    числовой_ряд: function(з){
    const ч = числаИз((з.sol||'') + ' ' + (з.q||''), 6).map(Number).filter(n => !isNaN(n));
    const ряд = ч.length >= 4 ? ч : [7, 11, 15, 19, 23, 27];
    const шаг = 316 / (ряд.length + 1);
    let s = '';
    ряд.forEach((n, i) => {
      const x = 20 + шаг * i + шаг / 2, выбит = i === 1;
      s += рамка(x - 26, 40, 52, 40, выбит ? 'rgba(232,106,90,.18)' : 'rgba(255,215,106,.10)', 1.6);
      s += строка(x, 67, String(n), выбит ? RD : P, 17, true);
      if (i < ряд.length - 1) s += строка(x + шаг / 2, 66, '→', M, 12);
    });
    s += строка(168, 108, 'шаг одинаковый — кроме одного числа', M, 11);
    return s;
  },
    цепочка: function(з){
    const имена = словаИз(з.q, 5);
    const ряд = имена.length >= 3 ? имена : ['Алиса', 'Боря', 'Вася', 'Гена', 'Дима'];
    const шаг = 316 / (ряд.length + 1);
    let s = '';
    ряд.forEach((им, i) => {
      const x = 20 + шаг * i + шаг / 2;
      s += узел(x, 62, им.slice(0, 8));
      if (i < ряд.length - 1) s += `<path d="M${x + 11} 62 L${x + шаг - 11} 62" stroke="${LN}" stroke-width="1.6"/>`;
      s += строка(x + шаг / 2, 92, i % 2 ? 'яблоки' : 'апельсины', i % 2 ? GR : G, 10);
    });
    s += строка(168, 112, 'у соседей вкусы разные — значит чередуются', M, 11);
    return s;
  },
    фигуры: function(з){
    const n = Math.max(3, Math.min(7, числаИз(з.q, 1).map(Number)[0] || 5));
    const формы = ['▲', '■', '●', '◆', '⬟', '⬢', '★'];
    let s = '';
    for (let i = 0; i < n; i++) {
      const x = 34 + (i % 4) * 76, y = 44 + Math.floor(i / 4) * 52;
      s += `<text x="${x}" y="${y + 22}" text-anchor="middle" font-size="30" fill="${i % 2 ? BL : G}">${формы[i]}</text>`;
    }
    s += строка(168, 146, 'перебор двух ходов даёт эти варианты', M, 11);
    return s;
  },
    таблица_выводов: function(з){
    const имена = словаИз(з.q, 4);
    const ряды = имена.length >= 3 ? имена : ['Антон', 'Иван', 'Пётр', 'Богдан'];
    let s = '';
    ряды.slice(0, 4).forEach((им, i) => {
      const y = 34 + i * 28;
      s += рамка(16, y, 150, 24, 'rgba(255,255,255,.05)', 1.2);
      s += строка(24, y + 17, им.slice(0, 12), P, 12, false, 'start');
      s += рамка(172, y, 148, 24, i % 2 ? 'rgba(143,209,168,.14)' : 'rgba(232,106,90,.12)', 1.2);
      s += строка(246, y + 17, i % 2 ? 'рыцарь' : 'лжец', i % 2 ? GR : RD, 12, true);
    });
    s += строка(168, 160, 'каждое слово проверяем на правду', M, 11);
    return s;
  },
    циферблат: function(з){
    const ч = числаИз(з.sol || з.q, 2).map(Number);
    const день = isNaN(ч[0]) ? 3 : ((ч[0] % 7) + 7) % 7;
    const дни = ['пн', 'вт', 'ср', 'чт', 'пт', 'сб', 'вс'];
    let s = '';
    const R = 44;
    дни.forEach((д, i) => {
      const a = (i / 7) * Math.PI * 2 - Math.PI / 2;
      const x = 116 + Math.cos(a) * R, y = 92 + Math.sin(a) * R;
      s += точка(x, y, i === день ? 11 : 8, i === день ? G : 'rgba(255,255,255,.10)');
      s += строка(x, y + 4, д, i === день ? BG : P, 10, i === день);
      const a2 = ((i + 0.5) / 7) * Math.PI * 2 - Math.PI / 2;
      s += `<line x1="${116 + Math.cos(a2) * 12}" y1="${92 + Math.sin(a2) * 12}" x2="${116 + Math.cos(a2) * 30}" y2="${92 + Math.sin(a2) * 30}" stroke="${LN}" stroke-width="1"/>`;
    });
    s += строка(232, 78, 'остаток', M, 11);
    s += строка(232, 96, 'от деления', M, 11);
    s += строка(232, 114, 'на 7', M, 11);
    return s;
  },
    полоса_долей: function(з){
    const ч = числаИз(з.q, 3).map(Number).filter(n => !isNaN(n));
    const всего = ч[1] || 8, часть = ч[0] || 3;
    let s = '';
    s += доля(30, 44, 276, 30, Math.min(1, часть / всего), G);
    s += строка(168, 96, `${часть} из ${всего}`, G, 18, true);
    s += строка(168, 122, 'доля = часть : целое', M, 11);
    return s;
  },
    прямая_движения: function(з){
    const ч = числаИз(з.q, 3).map(Number).filter(n => !isNaN(n));
    let s = '';
    s += `<line x1="24" y1="86" x2="312" y2="86" stroke="${LN}" stroke-width="2"/>`;
    (ч.length >= 2 ? ч.slice(0, 3) : [40, 60, 20]).forEach((n, i) => {
      const x = 40 + i * 90;
      s += точка(x, 86, 6, i ? BL : G);
      s += строка(x, 108, `${n} мин`, P, 12, true);
    });
    s += `<path d="M40 62 L130 62" stroke="${G}" stroke-width="1.6" marker-end="url(#taArrow)"/>`;
    s += строка(85, 54, 'встреча', G, 11);
    s += строка(168, 132, 'одновременно, навстречу', M, 11);
    return s;
  },
    треугольник: function(з){
    const ч = числаИз(з.q, 2).map(Number).filter(n => !isNaN(n));
    const угол = ч[0] && ч[0] > 90 && ч[0] < 180 ? ч[0] : 134;
    let s = '';
    s += `<polygon points="80,140 250,140 160,50" fill="rgba(255,215,106,.10)" stroke="${G}" stroke-width="2"/>`;
    s += строка(160, 44, 'A', G, 12, true);
    s += строка(70, 152, 'B', G, 12, true);
    s += строка(258, 152, 'C', G, 12, true);
    s += `<path d="M96 140 A16 16 0 0 0 108 122" fill="none" stroke="${RD}" stroke-width="1.6"/>`;
    s += строка(120, 128, `${угол}°`, RD, 12, true, 'start');
    s += `<line x1="160" y1="50" x2="160" y2="140" stroke="${BL}" stroke-width="1.4" stroke-dasharray="4 3"/>`;
    s += строка(168, 100, 'высота', BL, 11, false, 'start');
    return s;
  },
    окружность: function(з){
    let s = '';
    s += `<circle cx="168" cy="90" r="58" fill="none" stroke="${G}" stroke-width="2"/>`;
    s += точка(168, 90, 3, M);
    s += `<line x1="168" y1="90" x2="218" y2="62" stroke="${BL}" stroke-width="1.6"/>`;
    s += строка(198, 70, 'R', BL, 12, true);
    s += `<line x1="110" y1="90" x2="226" y2="90" stroke="${GR}" stroke-width="1.6"/>`;
    s += строка(168, 84, 'хорда', GR, 11);
    s += `<line x1="168" y1="150" x2="230" y2="132" stroke="${RD}" stroke-width="1.6"/>`;
    s += строка(206, 156, 'касательная', RD, 11);
    return s;
  },
    граф: function(з){
    const имена = словаИз(з.q, 5);
    const узлы = имена.length >= 4 ? имена : ['Аля', 'Беня', 'Веня', 'Геша'];
    const позиции = [[60, 60], [168, 44], [276, 60], [110, 118], [226, 118]];
    let s = '';
    const рёбра = [[0, 1], [1, 2], [0, 3], [2, 4], [3, 4]];
    рёбра.forEach(([a, b]) => {
      if (позиции[a] && позиции[b])
        s += `<line x1="${позиции[a][0]}" y1="${позиции[a][1]}" x2="${позиции[b][0]}" y2="${позиции[b][1]}" stroke="${LN}" stroke-width="1.4"/>`;
    });
    узлы.slice(0, 5).forEach((им, i) => { if (позиции[i]) s += узел(позиции[i][0], позиции[i][1], им.slice(0, 8)); });
    s += строка(168, 156, 'кто кого видит — стрелки в условии', M, 11);
    return s;
  },
    таблица_делимости: function(з){
    const ч = числаИз(з.q, 6).map(Number).filter(n => !isNaN(n));
    const ряд = ч.length >= 3 ? ч.slice(0, 6) : [12, 18, 25, 30, 42, 49];
    let s = '';
    ряд.forEach((n, i) => {
      const x = 22 + (i % 3) * 104, y = 36 + Math.floor(i / 3) * 44;
      const дел = n % 3 === 0;
      s += плашка(x, y, 96, 36, `${n}${дел ? ' · 3' : ''}`, дел ? GR : P, 14,
                  дел ? 'rgba(143,209,168,.14)' : 'rgba(255,255,255,.05)');
    });
    s += строка(168, 140, 'делится — зелёное, нет — обычное', M, 11);
    return s;
  },
    сосуд_долей: function(з){
    const ч = числаИз(з.q, 3).map(Number).filter(n => !isNaN(n));
    const всего = ч[1] || 70, цена = ч[0] || 30;
    let s = '';
    s += `<path d="M120 40 L216 40 L206 140 L130 140 Z" fill="rgba(255,255,255,.05)" stroke="${LN}" stroke-width="1.8"/>`;
    s += `<path d="M126 84 L210 84 L206 140 L130 140 Z" fill="rgba(255,215,106,.28)"/>`;
    s += строка(168, 112, `${всего} л`, P, 14, true);
    s += строка(168, 160, `${цена} % — одна часть`, G, 11);
    return s;
  },
    весы: function(з){
    const ч = числаИз(з.q, 2).map(Number).filter(n => !isNaN(n));
    let s = '';
    s += `<line x1="168" y1="40" x2="168" y2="120" stroke="${LN}" stroke-width="2"/>`;
    s += `<line x1="86" y1="58" x2="250" y2="58" stroke="${G}" stroke-width="2"/>`;
    s += `<path d="M86 58 L62 92 L110 92 Z" fill="rgba(127,209,255,.14)" stroke="${BL}" stroke-width="1.4"/>`;
    s += `<path d="M250 58 L226 92 L274 92 Z" fill="rgba(143,209,168,.14)" stroke="${GR}" stroke-width="1.4"/>`;
    s += строка(86, 86, 'x', BL, 16, true);
    s += строка(250, 86, String(ч[0] || 12), GR, 16, true);
    s += `<line x1="140" y1="120" x2="196" y2="120" stroke="${LN}" stroke-width="2"/>`;
    s += строка(168, 148, 'одно неизвестное — ищем его', M, 11);
    return s;
  },
    дробная_полоса: function(з){
    const ч = числаИз(з.q, 4).map(Number).filter(n => !isNaN(n));
    let s = '';
    s += строка(168, 56, `${ч[0] || 3},${ч[1] || 5}`, G, 26, true);
    s += строка(168, 84, '+', P, 16, true);
    s += строка(168, 112, `${ч[2] || 1},${ч[3] || 4}`, BL, 26, true);
    s += `<line x1="118" y1="132" x2="218" y2="132" stroke="${LN}" stroke-width="2"/>`;
    s += строка(168, 156, 'запятая отделяет десятые', M, 11);
    return s;
  },
    схема_выражения: function(з){
    const ч = числаИз(з.q, 4).map(Number).filter(n => !isNaN(n));
    const ряд = ч.length >= 3 ? ч.slice(0, 4) : [3, 4, 5, 27];
    let s = '';
    ряд.forEach((n, i) => {
      const x = 30 + i * 78;
      s += плашка(x, 52, 56, 44, String(n), P, 18);
      if (i < ряд.length - 1) s += строка(x + 67, 82, i % 2 ? '×' : '+', G, 18, true);
    });
    s += строка(168, 126, 'расставь знаки и скобки', M, 11);
    return s;
  },
    шкала_оценки: function(з){
    let s = '';
    s += `<line x1="30" y1="90" x2="306" y2="90" stroke="${LN}" stroke-width="2"/>`;
    [0, 1, 2, 3, 4].forEach(i => {
      const x = 42 + i * 66;
      s += точка(x, 90, 5, i < 3 ? GR : M);
      s += строка(x, 112, String(i + 1), P, 12, true);
    });
    s += `<rect x="30" y="60" width="132" height="18" rx="9" fill="rgba(143,209,168,.18)" stroke="${GR}" stroke-width="1.2"/>`;
    s += строка(96, 74, 'граница', GR, 11);
    s += строка(168, 142, 'оценка снизу и пример сверху', M, 11);
    return s;
  },
    дерево: function(з){
    const корень = 'все варианты';
    let s = '';
    s += плашка(120, 34, 96, 26, корень, G, 11, 'rgba(255,215,106,.12)');
    [0, 1, 2].forEach(i => {
      const x = 70 + i * 98;
      s += `<line x1="168" y1="60" x2="${x}" y2="88" stroke="${LN}" stroke-width="1.4"/>`;
      s += плашка(x - 34, 88, 68, 24, `ветка ${i + 1}`, P, 11);
      s += `<line x1="${x}" y1="112" x2="${x - 20}" y2="136" stroke="${LN}" stroke-width="1.2"/>`;
      s += `<line x1="${x}" y1="112" x2="${x + 20}" y2="136" stroke="${LN}" stroke-width="1.2"/>`;
      s += плашка(x - 40, 136, 40, 20, '×', M, 10);
      s += плашка(x + 2, 136, 40, 20, '✓', GR, 10);
    });
    return s;
  },
    разбор: function(з){
    const ч = числаИз(з.q, 3);
    let s = '';
    ч.slice(0, 3).forEach((n, i) => {
      const x = 60 + i * 108;
      s += плашка(x - 44, 48, 88, 44, n, P, 18);
    });
    s += строка(168, 118, 'выпиши данные и ищи связь', M, 11);
    return s;
  }
  };
  const КАРТА = {
    '705-1': 'полоса_долей',
    '705-2': 'таблица_выводов',
    '705-3': 'числовой_ряд',
    '705-4': 'весы',
    '705-5': 'фигуры',
    '706-1': 'прямая_движения',
    '706-2': 'треугольник',
    '706-3': 'фигуры',
    '706-4': 'треугольник',
    '706-5': 'шкала_оценки',
    '707-1': 'сосуд_долей',
    '707-2': 'фигуры',
    '707-3': 'полоса_долей',
    '707-4': 'цепочка',
    '707-5': 'треугольник',
    '708-1': 'разбор',
    '708-2': 'разбор',
    '708-3': 'разбор',
    '709-1': 'полоса_долей',
    '709-2': 'граф',
    '709-3': 'разбор',
    '710-1': 'разбор',
    '710-2': 'разбор',
    '710-3': 'разбор',
    '710-4': 'разбор',
    '710-5': 'дерево',
    '711-1': 'разбор',
    '711-2': 'разбор',
    '711-3': 'разбор',
    '711-4': 'разбор',
    '711-5': 'граф',
    '712-1': 'разбор',
    '712-2': 'разбор',
    '712-3': 'разбор',
    '712-4': 'разбор',
    '712-5': 'граф',
    '713-1': 'разбор',
    '713-2': 'разбор',
    '713-3': 'разбор',
    '713-4': 'разбор',
    '713-5': 'граф',
    '713-6': 'разбор',
    '714-1': 'разбор',
    '714-2': 'разбор',
    '715-1': 'разбор',
    '715-2': 'разбор',
    '715-3': 'фигуры',
    '716-1': 'разбор',
    '716-2': 'фигуры',
    '716-3': 'фигуры',
    '717-1': 'фигуры',
    '717-2': 'разбор',
    '718-1': 'фигуры',
    '718-2': 'разбор',
    '719-1': 'разбор',
    '719-2': 'фигуры',
    '719-3': 'таблица_делимости',
    '719-4': 'таблица_выводов',
    '720-1': 'разбор',
    '720-2': 'прямая_движения',
    '720-3': 'разбор',
    '720-4': 'прямая_движения',
    '721-1': 'полоса_долей',
    '721-2': 'разбор',
    '721-3': 'разбор',
    '722-1': 'разбор',
    '722-2': 'прямая_движения',
    '722-3': 'весы',
    '722-4': 'прямая_движения',
    '723-1': 'разбор',
    '723-2': 'разбор',
    '723-3': 'разбор',
    '724-1': 'весы',
    '724-2': 'разбор',
    '725-1': 'весы',
    '725-2': 'разбор',
    '725-3': 'сосуд_долей',
    '725-4': 'весы',
    '725-5': 'разбор'
  };
  /* Рисунок задания. Полоса в сцене — под подписями (тема, баллы), поэтому
     рисунок вписывается в прямоугольник 336 x 118 со сдвигом вниз: иначе он
     налезает на тему задания и на подпись баллов. */
  const ПОЛОСА_X = 0, ПОЛОСА_Y = 122, ПОЛОСА_W = 336, ПОЛОСА_H = 128;
  window.TOUR_ART = function(id, n, з, полоса){
    try{
      const вид = КАРТА[id + '-' + n] || 'разбор';
      const фн = ВИД[вид] || ВИД.разбор;
      const внутри = фн(з || {});
      const x = полоса ? полоса.x : ПОЛОСА_X,
            y = полоса ? полоса.y : ПОЛОСА_Y,
            w = полоса ? полоса.w : ПОЛОСА_W,
            h = полоса ? полоса.h : ПОЛОСА_H;
      const м = Math.min(w / 336, h / 168);
      const tx = x + (w - 336 * м) / 2, ty = y + (h - 168 * м) / 2;
      return '<svg viewBox="0 0 ' + w + ' ' + h + '" preserveAspectRatio="xMidYMid meet" class="c2-scene"' +
        ' style="display:block;width:100%;height:auto">' +
        '<defs><marker id="taArrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">' +
        '<path d="M0 0 L10 5 L0 10 z" fill="rgba(255,255,255,.5)"/></marker></defs>' +
        /* Порядок важен: сначала масштаб, потом перенос — иначе перенос
           умножается на масштаб и рисунок уезжает вниз за кадр. */
        '<g transform="scale(' + м.toFixed(3) + ') translate(' + tx.toFixed(1) + ',' + ty.toFixed(1) + ')">' +
        '<rect x="0" y="0" width="336" height="168" rx="10" fill="rgba(255,255,255,.03)"/>' + внутри + '</g></svg>';
    }catch(e){ return ''; }
  };
})();
