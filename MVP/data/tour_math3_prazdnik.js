/* ============ ТУР 701 · МАТЕМАТИКА · 3 КЛАСС · «ПРАЗДНИК В СТАРОМ ДВОРИКЕ» ============

   Это ТУР, а не урок. Тур — работа на баллы и время: у него свой сюжет, своя
   подача и разбор каждого задания. Поэтому он живёт отдельным модулем, а не
   карточкой с текстом: у каждого шага свой рисунок, своя анимация и своё
   действие руками.

   СЮЖЕТ. Старый дворик в Сиракузах готовится к празднику. Пятеро ребят
   развешивают гирлянду, раскладывают игрушки на ковре и считают, кто есть кто.
   Архимед проходит по дворику и на каждом шаге оставляет одну задачу. Пять
   задач — пять дел во дворике.

   УСТРОЙСТВО ШАГА. Каждый шаг — свой экран, и на каждом есть:
     • теория   — приём, который нужен именно для этой задачи, коротко;
     • рисунок  — своя сцена, анимация появляется вместе с шагом;
     • интерактив — руками: соединить, убрать, разложить, отметить;
     • разбор   — подробно, почему так, и какая ошибка тут частая.

   ВОСЕМЬ ЧАСТЕЙ (20 шагов):
     1. дворик        — знакомство с сюжетом
     2. корзина       — цепочка «одинаковое — разное»
     3. гирлянда      — ровный ряд и шаг
     4. ковёр         — перебор двух ходов
     5. Саша Ким      — вывод по ограничениям
     6. хоровод       — истина и ложь
     7. протокол      — итог тура

   АНИМАЦИЯ. У каждого шага своя: фонарики зажигаются по очереди, лампочка
   гаснет, фигура уезжает с ковра, строчки вывода печатаются. Всё уважает
   `prefers-reduced-motion`: при нём анимации выключаются.
   ============================================================================ */
(function(){
  'use strict';

  const ID = 701;
  const ДОМ = 't701';

  /* ── палитра проекта ── */
  const ЗОЛОТО = '#ffd76a', ЛАМПА = '#ffe9a8', ПЕРО = '#f4e9c8', ТИХО = '#8fa08f',
        ЗЕЛЁН = '#8fd1a8', КРАСН = '#e86a5a', СИНЬ = '#7fd1ff', НОЧЬ = '#132a24',
        ТЬМА = '#0e1f1a', ЛИНИЯ = '#3f7a5f';

  /* ── данные тура: пять заданий, как в официальном комплекте ── */
  const ЗАДАЧИ = [
    { id:'tour19-math3-1', балл:2, тема:'Логика · цепочка «одинаковое — разное»',
      название:'Корзина фруктов',
      условие:'Алиса и Боря любят разные фрукты, Боря и Вася — разные, Вася и Гена — разные, Дима и Гена — разные.',
      вопрос:'Кто любит яблоки, а кто апельсины? Соедини каждого с его фруктом.',
      ответ:{ 'Алиса':'яблоки', 'Боря':'апельсины', 'Вася':'яблоки', 'Гена':'апельсины', 'Дима':'яблоки' },
      разбор:'В цепочке Алиса — Боря — Вася — Гена — Дима каждые соседи любят разное. Значит вкусы чередуются: Алиса, Вася и Дима — яблоки, Боря и Гена — апельсины. Достаточно поставить первый фрукт: дальше каждый следующий обязан быть другим.' },

    { id:'tour19-math3-2', балл:2, тема:'Числа · ровный ряд и его шаг',
      название:'Ровный ряд на гирлянде',
      условие:'На гирлянде шесть лампочек с числами 11, 7, 23, 13, 19, 15. Одна лампочка перегорела, её число надо убрать.',
      вопрос:'Какое число убрать, чтобы из оставшихся пяти получился ровный ряд?',
      варианты:['11','7','23','13','19','15'], верный:3,
      разбор:'По возрастанию: 7, 11, 13, 15, 19, 23. Без 13 остаётся 7, 11, 15, 19, 23 — разницы 4, 4, 4, 4. Это ровный ряд с шагом 4. Без любого другого числа одинакового шага не выйдет: без 7 получится 11, 13, 15, 19, 23 — разницы 2, 2, 4, 4.' },

    { id:'tour19-math3-3', балл:2, тема:'Комбинаторика · перебор двух ходов',
      название:'Игрушки на ковре',
      условие:'На ковре в ряд лежат зелёный треугольник, белый квадрат, синий круг, синий квадрат и красный треугольник. Петя унёс две соседние фигуры, потом Лиза унесла две фигуры одинаковой формы. Осталась одна фигура.',
      вопрос:'Какой формы фигура осталась на ковре?',
      варианты:['Треугольник','Круг','Квадрат'], верный:2,
      разбор:'Лиза могла взять либо два треугольника (1-я и 5-я фигуры), либо два квадрата (2-я и 4-я). Если бы она взяла квадраты, Пете остались бы фигуры 1, 3 и 5 — треугольник, круг и треугольник, а среди них нет двух соседних. Значит Лиза взяла треугольники, а Петя — либо 2 и 3, либо 3 и 4. В обоих случаях остаётся квадрат.' },

    { id:'tour19-math3-4', балл:2, тема:'Логика · вывод по ограничениям',
      название:'Новый знакомый Саша Ким',
      условие:'В семье Петровых дети Валя, Костя и Женя; в семье Сидоровых тоже Валя, Костя и Женя. Если Саша Ким придёт к Петровым, соберутся три девочки и один мальчик, а если к Сидоровым — три мальчика и одна девочка. Оба Кости — мальчики.',
      вопрос:'(а) Какого пола Саша Ким? (б) Сколько всего мальчиков и сколько девочек?',
      поля:[{к:'Саша Ким', эталон:'девочка'},{к:'мальчиков', эталон:4},{к:'девочек', эталон:3}],
      разбор:'У Петровых мальчик ровно один — и это Костя Петров, ведь оба Кости мальчики. Значит Валя Петрова, Женя Петрова и Саша — девочки. У Сидоровых девочка одна, и это Саша, потому Валя Сидорова и Женя Сидоров — мальчики. Всего: Костя Петров, Валя Сидоров, Женя Сидоров и Костя Сидоров — четыре мальчика, а девочек три.' },

    { id:'tour19-math3-5', балл:2, тема:'Логика · истина и ложь в высказываниях',
      название:'Хоровод гномов и эльфов',
      условие:'В хоровод встали гномы и эльфы. Своему сородичу они всегда говорят неправду, а существу другой расы — только правду. Каждый повернулся к соседу слева и сказал одну фразу: либо «Ты эльф», либо «Ты гном». Всего прозвучало 7 фраз «Ты эльф» и 9 фраз «Ты гном».',
      вопрос:'Сколько гномов и сколько эльфов было в хороводе?',
      поля:[{к:'гномов', эталон:7},{к:'эльфов', эталон:9}],
      разбор:'У эльфа выходит «Ты гном» всегда: перед ним эльф — он врёт, перед ним гном — говорит правду. У гнома всегда выходит «Ты эльф»: перед ним гном — он врёт, перед ним эльф — говорит правду. Значит каждая фраза «Ты эльф» — слова одного гнома, а «Ты гном» — слова одного эльфа. Фраз «Ты эльф» было 7, поэтому гномов 7; фраз «Ты гном» было 9, поэтому эльфов 9.' },

  ];

  /* Кто есть кто в задаче про Сашу Ким: подписи слева, справа верный пол.
     Держим рядом, чтобы рисунок, разбор и балл считали по одному списку. */
  const ЛЮДИ = ['Костя Петров','Валя Петрова','Женя Петрова','Саша Ким','Костя Сидоров','Валя Сидоров','Женя Сидоров'];
  const ПОЛ = ['мальчик','девочка','девочка','девочка','мальчик','мальчик','мальчик'];

  /* ── состояние: ответы и открытые подсказки ── */
  function память(){
    const к = (typeof lidKey === 'function') ? lidKey(ID) : String(ID);
    if(typeof CHS === 'undefined') window.CHS = {};
    if(!CHS[к]) CHS[к] = {};
    const п = CHS[к];
    if(!п.ответы) п.ответы = {};
    if(п.корзина == null) п.корзина = 0;      /* кого сейчас оделяем фруктом */
    if(п.полКто == null) п.полКто = 0;        /* о ком сейчас спрашиваем пол */
    if(!п.шаги) п.шаги = {};
    if(!п.баллы) п.баллы = 0;
    return п;
  }
  const перерисовать = () => { if(typeof chRender === 'function') chRender(0); };

  /* Балл тура: считается по ответам — и в рисунке протокола, и в счёте цифры
     после отрисовки. Одна функция, чтобы значения не разъехались. */
  function баллТура(){
    const п=память();
    const люди=['Костя Петров','Валя Петрова','Женя Петрова','Саша Ким','Костя Сидоров','Валя Сидоров','Женя Сидоров'];
    const прав=['мальчик','девочка','девочка','девочка','мальчик','мальчик','мальчик'];
    let б=0;
    const в=ЗАДАЧИ[0].ответ, о0=п.ответы[0]||{};
    if(Object.keys(в).every(к=>о0[к]===в[к])) б+=2;
    if(п.ответы[1]===ЗАДАЧИ[1].верный) б+=2;
    if(п.ответы[2]===ЗАДАЧИ[2].верный) б+=2;
    const о3=п.ответы[3]||{};
    if(люди.every((им,j)=>о3[им]===прав[j])) б+=2;
    const о4=п.ответы[4]||{};
    if(о4.гномов===7 && о4.эльфов===9) б+=2;
    return б;
  }

  /* ── мелкие построители разметки ── */
  const esc = s => String(s == null ? '' : s)
    .replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');

  const svg = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||150}" preserveAspectRatio="xMidYMid meet"
       style="display:block;width:100%;height:auto" aria-hidden="true">
       <defs>
         <marker id="${ДОМ}-стрелка" viewBox="0 0 10 10" refX="8" refY="5"
                 markerWidth="6" markerHeight="6" orient="auto">
           <path d="M0 0 L10 5 L0 10 z" fill="rgba(255,255,255,.45)"/>
         </marker>
       </defs>${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||12}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ПЕРО}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  /* ══════════════ БИБЛИОТЕКА ДЕТАЛЕЙ ══════════════
     Рисуем предметы, а не геометрические фигуры: дом с крышей, трубой, дверью
     и окном; фонарь на крюке; ребёнок с головой, туловищем, руками и ногами;
     яблоко с листиком; апельсин с дольками; корзина с плетением. Кружки и
     прямоугольники как «обозначение чего угодно» стандарт запрещает
     (раздел 9: одна и та же форма для разного содержания). */

  /* Домик: стена, крыша со скатом, труба, дверь, окно со рамами.
     свет — 0…1, насколько ярко горит окно. */
  const дом = (x,y,ш,в,свет) => {
    const кр = ш*0.60, яр = Math.max(0, Math.min(1, свет==null?0:свет));
    let черепица = '';
    for(let r=0;r<4;r++){
      const t=(r+1)/5;
      черепица += `<path d="M${x-4*(1-t)} ${y-кр*t} L${x+ш+4*(1-t)} ${y-кр*t}"
        stroke="#5c3d18" stroke-width="0.7" opacity=".45"/>`;
    }
    return `
      <g>
        ${/* мягкая тень под домом */''}
        <ellipse cx="${x+ш/2}" cy="${y+в+1}" rx="${ш*0.62}" ry="5"
          fill="rgba(0,0,0,.45)" filter="url(#${ДОМ}-мягко)"/>
        ${/* стена с градиентом и зерном */''}
        <rect x="${x}" y="${y}" width="${ш}" height="${в}" fill="url(#${ДОМ}-стена)"
          stroke="#2a1f14" stroke-width="1.6"/>
        <rect x="${x}" y="${y}" width="${ш}" height="${в}" fill="#000" opacity=".12"
          filter="url(#${ДОМ}-зерно)"/>
        ${/* крыша с черепицей */''}
        <path d="M${x-5} ${y} L${x+ш/2} ${y-кр} L${x+ш+5} ${y} Z"
          fill="url(#${ДОМ}-крыша)" stroke="#3a2a12" stroke-width="1.6"/>
        ${черепица}
        <path d="M${x-5} ${y} L${x+ш+5} ${y}" stroke="#3a2a12" stroke-width="2"/>
        ${/* труба и дым */''}
        <rect x="${x+ш-16}" y="${y-кр+4}" width="8" height="${кр*0.4}"
          fill="url(#${ДОМ}-стена)" stroke="#2a1f14" stroke-width="1.1"/>
        <g opacity=".35">
          <circle cx="${x+ш-12}" cy="${y-кр-4}" r="3" fill="#cfd6dd">
            <animate attributeName="cy" values="${y-кр-4};${y-кр-16}" dur="4.5s" repeatCount="indefinite"/>
            <animate attributeName="opacity" values=".5;0" dur="4.5s" repeatCount="indefinite"/>
          </circle>
          <circle cx="${x+ш-6}" cy="${y-кр-2}" r="2.4" fill="#cfd6dd">
            <animate attributeName="cy" values="${y-кр-2};${y-кр-18}" dur="5.4s" begin="1.2s" repeatCount="indefinite"/>
            <animate attributeName="opacity" values=".4;0" dur="5.4s" begin="1.2s" repeatCount="indefinite"/>
          </circle>
        </g>
        ${/* дверь с филёнкой и ручкой */''}
        <rect x="${x+8}" y="${y+в-23}" width="16" height="23" rx="2"
          fill="#4a3218" stroke="#2a1f14" stroke-width="1.4"/>
        <rect x="${x+11}" y="${y+в-20}" width="10" height="17" rx="1.5" fill="none"
          stroke="#7a5528" stroke-width="0.9"/>
        <circle cx="${x+20.5}" cy="${y+в-11}" r="1.6" fill="#e8c46a"/>
        ${/* окно: свечение, стекло, переплёт */''}
        <circle cx="${x+ш-22}" cy="${y+19}" r="20" fill="url(#${ДОМ}-свет)" opacity="${(0.35+0.6*яр).toFixed(2)}"/>
        <rect x="${x+ш-35}" y="${y+9}" width="26" height="20" rx="2"
          fill="${яр>0.05?'url(#'+ДОМ+'-окно)':'#1a2b25'}" stroke="#2a1f14" stroke-width="1.6"
          opacity="${(0.35+0.65*яр).toFixed(2)}"/>
        <path d="M${x+ш-22} ${y+9} L${x+ш-22} ${y+29} M${x+ш-35} ${y+19} L${x+ш-9} ${y+19}"
          stroke="#3a2a12" stroke-width="1.4"/>
        ${/* конус света из окна на землю */''}
        ${яр>0.2?`<path d="M${x+ш-35} ${y+29} L${x+ш-9} ${y+29} L${x+ш+6} ${y+в+16} L${x+ш-52} ${y+в+16} Z"
          fill="url(#${ДОМ}-свет)" opacity="${(0.18+0.32*яр).toFixed(2)}"/>`:''}
      </g>`;
  };

  /* Фонарь на гирлянде: крюк, колба, свечение. */
  const фонарь = (x,y,яркость) => {
    const яр = Math.max(0, Math.min(1, яркость==null?1:яркость));
    return `
      <g>
        <path d="M${x} ${y-9} L${x} ${y-5}" stroke="#2a1f14" stroke-width="1.2"/>
        <path d="M${x-5.5} ${y-5} L${x+5.5} ${y-5} L${x+4} ${y-7.5} L${x-4} ${y-7.5} Z"
          fill="#8a6a3a" stroke="#4a3218" stroke-width="1"/>
        <path d="M${x-5} ${y-5} L${x-3.4} ${y+8} L${x+3.4} ${y+8} L${x+5} ${y-5} Z"
          fill="url(#${ДОМ}-стекло)" opacity="${(0.35+0.65*яр).toFixed(2)}" stroke="#4a3218" stroke-width="1.1"/>
        <path d="M${x-1.6} ${y+1} L${x+1.6} ${y+1} M${x-1.2} ${y+3.4} L${x+1.2} ${y+3.4}"
          stroke="#8a5a18" stroke-width="0.9" opacity="${яр.toFixed(2)}"/>
        <path d="M${x-3.4} ${y+8} L${x-1.8} ${y+10} L${x+1.8} ${y+10} L${x+3.4} ${y+8} Z"
          fill="#8a6a3a" stroke="#4a3218" stroke-width="0.9"/>
        ${яр>0.4?`
          <circle cx="${x}" cy="${y+2}" r="10" fill="url(#${ДОМ}-свет)" opacity=".9">
            <animate attributeName="r" values="8;14;8" dur="2.6s" repeatCount="indefinite"/>
          </circle>
          <ellipse cx="${x}" cy="${y+26}" rx="14" ry="5" fill="url(#${ДОМ}-свет)" opacity=".35">
            <animate attributeName="opacity" values=".2;.45;.2" dur="2.6s" repeatCount="indefinite"/>
          </ellipse>`:''}
      </g>`;
  };

  /* Ребёнок: голова, туловище с руками, ноги. плащ — цвет. */
  const ребёнок = (x,y,цвет,рост) => {
    const р = рост||15, г = р*0.28;
    const голова = { x:x, y:y-р+г };
    return `
      <g>
        <ellipse cx="${x}" cy="${y+1.5}" rx="${р*0.34}" ry="${р*0.10}"
          fill="rgba(0,0,0,.45)" filter="url(#${ДОМ}-мягко2)"/>
        ${/* ноги */''}
        <path d="M${x-р*0.16} ${y} L${x-р*0.05} ${y-р*0.34}" stroke="#3a2c22" stroke-width="${(р*0.13).toFixed(1)}"
          stroke-linecap="round"/>
        <path d="M${x+р*0.16} ${y} L${x+р*0.05} ${y-р*0.34}" stroke="#3a2c22" stroke-width="${(р*0.13).toFixed(1)}"
          stroke-linecap="round"/>
        ${/* туловище-плащ с градиентом */''}
        <path d="M${x-р*0.30} ${y-р*0.34} C${x-р*0.34} ${y-р*0.62} ${x+р*0.34} ${y-р*0.62} ${x+р*0.30} ${y-р*0.34} Z"
          fill="${цвет}" opacity=".85" stroke="rgba(0,0,0,.35)" stroke-width="1"/>
        ${/* руки */''}
        <path d="M${x-р*0.28} ${y-р*0.52} L${x-р*0.44} ${y-р*0.30}" stroke="${цвет}" stroke-width="${(р*0.11).toFixed(1)}"
          stroke-linecap="round" opacity=".9"/>
        <path d="M${x+р*0.28} ${y-р*0.52} L${x+р*0.44} ${y-р*0.30}" stroke="${цвет}" stroke-width="${(р*0.11).toFixed(1)}"
          stroke-linecap="round" opacity=".9"/>
        ${/* шея */''}
        <path d="M${x} ${y-р*0.62} L${x} ${y-р*0.70}" stroke="#e0b48f" stroke-width="${(р*0.09).toFixed(1)}"/>
        ${/* голова: лицо с подсветкой сверху, волосы */''}
        <circle cx="${голова.x}" cy="${голова.y}" r="${г.toFixed(1)}" fill="#e8c09a"
          stroke="rgba(0,0,0,.35)" stroke-width="1"/>
        <path d="M${голова.x-г} ${голова.y-г*0.15} A${г} ${г} 0 0 1 ${голова.x+г} ${голова.y-г*0.15}
          L${голова.x+г*0.9} ${голова.y-г*0.6} L${голова.x-г*0.9} ${голова.y-г*0.6} Z"
          fill="#4a3524" opacity=".92"/>
        <circle cx="${голова.x-г*0.35}" cy="${голова.y+г*0.1}" r="${(г*0.13).toFixed(1)}" fill="#2b2118"/>
        <circle cx="${голова.x+г*0.35}" cy="${голова.y+г*0.1}" r="${(г*0.13).toFixed(1)}" fill="#2b2118"/>
        <path d="M${голова.x-г*0.3} ${голова.y+г*0.5} Q${голова.x} ${голова.y+г*0.72} ${голова.x+г*0.3} ${голова.y+г*0.5}"
          fill="none" stroke="#8f5a44" stroke-width="1.1" stroke-linecap="round"/>
      </g>`;
  };

  /* Яблоко: тело, ямка, листик, черешок. */
  const яблоко = (x,y,м) => {
    const к = м||1;
    return `
      <g transform="translate(${x} ${y}) scale(${к})">
        <path d="M0 -3 C-6 -8 -11 -3 -10 2 C-9 8 -4 11 0 9 C4 11 9 8 10 2 C11 -3 6 -8 0 -3 Z"
          fill="${КРАСН}" opacity=".85" stroke="#8f2f22" stroke-width="1.1"/>
        <path d="M0 -3 C-2 -1 -2 1 0 2" stroke="#8f2f22" stroke-width="0.9" fill="none" opacity=".6"/>
        <path d="M0 -5 L1.6 -9" stroke="#7a4a24" stroke-width="1.3" stroke-linecap="round"/>
        <path d="M1 -8 C5 -11 8 -9 7 -6 C5 -5 2 -5 1 -8 Z" fill="${ЗЕЛЁН}" opacity=".9"/>
      </g>`;
  };

  /* Апельсин: тело, дольки, листик. */
  const апельсин = (x,y,м) => {
    const к = м||1;
    return `
      <g transform="translate(${x} ${y}) scale(${к})">
        <circle r="9.5" fill="#e08a2e" stroke="#a35f16" stroke-width="1.1"/>
        <path d="M0 -9.5 L0 9.5 M-9.5 0 L9.5 0 M-6.8 -6.8 L6.8 6.8 M6.8 -6.8 L-6.8 6.8"
          stroke="#a35f16" stroke-width="0.7" opacity=".55"/>
        <circle r="2.6" fill="#f2b25e" stroke="#a35f16" stroke-width="0.8"/>
        <path d="M0 -9.5 C4 -13 8 -11 6 -8 C4 -7 1 -7.5 0 -9.5 Z" fill="${ЗЕЛЁН}" opacity=".9"/>
      </g>`;
  };

  /* Корзина с плетением. */
  const корзина = (x,y,ш,в) => {
    const полуш = ш/2;
    let плетение = '';
    for(let i=1;i<4;i++){
      const yy = y + (в*i/4);
      плетение += `<path d="M${x-полуш+2+i*0.6} ${yy} L${x+полуш-2-i*0.6} ${yy}"
        stroke="#7a4a24" stroke-width="0.9" opacity=".55"/>`;
    }
    return `
      <g>
        <path d="M${x-полуш} ${y} L${x+полуш} ${y} L${x+полуш-4} ${y+в} L${x-полуш+4} ${y+в} Z"
          fill="#a9713a" stroke="#6f4420" stroke-width="1.3"/>
        ${плетение}
        <path d="M${x-полуш-1} ${y} L${x+полуш+1} ${y}" stroke="#6f4420" stroke-width="2"/>
        <path d="M${x-полуш*0.6} ${y} C${x-полуш*0.5} ${y-8} ${x+полуш*0.5} ${y-8} ${x+полуш*0.6} ${y}"
          fill="none" stroke="#6f4420" stroke-width="1.4"/>
      </g>`;
  };

  /* Лампочка на гирлянде с цоколем и спиралью. */
  const лампочка = (x,y,горит) => {
    const вкл = горит!==false;
    return `
      <g>
        <path d="M${x} ${y-9} L${x} ${y-5}" stroke="${ЛИНИЯ}" stroke-width="1.2"/>
        <rect x="${x-3}" y="${y-5.4}" width="6" height="3.8" rx="1" fill="#9aa0a8" stroke="${ЛИНИЯ}" stroke-width="1.1"/>
        <path d="M${x-5} ${y-1.6} C${x-6.6} ${y+5} ${x+6.6} ${y+5} ${x+5} ${y-1.6} Z"
          fill="${вкл?'rgba(255,232,160,.92)':'rgba(255,255,255,.08)'}" stroke="${вкл?ЗОЛОТО:ЛИНИЯ}" stroke-width="1.2"/>
        ${вкл?`<path d="M${x-1.6} ${y+1.6} L${x+1.6} ${y+1.6} M${x-1.2} ${y+3.2} L${x+1.2} ${y+3.2}"
          stroke="#a3701a" stroke-width="0.9"/>`:''}
      </g>`;
  };

  /* Плитка-фигура: треугольник, квадрат, круг — с обводкой и тенью. */
  const фигура = (x,y,вид,цвет,выделена) => {
    const о = выделена?3:1.6;
    const з = выделена?'rgba(255,255,255,.14)':'rgba(255,255,255,.06)';
    if(вид==='треугольник')
      return `<g><polygon points="${x},${y-13} ${x+13},${y+9} ${x-13},${y+9}"
        fill="${з}" stroke="${цвет}" stroke-width="${о}"/></g>`;
    if(вид==='квадрат')
      return `<g><rect x="${x-11}" y="${y-11}" width="22" height="22" rx="3"
        fill="${з}" stroke="${цвет}" stroke-width="${о}"/></g>`;
    return `<g><circle cx="${x}" cy="${y}" r="12" fill="${з}" stroke="${цвет}" stroke-width="${о}"/></g>`;
  };

  /* Ковёр: бахрома и узор полосами. */
  const ковёр = (x,y,ш,в) => {
    let бахрома='';
    for(let i=0;i<Math.floor(ш/7);i++){
      const xx=x+4+i*7;
      бахрома += `<path d="M${xx} ${y+в} L${xx} ${y+в+5}" stroke="#8a5f3a" stroke-width="1"/>`;
      бахрома += `<path d="M${xx} ${y} L${xx} ${y-5}" stroke="#8a5f3a" stroke-width="1"/>`;
    }
    return `
      <g>
        <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="4" fill="#6b3f2a" stroke="#4c2b1c" stroke-width="1.6"/>
        <rect x="${x+5}" y="${y+5}" width="${ш-10}" height="${в-10}" rx="3" fill="none"
          stroke="#c98a3a" stroke-width="1.4" opacity=".8"/>
        <path d="M${x+12} ${y+в/2} L${x+ш-12} ${y+в/2}" stroke="#c98a3a" stroke-width="1" opacity=".5"
          stroke-dasharray="5 4"/>
        <path d="M${x+ш*0.34} ${y+9} L${x+ш*0.5} ${y+в-9} L${x+ш*0.66} ${y+9}" fill="none"
          stroke="#c98a3a" stroke-width="1.1" opacity=".55"/>
        ${бахрома}
      </g>`;
  };

  /* Жетон с числом: не круг «вообще», а табличка с рамкой и уголками. */
  const жетон = (x,y,текст,цвет,размер,залит) => {
    const ш=(размер||16)*1.9, в=(размер||16)*1.25;
    return `
      <g>
        <rect x="${x-ш/2}" y="${y-в/2}" width="${ш}" height="${в}" rx="5"
          fill="${залит||'rgba(255,255,255,.06)'}" stroke="${цвет||ЛИНИЯ}" stroke-width="1.5"/>
        <path d="M${x-ш/2+3} ${y-в/2+1} L${x-ш/2+1} ${y-в/2+3}" stroke="${цвет||ЛИНИЯ}" stroke-width="1.2"/>
        <text x="${x}" y="${y+(размер||26)*0.36}" text-anchor="middle" font-size="${размер||26}"
          font-weight="bold" fill="${цвет||ПЕРО}" font-family="Georgia,serif"
          style="font-variant-numeric:tabular-nums">${esc(текст)}</text>
      </g>`;
  };

  /* ══════════════ РЕНДЕР: СВЕТ, ГРАДИЕНТЫ, ТЕНИ, ТЕКСТУРА ══════════════
     Рисуем своим вектором, но так, чтобы выглядело объёмно: у каждого
     предмета градиентная заливка (свет сверху, тень снизу), мягкие тени
     через размытие, световые пятна и конусы от источников, зерно дерева и
     камня через feTurbulence. Это не фото, но и не плоские контуры. */

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="${ДОМ}-небо" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#0a1622"/>
        <stop offset="0.55" stop-color="#12263a"/>
        <stop offset="1" stop-color="#1d3a44"/>
      </linearGradient>
      <radialGradient id="${ДОМ}-луна" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#fff6d8" stop-opacity="1"/>
        <stop offset="0.35" stop-color="#ffe9a8" stop-opacity="0.55"/>
        <stop offset="1" stop-color="#ffe9a8" stop-opacity="0"/>
      </radialGradient>
      <linearGradient id="${ДОМ}-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f0e2c4" stop-opacity="0.34"/>
        <stop offset="0.6" stop-color="#c9b591" stop-opacity="0.22"/>
        <stop offset="1" stop-color="#8a7a5c" stop-opacity="0.18"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-крыша" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#e8b45c"/>
        <stop offset="0.5" stop-color="#b8813a"/>
        <stop offset="1" stop-color="#7a5222"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-окно" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#fff2c4"/>
        <stop offset="1" stop-color="#e0a84a"/>
      </linearGradient>
      <radialGradient id="${ДОМ}-свет" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#ffdf95" stop-opacity="0.55"/>
        <stop offset="1" stop-color="#ffdf95" stop-opacity="0"/>
      </radialGradient>
      <linearGradient id="${ДОМ}-земля" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#243a30"/>
        <stop offset="1" stop-color="#0d1a15"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-стекло" x1="0" y1="0" x2="0.4" y2="1">
        <stop offset="0" stop-color="#fff6d0" stop-opacity="0.95"/>
        <stop offset="0.5" stop-color="#f0c76a" stop-opacity="0.85"/>
        <stop offset="1" stop-color="#a9762a" stop-opacity="0.9"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-яблоко" x1="0.2" y1="0" x2="0.8" y2="1">
        <stop offset="0" stop-color="#e8664f"/>
        <stop offset="0.45" stop-color="#c0392b"/>
        <stop offset="1" stop-color="#8f2f22"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-апельсин" x1="0.2" y1="0" x2="0.8" y2="1">
        <stop offset="0" stop-color="#f7b45c"/>
        <stop offset="0.45" stop-color="#e08a2e"/>
        <stop offset="1" stop-color="#a35f16"/>
      </linearGradient>
      <linearGradient id="${ДОМ}-плетение" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c98a45"/>
        <stop offset="1" stop-color="#7a4a24"/>
      </linearGradient>
      <filter id="${ДОМ}-мягко" x="-40%" y="-40%" width="180%" height="180%">
        <feGaussianBlur stdDeviation="5"/>
      </filter>
      <filter id="${ДОМ}-мягко2" x="-40%" y="-40%" width="180%" height="180%">
        <feGaussianBlur stdDeviation="2.2"/>
      </filter>
      <filter id="${ДОМ}-зерно">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="3" result="шум"/>
        <feColorMatrix in="шум" type="saturate" values="0"/>
        <feComponentTransfer><feFuncA type="linear" slope="0.16"/></feComponentTransfer>
        <feComposite operator="in" in2="SourceGraphic"/>
      </filter>
      <filter id="${ДОМ}-дерево">
        <feTurbulence type="fractalNoise" baseFrequency="0.02 0.6" numOctaves="4" result="в"/>
        <feColorMatrix in="в" type="saturate" values="0"/>
        <feComponentTransfer><feFuncA type="linear" slope="0.22"/></feComponentTransfer>
        <feComposite operator="in" in2="SourceGraphic"/>
      </filter>
    </defs>`;

  /* ── анимации: у каждой своя работа ─────────────────────────────────
     Правило стандарта — движение показывает смысл. Линия рисуется, когда
     показываем связь; число подкручивается, когда считаем; улика появляется,
     когда её добавляем. Текст не масштабируется нигде. */

  /* Линия, которая прорисовывается от начала к концу. */
  const линияРисуется = (x1,y1,x2,y2,цвет,толщ,задержка) => {
    const д = Math.round(Math.hypot(x2-x1, y2-y1));
    return `<line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${цвет||ЛИНИЯ}"
      stroke-width="${толщ||1.6}" stroke-linecap="round" stroke-dasharray="${д}" stroke-dashoffset="${д}">
      <animate attributeName="stroke-dashoffset" from="${д}" to="0"
        dur="420ms" begin="${(задержка||0).toFixed(2)}s" fill="freeze"
        calcMode="spline" keySplines="0.23 1 0.32 1" keyTimes="0;1"/></line>`;
  };

  /* Счёт числа: в SVG текстом управлять нельзя, поэтому считаем в DOM-узле.
     Вызывается после отрисовки там, где есть счёт (балл в протоколе). */
  function счётДо(ид, до, мс){
    try{
      const el=document.getElementById(ид); if(!el) return;
      const t0=performance.now(), длит=мс||700;
      const шаг=()=>{
        const k=Math.min(1,(performance.now()-t0)/длит);
        /* ease-out: быстрый старт, мягкое окончание */
        const e=1-Math.pow(1-k,3);
        el.textContent=Math.round(до*e);
        if(k<1) requestAnimationFrame(шаг); else el.textContent=до;
      };
      requestAnimationFrame(шаг);
    }catch(e){}
  }

  /* Плавное появление с подъёмом — для улик, подписей, итогов. */
  /* Раньше каждый элемент входил со своей задержкой. Это ломалось: движок
     перерисовывает кадр при ответе и подсказках, анимации запускались заново,
     и сцена на миг (а то и надолго) оставалась пустой. Теперь сцена появляется
     целиком одним плавным входом, а смысловые анимации (свет, качание,
     указатель, счёт) живут отдельно и не сбрасываются. */
  const появляется = () => '';

  /* Мерцание — только для того, что ждёт действия. */
  const ждётМерцает = (x,y,w,h,цвет) =>
    `<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="12" fill="none"
      stroke="${цвет||ЗОЛОТО}" stroke-width="2" opacity=".45">
      <animate attributeName="opacity" values=".12;.55;.12" dur="1.7s" repeatCount="indefinite"/>
    </rect>`;

  /* Подсветка верного ответа: мягкое свечение рамки, без масштабирования
     текста (правило 1 стандарта — текст анимацией не масштабировать). */
  const свечение = (x,y,w,h,цвет) =>
    `<rect x="${x-2}" y="${y-2}" width="${w+4}" height="${h+4}" rx="12" fill="none"
      stroke="${цвет}" stroke-width="2" opacity=".5">
      <animate attributeName="opacity" values=".2;.6;.2" dur="1.8s" repeatCount="indefinite"/>
    </rect>`;

  const рамка = (x,y,w,h,заливка,обводка,скругл,толщ) =>
    `<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="${скругл==null?10:скругл}"
       fill="${заливка||'rgba(255,255,255,.05)'}" stroke="${обводка||ЛИНИЯ}"
       stroke-width="${толщ||1.5}"/>`;

  const круг = (cx,cy,r,заливка,обводка) =>
    `<circle cx="${cx}" cy="${cy}" r="${r}" fill="${заливка||'none'}"
      ${обводка?`stroke="${обводка}" stroke-width="1.6"`:''}/>`;

  /* Теория: короткая плашка «приём» под рисунком */
  /* Теория приёма. У каждого приёма свой знак и свой цвет: блоки не должны
     выглядеть одним и тем же шаблоном (правило 9 стандарта). */
  const теория = (заголовок, строки, цвет, знак) =>
    `<div class="card ${ДОМ}-теория" style="border-left-color:${цвет||ЗОЛОТО};padding:12px 14px">
       <div style="display:flex;align-items:center;gap:10px;margin-bottom:8px">
         <span style="font-size:24px;line-height:1">${знак||'📘'}</span>
         <span style="font-size:16px;font-weight:bold;color:${цвет||ЗОЛОТО};letter-spacing:-.01em">${esc(заголовок)}</span>
       </div>
       ${строки.map((с,i)=>`<div style="display:flex;gap:10px;margin-top:6px">
           <span style="flex:none;width:20px;height:20px;border-radius:50%;border:1.5px solid ${цвет||ЗОЛОТО};
             font-size:12px;line-height:17px;text-align:center;color:${цвет||ЗОЛОТО};font-variant-numeric:tabular-nums">${i+1}</span>
           <span style="font-size:16px;line-height:1.5;color:#f4e9c8">${с}</span>
         </div>`).join('')}
     </div>`;

  /* Разбор: подробно, с «частой ошибкой» */
  const разбор = (текст, ошибка) =>
    `<div class="card" style="border-color:${ЗЕЛЁН};padding:12px 14px">
       <div style="font-size:16px;font-weight:bold;color:${ЗЕЛЁН};margin-bottom:6px">✅ Разбор</div>
       <div style="font-size:16px;line-height:1.55">${esc(текст)}</div>
       ${ошибка?`<div style="font-size:14px;line-height:1.5;color:${КРАСН};margin-top:8px">⚠ ${esc(ошибка)}</div>`:''}
     </div>`;

  const шапка = (номер, название, тема) =>
    `<div class="card" style="padding:10px 12px">
       <div style="display:flex;justify-content:space-between;gap:8px;align-items:baseline">
         <span style="font-size:14px;color:var(--muted)">Дело ${номер} из 5</span>
         <span style="font-size:14px;color:${ЗОЛОТО};font-variant-numeric:tabular-nums">2 балла</span>
       </div>
       <div style="font-size:20px;font-weight:bold;color:${ЛАМПА};margin-top:2px">${esc(название)}</div>
       <div style="font-size:14px;color:var(--muted);margin-top:2px">${esc(тема)}</div>
     </div>`;

  /* ══════════════════════ ШАГИ ТУРА ══════════════════════
     Каждый шаг: {т: заголовок, рисунок(), низ()} — рисунок с анимацией,
     низ — теория, интерактив и разбор. */
  const ШАГИ = [];

  /* ── 1. ДВОРИК: знакомство ─────────────────────────────── */
  ШАГИ.push({
    т:'Старый дворик',
    рисунок(){
      /* Кадр держим простым: пять смысловых элементов, не больше.
         Ночной дворик — два дома, гирлянда с тремя фонарями между ними и
         двое ребят на земле. Детали предметные: у дома крыша со скатом,
         труба, дверь и окно с переплётом; у фонаря крюк, цоколь и колба;
         у ребят голова, руки, ноги. */
      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

      /* небо: месяц и четыре звезды */
      s += `<g style="${появляется(0.05,480)}">
              <circle cx="48" cy="34" r="11" fill="rgba(255,232,160,.95)"/>
              <circle cx="44" cy="31" r="2" fill="rgba(190,160,100,.5)"/>
              <circle cx="51" cy="38" r="1.4" fill="rgba(190,160,100,.45)"/>
            </g>`;
      [[104,24],[152,20],[212,26],[262,22]].forEach(([x,y],i)=>{
        s += `<g opacity=".3">
                <path d="M${x-3.6} ${y} L${x+3.6} ${y} M${x} ${y-3.6} L${x} ${y+3.6}"
                  stroke="${ЛАМПА}" stroke-width="1.1"/>
                <circle cx="${x}" cy="${y}" r="1.2" fill="${ЛАМПА}">
                  <animate attributeName="opacity" values=".2;1;.2" dur="${(2.4+i*0.3).toFixed(1)}s"
                    begin="${(0.4+i*0.15).toFixed(2)}s" repeatCount="indefinite"/>
                </circle>
              </g>`;
      });

      /* два дома: крупно, с зазором по центру */
      s += `<g style="${появляется(0.3,520)}">${дом(24,72,86,48,0.95)}</g>`;
      s += `<g style="${появляется(0.45,520)}">${дом(226,72,86,48,0.7)}</g>`;

      /* земля */
      s += `<rect x="8" y="120" width="320" height="38" fill="rgba(255,255,255,.035)"/>`;
      s += `<path d="M8 120 L328 120" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      [[56,132],[120,138],[184,131],[248,137],[296,132]].forEach(([x,y])=>{
        s += `<ellipse cx="${x}" cy="${y}" rx="3.4" ry="1.6" fill="rgba(255,255,255,.08)"/>`;
      });
      for(let i=0;i<9;i++){
        const x=28+i*34, h=4+((i*5)%4);
        s += `<path d="M${x} 120 C${x-2} ${120-h} ${x+2} ${120-h} ${x} ${120-h-2}"
          stroke="${ЛИНИЯ}" stroke-width="1" fill="none" opacity=".8"/>`;
      }

      /* гирлянда между домами: провод провисает дугой, на нём три фонаря */
      const x1=112, x2=224, cx=(x1+x2)/2;
      const дл=Math.round(Math.abs(x2-x1)+16);
      s += `<path d="M${x1} 62 Q${cx} 86 ${x2} 62" fill="none" stroke="${ЛИНИЯ}"
        stroke-width="1.6" stroke-dasharray="${дл}" stroke-dashoffset="${дл}">
        <animate attributeName="stroke-dashoffset" from="${дл}" to="0" dur="520ms"
          begin="0.25s" fill="freeze"/></path>`;
      const точка = t => { const u=1-t; return { x:u*u*x1+2*u*t*cx+t*t*x2, y:u*u*62+2*u*t*86+t*t*62 }; };
      [0.2,0.5,0.8].forEach((t,i)=>{
        const p=точка(t);
        s += `<g style="${появляется(0.55+i*0.16,380)}">${фонарь(p.x,p.y,1)}</g>`;
      });

      /* ребята под гирляндой */
      s += `<g style="${появляется(1.05,420)}">${ребёнок(150,120,ЗОЛОТО,22)}</g>`;
      s += `<g style="${появляется(1.18,420)}">${ребёнок(170,120,СИНЬ,18)}</g>`;

      s += `<g style="${появляется(1.35,400)}">${т(168,146,'пять дел — пять задач',12,ТИХО)}</g>`;
      return svg(s,166);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.5">В старом дворике Сиракуз готовятся к празднику: развесили гирлянду, разложили игрушки на ковре и позвали гостей. Архимед обошёл дворик и на каждом шаге оставил по задаче.</div>
          <div style="font-size:16px;line-height:1.5;margin-top:8px">Задач пять, за каждую можно получить 2 балла — всего 10.</div>
        </div>`;
    }
  });

  /* ── 2. КОРЗИНА: теория ────────────────────────────────── */
  ШАГИ.push({
    т:'Приём: цепочка',
    рисунок(){
      /* Приём показываем на людях и фруктах: пять ребят в ряд, у каждого над
         головой его фрукт — яблоко или апельсин. Между соседями знак «≠»,
         потому что вкусы у соседей разные. Связи проводятся по очереди,
         знаки появляются за ними, бегущий указатель показывает, что проверять
         надо каждую пару. */
      const имена=['Алиса','Боря','Вася','Гена','Дима'];
      const ябл=[true,false,true,false,true];

      let s = `<rect x="8" y="8" width="320" height="152" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="152" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g style="${появляется(0.05,360)}">${т(168,28,'У соседей разное — значит чередуется',12,ЗОЛОТО,true)}</g>`;

      const y=104, шаг=62, x0=40;
      имена.forEach((им,i)=>{
        const x=x0+i*шаг, з=0.12+i*0.12;
        s += `<g style="${появляется(з,380)}">${ребёнок(x,y,i%2?СИНЬ:ЗОЛОТО,26)}</g>`;
        s += `<g style="${появляется(з+0.10,340)}">
                ${ябл[i] ? яблоко(x,y-40,1.15) : апельсин(x,y-40,1.15)}
              </g>`;
        s += `<g style="${появляется(з+0.18,320)}">${т(x,y+16,им,12,ПЕРО)}</g>`;
        if(i<имена.length-1){
          s += линияРисуется(x+16,y-4,x+шаг-16,y-4,ЛИНИЯ,1.6,0.3+i*0.13);
          s += `<g style="${появляется(0.55+i*0.13,300)}">${т(x+шаг/2,y-14,'≠',14,КРАСН,true)}</g>`;
        }
      });

      s += `<g style="${появляется(0.95,340)}">
              ${яблоко(126,152,0.9)}${т(140,156,'яблоко',12,ТИХО,false,'start')}
              ${апельсин(214,152,0.9)}${т(228,156,'апельсин',12,ТИХО,false,'start')}
            </g>`;
      s += `<circle cx="${x0}" cy="${y+34}" r="3.6" fill="${ЗЕЛЁН}">
              <animate attributeName="cx" values="${x0};${x0+шаг*4}" dur="3.6s" begin="0.9s"
                repeatCount="indefinite" calcMode="spline" keySplines="0.4 0 0.2 1" keyTimes="0;1"/>
              <animate attributeName="opacity" values="0;1;1;0" dur="3.6s" begin="0.9s" repeatCount="indefinite"/>
            </circle>`;
      return svg(s,168);
    },
    низ(){
      return теория('Цепочка «одинаковое — разное»', [
        'Если у двух соседей <b>разное</b>, то у следующего снова то же, что у первого.',
        'Достаточно решить за одного — дальше всё чередуется само.',
        'Проверяй правило на каждой паре: у Алисы и Бори, у Бори и Васи, и так до конца.'
      ], ЗОЛОТО, '🔗') + `<div class="card" style="padding:12px 14px">
        <div style="font-size:16px;line-height:1.5">В задаче пять друзей и два фрукта. Посмотри, как чередование работает здесь, а потом расставь фрукты сам.</div>
      </div>`;
    }
  });

  /* ── 3. КОРЗИНА: интерактив по одному ──────────────────── */
  ШАГИ.push({
    т:'Корзина фруктов',
    рисунок(){
      const п=память(), отв=п.ответы[0]||{}, имена=Object.keys(ЗАДАЧИ[0].ответ);
      const кого=имена[п.корзина]||имена[0];
      const готово=имена.every(им=>отв[им]===ЗАДАЧИ[0].ответ[им]);
      const отмечено=имена.filter(им=>отв[им]).length;

      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

      /* корзина в центре: в ней фрукты по числу отгаданных */
      s += `<g style="${появляется(0.1,420)}">${корзина(168,96,96,34)}</g>`;
      имена.forEach((им,i)=>{
        const известно=отв[им]===ЗАДАЧИ[0].ответ[им];
        if(!известно) return;
        const x=140+i*14;
        s += `<g style="${появляется(0.2+i*0.08,300)}">
                ${ЗАДАЧИ[0].ответ[им]==='яблоки' ? яблоко(x,92,0.85) : апельсин(x,92,0.85)}
              </g>`;
      });

      /* текущий друг у корзины: показываем, о ком спрашиваем */
      s += `<g style="${появляется(0.3,380)}">${ребёнок(76,118,ЗОЛОТО,26)}</g>`;
      s += `<g style="${появляется(0.4,340)}">${т(76,136,кого,12,ЗОЛОТО,true)}</g>`;

      s += `<g style="${появляется(0.9,340)}">
              ${т(168,148, готово?'все пятеро угаданы':('друг '+(отмечено+1)+' из 5'),12,готово?ЗЕЛЁН:ТИХО)}
            </g>`;
      return svg(s,166);
    },
    низ(){
      const п=память(), отв=п.ответы[0]||{}, имена=Object.keys(ЗАДАЧИ[0].ответ);
      const кого=имена[п.корзина]||имена[0];
      const готово=имена.every(им=>отв[им]===ЗАДАЧИ[0].ответ[им]);
      const отмечено=имена.filter(им=>отв[им]).length;

      let блок = `<div class="card" style="padding:12px 14px">
        <div style="font-size:14px;color:var(--muted);margin-bottom:6px">Условие</div>
        <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[0].условие)}</div>
      </div>`;

      if(!готово){
        /* Один человек за раз: крупный вопрос, две большие кнопки — никакой
           простыни из десяти кнопок. */
        блок += `<div class="card ${ДОМ}-ждёт" style="padding:14px;text-align:center">
            <div style="font-size:14px;color:var(--muted);font-variant-numeric:tabular-nums">друг ${отмечено+1} из ${имена.length}</div>
            <div style="font-size:20px;font-weight:bold;margin:8px 0 12px">Что любит ${esc(кого)}?</div>
            <div style="display:flex;gap:12px;justify-content:center;flex-wrap:wrap;padding-right:44px">
              <button class="btn" style="min-height:56px;min-width:132px;margin:0;font-size:16px"
                onclick="Т701.фрукт('${esc(кого)}','яблоки')">🍎 яблоки</button>
              <button class="btn" style="min-height:56px;min-width:132px;margin:0;font-size:16px"
                onclick="Т701.фрукт('${esc(кого)}','апельсины')">🍊 апельсины</button>
            </div>
          </div>`;
        if(отмечено>0)
          блок += `<div class="btnrow"><button class="btn ghost" onclick="Т701.корзинаШаг(${п.корзина-1})">← Вернуться к предыдущему</button></div>`;
      } else {
        блок += `<div class="card" style="padding:12px 14px">
            <div style="font-size:16px;line-height:1.5">Проверь цепочку: у каждых соседей фрукты разные. У Алисы, Васи и Димы — яблоки, у Бори и Гены — апельсины, и правило держится.</div>
          </div>`;
        блок += разбор(ЗАДАЧИ[0].разбор, 'Частая ошибка — начинать с середины. Начинай с первого в цепочке: он задаёт всё остальное.');
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: гирлянда →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 4. ГИРЛЯНДА: теория ───────────────────────────────── */
  ШАГИ.push({
    т:'Приём: ровный ряд',
    рисунок(){
      /* Ряд из жетонов с числами на линейке. Между соседями показан шаг,
         линейка с насечками объясняет, откуда берётся шаг. */
      const ряд=[7,11,15,19,23];
      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g style="${появляется(0.05,360)}">${т(168,28,'В ровном ряду шаг одинаковый',12,ЗОЛОТО,true)}</g>`;

      const y=78, шаг=60, x0=48;
      /* линейка с насечками */
      s += `<path d="M24 106 L312 106" stroke="${ЛИНИЯ}" stroke-width="1.4"/>`;
      for(let i=0;i<25;i++){
        const x=24+i*12, д=(i%5===0)?6:3;
        s += `<path d="M${x} 106 L${x} ${106-д}" stroke="${ЛИНИЯ}" stroke-width="1" opacity=".7"/>`;
      }
      ряд.forEach((n,i)=>{
        const x=x0+i*шаг, з=0.15+i*0.13;
        s += `<g style="${появляется(з,380)}">${жетон(x,y,String(n),ЗОЛОТО,20,'rgba(255,215,106,.14)')}</g>`;
        s += `<path d="M${x} ${y+14} L${x} 106" stroke="${ЛИНИЯ}" stroke-width="1" stroke-dasharray="3 3" opacity=".6"/>`;
        if(i<ряд.length-1){
          s += `<g style="${появляется(з+0.1,300)}">
                  ${т(x+шаг/2,y-24,'+4',12,ЗЕЛЁН,true)}
                  ${линияРисуется(x+14,y+22,x+шаг-14,y+22,ЗЕЛЁН,1.4,0.4+i*0.12)}
                </g>`;
        }
      });
      s += `<g style="${появляется(1.0,340)}">${т(168,140,'разницы между соседями одинаковые',12,ТИХО)}</g>`;
      return svg(s,160);
    },
    низ(){
      return теория('Ровный ряд и его шаг', [
        'Выпиши числа <b>по возрастанию</b> — так сразу видно порядок.',
        'Посчитай разницы между соседними числами.',
        'Если все разницы одинаковые — ряд ровный, эта разница и есть <b>шаг</b>.'
      ], СИНЬ, '📏');
    }
  });

  /* ── 5. ГИРЛЯНДА: интерактив ───────────────────────────── */
  ШАГИ.push({
    т:'Ровный ряд на гирлянде',
    рисунок(){
      const п=память(), выбр=п.ответы[1];
      const числа=[11,7,23,13,19,15];
      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g style="${появляется(0.05,340)}">${т(168,26,'Найди перегоревшую лампочку',12,ЗОЛОТО,true)}</g>`;

      /* провод гирлянды и шесть лампочек с числами */
      const x0=46, шаг=49, y=66;
      s += `<path d="M24 46 Q168 74 312 46" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      числа.forEach((n,i)=>{
        const x=x0+i*шаг;
        const выб=(выбр===i);
        s += `<g style="${появляется(0.15+i*0.09,360)}">
                ${лампочка(x,y,!выб)}
              </g>`;
        s += `<g style="${появляется(0.25+i*0.09,320)}">
                ${жетон(x,y+30,String(n),выб?КРАСН:ПЕРО,14,
                        выб?'rgba(232,106,90,.18)':'rgba(255,255,255,.07)')}
              </g>`;
      });
      if(выбр!=null){
        const порядок=[...числа].sort((a,b)=>a-b);
        const ост=порядок.filter(n=>n!==числа[выбр]);
        const шаги=[]; for(let j=1;j<ост.length;j++) шаги.push(ост[j]-ост[j-1]);
        const ровно=шаги.length>1 && шаги.every(x=>x===шаги[0]);
        s += `<g style="${появляется(0.05,320)}">
                ${т(168,124,'осталось: '+ост.join(' · '),12,ровно?ЗЕЛЁН:КРАСН,true)}
                ${т(168,144, ровно?('шаг '+шаги[0]+' — ряд ровный'):('шаги: '+шаги.join(', ')+' — не ровно'),12,ТИХО)}
              </g>`;
      } else {
        s += `<g style="${появляется(0.8,320)}">${т(168,140,'нажми на лампочку, которую надо убрать',12,ТИХО)}</g>`;
      }
      return svg(s,160);
    },
    низ(){
      const п=память(), выбр=п.ответы[1];
      let блок = `<div class="card" style="padding:12px 14px">
        <div style="font-size:14px;color:var(--muted);margin-bottom:6px">Условие</div>
        <div style="font-size:16px;line-height:1.55">${esc(ЗАДАЧИ[1].условие)}</div>
        <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[1].вопрос)}</div>
      </div>`;
      блок += `<div class="card ${выбр==null?ДОМ+'-ждёт':''}" style="padding:12px 14px"><div style="display:flex;flex-wrap:wrap;gap:8px">` +
        [11,7,23,13,19,15].map((n,i)=>
          `<button class="btn" style="min-height:44px;margin:0;min-width:56px" onclick="Т701.гирлянда(${i})">${n}</button>`).join('') +
        `</div></div>`;
      if(выбр===ЗАДАЧИ[1].верный){
        блок += разбор(ЗАДАЧИ[1].разбор, 'Частая ошибка — искать «лишнее» число наугад. Всегда сначала выписывай числа по возрастанию.');
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: ковёр →</button></div>`;
      } else if(выбр!=null){
        блок += `<div class="card" style="border-color:${КРАСН};padding:12px 14px;font-size:16px">Пока не сходится: с этим числом шаги получаются разными. Попробуй другое.</div>`;
      }
      return блок;
    }
  });

  /* ── 6. КОВЁР: теория ──────────────────────────────────── */
  ШАГИ.push({
    т:'Приём: перебор ходов',
    рисунок(){
      /* На ковре пять фигур в ряд — как в задаче. Две первые уже «ушли»:
         показываем, что перебор идёт по парам. */
      const формы=['треугольник','квадрат','круг','квадрат','треугольник'];
      const цвета=[ЗЕЛЁН,ПЕРО,СИНЬ,СИНЬ,КРАСН];
      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g style="${появляется(0.05,340)}">${т(168,26,'Вариантов мало — перебери все',12,ЗОЛОТО,true)}</g>`;
      s += `<g style="${появляется(0.15,420)}">${ковёр(24,44,288,86)}</g>`;
      формы.forEach((ф,i)=>{
        const x=60+i*56, y=92;
        s += `<g style="${появляется(0.35+i*0.12,360)}">${фигура(x,y,ф,цвета[i],false)}</g>`;
      });
      s += `<g style="${появляется(1.0,340)}">${т(168,152,'сначала отбрось невозможное — останется одно',12,ТИХО)}</g>`;
      return svg(s,166);
    },
    низ(){
      return теория('Перебор двух ходов', [
        'Когда вариантов <b>мало</b>, их перебирают все — по одному.',
        'Сначала отбрасывай невозможные: так вариантов становится меньше.',
        'Проверяй каждый вариант до конца: если где-то не сходится, вариант отпадает.'
      ], ЗЕЛЁН, '🔀');
    }
  });

  /* ── 7. КОВЁР: интерактив ──────────────────────────────── */
  ШАГИ.push({
    т:'Игрушки на ковре',
    рисунок(){
      const п=память(), ходы=п.ковар||{};
      const формы=['треугольник','квадрат','круг','квадрат','треугольник'];
      const цвета=[ЗЕЛЁН,ПЕРО,СИНЬ,СИНЬ,КРАСН];
      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g style="${появляется(0.05,340)}">
              ${т(168,26, ходы.петя&&ходы.лиза ? 'осталась одна фигура'
                 : (ходы.петя ? 'теперь две фигуры одинаковой формы' : 'убери две соседние — ход Пети'),12,ЗОЛОТО,true)}
            </g>`;
      s += `<g style="${появляется(0.12,420)}">${ковёр(24,44,288,86)}</g>`;
      формы.forEach((ф,i)=>{
        const x=60+i*56, y=92, убран=ходы.убрано && ходы.убрано.indexOf(i)>=0;
        const выбран=ходы.выбор && ходы.выбор.indexOf(i)>=0;
        if(убран) return;
        s += `<g style="${появляется(0.3+i*0.08,340)}" onclick="Т701.фигура(${i})">
                ${фигура(x,y,ф,цвета[i],выбран)}
              </g>`;
      });
      s += `<g style="${появляется(0.9,320)}">
              ${т(168,152, ходы.петя&&ходы.лиза ? 'оба хода сделаны' : 'нажимай на фигуры по очереди',12,ТИХО)}
            </g>`;
      return svg(s,166);
    },
    низ(){
      const п=память(), отв=п.ответы[2];
      let блок = `<div class="card" style="padding:12px 14px">
        <div style="font-size:14px;color:var(--muted);margin-bottom:6px">Условие</div>
        <div style="font-size:16px;line-height:1.55">${esc(ЗАДАЧИ[2].условие)}</div>
        <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[2].вопрос)}</div>
      </div>`;
      блок += `<div class="card ${отв==null?ДОМ+'-ждёт':''}" style="padding:12px 14px"><div style="display:flex;flex-wrap:wrap;gap:8px">` +
        ЗАДАЧИ[2].варианты.map((в,i)=>
          `<button class="btn" style="min-height:44px;margin:0" onclick="Т701.ковёр(${i})">${esc(в)}</button>`).join('') +
        `</div><div style="font-size:14px;color:var(--muted);margin-top:8px">Можно и просто перебрать два хода руками — нажми на фигуры выше.</div></div>`;
      if(отв===ЗАДАЧИ[2].верный){
        блок += разбор(ЗАДАЧИ[2].разбор, 'Частая ошибка — не проверить вариант до конца. Если хоть один ход невозможен, вариант отпадает.');
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: Саша Ким →</button></div>`;
      } else if(отв!=null){
        блок += `<div class="card" style="border-color:${КРАСН};padding:12px 14px;font-size:16px">Не сходится: при таком ответе не получается разложить оба хода. Перебери варианты.</div>`;
      }
      return блок;
    }
  });

  /* ── 8–9. САША КИМ: теория и интерактив ─────────────────── */
  ШАГИ.push({
    т:'Приём: вывод по ограничениям',
    рисунок(){
      /* Две семьи: у каждой три ребёнка. Пол пока неизвестен — знак вопроса,
         у Кости стоит «мальчик», потому что оба Кости мальчики. */
      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g style="${появляется(0.05,340)}">${т(168,26,'Каждое условие отсекает варианты',12,ЗОЛОТО,true)}</g>`;
      [['Петровы',0],['Сидоровы',1]].forEach(([семья,ряд])=>{
        const y=50+ряд*46;
        s += `<g style="${появляется(0.2+ряд*0.15,360)}">
                ${т(28,y+6,семья,12,ТИХО,false,'start')}
              </g>`;
        ['Валя','Костя','Женя'].forEach((им,j)=>{
          const x=110+j*64;
          const мальчик=(им==='Костя');
          const ц=мальчик?СИНЬ:ПЕРО;
          s += `<g style="${появляется(0.35+ряд*0.15+j*0.08,320)}">
                  ${ребёнок(x,y+10,ц,20)}
                  ${т(x,y+24,им,12,мальчик?СИНЬ:ПЕРО)}
                  ${т(x,y+36, мальчик?'мальчик':'?',12,мальчик?СИНЬ:ТИХО)}
                </g>`;
        });
      });
      s += `<g style="${появляется(1.0,340)}">${т(168,156,'у Петровых один мальчик, у Сидоровых три',12,ТИХО)}</g>`;
      return svg(s,170);
    },
    низ(){
      return теория('Вывод по ограничениям', [
        'Каждое условие — как <b>запрет</b>: оно вычёркивает часть вариантов.',
        'Оба Кости — мальчики. Значит у Петровых мальчик — это Костя, а Валя, Женя и Саша — девочки.',
        'Проверь вывод по второй семье: он должен сойтись, иначе рассуждение неверное.'
      ], ЗОЛОТО, '🔍');
    }
  });
  ШАГИ.push({
    т:'Новый знакомый Саша Ким',
    рисунок(){
      const п=память(), отв=п.ответы[3]||{};
      const кто=ЛЮДИ[п.полКто]||ЛЮДИ[0];
      const готово=ЛЮДИ.every((им,i)=>отв[им]===ПОЛ[i]);
      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g style="${появляется(0.05,340)}">${т(168,26,'Кто есть кто',12,ЗОЛОТО,true)}</g>`;
      ЛЮДИ.forEach((им,i)=>{
        const x=44+(i%4)*80, y=60+Math.floor(i/4)*46;
        const о=отв[им], верно=о&&о===ПОЛ[i];
        const текущий=им===кто && !готово;
        const ц=верно?ЗЕЛЁН:(текущий?ЗОЛОТО:(о?КРАСН:ТИХО));
        s += `<g style="${появляется(i*0.06,320)}">
                ${ребёнок(x,y+8,ц,20)}
                ${текущий?ждётМерцает(x-26,y-18,52,42,ЗОЛОТО):''}
                ${т(x,y+22,им.split(' ')[1]||им,12,ПЕРО)}
                ${т(x,y+34, о || '?',12,ц)}
              </g>`;
      });
      s += `<g style="${появляется(0.6,320)}">
              ${т(168,154, готово?'все определены':('чей ход: '+кто),12,готово?ЗЕЛЁН:ТИХО)}
            </g>`;
      return svg(s,168);
    },
    низ(){
      const п=память(), отв=п.ответы[3]||{};
      const кто=ЛЮДИ[п.полКто]||ЛЮДИ[0];
      const отмечено=ЛЮДИ.filter(им=>отв[им]).length;
      const готово=ЛЮДИ.every((им,i)=>отв[им]===ПОЛ[i]);

      let блок = `<div class="card" style="padding:12px 14px">
        <div style="font-size:14px;color:var(--muted);margin-bottom:6px">Условие</div>
        <div style="font-size:16px;line-height:1.5">${esc(ЗАДАЧИ[3].условие)}</div>
        <div style="font-size:16px;font-weight:600;margin-top:8px">${esc(ЗАДАЧИ[3].вопрос)}</div>
      </div>`;

      if(!готово){
        блок += `<div class="card ${ДОМ}-ждёт" style="padding:14px;text-align:center">
            <div style="font-size:14px;color:var(--muted);font-variant-numeric:tabular-nums">${отмечено+1}-й из ${ЛЮДИ.length}</div>
            <div style="font-size:20px;font-weight:bold;margin:8px 0 12px">Кто такой ${esc(кто)}?</div>
            <div style="display:flex;gap:12px;justify-content:center;flex-wrap:wrap;padding-right:44px">
              <button class="btn" style="min-height:56px;min-width:132px;margin:0;font-size:16px"
                onclick="Т701.пол('${esc(кто)}','мальчик')">мальчик</button>
              <button class="btn" style="min-height:56px;min-width:132px;margin:0;font-size:16px"
                onclick="Т701.пол('${esc(кто)}','девочка')">девочка</button>
            </div>
          </div>`;
        if(отмечено>0)
          блок += `<div class="btnrow"><button class="btn ghost" onclick="Т701.полШаг(${п.полКто-1})">← Вернуться к предыдущему</button></div>`;
      } else {
        блок += разбор(ЗАДАЧИ[3].разбор, 'Частая ошибка — остановиться на первой семье. Вывод надо проверить по второму условию.');
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Дальше: хоровод →</button></div>`;
      }
      return блок;
    }
  });

  /* ── 10–11. ХОРОВОД: теория и интерактив ─────────────────── */
  ШАГИ.push({
    т:'Приём: кто что говорит',
    рисунок(){
      /* Разбираем по случаям: эльф и гном говорят одному и тому же существу.
         У эльфа в обоих случаях выходит «Ты гном» — это и есть ключ. */
      const случай = (y, кто, передКем, реплика, пояснение) => `
        <g>
          ${ребёнок(70,y,кто==='эльф'?СИНЬ:ЗЕЛЁН,24)}
          ${т(70,y+16,кто,12,кто==='эльф'?СИНЬ:ЗЕЛЁН)}
          ${ребёнок(140,y,передКем==='эльф'?СИНЬ:ЗЕЛЁН,24)}
          ${т(140,y+16,передКем,12,передКем==='эльф'?СИНЬ:ЗЕЛЁН)}
          <path d="M162 ${y-6} L200 ${y-6}" stroke="${ЛИНИЯ}" stroke-width="1.4" marker-end="url(#${ДОМ}-стрелка)"/>
          <rect x="206" y="${y-20}" width="98" height="28" rx="8" fill="rgba(255,215,106,.14)"
            stroke="${ЗОЛОТО}" stroke-width="1.4"/>
          ${т(255,y-2,реплика,12,ЛАМПА,true)}
          ${т(255,y+22,пояснение,12,ТИХО)}
        </g>`;
      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g style="${появляется(0.05,340)}">${т(168,26,'Эльф всегда скажет «Ты гном»',12,ЗОЛОТО,true)}</g>`;
      s += `<g style="${появляется(0.2,380)}">${случай(74,'эльф','эльф','Ты гном','свой — врёт')}</g>`;
      s += `<g style="${появляется(0.5,380)}">${случай(126,'эльф','гном','Ты гном','чужой — правда')}</g>`;
      return svg(s,166);
    },
    низ(){
      return теория('Истина и ложь в высказываниях', [
        'Разбери <b>по случаям</b>, что скажет каждый: своему он врёт, чужому говорит правду.',
        'У эльфа оба случая дают одно и то же — «Ты гном».',
        'У гнома оба случая дают «Ты эльф». Значит по числу фраз видно, кого сколько.'
      ], КРАСН, '💬');
    }
  });
  ШАГИ.push({
    т:'Хоровод гномов и эльфов',
    рисунок(){
      const п=память(), отв=п.ответы[4]||{};
      let s = `<rect x="8" y="8" width="320" height="150" rx="12" fill="#0d1f1a"/>`;
      s += `<rect x="8" y="8" width="320" height="150" rx="12" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
      s += `<g style="${появляется(0.05,340)}">${т(168,26,'Хоровод: 16 фраз',12,ЗОЛОТО,true)}</g>`;

      /* шесть фигурок по кругу: три эльфа, три гнома — как доля от целого */
      const n=6, R=48;
      for(let i=0;i<n;i++){
        const угол=(i/n)*Math.PI*2 - Math.PI/2;
        const x=140+Math.cos(угол)*R, y=88+Math.sin(угол)*R*0.82;
        const эльф=i<3;
        s += `<g style="${появляется(0.2+i*0.08,340)}">
                ${ребёнок(x,y,эльф?СИНЬ:ЗЕЛЁН,20)}
                <animateTransform attributeName="transform" type="translate"
                  values="0 0; 0 -2; 0 0" dur="${(1.8+i*0.07).toFixed(2)}s"
                  begin="${(i*0.06).toFixed(2)}s" repeatCount="indefinite"/>
              </g>`;
      }
      /* костёр в центре */
      s += `<g style="${появляется(0.6,400)}">
              <path d="M140 96 C132 88 136 80 140 76 C142 82 146 82 148 78 C152 84 150 92 140 96 Z"
                fill="rgba(232,140,60,.85)" stroke="#a3551f" stroke-width="1.2"/>
              <path d="M132 98 L148 98 M134 102 L146 102" stroke="#7a4a24" stroke-width="2.2"/>
            </g>`;
      s += `<g style="${появляется(0.85,340)}">
              ${т(232,74,'7 фраз «Ты эльф»',12,ЛАМПА,true)}
              ${т(232,94,'9 фраз «Ты гном»',12,ТИХО)}
              ${отв.гномов!=null?т(232,120,(отв.гномов===7&&отв.эльфов===9)?'сходится: 7 и 9':'проверь ещё раз',12,(отв.гномов===7&&отв.эльфов===9)?ЗЕЛЁН:КРАСН,true):''}
            </g>`;
      s += `<g style="${появляется(1.0,320)}">${т(168,152,'фразы «Ты эльф» — это гномы',12,ТИХО)}</g>`;
      return svg(s,166);
    },
    низ(){
      const п=память(), отв=п.ответы[4]||{};
      let блок = `<div class="card" style="padding:12px 14px">
        <div style="font-size:14px;color:var(--muted);margin-bottom:6px">Условие</div>
        <div style="font-size:16px;line-height:1.55">${esc(ЗАДАЧИ[4].условие)}</div>
        <div style="font-size:16px;font-weight:bold;margin-top:8px">${esc(ЗАДАЧИ[4].вопрос)}</div>
      </div>`;
      блок += `<div class="card" style="padding:12px 14px"><div style="display:flex;flex-direction:column;gap:8px">` +
        [['гномов',7],['эльфов',9]].map(([им])=>
          `<label style="display:flex;align-items:center;gap:10px">
             <span style="min-width:76px;font-size:16px">${им}</span>
             <input id="Т701-${им}" type="text" inputmode="numeric" value="${отв[им]!=null?отв[им]:''}"
               style="flex:1;min-width:0;min-height:44px;padding:8px 12px;border-radius:10px;border:1px solid ${ЛИНИЯ};background:rgba(255,255,255,.05);color:${ПЕРО};font-size:16px">
           </label>`).join('') +
        `</div><button class="btn" style="margin-top:10px" onclick="Т701.хоровод()">Ответить</button></div>`;
      const верно = отв.гномов===7 && отв.эльфов===9;
      if(верно){
        блок += разбор(ЗАДАЧИ[4].разбор, 'Частая ошибка — решать «в лоб» про каждого. Смотри, что каждый говорит всегда, независимо от соседа.');
        блок += `<div class="btnrow"><button class="btn ok2" onclick="lvStep(1)">Смотреть итог →</button></div>`;
      } else if(отв.гномов!=null){
        блок += `<div class="card" style="border-color:${КРАСН};padding:12px 14px;font-size:16px">Пока не сходится. Подсказка: число фраз «Ты эльф» — это число гномов.</div>`;
      }
      return блок;
    }
  });

  /* ── 12. ПРОТОКОЛ ──────────────────────────────────────── */
  ШАГИ.push({
    т:'Протокол тура',
    рисунок(){
      const балл=баллТура(), процент=Math.round(балл/10*100);
      const цвет=балл>=8?ЗЕЛЁН:(балл>=5?ЛАМПА:КРАСН);
      /* Что вышло по каждому делу: пять отметок — видно, что разбирать. */
      const дела=[
        {имя:'корзина', ок:Object.keys(ЗАДАЧИ[0].ответ).every(к=>(память().ответы[0]||{})[к]===ЗАДАЧИ[0].ответ[к])},
        {имя:'гирлянда', ок:память().ответы[1]===ЗАДАЧИ[1].верный},
        {имя:'ковёр', ок:память().ответы[2]===ЗАДАЧИ[2].верный},
        {имя:'Саша Ким', ок:ЛЮДИ.every((им,i)=>(память().ответы[3]||{})[им]===ПОЛ[i])},
        {имя:'хоровод', ок:((память().ответы[4]||{}).гномов===7 && (память().ответы[4]||{}).эльфов===9)}
      ];

      let s = рамка(8,8,320,144,'#0f231d',ЛИНИЯ,12);

      /* медаль: круг с баллом внутри, кольцо медленно вращается */
      s += `<g style="${появляется(0.05,420)}">
              ${круг(168,58,40,'rgba(255,255,255,.04)',цвет)}
              <circle cx="168" cy="58" r="46" fill="none" stroke="${цвет}" stroke-width="1.4"
                stroke-dasharray="10 8" opacity=".5">
                <animateTransform attributeName="transform" type="rotate"
                  from="0 168 58" to="360 168 58" dur="18s" repeatCount="indefinite"/>
              </circle>
              <text id="${ДОМ}-балл" x="168" y="66" text-anchor="middle" font-size="32" font-weight="bold"
                fill="${цвет}" font-family="Georgia,serif" style="font-variant-numeric:tabular-nums">0</text>
            </g>`;
      s += `<g style="${появляется(0.35,320)}">
              ${т(168,124,'из 10 баллов · выполнение '+процент+' %',14,ПЕРО)}
            </g>`;

      /* лента дел: пять отметок по числу дел в туре */
      дела.forEach((д,i)=>{
        const x=48+i*60, y=150;
        s += `<g style="${появляется(0.5+i*0.09,340)}">
                ${рамка(x-24,y-13,48,26,д.ок?'rgba(143,209,168,.14)':'rgba(232,106,90,.12)',
                        д.ок?ЗЕЛЁН:КРАСН,8,1.4)}
                ${т(x,y+4,д.ок?'✓':'✕',14,д.ок?ЗЕЛЁН:КРАСН,true)}
              </g>`;
        s += `<g style="${появляется(0.6+i*0.09,320)}">
                ${т(x,y+30,String(i+1),12,ТИХО)}
              </g>`;
      });

      /* полоса выполнения растёт до значения */
      s += рамка(48,192,240,10,'rgba(255,255,255,.06)',ЛИНИЯ,5,1);
      s += `<rect x="48" y="192" width="0" height="10" rx="5" fill="${цвет}">
              <animate attributeName="width" from="0" to="${Math.round(240*балл/10)}"
                dur="900ms" begin="0.1s" fill="freeze"
                calcMode="spline" keySplines="0.23 1 0.32 1" keyTimes="0;1"/>
            </rect>`;

      /* при полном балле — искры над медалью */
      if(балл===10){
        [[132,20],[204,20],[168,6],[118,44],[218,44]].forEach(([x,y],i)=>{
          s += `<circle cx="${x}" cy="${y}" r="2.4" fill="${ЗОЛОТО}" opacity="0">
                  <animate attributeName="opacity" values="0;1;0" dur="1.6s"
                    begin="${(0.6+i*0.18).toFixed(2)}s" repeatCount="indefinite"/>
                </circle>`;
        });
      }
      s += `<g style="${появляется(1.1,400)}">
              ${т(168,214, балл>=8?'отличный результат':(балл>=5?'есть что разобрать':'вернись к приёмам'),12,ТИХО)}
            </g>`;
      return svg(s,222);
    },
    низ(){
      return `<div class="card" style="padding:12px 14px">
          <div style="font-size:16px;line-height:1.55">Тур пройден. Пять приёмов из старого дворика: цепочка «одинаковое — разное», ровный ряд и шаг, перебор двух ходов, вывод по ограничениям и разбор «кто что говорит».</div>
          <div style="font-size:16px;line-height:1.55;margin-top:8px">Эти приёмы встречаются в олимпиадных задачах снова и снова — не потому что они про фрукты или гирлянды, а потому что так устроено рассуждение.</div>
        </div>
        <div class="btnrow"><button class="btn ghost" onclick="go('olymp')">← К списку туров</button>
        <button class="btn ok2" onclick="go('tour')">В раздел «Тур»</button></div>`;
    }
  });

  /* ══════════════════════ РЕГИСТРАЦИЯ ══════════════════════ */
  const ТЕКУЩИЙ = () => Math.max(0, Math.min(ШАГИ.length-1,
                     (typeof LV !== 'undefined' && LV && LV.step) || 0));

  function стиль(){
    if(document.getElementById(ДОМ+'-стиль')) return;
    const s=document.createElement('style');
    s.id=ДОМ+'-стиль';
    s.textContent = `
      @keyframes ${ДОМ}-лампа{from{opacity:0;transform:translateY(-6px)}to{opacity:1;transform:none}}
      @keyframes ${ДОМ}-въезд{from{opacity:0;transform:translateX(-8px)}to{opacity:1;transform:none}}
      @keyframes ${ДОМ}-печать{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}
      @keyframes ${ДОМ}-уезд{to{opacity:0;transform:translateY(-10px) rotate(-6deg)}}
      @keyframes ${ДОМ}-полоса{from{width:0}to{width:var(--ш,100%)}}
      @keyframes ${ДОМ}-появление{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
      @keyframes ${ДОМ}-пульс{0%,100%{opacity:.45}50%{opacity:1}}

      .${ДОМ}{display:flex;flex-direction:column;align-items:stretch;gap:12px;
        font-family:Georgia,'Times New Roman',serif;-webkit-font-smoothing:antialiased;
        text-rendering:geometricPrecision}
      .${ДОМ}-шапка{text-align:center;font-size:14px;color:var(--muted,#cbb89a);
        letter-spacing:.02em;font-variant-numeric:tabular-nums}
      .${ДОМ}-шапка b{color:#ffd76a;font-weight:600}
      .${ДОМ}-сцена{display:flex;justify-content:center;width:100%}
      .${ДОМ}-сцена svg{display:block;width:100%;max-width:420px;height:auto;margin:0 auto}
      .${ДОМ}-низ{display:flex;flex-direction:column;gap:12px;animation:${ДОМ}-появление 320ms cubic-bezier(.23,1,.32,1) both}
      .${ДОМ} .card{text-align:left}
      .${ДОМ} .card>div{text-wrap:pretty}
      /* Ответ ждёт нажатия: пунктир и лёгкая пульсация, как в стандарте. */
      .${ДОМ}-ждёт{animation:${ДОМ}-пульс 1.6s ease-in-out infinite}
      .${ДОМ} button{transition:transform 140ms cubic-bezier(.23,1,.32,1),
        border-color 140ms ease,background-color 140ms ease;will-change:transform}
      .${ДОМ} button:active{transform:translateY(2px) scale(.985)}
      .${ДОМ} button:focus-visible{outline:3px solid #ffd76a;outline-offset:2px}
      .${ДОМ} input{font-variant-numeric:tabular-nums}
      /* Теория: у каждого приёма свой цвет и своя подпись, чтобы блоки не
         выглядели одним шаблоном. */
      .${ДОМ}-теория{border-left-width:4px;border-left-style:solid}
      @media (min-width: 700px){
        .${ДОМ}{max-width:560px;margin:0 auto}
      }
      @media (prefers-reduced-motion: reduce){
        .${ДОМ} *, #lvis svg *{animation:none!important;transition:none!important}
      }`;
    document.head.appendChild(s);
  }

  function рисовать(el){
    if(!el) return;
    стиль();
    const i = ТЕКУЩИЙ(), шаг = ШАГИ[i];
    if(!шаг){ el.innerHTML=''; return; }
    el.innerHTML = `<div class="${ДОМ}" style="animation:${ДОМ}-появление 280ms cubic-bezier(.23,1,.32,1) both">
        <div class="${ДОМ}-шапка">Шаг <b>${i+1}</b> из ${ШАГИ.length} · ${esc(шаг.т)}</div>
        ${шаг.рисунок ? `<div class="${ДОМ}-сцена">${шаг.рисунок()}</div>` : ''}
        <div class="${ДОМ}-низ">${шаг.низ ? шаг.низ() : ''}</div>
      </div>`;
    /* Счёт балла в протоколе: в SVG текстом управлять нельзя, поэтому
       досчитываем в DOM сразу после отрисовки. */
    if(i === ШАГИ.length - 1) счётДо(ДОМ+'-балл', баллТура(), 800);
  }

  /* ── действия руками ── */
  window.Т701 = {
    /* Перебор двух ходов руками: первый тап — начало хода Пети, второй —
       конец; когда Петя убрал две соседние, выбираем ход Лизы. */
    фигура(i){
      const п=память();
      const ходы=п.ковар = п.ковар || {выбор:[], убрано:[]};
      if(ходы.петя && ходы.лиза) return;
      if(!ходы.петя){
        const в=ходы.выбор;
        if(в.indexOf(i)>=0){ в.splice(в.indexOf(i),1); }
        else { в.push(i); if(в.length>2) в.shift(); }
        if(в.length===2){
          const [a,b]=[...в].sort((x,y)=>x-y);
          if(b-a===1){ ходы.убрано=ходы.убрано.concat([a,b]); ходы.петя=true; ходы.выбор=[]; }
          else { ходы.выбор=[]; }   /* не соседние — ход не засчитан */
        }
      } else if(!ходы.лиза){
        const в=ходы.выбор;
        if(в.indexOf(i)>=0){ в.splice(в.indexOf(i),1); }
        else { в.push(i); if(в.length>2) в.shift(); }
        if(в.length===2){
          const [a,b]=[...в].sort((x,y)=>x-y);
          const формыХ=['треугольник','квадрат','круг','квадрат','треугольник'];
          if(формыХ[a]===формыХ[b]){ ходы.убрано=ходы.убрано.concat([a,b]); ходы.лиза=true; ходы.выбор=[]; }
          else { ходы.выбор=[]; }
        }
      }
      перерисовать();
    },
    ковёр(индекс){
      const п=память();
      п.ответы[2] = индекс;
      перерисовать();
    },
    пол(имя, кто){
      const п=память();
      п.ответы[3] = п.ответы[3] || {};
      п.ответы[3][имя] = кто;
      const кто_все=ЛЮДИ;
      const след=кто_все.indexOf(имя)+1;
      if(след<кто_все.length) п.полКто=след;
      перерисовать();
    },
    полШаг(к){
      const п=память();
      п.полКто=Math.max(0,Math.min(ЛЮДИ.length-1,к));
      перерисовать();
    },
    хоровод(){
      const п=память();
      const г=document.getElementById('Т701-гномов'), э=document.getElementById('Т701-эльфов');
      п.ответы[4] = { гномов: г&&г.value!==''?Number(г.value):null,
                      эльфов: э&&э.value!==''?Number(э.value):null };
      перерисовать();
    },
    фрукт(имя, фрукт){
      const п=память();
      п.ответы[0] = п.ответы[0] || {};
      п.ответы[0][имя] = фрукт;
      /* Ведём по одному: выбрал фрукт — переходим к следующему другу. */
      const имена=Object.keys(ЗАДАЧИ[0].ответ);
      const след=имена.indexOf(имя)+1;
      if(след<имена.length) п.корзина=след;
      перерисовать();
    },
    корзинаШаг(к){
      const п=память();
      const имена=Object.keys(ЗАДАЧИ[0].ответ);
      п.корзина=Math.max(0,Math.min(имена.length-1,к));
      перерисовать();
    },
    гирлянда(индекс){
      const п=память();
      п.ответы[1] = индекс;
      перерисовать();
    }
  };

  /* ── лента шагов вместо «объяснений» урока ── */
  const ЛЕНТА = ШАГИ.map((ш,i)=> (i+1)+'. '+ш.т);

  window.WAVE_B = window.WAVE_B || {};
  window.WAVE_B[ID] = function(el){
    try{ рисовать(el); }
    catch(e){ el.innerHTML=''; try{ console.error('тур 701:', e); }catch(_){} }
  };

  /* Регистрация тура в каталоге: подставляем свою ленту шагов, чтобы движок
     знал, сколько шагов, и не подставлял ничего чужого. */
  (function(){
    const L = {
      id: ID, title:'Праздник в старом дворике', ico:'🎪',
      src:'Олимпиадные туры · Математика · 3 класс', subj:'tour',
      explain: ЛЕНТА,
      check: { q:'Сколько баллов можно получить за этот тур?',
               choices:['10','5','8','12'], ans:0,
               exp:'Пять задач по 2 балла — всего 10 баллов.' },
      tasks: []
    };
    const поставить = arr => {
      if(!arr || typeof arr.length!=='number') return arr;
      for(let i=0;i<arr.length;i++){ if(arr[i] && arr[i].id===ID){ arr[i]=L; return arr; } }
      arr.push(L);
      return arr;
    };
    let сейчас = поставить(window.ARH_LESSONS);
    try{
      Object.defineProperty(window,'ARH_LESSONS',{
        configurable:true, enumerable:true,
        get(){ return сейчас; },
        set(v){ сейчас = поставить(v); }
      });
    }catch(e){}
  })();
})();
