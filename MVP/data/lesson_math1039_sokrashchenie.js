/* ============ МАТЕМАТИКА · УРОК 1039 · «СОКРАЩЕНИЕ ДРОБЕЙ: НОД В ДЕЛЕ» ============
   Техзадание: школа № 1517, диагностика 6 → 7 класс, блок 1 —
   «Обыкновенная дробь, основное свойство дроби, сокращение дробей.
    Сравнение дробей. Арифметические действия с обыкновенными и десятичными дробями».
   Сюжет: шоколадная плитка 12 × 18 долек — ту же плитку переупаковываем
   в более крупные дольки: дробь не меняется, меняется только нарезка.

   ПО 12 ПРАВИЛАМ АУДИТА ЭКСПЕРТОВ:
     1) ответ не виден до выбора — и правило, и РИСУНОК (нарезка, результат,
        итоговые панели появляются только после ответа);
     2) верный вариант не первый — детерминированный сдвиг порядка;
     3) три варианта, неверные — реальные ошибки (зачеркнул цифры, сократил
        слагаемые, остановился на первом общем делителе);
     4) затухание: кадр 4 «я» (12/18 целиком), кадр 5 «мы» (шаг пропущен),
        кадр 6 «ты» (45/60 сам);
     5) интерливинг практики: 1–2 новая тема, 3 — прошлый урок (НОД и Евклид),
        4 — смешанная (сократить и сравнить), 5 — обёртка (рецепт и масштаб);
     6) разминка в кадре 1 — НОД(24, 15) = 3 по алгоритму Евклида (урок 379);
     7) самообъяснение — кадры 3 и 8: выбирается ПРИЧИНА;
     8) ошибка как данные — третий тренажёр: чужая запись, назови сбой;
     9) вариативность: 12/18 → 16/64 → 45/60 → 91/119 → не-пример со слагаемыми;
    10) я — мы — ты: кадры 4, 5, 6;
    11) выходной билет после пяти уровней;
    12) машинные проверки: qa_layout.py (включая переполнение кнопок).

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     12/18: НОД 6 → 2/3 (0,6667 = 0,6667)      16/64: НОД 16 → 1/4
     24/36: НОД 12 → 2/3                       45/60: НОД 15 → 3/4
     17/51: НОД 17 → 1/3                       15/35: НОД 5 → 3/7
     91/119: НОД 7 → 13/17 (91 = 7·13, 119 = 7·17)   48/72: НОД 24 → 2/3
     основное свойство: 2/3 = (2·9)/(3·9) = 18/27
     несократимые: 7/9, 15/28, 121/144, 17/19 (НОД = 1)
     ЛОВУШКА 1: «зачеркнуть шестёрки» в 16/64 даёт 1/4 — совпало СЛУЧАЙНО;
       в 12/24 зачёркивание двойки даёт 1/4, а верный ответ 1/2 ✗
     ЛОВУШКА 2: (3 + 5)/(3 + 7) = 8/10 = 4/5 = 0,8; «сократить тройки» дало бы
       5/7 ≈ 0,714 — слагаемые сокращать нельзя ✗
     ЛОВУШКА 3: произведение (2 · 3)/(4 · 3) = 2/4 = 1/2 — множители можно ✓

   СТАНДАРТ: deploy/АНИМАЦИЯ_УРОКОВ.md, deploy/РИСОВАНИЕ_ФИГУР.md.
   Движение — сама математика: сетка плитки перестраивается в более крупную,
   дольки объединяются, зачёркнутые цифры гаснут. reduced-motion глушит SMIL. */
(function(){
  'use strict';

  const ID = 1039;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  const CSS=`
  #lvis .s6.l1039{gap:14px}
  #lvis .s6.l1039 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l1039 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l1039 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l1039 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l1039 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l1039 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l1039 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l1039 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l1039 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l1039 .правило b{color:${GOLD}}
  #lvis .s6.l1039 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l1039 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;
    font-size:16px;font-weight:600;line-height:1.35;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1039 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l1039 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l1039 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l1039 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l1039 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l1039 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l1039 .уровни .точка.сейчас{background:${GOLD};animation:l1039dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l1039dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l1039 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l1039{-webkit-text-size-adjust:100%}
  #lvis .s6.l1039 [data-anim]{animation:l1039rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l1039rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l1039 [data-anim]{animation:none!important}
    #lvis .s6.l1039 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l1039 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l1039-style');
      if(!s){ s=document.createElement('style'); s.id='l1039-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(String(ID)) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ПРАВИЛО2 = (ответ, до, после) => (ответ!=null) ? ПРАВИЛО(после) : ПРАВИЛО(до);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');
  const гаснет = (длит,доля,конец) => анК('opacity','1;1;0.2;0.2',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c1039-фон" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2b2420"/><stop offset="0.45" stop-color="#1d1815"/>
        <stop offset="1" stop-color="#100d0b"/>
      </linearGradient>
      <linearGradient id="c1039-шоколад" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#8a5a34"/><stop offset="0.45" stop-color="#5f3a1f"/>
        <stop offset="1" stop-color="#35200f"/>
      </linearGradient>
      <linearGradient id="c1039-долька" x1="0" y1="0" x2="0.4" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#b07a45"/><stop offset="0.5" stop-color="#8a5528"/>
        <stop offset="1" stop-color="#5a3517"/>
      </linearGradient>
      <linearGradient id="c1039-долька2" x1="0" y1="0" x2="0.4" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#c9a06a"/><stop offset="0.5" stop-color="#a2743f"/>
        <stop offset="1" stop-color="#6d4a22"/>
      </linearGradient>
      <linearGradient id="c1039-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <linearGradient id="c1039-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".6"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".22"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".2"/>
      </linearGradient>
      <radialGradient id="c1039-свет" cx="0.26" cy="0.12" r="0.8" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffe9c0" stop-opacity=".2"/>
        <stop offset="0.6" stop-color="#ffe9c0" stop-opacity=".05"/>
        <stop offset="1" stop-color="#000000" stop-opacity=".34"/>
      </radialGradient>
      <filter id="c1039-тень" x="-60%" y="-60%" width="220%" height="220%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3" result="р"/>
        <feOffset in="р" dx="3" dy="5" result="с"/>
        <feFlood flood-color="#120a04" flood-opacity="0.45" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c1039-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c1039-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c1039-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="url(#c1039-свет)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(12,8,4,.86)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#120a04" opacity=".34" filter="url(#c1039-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c1039-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  const бумага = (x,y,ш,в,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="6" fill="#120a04" opacity=".36" filter="url(#c1039-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="url(#c1039-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="none" filter="url(#c1039-шум)" opacity=".1"/>
  </g>`;

  /* ПЛИТКА ШОКОЛАДА: кл × ряд долек, нарезка клеткой «клетка» */
  function плитка(x,y,кл,ряд,клетка,нач,целая){
    const ш=кл*клетка, в=ряд*клетка;
    let дольки='';
    for(let i=0;i<кл;i++) for(let j=0;j<ряд;j++){
      дольки+=`<rect x="${x+i*клетка+1}" y="${y+j*клетка+1}" width="${клетка-2}" height="${клетка-2}" rx="2"
        fill="url(#c1039-${(i+j)%2?'долька':'долька2'})" stroke="rgba(40,22,8,.5)" stroke-width="0.8"/>`;
    }
    return `<g>
      <rect x="${x+3}" y="${y+5}" width="${ш}" height="${в}" rx="5" fill="#120a04" opacity=".5" filter="url(#c1039-тень)"/>
      ${целая?`<rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="5" fill="url(#c1039-шоколад)" stroke="${ОБВОД}" stroke-width="1.6"/>`:''}
      ${дольки}
      <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="5" fill="none" stroke="${ОБВОД}" stroke-width="1.6">
        ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
      <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="5" fill="none" filter="url(#c1039-шум)" opacity=".08"/>
    </g>`;
  }

  /* ГРУППИРОВКА: те же дольки, но обведены блоками «блок × блок» */
  function группировка(x,y,кл,ряд,клетка,блок,нач,цвет){
    const ш=кл*клетка, в=ряд*клетка;
    let рамки='';
    for(let i=0;i<кл;i+=блок) for(let j=0;j<ряд;j+=блок){
      рамки+=`<rect x="${x+i*клетка}" y="${y+j*клетка}" width="${блок*клетка}" height="${блок*клетка}"
        fill="none" stroke="${цвет||GOLD}" stroke-width="2.2" rx="3"/>`;
    }
    return `<g>${проявить('15s',нач,нач+0.05)}
      ${плитка(x,y,кл,ряд,клетка,null,true)}
      ${рамки}
    </g>`;
  }

  /* ДРОБЬ в рамке */
  const дробь = (x,y,верх,низ,цвет,нач,кегль) => {
    const к=кегль||20;
    return `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
      ${т(x,y-6,верх,к,цвет||ИНК,true)}
      <line x1="${x-22}" y1="${y+2}" x2="${x+22}" y2="${y+2}" stroke="${цвет||ИНК}" stroke-width="2"/>
      ${т(x,y+24,низ,к,цвет||ИНК,true)}
    </g>`;
  };

  /* ================= КАДРЫ ================= */

  /* 1. Разминка (НОД, урок 379) + сюжет */
  function F1(s){
    const в = s.разминка;
    return A(2,'cap','Разминка: прошлый урок про НОД') +
      ВОПРОС('Наибольший общий делитель чисел 24 и 15?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Та же плитка, другая нарезка',14,GOLD,true)}
        ${плитка(60,46,12,6,16,0.06,true)}
        ${подпись(168,168,'12 долек в ширину, 6 в высоту',МУТ,12)}
        <g>${проявить('15s',0.5,0.6)}
          ${стекло(24,182,288,42,0.5)}
          ${т(168,208,'дробь 12/6 — это часть плитки',12,ИНК,true)}</g>
        ${в!=null?`<g>${проявить('15s',0.7,0.82)}
          ${подпись(168,240,'НОД(24, 15) = 3',GREEN,14)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), '3 — это НОД(24, 15)', "r1039Разминка('три')")}
        ${BTN(4, в==='пять'?'miss':'', '5 — половина от 15', "r1039Разминка('пять')")}
        ${BTN(5, в==='девять'?'miss':'', '9 — это 24 − 15', "r1039Разминка('девять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: алгоритм Евклида даёт НОД(24, 15) = 3. Сегодня этот НОД поможет сокращать дроби.'
                  : 'Нет: вспомни алгоритм Евклида. 24 − 15 = 9, 15 − 9 = 6, 9 − 6 = 3 — значит НОД равен 3.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'На прошлом уроке мы искали наибольший общий делитель.',
                 'НОД(24, 15) = <b>3</b> — он нам сегодня понадобится.');
  }

  /* 2. Основное свойство дроби */
  function F2(s){
    const в = s.свойство;
    return ВОПРОС('Плитку 12 × 6 переупаковали в дольки 2 × 2. Изменилась ли дробь?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Основное свойство дроби',14,GOLD,true)}
        ${группировка(60,44,12,6,16,2,0.05,BLUE)}
        <g>${проявить('15s',0.42,0.52)}
          ${стекло(24,156,288,40,0.42)}
          ${т(168,180,'долек вчетверо меньше, а плитка та же',12,ИНК,true)}</g>
        ${в!=null?`<g>${проявить('15s',0.6,0.72)}
          ${дробь(120,196,'12','6',GOLD,0.6,20)}
          ${т(150,214,'=',20,ИНК,true)}
          ${дробь(200,196,'2','1',GREEN,0.68,20)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нет'?'hit':(в?'miss':''), 'Дробь та же: 12/6 = 2/1 = 2', "r1039Свойство('нет')")}
        ${BTN(4, в==='да'?'miss':'', 'Дробь уменьшилась вместе с дольками', "r1039Свойство('да')")}
        ${BTN(5, в==='выросла'?'miss':'', 'Дробь выросла, ведь долек стало больше', "r1039Свойство('выросла')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нет',
        в==='нет' ? 'Верно: числитель и знаменатель разделили на одно и то же число 6 — сама дробь не изменилась.'
                  : 'Нет: и числитель, и знаменатель делятся на одно число, поэтому значение дроби остаётся прежним.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Подумай, что происходит с числителем и знаменателем вместе.',
                 'Основное свойство: <b>числитель и знаменатель делим на одно число</b>.');
  }

  /* 3. Сокращение — самообъяснение */
  function F3(s){
    const в = s.сокращение;
    return ВОПРОС('Почему 12/18 можно заменить на 2/3?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Что такое сокращение',14,GOLD,true)}
        ${в!=null?`<g>${проявить('15s',0.06,0.16)}
          ${дробь(90,48,'12','18',GOLD,0.06,18)}
          ${т(126,72,'=',20,ИНК,true)}
          ${дробь(166,48,'6','9',BLUE,0.14,20)}
          ${т(202,72,'=',20,ИНК,true)}
          ${дробь(242,48,'2','3',GREEN,0.22,20)}</g>`:''}
        ${группировка(72,84,12,6,16,3,0.3,GOLD)}
        <g>${проявить('15s',0.56,0.66)}
          ${стекло(24,192,288,48,0.56)}
          ${т(168,212,'делим верх и низ на общий делитель',12,ИНК,true)}
          ${т(168,230,'и дробь записывается проще',12,МУТ)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='делим'?'hit':(в?'miss':''), 'Числитель и знаменатель делятся на общий делитель 6', "r1039Сокращ('делим')")}
        ${BTN(4, в==='вычитаем'?'miss':'', 'Из знаменателя вычитается числитель: 18 − 12 = 6', "r1039Сокращ('вычитаем')")}
        ${BTN(5, в==='зачёркиваем'?'miss':'', 'В записи 12/18 зачёркиваем одинаковые цифры', "r1039Сокращ('зачёркиваем')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='делим',
        в==='делим' ? 'Верно: 12 = 6 · 2 и 18 = 6 · 3, поэтому 12/18 = 2/3. Сокращение — это деление на общий делитель.'
                    : 'Нет: вычитать и зачёркивать цифры нельзя. Сокращение — это деление числителя и знаменателя на общий делитель.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Вспомни основное свойство дроби.',
                 'Сократить — значит <b>разделить верх и низ на общий делитель</b>.');
  }

  /* 4. «Я»: 12/18 через НОД */
  function F4(s){
    const в = s.я;
    return A(2,'cap','Я решаю: сокращаю 12/18') +
      ВОПРОС('Как сократить 12/18 сразу до несократимой дроби?') +
      `<div class="pic">${свг(`
        ${фон(272)}
        ${т(168,24,'Сразу на НОД',14,GOLD,true)}
        ${в!=null?`<g>${проявить('15s',0.06,0.16)}
          ${бумага(24,38,288,44,0.06,-0.4)}
          ${т(168,58,'НОД(12, 18) = 6',14,'#2b2110',true)}
          ${т(168,76,'12 : 6 = 2,  18 : 6 = 3',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.24,0.36)}
          ${дробь(120,104,'12','18',МУТ,0.24,20)}
          ${т(150,128,'=',20,ИНК,true)}
          ${дробь(200,104,'2','3',GREEN,0.32,20)}</g>`:''}
        ${группировка(78,136,12,6,15,6,0.4,GOLD)}
        <g>${проявить('15s',0.6,0.7)}
          ${стекло(24,234,288,30,0.6)}
          ${т(168,254,'проверка: 12 : 18 = 2 : 3',12,МУТ)}</g>
      `,272)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нод'?'hit':(в?'miss':''), 'Найти НОД(12, 18) = 6 и разделить оба числа', "r1039Я('нод')")}
        ${BTN(4, в==='два'?'miss':'', 'Разделить оба числа на 2, и всё', "r1039Я('два')")}
        ${BTN(5, в==='вычесть'?'miss':'', 'Вычесть из знаменателя числитель', "r1039Я('вычесть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нод',
        в==='нод' ? 'Верно: НОД сразу даёт несократимую дробь. 12 : 6 = 2, 18 : 6 = 3, значит 12/18 = 2/3.'
                  : 'Нет: делить на 2 можно, но получится 6/9 — ещё сократимая дробь. НОД делает работу за один шаг.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Выбери делитель, который сразу даст несократимую дробь.',
                 '12/18 = <b>2/3</b>: делим на НОД 6.');
  }

  /* 5. «Мы»: 16/64, шаг пропущен */
  function F5(s){
    const в = s.мы;
    return A(2,'cap','Мы решаем вместе: шаг пропущен') +
      ВОПРОС('Сокращаем 16/64. НОД равен 16. Что впишем в пропуск: 64 : ? = ?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Дописываем шаг',14,GOLD,true)}
        ${в!=null?`<g>${проявить('15s',0.06,0.16)}
          ${бумага(24,38,288,44,0.06,-0.4)}
          ${т(168,58,'НОД(16, 64) = 16',14,'#2b2110',true)}
          ${т(168,76,'16 : 16 = 1',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.26,0.38)}
          ${дробь(120,104,'16','64',МУТ,0.26,20)}
          ${т(150,128,'=',20,ИНК,true)}
          ${дробь(200,104,'1','4',GREEN,0.34,20)}</g>`:''}
        ${группировка(60,150,16,4,14,4,0.44,BLUE)}
        ${в==null?`<g>${проявить('15s',0.3,0.4)}
          ${стекло(24,214,288,30,0.3)}
          ${т(168,234,'на что разделить 64?',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шестнадцать'?'hit':(в?'miss':''), 'На 16: 64 : 16 = 4, значит 16/64 = 1/4', "r1039Мы('шестнадцать')")}
        ${BTN(4, в==='шесть'?'miss':'', 'На 6: зачеркнуть шестёрки в записи', "r1039Мы('шесть')")}
        ${BTN(5, в==='тридцатьдва'?'miss':'', 'На 32: взять половину знаменателя', "r1039Мы('тридцатьдва')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шестнадцать',
        в==='шестнадцать' ? 'Верно: 16 : 16 = 1 и 64 : 16 = 4, поэтому 16/64 = 1/4. Проверим: 0,25 = 0,25.'
                          : 'Нет: цифры зачёркивать нельзя, а 6 не делит 16. Делим на НОД 16: получится 1/4.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Дели оба числа на один и тот же делитель.',
                 '16/64 = <b>1/4</b> (делим на 16).');
  }

  /* 6. «Ты»: 45/60 */
  function F6(s){
    const в = s.ты;
    return A(2,'cap','Теперь ты: сократи сам') +
      ВОПРОС('Сократи дробь 45/60 до несократимой.') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Решаем самостоятельно',14,GOLD,true)}
        ${в!=null?`<g>${проявить('15s',0.06,0.16)}
          ${бумага(24,38,288,44,0.06,-0.4)}
          ${т(168,58,'НОД(45, 60) = 15',14,'#2b2110',true)}
          ${т(168,76,'45 : 15 = 3,  60 : 15 = 4',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.26,0.38)}
          ${дробь(120,100,'45','60',МУТ,0.26,20)}
          ${т(150,124,'=',20,ИНК,true)}
          ${дробь(200,100,'3','4',GREEN,0.34,20)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.28,0.38)}
          ${стекло(24,196,288,30,0.28)}
          ${т(168,216,'сначала найди НОД, потом дели',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тричетвёртых'?'hit':(в?'miss':''), '3/4: НОД = 15, 45 : 15 = 3, 60 : 15 = 4', "r1039Ты('тричетвёртых')")}
        ${BTN(4, в==='девятьдвенадцатых'?'miss':'', '9/12: разделил только на 5', "r1039Ты('девятьдвенадцатых')")}
        ${BTN(5, в==='пятнадцатьдвадцатых'?'miss':'', '15/20: разделил только на 3', "r1039Ты('пятнадцатьдвадцатых')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тричетвёртых',
        в==='тричетвёртых' ? 'Верно: НОД(45, 60) = 15, поэтому 45/60 = 3/4. Проверка: 0,75 = 0,75.'
                           : 'Нет: 9/12 и 15/20 ещё сократимы. Дели сразу на НОД 15 — получится 3/4.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Не забудь довести дробь до несократимой.',
                 '45/60 = <b>3/4</b> (НОД = 15).');
  }

  /* 7. Ловушка: зачёркивание цифр */
  function F7(s){
    const в = s.ловушка;
    return ВОПРОС('Ученик сократил 12/24 так: зачеркнул двойки и получил 1/4. Что не так?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Ловушка: зачёркивание цифр',14,GOLD,true)}
        <g>${проявить('15s',0.05,0.15)}
          ${бумага(24,40,288,48,0.05,-0.5)}
          ${т(120,62,'12',20,'#2b2110',true)}
          <line x1="80" y1="68" x2="240" y2="68" stroke="#2b2110" stroke-width="2"/>
          ${т(120,92,'24',20,'#2b2110',true)}
          <line x1="132" y1="48" x2="168" y2="96" stroke="${RED}" stroke-width="2.4">
            ${гаснет('15s',0.2,0.3)}</line>
          ${т(206,80,'→ 1/4',20,RED,true)}</g>
        ${в!=null?`<g>${проявить('15s',0.3,0.42)}
          ${стекло(24,100,288,40,0.3)}
          ${т(168,118,'12 : 24 = 0,5,',12,ИНК,true)}
          ${т(168,134,'а 1 : 4 = 0,25 — разные числа',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${бумага(24,150,288,44,0.5,0.4)}
          ${т(168,170,'НОД(12, 24) = 12',14,'#2b2110',true)}
          ${т(168,186,'12 : 12 = 1, 24 : 12 = 2 → 1/2',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.68,0.8)}
          ${подпись(168,222,'верный ответ 1/2, а не 1/4',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нельзя'?'hit':(в?'miss':''), 'Зачёркивать цифры нельзя: 12/24 = 1/2', "r1039Лов('нельзя')")}
        ${BTN(4, в==='можно'?'miss':'', 'Всё верно: 12/24 действительно равно 1/4', "r1039Лов('можно')")}
        ${BTN(5, в==='надо6'?'miss':'', 'Надо было разделить на 6: получится 2/4', "r1039Лов('надо6')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нельзя',
        в==='нельзя' ? 'Верно: зачёркивание цифр — не сокращение. 12 : 24 = 0,5, значит верная дробь 1/2, а НОД равен 12.'
                    : 'Нет: проверьте делением. 12 : 24 = 0,5, а 1 : 4 = 0,25. Верный ответ 1/2.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Проверь ответ делением.',
                 'Зачёркивать цифры <b>нельзя</b>: 12/24 = 1/2.');
  }

  /* 8. Самообъяснение: слагаемые нельзя */
  function F8(s){
    const в = s.слагаемые;
    return ВОПРОС('Почему в выражении (3 + 5)/(3 + 7) нельзя сократить тройки?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Слагаемые и множители',14,GOLD,true)}
        ${бумага(24,40,288,46,0.05,-0.5)}
        ${т(168,68,'(3 + 5) / (3 + 7)',20,'#2b2110',true)}
        ${в!=null?`<g>${проявить('15s',0.2,0.32)}
          ${стекло(24,96,288,40,0.2)}
          ${т(168,114,'сверху 8, снизу 10',12,ИНК,true)}
          ${т(168,130,'значит дробь равна 4/5 = 0,8',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.4,0.52)}
          ${бумага(24,144,288,44,0.4,0.4)}
          ${т(168,164,'а «сокращение троек» дало бы 5/7 ≈ 0,71',12,'#6a5a3a')}
          ${т(168,180,'это другое число — так нельзя',14,'#2b2110',true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.6,0.72)}
          ${подпись(168,214,'сокращать можно только множители',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='слагаемые'?'hit':(в?'miss':''), '3 — это слагаемое, а сокращать можно только множители', "r1039Слаг('слагаемые')")}
        ${BTN(4, в==='нельзя0'?'miss':'', 'Потому что тройки стоят в разных скобках', "r1039Слаг('нельзя0')")}
        ${BTN(5, в==='можно'?'miss':'', 'На самом деле можно, получится 5/7', "r1039Слаг('можно')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='слагаемые',
        в==='слагаемые' ? 'Верно: делить на общее можно только множители. Слагаемые сокращать нельзя — дробь равна 8/10 = 4/5.'
                       : 'Нет: дело не в скобках. Сокращение работает для множителей, а здесь тройки — слагаемые.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Вспомни основное свойство: что именно делится на общий делитель?',
                 'Сокращать можно <b>только множители</b>, не слагаемые.');
  }

  /* 9. Проверка: несократима ли дробь */
  function F9(s){
    const в = s.проверка;
    return ВОПРОС('Как узнать, что дробь 7/9 уже нельзя сократить?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Проверка на несократимость',14,GOLD,true)}
        ${/* нейтрально: сами дроби, без вывода про НОД */''}
        <g>${проявить('15s',0.04,0.14)}
          ${дробь(120,104,'7','9',МУТ,0.04,20)}
          ${т(150,128,'и',16,МУТ,true)}
          ${дробь(200,104,'15','28',МУТ,0.1,20)}
          ${стекло(24,166,288,30,0.14)}
          ${т(168,186,'сократимы ли эти дроби?',12,МУТ)}</g>
        ${в!=null?`<g>${проявить('15s',0.06,0.18)}
          ${бумага(24,40,288,44,0.06,-0.4)}
          ${т(168,60,'НОД(7, 9) = 1',16,'#2b2110',true)}
          ${т(168,76,'общих делителей, кроме 1, нет',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.3,0.42)}
          ${дробь(120,104,'7','9',GREEN,0.3,20)}
          ${т(150,128,'и',16,МУТ,true)}
          ${дробь(200,104,'15','28',GREEN,0.38,20)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.52,0.64)}
          ${стекло(24,160,288,42,0.52)}
          ${т(168,180,'если НОД равен 1,',12,ИНК,true)}
          ${т(168,196,'дробь уже несократима',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.7,0.82)}
          ${подпись(168,228,'такие числа называют взаимно простыми',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нод1'?'hit':(в?'miss':''), 'Найти НОД: если он равен 1, дробь несократима', "r1039Пров('нод1')")}
        ${BTN(4, в==='нечёт'?'miss':'', 'Числа 7 и 9 нечётные, поэтому сократить нельзя', "r1039Пров('нечёт')")}
        ${BTN(5, в==='мало'?'miss':'', 'Дробь слишком маленькая для сокращения', "r1039Пров('мало')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нод1',
        в==='нод1' ? 'Верно: НОД(7, 9) = 1, значит общих делителей нет и дробь несократима. Числа 7 и 9 взаимно простые.'
                  : 'Нет: нечётность не при чём — 9/15 тоже нечётные, но сокращаются на 3. Проверяем НОД: он равен 1.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Подумай, чем проверить дробь.',
                 'Дробь несократима, если <b>НОД(числителя, знаменателя) = 1</b>.');
  }

  /* 10. Большие числа: 91/119 через Евклид */
  function F10(s){
    const в = s.большие;
    return ВОПРОС('Сократи 91/119. Чему равен НОД?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Большие числа: снова Евклид',14,GOLD,true)}
        ${/* нейтрально: дробь и вопрос, без ответа */''}
        <g>${проявить('15s',0.04,0.14)}
          ${дробь(168,146,'91','119',МУТ,0.04,20)}
          ${стекло(24,186,288,30,0.1)}
          ${т(168,206,'какой общий делитель у этих чисел?',12,МУТ)}</g>
        ${в!=null?`<g>${проявить('15s',0.06,0.18)}
          ${бумага(24,38,288,60,0.06,-0.4)}
          ${т(168,58,'119 : 91 = 1 (ост. 28)',12,'#2b2110',true)}
          ${т(168,76,'91 : 28 = 3 (ост. 7)',12,'#2b2110',true)}
          ${т(168,94,'28 : 7 = 4 (ост. 0)',12,'#2b2110',true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.3,0.42)}
          ${дробь(120,124,'91','119',МУТ,0.3,20)}
          ${т(150,148,'=',20,ИНК,true)}
          ${дробь(200,124,'13','17',GREEN,0.38,20)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.52,0.62)}
          ${стекло(24,168,288,40,0.52)}
          ${т(168,188,'91 = 7 · 13,  119 = 7 · 17',12,ИНК,true)}
          ${т(168,204,'общий множитель только 7',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='семь'?'hit':(в?'miss':''), 'НОД = 7: 91 : 7 = 13, 119 : 7 = 17', "r1039Бол('семь')")}
        ${BTN(4, в==='тринадцать'?'miss':'', 'НОД = 13: тогда 91 : 13 = 7', "r1039Бол('тринадцать')")}
        ${BTN(5, в==='один'?'miss':'', 'НОД = 1, дробь несократима', "r1039Бол('один')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='семь',
        в==='семь' ? 'Верно: алгоритм Евклида даёт НОД = 7, значит 91/119 = 13/17 — дробь несократима.'
                   : 'Нет: проверь алгоритмом Евклида. 119 − 91 = 28, 91 − 28·3 = 7, 28 − 7·4 = 0 — НОД равен 7.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Примени алгоритм Евклида к большим числам.',
                 '91/119 = <b>13/17</b> — НОД равен 7.');
  }

  /* 11. Сводка */
  function F11(s){
    const в = s.сводка;
    return ВОПРОС('Как сократить дробь до конца и не ошибиться?') +
      `<div class="pic">${свг(`
        ${фон(266)}
        ${т(168,24,'Порядок действий',14,GOLD,true)}
        ${['найди НОД числителя и знаменателя','раздели оба числа на НОД','проверь делением: значения равны','если НОД = 1 — дробь несократима']
          .map((с,i)=>`<g>${проявить('15s',0.05+i*0.08,0.14+i*0.08)}
            <rect x="24" y="${38+i*34}" width="288" height="28" rx="7"
              fill="${i===1?'rgba(255,215,106,.16)':'rgba(255,255,255,.06)'}" stroke="${i===1?GOLD:ЛИНИЯ}" stroke-width="1.1"/>
            ${т(168,57+i*34,(i+1)+'. '+с,12,ИНК,true)}</g>`).join('')}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${подпись(168,222,'слагаемые и цифры не сокращаем',RED,12)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.7,0.82)}
          ${стекло(24,232,288,28,0.7)}
          ${т(168,250,'ответ всегда проверяй делением',12,МУТ)}</g>`:''}
      `,266)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нод'?'hit':(в?'miss':''), 'Найти НОД и разделить на него оба числа, потом проверить', "r1039Свод('нод')")}
        ${BTN(4, в==='сразу'?'miss':'', 'Разделить на 2 и остановиться', "r1039Свод('сразу')")}
        ${BTN(5, в==='цифры'?'miss':'', 'Зачеркнуть одинаковые цифры в записи', "r1039Свод('цифры')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нод',
        в==='нод' ? 'Верно: НОД сразу даёт несократимую дробь, а деление подтверждает ответ. Цифры и слагаемые не трогаем.'
                  : 'Нет: деление на 2 может оставить дробь сократимой, а зачёркивание цифр вообще не сокращение. Нужен НОД.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Выбери надёжный способ.',
                 'Сокращение: <b>делим на НОД и проверяем делением</b>.');
  }

  /* 12. Практика: интерливинг + выходной билет */
  const УРОВНИ=[
    { тип:'новая', вопрос:'Сократи 24/36.', варианты:[
        {т:'2/3', ок:true,  почему:'НОД(24, 36) = 12; 24 : 12 = 2, 36 : 12 = 3.'},
        {т:'12/18', ок:false, почему:'Разделил только на 2 — дробь ещё сократима.'},
        {т:'4/6', ок:false, почему:'Разделил на 6 — тоже не до конца.'}]},
    { тип:'новая', вопрос:'Сократи 15/35.', варианты:[
        {т:'3/7', ок:true,  почему:'НОД = 5; 15 : 5 = 3, 35 : 5 = 7.'},
        {т:'5/7', ок:false, почему:'Это 15 : 3 и 35 : 5 — делить надо на одно число.'},
        {т:'1/2', ок:false, почему:'15/35 ≠ 1/2: проверь делением 0,43 против 0,5.'}]},
    { тип:'прошлая', вопрос:'Вспомним прошлый урок: НОД(48, 18) = ?', варианты:[
        {т:'6', ок:true,  почему:'48 : 18 = 2 (12); 18 : 12 = 1 (6); 12 : 6 = 2 (0).'},
        {т:'3', ок:false, почему:'3 — общий делитель, но не наибольший.'},
        {т:'9', ок:false, почему:'9 не делит 48.'}]},
    { тип:'смешанная', вопрос:'Сократи 17/51 и сравни с 1/3.', варианты:[
        {т:'1/3, они равны', ок:true,  почему:'51 = 3 · 17, НОД = 17.'},
        {т:'17/51 несократима', ок:false, почему:'17 и 51 имеют общий делитель 17.'},
        {т:'1/4, это больше 1/3', ок:false, почему:'17 : 51 = 1 : 3, а не 1 : 4.'}]},
    { тип:'обёртка', вопрос:'В рецепте 12/18 стакана сахара. Как записать это проще?', варианты:[
        {т:'2/3 стакана', ок:true,  почему:'НОД(12, 18) = 6.'},
        {т:'1/2 стакана', ок:false, почему:'12/18 = 0,667, а 1/2 = 0,5.'},
        {т:'6/9 стакана', ок:false, почему:'Сократили, но не до конца.'}]}
  ];
  function F12(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Выходной билет: придумай дробь, которая сокращается на 7, и запиши её несократимый вид.') +
        РАЗБОР(true,'Например, 14/21 = 2/3 или 35/49 = 5/7: числитель и знаменатель делятся на 7.') +
        `<div class="ask">${BTN(4,'','пройти заново','r1039Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const порядок=сдвиг(ур.варианты, и+1);
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length+' · '+ур.тип) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + порядок.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r1039Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(порядок[выбран] && порядок[выбран].ок, (порядок[выбран]||{}).почему||'') : ЖДЁТ()) +
      ПРАВИЛО('Дели верх и низ на <b>НОД</b> и проверяй ответ делением.');
  }

  /* 13–15. Тренажёры */
  const Т1={
    имя:'Тренажёр 1 · сокращаем',
    задания:[
      { ф:'Сократи 12/18.', о:['2/3','6/9','1/2'], в:0, р:'НОД = 6.' },
      { ф:'Сократи 16/64.', о:['1/4','1/2','4/16'], в:0, р:'НОД = 16.' },
      { ф:'Сократи 45/60.', о:['3/4','9/12','4/5'], в:0, р:'НОД = 15.' },
      { ф:'Сократи 48/72.', о:['2/3','4/6','6/8'], в:0, р:'НОД = 24.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · несократима ли',
    задания:[
      { ф:'НОД(7, 9) = 1. Что это значит?', о:['дробь 7/9 несократима','дробь сократима','ошибка'], в:0, р:'Общих делителей нет.' },
      { ф:'Сократима ли 15/28?', о:['нет, НОД = 1','да, на 7','да, на 4'], в:0, р:'15 = 3·5, 28 = 4·7.' },
      { ф:'Сократима ли 91/119?', о:['да, на 7 → 13/17','нет','да, на 13'], в:0, р:'91 = 7·13, 119 = 7·17.' },
      { ф:'Сократима ли 17/51?', о:['да, на 17 → 1/3','нет','да, на 3'], в:0, р:'51 = 3 · 17.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · разбор чужой записи',
    задания:[
      { ф:'Ученик зачеркнул двойки в 12/24 и получил 1/4. Что не так?', о:['верно 1/2: цифры зачёркивать нельзя','всё верно','надо было получить 1/3'], в:0, р:'12 : 24 = 0,5.' },
      { ф:'Ученик сократил (3 + 5)/(3 + 7) на 3 и получил 5/7. Что не так?', о:['слагаемые сокращать нельзя, верно 4/5','всё верно','надо было на 5'], в:0, р:'8/10 = 4/5.' },
      { ф:'Ученик сократил 45/60 на 5 и получил 9/12. Что не так?', о:['дробь ещё сократима: верно 3/4','всё верно','надо было на 3'], в:0, р:'9/12 = 3/4.' },
      { ф:'Ученик сказал, что 15/28 сократима на 7. Что не так?', о:['15 на 7 не делится: НОД = 1','всё верно','надо было на 4'], в:0, р:'15 = 3 · 5.' }
    ]
  };

  /* сдвиг вариантов: верный ответ не всегда первый */
  function сдвиг(варианты, ключ){
    const н=варианты.length; if(н<2) return варианты;
    const к=((ключ*7+3)%н);
    return варианты.slice(к).concat(варианты.slice(0,к));
  }

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const порядок=сдвиг(з.о.map((о,к)=>({о,к})), шаг+1).map(х=>х.о);
    const верный=порядок.indexOf(з.о[з.в]);
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===верный;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + порядок.map((о,к)=>
        BTN(4+к, готово&&к===верный?'hit':(готово&&к===отв?'miss':''), о, `r1039T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r1039TNext('${ключ}')`)}</div>` : '');
  }
  function F13(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F14(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F15(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L1039={
    id: ID, title:'Сокращение дробей: НОД в деле', ico:'🍫',
    src:'Математика · 6 класс · Дроби', subj:'math',
    explain: [
      'Начнём с разминки по прошлому уроку: наибольший общий делитель чисел 24 и 15 равен 3 — это дал алгоритм Евклида. Сегодня он поработает по-настоящему: с его помощью мы будем сокращать дроби. Сюжет простой: у нас шоколадная плитка из 12 долек в ширину и 6 в высоту. Если переупаковать её в более крупные дольки, сама плитка не изменится — изменится только нарезка.',

      'Посмотрим на плитку 12 на 6 и обведём дольки блоками 2 на 2. Долек стало вчетверо меньше, но плитка осталась той же. Это и есть основное свойство дроби: если числитель и знаменатель разделить на одно и то же число, значение дроби не меняется. Дробь 12/6 равна дроби 2/1, то есть двум. Меняется запись, а не число.',

      'Сократить дробь — значит разделить числитель и знаменатель на их общий делитель. Возьмём 12/18. Числа 12 и 18 делятся на 6: получаем 2/3. Если делить по шагам (сначала на 2, потом на 3), придём к тому же результату, но за два действия. Важно понять, чем сокращение не является: это не вычитание и не зачёркивание цифр, а именно деление обоих чисел на одно и то же.',

      'Самый быстрый путь — сразу найти наибольший общий делитель. Для 12 и 18 это 6: делим 12 на 6 и получаем 2, делим 18 на 6 и получаем 3. Дробь 12/18 превращается в 2/3, и эта дробь уже несократима, потому что у 2 и 3 нет общих делителей. Проверим делением: 12 : 18 = 0,666… и 2 : 3 = 0,666… — значения совпали.',

      'Теперь вместе: сократим 16/64. Наибольший общий делитель здесь 16, потому что 64 = 4 · 16. Делим: 16 : 16 = 1, 64 : 16 = 4. Получаем 1/4. Проверка: 16 : 64 = 0,25 и 1 : 4 = 0,25. Я специально не стал делить сначала на 2 или на 4 — сразу на НОД, чтобы сделать всё одним действием.',

      'Попробуй сам: сократи 45/60. Сначала найди наибольший общий делитель. Подсказка: 45 = 15 · 3, а 60 = 15 · 4, значит НОД равен 15. Теперь раздели: 45 : 15 = 3 и 60 : 15 = 4. Получается 3/4 — несократимая дробь. Проверь делением: 45 : 60 = 0,75 и 3 : 4 = 0,75. Если у тебя получилось 9/12 или 15/20, значит ты остановился на шаг раньше.',

      'Разберём ловушку, в которую попадают многие. Ученик «сократил» 12/24 так: зачеркнул одинаковые цифры и получил 1/4. Проверим делением: 12 : 24 = 0,5, а 1 : 4 = 0,25 — это разные числа, значит ответ неверный. На самом деле НОД(12, 24) = 12, поэтому 12 : 12 = 1 и 24 : 12 = 2, а верная дробь — 1/2. Отдельно заметим: в дроби 16/64 зачёркивание шестёрок случайно даёт правильный ответ 1/4, но так совпадает далеко не всегда, и полагаться на это нельзя.',

      'Ещё одна важная тонкость: сокращать можно только множители, но не слагаемые. Посмотрим на выражение (3 + 5) / (3 + 7). Сверху получается 8, снизу 10, значит дробь равна 8/10, то есть 4/5 — примерно 0,8. Если же «сократить» тройки, выйдет 5/7, то есть примерно 0,71. Это другое число, и такой приём недопустим. Сокращение работает потому, что мы делим общий множитель, а слагаемые на множители не раскладываются.',

      'Как понять, что дробь уже несократима? Нужно найти НОД числителя и знаменателя. Если он равен единице, общих делителей нет, и дробь сократить нельзя. Например, у 7/9 НОД равен 1, у 15/28 тоже: 15 = 3 · 5, а 28 = 4 · 7 — общих множителей нет. Такие числа называют взаимно простыми. Обратите внимание: нечётность здесь не при чём — 9 и 15 оба нечётные, но сокращаются на 3.',

      'Проверим на больших числах, где пригодится алгоритм Евклида. Сократим 91/119. Применим деление с остатком: 119 : 91 = 1 и остаток 28; 91 : 28 = 3 и остаток 7; 28 : 7 = 4 и остаток 0. Последний ненулевой остаток — 7, это и есть НОД. Проверим разложением: 91 = 7 · 13, 119 = 7 · 17. Делим: 91 : 7 = 13 и 119 : 7 = 17. Получаем 13/17 — несократимую дробь.',

      'Соберём порядок действий. Первое: найти наибольший общий делитель числителя и знаменателя — делением с остатком или разложением на множители. Второе: разделить на него оба числа. Третье: проверить ответ делением — значения исходной и новой дроби должны совпадать. И помните два запрета: нельзя зачёркивать цифры и нельзя сокращать слагаемые. Если НОД равен 1, дробь уже несократима.',

      'Пройти практику: пять уровней. Первые два — сокращение, третий вернёт нас к прошлому уроку про НОД, четвёртый смешает темы, а пятый спрячет сокращение в рецепт. В конце — выходной билет: придумай дробь, которая сокращается на 7, и запиши её несократимый вид.',

      'Первый тренажёр — сокращаем дроби, каждый раз через НОД.',

      'Второй тренажёр — проверяем, сократима ли дробь, и находим общий делитель.',

      'Третий тренажёр — разбор чужой записи: в каждой задаче ученик ошибся, и нужно назвать, что именно он сделал не так.'
    ],
    check: {
      q:'Сократи дробь 12/18.',
      choices:['2/3','6/9','1/2'],
      ans:0,
      exp:'НОД(12, 18) = 6; 12 : 6 = 2, 18 : 6 = 3, значит 2/3.'
    },
    tasks: [
      { q:'Сократи 45/60.', kind:'choice',
        choices:['3/4','9/12'], ans:0,
        hints:['Найди НОД.','45 = 15 · 3, 60 = 15 · 4.'],
        sol:'3/4.' },
      { q:'Почему 12/24 нельзя «сократить» зачёркиванием двоек?', kind:'choice',
        choices:['12 : 24 = 0,5, а 1 : 4 = 0,25','Потому что двойки стоят в разных числах'], ans:0,
        hints:['Проверь делением.','Верная дробь — 1/2.'],
        sol:'Зачёркивание меняет значение дроби.' },
      { q:'Сократима ли дробь 15/28?', kind:'choice',
        choices:['Нет, НОД = 1','Да, на 7'], ans:0,
        hints:['15 = 3 · 5.','28 = 4 · 7.'],
        sol:'Несократима.' },
      { q:'Чему равен НОД(91, 119)?', kind:'choice',
        choices:['7','13'], ans:0,
        hints:['Примени алгоритм Евклида.','119 : 91 = 1 (28); 91 : 28 = 3 (7); 28 : 7 = 4 (0).'],
        sol:'7, значит 91/119 = 13/17.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L1039.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else if(f===14) сцена=F14(s);
    else сцена=F15(s);

    const ЗАГОЛОВКИ={
      1:['Разминка','НОД и шоколадная плитка'],
      2:['Основное свойство','Та же плитка, крупнее дольки'],
      3:['Что такое сокращение','Делим на общий делитель'],
      4:['Я решаю','12/18 через НОД'],
      5:['Мы решаем','16/64, шаг пропущен'],
      6:['Ты решаешь','45/60 сам'],
      7:['Ловушка','Зачёркивать цифры нельзя'],
      8:['Почему так','Слагаемые и множители'],
      9:['Проверка','Несократима ли дробь'],
      10:['Большие числа','91/119 и Евклид'],
      11:['Сводка','Порядок действий'],
      12:['Практика','Пять уровней'],
      13:['Тренажёр 1','Сокращаем'],
      14:['Тренажёр 2','Несократима ли'],
      15:['Тренажёр 3','Разбор чужой записи']
    };
    const з=ЗАГОЛОВКИ[f]||['Сокращение дробей','НОД в деле'];
    el.innerHTML = `<div class="s6 l1039" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r1039Разминка=(к)=>{ S().разминка=к; chRender(0); };
  window.r1039Свойство=(к)=>{ S().свойство=к; chRender(0); };
  window.r1039Сокращ=(к)=>{ S().сокращение=к; chRender(0); };
  window.r1039Я=(к)=>{ S().я=к; chRender(0); };
  window.r1039Мы=(к)=>{ S().мы=к; chRender(0); };
  window.r1039Ты=(к)=>{ S().ты=к; chRender(0); };
  window.r1039Лов=(к)=>{ S().ловушка=к; chRender(0); };
  window.r1039Слаг=(к)=>{ S().слагаемые=к; chRender(0); };
  window.r1039Пров=(к)=>{ S().проверка=к; chRender(0); };
  window.r1039Бол=(к)=>{ S().большие=к; chRender(0); };
  window.r1039Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r1039Pick=(уровень,вариант)=>{
    const s=S();
    const порядок=сдвиг(УРОВНИ[уровень].варианты, уровень+1);
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(порядок[вариант] && порядок[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r1039Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r1039T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    const порядок=сдвиг(з.о.map((о,к)=>({о,к})), шаг+1).map(х=>х.о);
    const верный=порядок.indexOf(з.о[з.в]);
    s[ключ+'Ответ']=вариант;
    if(вариант===верный) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r1039TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L1039; else arr.push(L1039);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA1039={render:render, L:L1039};
})();
