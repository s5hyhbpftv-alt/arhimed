/* ====== МАТЕМАТИКА · УРОК 23 · «ВРЕМЯ НА ЧАСАХ: МОДУЛЬ 24» ============================
   3–5 класс. Переделан с нуля по эталону 1022 (deploy/ЭТАЛОН_УРОКА.md), рисунки —
   сцена из сюжета. Прежняя версия (vis_23_393.js L23, lessons.js) остаётся;
   этот файл регистрируется поверх.

   ГЛАВНАЯ МЫСЛЬ. Сутки — круг из 24 часов: через 24 часа стрелка встаёт
   туда же. Лишние часы раскладываем как в делении с остатком: полные сутки
   выбрасываем, остаток прибавляем. Перевалило за 23 — вычти 24.

   СЮЖЕТ. «Ночной переход». Торговый корабль идёт через открытое море в
   Сиракузы. Юнга ведёт вахтенный журнал по большим корабельным «часам
   суток» — бронзовому кругу, где полночь наверху, а полдень внизу. Корабль
   должен войти в гавань к рассвету: днём у входа поднимают цепь.

   РУКАМИ: круг суток — кнопки «+1 ч», «+5 ч», «+24 ч», «сначала»: стрелка
   бежит по кругу, после полуночи счётчик суток прибавляется.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ:
     7:00 + 24 ч = 7:00; 7:00 + 50 ч: 50 = 24 · 2 + 2 → 9:00;
     3 часа дня = 15:00; 22:00 + 5 ч = 27 → 27 − 24 = 3:00;
     16:00 + 48 ч = 16:00; 7:00 + 100 ч: 100 = 24 · 4 + 4 → 11:00;
     2 суток 7 ч = 2 · 24 + 7 = 55 ч; 3:00 − 5 ч = −2 → 22:00;
     20:00 + 34 ч: 34 = 24 + 10, 20 + 10 = 30 → 6:00.

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md; в каждом кадре есть <animate>. */
(function(){
  'use strict';

  const ID = 23;
  const GOLD='#ffd76a', GREEN='#8fe0b0', RED='#ff8a78', ЛАЗУРЬ='#8fd0f0', ЛУНА='#b8c8f8';
  const ИНК='#f5efe2', МУТ='#b8bfcf', ЛИНИЯ='#4d5a80', ОБВОД='#0c0a08';

  const ДЕЛА = [
    {ключ:'прибытие', имя:'Рассчитать прибытие',     итог:'9:00'},
    {ключ:'полночь',  имя:'Вахта через полночь',     итог:'3:00'},
    {ключ:'сто',      имя:'Сто часов пути',          итог:'11:00'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const CSS=`
  #lvis .s6.l23{gap:14px}
  #lvis .s6.l23 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l23 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l23 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#1f2a44,#131a2e);border:1.5px solid var(--line)}
  #lvis .s6.l23 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l23 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l23 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l23 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l23 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l23 .карт .текст b{color:${GOLD}}
  #lvis .s6.l23 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l23 .правило b{color:${GOLD}}
  #lvis .s6.l23 .лист{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(184,200,248,.16),rgba(184,200,248,.04));
    border:1.5px solid rgba(184,200,248,.5);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l23 .лист .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l23 .лист .шапка b{font-size:20px;letter-spacing:0;text-transform:none;color:${ЛУНА}}
  #lvis .s6.l23 .лист .шапка b.готово{color:${GREEN}}
  #lvis .s6.l23 .лист ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l23 .лист li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l23 .лист li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;
    border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l23 .лист li.есть{color:${ИНК}}
  #lvis .s6.l23 .лист li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,224,176,.14)}
  #lvis .s6.l23 .лист li span{margin-left:auto;white-space:nowrap;font-variant-numeric:tabular-nums;color:var(--mut);font-size:14px}
  #lvis .s6.l23 .лист li.есть span{color:${GREEN}}
  #lvis .s6.l23 .ряд{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;width:100%}
  #lvis .s6.l23 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:18px;font-weight:700;
    border:1.5px solid rgba(184,200,248,.5);background:rgba(184,200,248,.12);color:${ИНК};padding:6px 4px;
    font-variant-numeric:tabular-nums;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l23 .ряд button:active{transform:translateY(2px);background:rgba(184,200,248,.26)}
  #lvis .s6.l23 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l23 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l23 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l23 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l23 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l23 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-variant-numeric:tabular-nums;font-size:18px}
  #lvis .s6.l23 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-variant-numeric:tabular-nums;font-size:18px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l23 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l23 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l23 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l23 .уровни .точка.сейчас{background:${GOLD};animation:l23dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l23dot{0%,100%{opacity:1}50%{opacity:.6}}
  #lvis .s6.l23 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l23{-webkit-text-size-adjust:100%}
  #lvis .s6.l23 [data-anim]{animation:l23rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l23rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l23 [data-anim]{animation:none!important}
    #lvis .s6.l23 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l23 button{transition:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l23-style');
      if(!s){ s=document.createElement('style'); s.id='l23-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,выкл) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}"${выкл?' disabled':''}>${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Вахтенный журнал</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,обр) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, обр+"("+к+")")).join('')}</div>`;
  const ЛИСТ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'лист',
      `<div class="шапка"><span>Переход в Сиракузы</span><b class="${всё?'готово':''}">${
        всё?'гавань к рассвету':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}
           <span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };
  const чч = (h) => h+':00';

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const анСдвиг = (значения,длит,keyTimes) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}"
              repeatCount="indefinite" calcMode="spline" keyTimes="${keyTimes}"
              keySplines="${сплайны(keyTimes.split(';').length-1)}"/>${ЗАВОД}` : '';
  const поворотРаз = (от,до,cx,cy,длит) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" from="${от} ${cx} ${cy}" to="${до} ${cx} ${cy}" dur="${длит}"
              fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>${ЗАВОД}` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const вырасти = (длит,доля) => анК('opacity','0.15;0.15;1;1',длит,'0;'+кт(доля)+';'+кт(доля+0.06)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c23-ночь" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#060a24"/><stop offset="0.6" stop-color="#141c48"/><stop offset="1" stop-color="#26306a"/>
      </linearGradient>
      <linearGradient id="c23-рассвет" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#1a2a5a"/><stop offset="0.45" stop-color="#8a6aa0"/><stop offset="0.8" stop-color="#f4a878"/><stop offset="1" stop-color="#ffd8a0"/>
      </linearGradient>
      <linearGradient id="c23-море" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#1a3060"/><stop offset="1" stop-color="#060e26"/>
      </linearGradient>
      <linearGradient id="c23-море-утро" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#5a6aa0"/><stop offset="1" stop-color="#1a2a5a"/>
      </linearGradient>
      <radialGradient id="c23-луна" cx="0.4" cy="0.4" r="0.6">
        <stop offset="0" stop-color="#ffffff"/><stop offset="1" stop-color="#b8c4ee"/>
      </radialGradient>
      <radialGradient id="c23-ореол" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#dfe6ff" stop-opacity=".45"/><stop offset="1" stop-color="#dfe6ff" stop-opacity="0"/>
      </radialGradient>
      <radialGradient id="c23-солнце" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#fffae0"/><stop offset="0.35" stop-color="#ffd070" stop-opacity=".9"/><stop offset="1" stop-color="#ff9a50" stop-opacity="0"/>
      </radialGradient>
      <radialGradient id="c23-фонарь" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#fff0b0" stop-opacity=".9"/><stop offset="0.4" stop-color="#ffa040" stop-opacity=".35"/><stop offset="1" stop-color="#ff7020" stop-opacity="0"/>
      </radialGradient>
      <linearGradient id="c23-бронза" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0" stop-color="#ffe6a8"/><stop offset="0.45" stop-color="#c98f3e"/><stop offset="1" stop-color="#5a3a10"/>
      </linearGradient>
      <radialGradient id="c23-циферблат-ночь" cx="0.5" cy="0.8" r="0.9">
        <stop offset="0" stop-color="#2a3a7a"/><stop offset="1" stop-color="#0c1236"/>
      </radialGradient>
      <radialGradient id="c23-циферблат-день" cx="0.5" cy="0.2" r="0.9">
        <stop offset="0" stop-color="#fff0c0"/><stop offset="1" stop-color="#e0a850"/>
      </radialGradient>
      <linearGradient id="c23-корпус" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#8a5a30"/><stop offset="0.5" stop-color="#5a3418"/><stop offset="1" stop-color="#2a1608"/>
      </linearGradient>
      <linearGradient id="c23-парус" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#c8bca0"/><stop offset="0.5" stop-color="#ece2c8"/><stop offset="1" stop-color="#b8aa88"/>
      </linearGradient>
      <linearGradient id="c23-пергамент" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f4e6c6"/><stop offset="1" stop-color="#d4ba86"/>
      </linearGradient>
      <filter id="c23-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="0" dy="2" stdDeviation="1.8" flood-color="#000" flood-opacity=".55"/>
      </filter>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.66+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c23-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(12,10,8,.9)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  const ночь = (Н,утро) => `<rect x="0" y="0" width="336" height="${Н}" fill="url(#c23-${утро?'рассвет':'ночь'})"/>
    ${утро?'':[[24,20],[70,44],[128,16],[200,36],[244,14],[300,50],[98,70],[168,58],[318,24],[40,86]].map(([x,y],i)=>
      `<circle cx="${x}" cy="${y}" r="${i%3?1:1.5}" fill="#fff">${анЛин('opacity','1;0.3;1',(2+i*0.37).toFixed(1)+'s')}</circle>`).join('')}`;
  const волны = (y,Н,утро) => `<rect x="0" y="${y}" width="336" height="${Н-y}" fill="url(#c23-море${утро?'-утро':''})"/>
    ${[0,1,2].map(i=>`<path d="M${-20+i*14} ${y+10+i*16} q30 -3 60 0 t60 0 t60 0 t60 0 t60 0 t60 0" fill="none" stroke="${утро?'#ffd8a0':'#9fc8ff'}" stroke-width="1" opacity="${(0.4-i*0.1).toFixed(2)}" stroke-dasharray="14 18">${анЛин('stroke-dashoffset','0;-32',(3+i*0.6).toFixed(1)+'s')}</path>`).join('')}`;
  /* торговый корабль: пузатый корпус, мачта, парус, фонарь на корме */
  const корабль = (x,y,м) => `<g transform="translate(${x} ${y}) scale(${м||1})" filter="url(#c23-тень)">
    <path d="M-50 -4 q6 18 30 20 h46 q20 -2 30 -20 l4 -4 h-114 z" fill="url(#c23-корпус)" stroke="${ОБВОД}" stroke-width=".7"/>
    <path d="M58 -8 q12 -6 10 -22" fill="none" stroke="#5a3418" stroke-width="3.4"/>
    <path d="M-52 -8 q-8 -10 -4 -22 q4 6 0 10" fill="none" stroke="#5a3418" stroke-width="3"/>
    <line x1="-46" y1="-2" x2="58" y2="-2" stroke="#c8944a" stroke-width="1"/>
    ${[-30,-10,10,30].map(d=>`<rect x="${d-3}" y="0" width="6" height="4" rx="1" fill="#2a1608"/>`).join('')}
    <line x1="0" y1="-8" x2="0" y2="-72" stroke="#3a2010" stroke-width="2.6"/>
    <line x1="-30" y1="-66" x2="30" y2="-66" stroke="#3a2010" stroke-width="2"/>
    <path d="M-28 -65 q28 8 56 0 v40 q-28 8 -56 0 z" fill="url(#c23-парус)" stroke="#8a7a5a" stroke-width=".6">
      ${анЛин('d','M-28 -65 q28 8 56 0 v40 q-28 8 -56 0 z;M-28 -65 q28 13 56 0 v40 q-28 13 -56 0 z;M-28 -65 q28 8 56 0 v40 q-28 8 -56 0 z','3.4s')}</path>
    <circle cx="0" cy="-48" r="9" fill="none" stroke="#8a2a1a" stroke-width="2.4" opacity=".8"/>
    ${[-20,0,20].map(d=>`<line x1="0" y1="-72" x2="${d*2.4}" y2="-8" stroke="#6a5a40" stroke-width=".5"/>`).join('')}
    <circle cx="-46" cy="-18" r="12" fill="url(#c23-фонарь)">${анЛин('r','10;14;10','1.4s')}</circle>
    <rect x="-49" y="-22" width="6" height="8" rx="1.5" fill="#ffd070" stroke="#6a4a10" stroke-width=".6"/></g>`;

  /* ---- круг суток: полночь наверху, полдень внизу; ночь — верхняя половина ---- */
  const угол = (ч) => -90 + ч*15;
  const точкаКруга = (cx,cy,r,ч) => { const a=угол(ч)*Math.PI/180; return [cx+r*Math.cos(a), cy+r*Math.sin(a)]; };
  const дуга = (cx,cy,r,ч1,ч2) => { const [x1,y1]=точкаКруга(cx,cy,r,ч1), [x2,y2]=точкаКруга(cx,cy,r,ч2);
    const шаг=((ч2-ч1)%24+24)%24; return `M${x1.toFixed(1)} ${y1.toFixed(1)} A${r} ${r} 0 ${шаг>12?1:0} 1 ${x2.toFixed(1)} ${y2.toFixed(1)}`; };
  function кругСуток(cx,cy,R,ч,опц){ const о=опц||{};
    const [xd1,yd1]=точкаКруга(cx,cy,R-6,6), [xd2,yd2]=точкаКруга(cx,cy,R-6,18);
    let s=`<g filter="url(#c23-тень)"><circle cx="${cx}" cy="${cy}" r="${R+8}" fill="url(#c23-бронза)"/></g>
      <circle cx="${cx}" cy="${cy}" r="${R-6}" fill="url(#c23-циферблат-ночь)"/>
      <path d="M${xd1.toFixed(1)} ${yd1.toFixed(1)} A${R-6} ${R-6} 0 0 1 ${xd2.toFixed(1)} ${yd2.toFixed(1)} Z" fill="url(#c23-циферблат-день)"/>
      <circle cx="${cx}" cy="${cy-R*0.32}" r="${R*0.1}" fill="url(#c23-луна)"/>
      <circle cx="${cx+R*0.06}" cy="${cy-R*0.34}" r="${R*0.08}" fill="#2a3a7a"/>
      <circle cx="${cx}" cy="${cy+R*0.32}" r="${R*0.1}" fill="#ffc040" stroke="#ffe890" stroke-width="1.5"/>
      ${Array.from({length:8},(_,i)=>{ const a=i*Math.PI/4, r1=R*0.13, r2=R*0.2; return `<line x1="${(cx+r1*Math.cos(a)).toFixed(1)}" y1="${(cy+R*0.32+r1*Math.sin(a)).toFixed(1)}" x2="${(cx+r2*Math.cos(a)).toFixed(1)}" y2="${(cy+R*0.32+r2*Math.sin(a)).toFixed(1)}" stroke="#ffc040" stroke-width="1.5"/>`; }).join('')}`;
    for(let h=0;h<24;h++){ const [x1,y1]=точкаКруга(cx,cy,R-6,h), [x2,y2]=точкаКруга(cx,cy,R-(h%3?13:18),h);
      s+=`<line x1="${x1.toFixed(1)}" y1="${y1.toFixed(1)}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}" stroke="${h>=6&&h<=18?'#6a4a10':'#c8d4ff'}" stroke-width="${h%3?1:2}"/>`; }
    if(!о.безЧисел) for(let h=0;h<24;h+=3){ const [x,y]=точкаКруга(cx,cy,R+(R>60?-28:-24),h);
      s+=т(x.toFixed(1),(y+4).toFixed(1),String(h),R>60?12:10,h>6&&h<18?'#5a3a08':'#e8ecff',true); }
    if(о.дуга) s+=`<path d="${о.дуга}" fill="none" stroke="${о.цветДуги||GOLD}" stroke-width="5" stroke-linecap="round" opacity=".85" stroke-dasharray="10 5">${анЛин('stroke-dashoffset','0;-30','1.2s')}</path>`;
    /* стрелка */
    const длина=R-20, от=о.от!=null?о.от:null;
    s+=`<g transform="rotate(${угол(ч)+90} ${cx} ${cy})">${от!=null?поворотРаз(угол(от)+90, угол(от)+90+(о.шаг||0)*15, cx, cy, '1.1s'):''}
        <path d="M${cx-4} ${cy+10} L${cx} ${cy-длина} L${cx+4} ${cy+10} Z" fill="url(#c23-бронза)" stroke="#3a2406" stroke-width=".8"/>
        <circle cx="${cx}" cy="${cy-длина+10}" r="3" fill="#ffe6a8"/></g>
      <circle cx="${cx}" cy="${cy}" r="7" fill="url(#c23-бронза)" stroke="#3a2406"/>`;
    return s;
  }

  /* ================= КАДРЫ ================= */

  /* 1. Ночной переход */
  function F1(s){
    const Н=320, в=s.ответ1;
    return ЛИСТ(s) +
      ЗАДАЧА('Торговый корабль идёт через открытое море в <b>Сиракузы</b>. Капитан доверил юнге вахтенный журнал и большие корабельные «<b>часы суток</b>»: бронзовый круг на 24 часа. Полночь — наверху, полдень — внизу, ночная половина тёмная, дневная — золотая. Стрелка обегает круг ровно за сутки.') +
      `<div class="pic">${свг(`
        ${ночь(Н)}
        <circle cx="286" cy="46" r="42" fill="url(#c23-ореол)" data-декор="1"/><circle cx="286" cy="46" r="15" fill="url(#c23-луна)"/>
        ${волны(206,Н)}
        <g>${анСдвиг('0 0;0 -4;0 0','3s','0;0.5;1')}${корабль(236,214,1.1)}</g>
        ${кругСуток(96,150,76,7)}
        ${подпись(96,Н-10, в===1?'полный круг — время то же':'сейчас 7:00 утра', в===1?GREEN:GOLD,13)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['31:00','7:00','8:00'],1,в,'r23Отв1') +
      (в==null ? СКАЗ('Вопрос','Сейчас <b>7:00</b>. Что покажут часы через <b>24 часа</b>?') :
        РАЗБОР(в===1, ['31 часа на круге нет: после 23 идёт снова 0. За 24 часа стрелка обегает <b>весь круг</b> и встаёт на 7.','Сутки — полный круг. Стрелка вернулась туда же: <b>7:00</b>, только уже завтра.','8:00 было бы через 25 часов. Через 24 — ровно круг: <b>7:00</b>.'][в])) +
      (в===1 ? ПРАВИЛО('<b>Сутки = 24 часа</b> = один полный круг. Через 24 часа время на часах то же.') : '');
  }

  /* 2. Покрути сам — прибытие */
  function F2(s){
    const Н=330, в=s.ответ2;
    const всего=s.всего2||0, ч=(7+всего)%24, сутки=Math.floor((7+всего)/24);
    const от=s.от2, шаг=s.шаг2||0;
    return ЛИСТ(s) +
      ЗАДАЧА('Капитан: «Вышли в <b>7:00</b>, до Сиракуз <b>50 часов</b> хода. Когда придём?» Юнга крутит стрелку. Попробуй сам: «+24 ч» — это целые сутки, «+1 ч» — один час.') +
      `<div class="pic">${свг(`
        ${ночь(Н)}
        ${волны(270,Н)}
        ${кругСуток(168,150,108,ч,{от:от, шаг:шаг})}
        ${т(20,24,'прошло: '+всего+' ч',15,ИНК,true,'start','#060a24')}
        ${т(316,24,'сутки: '+сутки,15,GOLD,true,'end','#060a24')}
        ${т(168,294,'на часах '+чч(ч),20,GOLD,true,undefined,'#060a24')}
        ${подпись(168,Н-10, всего===50?'50 = 24 + 24 + 2':'нажимай кнопки ниже', всего===50?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="ряд">${BTN(3,'','+1 ч',"r23Час(1)")}${BTN(3,'','+5 ч',"r23Час(5)")}</div>` +
      `<div class="ряд">${BTN(3,'','+24 ч',"r23Час(24)")}${BTN(3,'','↺ сначала',"r23Час(0)")}</div>` +
      ОТВЕТЫ('три',['57:00','9:00','7:00'],1,в,'r23Отв2') +
      (в==null ? СКАЗ('Вопрос','В котором часу корабль придёт в Сиракузы?') : РАЗБОР(в===1, ['57 часов на круге нет. 50 = 24 + 24 + 2: двое суток стрелка вернулась на 7, и ещё <b>2 часа</b> — 9:00.','50 = 24 · 2 + 2. Двое полных суток выбросили, остаток 2 прибавили: 7 + 2 = <b>9:00</b>.','7:00 было бы через 48 часов. Ещё 2 часа сверху: <b>9:00</b>.'][в])) +
      (в===1 ? ПРАВИЛО('Раздели часы на 24: <b>полные сутки выбрось, остаток прибавь</b>.') : '');
  }

  /* 3. 12 и 24 */
  function F3(s){
    const Н=270, в=s.ответ3;
    const двенадцать = (cx,cy,R,ч) => `<g filter="url(#c23-тень)"><circle cx="${cx}" cy="${cy}" r="${R+6}" fill="url(#c23-бронза)"/></g>
      <circle cx="${cx}" cy="${cy}" r="${R-2}" fill="#f4ecd8"/>
      ${Array.from({length:12},(_,i)=>{ const a=(-90+i*30)*Math.PI/180, [x,y]=[cx+(R-14)*Math.cos(a),cy+(R-14)*Math.sin(a)]; return т(x.toFixed(1),(y+4).toFixed(1),String(i||12),11,'#3a2410',true); }).join('')}
      <g transform="rotate(${ч*30} ${cx} ${cy})"><path d="M${cx-3} ${cy+6} L${cx} ${cy-R+26} L${cx+3} ${cy+6} Z" fill="#3a2410"/></g>
      <circle cx="${cx}" cy="${cy}" r="4" fill="#3a2410"/>`;
    return ЛИСТ(s) +
      ЗАДАЧА('Штурман кричит с кормы: «Маяк Пахин пройдём в <b>три часа дня</b>!» Юнга записывает в журнал по-корабельному, на 24 часа. На домашних часах круг на 12 — а у суток <b>два таких круга</b>: утренний и вечерний.') +
      `<div class="pic">${свг(`
        ${ночь(Н,true)}
        ${двенадцать(86,120,62,3)}
        ${кругСуток(250,120,62,15,{})}
        ${т(86,212,'часы на 12',13,ИНК,true,undefined,'#1a2a5a')}${т(250,212,'часы суток',13,ИНК,true,undefined,'#1a2a5a')}
        ${подпись(168,Н-12, в===1?'3 дня = 3 + 12 = 15:00':'после полудня прибавь 12', в===1?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['3:00','15:00','13:00'],1,в,'r23Отв3') +
      (в==null ? СКАЗ('Вопрос','Как записать «три часа дня» в журнале?') : РАЗБОР(в===1, ['3:00 — это три часа <b>ночи</b>. Днём к часам прибавляют 12: <b>15:00</b>.','Полдень — 12:00, ещё 3 часа: <b>15:00</b>. Стрелка суток — в золотой, дневной половине.','13:00 — час дня. Три часа дня: 12 + 3 = <b>15:00</b>.'][в])) +
      (в===1 ? ПРАВИЛО('После полудня: <b>часы дня + 12</b>. 3 часа дня = 15:00, 7 вечера = 19:00.') : '');
  }

  /* 4. Через полночь */
  function F4(s){
    const Н=300, в=s.ответ4;
    return ЛИСТ(s) +
      ЗАДАЧА('Юнга заступил на ночную вахту в <b>22:00</b>. Вахта длится <b>5 часов</b>. Когда его сменят? Он складывает: 22 + 5 = 27… Но на круге нет числа 27!') +
      `<div class="pic">${свг(`
        ${ночь(Н)}
        ${волны(250,Н)}
        ${кругСуток(168,142,104,в===1?3:22,{дуга:дуга(168,142,112,22,3),цветДуги:в===1?GREEN:GOLD})}
        ${т(168,22,'22 → 23 → 0 → 1 → 2 → 3',13,МУТ,true,undefined,'#060a24')}
        ${подпись(168,Н-10, в===1?'27 − 24 = 3: три часа ночи':'полночь — снова ноль', в===1?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['27:00','3:00','5:00'],1,в,'r23Отв4') +
      (в==null ? СКАЗ('Вопрос','Во сколько его сменят?') : РАЗБОР(в===1, ['После 23 идёт 0 — новые сутки. Из 27 вычитаем 24: <b>3:00</b>.','22 + 5 = 27, а сутки кончаются на 24: 27 − 24 = <b>3:00</b>. Стрелка перешла через полночь.','5:00 — это пять часов вахты, а не время. Время: 22 + 5 − 24 = <b>3:00</b>.'][в])) +
      (в===1 ? ПРАВИЛО('Сумма больше 23 — <b>вычти 24</b>. Это переход через полночь.') : '');
  }

  /* 5. Остаток ноль */
  function F5(s){
    const Н=260, в=s.ответ5;
    return ЛИСТ(s) +
      ЗАДАЧА('Штиль! Корабль стоял без ветра ровно <b>48 часов</b>. Штиль начался в <b>16:00</b>. Во сколько подул ветер?') +
      `<div class="pic">${свг(`
        ${ночь(Н,true)}
        ${волны(200,Н,true)}
        ${кругСуток(96,118,70,16,{})}
        ${[0,1].map(i=>`<g>${вырасти('6s',0.1+i*0.3)}<circle cx="${210+i*56}" cy="110" r="22" fill="none" stroke="${GOLD}" stroke-width="3" stroke-dasharray="6 4"/>${т(210+i*56,115,'24',13,GOLD,true)}</g>`).join('')}
        ${т(238,158,'48 = 24 + 24',14,ИНК,true,undefined,'#1a2a5a')}
        ${подпись(168,Н-10, в===0?'остаток 0 — стрелка на месте':'двое суток — два полных круга', в===0?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['16:00','18:00','64:00'],0,в,'r23Отв5') +
      (в==null ? СКАЗ('Вопрос','Во сколько подул ветер?') : РАЗБОР(в===0, ['48 = 24 · 2 + <b>0</b>. Два полных круга, остатка нет — стрелка на том же месте: <b>16:00</b>.','18:00 было бы через 50 часов. 48 — ровно двое суток: <b>16:00</b>.','64 часа на круге нет. 16 + 48 = 64, минус 24 и ещё 24 — снова <b>16</b>.'][в])) +
      (в===0 ? ПРАВИЛО('Если часы делятся на 24 <b>без остатка</b> — время то же самое.') : '');
  }

  /* 6. Сто часов */
  function F6(s){
    const Н=304, в=s.ответ6;
    return ЛИСТ(s) +
      ЗАДАЧА('Капитан хвалится: «Этот корабль однажды шёл без остановки <b>100 часов</b>! Вышли мы тогда в 7:00». Юнга задумался: во сколько же тогда пришли в порт?') +
      `<div class="pic">${свг(`
        ${ночь(Н)}
        ${[0,1,2,3].map(i=>`<g>${вырасти('8s',0.05+i*0.12)}<circle cx="${50+i*56}" cy="74" r="22" fill="rgba(184,200,248,.1)" stroke="${ЛУНА}" stroke-width="3"/>${т(50+i*56,79,'24',13,ЛУНА,true)}</g>`).join('')}
        <g>${вырасти('8s',0.6)}${т(280,80,'+4',22,GOLD,true)}</g>
        ${т(168,124,'100 = 24 · 4 + 4',18,ИНК,true,undefined,'#060a24')}
        ${кругСуток(168,202,54,в===1?11:7,{дуга:дуга(168,202,62,7,11),безЧисел:true})}
        ${подпись(168,Н-10, в===1?'4 полных суток + 4 часа → 11:00':'сколько полных кругов в 100?', в===1?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['4:00','11:00','17:00'],1,в,'r23Отв6') +
      (в==null ? СКАЗ('Вопрос','Во сколько пришли в порт?') : РАЗБОР(в===1, ['4 — это остаток, а не время. Прибавь его к 7:00: <b>11:00</b>.','100 = 24 · 4 + 4. Четыре круга выбрасываем, 4 часа прибавляем: 7 + 4 = <b>11:00</b>.','17:00 — это 7 + 10. А остаток от 100 при делении на 24 — 4: <b>11:00</b>.'][в])) +
      (в===1 ? ПРАВИЛО('Много часов — это «<b>сутки · 24 + остаток</b>». Время сдвигается только на остаток.') : '');
  }

  /* 7. Сутки и часы → часы */
  function F7(s){
    const Н=240, в=s.ответ7;
    return ЛИСТ(s) +
      ЗАДАЧА('В журнале прошлого капитана записано: «От Карфагена до Сиракуз — <b>2 суток и 7 часов</b>». Юнге нужно переписать это в часах. Сколько часов в пути?') +
      `<div class="pic">${свг(`
        ${ночь(Н)}
        <g filter="url(#c23-тень)"><rect x="36" y="48" width="264" height="120" rx="4" fill="url(#c23-пергамент)"/>
          <rect x="30" y="44" width="10" height="128" rx="5" fill="#8a6a3a"/><rect x="296" y="44" width="10" height="128" rx="5" fill="#8a6a3a"/></g>
        ${т(168,82,'2 суток и 7 часов',18,'#3a2410',true)}
        ${т(168,116,'2 · 24 + 7 = ?',20,'#3a2410',true)}
        ${в===0?т(168,150,'= 55 часов',18,'#1a6a3a',true):''}
        <g>${анЛин('opacity','1;0.4;1','1.4s')}<line x1="80" y1="126" x2="256" y2="126" stroke="${GOLD}" stroke-width="1.6" stroke-dasharray="5 4"/></g>
        ${подпись(168,Н-12, в===0?'сутки — это 24 часа, а не 1':'одни сутки = 24 часа', в===0?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['55','27','9'],0,в,'r23Отв7') +
      (в==null ? СКАЗ('Вопрос','Сколько часов в пути?') : РАЗБОР(в===0, ['Двое суток — 48 часов, и ещё 7: <b>55</b>.','27 = 24 + 3? Нет — двое суток это 48, а не 24. 48 + 7 = <b>55</b>.','9 = 2 + 7 — сутки посчитаны как часы. Сутки — 24 часа: 2 · 24 + 7 = <b>55</b>.'][в])) +
      (в===0 ? ПРАВИЛО('Сутки в часы: <b>сутки · 24 + часы</b>.') : '');
  }

  /* 8. Назад во времени */
  function F8(s){
    const Н=300, в=s.ответ8;
    return ЛИСТ(s) +
      ЗАДАЧА('В <b>3:00</b> ночи вахтенный заметил огонь чужого корабля и вспомнил, что видел его ещё <b>5 часов назад</b>. Во сколько это было?') +
      `<div class="pic">${свг(`
        ${ночь(Н)}
        ${волны(250,Н)}
        ${кругСуток(168,142,104,в===1?22:3,{дуга:дуга(168,142,112,22,3),цветДуги:в===1?GREEN:RED})}
        ${т(168,22,'3 → 2 → 1 → 0 → 23 → 22',13,МУТ,true,undefined,'#060a24')}
        ${подпись(168,Н-10, в===1?'3 − 5 = −2 → −2 + 24 = 22':'назад через полночь', в===1?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['2:00','22:00','8:00'],1,в,'r23Отв8') +
      (в==null ? СКАЗ('Вопрос','Во сколько он впервые увидел огонь?') : РАЗБОР(в===1, ['2:00 — это час назад. Пять часов назад стрелка перешла полночь в обратную сторону: <b>22:00</b> вчерашнего вечера.','Шаг назад через полночь: 3, 2, 1, 0, 23, 22 — <b>22:00</b>. Считай так: 3 + 24 − 5 = 22.','8:00 — это 3 + 5, вперёд. Нужно назад: <b>22:00</b>.'][в])) +
      (в===1 ? ПРАВИЛО('Назад через полночь: <b>прибавь 24</b> и только потом вычитай.') : '');
  }

  /* 9. К рассвету */
  function F9(s){
    const Н=300, в=s.ответ9;
    return ЛИСТ(s) +
      ЗАДАЧА('Последний отрезок. Днём у входа в гавань Сиракуз поднимают цепь, пустят только <b>на рассвете</b>, с 5:00 до 7:00. Корабль выходит из последней бухты в <b>20:00</b>, а идти <b>34 часа</b>. Успеет ли он к рассвету?') +
      `<div class="pic">${свг(`
        ${ночь(Н,true)}
        <circle cx="168" cy="${Н-96}" r="60" fill="url(#c23-солнце)"/>
        ${волны(Н-100,Н,true)}
        ${кругСуток(96,120,74,в===2?6:20,{дуга:в===2?дуга(96,120,82,5,7):'',цветДуги:GREEN})}
        <g>${анСдвиг('0 0;30 -2;30 -2','8s','0;0.8;1')}${корабль(220,Н-80,0.8)}</g>
        ${т(250,76,'34 = 24 + 10',15,ИНК,true,undefined,'#1a2a5a')}
        ${т(250,100,'20 + 10 = 30 → ?',15,ИНК,true,undefined,'#1a2a5a')}
        ${подпись(168,Н-10, в===2?'30 − 24 = 6: в 6:00 — на рассвете!':'рассвет: с 5:00 до 7:00', в===2?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['54:00','10:00','6:00'],2,в,'r23Отв9') +
      (в==null ? СКАЗ('Вопрос','Во сколько корабль подойдёт к гавани?') : РАЗБОР(в===2, ['54 = 20 + 34 — сутки не вычтены. 54 − 24 − 24 = <b>6:00</b>.','10 — это остаток от 34, его надо <b>прибавить к 20</b>: 30, и вычесть сутки — 6:00.','34 = 24 + 10, 20 + 10 = 30, 30 − 24 = <b>6:00</b>. Корабль входит в гавань на рассвете!'][в])) +
      (в===2 ? ПРАВИЛО('Сначала выбрось полные сутки, потом прибавь остаток и, если нужно, <b>ещё раз вычти 24</b>.') : '');
  }

  /* 10. Сиракузы на рассвете */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=300;
    return ЛИСТ(s) +
      ЗАДАЧА(всё
        ? 'Ровно в 6:00 из утреннего тумана встают башни Ортигии. Цепь у входа опущена, корабль скользит в гавань. Капитан хлопает юнгу по плечу: «Ни разу не сбился со счёта — будешь штурманом!» На пристани ждёт Архимед: ему привезли папирусы из Александрии.'
        : 'Переход ещё не рассчитан — вернись к делам в списке. Вот главное.') +
      `<div class="pic">${свг(`
        ${ночь(Н,true)}
        <circle cx="240" cy="${Н-110}" r="70" fill="url(#c23-солнце)"/>
        <path d="M0 ${Н-120} h40 v-26 h14 v10 h12 v-10 h14 v26 h50 v-40 h18 v12 h14 v-12 h18 v40 h60 v-20 h12 v8 h12 v-8 h12 v20 h60 V${Н-100} H0 Z" fill="#6a5a6a" opacity=".85"/>
        ${волны(Н-100,Н,true)}
        <g>${анСдвиг('-60 0;20 0;20 0','9s','0;0.8;1')}${корабль(150,Н-70,1)}</g>
        <g>${проявить('9s',0.1,0.2)}${подпись(168,30,'сутки — круг из 24 часов',GOLD,12)}</g>
        <g>${проявить('9s',0.3,0.4)}${подпись(168,58,'полные сутки выбрось, остаток прибавь',GOLD,12)}</g>
        <g>${проявить('9s',0.5,0.6)}${подпись(168,Н-12,'больше 23 — вычти 24',GREEN,13)}</g>
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','Сутки — круг из 24 часов: после 23:00 снова 0:00. Через 24 часа время то же. Много часов раскладываем: <b>сутки · 24 + остаток</b> — и прибавляем только остаток. Если получилось больше 23 — вычитаем 24. Назад через полночь — сначала прибавь 24.') +
      ПРАВИЛО('<b>Полные сутки не двигают стрелку.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Сейчас 10:00. Сколько будет через 24 часа?', варианты:[{т:'10:00',ок:true},{т:'34:00',ок:false}], разбор:'Полный круг — время то же.' },
    { вопрос:'Сейчас 21:00. Сколько будет через 4 часа?', варианты:[{т:'25:00',ок:false},{т:'1:00',ок:true}], разбор:'25 − 24 = 1.' },
    { вопрос:'Как записать 8 часов вечера?', варианты:[{т:'20:00',ок:true},{т:'8:00',ок:false}], разбор:'8 + 12 = 20.' },
    { вопрос:'Сейчас 6:00. Сколько будет через 30 часов?', варианты:[{т:'12:00',ок:true},{т:'36:00',ок:false}], разбор:'30 = 24 + 6: 6 + 6 = 12.' },
    { вопрос:'Сейчас 2:00. Который был час 4 часа назад?', варианты:[{т:'22:00',ок:true},{т:'6:00',ок:false}], разбор:'2 + 24 − 4 = 22.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r23Reset()")}</div>` +
        ПРАВИЛО('<b>Больше 23 — вычти 24.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r23Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Вперёд по кругу', задания:[
    {q:'Сейчас 5:00. Через 26 часов?', в:1, варианты:['31:00','7:00'], раз:'26 = 24 + 2.'},
    {q:'Сейчас 18:00. Через 8 часов?', в:0, варианты:['2:00','26:00'], раз:'26 − 24 = 2.'},
    {q:'Сейчас 12:00. Через 72 часа?', в:1, варианты:['84:00','12:00'], раз:'72 = 3 суток ровно.'},
    {q:'Сейчас 9:00. Через 60 часов?', в:0, варианты:['21:00','15:00'], раз:'60 = 48 + 12: 9 + 12 = 21.'}
  ]};
  const Т2 = { имя:'Сутки и часы', задания:[
    {q:'3 суток — это сколько часов?', в:1, варианты:['27','72'], раз:'3 · 24.'},
    {q:'30 часов — это…', в:0, варианты:['1 сутки и 6 часов','3 суток'], раз:'30 = 24 + 6.'},
    {q:'Как записать 11 вечера?', в:1, варианты:['11:00','23:00'], раз:'11 + 12.'},
    {q:'Полночь — это…', в:0, варианты:['0:00 (или 24:00)','12:00'], раз:'12:00 — полдень.'}
  ]};
  const Т3 = { имя:'Назад и хитрые', задания:[
    {q:'Сейчас 1:00. Который был час 3 часа назад?', в:1, варианты:['2:00','22:00'], раз:'1 + 24 − 3 = 22.'},
    {q:'Сейчас 4:00. Который был час 28 часов назад?', в:0, варианты:['0:00','20:00'], раз:'28 = 24 + 4: 4 − 4 = 0.'},
    {q:'Поезд вышел в 23:00 и шёл 49 часов. Во сколько прибыл?', в:1, варианты:['72:00','0:00'], раз:'49 = 48 + 1: 23 + 1 = 24 → 0:00.'},
    {q:'Стрелка часов суток сделала 3 оборота. Сколько прошло часов?', в:0, варианты:['72','36'], раз:'Оборот — 24 часа.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r23T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r23TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L23 = {
    id: ID,
    title: 'Время на часах: модуль 24',
    ico: '🕐',
    src: 'Математика · 3–5 класс · Время и сутки',
    subj: 'math',
    explain: [
      'Корабль идёт в Сиракузы, юнга ведёт журнал по часам суток — кругу на 24 часа, полночь наверху. Через 24 часа стрелка встаёт туда же.',
      'Вышли в 7:00, идти 50 часов: 50 = 24 · 2 + 2. Полные сутки выбрасываем, остаток прибавляем — 9:00.',
      'На домашних часах круг на 12, у суток таких кругов два. После полудня к часам прибавляют 12: 3 часа дня — 15:00.',
      'Через полночь: 22:00 + 5 часов = 27, вычитаем 24 — 3:00.',
      'Если часы делятся на 24 без остатка, время то же: 16:00 + 48 часов = 16:00.',
      '100 часов: 100 = 24 · 4 + 4. Из 7:00 — в 11:00.',
      'Обратно: 2 суток и 7 часов = 2 · 24 + 7 = 55 часов.',
      'Назад через полночь: 3:00 − 5 часов = 22:00 (сначала прибавь 24).',
      'Из 20:00 плыть 34 часа: 34 = 24 + 10, 20 + 10 = 30, 30 − 24 = 6:00 — как раз к рассвету.',
      'Итог: сутки — круг из 24 часов, полные сутки выбрасываем, остаток прибавляем, больше 23 — вычитаем 24.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: вперёд по кругу.',
      'Тренажёр 2: сутки и часы.',
      'Тренажёр 3: назад и хитрые задачи.'
    ],
    check: {
      q: 'Сейчас 7:00. Какое время будет через 24 часа?',
      choices: ['8:00','7:00','6:00','12:00'],
      ans: 1,
      exp: '24 часа — ровно сутки, стрелка обежала круг и встала на семь.'
    },
    tasks: [
      { q:'Сейчас 7:00. Который час будет через 50 часов?', kind:'unit', ans:9, tol:0,
        hints:['50 = 24 · 2 + 2 — двое суток и 2 часа.','7 + 2 = 9.'], sol:'50 = 24 · 2 + 2 → 7:00 + 2 ч = 9:00.' },
      { q:'Сейчас 7:00. Который час будет через 100 часов?', kind:'unit', ans:11, tol:0,
        hints:['100 = 24 · 4 + 4.','7 + 4 = 11.'], sol:'100 = 24 · 4 + 4 → 7:00 + 4 ч = 11:00.' },
      { q:'Сейчас 22:00. Который час будет через 5 часов?', kind:'unit', ans:3, tol:0,
        hints:['22 + 5 = 27.','27 − 24.'], sol:'3:00.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L23.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    const ЗАГОЛОВКИ={1:'Часы суток',2:'Когда придём',3:'Три часа дня',4:'Вахта через полночь',5:'Штиль',
      6:'Сто часов',7:'Старый журнал',8:'Огонь в ночи',9:'К рассвету',10:'Сиракузы на рассвете',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l23" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Часы суток'}</h2>${сцена}</div>`;
    s.от2 = null; s.шаг2 = 0;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r23Отв1=(к)=>{ S().ответ1=к; chRender(0); };
  window.r23Час=(д)=>{ const s=S(), было=s.всего2||0;
    if(д===0){ s.всего2=0; s.от2=null; s.шаг2=0; chRender(0); return; }
    s.от2=(7+было)%24; s.шаг2=д; s.всего2=Math.min(240,было+д); chRender(0); };
  window.r23Отв2=(к)=>{ const s=S(); s.ответ2=к; if(к===1) s.дело_прибытие=true; chRender(0); };
  window.r23Отв3=(к)=>{ S().ответ3=к; chRender(0); };
  window.r23Отв4=(к)=>{ const s=S(); s.ответ4=к; if(к===1) s.дело_полночь=true; chRender(0); };
  window.r23Отв5=(к)=>{ S().ответ5=к; chRender(0); };
  window.r23Отв6=(к)=>{ const s=S(); s.ответ6=к; if(к===1) s.дело_сто=true; chRender(0); };
  window.r23Отв7=(к)=>{ S().ответ7=к; chRender(0); };
  window.r23Отв8=(к)=>{ S().ответ8=к; chRender(0); };
  window.r23Отв9=(к)=>{ S().ответ9=к; chRender(0); };
  window.r23Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r23Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r23T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r23TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L23; else arr.push(L23); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA23={render:render, L:L23};
})();
