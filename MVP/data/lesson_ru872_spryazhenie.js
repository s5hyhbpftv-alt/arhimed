/* ============ РУССКИЙ ЯЗЫК · УРОК 872 · «СПРЯЖЕНИЕ ГЛАГОЛА» ============
   4 класс, «Глагол». Была заглушкой в soon_lessons.js — теперь урок.
   Путь «Открытое море», после 869 и 870. Рисунки — общая библиотека моря
   MVP/data/ris_more.js (штурвал, рында, грифельная доска, ходовые огни).

   СЮЖЕТ. «Две вахты». На фрегате команда делится на две вахты, и глаголы тоже:
   у одних окончания -ешь, -ет, -ут, у других -ишь, -ит, -ат. Смену вахт
   отбивают в рынду. Штурвал с шестью спицами — «я, ты, он, мы, вы, они»:
   повернёшь — глагол меняет лицо и окончание.

   РУКАМИ: штурвал лиц — касаешься лица, штурвал поворачивается, на двух досках
   мелом: пишу, пишешь… и вижу, видишь…; разводка по вахтам — глаголы с ударным
   окончанием по двум доскам, рында звонит; «ты стро_шь» — ставишь в
   неопределённую форму; пропуски в вахтенном журнале.

   ПРОВЕРЕНО ПО ПРОГРАММЕ 4 КЛАССА (Канакина, Горецкий):
     изменение глаголов наст. и буд. времени по лицам и числам — спряжение;
     лица: 1-е (я, мы), 2-е (ты, вы), 3-е (он, она, оно, они);
     I спряжение: -у(-ю), -ешь, -ет, -ем, -ете, -ут(-ют);
     II спряжение: -у(-ю), -ишь, -ит, -им, -ите, -ат(-ят);
     при ударном окончании спряжение видно по окончанию (плывёшь — I, стоишь — II);
     при безударном — по неопределённой форме: на -ить — II (кроме брить,
     стелить), исключения II: гнать, держать, дышать, слышать, смотреть, видеть,
     ненавидеть, зависеть, вертеть, обидеть, терпеть; остальные — I;
     во 2-м лице ед. ч. после ш пишется ь: слышишь. */
(function(){
  'use strict';

  const ID = 872;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f', ОБВОД='#33291e';
  const ЧЕРНИЛА='#2e2416', МЕЛ='#f4f0e6', МЕЛЖ='#ffd76a';

  const ДЕЛА = [
    {ключ:'штурвал', имя:'Повернуть штурвал лиц',  итог:'спряжение'},
    {ключ:'вахты',   имя:'Развести по вахтам',      итог:'I и II'},
    {ключ:'журнал',  имя:'Заполнить вахтенный журнал', итог:'смотрит, качают'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const ЛИЦА = ['я','ты','он','мы','вы','они'];
  const ЛЧ = ['1-е лицо, ед. ч.','2-е лицо, ед. ч.','3-е лицо, ед. ч.','1-е лицо, мн. ч.','2-е лицо, мн. ч.','3-е лицо, мн. ч.'];
  const ПИСАТЬ = [['пиш','у'],['пиш','ешь'],['пиш','ет'],['пиш','ем'],['пиш','ете'],['пиш','ут']];
  const ВИДЕТЬ = [['виж','у'],['вид','ишь'],['вид','ит'],['вид','им'],['вид','ите'],['вид','ят']];
  const ОК1 = ['-у (-ю)','-ешь','-ет','-ем','-ете','-ут (-ют)'];
  const ОК2 = ['-у (-ю)','-ишь','-ит','-им','-ите','-ат (-ят)'];
  const СОРТ = [
    {w:'плывёшь', осн:'плыв', ок:'ёшь', с:1},
    {w:'стоишь',  осн:'сто',  ок:'ишь', с:2},
    {w:'несёт',   осн:'нес',  ок:'ёт',  с:1},
    {w:'кричит',  осн:'крич', ок:'ит',  с:2},
    {w:'поём',    осн:'по',   ок:'ём',  с:1},
    {w:'лежим',   осн:'леж',  ок:'им',  с:2}
  ];
  const ИСКЛ = ['гнать, держать,','дышать, слышать,','смотреть, видеть,','ненавидеть,','зависеть, вертеть,','обидеть, терпеть'];
  const ПРОПУСКИ = [
    {до:'Боцман ', осн:'смотр', после:'т на море', вар:['е','и'], верно:'и', why:'смотреть — исключение, II спряжение: смотрит'},
    {до:'а волны ', осн:'кача', после:'т фрегат', вар:['ю','я'], верно:'ю', why:'качать — на -ать, не исключение: I спряжение, качают'}
  ];


  const CSS=`
  #lvis .s6.l872{gap:14px}
  #lvis .s6.l872 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l872 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l872 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l872 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l872 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l872 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l872 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l872 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l872 .карт .текст b{color:${GOLD}}
  #lvis .s6.l872 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l872 .правило b{color:${GOLD}}
  #lvis .s6.l872 .журнал{width:100%;padding:12px 14px;border-radius:6px 16px 16px 6px;
    background:linear-gradient(90deg,#efe2c0,#dcc79a);border-left:7px solid #7a4a24;display:flex;flex-direction:column;gap:7px;color:${ЧЕРНИЛА}}
  #lvis .s6.l872 .журнал .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:#6a4a24}
  #lvis .s6.l872 .журнал .шапка b{font-size:18px;letter-spacing:0;text-transform:none;color:#7a2a10;font-family:Georgia,serif}
  #lvis .s6.l872 .журнал .шапка b.готово{color:#1a6a3a}
  #lvis .s6.l872 .журнал ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l872 .журнал li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:#6a5236}
  #lvis .s6.l872 .журнал li i{flex:0 0 22px;width:22px;height:22px;border-radius:50%;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;border:1.5px solid #a88a5a;color:#8a6a3a}
  #lvis .s6.l872 .журнал li.есть{color:${ЧЕРНИЛА}}
  #lvis .s6.l872 .журнал li.есть i{border-color:#1a4a6a;background:#2a6a9a;color:#fff}
  #lvis .s6.l872 .журнал li span{margin-left:auto;white-space:nowrap;font-size:14px;color:#8a6a3a;font-family:Georgia,serif}
  #lvis .s6.l872 .журнал li.есть span{color:#1a6a3a;font-weight:700}
  #lvis .s6.l872 .буйки{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;width:100%}
  #lvis .s6.l872 .буйки button{min-height:56px;border-radius:28px;cursor:pointer;font:inherit;font-size:19px;font-weight:700;
    font-family:Georgia,serif;border:2px solid #e86a5a;background:linear-gradient(180deg,#fff6ea,#f0dcc0);color:${ЧЕРНИЛА};
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),opacity 200ms}
  #lvis .s6.l872 .буйки button:active{transform:scale(.96)}
  #lvis .s6.l872 .буйки button.пойман{opacity:.35;border-color:${GREEN};text-decoration:line-through}
  #lvis .s6.l872 .буйки button.мимо{border-color:${RED};animation:l872нет 360ms cubic-bezier(.23,1,.32,1)}
  @keyframes l872нет{0%,100%{transform:none}25%{transform:translateX(-6px)}75%{transform:translateX(6px)}}
  #lvis .s6.l872 .ряд{display:grid;gap:8px;width:100%}
  #lvis .s6.l872 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:18px;font-weight:700;
    border:1.5px solid rgba(127,209,255,.5);background:rgba(127,209,255,.1);color:${ИНК};padding:6px 4px;font-family:Georgia,serif;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l872 .ряд button:active{transform:translateY(2px)}
  #lvis .s6.l872 .ряд button.вкл{border-color:${GOLD};background:rgba(255,215,106,.22);color:${GOLD}}
  #lvis .s6.l872 .случай{width:100%;display:flex;flex-direction:column;gap:6px}
  #lvis .s6.l872 .случай .что{font-size:19px;color:var(--ink);font-family:Georgia,serif}
  #lvis .s6.l872 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l872 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:17px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l872 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l872 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l872 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l872 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-size:18px}
  #lvis .s6.l872 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-size:18px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l872 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l872 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l872 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l872 .уровни .точка.сейчас{background:${GOLD};animation:l872dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l872dot{0%,100%{opacity:1}50%{opacity:.55}}
  #lvis .s6.l872 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l872{-webkit-text-size-adjust:100%}
  #lvis .s6.l872 [data-anim]{animation:l872rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l872rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  #lvis .s6.l872 .падежи{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;width:100%}
  #lvis .s6.l872 .падежи button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:16px;font-family:Georgia,serif;
    border:1.5px solid rgba(255,215,106,.45);background:rgba(255,215,106,.08);color:${ИНК};padding:6px 4px;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1),background 160ms}
  #lvis .s6.l872 .падежи button b{color:${GOLD};font-size:18px;margin-right:4px}
  #lvis .s6.l872 .падежи button.был{border-color:rgba(143,209,168,.6)}
  #lvis .s6.l872 .падежи button.вкл{border-color:${GOLD};background:rgba(255,215,106,.24)}
  #lvis .s6.l872 .падежи button:active{transform:translateY(2px)}
  #lvis .s6.l872 .ask.шесть button{flex:1 1 calc(16% - 8px);text-align:center;font-size:17px;padding:13px 2px;min-width:44px;font-family:Georgia,serif}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l872 [data-anim]{animation:none!important}
    #lvis .s6.l872 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l872 button{transition:none!important;animation:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l872-style');
      if(!s){ s=document.createElement('style'); s.id='l872-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Судовой журнал</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,f) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, 'r872Отв('+f+','+к+')')).join('')}</div>`;
  const ЖУРНАЛ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'журнал',
      `<div class="шапка"><span>Смена вахты</span><b class="${всё?'готово':''}">${
        всё?'вахты разведены':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}<span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.45;0.45;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const сдвигРаз = (из,в,длит,задержка) => ДВИЖ ?
    `<animateTransform attributeName="transform" type="translate" from="${из}" to="${в}" dur="${длит}" begin="${(задержка||0).toFixed(2)}s"
       fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  /* слово с выделенным окончанием */
  const тОк = (x,y,осн,ок,кегль,цвет,цветОк,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль}" font-weight="bold" fill="${цвет}" font-family="Georgia,'Times New Roman',serif">${esc(осн)}<tspan fill="${цветОк}">${esc(ок)}</tspan></text>`;
  const Р = () => window.РМ;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <filter id="c872-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="1.5" dy="2" stdDeviation="1.6" flood-color="#0b1c2a" flood-opacity=".45"/>
      </filter>
      <radialGradient id="c872-лампа" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#ffd890" stop-opacity=".5"/><stop offset="1" stop-color="#ff9a40" stop-opacity="0"/>
      </radialGradient>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${Р().defs()}${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c872-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(14,26,20,.92)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  /* облачко-реплика */
  const реплика = (x,y,ш,текст,хвост) => `<g filter="url(#c872-тень)">
      <rect x="${x}" y="${y}" width="${ш}" height="30" rx="14" fill="#fffaf0" stroke="${ОБВОД}" stroke-width="1"/>
      <path d="M${хвост[0]} ${y+29} l${хвост[1]} 12 l6 -12z" fill="#fffaf0" stroke="${ОБВОД}" stroke-width="1"/>
      <rect x="${хвост[0]-1}" y="${y+26}" width="10" height="5" fill="#fffaf0"/></g>
      ${т(x+ш/2,y+20,текст,14,ЧЕРНИЛА,true)}`;
  /* мел: надпись на доске, новая «пишется» слева направо */
  let счётМел = 0;
  const мел = (x,y,html,кегль,якорь,пишется,ширина) => {
    const tx=`<text x="${x}" y="${y}" text-anchor="${якорь||'start'}" font-size="${кегль||16}" fill="${МЕЛ}" font-family="'Comic Sans MS','Marker Felt',Georgia,serif" opacity=".95">${html}</text>`;
    if(!пишется||!ДВИЖ) return tx;
    const ид='c872-мел-'+(++счётМел), ш=ширина||200, x0=якорь==='middle'?x-ш/2:x;
    return `<clipPath id="${ид}"><rect x="${x0-2}" y="${y-(кегль||16)-4}" width="0" height="${(кегль||16)+10}"><animate attributeName="width" from="0" to="${ш+6}" dur="0.9s" fill="freeze"/></rect></clipPath><g clip-path="url(#${ид})">${tx}</g>`;
  };
  const жёлт = (s) => `<tspan fill="${МЕЛЖ}">${esc(s)}</tspan>`;
  const палуба = (y,Н) => { const М=Р(); return `${М.доски(0,y,336,Н-y,false)}
    ${Array.from({length:9},(_,k)=>`<rect x="${6+k*40}" y="${y-22}" width="5" height="22" fill="url(#рм-мачта)" stroke="${ОБВОД}" stroke-width=".5"/>`).join('')}
    <rect x="-2" y="${y-26}" width="340" height="6" rx="3" fill="url(#рм-доска)" stroke="${ОБВОД}" stroke-width=".8"/>`; };

  /* ================= КАДРЫ ================= */

  /* 1. Штурвал лиц */
  function F1(s){
    const Н=340, М=Р(), к=s.лицо1==null?0:s.лицо1, вид=Array.isArray(s.вид1)?s.вид1:[0], все=вид.length>=6, в=s.ответ1, ок=в===0;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('На юте у штурвала шесть рукоятей, и у каждой своё имя: <b>я, ты, он, мы, вы, они</b>. Поверни штурвал — и глаголы на досках поменяют окончания. Пройди все шесть лиц.') +
      `<div class="pic">${свг(`
        ${М.небо(336,150,{облака:[[60,40,0.5,8],[290,60,0.4,-6]]})}
        ${М.море(150,40,336,{})}
        ${палуба(196,Н)}
        <rect x="160" y="120" width="16" height="92" fill="url(#рм-доскатём)" stroke="${ОБВОД}" stroke-width="1"/>
        <rect x="156" y="196" width="24" height="12" rx="2" fill="url(#рм-латунь)" stroke="${ОБВОД}" stroke-width=".8"/>
        ${М.штурвал(168,106,54,{подписи:ЛИЦА,угол:к*60,было:s.было1!=null?s.было1*60:null,свет:к})}
        ${ЛИЦА.map((_,i)=>{ const a=(-90+i*60)*Math.PI/180, R=54*1.62; return `<g onclick="r872Лицо(${i})" style="cursor:pointer"><circle cx="${(168+R*Math.cos(a)).toFixed(1)}" cy="${(106+R*Math.sin(a)).toFixed(1)}" r="20" fill="transparent"/></g>`; }).join('')}
        ${М.доска(8,222,156,110,{})}${М.доска(172,222,156,110,{})}
        ${мел(86,250,'писать',13,'middle')}${мел(250,250,'видеть',13,'middle')}
        ${мел(86,288,esc(ЛИЦА[к])+' '+esc(ПИСАТЬ[к][0])+жёлт(ПИСАТЬ[к][1]),21,'middle',true,130)}
        ${мел(250,288,esc(ЛИЦА[к])+' '+esc(ВИДЕТЬ[к][0])+жёлт(ВИДЕТЬ[к][1]),21,'middle',true,130)}
        ${мел(86,316,ЛЧ[к],11.5,'middle')}${мел(250,316,ЛЧ[к],11.5,'middle')}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="падежи">${ЛИЦА.map((x,i)=>BTN(3+(i%5),(i===к?'вкл':'')+(вид.includes(i)?' был':''),'<b>'+x+'</b>','r872Лицо('+i+')')).join('')}</div>` +
      (!все ? СКАЗ('Смотри на концы слов','Пиш<b>ешь</b> — вид<b>ишь</b>, пиш<b>ут</b> — вид<b>ят</b>. Меняются окончания.') :
        ОТВЕТЫ('пара',['спряжение','склонение'],0,в,1) +
        (в==null ? СКАЗ('Вопрос','Как называется изменение глаголов по лицам и числам?') :
          РАЗБОР(ок,['Пишу, пишешь, пишет… — глагол изменяется по <b>лицам и числам</b>. Это <b>спряжение</b>.',
            'Склоняются существительные — по падежам. Глаголы изменяются по лицам и числам — это <b>спряжение</b>.'][в]))) +
      (ок ? ПРАВИЛО('Изменение глаголов настоящего и будущего времени по <b>лицам</b> и <b>числам</b> — <b>спряжение</b>. «Пишешь» и «видишь» спрягаются по-разному.') : '');
  }

  /* сцена с рындой и двумя досками — для 2 и 3 кадров */
  function вахты(Н,звонит,лево,право){
    const М=Р();
    return `${М.небо(336,110,{солнце:[296,30,10]})}
      ${М.море(110,20,336,{})}
      ${палуба(150,Н)}
      <rect x="126" y="0" width="12" height="150" fill="url(#рм-мачта)" stroke="${ОБВОД}" stroke-width="1"/>
      ${М.рында(166,22,0.85,{звонит:звонит})}
      ${М.боцман(56,150,0.5,{поза:звонит?'свисток':'указывает'})}
      ${М.юнга(290,150,0.56,{поза:звонит?'машет':'стоит'})}
      ${М.доска(8,156,156,Н-164,{})}${М.доска(172,156,156,Н-164,{})}
      ${лево}${право}`;
  }

  /* 2. Две вахты */
  function F2(s){
    const Н=340, в=s.ответ2, ок=в===0;
    const столбик=(x,заг,ок2,круг)=>мел(x+78,184,заг,14,'middle')+ок2.map((о,i)=>{ const y=208+i*20;
      return мел(x+22,y,esc(ЛИЦА[i]),13.5)+мел(x+66,y,жёлт(о),14.5)+
        (круг===i?`<ellipse cx="${x+96}" cy="${y-5}" rx="36" ry="11" fill="none" stroke="${МЕЛЖ}" stroke-width="1.6">${анЛин('opacity','1;0.3;1','1.2s')}</ellipse>`:''); }).join('');
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Боцман выстроил две вахты. У <b>I спряжения</b> окончания -ешь, -ет, -ем, -ете, -ут (-ют). У <b>II спряжения</b> -ишь, -ит, -им, -ите, -ат (-ят). В какую вахту встанет «они <b>пишут</b>»?') +
      `<div class="pic">${свг(`
        ${вахты(Н,ок,столбик(8,'I спряжение',ОК1,ок?5:null),столбик(172,'II спряжение',ОК2,null))}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['I спряжение','II спряжение'],0,в,2) +
      (в==null ? СКАЗ('Вопрос','«Они пишут» — какое спряжение?') :
        РАЗБОР(ок,['Пиш<b>ут</b>: окончание -ут есть только у <b>I спряжения</b>. У II было бы -ат, -ят (видят).',
          'У II спряжения в 3-м лице мн. ч. -ат, -ят: вид<b>ят</b>. А пиш<b>ут</b> — окончание <b>I спряжения</b>.'][в])) +
      (ок ? ПРАВИЛО('<b>I спр.</b>: -ешь, -ет, -ем, -ете, -ут (-ют). <b>II спр.</b>: -ишь, -ит, -им, -ите, -ат (-ят).') : '');
  }

  /* 3. Разводка по вахтам */
  function F3(s){
    const Н=340, М=Р(), разв=s.разв3||{}, выб=s.выбор3, отв=s.отв3, порядок=s.порядок3||[];
    const все=СОРТ.every(г=>разв[г.w]);
    const список=(x,с,заг)=>мел(x+78,184,заг,13,'middle')+порядок.filter(w=>разв[w]===с).map((w,i)=>{ const г=СОРТ.find(z=>z.w===w);
      return мел(x+78,218+i*30,esc(г.осн)+жёлт(г.ок),20,'middle',отв&&отв.ок&&отв.w===w,110); }).join('');
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Смена вахты! У этих глаголов окончание <b>ударное</b> — его хорошо слышно. Выбери глагол, потом вахту. Верно — рында отобьёт склянки.') +
      `<div class="pic">${свг(`
        ${вахты(Н,!!(отв&&отв.ок),список(8,1,'I: -ешь, -ет, -ут'),список(172,2,'II: -ишь, -ит, -ат'))}
        ${выб?подпись(250,56,выб,GOLD,15):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="падежи">${СОРТ.map((г,i)=>{ const есть=разв[г.w];
        return BTN(3+(i%5),(есть?'был':'')+(выб===г.w?' вкл':''),(есть?'✓ ':'')+г.w,есть?'':'r872Глаг('+i+')'); }).join('')}</div>` +
      (все ? '' : `<div class="ask пара">${[1,2].map(с=>BTN(4+с,'',(с===1?'I':'II')+' спряжение','r872Вахта('+с+')')).join('')}</div>`) +
      (все ? РАЗБОР(true,'Вахты разведены: <b>плывёшь, несёт, поём</b> — I спряжение; <b>стоишь, кричит, лежим</b> — II.')
        : отв ? РАЗБОР(отв.ок, (()=>{ const г=СОРТ.find(z=>z.w===отв.w); return отв.ок
            ? '«'+г.w+'»: ударное окончание -'+г.ок+' — <b>'+(г.с===1?'I':'II')+' спряжение</b>.'
            : '«'+г.w+'»: окончание -'+г.ок+' — это '+(г.с===1?'I':'II')+' спряжение ('+(г.с===1?'-ешь, -ет, -ем':'-ишь, -ит, -им')+'). Выбери глагол ещё раз.'; })())
        : СКАЗ(выб?'Глагол выбран':'Сначала глагол', выб?'«'+esc(выб)+'» — в какую вахту?':'Нажми на глагол выше.')) +
      (все ? ПРАВИЛО('Если окончание <b>ударное</b>, спряжение слышно: -ёшь, -ёт, -ём — I; -ишь, -ит, -им — II.') : '');
  }

  /* 4. Безударное окончание: неопределённая форма */
  function F4(s){
    const Н=280, М=Р(), нф=!!s.нф4, в=s.ответ4, ок=в===1;
    const бревно=(y,k)=>`<rect x="${178+k*3}" y="${y}" width="128" height="13" rx="6.5" fill="url(#рм-доска)" stroke="${ОБВОД}" stroke-width=".9"/><ellipse cx="${306+k*3}" cy="${y+6.5}" rx="4" ry="6" fill="#d8b07a" stroke="${ОБВОД}" stroke-width=".7"/>`;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('На берегу юнга строит плот. Штурман пишет мелом: «Ты стро<b>_</b>шь плот». Окончание безударное — не слышно, е или и. Поставь глагол в <b>неопределённую форму</b>: что делать?') +
      `<div class="pic">${свг(`
        ${М.небо(336,120,{солнце:[40,30,11]})}
        ${М.море(120,50,336,{})}
        <path d="M0 176 Q90 158 180 170 T336 162 V${Н} H0 Z" fill="url(#рм-песок)" stroke="#c8a870" stroke-width="1"/>
        <path d="M0 176 Q90 158 180 170 T336 162" stroke="#e8f6ff" stroke-width="2" fill="none" opacity=".7">${анЛин('opacity','0.7;0.2;0.7','2.6s')}</path>
        ${[0,1,2,3,4].map(k=>бревно(200+k*13,k%2)).join('')}
        ${[204,262].map(x=>`<path d="M${x} 198 v68" stroke="#8a7a5a" stroke-width="3" stroke-dasharray="4 3"/>`).join('')}
        ${М.юнга(96,262,0.9,{поза:'стоит'})}
        ${М.доска(178,12,150,92,{})}
        ${мел(253,48,нф?'строить → -ить':'ты стро<tspan fill="'+МЕЛЖ+'">_</tspan>шь',нф?16:20,'middle',нф,130)}
        ${нф?мел(253,80,в==null?'II спряжение?':(ок?'ты стро'+жёлт('и')+'шь':'ты стро'+жёлт('е')+'шь ✗'),16,'middle'):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      (!нф ? `<div class="ask">${BTN(4,'','Поставить в неопределённую форму','r872НФ()')}</div>` + СКАЗ('Как','Спроси: <b>что делать?</b> — строить.') :
        ОТВЕТЫ('пара',['е','и'],1,в,4) +
        (в==null ? СКАЗ('Вопрос','Строить — на -ить. Какую букву писать?') :
          РАЗБОР(ок,['Строить — на <b>-ить</b>, значит, <b>II спряжение</b>: -ишь. Ты стро<b>и</b>шь.',
            'Строить — на -ить: <b>II спряжение</b>, окончание -ишь: ты стро<b>и</b>шь.'][в]))) +
      (ок ? ПРАВИЛО('Окончание <b>безударное</b>? Ставь глагол в неопределённую форму. На <b>-ить</b> — II спряжение (кроме брить, стелить).') : '');
  }

  /* 5. Исключения */
  function F5(s){
    const Н=290, М=Р(), в=s.ответ5, ок=в===1;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Штурман <b>смотр_т</b> в подзорную трубу. «Смотреть» — на -еть, такие глаголы обычно I спряжения. Но у штурмана есть список — одиннадцать глаголов-исключений II спряжения. Какую букву писать?') +
      `<div class="pic">${свг(`
        ${М.небо(336,150,{облака:[[100,40,0.5,8]]})}
        ${М.утёс(130,152,0.35)}
        ${М.море(150,50,336,{})}
        ${палуба(200,Н)}
        ${М.штурман(74,276,0.9,{смотрит:true})}
        ${М.лист(178,14,150,176,{})}
        ${т(253,36,'исключения · II спр.',11,'#7a4a24',true)}
        ${ИСКЛ.map((стр,i)=>`<text x="190" y="${62+i*21}" font-size="12.5" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">${стр.split(' ').map(w=>ок&&w.startsWith('смотреть')?`<tspan fill="#1a7a4a" font-weight="bold">${esc(w)}</tspan>`:esc(w)).join(' ')}</text>`).join('')}
        ${М.лист(178,208,150,62,{})}
        <text x="253" y="248" text-anchor="middle" font-size="21" font-weight="bold" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">смотр<tspan fill="${в==null?'#c8901a':ок?'#1a7a4a':'#b8321e'}">${в==null?'_':['е','и'][в]}</tspan>т</text>
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['е','и'],1,в,5) +
      (в==null ? СКАЗ('Вопрос','Смотр<b>е</b>т или смотр<b>и</b>т?') :
        РАЗБОР(ок,['«Смотреть» — в списке исключений: это <b>II спряжение</b>. Штурман смотр<b>и</b>т.',
          'Верно: смотреть — <b>исключение</b>, II спряжение: смотр<b>и</b>т, смотр<b>и</b>шь, смотр<b>я</b>т.'][в])) +
      (ок ? ПРАВИЛО('Исключения — <b>II спряжение</b>: гнать, держать, дышать, слышать, смотреть, видеть, ненавидеть, зависеть, вертеть, обидеть, терпеть.') : '');
  }

  /* 6. Остальные — I спряжение */
  function F6(s){
    const Н=270, М=Р(), в=s.ответ6, ок=в===0;
    const ветер=[[20,70],[4,104],[30,138]].map(([x,y],i)=>`<path d="M${x} ${y} q30 -12 60 0 t60 0 q14 -10 6 -18" stroke="#fff" stroke-width="2.2" fill="none" stroke-linecap="round" opacity=".75" stroke-dasharray="46 160">${анЛин('stroke-dashoffset','206;0','2.2s',`begin="${(i*0.5).toFixed(1)}s"`)}</path>`).join('');
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга пишет: «Ветер <b>ду_т</b> в паруса». Окончание безударное. Дуть — не на -ить и не из списка исключений. Е или И?') +
      `<div class="pic">${свг(`
        ${М.небо(336,150,{солнце:[300,32,11]})}
        ${М.море(150,120,336,{дорожка:300})}
        ${М.фрегат(180,212,0.64,{})}
        ${ветер}
        ${М.доска(8,184,130,72,{})}
        ${мел(73,216,'ветер ду'+(в==null?'<tspan fill="'+МЕЛЖ+'">_</tspan>':жёлт(['е','и'][в]))+'т',19,'middle')}
        ${мел(73,242,ок?'дуть → I':'дуть → ?',13,'middle')}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['е','и'],0,в,6) +
      (в==null ? СКАЗ('Вопрос','Ду<b>е</b>т или ду<b>и</b>т?') :
        РАЗБОР(ок,['Дуть — на -уть, не исключение: <b>I спряжение</b>, окончание -ет. Ветер ду<b>е</b>т.',
          'II спряжение — только на -ить и одиннадцать исключений. Дуть — <b>I спряжение</b>: ду<b>е</b>т.'][в])) +
      (ок ? ПРАВИЛО('Все остальные глаголы (на -ать, -еть, -оть, -уть, -ять и др., кроме исключений) и брить, стелить — <b>I спряжение</b>.') : '');
  }

  /* 7. 3-е лицо мн. ч. */
  function F7(s){
    const Н=280, М=Р(), в=s.ответ7, ок=в===1;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Утром юнги <b>драят палубу</b> — чистят её до блеска. Как пишется: «юнги дра<b>ю</b>т» или «юнги дра<b>я</b>т»? Глагол — <b>драить</b>.') +
      `<div class="pic">${свг(`
        ${М.небо(336,110,{облака:[[240,30,0.5,-6]]})}
        ${М.море(110,40,336,{})}
        ${палуба(160,Н)}
        <ellipse cx="170" cy="236" rx="70" ry="12" fill="#bfe6ff" opacity=".35" data-декор="1">${анЛин('opacity','0.35;0.15;0.35','2s')}</ellipse>
        <g><path d="M40 222 l6 34 h28 l6 -34 z" fill="url(#рм-железо)" stroke="${ОБВОД}" stroke-width="1"/><ellipse cx="60" cy="222" rx="20" ry="4" fill="#7ac0e8" stroke="${ОБВОД}" stroke-width=".8"/>
          <path d="M42 222 q18 -26 36 0" stroke="#3a3e46" stroke-width="1.4" fill="none"/></g>
        ${ДВИЖ?[0,1,2].map(k=>`<circle cx="${150+k*22}" cy="230" r="3.4" fill="#fff" stroke="#bfe6ff" opacity="0"><animate attributeName="cy" values="232;200" dur="2s" begin="${k*0.6}s" repeatCount="indefinite"/><animate attributeName="opacity" values="0;0.9;0" dur="2s" begin="${k*0.6}s" repeatCount="indefinite"/></circle>`).join(''):''}
        ${М.юнга(126,262,0.9,{поза:'стоит'})}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="rotate" values="0 144 228;12 144 228;0 144 228" dur="1.2s" repeatCount="indefinite"/>`:''}
          <path d="M144 228 L186 262" stroke="#8a5a2e" stroke-width="4" stroke-linecap="round"/>
          ${[-8,-4,0,4,8].map(d=>`<path d="M186 262 q${d} 6 ${d*1.6+6} 10" stroke="#e8dcc0" stroke-width="2.4" fill="none" stroke-linecap="round"/>`).join('')}</g>
        ${М.боцман(282,262,0.8,{поза:'указывает',влево:true})}
        ${М.доска(40,10,180,76,{})}
        ${мел(130,46,'юнги дра'+(в==null?'<tspan fill="'+МЕЛЖ+'">_</tspan>':жёлт(['ю','я'][в]))+'т',20,'middle')}
        ${мел(130,72,ок?'драить → -ить → II':'драить → ?',13,'middle')}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['драют','драят'],1,в,7) +
      (в==null ? СКАЗ('Вопрос','Драить — какое спряжение? Какое окончание в 3-м лице мн. ч.?') :
        РАЗБОР(ок,['Драить — на <b>-ить</b>: II спряжение. В 3-м лице мн. ч. у II — <b>-ат, -ят</b>: дра<b>я</b>т. «-ют» — у I спряжения.',
          'Драить — на -ить, <b>II спряжение</b>: окончание -ят — юнги дра<b>я</b>т.'][в])) +
      (ок ? ПРАВИЛО('В 3-м лице мн. ч.: I спряжение — <b>-ут, -ют</b> (пишут, качают); II — <b>-ат, -ят</b> (видят, драят).') : '');
  }

  /* 8. Мягкий знак во 2-м лице */
  function F8(s){
    const Н=270, М=Р(), в=s.ответ8, ок=в===0;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Бьют склянки. Боцман кричит юнге с мачты: «Ты <b>слышиш_</b> рынду?» Как закончить слово: с мягким знаком или без?') +
      `<div class="pic">${свг(`
        ${М.небо(336,140,{облака:[[260,30,0.5,-6]]})}
        ${М.море(140,30,336,{})}
        ${палуба(190,Н)}
        <rect x="70" y="0" width="14" height="190" fill="url(#рм-мачта)" stroke="${ОБВОД}" stroke-width="1"/>
        ${М.рында(114,24,1.3,{звонит:true})}
        ${М.юнга(262,252,0.9,{поза:'машет'})}
        ${М.доска(14,176,176,80,{})}
        ${мел(102,214,'ты слыш'+жёлт('и')+'ш'+(в==null?'<tspan fill="'+МЕЛЖ+'">_</tspan>':в===0?жёлт('ь'):''),22,'middle')}
        ${мел(102,242,'2-е лицо, ед. ч.',13,'middle')}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['слышишь','слышиш'],0,в,8) +
      (в==null ? СКАЗ('Вопрос','Ставить ли ь после ш?') :
        РАЗБОР(ок,['Во 2-м лице ед. ч. глаголы оканчиваются на <b>-шь</b>: слышишь, пишешь, видишь. Слышать — исключение, поэтому слыш<b>и</b>шь.',
          'У глаголов 2-го лица ед. ч. после ш всегда пишется <b>ь</b>: ты слышишь, ты пишешь.'][в])) +
      (ок ? ПРАВИЛО('Во 2-м лице ед. ч. после <b>ш</b> пишется <b>ь</b>: ты слышиш<b>ь</b>, ты дышиш<b>ь</b>, ты плывёш<b>ь</b>.') : '');
  }

  /* 9. Вахтенный журнал */
  function F9(s){
    const Н=260, М=Р(), реш=s.реш9||{}, все=ПРОПУСКИ.every((_,i)=>реш[i]!=null), верно=все&&ПРОПУСКИ.every((x,i)=>реш[i]===x.верно);
    const бук=(i)=>{ const р=реш[i]; return `<tspan fill="${р==null?'#c8901a':р===ПРОПУСКИ[i].верно?'#1a7a4a':'#b8321e'}">${р||'_'}</tspan>`; };
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Вахта сдаёт журнал. Впиши окончания: «Боцман смотр<b>_</b>т на море, а волны кача<b>_</b>т фрегат». Для каждого глагола найди неопределённую форму.') +
      `<div class="pic">${свг(`
        ${М.небо(336,84,{солнце:[296,30,11]})}
        ${М.море(84,30,336,{})}
        ${М.лист(12,98,312,Н-110,{линии:3})}
        <text x="28" y="138" font-size="19" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">Боцман смотр${бук(0)}т на море,</text>
        <text x="28" y="175" font-size="19" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">а волны кача${бук(1)}т</text>
        <text x="28" y="212" font-size="19" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">фрегат.</text>
        ${верно?т(306,212,'✓',22,'#1a7a4a',true,'end'):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ПРОПУСКИ.map((x,i)=>`<div class="случай">${A(3+i,'что',(i?'…':'')+x.до+'<b>'+x.осн+'_'+x.после+'</b>')}<div class="ask пара">${x.вар.map(б=>
        BTN(4+i,реш[i]===б?(б===x.верно?'hit':'miss'):'',б,"r872Проп("+i+",'"+б+"')")).join('')}</div></div>`).join('') +
      (!все ? СКАЗ('Подсказка','Смотреть — есть в списке исключений? Качать — на -ать, в списке его нет.') :
        РАЗБОР(верно, верно ? 'Смотр<b>и</b>т — исключение, II спряжение; кача<b>ю</b>т — I спряжение.' :
          ПРОПУСКИ.filter((x,i)=>реш[i]!==x.верно).map(x=>x.why).join('; ')+'.')) +
      (верно ? ПРАВИЛО('Безударное окончание: <b>неопределённая форма → спряжение → окончание</b>.') : '');
  }

  /* 10. Ночная вахта */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=320, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА(всё
        ? 'Ночь. Горят ходовые огни: красный у левого борта, зелёный у правого. Вахты сменились без единой ошибки в журнале. Рында отбивает полночь.'
        : 'Вахты ещё не разведены — вернись к делам в списке. Вот что получится в конце.') +
      `<div class="pic">${свг(`
        ${М.небо(336,160,{ночь:true,луна:[296,130]})}
        ${М.море(160,160,336,{ночь:true})}
        ${М.фрегат(168,244,0.66,{})}
        ${М.фонарь(98,196,0.55,'#e8402a')}${М.фонарь(246,196,0.55,'#3ac86a')}
        ${[['I: -ешь, -ет, -ем, -ете, -ут',GOLD],['II: -ишь, -ит, -им, -ите, -ат',BLUE],['не слышно — по неопределённой форме',GREEN]].map(([t0,ц],i)=>`<g>${проявить('9s',0.1+i*0.18,0.16+i*0.18)}${подпись(168,34+i*28,t0,ц,12)}</g>`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','Изменение глаголов по лицам и числам — <b>спряжение</b>. <b>I спряжение</b>: -ешь, -ет, -ем, -ете, -ут (-ют). <b>II спряжение</b>: -ишь, -ит, -им, -ите, -ат (-ят). Если окончание ударное — спряжение слышно. Если безударное — ставим глагол в неопределённую форму: на -ить (кроме брить, стелить) и 11 исключений — II спряжение, остальные — I. Во 2-м лице ед. ч. после ш пишется ь.') +
      ПРАВИЛО('<b>Не слышно — спроси «что делать?»</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'«Ты пишешь» — какое лицо?', варианты:[{т:'2-е',ок:true},{т:'1-е',ок:false}], разбор:'Ты — 2-е лицо.' },
    { вопрос:'«Они видят» — какое спряжение?', варианты:[{т:'I',ок:false},{т:'II',ок:true}], разбор:'-ят — II спр.' },
    { вопрос:'«Он плывёт» — какое спряжение?', варианты:[{т:'I',ок:true},{т:'II',ок:false}], разбор:'Ударное -ёт — I.' },
    { вопрос:'«Ты кле_шь» (клеить)', варианты:[{т:'е',ок:false},{т:'и',ок:true}], разбор:'На -ить — II.' },
    { вопрос:'«Ты держ_шь» (держать)', варианты:[{т:'и',ок:true},{т:'е',ок:false}], разбор:'Исключение — II.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r872Reset()")}</div>` +
        ПРАВИЛО('<b>Не слышно — спроси «что делать?»</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r872Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Ударные окончания', задания:[
    {q:'«Летишь» — спряжение?', в:1, варианты:['I','II'], раз:'Ударное -ишь — II.'},
    {q:'«Ждёшь» — спряжение?', в:0, варианты:['I','II'], раз:'Ударное -ёшь — I.'},
    {q:'«Молчим» — спряжение?', в:1, варианты:['I','II'], раз:'Ударное -им — II.'},
    {q:'«Живёт» — спряжение?', в:0, варианты:['I','II'], раз:'Ударное -ёт — I.'}
  ]};
  const Т2 = { имя:'Е или И', задания:[
    {q:'«Ты гон_шь мяч» (гнать)', в:1, варианты:['е','и'], раз:'Исключение — II.'},
    {q:'«Он кол_т дрова» (колоть)', в:0, варианты:['е','и'], раз:'На -оть — I.'},
    {q:'«Кит дыш_т» (дышать)', в:1, варианты:['е','и'], раз:'Исключение — II.'},
    {q:'«Юнга стел_т гамак» (стелить)', в:0, варианты:['е','и'], раз:'Стелить — I спр.'}
  ]};
  const Т3 = { имя:'Они…', задания:[
    {q:'«Матросы вяж_т узлы» (вязать)', в:0, варианты:['у','а'], раз:'I спр. — -ут.'},
    {q:'«Волны шум_т» (шуметь)', в:1, варианты:['ю','я'], раз:'Шумят — II спр.'},
    {q:'«Рыбаки лов_т рыбу» (ловить)', в:1, варианты:['ю','я'], раз:'На -ить — II: ловят.'},
    {q:'«Чайки кача_тся» (качаться)', в:0, варианты:['ю','я'], раз:'I спр. — качаются.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r872T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r872TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L872 = {
    id: ID,
    title: 'Спряжение глагола',
    ico: '☸️',
    src: 'Русский язык · 4 класс · Глагол', subj: 'rus',
    explain: [
      'Штурвал лиц: я пишу, ты пишешь… я вижу, ты видишь… Изменение глаголов по лицам и числам — спряжение.',
      'I спряжение: -ешь, -ет, -ем, -ете, -ут (-ют). II спряжение: -ишь, -ит, -им, -ите, -ат (-ят).',
      'Ударное окончание слышно: плывёшь, несёт, поём — I; стоишь, кричит, лежим — II.',
      'Безударное окончание: строить → на -ить → II → строишь.',
      'Исключения II спряжения: гнать, держать, дышать, слышать, смотреть, видеть, ненавидеть, зависеть, вертеть, обидеть, терпеть.',
      'Остальные глаголы — I спряжение: дуть → дует.',
      '3-е лицо мн. ч.: драить → II → драят.',
      'Во 2-м лице ед. ч. после ш пишется ь: слышишь.',
      'Смотрит — исключение II; качают — I.',
      'Итог: не слышно — спроси «что делать?».',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: ударные окончания.',
      'Тренажёр 2: е или и.',
      'Тренажёр 3: окончания 3-го лица мн. ч.'
    ],
    check: {
      q: '«Ты видишь» — какое спряжение?',
      choices: ['I','II'],
      ans: 1,
      exp: 'Видеть — исключение, II спряжение.'
    },
    tasks: [
      { q:'Сколько спряжений у глаголов?', kind:'unit', ans:2, tol:0,
        hints:['I и II.'], sol:'2.' },
      { q:'«Они пишут» — какое спряжение?', kind:'choice', choices:['I','II'], ans:0, tol:0,
        hints:['Окончание -ут.'], sol:'I.' },
      { q:'«Ты стро_шь» — какая буква?', kind:'choice', choices:['е','и'], ans:1, tol:0,
        hints:['Строить — на -ить.'], sol:'и.' }
    ]
  };

  function render(el){
    if(!window.РМ){ el.innerHTML=''; return; }
    css();
    const s = S();
    const step = Math.max(0, Math.min(L872.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    s.было1=null;
    const ЗАГОЛОВКИ={1:'Штурвал лиц',2:'Две вахты',3:'Смена вахты',4:'Плот на берегу',5:'Список штурмана',
      6:'Ветер в парусах',7:'Юнги драят палубу',8:'Бьют склянки',9:'Вахтенный журнал',10:'Ночная вахта',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l872" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Спряжение'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r872Лицо=(i)=>{ const s=S(); s.было1=s.лицо1==null?0:s.лицо1; s.лицо1=i;
    const в=Array.isArray(s.вид1)?s.вид1.slice():[0]; if(!в.includes(i)) в.push(i); s.вид1=в; chRender(0); };
  window.r872Отв=(f,к)=>{ const s=S(); s['ответ'+f]=к; if(f===1&&к===0) s.дело_штурвал=true; chRender(0); };
  window.r872Глаг=(i)=>{ const s=S(); s.выбор3=СОРТ[i].w; s.отв3=null; chRender(0); };
  window.r872Вахта=(с)=>{ const s=S(); const w=s.выбор3; if(!w) return;
    const г=СОРТ.find(x=>x.w===w);
    if(г.с===с){ const р=Object.assign({},s.разв3||{}); р[w]=с; s.разв3=р;
      s.порядок3=(s.порядок3||[]).concat([w]); s.выбор3=null; s.отв3={w:w,ок:true};
      if(СОРТ.every(x=>р[x.w])) s.дело_вахты=true;
    } else s.отв3={w:w,с:с,ок:false};
    chRender(0); };
  window.r872НФ=()=>{ const s=S(); s.нф4=true; chRender(0); };
  window.r872Проп=(i,б)=>{ const s=S(); const р=Object.assign({},s.реш9||{}); р[i]=б; s.реш9=р;
    if(ПРОПУСКИ.every((x,k)=>р[k]===x.верно)) s.дело_журнал=true; chRender(0); };
  window.r872Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r872Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r872T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r872TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L872; else arr.push(L872); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.RU872={render:render, L:L872};
})();
