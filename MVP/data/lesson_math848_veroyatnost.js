/* ============ МАТЕМАТИКА · УРОК 848 · «ЧАСТОТА И ВЕРОЯТНОСТЬ: ОПЫТЫ» ============
   6 класс. Сюжет: бросим кубик сто раз и запишем, что выпало.

   СВЕРКА С ПЕТЕРСОНОМ (6 класс): случайные события, вероятность, частота
   события, равновозможные исходы, сравнение частоты с вероятностью.
   Источники: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     у кубика 6 равновозможных исходов, вероятность каждой грани 1/6 ≈ 0,1667
     опыт 10 бросков: выпало 3,6,1,4,6,2,5,6,3,2 →
       счётчики 1,2,2,1,1,3; частоты 0,10 0,20 0,20 0,10 0,10 0,30
     опыт 100 бросков: счётчики 15,18,17,16,19,15 (в сумме 100),
       частоты 0,15 0,18 0,17 0,16 0,19 0,15
     опыт 1000 бросков: счётчики 168,165,171,164,166,166 (в сумме 1000),
       отклонения от 1/6: 0,0013 0,0017 0,0043 0,0027 0,0007 0,0007
     события: чётное {2,4,6} → 3/6 = 1/2; больше 4 {5,6} → 2/6 = 1/3;
       хотя бы 3 {3,4,5,6} → 4/6 = 2/3
     два кубика: 36 равновозможных пар; суммы 2..12 имеют 1,2,3,4,5,6,5,4,3,2,1
       способов; сумма 7 — шесть способов, вероятность 6/36 = 1/6
     монета: две равновозможные стороны, вероятность каждой 1/2
     мешок: 3 красных и 2 синих шара → вероятность красного 3/5 = 0,6

   ДЕТАЛИЗАЦИЯ РИСУНКОВ (просьба владельца — «ещё более детально»):
     • КУБИК с точками в правильных местах: 1 — центр, 2 — по диагонали,
       3 — диагональ и центр, 4 — четыре угла, 5 — углы и центр,
       6 — два столбца по три; точки с бликом и тенью, грань с фаской;
     • ТАБЛИЦЫ частот: строка на каждую грань — счётчик, дробь и десятичная
       дробь, строки появляются по очереди, итоговая строка внизу;
     • ДИАГРАММА частот со шкалой, сеткой, подписями значений и штриховой
       линией теоретической вероятности 1/6;
     • ТРИ ПАНЕЛИ СХОДИМОСТИ: 10, 100 и 1000 бросков — видно, как столбики
       подтягиваются к линии;
     • СЕТКА 6×6: все 36 пар двух кубиков с их суммами, диагональ суммы 7
       подсвечивается по клеткам;
     • МОНЕТА с орлом и решкой, МЕШОК С ШАРАМИ, у каждого предмета две тени.

   АНИМАЦИЯ: SMIL calcMode="spline", keySplines="0.23 1 0.32 1".
   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, keyTimes строго в [0;1], reduced-motion выключает
   SMIL полностью, ничего не выходит за край кадра. */
(function(){
  'use strict';

  const ID = 848;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';
  const КРАС='#e8735a', СИН='#5fa8d8';

  const CSS=`
  #lvis .s6.l848{gap:14px}
  #lvis .s6.l848 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l848 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l848 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l848 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l848 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l848 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l848 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l848 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l848 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l848 .правило b{color:${GOLD}}
  #lvis .s6.l848 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l848 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l848 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l848 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l848 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l848 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l848 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l848 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l848 .уровни .точка.сейчас{background:${GOLD};animation:l848dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l848dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l848 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l848{-webkit-text-size-adjust:100%}
  #lvis .s6.l848 [data-anim]{animation:l848rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l848rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l848 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l848 [data-anim]{animation:none!important}
    #lvis .s6.l848 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l848 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l848-style');
      if(!s){ s=document.createElement('style'); s.id='l848-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анВК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');
  const растёт = (длит,доля,конец,W) => анК('width','1;1;'+W+';'+W,длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');
  const растётВ = (длит,доля,конец,H) => анК('height','1;1;'+H+';'+H,длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c848-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2a3f33"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#111c15"/>
      </linearGradient>
      <linearGradient id="c848-грань" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.45" stop-color="#f0e6cd"/>
        <stop offset="1" stop-color="#cbbf9e"/>
      </linearGradient>
      <linearGradient id="c848-бок" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#b9ad8c"/><stop offset="1" stop-color="#8a7f61"/>
      </linearGradient>
      <linearGradient id="c848-столбик" x1="0" y1="1" x2="0" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2b6d99"/><stop offset="0.45" stop-color="#5fa8d8"/>
        <stop offset="1" stop-color="#a8dcff"/>
      </linearGradient>
      <linearGradient id="c848-металл" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#5d6a76"/><stop offset="0.18" stop-color="#e8eef4"/>
        <stop offset="0.5" stop-color="#9fb0bd"/><stop offset="0.8" stop-color="#57636f"/>
        <stop offset="1" stop-color="#434d57"/>
      </linearGradient>
      <linearGradient id="c848-золото" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#8a6418"/><stop offset="0.2" stop-color="#ffe9a8"/>
        <stop offset="0.5" stop-color="#dcae43"/><stop offset="0.8" stop-color="#a87c1c"/>
        <stop offset="1" stop-color="#6f5210"/>
      </linearGradient>
      <linearGradient id="c848-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <linearGradient id="c848-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".6"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".22"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".2"/>
      </linearGradient>
      <filter id="c848-тень" x="-70%" y="-70%" width="240%" height="240%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3" result="р"/>
        <feOffset in="р" dx="3" dy="4" result="с"/>
        <feFlood flood-color="#0a1a24" flood-opacity="0.4" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c848-контакт" x="-70%" y="-150%" width="240%" height="400%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.2" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#1d1508" flood-opacity="0.4" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c848-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.1"/>
      </filter>
      <filter id="c848-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c848-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c848-шум)" opacity="0.05"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.84)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* ТОЧКИ НА ГРАНИ: правильные раскладки для 1..6 */
  const ТОЧКИ_ГРАНИ = {
    1:[[0.5,0.5]],
    2:[[0.3,0.3],[0.7,0.7]],
    3:[[0.28,0.28],[0.5,0.5],[0.72,0.72]],
    4:[[0.3,0.3],[0.7,0.3],[0.3,0.7],[0.7,0.7]],
    5:[[0.3,0.3],[0.7,0.3],[0.5,0.5],[0.3,0.7],[0.7,0.7]],
    6:[[0.3,0.28],[0.7,0.28],[0.3,0.5],[0.7,0.5],[0.3,0.72],[0.7,0.72]]
  };

  /* КУБИК: грань с фаской, точки с бликом */
  const кубик = (x,y,размер,грань,нач,поворот) => {
    const p=ТОЧКИ_ГРАНИ[грань]||ТОЧКИ_ГРАНИ[1], r=размер*0.085;
    return `<g transform="rotate(${поворот||0} ${x+размер/2} ${y+размер/2})">
      <rect x="${x+3}" y="${y+4}" width="${размер}" height="${размер}" rx="${размер*0.16}"
        fill="#08171f" opacity=".34" filter="url(#c848-тень)"/>
      <rect x="${x}" y="${y}" width="${размер}" height="${размер}" rx="${размер*0.16}"
        fill="url(#c848-грань)" stroke="${ОБВОД}" stroke-width="1.6">
        ${нач!=null?проявить('14s',нач,нач+0.05):''}</rect>
      <rect x="${x+2}" y="${y+размер-размер*0.18}" width="${размер-4}" height="${размер*0.14}"
        rx="${размер*0.06}" fill="url(#c848-бок)" opacity=".55"/>
      <path d="M${x+размер*0.12} ${y+размер*0.24} L${x+размер*0.12} ${y+размер*0.12} L${x+размер*0.24} ${y+размер*0.12}"
        fill="none" stroke="rgba(255,255,255,.7)" stroke-width="1.4"/>
      ${p.map(([cx,cy])=>{
        const px=x+размер*cx, py=y+размер*cy;
        return `<circle cx="${px}" cy="${py}" r="${r}" fill="#2b2110"/>
          <circle cx="${px-r*0.3}" cy="${py-r*0.3}" r="${r*0.34}" fill="rgba(255,255,255,.55)"/>`;
      }).join('')}
    </g>`;
  };

  /* СТОЛБИК диаграммы: значение задаётся долей от максимума */
  const столбик = (x,основание,ш,высМакс,доля,цвет,нач,конец,подписьЗнач) => {
    const в=высМакс*доля;
    return `<g>
      <rect x="${x}" y="${основание}" width="${ш}" height="1" rx="3" fill="url(#c848-${цвет})">
        ${анК('y',основание+';'+основание+';'+(основание-в)+';'+(основание-в),'14s','0;'+кт(нач)+';'+кт(конец)+';1')}
        ${растётВ('14s',нач,конец,в)}</rect>
      <rect x="${x}" y="${основание-в}" width="${ш}" height="3" rx="1.5" fill="url(#c848-металл)">
        ${анК('y',основание+';'+основание+';'+(основание-в)+';'+(основание-в),'14s','0;'+кт(нач)+';'+кт(конец)+';1')}</rect>
      ${подписьЗнач?`<g>${проявить('14s',конец,конец+0.05)}
        ${т(x+ш/2,основание-в-8,подписьЗнач,12,GOLD,true)}</g>`:''}
    </g>`;
  };

  /* СТРОКА ТАБЛИЦЫ */
  const строкаТ = (x,y,ш,слева,центр,справа,нач,цвет) => `<g>${проявить('14s',нач,нач+0.04)}
    <line x1="${x}" y1="${y+11}" x2="${x+ш}" y2="${y+11}" stroke="rgba(120,100,60,.3)" stroke-width="1"/>
    ${т(x+8,y+5,слева,12,цвет||'#2b2110',true,'start')}
    ${т(x+ш*0.52,y+5,центр,12,'#5a4a2a',false,'start')}
    ${т(x+ш-6,y+5,справа,12,'#2f6b4a',true,'end')}
  </g>`;

  const бумага = (x,y,ш,в,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="6" fill="#08171f" opacity=".36" filter="url(#c848-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="url(#c848-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('14s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="none" filter="url(#c848-шум)" opacity=".08"/>
  </g>`;

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#08171f" opacity=".32" filter="url(#c848-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c848-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('14s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  /* МОНЕТА: орёл (круг с точкой) или решка */
  const монета = (x,y,r,сторона,нач) => `<g>
    <ellipse cx="${x+2}" cy="${y+r+3}" rx="${r*1.05}" ry="${r*0.24}" fill="#08171f" opacity=".36" filter="url(#c848-тень)"/>
    <circle cx="${x}" cy="${y}" r="${r}" fill="url(#c848-золото)" stroke="rgba(60,45,10,.55)" stroke-width="1.2">
      ${нач!=null?проявить('14s',нач,нач+0.04):''}</circle>
    <circle cx="${x}" cy="${y}" r="${r*0.72}" fill="none" stroke="rgba(255,255,255,.4)" stroke-width="1.2"/>
    ${сторона==='о'?`${т(x,y+r*0.22,'О',24,'#5a4410',true)}`
                   :`${т(x,y+r*0.22,'Р',24,'#5a4410',true)}`}
    <circle cx="${x-r*0.34}" cy="${y-r*0.38}" r="${r*0.14}" fill="#fff" opacity=".8" filter="url(#c848-мягко)"/>
  </g>`;

  /* ШАР в мешке */
  const шар = (x,y,r,цвет,нач) => `<g>${проявить('14s',нач,нач+0.04)}
    <circle cx="${x}" cy="${y}" r="${r}" fill="${цвет}" stroke="${ОБВОД}" stroke-width="1.2"/>
    <circle cx="${x-r*0.32}" cy="${y-r*0.34}" r="${r*0.28}" fill="#fff" opacity=".55" filter="url(#c848-мягко)"/>
  </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Кубик бросают: предсказать нельзя */
  function F1(s){
    const в = s.прогноз;
    return ВОПРОС('Кубик бросают один раз. Можно ли заранее сказать, что выпадет?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Бросок кубика',14,GOLD,true)}
        <g>${анВК('0 120 140;0 120 140;360 120 140;360 120 140;0 120 140','14s','0;0.2;0.6;0.86;1')}
          ${кубик(84,104,72,5,0.06)}
        </g>
        ${кубик(210,110,60,3,0.2,8)}
        <g>${проявить('14s',0.5,0.6)}
          ${подпись(120,86,'может выпасть любая грань',ИНК,12)}</g>
        <g>${проявить('14s',0.7,0.8)}
          ${т(168,208,'заранее ответить нельзя — это случайное событие',12,МУТ)}
          ${подпись(168,236,'но можно оценить шансы',GOLD,12)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нельзя'?'hit':(в?'miss':''), 'Нельзя: любая из шести граней равновозможна', "r848Прогноз('нельзя')")}
        ${BTN(4, в==='можно'?'miss':'', 'Можно: чаще выпадает шестёрка', "r848Прогноз('можно')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нельзя',
        в==='нельзя' ? 'Верно: исход случаен, предсказать один бросок нельзя. Зато можно посчитать вероятность каждой грани.'
                     : 'Нет: ни одна грань не выпадает чаще другой. Все шесть исходов равновозможны.') : ЖДЁТ()) +
      ПРАВИЛО('Один бросок предсказать нельзя, но <b>шансы считаются</b>.');
  }

  /* 2. Шесть равновозможных исходов */
  function F2(s){
    const в = s.исходы;
    return ВОПРОС('Чему равна вероятность выпадения каждой грани?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Шесть равновозможных исходов',14,GOLD,true)}
        ${[1,2,3,4,5,6].map((г,i)=>кубик(22+i*50,40,44,г,0.04+i*0.05)).join('')}
        ${[1,2,3,4,5,6].map((г,i)=>т(44+i*50,98,String(г),12,МУТ,true)).join('')}
        <g>${проявить('14s',0.42,0.52)}
          ${стекло(16,112,304,54,0.42)}
          ${т(168,136,'благоприятный исход — 1, всех исходов — 6',12,МУТ)}
          ${т(168,158,'вероятность = 1/6 ≈ 0,17',20,GREEN,true)}</g>
        <g>${проявить('14s',0.66,0.76)}
          ${т(168,192,'вероятность — это доля, она не больше единицы',12,ИНК)}
          ${т(168,216,'сумма вероятностей всех исходов равна 1',12,МУТ)}
          ${подпись(168,242,'1/6 + 1/6 + 1/6 + 1/6 + 1/6 + 1/6 = 1',GOLD,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шестая'?'hit':(в?'miss':''), '1/6 ≈ 0,17 — одна шестая', "r848Исходы('шестая')")}
        ${BTN(4, в==='половина'?'miss':'', '1/2 — половина', "r848Исходы('половина')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шестая',
        в==='шестая' ? 'Верно: исходов шесть и все равновозможны, поэтому вероятность каждого равна 1/6 ≈ 0,17.'
                     : 'Нет: половина была бы при двух равных исходах. Здесь их шесть, значит 1/6.') : ЖДЁТ()) +
      ПРАВИЛО('Вероятность = <b>число благоприятных исходов : число всех исходов</b>.');
  }

  /* 3. Опыт: 10 бросков */
  function F3(s){
    const в = s.опыт10;
    const броски=[3,6,1,4,6,2,5,6,3,2];
    return ВОПРОС('Сделали 10 бросков. Сколько раз выпала шестёрка?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Десять бросков подряд',14,GOLD,true)}
        ${броски.map((г,i)=>{
          const к=i%5, р=Math.floor(i/5);
          return `<g>${проявить('14s',0.04+i*0.045,0.1+i*0.045)}
            ${кубик(24+к*62,44+р*62,38,г)}
            ${т(43+к*62,44+р*62+50,String(г),12,г===6?GREEN:МУТ,true)}</g>`;
        }).join('')}
        <g>${проявить('14s',0.62,0.72)}
          ${подпись(168,196,'шестёрка выпала 3 раза из 10',GREEN,12)}</g>
        <g>${проявить('14s',0.8,0.88)}
          ${подпись(168,230,'частота = 3/10 = 0,3',GOLD,14)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), '3 раза: частота 3/10 = 0,3', "r848Опыт10('три')")}
        ${BTN(4, в==='две'?'miss':'', '2 раза: частота 0,2', "r848Опыт10('две')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: шестёрка выпала три раза, значит частота равна 3 : 10 = 0,3 — больше вероятности 0,17.'
                  : 'Нет: посчитай шестёрки в ряду — их три из десяти. Частота 0,3.') : ЖДЁТ()) +
      ПРАВИЛО('Частота = <b>сколько раз случилось : сколько всего опытов</b>.');
  }

  /* 4. Таблица частот за 10 бросков */
  function F4(s){
    const в = s.таблица10;
    const данные=[[1,1],[2,2],[3,2],[4,1],[5,1],[6,3]];
    return ВОПРОС('Какая грань выпадала чаще всех в десяти бросках?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Таблица частот: 10 бросков',14,GOLD,true)}
        ${бумага(14,38,308,176,0.04,0)}
        ${т(56,58,'грань',12,'#7a6a44',true)}
        ${т(150,58,'счёт',12,'#7a6a44',true)}
        ${т(238,58,'частота',12,'#7a6a44',true)}
        ${данные.map(([г,к],i)=>строкаТ(20,80+i*21,296,String(г),к+' раз',(к/10).toFixed(2).replace('.',','),'',0.08+i*0.06,г===6?GREEN:'#2b2110')).join('')}
        <g>${проявить('14s',0.5,0.6)}
          <line x1="20" y1="194" x2="316" y2="194" stroke="rgba(120,100,60,.5)" stroke-width="1.2"/>
          ${т(56,206,'всего',12,'#2b2110',true,'start')}
          ${т(150,206,'10',12,'#2b2110',true)}
          ${т(262,206,'сумма долей 1,00',12,'#2f6b4a',true)}</g>
        <g>${проявить('14s',0.74,0.84)}
          ${подпись(168,238,'чаще всех выпадала шестёрка',GREEN,12)}</g>
      `,264)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), 'Шестёрка: 3 раза из 10, частота 0,30', "r848Таб10('шесть')")}
        ${BTN(4, в==='поровну'?'miss':'', 'Все грани выпадали одинаково', "r848Таб10('поровну')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: у шестёрки счёт 3 — больше, чем у остальных граней. В десяти опытах так бывает часто.'
                    : 'Нет: в таблице видно, что счёты разные: у шестёрки 3, а у первой и четвёртой по 1.') : ЖДЁТ()) +
      ПРАВИЛО('В коротком опыте частоты <b>сильно расходятся</b> с вероятностью.');
  }

  /* 5. Опыт 100 бросков */
  function F5(s){
    const в = s.опыт100;
    const данные=[[1,15],[2,18],[3,17],[4,16],[5,19],[6,15]];
    return ВОПРОС('Сделали 100 бросков. Какая частота у грани 5?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Таблица частот: 100 бросков',14,GOLD,true)}
        ${бумага(14,36,308,182,0.04,0)}
        ${т(56,56,'грань',12,'#7a6a44',true)}
        ${т(150,56,'счёт',12,'#7a6a44',true)}
        ${т(240,56,'частота',12,'#7a6a44',true)}
        ${данные.map(([г,к],i)=>строкаТ(20,62+i*22,296,String(г),String(к)+' раз',(к/100).toFixed(2).replace('.',','),0,0.06+i*0.05,г===5?GREEN:'#2b2110')).join('')}
        <g>${проявить('14s',0.44,0.54)}
          <line x1="20" y1="194" x2="316" y2="194" stroke="rgba(120,100,60,.5)" stroke-width="1.2"/>
          ${т(56,206,'всего',12,'#2b2110',true,'start')}
          ${т(150,212,'100',12,'#2b2110',true)}
          ${т(268,212,'все частоты ≈ 0,17',12,'#2f6b4a',true)}</g>
        <g>${проявить('14s',0.7,0.8)}
          ${подпись(168,240,'0,19 против теории 0,17',GREEN,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='девятнадцать'?'hit':(в?'miss':''), '0,19: 19 раз из 100', "r848Опыт100('девятнадцать')")}
        ${BTN(4, в==='шестнадцать'?'miss':'', '0,16: 16 раз из 100', "r848Опыт100('шестнадцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='девятнадцать',
        в==='девятнадцать' ? 'Верно: у грани 5 счёт 19, значит частота 19 : 100 = 0,19. Это уже близко к 1/6 ≈ 0,17.'
                           : 'Нет: 16 раз — это грань 4. У грани 5 счёт 19, частота 0,19.') : ЖДЁТ()) +
      ПРАВИЛО('В ста опытах частоты <b>подходят ближе</b> к вероятности.');
  }

  /* 6. Диаграмма частот и линия вероятности */
  function F6(s){
    const в = s.диаграмма;
    const частоты=[0.15,0.18,0.17,0.16,0.19,0.15];
    const основание=206, высМакс=120;
    const линияТеории=основание-высМакс*(1/6)/0.20;
    return ВОПРОС('Где на диаграмме проходит линия теоретической вероятности?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Частоты и вероятность 1/6',14,GOLD,true)}
        ${[0,0.05,0.10,0.15,0.20].map(д=>{
          const y=основание-высМакс*д/0.20;
          return `<g>${проявить('14s',0.04,0.1)}
            <line x1="40" y1="${y.toFixed(1)}" x2="320" y2="${y.toFixed(1)}" stroke="rgba(255,255,255,.12)" stroke-width="1"/></g>`;
        }).join('')}
        <line x1="40" y1="${линияТеории.toFixed(1)}" x2="320" y2="${линияТеории.toFixed(1)}"
          stroke="${GOLD}" stroke-width="1.8" stroke-dasharray="7 5">${проявить('14s',0.5,0.6)}</line>
        ${частоты.map((ч,i)=>столбик(54+i*44,основание,30,высМакс,ч/0.20,'столбик',0.1+i*0.06,0.3+i*0.06,ч.toFixed(2).replace('.',','))).join('')}
        <line x1="40" y1="${основание}" x2="320" y2="${основание}" stroke="${ЛИНИЯ}" stroke-width="2"/>
        ${[1,2,3,4,5,6].map((г,i)=>т(69+i*44,220,String(г),12,МУТ,true)).join('')}
        <g>${проявить('14s',0.72,0.82)}
          ${т(200,52,'частота',12,МУТ,false,'start')}
          ${подпись(276,52,'1/6 ≈ 0,17',GOLD,12)}</g>
        <g>${проявить('14s',0.86,0.94)}
          ${подпись(168,244,'штриховая линия — вероятность 1/6',GREEN,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='посередине'?'hit':(в?'miss':''), 'На уровне 0,17 — чуть выше середины шкалы', "r848Диагр('посередине')")}
        ${BTN(4, в==='внизу'?'miss':'', 'В самом низу, у нуля', "r848Диагр('внизу')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='посередине',
        в==='посередине' ? 'Верно: вероятность одной грани 1/6 ≈ 0,17, и почти все столбики оказались рядом с этой линией.'
                         : 'Нет: внизу была бы нулевая вероятность. У каждой грани 1/6 ≈ 0,17.') : ЖДЁТ()) +
      ПРАВИЛО('Столбики частот <b>колеблются вокруг</b> линии вероятности.');
  }

  /* 7. Чем больше опытов, тем точнее */
  function F7(s){
    const в = s.сходимость;
    const наборы=[
      ['10', [0.10,0.20,0.20,0.10,0.10,0.30]],
      ['100',[0.15,0.18,0.17,0.16,0.19,0.15]],
      ['1000',[0.168,0.165,0.171,0.164,0.166,0.166]]
    ];
    return ВОПРОС('Что происходит с частотами, когда опытов становится больше?') +
      `<div class="pic">${свг(`
        ${фон(272)}
        ${т(168,24,'10, 100 и 1000 бросков',14,GOLD,true)}
        ${наборы.map(([имя,частоты],п)=>{
          const x0=16+п*106, основание=196, выс=120;
          const линия=основание-выс*(1/6)/0.32;
          return `<g>${проявить('14s',0.05+п*0.1,0.14+п*0.1)}
            <rect x="${x0}" y="44" width="100" height="160" rx="6" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.2"/>
            <line x1="${x0+8}" y1="${линия.toFixed(1)}" x2="${x0+92}" y2="${линия.toFixed(1)}"
              stroke="${GOLD}" stroke-width="1.4" stroke-dasharray="5 4"/>
            ${частоты.map((ч,i)=>`<g>${проявить('14s',0.12+п*0.1+i*0.02,0.2+п*0.1+i*0.02)}
              <rect x="${x0+12+i*14}" y="${(основание-выс*ч/0.32).toFixed(1)}" width="10"
                height="${(выс*ч/0.32).toFixed(1)}" rx="2" fill="url(#c848-столбик)"/></g>`).join('')}
            <line x1="${x0+8}" y1="${основание}" x2="${x0+92}" y2="${основание}" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
            ${подпись(x0+50,196+30,имя+' бросков',GOLD,12)}</g>`;
        }).join('')}
        <g>${проявить('14s',0.6,0.7)}
          ${т(168,240,'разброс столбиков уменьшается',14,ИНК,true)}
          ${т(168,262,'частота подходит к вероятности',12,МУТ)}</g>
      `,272)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='ближе'?'hit':(в?'miss':''), 'Они всё ближе к вероятности 1/6', "r848Сход('ближе')")}
        ${BTN(4, в==='дальше'?'miss':'', 'Они всё дальше от вероятности', "r848Сход('дальше')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='ближе',
        в==='ближе' ? 'Верно: при 10 бросках частоты разбросаны, при 100 подтянулись, при 1000 почти встали на 1/6.'
                    : 'Нет: наоборот, чем больше опытов, тем теснее частоты собираются вокруг вероятности.') : ЖДЁТ()) +
      ПРАВИЛО('Чем больше опытов, тем <b>точнее частота предсказывает вероятность</b>.');
  }

  /* 8. Вероятность события */
  function F8(s){
    const в = s.событие;
    return ВОПРОС('Какова вероятность, что выпадет число больше четырёх?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Событие: выпало больше 4',14,GOLD,true)}
        ${[1,2,3,4,5,6].map((г,i)=>{
          const подходит=г>4;
          return `<g>${проявить('14s',0.04+i*0.05,0.1+i*0.05)}
            ${кубик(22+i*50,44,44,г,null)}
            ${подпись(44+i*50,104,подходит?'✓':'✗',подходит?GREEN:МУТ,12)}</g>`;
        }).join('')}
        <g>${проявить('14s',0.42,0.52)}
          ${стекло(16,122,304,48,0.42)}
          ${т(168,152,'подходят 5 и 6 — это два исхода из шести',12,МУТ)}</g>
        <g>${проявить('14s',0.62,0.72)}
          ${подпись(168,192,'вероятность 2/6 = 1/3 ≈ 0,33',GREEN,14)}</g>
        <g>${проявить('14s',0.8,0.88)}
          ${т(168,222,'у события «выпало чётное»: 3/6 = 1/2',12,ИНК)}
          ${т(168,242,'вероятности разных событий разные',12,МУТ)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='треть'?'hit':(в?'miss':''), '1/3 ≈ 0,33: подходят 5 и 6 из шести', "r848Событие('треть')")}
        ${BTN(4, в==='половина'?'miss':'', '1/2: подходит половина граней', "r848Событие('половина')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='треть',
        в==='треть' ? 'Верно: условию «больше четырёх» отвечают только 5 и 6, значит 2/6 = 1/3.'
                    : 'Нет: половина была бы у события «выпало чётное» — там три грани. А больше четырёх только две.') : ЖДЁТ()) +
      ПРАВИЛО('Вероятность события = <b>подходящие исходы : все исходы</b>.');
  }

  /* 9. Два кубика: сетка 6×6 */
  function F9(s){
    const в = s.два_кубика;
    const разм=25, x0=32, y0=58;
    return ВОПРОС('Два кубика бросают вместе. Сколько способов получить сумму 7?') +
      `<div class="pic">${свг(`
        ${фон(264)}
        ${т(168,24,'Все 36 пар двух кубиков',14,GOLD,true)}
        ${[1,2,3,4,5,6].map((г,i)=>т(x0+разм/2+i*разм,52,String(г),12,МУТ,true)).join('')}
        ${[1,2,3,4,5,6].map((г,i)=>т(x0-8,y0+разм/2+i*разм+4,String(г),12,МУТ,true)).join('')}
        ${[1,2,3,4,5,6].map((а,ia)=>[1,2,3,4,5,6].map((б,ib)=>{
          const сумма=а+б, наДиаг=сумма===7;
          return `<g>${проявить('14s',0.06+ia*0.05+ib*0.01,0.12+ia*0.05+ib*0.01)}
            <rect x="${x0+ib*разм}" y="${y0+ia*разм}" width="${разм-2}" height="${разм-2}" rx="3"
              fill="${наДиаг?'rgba(255,215,106,.3)':'rgba(255,255,255,.05)'}"
              stroke="${наДиаг?GOLD:ЛИНИЯ}" stroke-width="${наДиаг?1.6:1}"/>
            ${т(x0+ib*разм+разм/2-1,y0+ia*разм+разм/2+4,String(сумма),12,наДиаг?GOLD:ИНК,true)}</g>`;
        }).join('')).join('')}
        <g>${проявить('14s',0.62,0.72)}
          ${подпись(168,246,'сумма 7 — шесть клеток из 36',GREEN,12)}</g>
        <g>${проявить('14s',0.8,0.88)}
          ${т(168,222,'вероятность 6/36 = 1/6 ≈ 0,17',14,ИНК,true)}</g>
      `,264)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), '6 способов: 1+6, 2+5, 3+4, 4+3, 5+2, 6+1', "r848Два('шесть')")}
        ${BTN(4, в==='три'?'miss':'', '3 способа: 1+6, 2+5, 3+4', "r848Два('три')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: порядок кубиков разный, поэтому пар шесть: 1+6, 2+5, 3+4 и обратные. Вероятность 6/36 = 1/6.'
                    : 'Нет: кубики различимы, поэтому каждая пара считается дважды. Способов шесть, вероятность 1/6.') : ЖДЁТ()) +
      ПРАВИЛО('Всего пар <b>36</b>; сумма 7 выпадает <b>шестью</b> способами из 36.');
  }

  /* 10. Монета, мешок и сводка */
  function F10(s){
    const в = s.сводка;
    return ВОПРОС('В мешке 3 красных и 2 синих шара. Какова вероятность вынуть красный?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Другие опыты',14,GOLD,true)}
        ${монета(56,62,26,'о',0.05)}
        ${монета(120,62,26,'р',0.14)}
        ${подпись(88,110,'1/2 и 1/2',GOLD,12)}
        ${[0,1,2].map(i=>шар(196+i*30,58,13,КРАС,0.2+i*0.05)).join('')}
        ${[0,1].map(i=>шар(226+i*30,88,13,СИН,0.34+i*0.05)).join('')}
        ${подпись(232,124,'3 красных, 2 синих',ИНК,12)}
        <g>${проявить('14s',0.5,0.6)}
          ${стекло(16,140,304,44,0.5)}
          ${т(168,168,'вероятность красного 3/5 = 0,6',16,GREEN,true)}</g>
        <g>${проявить('14s',0.68,0.78)}
          ${подпись(168,206,'вероятность — про шансы, частота — про опыт',GOLD,12)}</g>
        <g>${проявить('14s',0.84,0.92)}
          ${т(168,236,'частота — то, что получилось в опыте',12,ИНК)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три пятых'?'hit':(в?'miss':''), '3/5 = 0,6: три красных из пяти шаров', "r848Свод('три пятых')")}
        ${BTN(4, в==='половина'?'miss':'', '1/2: красные и синие почти поровну', "r848Свод('половина')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три пятых',
        в==='три пятых' ? 'Верно: всего пять шаров, красных три, значит вероятность 3/5 = 0,6.'
                       : 'Нет: шаров пять, а не два вида поровну. Красных три из пяти, вероятность 0,6.') : ЖДЁТ()) +
      ПРАВИЛО('Вероятность считают по <b>числу исходов</b>, частота — по <b>результату опытов</b>.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Какова вероятность выпадения шестёрки на кубике?', варианты:[
        {т:'1/6 ≈ 0,17', ок:true,  почему:'Один благоприятный исход из шести.'},
        {т:'1/2 = 0,5', ок:false, почему:'Половина была бы при двух равных исходах.'}]},
    { вопрос:'В 10 бросках шестёрка выпала 3 раза. Какова частота?', варианты:[
        {т:'0,3', ок:true,  почему:'3 : 10 = 0,3.'},
        {т:'0,17', ок:false, почему:'0,17 — это вероятность, а не частота.'}]},
    { вопрос:'Что происходит с частотой при 1000 бросках?', варианты:[
        {т:'Приближается к 1/6', ок:true,  почему:'Чем больше опытов, тем точнее.'},
        {т:'Становится ровно 1/6', ок:false, почему:'Точно равна она может и не быть.'}]},
    { вопрос:'Какова вероятность выбросить число больше четырёх?', варианты:[
        {т:'1/3 ≈ 0,33', ок:true,  почему:'Подходят 5 и 6, значит 2/6.'},
        {т:'1/2 = 0,5', ок:false, почему:'Половина — у чётного числа.'}]},
    { вопрос:'Сколько способов получить сумму 7 на двух кубиках?', варианты:[
        {т:'6 из 36', ок:true,  почему:'1+6, 2+5, 3+4 и обратные.'},
        {т:'3 из 36', ок:false, почему:'Порядок кубиков разный, поэтому шесть.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Одна опора: вероятность считаем по исходам, частоту — по опытам, и чем опытов больше, тем ближе частота к вероятности.') +
        `<div class="ask">${BTN(4,'','пройти заново','r848Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r848Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Вероятность — <b>по исходам</b>, частота — <b>по опытам</b>.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · вероятность',
    задания:[
      { ф:'Вероятность шестёрки на кубике?', о:['1/6','1/2','1/3'], в:0, р:'Один исход из шести.' },
      { ф:'Вероятность чётного числа?', о:['1/2','1/3','1/6'], в:0, р:'2, 4, 6 — три исхода из шести.' },
      { ф:'Вероятность числа больше четырёх?', о:['1/3','1/2','2/3'], в:0, р:'5 и 6 — два из шести.' },
      { ф:'Вероятность орла при бросании монеты?', о:['1/2','1','0'], в:0, р:'Две равновозможные стороны.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · частота',
    задания:[
      { ф:'20 бросков, шестёрка 4 раза. Частота?', о:['0,2','0,17','0,4'], в:0, р:'4 : 20 = 0,2.' },
      { ф:'100 бросков, грань 1 выпала 15 раз. Частота?', о:['0,15','0,16','0,5'], в:0, р:'15 : 100.' },
      { ф:'Сколько всего исходов у кубика?', о:['6','12','36'], в:0, р:'Шесть граней.' },
      { ф:'Частота при 1000 бросках …', о:['близка к вероятности','равна единице'], в:0, р:'Чем больше опытов, тем точнее.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · два кубика и мешок',
    задания:[
      { ф:'Сколько всего пар у двух кубиков?', о:['36','12','6'], в:0, р:'6 · 6 = 36.' },
      { ф:'Сколько способов получить сумму 7?', о:['6','3','12'], в:0, р:'Порядок разный: 1+6 … 6+1.' },
      { ф:'Вероятность суммы 7 на двух кубиках?', о:['1/6','1/12','1/3'], в:0, р:'6 из 36.' },
      { ф:'В мешке 3 красных и 2 синих. Вероятность красного?', о:['0,6','0,5','0,3'], в:0, р:'3 из 5.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r848T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r848TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L848={
    id: ID, title:'Частота и вероятность: опыты', ico:'🎲',
    src:'Математика · 6 класс · Вероятность', subj:'math',
    explain: [
      'Кубик бросают один раз. Что выпадет — предсказать нельзя: может выйти любая из шести граней. Такое событие называют случайным. Но это не значит, что о нём нельзя ничего сказать: шансы всех исходов можно посчитать заранее. Этим и занимается теория вероятностей.',

      'У кубика шесть граней, и ни одна из них не выделена: все шесть исходов равновозможны. Благоприятным исходом для события «выпала шестёрка» будет только один — сама шестёрка. Значит, вероятность равна 1/6, то есть примерно 0,17. Вероятность — это доля, поэтому она всегда от 0 до 1, а сумма вероятностей всех исходов равна единице: шесть раз по 1/6 как раз даёт 1. Частая ошибка — считать вероятность больше единицы. Как проверить себя: сложи вероятности всех исходов.',

      'Проверим на опыте. Бросим кубик десять раз и запишем, что выпало: 3, 6, 1, 4, 6, 2, 5, 6, 3, 2. Шестёрка выпала три раза. Отношение числа случившихся событий к общему числу опытов называют частотой: 3 : 10 = 0,3. Обратите внимание: частота 0,3 заметно отличается от вероятности 0,17 — в коротком опыте так бывает почти всегда. Частая ошибка — считать, что в десяти бросках каждая грань выпадет ровно один-два раза. Как проверить себя: пересчитай исходы в записи.',

      'Занесём результаты в таблицу. В первой колонке — номер грани, во второй — сколько раз она выпала, в третьей — частота. У грани 1 счёт 1 и частота 0,10; у грани 2 — 2 и 0,20; у грани 3 — 2 и 0,20; у грани 4 — 1 и 0,10; у грани 5 — 1 и 0,10; у грани 6 — 3 и 0,30. Сумма счётов равна 10, а сумма частот — единице. Частая ошибка — забыть, что частоты в сумме дают единицу. Как проверить себя: сложи третий столбец.',

      'Теперь сделаем сто бросков. Счёты получились такие: 15, 18, 17, 16, 19, 15 — в сумме ровно 100. Частоты: 0,15; 0,18; 0,17; 0,16; 0,19; 0,15. Обратите внимание, насколько они ближе к 1/6, чем в десяти бросках: раньше были и 0,10, и 0,30, а теперь всё уместилось между 0,15 и 0,19. Частая ошибка — считать, что при ста бросках частоты станут точно равны 1/6. Как проверить себя: сравни разброс значений.',

      'Изобразим частоты столбиками. По горизонтальной оси — грани от 1 до 6, по вертикальной — частота. Проведём на уровне 1/6 ≈ 0,17 штриховую линию: это теоретическая вероятность. Видно, что столбики колеблются вокруг неё: один чуть выше, другой чуть ниже. Так и должно быть: вероятность задаёт центр, вокруг которого гуляют частоты. Частая ошибка — считать, что столбики обязаны точно совпасть с линией. Как проверить себя: посмотри, с какой стороны от линии оказался каждый столбик.',

      'Сравним три опыта: 10, 100 и 1000 бросков. В десяти бросках столбики разбросаны широко: от 0,10 до 0,30. В ста — уже теснее: от 0,15 до 0,19. В тысяче бросков частоты почти встали на линию: 0,168; 0,165; 0,171; 0,164; 0,166; 0,166 — отклонения меньше пяти тысячных. Это важное свойство: чем больше опытов, тем ближе частота к вероятности. Частая ошибка — думать, что при большом числе опытов совпадение станет точным. Как проверить себя: посмотри на разброс в третьей колонке.',

      'Часто спрашивают не про одну грань, а про событие. Например: какова вероятность, что выпадет число больше четырёх? Благоприятные исходы — это 5 и 6, их два из шести, значит вероятность 2/6 = 1/3 ≈ 0,33. А для события «выпало чётное число» подходят 2, 4 и 6 — три исхода, вероятность 3/6 = 1/2. Одно и то же число исходов даёт разные вероятности для разных событий. Частая ошибка — считать благоприятные исходы по числу, а не по условию. Как проверить себя: выпиши подходящие исходы.',

      'Разберём опыт с двумя кубиками. Всего пар может быть 36, и все они равновозможны. Запишем в таблицу 6 на 6 сумму выпавших чисел. Сумма 7 получается шестью способами: 1 + 6, 2 + 5, 3 + 4, 4 + 3, 5 + 2, 6 + 1 — эти клетки образуют диагональ. Значит, вероятность равна 6/36 = 1/6. А вот сумма 2 или 12 выпадает только одним способом: вероятность 1/36. Частая ошибка — считать 3 + 4 и 4 + 3 одним способом. Как проверить себя: посчитай клетки в таблице.',

      'Ещё опыты. Монета: у неё две равновозможные стороны, поэтому вероятность орла и решки равна 1/2. В мешке 3 красных и 2 синих шара: всего пять шаров, благоприятных для красного три, значит вероятность 3/5 = 0,6. И запомним разницу: вероятность — это про шансы, вычисленные заранее, а частота — про то, что получилось в опыте. Частая ошибка — путать эти два слова. Как проверить себя: спроси, считаем мы по исходам или по результатам опытов.',

      'Пройти практику: пять уровней про вероятность одной грани, частоту в опыте, диаграмму, события и два кубика. Дальше ждут три тренажёра.',

      'Первый тренажёр — вероятность: считаем благоприятные исходы и делим на все.',

      'Второй тренажёр — частота: делим число случившихся событий на число опытов.',

      'Третий тренажёр — два кубика и мешок с шарами: разбираем все исходы по клеткам.'
    ],
    check: {
      q:'Какова вероятность выпадения шестёрки на кубике?',
      choices:['1/6 ≈ 0,17','1/2 = 0,5','1/3 ≈ 0,33'],
      ans:0,
      exp:'Всего шесть равновозможных исходов, благоприятный один: 1/6 ≈ 0,17.'
    },
    tasks: [
      { q:'В 10 бросках шестёрка выпала 3 раза. Какова частота?', kind:'choice',
        choices:['0,3','0,17'], ans:0,
        hints:['Частота — это отношение.','3 : 10.'],
        sol:'0,3.' },
      { q:'Какова вероятность выбросить число больше четырёх?', kind:'choice',
        choices:['1/3','1/2'], ans:0,
        hints:['Подходят 5 и 6.','2 : 6.'],
        sol:'1/3 ≈ 0,33.' },
      { q:'Сколько способов получить сумму 7 на двух кубиках?', kind:'choice',
        choices:['6 из 36','3 из 36'], ans:0,
        hints:['Порядок кубиков разный.','1+6, 2+5, 3+4 и обратные.'],
        sol:'6 из 36, вероятность 1/6.' },
      { q:'Что происходит с частотой при 1000 бросков?', kind:'choice',
        choices:['Приближается к 1/6','Становится ровно 1/6'], ans:0,
        hints:['Чем больше опытов, тем теснее разброс.','Но точного равенства может и не быть.'],
        sol:'Приближается к 1/6.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L848.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Бросок','Случайное событие'],
      2:['Шесть исходов','Вероятность 1/6'],
      3:['Опыт','Десять бросков'],
      4:['Таблица','Частоты за 10 бросков'],
      5:['Сто бросков','Частоты ближе к 1/6'],
      6:['Диаграмма','И линия вероятности'],
      7:['Сходимость','10, 100, 1000'],
      8:['Событие','Больше четырёх'],
      9:['Два кубика','36 пар и сумма 7'],
      10:['Монета и мешок','Вероятность и частота'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','Вероятность'],
      13:['Тренажёр 2','Частота'],
      14:['Тренажёр 3','Два кубика и мешок']
    };
    const з=ЗАГОЛОВКИ[f]||['Вероятность и частота','Опыты'];
    el.innerHTML = `<div class="s6 l848" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r848Прогноз=(к)=>{ S().прогноз=к; chRender(0); };
  window.r848Исходы=(к)=>{ S().исходы=к; chRender(0); };
  window.r848Опыт10=(к)=>{ S().опыт10=к; chRender(0); };
  window.r848Таб10=(к)=>{ S().таблица10=к; chRender(0); };
  window.r848Опыт100=(к)=>{ S().опыт100=к; chRender(0); };
  window.r848Диагр=(к)=>{ S().диаграмма=к; chRender(0); };
  window.r848Сход=(к)=>{ S().сходимость=к; chRender(0); };
  window.r848Событие=(к)=>{ S().событие=к; chRender(0); };
  window.r848Два=(к)=>{ S().два_кубика=к; chRender(0); };
  window.r848Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r848Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r848Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r848T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r848TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L848; else arr.push(L848);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA848={render:render, L:L848};
})();
