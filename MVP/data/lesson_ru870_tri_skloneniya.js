/* ============ РУССКИЙ ЯЗЫК · УРОК 870 · «ТРИ СКЛОНЕНИЯ» ============
   4 класс, «Имя существительное». Была заглушкой в soon_lessons.js — теперь урок.
   Путь «Открытое море», второй урок после 869 «Склонение существительных».
   Рисунки — общая библиотека моря MVP/data/ris_more.js (порт: боцман, ящики,
   грузовая стрела, трюмы в разрезе, пристань, склад, фонари).

   СЮЖЕТ. «Три трюма». Фрегат «Чайка» стоит в порту и грузится. Боцман ведёт
   грузовую ведомость: у фрегата три трюма, и каждый ящик со словом надо
   опустить в свой. «Земля», «стол» и «ночь» склоняются каждый по-своему —
   значит, и лежать им в разных трюмах.

   РУКАМИ: три ящика на пристани — выбираешь падеж, и слова на ярлыках меняются
   (к земле, к столу, к ночи: три разных окончания); погрузка — выбираешь ящик,
   стрела поднимает его, выбираешь трюм, ящик опускается вниз; «на палубе» —
   ставишь в начальную форму; пропуски е/и в журнале.

   ПРОВЕРЕНО ПО ПРОГРАММЕ 4 КЛАССА (Канакина, Горецкий):
     1-е склонение — существительные ж. и м. р. с окончаниями -а, -я
       (земля, мачта, юнга, дядя);
     2-е склонение — м. р. с нулевым окончанием и ср. р. с окончаниями -о, -е
       (стол, парус, якорь, море, весло);
     3-е склонение — ж. р. с мягким знаком на конце (ночь, соль, мель, пристань);
     склонение определяют по начальной форме (И. п. ед. ч.): на палубе → палуба;
     у слов во мн. ч. склонение не определяют: паруса → парус;
     м. р. на ь (якорь, конь, день) — 2-е склонение, не 3-е;
     безударные окончания: в Д. и П. п. 1-е скл. — е, 3-е — и, 2-е в П. п. — е;
     проверочные слова с ударным окончанием: земля, стол, степь. */
(function(){
  'use strict';

  const ID = 870;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f', ОБВОД='#33291e';
  const ЧЕРНИЛА='#2e2416', ОКОН='#b85a10';

  const ДЕЛА = [
    {ключ:'три',    имя:'Сравнить три слова',  итог:'три склонения'},
    {ключ:'трюмы',  имя:'Загрузить трюмы',     итог:'6 ящиков'},
    {ключ:'журнал', имя:'Вписать е или и',     итог:'к пристани, по палубе'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const ПАДЕЖИ = [
    {б:'И', вопр:'кто? что?',     пом:'вот'},
    {б:'Р', вопр:'кого? чего?',   пом:'нет'},
    {б:'Д', вопр:'кому? чему?',   пом:'к'},
    {б:'В', вопр:'кого? что?',    пом:'вижу'},
    {б:'Т', вопр:'кем? чем?',     пом:'любуюсь'},
    {б:'П', вопр:'о ком? о чём?', пом:'о'}
  ];
  const ТРИ_СЛОВА = [
    {осн:['земл','земл','земл','земл','земл','земл'], ок:['я','и','е','ю','ёй','е']},
    {осн:['стол','стол','стол','стол','стол','стол'], ок:['','а','у','','ом','е']},
    {осн:['ночь','ноч','ноч','ночь','ночь','ноч'],    ок:['','и','и','','ю','и']}
  ];
  const ГРУЗ = [
    {w:'мачта', т:1, why:'женский род, окончание -а'},
    {w:'парус', т:2, why:'мужской род, окончание нулевое'},
    {w:'соль',  т:3, why:'женский род, на конце ь'},
    {w:'дядя',  т:1, why:'мужской род, но окончание -я'},
    {w:'море',  т:2, why:'средний род, окончание -е'},
    {w:'мель',  т:3, why:'женский род, на конце ь'}
  ];
  const ПРОПУСКИ = [
    {до:'Шлюпка пристала к ', осн:'пристан', верно:'и', why:'пристань — ж. р. на ь, 3-е склонение: к пристани, как к степи'},
    {до:'юнга пробежал по ', осн:'палуб', верно:'е', why:'палуба — ж. р. на -а, 1-е склонение: по палубе, как по земле'}
  ];


  const CSS=`
  #lvis .s6.l870{gap:14px}
  #lvis .s6.l870 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l870 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l870 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l870 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l870 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l870 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l870 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l870 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l870 .карт .текст b{color:${GOLD}}
  #lvis .s6.l870 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l870 .правило b{color:${GOLD}}
  #lvis .s6.l870 .журнал{width:100%;padding:12px 14px;border-radius:6px 16px 16px 6px;
    background:linear-gradient(90deg,#efe2c0,#dcc79a);border-left:7px solid #7a4a24;display:flex;flex-direction:column;gap:7px;color:${ЧЕРНИЛА}}
  #lvis .s6.l870 .журнал .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:#6a4a24}
  #lvis .s6.l870 .журнал .шапка b{font-size:18px;letter-spacing:0;text-transform:none;color:#7a2a10;font-family:Georgia,serif}
  #lvis .s6.l870 .журнал .шапка b.готово{color:#1a6a3a}
  #lvis .s6.l870 .журнал ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l870 .журнал li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:#6a5236}
  #lvis .s6.l870 .журнал li i{flex:0 0 22px;width:22px;height:22px;border-radius:50%;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;border:1.5px solid #a88a5a;color:#8a6a3a}
  #lvis .s6.l870 .журнал li.есть{color:${ЧЕРНИЛА}}
  #lvis .s6.l870 .журнал li.есть i{border-color:#1a4a6a;background:#2a6a9a;color:#fff}
  #lvis .s6.l870 .журнал li span{margin-left:auto;white-space:nowrap;font-size:14px;color:#8a6a3a;font-family:Georgia,serif}
  #lvis .s6.l870 .журнал li.есть span{color:#1a6a3a;font-weight:700}
  #lvis .s6.l870 .буйки{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;width:100%}
  #lvis .s6.l870 .буйки button{min-height:56px;border-radius:28px;cursor:pointer;font:inherit;font-size:19px;font-weight:700;
    font-family:Georgia,serif;border:2px solid #e86a5a;background:linear-gradient(180deg,#fff6ea,#f0dcc0);color:${ЧЕРНИЛА};
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),opacity 200ms}
  #lvis .s6.l870 .буйки button:active{transform:scale(.96)}
  #lvis .s6.l870 .буйки button.пойман{opacity:.35;border-color:${GREEN};text-decoration:line-through}
  #lvis .s6.l870 .буйки button.мимо{border-color:${RED};animation:l870нет 360ms cubic-bezier(.23,1,.32,1)}
  @keyframes l870нет{0%,100%{transform:none}25%{transform:translateX(-6px)}75%{transform:translateX(6px)}}
  #lvis .s6.l870 .ряд{display:grid;gap:8px;width:100%}
  #lvis .s6.l870 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:18px;font-weight:700;
    border:1.5px solid rgba(127,209,255,.5);background:rgba(127,209,255,.1);color:${ИНК};padding:6px 4px;font-family:Georgia,serif;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l870 .ряд button:active{transform:translateY(2px)}
  #lvis .s6.l870 .ряд button.вкл{border-color:${GOLD};background:rgba(255,215,106,.22);color:${GOLD}}
  #lvis .s6.l870 .случай{width:100%;display:flex;flex-direction:column;gap:6px}
  #lvis .s6.l870 .случай .что{font-size:19px;color:var(--ink);font-family:Georgia,serif}
  #lvis .s6.l870 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l870 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:17px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l870 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l870 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l870 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l870 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-size:18px}
  #lvis .s6.l870 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-size:18px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l870 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l870 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l870 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l870 .уровни .точка.сейчас{background:${GOLD};animation:l870dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l870dot{0%,100%{opacity:1}50%{opacity:.55}}
  #lvis .s6.l870 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l870{-webkit-text-size-adjust:100%}
  #lvis .s6.l870 [data-anim]{animation:l870rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l870rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  #lvis .s6.l870 .падежи{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;width:100%}
  #lvis .s6.l870 .падежи button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:16px;font-family:Georgia,serif;
    border:1.5px solid rgba(255,215,106,.45);background:rgba(255,215,106,.08);color:${ИНК};padding:6px 4px;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1),background 160ms}
  #lvis .s6.l870 .падежи button b{color:${GOLD};font-size:18px;margin-right:4px}
  #lvis .s6.l870 .падежи button.был{border-color:rgba(143,209,168,.6)}
  #lvis .s6.l870 .падежи button.вкл{border-color:${GOLD};background:rgba(255,215,106,.24)}
  #lvis .s6.l870 .падежи button:active{transform:translateY(2px)}
  #lvis .s6.l870 .ask.шесть button{flex:1 1 calc(16% - 8px);text-align:center;font-size:17px;padding:13px 2px;min-width:44px;font-family:Georgia,serif}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l870 [data-anim]{animation:none!important}
    #lvis .s6.l870 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l870 button{transition:none!important;animation:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l870-style');
      if(!s){ s=document.createElement('style'); s.id='l870-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Судовой журнал</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,f) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, 'r870Отв('+f+','+к+')')).join('')}</div>`;
  const ЖУРНАЛ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'журнал',
      `<div class="шапка"><span>Грузовая ведомость</span><b class="${всё?'готово':''}">${
        всё?'трюмы загружены':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}<span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.45;0.45;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const сдвигРаз = (из,в,длит,задержка) => ДВИЖ ?
    `<animateTransform attributeName="transform" type="translate" from="${из}" to="${в}" dur="${длит}" begin="${(задержка||0).toFixed(2)}s"
       fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  /* слово с выделенным окончанием */
  const тОк = (x,y,осн,ок,кегль,цвет,цветОк,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль}" font-weight="bold" fill="${цвет}" font-family="Georgia,'Times New Roman',serif">${esc(осн)}<tspan fill="${цветОк}">${esc(ок)}</tspan></text>`;
  const Р = () => window.РМ;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <filter id="c870-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="1.5" dy="2" stdDeviation="1.6" flood-color="#0b1c2a" flood-opacity=".45"/>
      </filter>
      <radialGradient id="c870-лампа" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#ffd890" stop-opacity=".5"/><stop offset="1" stop-color="#ff9a40" stop-opacity="0"/>
      </radialGradient>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${Р().defs()}${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c870-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(14,26,20,.92)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  /* облачко-реплика */
  const реплика = (x,y,ш,текст,хвост) => `<g filter="url(#c870-тень)">
      <rect x="${x}" y="${y}" width="${ш}" height="30" rx="14" fill="#fffaf0" stroke="${ОБВОД}" stroke-width="1"/>
      <path d="M${хвост[0]} ${y+29} l${хвост[1]} 12 l6 -12z" fill="#fffaf0" stroke="${ОБВОД}" stroke-width="1"/>
      <rect x="${хвост[0]-1}" y="${y+26}" width="10" height="5" fill="#fffaf0"/></g>
      ${т(x+ш/2,y+20,текст,14,ЧЕРНИЛА,true)}`;
  /* ящик с одним словом; окончание можно подсветить, нулевое — квадратик */
  const слово = (осн,ок,цвет) => `${esc(осн)}<tspan fill="${цвет||ОКОН}">${ок===''?'□':esc(ок)}</tspan>`;
  const груз = (w,к) => Р().ящик(0,0,78,31,{надпись:esc(w),кегль:к||12.5});
  const стопка = (слова,новое) => слова.map((w,k)=>{ const я=`<g transform="translate(0 ${-k*32})">${груз(w)}</g>`;
    return w===новое ? `<g>${сдвигРаз('0 -150','0 0',1.1,0)}${я}</g>` : я; }).join('');
  const вода = (y,Н) => `<path d="M0 ${y} ${Array.from({length:9},(_,k)=>`q21 ${k%2?4:-4} 42 0`).join(' ')} V${Н} H0 Z" fill="#1f5a8a" opacity=".5" data-декор="1">${анЛин('opacity','0.5;0.4;0.5','3s')}</path>`;

  /* ================= КАДРЫ ================= */

  /* 1. Три ящика на пристани */
  function F1(s){
    const Н=300, М=Р(), к=s.пад1==null?0:s.пад1, вид=Array.isArray(s.вид1)?s.вид1:[0], все=вид.length>=6, в=s.ответ1, ок=в===0;
    const п=ПАДЕЖИ[к];
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Фрегат «Чайка» грузится в порту. На пристани три ящика: «земля», «стол» и «ночь». Боцман просит проверить, как эти слова меняются по падежам. Выбирай падеж — ярлыки на ящиках поменяются. Пройди все шесть.') +
      `<div class="pic">${свг(`
        ${М.небо(336,140,{солнце:[34,30,11]})}
        ${М.берег(140,{x0:0,x1:130})}
        ${М.склад(66,152,100,64,{надпись:'СКЛАД'})}
        ${М.море(140,160,336,{})}
        ${М.фрегат(292,152,0.3,{})}
        ${подпись(206,40,п.б+'. · '+п.вопр,GOLD,13)}
        ${М.пристань(0,236,336,64,{кнехт:false})}
        ${ТРИ_СЛОВА.map((сл,i)=>{ const x=62+i*106;
          return `<g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 -6;0 0" dur="0.45s" fill="freeze"/>`:''}
            ${М.ящик(x,236,88,50,{надпись:слово(сл.осн[к],сл.ок[к]),кегль:16})}</g>
            ${т(x,176,п.пом,14,ИНК,true,'middle','#0b1c2a')}`; }).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="падежи">${ПАДЕЖИ.map((x,i)=>BTN(3+(i%5),(i===к?'вкл':'')+(вид.includes(i)?' был':''),'<b>'+x.б+'</b> '+x.пом,'r870Пад('+i+')')).join('')}</div>` +
      (!все ? СКАЗ('Смотри на окончания','Сравни концы слов на трёх ящиках: в каких падежах они разные? Квадратик □ — нулевое окончание.') :
        ОТВЕТЫ('пара',['по-разному','одинаково'],0,в,1) +
        (в==null ? СКАЗ('Вопрос','Одинаково ли склоняются «земля», «стол» и «ночь»?') :
          РАЗБОР(ок,['К земл<b>е</b>, к стол<b>у</b>, к ноч<b>и</b> — три разных окончания в одном падеже. Слова склоняются <b>по-разному</b>.',
            'Посмотри на дательный: к земл<b>е</b>, к стол<b>у</b>, к ноч<b>и</b>. Окончания разные — слова склоняются <b>по-разному</b>.'][в]))) +
      (ок ? ПРАВИЛО('Существительные склоняются по-разному. В русском языке <b>три склонения</b>: 1-е (земля), 2-е (стол), 3-е (ночь).') : '');
  }

  /* 2. Три трюма: правило */
  function F2(s){
    const Н=300, М=Р(), в=s.ответ2, ок=в===0;
    const правила=[['ж. и м. р.','на -а, -я'],['м. р. □','ср. р. -о, -е'],['ж. р.','на ь']];
    const от=М.трюмыОтсеки(10,80,316,190,3);
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Боцман показывает фрегат в разрезе: «Три трюма — три склонения. В первый — слова женского и мужского рода на -а, -я. Во второй — мужской род без окончания и средний на -о, -е. В третий — женский род на ь». А куда пойдёт ящик «<b>юнга</b>»?') +
      `<div class="pic">${свг(`
        ${М.небо(336,120,{облака:[[200,30,0.5,-6]]})}
        ${М.море(120,180,336,{})}
        ${М.трюмы(10,80,316,190,{n:3,свет:true,подписи:['1 скл.','2 скл.','3 скл.'],
          внутри:[стопка(['земля','юнга']),стопка(['стол','море']),стопка(['ночь','мель'])]})}
        ${от.map((о,i)=>т(о.cx,134,правила[i][0],11.5,'#ffe8b0',true,'middle','#1a0e06')+т(о.cx,150,правила[i][1],11.5,'#ffe8b0',true,'middle','#1a0e06')).join('')}
        ${ок?`<rect x="${(от[0].cx-43).toFixed(1)}" y="${от[0].пол-68}" width="86" height="38" rx="5" fill="none" stroke="${GREEN}" stroke-width="2.4">${анЛин('opacity','1;0.4;1','1.2s')}</rect>`:''}
        ${М.боцман(292,76,0.5,{поза:'указывает',влево:true})}
        ${вода(284,Н)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['1-е склонение','2-е склонение'],0,в,2) +
      (в==null ? СКАЗ('Вопрос','«Юнга» — мужского рода. В какой трюм его?') :
        РАЗБОР(ок,['Юнга — мужской род, но окончание <b>-а</b>. Слова м. р. на -а, -я (юнга, дядя, дедушка) — <b>1-е склонение</b>.',
          'Во втором трюме — мужской род с <b>нулевым</b> окончанием: стол, парус. У «юнги» окончание -а — это <b>1-е склонение</b>.'][в])) +
      (ок ? ПРАВИЛО('<b>1-е</b>: ж. и м. р. на -а, -я (земля, юнга). <b>2-е</b>: м. р. с нулевым окончанием, ср. р. на -о, -е (стол, море). <b>3-е</b>: ж. р. на ь (ночь, мель).') : '');
  }

  /* 3. Погрузка */
  function F3(s){
    const Н=340, М=Р(), загр=s.груз3||{}, выб=s.выбор3, отв=s.отв3;
    const в_трюме=[1,2,3].map(h=>ГРУЗ.filter(г=>загр[г.w]===h).sort((a,b)=>(s.порядок3||[]).indexOf(a.w)-(s.порядок3||[]).indexOf(b.w)).map(г=>г.w));
    const все=ГРУЗ.every(г=>загр[г.w]);
    const висит = выб ? `${М.ящик(0,50,84,36,{надпись:esc(выб),кегль:14})}` : '';
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Погрузка! Выбери ящик — стрела поднимет его над палубой. Потом выбери трюм. Сначала вспомни род слова и посмотри на окончание.') +
      `<div class="pic">${свг(`
        ${М.небо(336,210,{солнце:[300,34,11],облака:[[96,36,0.5,6]]})}
        ${М.море(210,130,336,{})}
        ${М.трюмы(10,160,316,160,{n:3,свет:[0,1,2].filter(i=>в_трюме[i].length),подписи:['1 скл.','2 скл.','3 скл.'],
          внутри:в_трюме.map(сп=>стопка(сп,отв&&отв.ок?отв.w:null))})}
        ${М.стрела(36,156,172,40,16,висит,{стропы:30,высота:140,спуск:выб?-60:0})}
        ${М.боцман(292,156,0.5,{поза:отв&&отв.ок?'свисток':'стоит'})}
        ${вода(314,Н)}
        ${все?подпись(172,96,'все шесть ящиков в трюмах',GREEN,13):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="падежи">${ГРУЗ.map((г,i)=>{ const есть=загр[г.w];
        return BTN(3+(i%5),(есть?'был':'')+(выб===г.w?' вкл':''),есть?'✓ '+г.w:'📦 '+г.w,есть?'':'r870Ящик('+i+')'); }).join('')}</div>` +
      (все ? '' : `<div class="ask три">${[1,2,3].map(h=>BTN(4+h,'',h+'-й трюм','r870Трюм('+h+')')).join('')}</div>`) +
      (все ? РАЗБОР(true,'Трюмы загружены: <b>мачта, дядя</b> — 1-е, <b>парус, море</b> — 2-е, <b>соль, мель</b> — 3-е склонение.')
        : отв ? РАЗБОР(отв.ок, отв.ок ? '«'+отв.w+'» — '+ГРУЗ.find(г=>г.w===отв.w).why+': <b>'+ГРУЗ.find(г=>г.w===отв.w).т+'-е склонение</b>.'
            : '«'+отв.w+'» — '+ГРУЗ.find(г=>г.w===отв.w).why+'. Это '+ГРУЗ.find(г=>г.w===отв.w).т+'-е склонение, а не '+отв.h+'-е. Выбери ящик ещё раз.')
        : СКАЗ(выб?'Ящик на стреле':'Сначала ящик', выб?'Ящик «'+esc(выб)+'» висит над палубой. В какой трюм?':'Нажми на ящик выше.')) +
      (все ? ПРАВИЛО('Склонение определяют по <b>роду</b> и <b>окончанию</b>. Мужской род бывает и в 1-м (дядя), и во 2-м (парус).') : '');
  }

  /* 4. Начальная форма */
  function F4(s){
    const Н=260, М=Р(), нф=!!s.нф4, в=s.ответ4, ок=в===0;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга записал: «Я стою на <b>палубе</b>». Какого склонения «палубе»? По такой форме не узнать: окончание -е бывает у всех трёх. Сначала поставь слово в <b>начальную форму</b> — именительный падеж, единственное число.') +
      `<div class="pic">${свг(`
        ${М.небо(336,120,{солнце:[40,34,11],облака:[[240,34,0.5,-6]]})}
        ${М.море(120,60,336,{})}
        <rect x="0" y="150" width="336" height="7" fill="url(#рм-мачта)" stroke="${ОБВОД}" stroke-width=".8"/>
        ${Array.from({length:9},(_,k)=>`<rect x="${6+k*40}" y="157" width="6" height="26" fill="url(#рм-мачта)" stroke="${ОБВОД}" stroke-width=".6"/>`).join('')}
        ${М.доски(0,180,336,80,false)}
        ${М.юнга(78,240,0.9,{поза:'пишет'})}
        <g>${нф&&ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 -14;0 0" dur="0.5s" fill="freeze"/>`:''}
          ${М.ящик(236,240,128,56,{надпись:нф?слово('палуб','а'):esc('на палубе'),кегль:17})}</g>
        ${нф?подпись(236,138,'палуба — ж. р., на -а',ок?GREEN:GOLD,12):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      (!нф ? `<div class="ask">${BTN(4,'','Поставить в начальную форму','r870НФ()')}</div>` + СКАЗ('Как','Спроси: <b>что?</b> — одна <b>палуба</b>.') :
        ОТВЕТЫ('три',['1-е','2-е','3-е'],0,в,4) +
        (в==null ? СКАЗ('Вопрос','Палуба — какого склонения?') :
          РАЗБОР(ок,['Палуба — ж. р., окончание -а: <b>1-е склонение</b>.',
            'Во 2-м склонении — м. р. без окончания и ср. р. на -о, -е. Палуба — ж. р. на -а: <b>1-е</b>.',
            'В 3-м — ж. р. на ь. У «палубы» окончание -а: <b>1-е склонение</b>.'][в]))) +
      (ок ? ПРАВИЛО('Чтобы определить склонение, поставь слово в <b>начальную форму</b>: на палубе → палуба, у мачты → мачта.') : '');
  }

  /* 5. Мягкий знак у мужского рода */
  function F5(s){
    const Н=270, М=Р(), в=s.ответ5, ок=в===0;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Фрегат бросил <b>якорь</b> у песчаной <b>мели</b>. Юнга рассуждает: «На конце ь — значит, оба слова 3-го склонения». Боцман хмурится. Какого склонения «якорь»?') +
      `<div class="pic">${свг(`
        ${М.небо(336,100,{облака:[[80,30,0.5,8]]})}
        ${М.море(100,170,336,{})}
        <g><ellipse cx="272" cy="152" rx="62" ry="13" fill="url(#рм-песок)" stroke="#c8a870" stroke-width="1"/>
          <ellipse cx="272" cy="152" rx="70" ry="17" fill="none" stroke="#e8f6ff" stroke-width="1.2" opacity=".6">${анЛин('rx','66;76;66','3s')}</ellipse></g>
        <g><path d="M232 146 v-26" stroke="#3a3e46" stroke-width="1.6"/><path d="M226 150 q6 -8 12 0 q-6 5 -12 0z" fill="#e05a3a" stroke="${ОБВОД}" stroke-width=".8"/>
          <path d="M232 120 l10 4 l-10 4z" fill="#e05a3a"/></g>
        ${М.фрегат(104,150,0.45,{})}
        <path d="M158 148 Q176 186 172 214" stroke="#3a3e46" stroke-width="2.2" fill="none" stroke-dasharray="3 2"/>
        ${М.якорь(172,250,0.8)}
        ${подпись(80,232,ок?'якорь — м. р. → 2-е':'якорь — м. р.',ок?GREEN:ИНК,12)}
        ${подпись(272,190,ок?'мель — ж. р. → 3-е':'мель — ж. р.',ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['2-е склонение','3-е склонение'],0,в,5) +
      (в==null ? СКАЗ('Вопрос','Якорь — какого склонения?') :
        РАЗБОР(ок,['Якорь — <b>мужской</b> род (мой якорь), окончание нулевое: <b>2-е склонение</b>, как стол. Мель — женский род (моя мель): 3-е.',
          'В 3-м склонении только <b>женский</b> род на ь. Якорь — мужской (мой якорь): это <b>2-е склонение</b>.'][в])) +
      (ок ? ПРАВИЛО('Мягкий знак на конце ещё не 3-е склонение. Проверь род: <b>мой</b> якорь, конь, день — 2-е; <b>моя</b> мель, ночь, соль — 3-е.') : '');
  }

  /* 6. Множественное число */
  function F6(s){
    const Н=270, М=Р(), в=s.ответ6, ок=в===0;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Боцман свистит в дудку: «Поднять <b>паруса</b>!» Юнга видит окончание -а и хочет отнести «паруса» к 1-му склонению. Как правильно узнать склонение?') +
      `<div class="pic">${свг(`
        ${М.небо(336,140,{солнце:[296,34,11]})}
        ${М.море(140,130,336,{дорожка:296})}
        ${М.чайка(56,54,0.6,{дрейф:'10 -3'})}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 8;0 0" dur="1.4s" fill="freeze"/>`:''}${М.фрегат(150,208,0.62,{})}</g>
        ${М.боцман(292,262,0.55,{поза:'свисток',влево:true})}
        ${подпись(150,Н-20,ок?'паруса → парус → 2-е':'паруса → ?',ок?GREEN:GOLD,13)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Поставить в ед. ч.: один парус','Смотреть на окончание -а'],0,в,6) +
      (в==null ? СКАЗ('Вопрос','Как узнать склонение слова «паруса»?') :
        РАЗБОР(ок,['Один <b>парус</b> — м. р., нулевое окончание: <b>2-е склонение</b>. Окончание -а у «паруса» — окончание множественного числа.',
          '-а в «паруса» — окончание <b>множественного</b> числа, оно не подсказывает склонение. Ставим в ед. ч.: парус — 2-е.'][в])) +
      (ок ? ПРАВИЛО('У слов во <b>множественном числе</b> склонение не определяют. Ставим в И. п. <b>ед. ч.</b>: паруса → парус, волны → волна.') : '');
  }

  /* 7. Е или И */
  function F7(s){
    const Н=260, М=Р(), в=s.ответ7, ок=в===1;
    const буква = в==null ? '_' : ['е','и'][в];
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Зачем знать склонение? Чтобы не ошибаться в окончаниях. Юнга пишет: «Фрегат подошёл к пристан<b>_</b>». Е или И? Узнай склонение слова «пристань».') +
      `<div class="pic">${свг(`
        ${М.небо(336,110,{облака:[[70,30,0.5,8]]})}
        ${М.берег(110,{x0:190,x1:336})}
        ${М.море(110,150,336,{})}
        ${М.склад(292,124,76,50,{})}
        ${М.пристань(196,200,140,60,{})}
        <g><rect x="214" y="150" width="4" height="50" fill="url(#рм-мачта)"/><rect x="314" y="150" width="4" height="50" fill="url(#рм-мачта)"/>
          <rect x="204" y="134" width="124" height="30" rx="4" fill="url(#рм-доскатём)" stroke="${ОБВОД}" stroke-width="1"/>
          <text x="266" y="155" text-anchor="middle" font-size="17" font-weight="bold" fill="#ffe8b0" font-family="Georgia,serif">к пристан<tspan fill="${в==null?GOLD:ок?GREEN:RED}">${буква}</tspan></text></g>
        <g>${сдвигРаз('-70 0','0 0',2.4,0)}${М.фрегат(118,196,0.46,{})}</g>
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['е','и'],1,в,7) +
      (в==null ? СКАЗ('Вопрос','К пристан<b>е</b> или к пристан<b>и</b>?') :
        РАЗБОР(ок,['<b>Е</b> пишут у 1-го склонения: к палубе. А пристань — <b>моя</b> пристань, ж. р. на ь: 3-е склонение. Значит, к пристан<b>и</b>.',
          'Пристань — ж. р. на ь, <b>3-е склонение</b>. В дательном падеже у 3-го склонения — <b>и</b>: к пристани, к ночи.'][в])) +
      (ок ? ПРАВИЛО('В Д. и П. п. ед. ч.: <b>1-е</b> скл. — <b>е</b> (к земле, на земле); <b>2-е</b> в П. п. — <b>е</b> (о столе); <b>3-е</b> — <b>и</b> (к ночи, о ночи).') : '');
  }

  /* 8. Проверочные слова */
  function F8(s){
    const Н=270, М=Р(), в=s.ответ8, ок=в===0;
    const фл='M0 0 q12 -4 24 0 q12 4 24 0 v18 q-12 4 -24 0 q-12 -4 -24 0 z', фл2='M0 0 q12 4 24 0 q12 -4 24 2 v18 q-12 -6 -24 -2 q-12 4 -24 0 z';
    const строки=[['земля','на земл','е','1-е'],['стол','на стол','е','2-е'],['степь','в степ','и','3-е']];
    return ЖУРНАЛ(s) +
      ЗАДАЧА('На фок-мачте поднимают флаг. Юнга пишет: «На мачт<b>_</b> поднят флаг». Боцман подсказывает: подставь <b>проверочное слово</b> того же склонения с ударным окончанием. Какое подойдёт?') +
      `<div class="pic">${свг(`
        ${М.небо(336,220,{облака:[[260,200,0.5,-6]]})}
        ${М.море(220,50,336,{})}
        <rect x="84" y="0" width="14" height="270" fill="url(#рм-мачта)" stroke="${ОБВОД}" stroke-width="1"/>
        <rect x="26" y="80" width="130" height="8" rx="4" fill="url(#рм-доска)" stroke="${ОБВОД}" stroke-width=".8"/>
        <path d="M96 20 V250" stroke="#8a7a5a" stroke-width="1.2"/>
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 170;0 0" dur="2.6s" fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="0.3 0.8 0.4 1"/>`:''}
          <g transform="translate(98 24)"><path d="${фл}" fill="#e05a3a" stroke="${ОБВОД}" stroke-width=".9">${ДВИЖ?`<animate attributeName="d" values="${фл};${фл2};${фл}" dur="1.4s" repeatCount="indefinite"/>`:''}</path></g></g>
        ${М.лист(172,24,152,124,{})}
        ${т(248,48,'проверочные слова',11.5,'#7a4a24',true)}
        ${строки.map(([сл,осн,ок2,н],i)=>`<g>${т(186,76+i*26,н,12,'#7a5a3a',true,'start')}
          <text x="214" y="${76+i*26}" font-size="14" font-weight="bold" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">${esc(осн)}<tspan fill="${ОКОН}">${ок2}</tspan></text></g>`).join('')}
        ${М.лист(172,172,152,56,{})}
        <text x="248" y="208" text-anchor="middle" font-size="21" font-weight="bold" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">на мачт<tspan fill="${в==null?'#c8901a':ок?'#1a7a4a':'#b8321e'}">${в==null?'_':['е','и'][в]}</tspan></text>
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['земля: на земле → е','степь: в степи → и'],0,в,8) +
      (в==null ? СКАЗ('Вопрос','Мачта — ж. р. на -а. Какое проверочное слово взять?') :
        РАЗБОР(ок,['Мачта — 1-е склонение, как <b>земля</b>: на земл<b>е</b> — на мачт<b>е</b>.',
          'Степь — 3-е склонение. А мачта — ж. р. на -а, это <b>1-е</b>, как земля: на земле — на мачт<b>е</b>.'][в])) +
      (ок ? ПРАВИЛО('Проверочные слова с ударным окончанием: <b>земля</b> (1-е), <b>стол</b> (2-е), <b>степь</b> (3-е). Подставь слово того же склонения в тот же падеж.') : '');
  }

  /* 9. Запись в журнале */
  function F9(s){
    const Н=260, М=Р(), реш=s.реш9||{}, все=ПРОПУСКИ.every((_,i)=>реш[i]!=null), верно=все&&ПРОПУСКИ.every((x,i)=>реш[i]===x.верно);
    const бук=(i)=>{ const р=реш[i]; return `<tspan fill="${р==null?'#c8901a':р===ПРОПУСКИ[i].верно?'#1a7a4a':'#b8321e'}">${р||'_'}</tspan>`; };
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга ведёт судовой журнал: «Шлюпка пристала к пристан<b>_</b>, и юнга пробежал по палуб<b>_</b>». Впиши е или и. Сначала определи склонение каждого слова.') +
      `<div class="pic">${свг(`
        ${М.небо(336,90,{солнце:[296,32,11]})}
        ${М.море(90,40,336,{})}
        ${М.лист(12,112,312,Н-124,{линии:3})}
        <text x="28" y="150" font-size="19" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">Шлюпка пристала</text>
        <text x="28" y="186" font-size="19" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">к пристан${бук(0)}, и юнга</text>
        <text x="28" y="222" font-size="19" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">пробежал по палуб${бук(1)}.</text>
        ${верно?т(306,222,'✓',22,'#1a7a4a',true,'end'):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ПРОПУСКИ.map((x,i)=>`<div class="случай">${A(3+i,'что',(i?'…':'')+x.до+'<b>'+x.осн+'_</b>')}<div class="ask пара">${['е','и'].map((б,j)=>
        BTN(4+i,реш[i]===б?(б===x.верно?'hit':'miss'):'',б,"r870Проп("+i+",'"+б+"')")).join('')}</div></div>`).join('') +
      (!все ? СКАЗ('Подсказка','Пристань — моя, на ь. Палуба — моя, на -а. Какие это склонения?') :
        РАЗБОР(верно, верно ? 'К пристан<b>и</b> — 3-е склонение, по палуб<b>е</b> — 1-е. Всё верно.' :
          ПРОПУСКИ.filter((x,i)=>реш[i]!==x.верно).map(x=>x.why).join('; ')+'.')) +
      (верно ? ПРАВИЛО('Сомневаешься, е или и? Определи склонение и подставь проверочное слово: <b>земля, стол, степь</b>.') : '');
  }

  /* 10. Трюмы загружены */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=330, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА(всё
        ? 'Трюмы загружены, фонари горят. Боцман закрывает ведомость: «Каждое слово на своём месте. Теперь и в журнале не будет ошибок в окончаниях». Фрегат снимается с якоря.'
        : 'Погрузка ещё не закончена — вернись к делам в списке. Вот что получится в конце.') +
      `<div class="pic">${свг(`
        ${М.небо(336,140,{закат:true,солнце:[272,122,16]})}
        ${М.море(140,190,336,{дорожка:272})}
        ${М.чайка(250,40,0.6,{дрейф:'10 -2'})}
        ${М.трюмы(10,120,316,180,{n:3,свет:true,подписи:['1 скл.','2 скл.','3 скл.'],
          внутри:[стопка(['мачта','дядя']),стопка(['парус','море']),стопка(['соль','мель'])]})}
        ${М.боцман(56,116,0.5,{поза:'свисток'})}
        <g>${проявить('6s',0.1,0.2)}${подпись(206,40,'три склонения — три трюма',GOLD,13)}</g>
        ${вода(302,Н)}
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','Склонений три. <b>1-е</b>: ж. и м. р. на -а, -я (земля, юнга). <b>2-е</b>: м. р. с нулевым окончанием и ср. р. на -о, -е (стол, море, якорь). <b>3-е</b>: ж. р. на ь (ночь, мель). Склонение определяют по начальной форме — И. п. ед. ч. В Д. и П. п. у 1-го склонения пишем <b>е</b>, у 3-го — <b>и</b>; помогают проверочные слова земля, стол, степь.') +
      ПРАВИЛО('<b>Род + окончание = склонение.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'«Лодка» — какого склонения?', варианты:[{т:'1-е',ок:true},{т:'2-е',ок:false}], разбор:'Ж. р., на -а.' },
    { вопрос:'«Конь» — какого склонения?', варианты:[{т:'3-е',ок:false},{т:'2-е',ок:true}], разбор:'Мой конь — м. р.' },
    { вопрос:'«Облако» — какого склонения?', варианты:[{т:'2-е',ок:true},{т:'1-е',ок:false}], разбор:'Ср. р., на -о.' },
    { вопрос:'«Дедушка» — какого склонения?', варианты:[{т:'2-е',ок:false},{т:'1-е',ок:true}], разбор:'М. р., но на -а.' },
    { вопрос:'«Подошли к пристан_»', варианты:[{т:'и',ок:true},{т:'е',ок:false}], разбор:'3-е скл. — и.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r870Reset()")}</div>` +
        ПРАВИЛО('<b>Род + окончание = склонение.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r870Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Определи склонение', задания:[
    {q:'«Кит» — какого склонения?', в:1, варианты:['1-е','2-е'], раз:'М. р., нулевое окончание.'},
    {q:'«Волна» — какого склонения?', в:0, варианты:['1-е','3-е'], раз:'Ж. р., на -а.'},
    {q:'«Тень» — какого склонения?', в:1, варианты:['2-е','3-е'], раз:'Моя тень — ж. р. на ь.'},
    {q:'«Поле» — какого склонения?', в:0, варианты:['2-е','1-е'], раз:'Ср. р., на -е.'}
  ]};
  const Т2 = { имя:'Ловушки', задания:[
    {q:'«Дядя» — какого склонения?', в:0, варианты:['1-е','2-е'], раз:'М. р., но на -я.'},
    {q:'«День» — какого склонения?', в:0, варианты:['2-е','3-е'], раз:'Мой день — м. р.'},
    {q:'«Волны» — какого склонения?', в:1, варианты:['2-е','1-е'], раз:'Волна — ж. р. на -а.'},
    {q:'«У мачты» — начальная форма?', в:1, варианты:['мачты','мачта'], раз:'И. п. ед. ч.: мачта.'}
  ]};
  const Т3 = { имя:'Е или И', задания:[
    {q:'«Сел на скамейк_»', в:0, варианты:['е','и'], раз:'1-е скл. — е.'},
    {q:'«Стоял на площад_»', в:1, варианты:['е','и'], раз:'3-е скл. — и.'},
    {q:'«Рассказал о пристан_»', в:1, варианты:['е','и'], раз:'3-е скл. — и.'},
    {q:'«Жили на остров_»', в:0, варианты:['е','и'], раз:'2-е скл., П. п. — е.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r870T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r870TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L870 = {
    id: ID,
    title: 'Три склонения',
    ico: '📦',
    src: 'Русский язык · 4 класс · Имя существительное', subj: 'rus',
    explain: [
      'Земля, стол, ночь склоняются по-разному: к земле, к столу, к ночи. Склонений три.',
      '1-е — ж. и м. р. на -а, -я; 2-е — м. р. без окончания и ср. р. на -о, -е; 3-е — ж. р. на ь. Юнга — 1-е.',
      'Погрузка: мачта, дядя — 1-е; парус, море — 2-е; соль, мель — 3-е.',
      'Склонение определяют по начальной форме: на палубе → палуба → 1-е.',
      'Якорь — м. р. на ь: 2-е склонение, не 3-е.',
      'Во множественном числе склонение не определяют: паруса → парус.',
      'К пристани — 3-е склонение, в Д. п. пишем и.',
      'Проверочные слова: земля, стол, степь. На мачте — как на земле.',
      'К пристани, по палубе — е и и по склонению.',
      'Итог: род + окончание = склонение.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: определи склонение.',
      'Тренажёр 2: ловушки.',
      'Тренажёр 3: е или и.'
    ],
    check: {
      q: 'Какого склонения слово «ночь»?',
      choices: ['1-е','2-е','3-е'],
      ans: 2,
      exp: 'Ж. р. на ь — 3-е склонение.'
    },
    tasks: [
      { q:'Сколько склонений у существительных?', kind:'unit', ans:3, tol:0,
        hints:['Земля, стол, ночь.'], sol:'3.' },
      { q:'«Юнга» — какого склонения?', kind:'choice', choices:['1-е','2-е','3-е'], ans:0, tol:0,
        hints:['Окончание -а.'], sol:'1-е.' },
      { q:'«Подошёл к пристан_» — какая буква?', kind:'choice', choices:['е','и'], ans:1, tol:0,
        hints:['Пристань — 3-е склонение.'], sol:'и.' }
    ]
  };

  function render(el){
    if(!window.РМ){ el.innerHTML=''; return; }
    css();
    const s = S();
    const step = Math.max(0, Math.min(L870.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    const ЗАГОЛОВКИ={1:'Три ящика',2:'Три трюма',3:'Погрузка',4:'Начальная форма',5:'Якорь и мель',
      6:'Поднять паруса',7:'К пристани',8:'Проверочное слово',9:'Запись в журнале',10:'Трюмы загружены',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l870" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Три склонения'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r870Пад=(i)=>{ const s=S(); s.пад1=i;
    const в=Array.isArray(s.вид1)?s.вид1.slice():[0]; if(!в.includes(i)) в.push(i); s.вид1=в; chRender(0); };
  window.r870Отв=(f,к)=>{ const s=S(); s['ответ'+f]=к; if(f===1&&к===0) s.дело_три=true; chRender(0); };
  window.r870Ящик=(i)=>{ const s=S(); s.выбор3=ГРУЗ[i].w; s.отв3=null; chRender(0); };
  window.r870Трюм=(h)=>{ const s=S(); const w=s.выбор3; if(!w){ return; }
    const г=ГРУЗ.find(x=>x.w===w);
    if(г.т===h){ const з=Object.assign({},s.груз3||{}); з[w]=h; s.груз3=з;
      s.порядок3=(s.порядок3||[]).concat([w]); s.выбор3=null; s.отв3={w:w,ок:true};
      if(ГРУЗ.every(x=>з[x.w])) s.дело_трюмы=true;
    } else s.отв3={w:w,h:h,ок:false};
    chRender(0); };
  window.r870НФ=()=>{ const s=S(); s.нф4=true; chRender(0); };
  window.r870Проп=(i,б)=>{ const s=S(); const р=Object.assign({},s.реш9||{}); р[i]=б; s.реш9=р;
    if(ПРОПУСКИ.every((x,k)=>р[k]===x.верно)) s.дело_журнал=true; chRender(0); };
  window.r870Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r870Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r870T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r870TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L870; else arr.push(L870); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.RU870={render:render, L:L870};
})();
