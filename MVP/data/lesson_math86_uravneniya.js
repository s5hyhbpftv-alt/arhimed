/* ============ МАТЕМАТИКА · УРОК 86 · «ЛИНЕЙНЫЕ УРАВНЕНИЯ» ============
   7 класс. Сюжет: весы в старой лавке — неизвестная гиря x, и её надо найти.
   Уравнение — это равновесие: что сделал с одной чашей, сделай и с другой.

   УРОК СОБРАН ПО 12 ПРАВИЛАМ КОМАНДЫ ЭКСПЕРТОВ:
     1) ответ не виден до выбора — и правило, и РИСУНОК (найденный корень,
        уровни весов, итоговые панели появляются только после ответа);
     2) верный вариант не первый — детерминированный сдвиг порядка;
     3) три варианта в каждом вопросе, неверные — реальные ошибки (забыл
        сделать то же с другой чашей, перепутал знак при переносе, разделил
        только одну часть);
     4) затухание: кадр 4 «я» (решение целиком), кадр 5 «мы» (шаг пропущен),
        кадр 6 «ты» (сам);
     5) интерливинг практики: 1–2 новая тема, 3 — прошлый урок (простые числа
        и делители), 4 — смешанная, 5 — новая в обёртке (задача про покупку);
     6) разминка в кадре 1 — простые числа до 20 (урок 378);
     7) самообъяснение — кадры 3 и 8: выбирается ПРИЧИНА, а не число;
     8) ошибка как данные — третий тренажёр: чужая запись, назови сбой;
     9) вариативность: 3x + 5 = 20 → 2x − 7 = 9 → 5x + 3 = 28 → x с двух
        сторон → не-пример с нулевым коэффициентом;
    10) я — мы — ты: кадры 4, 5, 6;
    11) выходной билет после пяти уровней;
    12) машинные проверки: qa_layout.py (включая переполнение кнопок).

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ ПОДСТАНОВКОЙ:
     3x + 5 = 20 → 3x = 15 → x = 5;  проверка 3·5 + 5 = 20 ✓
     2x − 7 = 9 → 2x = 16 → x = 8;  проверка 2·8 − 7 = 9 ✓
     5x + 3 = 28 → 5x = 25 → x = 5;  проверка 5·5 + 3 = 28 ✓
     4x − 3 = 2x + 7 → 2x = 10 → x = 5;  проверка 4·5 − 3 = 17 = 2·5 + 7 ✓
     7x = 3x + 12 → 4x = 12 → x = 3;  проверка 7·3 = 21 = 3·3 + 12 ✓
     задача: 3x + 40 = 250 → x = 70;  проверка 3·70 + 40 = 250 ✓
     0·x = 5 — корней нет;  0·x = 0 — корень любой x;  2x = 0 — корень x = 0
     разминка: простых до 20 восемь (2, 3, 5, 7, 11, 13, 17, 19) ✓
     интерливинг: делителей у 36 девять (1, 2, 3, 4, 6, 9, 12, 18, 36) ✓

   СТАНДАРТ: deploy/АНИМАЦИЯ_УРОКОВ.md, deploy/РИСОВАНИЕ_ФИГУР.md.
   Движение — сама математика: чаши весов опускаются и поднимаются, гири
   уезжают с обеих чаш, корень проявляется после решения.
   reduced-motion глушит SMIL целиком. */
(function(){
  'use strict';

  const ID = 86;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  const CSS=`
  #lvis .s6.l86{gap:14px}
  #lvis .s6.l86 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l86 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l86 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l86 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l86 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l86 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l86 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l86 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l86 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l86 .правило b{color:${GOLD}}
  #lvis .s6.l86 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l86 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;
    font-size:16px;font-weight:600;line-height:1.35;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l86 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l86 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l86 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l86 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l86 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l86 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l86 .уровни .точка.сейчас{background:${GOLD};animation:l86dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l86dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l86 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l86{-webkit-text-size-adjust:100%}
  #lvis .s6.l86 [data-anim]{animation:l86rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l86rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l86 [data-anim]{animation:none!important}
    #lvis .s6.l86 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l86 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l86-style');
      if(!s){ s=document.createElement('style'); s.id='l86-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(String(ID)) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ПРАВИЛО2 = (ответ, до, после) => (ответ!=null) ? ПРАВИЛО(после) : ПРАВИЛО(до);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анВК = (значения,длит,keyTimes,центр,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}"
              ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c86-лавка" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#3c2e1e"/><stop offset="0.45" stop-color="#281e12"/>
        <stop offset="1" stop-color="#15100a"/>
      </linearGradient>
      <pattern id="c86-доски" width="34" height="16" patternUnits="userSpaceOnUse">
        <path d="M0 6 q9 -4 17 0 q8 4 17 0" fill="none" stroke="rgba(255,225,180,.05)" stroke-width="1.4"/>
      </pattern>
      <linearGradient id="c86-латунь" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#7a5814"/><stop offset="0.22" stop-color="#ffe9a8"/>
        <stop offset="0.55" stop-color="#d3a53f"/><stop offset="0.82" stop-color="#8f6a18"/>
        <stop offset="1" stop-color="#5e440e"/>
      </linearGradient>
      <linearGradient id="c86-железо" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#454f58"/><stop offset="0.22" stop-color="#dfe7ee"/>
        <stop offset="0.55" stop-color="#8d9ca8"/><stop offset="0.82" stop-color="#454f58"/>
        <stop offset="1" stop-color="#2f373e"/>
      </linearGradient>
      <linearGradient id="c86-дерево" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#e0b478"/><stop offset="0.5" stop-color="#b9884a"/>
        <stop offset="1" stop-color="#7d5628"/>
      </linearGradient>
      <linearGradient id="c86-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <linearGradient id="c86-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".6"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".22"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".2"/>
      </linearGradient>
      <radialGradient id="c86-свет" cx="0.26" cy="0.12" r="0.8" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffe9b0" stop-opacity=".24"/>
        <stop offset="0.6" stop-color="#ffe9b0" stop-opacity=".06"/>
        <stop offset="1" stop-color="#000000" stop-opacity=".32"/>
      </radialGradient>
      <filter id="c86-тень" x="-60%" y="-60%" width="220%" height="220%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3" result="р"/>
        <feOffset in="р" dx="3" dy="5" result="с"/>
        <feFlood flood-color="#0d0803" flood-opacity="0.45" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c86-контакт" x="-70%" y="-150%" width="240%" height="400%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.1" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#1d1508" flood-opacity="0.45" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c86-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c86-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c86-лавка)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="url(#c86-доски)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="url(#c86-свет)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(12,8,3,.86)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#0d0803" opacity=".34" filter="url(#c86-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c86-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  const бумага = (x,y,ш,в,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="6" fill="#0d0803" opacity=".36" filter="url(#c86-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="url(#c86-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="none" filter="url(#c86-шум)" opacity=".1"/>
  </g>`;

  /* ГИРЯ (железная) */
  const гиря = (x,y,кг,нач) => {
    const ш=Math.max(20,14+кг*2.4), в=Math.max(18,14+кг*2);
    return `<g>${нач!=null?проявить('15s',нач,нач+0.04):''}
      <ellipse cx="${x+2}" cy="${y+в+3}" rx="${ш*0.55}" ry="4" fill="#0d0803" opacity=".45" filter="url(#c86-контакт)"/>
      <path d="M${x} ${y+в} L${x} ${y+4} q0 -4 4 -4 l${ш-8} 0 q4 0 4 4 l0 ${в-4} z"
        fill="url(#c86-железо)" stroke="rgba(25,30,35,.6)" stroke-width="1.2"/>
      <path d="M${x+3} ${y+5} l${ш-6} 0" stroke="rgba(255,255,255,.5)" stroke-width="1.4"/>
      ${т(x+ш/2,y+в/2+5,String(кг),12,'#1c2228',true)}
    </g>`;
  };

  /* КОРОБКА с неизвестным x */
  const коробка = (x,y,подписьТекста,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+16} ${y+14})">
    ${нач!=null?проявить('15s',нач,нач+0.04):''}
    <ellipse cx="${x+18}" cy="${y+30}" rx="16" ry="4" fill="#0d0803" opacity=".45" filter="url(#c86-контакт)"/>
    <rect x="${x}" y="${y}" width="32" height="28" rx="5" fill="url(#c86-дерево)" stroke="${ОБВОД}" stroke-width="1.3"/>
    <path d="M${x+4} ${y+5} l24 0" stroke="rgba(255,255,255,.45)" stroke-width="1.3"/>
    <path d="M${x+16} ${y+2} l0 24" stroke="rgba(90,60,20,.35)" stroke-width="1"/>
    ${т(x+16,y+19,подписьТекста||'x',14,'#2f1f0c',true)}
  </g>`;

  /* ВЕСЫ: стойка, коромысло, две чаши на цепочках.
     наклон — угол коромысла в градусах (0 — равновесие) */
  function весы(наклон,левая,правая,нач){
    const цx=168, цy=64;
    const у=наклон||0;
    const конец=(сторона)=>{
      const р=сторона*96, а=(у*Math.PI)/180;
      return [цx+Math.cos(а)*р, цy+Math.sin(а)*р];
    };
    const [лx,лy]=конец(-1), [пx,пy]=конец(1);
    const чаша=(x,y,вместимое,сторона)=>{
      const н=[];
      /* цепочки */
      н.push(`<line x1="${x}" y1="${y}" x2="${x-16}" y2="${y+30}" stroke="url(#c86-латунь)" stroke-width="1.2"/>`);
      н.push(`<line x1="${x}" y1="${y}" x2="${x+16}" y2="${y+30}" stroke="url(#c86-латунь)" stroke-width="1.2"/>`);
      н.push(`<path d="M${x-22} ${y+30} q22 16 44 0" fill="url(#c86-латунь)" stroke="rgba(60,45,10,.5)" stroke-width="1.2"/>`);
      н.push(`<path d="M${x-22} ${y+30} q22 10 44 0" fill="rgba(255,255,255,.22)"/>`);
      return н.join('');
    };
    return `<g>
      <ellipse cx="${цx+4}" cy="228" rx="86" ry="9" fill="#0d0803" opacity=".5" filter="url(#c86-тень)"/>
      ${/* стойка */''}
      <rect x="${цx-7}" y="${цy}" width="14" height="150" rx="5" fill="url(#c86-дерево)" stroke="${ОБВОД}" stroke-width="1.4">
        ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
      <rect x="${цx-40}" y="212" width="80" height="14" rx="6" fill="url(#c86-дерево)" stroke="${ОБВОД}" stroke-width="1.4"/>
      ${/* коромысло */''}
      <g transform="rotate(${у} ${цx} ${цy})">
        ${ДВИЖ?анВК('0 168 64;'+у+' 168 64;'+у+' 168 64;0 168 64','16s','0;0.35;0.85;1',цx+' '+цy):''}
        <rect x="${цx-100}" y="${цy-5}" width="200" height="10" rx="5" fill="url(#c86-латунь)" stroke="rgba(60,45,10,.5)" stroke-width="1.2"/>
        <circle cx="${цx}" cy="${цy}" r="9" fill="url(#c86-латунь)" stroke="rgba(60,45,10,.6)" stroke-width="1.2"/>
        <circle cx="${цx}" cy="${цy}" r="3.4" fill="#3a2c10"/>
      </g>
      ${чаша(лx,лy)}
      ${чаша(пx,пy)}
      ${/* содержимое чаш */''}
      ${левая?левая(лx,лy+30):''}
      ${правая?правая(пx,пy+30):''}
    </g>`;
  }

  /* РЕШЕНИЕ СТРОКОЙ: шаги уравнения */
  const шаги = (строки,шаг,нач,x,y,ш) => строки.slice(0,шаг).map((с,i)=>
    `<g>${проявить('15s',нач+i*0.07,нач+i*0.07+0.06)}
      <rect x="${x}" y="${y+i*26}" width="${ш}" height="22" rx="6"
        fill="${i===шаг-1?'rgba(143,209,168,.16)':'rgba(255,255,255,.06)'}" stroke="${i===шаг-1?GREEN:ЛИНИЯ}" stroke-width="1.1"/>
      ${т(x+ш/2,y+i*26+16,с,14,ИНК,true)}</g>`).join('');

  /* ================= КАДРЫ ================= */

  /* 1. Разминка + сюжет */
  function F1(s){
    const в = s.разминка;
    return A(2,'cap','Разминка: прошлый урок про простые числа') +
      ВОПРОС('Сколько простых чисел от 2 до 20?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Старая лавка: весы и неизвестная гиря',14,GOLD,true)}
        ${весы(0,()=>коробка(120,180,'x',0.3),()=>гиря(168,176,5,0.42),0.05)}
        <g>${проявить('15s',0.5,0.6)}
          ${стекло(24,214,288,30,0.5)}
          ${т(168,234,'простых чисел до 20 восемь',12,МУТ)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='восемь'?'hit':(в?'miss':''), 'Восемь: 2, 3, 5, 7, 11, 13, 17, 19', "r86Разминка('восемь')")}
        ${BTN(4, в==='девять'?'miss':'', 'Девять: включая единицу', "r86Разминка('девять')")}
        ${BTN(5, в==='десять'?'miss':'', 'Десять: все нечётные до 20', "r86Разминка('десять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='восемь',
        в==='восемь' ? 'Верно: простых до 20 ровно восемь. Единицу не считаем — у неё один делитель.'
                     : 'Нет: единица не простое число, а чётные кроме двойки делятся на 2. Простых восемь.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Вспомним решето: простому нужно ровно два делителя.',
                 'Простых до 20 — <b>восемь</b>: 2, 3, 5, 7, 11, 13, 17, 19.');
  }

  /* 2. Что такое уравнение и корень */
  function F2(s){
    const в = s.уравнение;
    return ВОПРОС('На левой чаше гиря 5 и коробка x, на правой — 8. Сколько весит коробка?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Уравнение — это равновесие',14,GOLD,true)}
        ${весы(0,()=>коробка(88,180,'x',0.2)+гиря(140,176,5,0.28),()=>гиря(190,176,8,0.36),0.05)}
        <g>${проявить('15s',0.44,0.54)}
          ${стекло(24,214,288,30,0.44)}
          ${т(168,234,'x + 5 = 8 — чаши в равновесии',12,МУТ)}</g>
        ${в!=null?`<g>${проявить('15s',0.62,0.74)}
          ${подпись(168,196,'x = 3',GREEN,16)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), '3 — тогда 3 + 5 = 8, чаши сравняются', "r86Уравнение('три')")}
        ${BTN(4, в==='тринадцать'?'miss':'', '13 — надо сложить 5 и 8', "r86Уравнение('тринадцать')")}
        ${BTN(5, в==='восемь'?'miss':'', '8 — как на правой чаше', "r86Уравнение('восемь')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: чтобы чаши уравновесились, коробка должна весить 3. Число 3 — корень уравнения x + 5 = 8.'
                  : 'Нет: если коробка весит 13, левая чаша тяжелее. Сложить гири нельзя — они на разных чашах. Правильный ответ 3.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Корень — это число, при котором равенство верно.',
                 'Корень уравнения x + 5 = 8 равен <b>3</b>.');
  }

  /* 3. Главное правило весов — самообъяснение */
  function F3(s){
    const в = s.правило;
    return ВОПРОС('Почему можно снять гирю 5 с обеих чаш сразу?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Главное правило весов',14,GOLD,true)}
        ${весы(0,()=>коробка(108,180,'x',0.2),()=>гиря(196,176,3,0.3),0.05)}
        ${в!=null?`<g>${проявить('15s',0.34,0.46)}
          ${гиря(96,148,5,0.3)}${гиря(206,148,5,0.36)}
          <path d="M118 148 q50 -22 100 -6" fill="none" stroke="${RED}" stroke-width="2" stroke-dasharray="6 5"/>
          ${подпись(168,138,'снимаем по 5 с обеих чаш',RED,12)}</g>`:''}
        <g>${проявить('15s',0.5,0.6)}
          ${стекло(24,214,288,30,0.5)}
          ${т(168,234,'равновесие не нарушится',12,МУТ)}</g>
        ${в!=null?`<g>${проявить('15s',0.66,0.78)}
          ${подпись(168,196,'x + 5 − 5 = 8 − 5, то есть x = 3',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='поровну'?'hit':(в?'miss':''), 'С обеих чаш сняли одинаковый груз — равновесие сохранилось', "r86Правило('поровну')")}
        ${BTN(4, в==='легче'?'miss':'', 'Левая чаша стала легче, поэтому коробку видно лучше', "r86Правило('легче')")}
        ${BTN(5, в==='гиря'?'miss':'', 'Потому что гирю можно заменить на любую другую', "r86Правило('гиря')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='поровну',
        в==='поровну' ? 'Верно: если снять одинаковый груз с обеих чаш, они останутся в равновесии. Это и есть главное правило решения уравнений.'
                     : 'Нет: снимать надо одинаково с обеих сторон. Тогда равновесие сохраняется, а уравнение становится проще.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Следи, чтобы обе чаши менялись одинаково.',
                 'Что сделал с одной частью — <b>сделай и с другой</b>.');
  }

  /* 4. «Я»: решаем 3x + 5 = 20 */
  function F4(s){
    const в = s.я;
    const строки=['3x + 5 = 20','3x = 20 − 5','3x = 15','x = 15 : 3','x = 5'];
    return A(2,'cap','Я решаю: 3x + 5 = 20') +
      ВОПРОС('Какое первое действие освободит x?') +
      `<div class="pic">${свг(`
        ${фон(258)}
        ${т(168,24,'Освобождаем x шаг за шагом',14,GOLD,true)}
        ${шаги(строки,в!=null?5:1,0.05,24,38,288)}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${подпись(168,206,'проверка: 3 · 5 + 5 = 20',GREEN,12)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.3,0.4)}
          ${стекло(24,196,288,30,0.3)}
          ${т(168,216,'убираем числа, мешающие x',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.7,0.82)}
          ${весы(0,()=>коробка(80,196,'x',0)+коробка(122,196,'x',0)+коробка(164,196,'x',0),()=>гиря(214,192,15,0),0.6)}
        </g>`:''}
      `,258)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='минус5'?'hit':(в?'miss':''), 'Вычесть 5 из обеих частей: 3x = 15', "r86Я('минус5')")}
        ${BTN(4, в==='делить3'?'miss':'', 'Сразу разделить обе части на 3', "r86Я('делить3')")}
        ${BTN(5, в==='плюс5'?'miss':'', 'Прибавить 5 к обеим частям', "r86Я('плюс5')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='минус5',
        в==='минус5' ? 'Верно: сначала убираем слагаемое 5, получаем 3x = 15, а затем делим на 3 и находим x = 5. Проверка сходится.'
                     : 'Нет: делить на 3 можно и позже, но сначала мешает слагаемое 5. Убираем его: 20 − 5 = 15.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Сначала убери слагаемое, потом дели на коэффициент.',
                 '3x + 5 = 20 → 3x = 15 → <b>x = 5</b> (проверка 3·5 + 5 = 20).');
  }

  /* 5. «Мы»: 2x − 7 = 9, шаг пропущен */
  function F5(s){
    const в = s.мы;
    return A(2,'cap','Мы решаем вместе: шаг пропущен') +
      ВОПРОС('2x − 7 = 9. Какое число вставим в пропуск: 2x = 9 ? 7?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Дописываем шаг',14,GOLD,true)}
        ${шаги(['2x − 7 = 9','2x = 9 ? 7','2x = 16','x = 8'],2,0.06,24,44,288)}
        ${в==null?`<g>${проявить('15s',0.34,0.44)}
          ${стекло(24,196,288,30,0.34)}
          ${т(168,216,'слагаемое 7 убираем с левой части',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${шаги(['2x − 7 = 9','2x = 9 + 7','2x = 16','x = 8'],4,0.5,24,44,288)}
          ${подпись(168,196,'проверка: 2 · 8 − 7 = 9',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='плюс'?'hit':(в?'miss':''), 'Плюс: 2x = 9 + 7 = 16', "r86Мы('плюс')")}
        ${BTN(4, в==='минус'?'miss':'', 'Минус: 2x = 9 − 7 = 2', "r86Мы('минус')")}
        ${BTN(5, в==='ничего'?'miss':'', 'Ничего не вставляем, шаг не нужен', "r86Мы('ничего')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='плюс',
        в==='плюс' ? 'Верно: чтобы убрать «минус 7», прибавляем 7 к обеим частям. Тогда 2x = 16, а x = 8. Проверка верна.'
                   : 'Нет: если вычесть 7, станет хуже. Убираем «минус 7» прибавлением: 9 + 7 = 16, значит x = 8.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Подумай, какое действие уберёт слагаемое.',
                 '2x = 9 + 7 = 16 → <b>x = 8</b> (проверка 2·8 − 7 = 9).');
  }

  /* 6. «Ты»: 5x + 3 = 28 */
  function F6(s){
    const в = s.ты;
    return A(2,'cap','Теперь ты: реши сам') +
      ВОПРОС('Реши уравнение 5x + 3 = 28. Чему равен x?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Решаем самостоятельно',14,GOLD,true)}
        ${шаги(['5x + 3 = 28','5x = 25','x = 5'],в!=null?3:1,0.06,24,52,288)}
        ${в!=null?`<g>${проявить('15s',0.4,0.52)}
          ${подпись(168,196,'проверка: 5 · 5 + 3 = 28',GREEN,12)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.28,0.38)}
          ${стекло(24,190,288,30,0.28)}
          ${т(168,210,'сначала 28 − 3, потом раздели на 5',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пять'?'hit':(в?'miss':''), 'x = 5: 25 : 5 = 5, проверка 5·5 + 3 = 28', "r86Ты('пять')")}
        ${BTN(4, в==='шесть'?'miss':'', 'x = 6: тогда 5·6 + 3 = 33, а не 28', "r86Ты('шесть')")}
        ${BTN(5, в==='двадцатьпять'?'miss':'', 'x = 25: это значение 5x, а не x', "r86Ты('двадцатьпять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пять',
        в==='пять' ? 'Верно: 28 − 3 = 25, затем 25 : 5 = 5. Корень x = 5, проверка сходится.'
                   : 'Нет: 25 — это 5x, а не x. Нужно ещё разделить на 5: x = 5.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Не забудь разделить на коэффициент при x.',
                 '5x = 25 → <b>x = 5</b> (проверка 5·5 + 3 = 28).');
  }

  /* 7. Короткий путь: перенос слагаемых */
  function F7(s){
    const в = s.перенос;
    return ВОПРОС('Как быстрее перейти от 3x + 5 = 20 к 3x = 15?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Короткий путь: перенос',14,GOLD,true)}
        ${бумага(20,40,296,66,0.05,-0.5)}
        ${т(168,66,'3x + 5 = 20',20,'#2b2110',true)}
        ${в!=null?`<g>${проявить('15s',0.3,0.42)}
          ${т(168,94,'3x = 20 − 5',20,'#2b2110',true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.48,0.6)}
          ${стекло(20,116,296,44,0.48)}
          ${т(168,136,'слагаемое перешло через знак «=»',12,ИНК,true)}
          ${т(168,152,'и поменяло свой знак',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.68,0.8)}
          ${подпись(168,186,'3x = 15, значит x = 5',GREEN,14)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.24,0.34)}
          ${стекло(20,116,296,44,0.24)}
          ${т(168,143,'есть короткое правило для слагаемых',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сменить'?'hit':(в?'miss':''), 'Перенести 5 вправо, сменив знак на минус', "r86Перенос('сменить')")}
        ${BTN(4, в==='вычесть'?'miss':'', 'Перенести 5, не меняя знак', "r86Перенос('вычесть')")}
        ${BTN(5, в==='умножить'?'miss':'', 'Умножить обе части на 5', "r86Перенос('умножить')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сменить',
        в==='сменить' ? 'Верно: при переносе через знак равенства слагаемое меняет знак: плюс 5 становится минус 5. Это то же самое, что вычесть 5 из обеих частей.'
                     : 'Нет: без смены знака получится 3x = 25 — неверно. Именно поэтому перенос сопровождается сменой знака.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Вспомни, что происходит со слагаемым при переносе.',
                 'При переносе через «=» слагаемое <b>меняет знак</b>.');
  }

  /* 8. Самообъяснение: почему меняется знак */
  function F8(s){
    const в = s.почему;
    return ВОПРОС('Почему при переносе слагаемое меняет знак?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Почему так получается',14,GOLD,true)}
        <g>${проявить('15s',0.05,0.15)}
          ${бумага(20,40,296,40,0.05,-0.4)}
          ${т(168,66,'3x + 5 = 20',16,'#2b2110',true)}</g>
        ${в!=null?`<g>${проявить('15s',0.24,0.36)}
          ${бумага(20,88,296,44,0.24,0.4)}
          ${т(168,106,'вычли 5 из обеих частей',12,'#6a5a3a')}
          ${т(168,124,'3x + 5 − 5 = 20 − 5',14,'#2b2110',true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.48,0.6)}
          ${стекло(20,140,296,42,0.48)}
          ${т(168,160,'слева «+5 − 5» взаимно уничтожились',12,ИНК,true)}
          ${т(168,176,'осталось 3x = 20 − 5',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.7,0.82)}
          ${подпись(168,204,'то же самое, что перенос со сменой знака',GREEN,12)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.3,0.4)}
          ${стекло(20,140,296,42,0.3)}
          ${т(168,166,'вспомни правило весов',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='вычли'?'hit':(в?'miss':''), 'Это то же самое, что вычесть одно и то же из обеих частей', "r86Почему('вычли')")}
        ${BTN(4, в==='правило'?'miss':'', 'Так договорились в математике, это просто правило', "r86Почему('правило')")}
        ${BTN(5, в==='меньше'?'miss':'', 'Перенесённое число всегда меньше исходного', "r86Почему('меньше')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='вычли',
        в==='вычли' ? 'Верно: перенос — сокращённая запись вычитания одного и того же из обеих частей. Поэтому знак и меняется.'
                    : 'Нет: это не договорённость, а следствие правила весов. Мы вычитаем 5 из обеих частей, и слева «+5 − 5» исчезает.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Подумай, какое действие стоит за переносом.',
                 'Перенос — это <b>вычитание одного и того же</b> из обеих частей.');
  }

  /* 9. x в обеих частях */
  function F9(s){
    const в = s.обе;
    const строки=['4x − 3 = 2x + 7','4x − 2x = 7 + 3','2x = 10','x = 5'];
    return ВОПРОС('4x − 3 = 2x + 7. Что делаем с 2x из правой части?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'x встречается дважды',14,GOLD,true)}
        ${шаги(строки,в!=null?4:1,0.06,24,46,288)}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${подпись(168,196,'проверка: 4·5 − 3 = 17 = 2·5 + 7',GREEN,12)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.26,0.36)}
          ${стекло(24,188,288,30,0.26)}
          ${т(168,208,'собери все x в одной части',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='вычесть2x'?'hit':(в?'miss':''), 'Вычесть 2x из обеих частей: останется 2x = 10', "r86Обе('вычесть2x')")}
        ${BTN(4, в==='прибавить2x'?'miss':'', 'Прибавить 2x к обеим частям', "r86Обе('прибавить2x')")}
        ${BTN(5, в==='ничего'?'miss':'', 'Ничего не делать: x уже найден', "r86Обе('ничего')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='вычесть2x',
        в==='вычесть2x' ? 'Верно: убираем 2x из правой части, вычитая его из обеих. Получаем 2x = 10, значит x = 5, и проверка сходится.'
                       : 'Нет: если прибавить 2x, справа станет 4x и x останется с двух сторон. Нужно вычитать: 2x = 10.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Собери слагаемые с x в одной части.',
                 '4x − 3 = 2x + 7 → 2x = 10 → <b>x = 5</b>.');
  }

  /* 10. Ловушка: нулевой коэффициент */
  function F10(s){
    const в = s.ловушка;
    return ВОПРОС('Решаем 0 · x = 5. Сколько корней у такого уравнения?') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${т(168,24,'Ловушка: коэффициент равен нулю',14,GOLD,true)}
        ${бумага(20,40,296,44,0.05,-0.5)}
        ${т(168,68,'0 · x = 5',20,'#2b2110',true)}
        ${в!=null?`<g>${проявить('15s',0.26,0.38)}
          ${стекло(20,94,296,40,0.26)}
          ${т(168,114,'слева всегда 0, какое x ни возьми',12,ИНК,true)}
          ${т(168,130,'а справа 5 — равенство невозможно',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.46,0.58)}
          ${подпись(168,158,'корней нет',RED,16)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.64,0.76)}
          ${бумага(20,182,296,40,0.64,0.5)}
          ${т(168,200,'а если 0 · x = 0, то подойдёт любое x',12,'#6a5a3a')}
          ${т(168,216,'это тождество, корней бесконечно много',12,'#2b2110',true)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.24,0.34)}
          ${стекло(20,94,296,40,0.24)}
          ${т(168,118,'посмотри, что осталось слева',12,МУТ)}</g>`:''}
      `,256)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нет'?'hit':(в?'miss':''), 'Ни одного: слева всегда 0, а справа 5', "r86Лов('нет')")}
        ${BTN(4, в==='один'?'miss':'', 'Один корень: x = 5', "r86Лов('один')")}
        ${BTN(5, в==='много'?'miss':'', 'Бесконечно много корней', "r86Лов('много')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нет',
        в==='нет' ? 'Верно: 0 · x = 0 при любом x, поэтому равенство 0 = 5 невозможно. Корней нет. А вот 0 · x = 0 верно всегда — там корней бесконечно много.'
                  : 'Нет: подставь любое число вместо x — слева получится 0, а не 5. Такое уравнение корней не имеет.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Проверь, не равен ли коэффициент при x нулю.',
                 '<b>0 · x = 5 — корней нет</b>; 0 · x = 0 — корней бесконечно много.');
  }

  /* 11. Сводка */
  function F11(s){
    const в = s.сводка;
    return ВОПРОС('В каком порядке решают линейное уравнение?') +
      `<div class="pic">${свг(`
        ${фон(266)}
        ${т(168,24,'Порядок действий',14,GOLD,true)}
        ${['раскрой скобки','собери x в одной части','числа — в другой','раздели на коэффициент','проверь подстановкой']
          .map((с,i)=>`<g>${проявить('15s',0.05+i*0.07,0.14+i*0.07)}
            <rect x="24" y="${38+i*32}" width="288" height="26" rx="7"
              fill="${i===3?'rgba(255,215,106,.16)':'rgba(255,255,255,.06)'}" stroke="${i===3?GOLD:ЛИНИЯ}" stroke-width="1.1"/>
            ${т(168,56+i*32,(i+1)+'. '+с,12,ИНК,true)}</g>`).join('')}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${подпись(168,220,'а если коэффициент 0 — проверь особый случай',GOLD,12)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.7,0.82)}
          ${стекло(24,232,288,28,0.7)}
          ${т(168,250,'корень обязательно проверяй',12,МУТ)}</g>`:''}
      `,266)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='собрать'?'hit':(в?'miss':''), 'Сначала собрать x в одной части, числа — в другой, потом делить', "r86Свод('собрать')")}
        ${BTN(4, в==='делить'?'miss':'', 'Сразу разделить всё на коэффициент при x', "r86Свод('делить')")}
        ${BTN(5, в==='угадать'?'miss':'', 'Подобрать корень и не считать', "r86Свод('угадать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='собрать',
        в==='собрать' ? 'Верно: сначала переносим слагаемые — x в одну часть, числа в другую, затем делим на коэффициент и проверяем.'
                      : 'Нет: делить можно только тогда, когда слагаемые уже собраны. Сначала перенос, потом деление.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Расставь шаги по порядку.',
                 'Порядок: <b>собрать x и числа, разделить, проверить</b>.');
  }

  /* 12. Практика: интерливинг + выходной билет */
  const УРОВНИ=[
    { тип:'новая', вопрос:'Реши 2x − 7 = 9.', варианты:[
        {т:'x = 8', ок:true,  почему:'2x = 16, x = 8; проверка 2·8 − 7 = 9.'},
        {т:'x = 1', ок:false, почему:'Это 9 − 7 = 2, но делить надо на 2, а не вычитать.'},
        {т:'x = 16', ок:false, почему:'16 — это 2x, а не x.'}]},
    { тип:'новая', вопрос:'Реши 4x − 3 = 2x + 7.', варианты:[
        {т:'x = 5', ок:true,  почему:'2x = 10, x = 5; проверка 17 = 17.'},
        {т:'x = 2', ок:false, почему:'2x = 10, значит x = 5.'},
        {т:'x = 10', ок:false, почему:'10 — это 2x, а не x.'}]},
    { тип:'прошлая', вопрос:'Вспомним прошлый урок: сколько делителей у 36?', варианты:[
        {т:'9', ок:true,  почему:'1, 2, 3, 4, 6, 9, 12, 18, 36.'},
        {т:'6', ок:false, почему:'Пересчитай: делителей девять.'},
        {т:'2', ок:false, почему:'Два делителя только у простого числа.'}]},
    { тип:'смешанная', вопрос:'Реши 7x = 3x + 12.', варианты:[
        {т:'x = 3', ок:true,  почему:'4x = 12, x = 3; проверка 21 = 21.'},
        {т:'x = 4', ок:false, почему:'4 — это коэффициент, а x = 3.'},
        {т:'x = 12', ок:false, почему:'12 — правая часть, а не корень.'}]},
    { тип:'обёртка', вопрос:'Тетрадь стоит x, три тетради и ручка за 40 — всего 250. Чему равен x?', варианты:[
        {т:'x = 70', ок:true,  почему:'3x + 40 = 250 → 3x = 210 → x = 70.'},
        {т:'x = 90', ок:false, почему:'250 : 3 ≈ 83 — так нельзя, ручку тоже надо учесть.'},
        {т:'x = 210', ок:false, почему:'210 — это 3x, а не x.'}]}
  ];
  function F12(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Выходной билет: придумай уравнение, у которого корень x = 4, и проверь его подстановкой.') +
        РАЗБОР(true,'Например, 3x + 5 = 17: подставляем 4 — получаем 17, равенство верно. Или 2x = x + 4 — тоже корень 4.') +
        `<div class="ask">${BTN(4,'','пройти заново','r86Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const порядок=сдвиг(ур.варианты, и+1);
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length+' · '+ур.тип) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + порядок.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r86Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(порядок[выбран] && порядок[выбран].ок, (порядок[выбран]||{}).почему||'') : ЖДЁТ()) +
      ПРАВИЛО('Переноси слагаемые со сменой знака и обязательно делай проверку.');
  }

  /* 13–15. Тренажёры */
  const Т1={
    имя:'Тренажёр 1 · решаем уравнение',
    задания:[
      { ф:'3x + 5 = 20. Чему равен x?', о:['5','15','25'], в:0, р:'3x = 15, x = 5.' },
      { ф:'5x + 3 = 28. Чему равен x?', о:['5','6','25'], в:0, р:'5x = 25, x = 5.' },
      { ф:'7x = 3x + 12. Чему равен x?', о:['3','4','12'], в:0, р:'4x = 12, x = 3.' },
      { ф:'2(x + 3) = 14. Чему равен x?', о:['4','7','11'], в:0, р:'x + 3 = 7, x = 4.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · проверка корня',
    задания:[
      { ф:'Является ли 5 корнем 3x + 5 = 20?', о:['да, 15 + 5 = 20','нет','нужно ещё делить'], в:0, р:'Подстановка сходится.' },
      { ф:'Является ли 6 корнем 5x + 3 = 28?', о:['нет, 33 ≠ 28','да','почти'], в:0, р:'5·6 + 3 = 33.' },
      { ф:'Является ли 0 корнем 2x = 0?', о:['да, 2·0 = 0','нет','корень не существует'], в:0, р:'Подстановка верна.' },
      { ф:'Является ли 4 корнем 4x − 3 = 2x + 7?', о:['нет, 13 ≠ 15','да','нужно делить'], в:0, р:'4·4 − 3 = 13, 2·4 + 7 = 15.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · разбор чужой записи',
    задания:[
      { ф:'Ученик решил 3x + 5 = 20 так: 3x = 20 + 5. Что не так?', о:['при переносе знак меняется на минус','всё верно','надо было делить сразу'], в:0, р:'Должно быть 20 − 5 = 15.' },
      { ф:'Ученик из 5x = 25 получил x = 25. Что не так?', о:['надо разделить на 5','всё верно','надо умножить на 5'], в:0, р:'x = 5.' },
      { ф:'Ученик из 4x − 3 = 2x + 7 получил 2x = 4. Что не так?', о:['числа перенесены без смены знака','всё верно','надо было вычесть 4x'], в:0, р:'7 + 3 = 10, а не 4.' },
      { ф:'Ученик сказал, что 0 · x = 5 имеет корень x = 5. Что не так?', о:['корней нет: слева всегда 0','всё верно','корень x = 0'], в:0, р:'0 = 5 невозможно.' }
    ]
  };

  /* сдвиг вариантов: верный ответ не всегда первый */
  function сдвиг(варианты, ключ){
    const н=варианты.length; if(н<2) return варианты;
    const к=((ключ*7+3)%н);
    return варианты.slice(к).concat(варианты.slice(0,к));
  }

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const порядок=сдвиг(з.о.map((о,к)=>({о,к})), шаг+1).map(х=>х.о);
    const верный=порядок.indexOf(з.о[з.в]);
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===верный;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + порядок.map((о,к)=>
        BTN(4+к, готово&&к===верный?'hit':(готово&&к===отв?'miss':''), о, `r86T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r86TNext('${ключ}')`)}</div>` : '');
  }
  function F13(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F14(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F15(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L86={
    id: ID, title:'Линейные уравнения', ico:'⚖️',
    src:'Математика · 7 класс · Линейные уравнения', subj:'math',
    explain: [
      'Начнём с разминки по прошлому уроку: простые числа до 20 — это 2, 3, 5, 7, 11, 13, 17 и 19, всего восемь. А теперь зайдём в старую лавку: на весах слева лежит коробка неизвестного веса и гиря 5, справа стоит гиря 8, и чаши в равновесии. Как узнать вес коробки?',

      'Запишем то, что видим, математически: x + 5 = 8. Здесь x — неизвестный вес коробки. Такая запись называется уравнением, а число, при котором равенство становится верным, — корнем уравнения. В нашем случае корень равен 3, потому что 3 + 5 = 8. Уравнение — это не просто пример, а условие равновесия.',

      'Главное правило весов: если с обеих чаш снять одинаковый груз, равновесие не нарушится. То же и с уравнением: что сделали с одной частью, то же делаем и с другой. Из x + 5 = 8 вычтем 5 из обеих частей — слева «плюс 5 минус 5» взаимно уничтожатся, справа останется 3. Получим x = 3. Обратите внимание: мы не переносили числа, а именно вычитали из обеих частей.',

      'Решим вместе более сложное уравнение: 3x + 5 = 20. Здесь мешают два числа: слагаемое 5 и коэффициент 3. Сначала убираем слагаемое — вычитаем 5 из обеих частей: 3x = 15. Теперь делим обе части на 3: x = 5. Обязательно проверим: подставим 5 в исходное уравнение, получим 3 · 5 + 5 = 20. Равенство верное, значит корень найден правильно.',

      'Попробуем вместе: 2x − 7 = 9. Чтобы убрать «минус 7», нужно прибавить 7 к обеим частям, а не вычитать. Тогда 2x = 16, и после деления на 2 получим x = 8. Здесь легко ошибиться: знак слагаемого подсказывает, какое действие выполнять. Проверка: 2 · 8 − 7 = 9 — сходится.',

      'Теперь реши сам: 5x + 3 = 28. Действуй по порядку — сначала убери слагаемое 3, потом раздели на коэффициент 5. Должно получиться 5x = 25 и x = 5. Проверь подстановкой: 5 · 5 + 3 = 28. Если получилось 25 вместо 5, значит ты остановился на шаг раньше: 25 — это значение 5x, а не корень.',

      'Есть короткий путь записи, который экономит время. Вместо того чтобы каждый раз писать «вычтем 5 из обеих частей», говорят: перенесём 5 в правую часть со сменой знака. Из 3x + 5 = 20 сразу получаем 3x = 20 − 5 = 15. Это то же самое действие, просто записанное короче. Главное — не забывать менять знак: переносится «плюс 5», а записывается «минус 5».',

      'Почему при переносе меняется знак? Потому что за переносом стоит вычитание одинакового числа из обеих частей. Запишем подробно: 3x + 5 − 5 = 20 − 5. Слева «плюс 5 минус 5» дают ноль, остаётся 3x. Справа 20 − 5 = 15. Вот и получилось 3x = 15. Значит, перенос со сменой знака — это не договорённость, а следствие правила весов.',

      'Бывает, что x встречается в обеих частях: 4x − 3 = 2x + 7. Тогда вычитаем 2x из обеих частей: слева остаётся 2x, справа 2x исчезает. Получаем 2x − 3 = 7, затем переносим число: 2x = 10, и x = 5. Проверка: 4 · 5 − 3 = 17 и 2 · 5 + 7 = 17 — равенство верное. Правило простое: собирай x в одной части, числа — в другой.',

      'Разберём ловушку. Что если коэффициент при x равен нулю? Уравнение 0 · x = 5 решить нельзя: при любом x слева получится 0, а справа стоит 5. Значит, корней нет. А вот 0 · x = 0 верно при любом x — такое уравнение называют тождеством, у него бесконечно много корней. И ещё одна частая ошибка: разделить на коэффициент только одну часть уравнения.',

      'Соберём порядок действий. Первое: раскрыть скобки, если они есть. Второе: собрать слагаемые с x в одной части, а числа — в другой, перенося их со сменой знака. Третье: разделить обе части на коэффициент при x. Четвёртое: обязательно подставить найденный корень в исходное уравнение и проверить. И пятое: если коэффициент оказался нулём, разбирай особый случай отдельно.',

      'Пройти практику: пять уровней. Первые два про решение уравнений, третий вернёт нас к прошлому уроку про делители, четвёртый смешает темы, а пятый спрячет уравнение в задачу про покупку. В конце — выходной билет: придумай уравнение с корнем 4 и проверь его.',

      'Первый тренажёр — решаем уравнение: переносим слагаемые и делим на коэффициент.',

      'Второй тренажёр — проверка корня: подставляем число и смотрим, верно ли равенство.',

      'Третий тренажёр — разбор чужой записи: в каждой задаче ученик ошибся, и нужно назвать, что именно он сделал не так.'
    ],
    check: {
      q:'Реши уравнение 3x + 5 = 20.',
      choices:['x = 5','x = 15','x = 25'],
      ans:0,
      exp:'3x = 20 − 5 = 15, затем x = 15 : 3 = 5. Проверка: 3·5 + 5 = 20.'
    },
    tasks: [
      { q:'Реши 2x − 7 = 9.', kind:'choice',
        choices:['x = 8','x = 1'], ans:0,
        hints:['Убери «минус 7» прибавлением.','2x = 16.'],
        sol:'x = 8.' },
      { q:'Реши 4x − 3 = 2x + 7.', kind:'choice',
        choices:['x = 5','x = 2'], ans:0,
        hints:['Вычти 2x из обеих частей.','2x = 10.'],
        sol:'x = 5.' },
      { q:'Сколько корней у уравнения 0 · x = 5?', kind:'choice',
        choices:['Ни одного','Один, x = 5'], ans:0,
        hints:['Что получится слева при любом x?','0 = 5 невозможно.'],
        sol:'Корней нет.' },
      { q:'Ученик из 5x = 25 получил x = 25. Что не так?', kind:'choice',
        choices:['Надо разделить на 5','Всё верно'], ans:0,
        hints:['25 — это 5x.','Раздели обе части на 5.'],
        sol:'Надо разделить на 5: x = 5.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L86.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else if(f===14) сцена=F14(s);
    else сцена=F15(s);

    const ЗАГОЛОВКИ={
      1:['Разминка','Весы и неизвестная гиря'],
      2:['Что такое уравнение','Корень равенства'],
      3:['Правило весов','Меняем обе чаши'],
      4:['Я решаю','3x + 5 = 20'],
      5:['Мы решаем','Шаг пропущен'],
      6:['Ты решаешь','5x + 3 = 28'],
      7:['Короткий путь','Перенос слагаемых'],
      8:['Почему так','Смена знака'],
      9:['x с двух сторон','4x − 3 = 2x + 7'],
      10:['Ловушка','Коэффициент равен нулю'],
      11:['Сводка','Порядок действий'],
      12:['Практика','Пять уровней'],
      13:['Тренажёр 1','Решаем уравнение'],
      14:['Тренажёр 2','Проверка корня'],
      15:['Тренажёр 3','Разбор чужой записи']
    };
    const з=ЗАГОЛОВКИ[f]||['Линейные уравнения','Весы и корни'];
    el.innerHTML = `<div class="s6 l86" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r86Разминка=(к)=>{ S().разминка=к; chRender(0); };
  window.r86Уравнение=(к)=>{ S().уравнение=к; chRender(0); };
  window.r86Правило=(к)=>{ S().правило=к; chRender(0); };
  window.r86Я=(к)=>{ S().я=к; chRender(0); };
  window.r86Мы=(к)=>{ S().мы=к; chRender(0); };
  window.r86Ты=(к)=>{ S().ты=к; chRender(0); };
  window.r86Перенос=(к)=>{ S().перенос=к; chRender(0); };
  window.r86Почему=(к)=>{ S().почему=к; chRender(0); };
  window.r86Обе=(к)=>{ S().обе=к; chRender(0); };
  window.r86Лов=(к)=>{ S().ловушка=к; chRender(0); };
  window.r86Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r86Pick=(уровень,вариант)=>{
    const s=S();
    const порядок=сдвиг(УРОВНИ[уровень].варианты, уровень+1);
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(порядок[вариант] && порядок[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r86Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r86T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    const порядок=сдвиг(з.о.map((о,к)=>({о,к})), шаг+1).map(х=>х.о);
    const верный=порядок.indexOf(з.о[з.в]);
    s[ключ+'Ответ']=вариант;
    if(вариант===верный) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r86TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L86; else arr.push(L86);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA86={render:render, L:L86};
})();
