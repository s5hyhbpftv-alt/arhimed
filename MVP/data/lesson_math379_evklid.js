/* ============ МАТЕМАТИКА · УРОК 379 · «НОД: АЛГОРИТМ ЕВКЛИДА» ============
   5–6 класс. Сюжет: мастерская мозаики — плиту 24 × 15 надо разрезать
   на одинаковые квадраты без отходов. Самый большой квадрат и есть НОД.

   ЧТО ВЗЯТО ИЗ МИРОВОЙ ПРАКТИКИ (исследование собрано командой экспертов,
   отчёт: nod-euclid-research.md, 13 источников):
     • CPA-подход Сингапура: сначала предмет (плита и квадраты), потом рисунок
       (чертёж замощения), потом символы (цепочка делений);
     • Eureka G6 M2 Lesson 19: прямоугольник → наибольшие квадраты → остаток
       замощается тем же способом: это буквально алгоритм Евклида;
     • White Rose: малые шаги «делители → ОБЩИЕ делители → НОД», без перескока;
     • «Начала» Евклида, книга VII: сначала ВЫЧИТАНИЕ (антанерезис), потом
       деление с остатком; если вычитание доходит до единицы — числа взаимно
       просты (48 и 35: 9 шагов, доходим до 1);
     • японское бансё: рядом вывешиваем РАЗНЫЕ способы и сравниваем их;
     • изюминки: исторический свиток, ритмы Евклида (E(3,8) = x..x..x.,
       тресильо — «слышно» НОД), сцена с шестерёнками;
     • формативное оценивание: вопрос-петля на стыке уровней 2–3, мини-доска
       после вычитания, выходной билет, диагностическая ошибка «остановились
       на первом общем делителе», «докажи словом».

   ТИПИЧНЫЕ ЗАБЛУЖДЕНИЯ, которые урок ловит (по исследованиям):
     • останавливаются на первом общем делителе, а не на наибольшем;
     • путают делители и кратные (НОД и НОК);
     • думают, что НОД всегда больше единицы (теряют взаимно простые);
     • считают, что НОД = произведению чисел.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     замощение 24 × 15: 1 квадрат 15×15, 1 квадрат 9×9, 1 квадрат 6×6,
       2 квадрата 3×3 → НОД = 3
     вычитанием: 24−15=9; 15−9=6; 9−6=3; 6−3=3; 3−3=0 → 3
     делением: 24:15=1(ост.9); 15:9=1(ост.6); 9:6=1(ост.3); 6:3=2(ост.0) → 3
     инвариант НОД(a,b) = НОД(b, a mod b) проверен на (24,15), (48,18),
       (1071,462), (100,75), (91,35)
     сравнение способов: 48 = 2·2·2·2·3, 18 = 2·3·3 → НОД 6; Евклид: 3 шага
     «Начала», кн. VII: 1071 и 462 → 1071:462=2(147); 462:147=3(21);
       147:21=7(0) → НОД 21 (всего три деления!)
     взаимно простые: 48 и 35 → вычитание за 9 шагов доходит до 1
     ритм Евклида: E(3,8) — удары на долях 0, 3, 6, промежутки 3, 3, 2
       (различаются не больше чем на 1 — раскладка самая ровная), запись x..x..x.

   СТАНДАРТ: deploy/АНИМАЦИЯ_УРОКОВ.md и deploy/РИСОВАНИЕ_ФИГУР.md.
   Движение — сама математика: квадраты вырезаются по одному, остаток
   замощается, цепочка вычитаний идёт по числовой прямой, ритм отбивает доли.
   Состояние всегда видимо, текст не масштабируется, keyTimes в [0;1],
   reduced-motion выключает SMIL полностью, ничего не выходит за край кадра. */
(function(){
  'use strict';

  const ID = 379;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  const CSS=`
  #lvis .s6.l379{gap:14px}
  #lvis .s6.l379 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l379 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l379 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l379 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l379 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l379 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l379 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l379 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l379 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l379 .правило b{color:${GOLD}}
  #lvis .s6.l379 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l379 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l379 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l379 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l379 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l379 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l379 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l379 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l379 .уровни .точка.сейчас{background:${GOLD};animation:l379dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l379dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l379 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l379{-webkit-text-size-adjust:100%}
  #lvis .s6.l379 [data-anim]{animation:l379rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l379rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l379 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l379 [data-anim]{animation:none!important}
    #lvis .s6.l379 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l379 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l379-style');
      if(!s){ s=document.createElement('style'); s.id='l379-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(String(ID)) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');
  const растёт = (имя,знач,длит,доля,конец,старт) => анК(имя,старт+';'+старт+';'+знач+';'+знач,длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c379-верстак" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#4a3620"/><stop offset="0.45" stop-color="#33240f"/>
        <stop offset="1" stop-color="#1d1408"/>
      </linearGradient>
      <pattern id="c379-доски" width="30" height="14" patternUnits="userSpaceOnUse">
        <path d="M0 5 q8 -3 15 0 q7 3 15 0" fill="none" stroke="rgba(255,225,180,.05)" stroke-width="1.4"/>
      </pattern>
      <!-- камень плиты: светлая плита с зерном -->
      <pattern id="c379-прожилки" width="46" height="30" patternUnits="userSpaceOnUse">
        <path d="M2 22 q12 -10 22 -2 q8 6 20 -4" fill="none" stroke="rgba(120,105,80,.22)" stroke-width="1.3"/>
        <path d="M6 8 q14 6 24 0" fill="none" stroke="rgba(255,255,255,.10)" stroke-width="1.1"/>
      </pattern>
      <linearGradient id="c379-плита" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#e8e2d4"/><stop offset="0.45" stop-color="#cfc7b4"/>
        <stop offset="1" stop-color="#9d9481"/>
      </linearGradient>
      <linearGradient id="c379-кв1" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#bcd9ef"/><stop offset="0.45" stop-color="#8bb4d2"/>
        <stop offset="1" stop-color="#5a7f9c"/>
      </linearGradient>
      <linearGradient id="c379-кв2" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#cbead6"/><stop offset="0.45" stop-color="#94c9a8"/>
        <stop offset="1" stop-color="#5c8f70"/>
      </linearGradient>
      <linearGradient id="c379-кв3" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffe6ad"/><stop offset="0.45" stop-color="#e8bf6a"/>
        <stop offset="1" stop-color="#a8842f"/>
      </linearGradient>
      <linearGradient id="c379-кв4" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#f2b6a6"/><stop offset="0.45" stop-color="#d88a74"/>
        <stop offset="1" stop-color="#9c5340"/>
      </linearGradient>
      <linearGradient id="c379-металл" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#5d6a76"/><stop offset="0.18" stop-color="#e8eef4"/>
        <stop offset="0.5" stop-color="#9fb0bd"/><stop offset="0.8" stop-color="#57636f"/>
        <stop offset="1" stop-color="#434d57"/>
      </linearGradient>
      <linearGradient id="c379-пергамент" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#f7e9c8"/><stop offset="0.4" stop-color="#ecd9ac"/>
        <stop offset="1" stop-color="#c6a570"/>
      </linearGradient>
      <linearGradient id="c379-кожа" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#f6e2c4"/><stop offset="0.5" stop-color="#e6c9a0"/>
        <stop offset="1" stop-color="#b98f5f"/>
      </linearGradient>
      <linearGradient id="c379-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <linearGradient id="c379-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".6"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".22"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".2"/>
      </linearGradient>
      <radialGradient id="c379-свет" cx="0.24" cy="0.14" r="0.85" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fff3d0" stop-opacity=".24"/>
        <stop offset="0.6" stop-color="#fff3d0" stop-opacity=".06"/>
        <stop offset="1" stop-color="#000000" stop-opacity=".28"/>
      </radialGradient>
      <filter id="c379-тень" x="-70%" y="-70%" width="240%" height="240%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3.4" result="р"/>
        <feOffset in="р" dx="4" dy="6" result="с"/>
        <feFlood flood-color="#0a1a24" flood-opacity="0.45" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c379-контакт" x="-70%" y="-150%" width="240%" height="400%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.2" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#1d1508" flood-opacity="0.45" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c379-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c379-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c379-верстак)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="url(#c379-доски)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="url(#c379-свет)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.86)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  const бумага = (x,y,ш,в,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="6" fill="#08171f" opacity=".36" filter="url(#c379-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="url(#c379-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="none" filter="url(#c379-шум)" opacity=".1"/>
  </g>`;

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#0a1a12" opacity=".34" filter="url(#c379-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c379-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  /* ПЛИТА: каменная плита ш × в (в клетках по м)
     Квадрат-деталь: заливка своим тоном, фаска, тень, номер */
  const М=10;   /* размер клетки: 24 · 10 = 240 — плита входит в кадр 336 */
  const плита = (x,y,ш,в,нач) => `<g>
    <rect x="${x+4}" y="${y+6}" width="${ш*М}" height="${в*М}" rx="3" fill="#0a1a12" opacity=".5" filter="url(#c379-тень)"/>
    <rect x="${x}" y="${y}" width="${ш*М}" height="${в*М}" rx="3" fill="url(#c379-плита)" stroke="${ОБВОД}" stroke-width="2">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш*М}" height="${в*М}" rx="3" fill="none" filter="url(#c379-шум)" opacity=".16"/>
  </g>`;

  const квадрат = (x,y,сторона,тон,нач,номер) => `<g>
    <rect x="${x+2}" y="${y+3}" width="${сторона*М}" height="${сторона*М}" rx="2" fill="#0a1a12" opacity=".45" filter="url(#c379-тень)"/>
    <rect x="${x}" y="${y}" width="${сторона*М}" height="${сторона*М}" rx="2"
      fill="url(#c379-кв${тон})" stroke="${ОБВОД}" stroke-width="1.8">
      ${нач!=null?проявить('16s',нач,нач+0.04):''}</rect>
    <path d="M${x+2} ${y+сторона*М*0.3} L${x+2} ${y+2} L${x+сторона*М*0.3} ${y+2}" fill="none"
      stroke="rgba(255,255,255,.6)" stroke-width="1.4"/>
    <path d="M${x+сторона*М-2} ${y+сторона*М*0.7} L${x+сторона*М-2} ${y+сторона*М-2} L${x+сторона*М*0.7} ${y+сторона*М-2}"
      fill="none" stroke="rgba(0,0,0,.2)" stroke-width="1.4"/>
    ${номер?т(x+сторона*М/2,y+сторона*М/2+5,номер,14,'#2b2110',true):''}
  </g>`;

  /* ЗАМОЩЕНИЕ ПЛИТЫ 24 × 15 — раскладка, ПРОВЕРЕННАЯ РАСЧЁТОМ:
       15×15 + 9×9 + 6×6 + 3×3 + 3×3 = 225 + 81 + 36 + 9 + 9 = 360 = 24 · 15 ✓
     Квадраты идут по шагам алгоритма Евклида: каждый раз отрезаем самый
     большой квадрат, остаток продолжает ту же работу. */
  const РАСКЛАДКА=[
    {x:0, y:0, сторона:15, тон:1},   /* 24 : 15 = 1 — целая плита по высоте */
    {x:15,y:0, сторона:9,  тон:2},   /* 15 : 9  = 1 */
    {x:15,y:9, сторона:6,  тон:3},   /* 9 : 6   = 1 */
    {x:21,y:9, сторона:3,  тон:4},   /* 6 : 3   = 2 */
    {x:21,y:12,сторона:3,  тон:2}
  ];
  function замощение(x,y,ш,в,шаг,нач){
    return РАСКЛАДКА.slice(0,шаг).map((к,i)=>
      квадрат(x+к.x*М, y+к.y*М, к.сторона, к.тон, нач+i*0.06, String(к.сторона))
    ).join('');
  }

  /* ЦЕПОЧКА ВЫЧИТАНИЙ: последовательность пар */
  function цепочкаВычитания(пары,x,y,шаг,нач){
    return пары.map((п,i)=>{
      if(i>=шаг) return '';
      const кx=x+(i%4)*80, кy=y+Math.floor(i/4)*34;
      return `<g>${проявить('16s',нач+i*0.05,нач+i*0.05+0.04)}
        <rect x="${кx}" y="${кy}" width="72" height="24" rx="6" fill="rgba(255,255,255,.06)" stroke="${ЛИНИЯ}" stroke-width="1.1"/>
        ${т(кx+36,кy+17,п,14,ИНК,true)}</g>`;
    }).join('');
  }

  /* ТАБЛИЦА ДЕЛЕНИЙ */
  const таблица = (x,y,ш,строки,шаг,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${16+строки.length*22}" rx="6" fill="#08171f" opacity=".34" filter="url(#c379-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${16+строки.length*22}" rx="6" fill="url(#c379-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1"/>
    ${т(x+ш*0.30,y+16,'деление',12,'#7a6a44',true)}
    ${т(x+ш*0.76,y+16,'остаток',12,'#7a6a44',true)}
    ${строки.map(([дел,ост],i)=>{ if(i>=шаг) return '';
      return `<g>${проявить('16s',нач+i*0.06,нач+i*0.06+0.05)}
        <line x1="${x+10}" y1="${y+24+i*22}" x2="${x+ш-10}" y2="${y+24+i*22}" stroke="rgba(120,100,60,.25)" stroke-width="1"/>
        ${т(x+ш*0.30,y+38+i*22,дел,12,'#2b2110',true)}
        ${т(x+ш*0.76,y+38+i*22,ост,12,ост==='0'?GREEN:'#8a5a10',true)}</g>`;
    }).join('')}
  </g>`;

  /* СВИТОК для исторического кадра */
  const свиток = (x,y,ш,в,нач) => `<g>${проявить('15s',нач,нач+0.05)}
    <rect x="${x+4}" y="${y+6}" width="${ш}" height="${в}" rx="10" fill="#0a1a12" opacity=".5" filter="url(#c379-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="10" fill="url(#c379-пергамент)" stroke="rgba(90,60,25,.55)" stroke-width="1.4"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="10" fill="none" filter="url(#c379-шум)" opacity=".12"/>
    <rect x="${x-6}" y="${y-4}" width="12" height="${в+8}" rx="6" fill="url(#c379-кожа)" stroke="rgba(90,60,25,.5)" stroke-width="1.2"/>
    <rect x="${x+ш-6}" y="${y-4}" width="12" height="${в+8}" rx="6" fill="url(#c379-кожа)" stroke="rgba(90,60,25,.5)" stroke-width="1.2"/>
  </g>`;

  /* ШЕСТЕРЁНКИ: зубья как прямоугольники по кругу */
  const шестерёнка = (x,y,r,зубьев,цвет,поворот,нач) => {
    const зуб=[]; 
    for(let i=0;i<зубьев;i++){
      const а=(i/зубьев)*2*Math.PI;
      const x1=x+Math.cos(а)*(r+4), y1=y+Math.sin(а)*(r+4);
      зуб.push(`<rect x="${(x1-2.4).toFixed(1)}" y="${(y1-2.4).toFixed(1)}" width="4.8" height="4.8" rx="1"
        fill="url(#c379-металл)" transform="rotate(${(i/зубьев*360).toFixed(1)} ${x1.toFixed(1)} ${y1.toFixed(1)})"/>`);
    }
    return `<g>${проявить('15s',нач,нач+0.05)}
      <circle cx="${x}" cy="${y}" r="${r}" fill="url(#c379-металл)" stroke="rgba(40,45,50,.6)" stroke-width="1.4"/>
      <circle cx="${x}" cy="${y}" r="${r*0.72}" fill="none" stroke="rgba(255,255,255,.35)" stroke-width="1.2"/>
      <circle cx="${x}" cy="${y}" r="${r*0.22}" fill="#2a3238" stroke="rgba(20,25,30,.6)" stroke-width="1.2"/>
      ${зуб.join('')}
      <g transform="rotate(${поворот} ${x} ${y})"><line x1="${x}" y1="${y}" x2="${x}" y2="${y-r*0.7}"
        stroke="${цвет||GOLD}" stroke-width="2.2" stroke-linecap="round"/></g>
    </g>`;
  };

  /* РИТМ: сетка долей и удары */
  const мастер = (x,y,пол,м,нач) => {
    const девочка=пол!=='м', к=м||1;
    return `<g transform="translate(${x} ${y}) scale(${к})">${нач!=null?проявить('15s',нач,нач+0.05):''}
      <ellipse cx="0" cy="2" rx="11" ry="3.4" fill="#08171f" opacity=".45" filter="url(#c379-тень)"/>
      ${девочка?'<path d="M-7 -13 l14 0 l3 13 l-20 0 Z" fill="#8fd1a8" stroke="'+ОБВОД+'" stroke-width="1"/>'
               :'<path d="M-5 -14 h10 l1.3 12 h-12.6 Z" fill="#7fb3d0" stroke="'+ОБВОД+'" stroke-width="1"/>'}
      <ellipse cx="-3" cy="-0.6" rx="3" ry="1.4" fill="#2b2f38"/>
      <ellipse cx="3" cy="-0.6" rx="3" ry="1.4" fill="#2b2f38"/>
      <path d="M-7 -33 q7 -2.4 14 0 l1.4 20 q-8.4 2 -16.8 0 Z" fill="${девочка?'#f2b25c':'#7fb3d0'}" stroke="${ОБВОД}" stroke-width="1.1"/>
      <path d="M-7 -29 q-3 7 -2 13" fill="none" stroke="${девочка?'#f2b25c':'#7fb3d0'}" stroke-width="3.8" stroke-linecap="round"/>
      <path d="M7 -29 q4 5 6 8" fill="none" stroke="${девочка?'#f2b25c':'#7fb3d0'}" stroke-width="3.8" stroke-linecap="round"/>
      <circle cx="-9.4" cy="-14" r="2.4" fill="#f4c9a3" stroke="${ОБВОД}" stroke-width="0.9"/>
      <circle cx="12" cy="-20" r="2.6" fill="#f4c9a3" stroke="${ОБВОД}" stroke-width="0.9"/>
      <path d="M13 -22 l9 -5 l5 3 l-9 5 Z" fill="url(#c379-металл)" stroke="rgba(40,45,50,.5)" stroke-width="0.8"/>
      <rect x="-2" y="-39" width="4" height="6" rx="2" fill="#d9a87e"/>
      ${девочка?'<path d="M-7 -46 q-3.4 9 -2.2 17 q2.8 -3 4.2 -9 Z" fill="#5a4028" stroke="'+ОБВОД+'" stroke-width="0.8"/><path d="M7 -46 q3.4 9 2.2 17 q-2.8 -3 -4.2 -9 Z" fill="#5a4028" stroke="'+ОБВОД+'" stroke-width="0.8"/>':''}
      <circle cx="0" cy="-45" r="6.6" fill="#f4c9a3" stroke="${ОБВОД}" stroke-width="1"/>
      <path d="M-6.8 -46.5 q6.8 ${девочка?-6.2:-5} 13.6 0 q-2.8 -1.9 -6.8 -1.9 q-4 0 -6.8 1.9 Z" fill="#5a4028" stroke="${ОБВОД}" stroke-width="0.8"/>
      <circle cx="-2.2" cy="-45.4" r="1.1" fill="#2a1d12"/>
      <circle cx="2.2" cy="-45.4" r="1.1" fill="#2a1d12"/>
      <circle cx="-2.6" cy="-45.8" r="0.4" fill="#fff"/>
      <circle cx="1.8" cy="-45.8" r="0.4" fill="#fff"/>
      <ellipse cx="-4.2" cy="-42.8" rx="1.6" ry="0.9" fill="#e88b7a" opacity=".45"/>
      <ellipse cx="4.2" cy="-42.8" rx="1.6" ry="0.9" fill="#e88b7a" opacity=".45"/>
      <path d="M-2.6 -41.6 q2.6 2.5 5.2 0" fill="none" stroke="#8a4a34" stroke-width="1" stroke-linecap="round"/>
    </g>`;
  };

  /* РАЗМЕРНАЯ ЛИНИЯ со стрелками — как на строительном чертеже */
  const размерная = (x1,y1,x2,y2,текст,нач,цвет) => {
    const цв=цвет||GOLD, угол=Math.atan2(y2-y1,x2-x1);
    const стр=(x,y,н)=>`<path d="M${x} ${y} l${(8*Math.cos(угол+н*0.42)).toFixed(1)} ${(8*Math.sin(угол+н*0.42)).toFixed(1)} M${x} ${y} l${(8*Math.cos(угол+н*Math.PI-0.42)).toFixed(1)} ${(8*Math.sin(угол+н*Math.PI-0.42)).toFixed(1)}" fill="none" stroke="${цв}" stroke-width="1.2"/>`;
    return `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
      <line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${цв}" stroke-width="1.2"/>
      ${стр(x1,y1,1)}${стр(x2,y2,-1)}
      ${подпись((x1+x2)/2+(y2===y1?0:16),(y1+y2)/2+(y2===y1?-12:0),текст,цв,12)}
    </g>`;
  };

  const ритм = (x,y,доли,удары,шаг,нач) => {
    const ш=(336-2*x)/доли;
    return `<g>
      ${Array.from({length:доли},(_,i)=>{
        const x1=x+i*ш;
        const удар=удары.includes(i);
        return `<g>${проявить('16s',нач+i*0.05,нач+i*0.05+0.04)}
          <rect x="${x1.toFixed(1)}" y="${y}" width="${(ш-3).toFixed(1)}" height="34" rx="5"
            fill="${удар?'url(#c379-кв4)':'rgba(255,255,255,.06)'}" stroke="${удар?ОБВОД:ЛИНИЯ}" stroke-width="1.4"/>
          ${т(x1+ш/2-1.5,y+22,удар?'удар':'·',удар?14:12,удар?'#2b2110':МУТ,true)}</g>`;
      }).join('')}
      <line x1="${x}" y1="${y+40}" x2="${x+доли*ш}" y2="${y+40}" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
      ${Array.from({length:доли+1},(_,i)=>`<line x1="${(x+i*ш).toFixed(1)}" y1="${y+36}" x2="${(x+i*ш).toFixed(1)}" y2="${y+44}" stroke="${ЛИНИЯ}" stroke-width="1.1"/>`).join('')}
    </g>`;
  };

  /* ================= КАДРЫ ================= */

  /* 1. Сюжет: плита 24 × 15 */
  function F1(s){
    const в = s.плита;
    return ВОПРОС('Плиту 24 × 15 надо разрезать на одинаковые квадраты без отходов. Какой квадрат самый большой?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Мастерская мозаики',14,GOLD,true)}
        ${плита(24,44,24,15,0.04)}
        ${размерная(24,44+15*М+16,24+24*М,44+15*М+16,'24',0.2)}
        ${размерная(24-16,44,24-16,44+15*М,'15',0.28)}
        ${мастер(296,224,'д',1.05,0.36)}
        <g>${проявить('15s',0.4,0.5)}
          ${стекло(198,92,120,44,0.4)}
          ${т(258,112,'без отходов',12,ИНК,true)}
          ${т(258,130,'и все квадраты равны',12,МУТ)}</g>
        <g>${проявить('15s',0.62,0.72)}
          ${подпись(168,228,'какой квадрат самый большой?',GOLD,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='большой'?'hit':(в?'miss':''), 'Надо найти самый большой квадрат, который уложится без остатка', "r379Плита('большой')")}
        ${BTN(4, в==='маленький'?'miss':'', 'Возьмём самый маленький — 1 × 1', "r379Плита('маленький')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='большой',
        в==='большой' ? 'Верно: квадрат 1 × 1 подойдёт всегда, но он не самый большой. Такой квадрат — это и есть НОД сторон.'
                      : 'Нет: 1 × 1 подойдёт, но это скучно и совсем не наибольший. Найдём самый большой.') : ЖДЁТ()) +
      ПРАВИЛО('Сторона наибольшего квадрата для плиты a × b — это <b>НОД(a, b)</b>.');
  }

  /* 2. Первый квадрат 15×15 */
  function F2(s){
    const в = s.первый;
    return ВОПРОС('Пробуем квадрат 15 × 15. Что останется от плиты?') +
      `<div class="pic">${свг(`
        ${фон(268)}
        ${т(168,24,'Отрезаем квадрат 15 × 15',14,GOLD,true)}
        ${плита(24,44,24,15,0.03)}
        ${замощение(24,44,24,15,1,0.18)}
        <line x1="${24+15*М}" y1="44" x2="${24+15*М}" y2="${44+15*М}" stroke="${GOLD}" stroke-width="1.6" stroke-dasharray="5 4">
          ${проявить('15s',0.34,0.42)}</line>
        <g>${проявить('15s',0.5,0.6)}
          ${стекло(24,196,288,42,0.5)}
          ${т(120,222,'осталась полоса',12,МУТ)}
          ${т(252,222,'9 × 15',14,GOLD,true)}</g>
        <g>${проявить('15s',0.68,0.78)}
          ${подпись(168,250,'квадрат съел 15 из 24 — осталось 9',ИНК,12)}</g>
      `,268)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='полоса'?'hit':(в?'miss':''), 'Осталась полоса 9 × 15: 24 − 15 = 9', "r379Первый('полоса')")}
        ${BTN(4, в==='ничего'?'miss':'', 'Ничего: квадрат 15 × 15 покрыл плиту целиком', "r379Первый('ничего')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='полоса',
        в==='полоса' ? 'Верно: 24 = 15 + 9, поэтому остаётся полоса шириной 9. Дальше будем резать её.'
                     : 'Нет: 15 меньше 24, значит плита не покрыта. Остаётся полоса 9 × 15.') : ЖДЁТ()) +
      ПРАВИЛО('Большой квадрат не подошёл — <b>работаем с остатком</b> 9 × 15.');
  }

  /* 3. Квадраты 9 и 6 */
  function F3(s){
    const в = s.дальше;
    return ВОПРОС('Продолжаем с полосой 9 × 15. Какие квадраты отрежутся?') +
      `<div class="pic">${свг(`
        ${фон(268)}
        ${т(168,24,'Режем остаток дальше',14,GOLD,true)}
        ${плита(24,44,24,15,0.03)}
        ${замощение(24,44,24,15,4,0.1)}
        <g>${проявить('15s',0.6,0.7)}
          ${стекло(24,196,288,44,0.6)}
          ${т(84,222,'сначала 9 × 9',12,МУТ)}
          ${т(196,222,'потом 6 × 6',12,МУТ)}
          ${т(292,222,'6 × 3',12,GOLD,true)}</g>
        <g>${проявить('15s',0.76,0.86)}
          ${подпись(168,252,'отрезаем самый большой квадрат',ИНК,12)}</g>
      `,268)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='девять'?'hit':(в?'miss':''), '9 × 9, потом 6 × 6: остаются 9 × 6 и 6 × 3', "r379Дальше('девять')")}
        ${BTN(4, в==='шесть'?'miss':'', 'Сразу 6 × 6 — пропустим девятку', "r379Дальше('шесть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='девять',
        в==='девять' ? 'Верно: от полосы 9 × 15 отрезаем 9 × 9, остаётся 9 × 6, потом 6 × 6 и остаётся 6 × 3. Каждый раз берём самый большой квадрат.'
                     : 'Нет: сначала в полосе 9 × 15 помещается квадрат 9 × 9 — его и отрезаем первым.') : ЖДЁТ()) +
      ПРАВИЛО('Правило одно: <b>всегда отрезай самый большой квадрат</b>.');
  }

  /* 4. Финал: 3 × 3 покрывает ровно */
  function F4(s){
    const в = s.финал;
    return ВОПРОС('Осталось 6 × 3. Какой квадрат закроет её без отходов?') +
      `<div class="pic">${свг(`
        ${фон(280)}
        ${т(168,24,'Последний квадрат — 3 × 3',14,GOLD,true)}
        ${плита(24,44,24,15,0.03)}
        ${замощение(24,44,24,15,5,0.08)}
        <g>${проявить('15s',0.66,0.76)}
          ${стекло(24,192,288,52,0.66)}
          ${т(70,212,'15 × 15 — 1',12,МУТ)}
          ${т(178,212,'9 × 9 — 1',12,МУТ)}
          ${т(268,212,'6 × 6 — 1',12,МУТ)}
          ${т(168,234,'3 × 3 — 2 (без отходов)',14,GREEN,true)}</g>
        <g>${проявить('15s',0.8,0.9)}
          ${подпись(168,262,'НОД(24, 15) = 3',GREEN,16)}</g>
      `,280)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), '3 × 3: сторона последнего квадрата и есть НОД', "r379Финал('три')")}
        ${BTN(4, в==='шесть'?'miss':'', '6 × 6: он же был раньше', "r379Финал('шесть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: 6 × 3 закрывается двумя квадратами 3 × 3, отходов нет. Значит наибольший квадрат — 3 × 3, то есть НОД(24, 15) = 3.'
                  : 'Нет: 6 × 3 квадратом 6 × 6 не закрыть. Подходит 3 × 3 — два таких квадрата.') : ЖДЁТ()) +
      ПРАВИЛО('Сторона последнего квадрата — <b>НОД</b>: НОД(24, 15) = 3.');
  }

  /* 5. Вычитание (Евклид, антанерезис) */
  function F5(s){
    const в = s.вычитание;
    const пары=['24 − 15 = 9','15 − 9 = 6','9 − 6 = 3','6 − 3 = 3','3 − 3 = 0'];
    return ВОПРОС('Ту же работу можно сделать вычитанием. С чего начать?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Способ Евклида: вычитание',14,GOLD,true)}
        ${цепочкаВычитания(пары,20,40,5,0.05)}
        <g>${проявить('15s',0.66,0.76)}
          ${стекло(20,120,296,42,0.66)}
          ${т(168,147,'вычитаем меньшее, пока не 0',12,ИНК,true)}</g>
        <g>${проявить('15s',0.8,0.9)}
          ${подпись(168,186,'последнее НЕнулевое число — это НОД',GREEN,12)}
          ${подпись(168,224,'НОД(24, 15) = 3',GREEN,16)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='вычесть'?'hit':(в?'miss':''), 'Из большего вычесть меньшее: 24 − 15 = 9', "r379Вычит('вычесть')")}
        ${BTN(4, в==='сложить'?'miss':'', 'Сложить числа: 24 + 15 = 39', "r379Вычит('сложить')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='вычесть',
        в==='вычесть' ? 'Верно: Евклид начинал именно с вычитания. 24 − 15 = 9, потом 15 − 9 = 6 и так до нуля.'
                     : 'Нет: сложение тут не помогает. Начинаем с разности: 24 − 15 = 9.') : ЖДЁТ()) +
      ПРАВИЛО('Алгоритм Евклида: <b>вычитай меньшее из большего</b>, пока не получишь 0.');
  }

  /* 6. Деление с остатком — короче */
  function F6(s){
    const в = s.деление;
    const строки=[['24:15','1 (ост.9)'],['15:9','1 (ост.6)'],['9:6','1 (ост.3)'],['6:3','2 (ост.0)']];
    return ВОПРОС('Вычитать долго. Как сократить работу?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Деление с остатком — то же самое',14,GOLD,true)}
        ${таблица(24,40,288,строки,4,0.05)}
        <g>${проявить('15s',0.6,0.7)}
          ${стекло(24,156,288,42,0.6)}
          ${т(168,182,'24 : 15 = 1 — это одно вычитание',12,ИНК,true)}</g>
        <g>${проявить('15s',0.76,0.86)}
          ${подпись(168,222,'НОД(24, 15) = 3',GREEN,16)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='деление'?'hit':(в?'miss':''), 'Делить с остатком: одно деление заменяет много вычитаний', "r379Дел('деление')")}
        ${BTN(4, в==='умножение'?'miss':'', 'Умножить числа и взять последние цифры', "r379Дел('умножение')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='деление',
        в==='деление' ? 'Верно: 24 : 15 = 1 и остаток 9 — это сразу «ушли» все целые пятнадцатки. Остаток и продолжаем делить.'
                     : 'Нет: умножение ничего не даёт. Деление с остатком сразу отбрасывает лишние вычитания.') : ЖДЁТ()) +
      ПРАВИЛО('Быстрая запись: <b>НОД(a, b) = НОД(b, остаток)</b>.');
  }

  /* 7. Почему работает */
  function F7(s){
    const в = s.почему;
    return ВОПРОС('Почему можно заменять пару чисел на пару «делитель и остаток»?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Отрезанный квадрат не меняет НОД',14,GOLD,true)}
        ${шестерёнка(86,120,44,12,GOLD,0,0.05)}
        ${шестерёнка(196,120,28,8,GREEN,24,0.2)}
        <g>${проявить('15s',0.36,0.46)}
          ${т(258,86,'24 и 15',14,ИНК,true)}
          ${т(258,112,'15 и 9',14,GOLD,true)}
          ${т(258,138,'9 и 6',14,ИНК,true)}</g>
        <g>${проявить('15s',0.56,0.66)}
          ${стекло(20,168,296,44,0.56)}
          ${т(168,190,'у этих пар один и тот же общий делитель',12,ИНК,true)}
          ${т(168,206,'поэтому НОД не меняется',12,МУТ)}</g>
        <g>${проявить('15s',0.78,0.88)}
          ${подпись(168,238,'на НОД шестерёнки вернутся в такт',GREEN,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тот же'?'hit':(в?'miss':''), 'Общий делитель у пар одинаковый, значит и НОД тот же', "r379Поч('тот же')")}
        ${BTN(4, в==='меньше'?'miss':'', 'НОД уменьшается вместе с числами', "r379Поч('меньше')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тот же',
        в==='тот же' ? 'Верно: 24 = 1 · 15 + 9. Любой общий делитель 24 и 15 делит и 9, поэтому набор общих делителей не меняется.'
                    : 'Нет: числа уменьшаются, а НОД остаётся. Проверьте: 24 и 15 → 3, 15 и 9 → 3, 9 и 6 → 3.') : ЖДЁТ()) +
      ПРАВИЛО('Шаг Евклида сохраняет НОД: <b>набор общих делителей не меняется</b>.');
  }

  /* 8. Сравнение способов и большие числа */
  function F8(s){
    const в = s.сравнение;
    const строки=[['1071:462','2 (147)'],['462:147','3 (21)'],['147:21','7 (0)']];
    return ВОПРОС('Каким способом быстрее найти НОД(1071, 462)?') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${т(168,24,'Два способа: разложение и Евклид',14,GOLD,true)}
        ${стекло(14,38,150,96,0.05)}
        ${т(89,62,'разложить на простые',12,МУТ)}
        ${т(89,84,'1071 = 3 · 3 · 7 · 17',12,ИНК,true)}
        ${т(89,104,'462 = 2 · 3 · 7 · 11',12,ИНК,true)}
        ${т(89,124,'общие: 3 · 7',14,GREEN,true)}
        ${таблица(174,38,148,строки,3,0.24)}
        <g>${проявить('15s',0.6,0.7)}
          ${бумага(14,144,308,44,0.6,-0.4)}
          ${т(168,162,'Евклид — три деления вместо разложения',14,'#2b2110',true)}
          ${т(168,180,'и разложение 1071 совсем не простое',12,'#7a6a44')}</g>
        <g>${проявить('15s',0.78,0.88)}
          ${подпись(168,222,'НОД(1071, 462) = 21',GREEN,16)}</g>
      `,256)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='евклид'?'hit':(в?'miss':''), 'Евклидом: три деления с остатком', "r379Сравн('евклид')")}
        ${BTN(4, в==='разложение'?'miss':'', 'Разложением: нужно угадать делители 1071', "r379Сравн('разложение')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='евклид',
        в==='евклид' ? 'Верно: Евклид даёт ответ за три деления — 1071:462, 462:147, 147:21. Разложение 1071 требует перебора.'
                     : 'Нет: разложить 1071 трудно — делители 3, 7, 17 подбираются перебором. Евклид справляется за три шага.') : ЖДЁТ()) +
      ПРАВИЛО('Для больших чисел <b>алгоритм Евклида быстрее разложения</b>.');
  }

  /* 9. Свиток: взаимно простые */
  function F9(s){
    const в = s.свиток;
    return ВОПРОС('Что будет, если вычитание дойдёт до единицы?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Из «Начал» Евклида, книга VII',14,GOLD,true)}
        ${свиток(30,42,276,120,0.05)}
        ${т(168,74,'«…если меньшее непрерывно',12,'#5a4020')}
        ${т(168,94,'вычитается из большего',12,'#5a4020')}
        ${т(168,114,'и остаток никогда не измеряет',12,'#5a4020')}
        ${т(168,134,'предыдущее, пока не останется',12,'#5a4020')}
        ${т(168,152,'единица, то числа взаимно просты»',12,'#5a4020')}
        <g>${проявить('15s',0.4,0.5)}
          ${стекло(20,172,296,44,0.4)}
          ${т(168,194,'48 и 35: вычитаем 9 раз',12,ИНК,true)}
          ${т(168,210,'и получаем 1 — общих делителей нет',12,МУТ)}</g>
        <g>${проявить('15s',0.66,0.76)}
          ${подпись(168,240,'НОД(48, 35) = 1 — числа взаимно простые',GREEN,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='единица'?'hit':(в?'miss':''), 'Единица: общих делителей, кроме 1, нет — числа взаимно простые', "r379Свиток('единица')")}
        ${BTN(4, в==='нод1'?'miss':'', 'Значит, мы ошиблись в вычислениях', "r379Свиток('нод1')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='единица',
        в==='единица' ? 'Верно: если алгоритм приходит к единице, то НОД равен 1, и такие числа называют взаимно простыми.'
                      : 'Нет: ошибки нет. Единица — законный ответ: значит, общих делителей нет.') : ЖДЁТ()) +
      ПРАВИЛО('Если алгоритм дошёл до <b>1</b>, числа взаимно простые.');
  }

  /* 10. Ритм Евклида — изюминка */
  function F10(s){
    const в = s.ритм;
    return ВОПРОС('Три удара на восемь долей. Как разложить их ровнее всего?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Ритм Евклида: 3 удара на 8 долей',14,GOLD,true)}
        ${ритм(24,60,8,[0,3,6],8,0.06)}
        <g>${проявить('15s',0.5,0.6)}
          ${стекло(20,124,296,50,0.5)}
          ${т(168,146,'промежутки между ударами: 3, 3, 2',14,ИНК,true)}
          ${т(168,166,'они различаются не больше чем на 1',12,МУТ)}</g>
        <g>${проявить('15s',0.68,0.78)}
          ${подпись(168,196,'это и делает алгоритм Евклида',GREEN,12)}</g>
        <g>${проявить('15s',0.82,0.9)}
          ${подпись(168,232,'x..x..x. — знаменитый ритм тресильо',GOLD,14)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='ровно'?'hit':(в?'miss':''), 'Разложить как можно ровнее: удары на долях 1, 4 и 7', "r379Ритм('ровно')")}
        ${BTN(4, в==='подряд'?'miss':'', 'Поставить удары подряд: первый, второй, третий', "r379Ритм('подряд')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='ровно',
        в==='ровно' ? 'Верно: ровнее всего — через равные промежутки 3, 3 и 2. Такой ритм есть в музыке разных народов, и строит его алгоритм Евклида.'
                    : 'Нет: подряд удары звучат как один долгий удар. Самый ровный вариант — промежутки 3, 3 и 2.') : ЖДЁТ()) +
      ПРАВИЛО('Ритм E(3, 8) = <b>x..x..x.</b> — тот же алгоритм, только про музыку.');
  }

  /* 11. Сводка */
  function F11(s){
    const в = s.сводка;
    return ВОПРОС('Что делать первым делом, чтобы найти НОД двух чисел?') +
      `<div class="pic">${свг(`
        ${фон(260)}
        ${т(168,24,'Два пути и один вывод',14,GOLD,true)}
        ${/* слева — плита, справа — деление: сравнение, а не три карточки */''}
        <g>${проявить('15s',0.05,0.16)}
          <rect x="14" y="38" width="150" height="124" rx="10" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.3"/>
          ${т(89,60,'режем квадраты',14,GOLD,true)}
          <rect x="34" y="74" width="100" height="62" rx="3" fill="rgba(232,115,90,.18)" stroke="#e8735a" stroke-width="1.2"/>
          <rect x="34" y="74" width="62" height="62" rx="3" fill="url(#c379-кв1)" stroke="${ОБВОД}" stroke-width="1.1"/>
          <rect x="96" y="74" width="38" height="38" rx="3" fill="url(#c379-кв2)" stroke="${ОБВОД}" stroke-width="1.1"/>
          ${т(89,152,'видно, но долго',12,МУТ)}</g>
        <g>${проявить('15s',0.22,0.34)}
          <rect x="172" y="38" width="150" height="124" rx="10" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.3"/>
          ${т(247,60,'делим с остатком',14,GOLD,true)}
          ${т(247,86,'24 : 15 = 1 (ост. 9)',12,ИНК,true)}
          ${т(247,106,'15 : 9 = 1 (ост. 6)',12,ИНК,true)}
          ${т(247,126,'9 : 6 = 1 (ост. 3)',12,ИНК,true)}
          ${т(247,146,'6 : 3 = 2 (ост. 0)',12,GREEN,true)}</g>
        <g>${проявить('15s',0.5,0.6)}
          ${бумага(14,172,308,44,0.5,-0.4)}
          ${т(168,190,'ищи НАИБОЛЬШИЙ общий делитель',14,'#2b2110',true)}
          ${т(168,208,'а не первый попавшийся',12,'#7a6a44')}</g>
        <g>${проявить('15s',0.72,0.82)}
          ${подпись(168,240,'НОД(a, b) = НОД(b, остаток)',GOLD,14)}</g>
      `,260)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='делить'?'hit':(в?'miss':''), 'Взять большее число и делить его на меньшее с остатком', "r379Свод('делить')")}
        ${BTN(4, в==='вычитать'?'miss':'', 'Вычитать, пока не надоест', "r379Свод('вычитать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='делить',
        в==='делить' ? 'Верно: деление с остатком — самый короткий путь. Дальше повторяем с парой «делитель и остаток», пока остаток не станет нулём.'
                     : 'Нет: вычитать можно, но долго. Деление с остатком делает то же самое сразу.') : ЖДЁТ()) +
      ПРАВИЛО('НОД ищи <b>делением с остатком</b>: последний ненулевой остаток и есть ответ.');
  }

  /* 12. Практика с вопросом-петлёй и выходным билетом */
  const УРОВНИ=[
    { вопрос:'Плита 12 × 8. Какой самый большой квадрат?', варианты:[
        {т:'4 × 4', ок:true,  почему:'НОД(12, 8) = 4.'},
        {т:'8 × 8', ок:false, почему:'8 не делит 12: 12 − 8 = 4, останется полоса.'}]},
    { вопрос:'Что можно сказать про число 4 для пары 48 и 36?', варианты:[
        {т:'Это общий делитель, но не наибольший', ок:true,  почему:'НОД(48, 36) = 12, а 4 тоже делит оба числа.'},
        {т:'Это наибольший общий делитель', ок:false, почему:'Наибольший — 12, ведь 12 делит и 48, и 36.'}]},
    { вопрос:'НОД(24, 15) по алгоритму Евклида?', варианты:[
        {т:'3', ок:true,  почему:'24:15 = 1 (9); 15:9 = 1 (6); 9:6 = 1 (3); 6:3 = 2 (0).'},
        {т:'5', ок:false, почему:'5 не делит 24.'}]},
    { вопрос:'НОД(1071, 462)?', варианты:[
        {т:'21', ок:true,  почему:'1071:462 = 2 (147); 462:147 = 3 (21); 147:21 = 7 (0).'},
        {т:'7', ок:false, почему:'7 делит оба, но есть больший — 21.'}]},
    { вопрос:'НОД(48, 35) = 1. Что это значит?', варианты:[
        {т:'Числа взаимно простые', ок:true,  почему:'Общих делителей, кроме 1, нет.'},
        {т:'В расчёте ошибка', ок:false, почему:'Единица — законный ответ.'}]}
  ];
  function F12(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Выходной билет: придумай пару чисел с НОД = 1 и пару с НОД = 5.') +
        РАЗБОР(true,'Например: 8 и 9 — взаимно простые; 10 и 15 — их НОД равен 5. Проверь себя алгоритмом Евклида.') +
        `<div class="ask">${BTN(4,'','пройти заново','r379Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r379Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Ищи <b>наибольший</b> общий делитель: сравни делители, не останавливайся на первом.');
  }

  /* 13–15. Три тренажёра (в третьем — диагностическая ошибка) */
  const Т1={
    имя:'Тренажёр 1 · квадраты',
    задания:[
      { ф:'Плита 12 × 8. Самый большой квадрат?', о:['4 × 4','8 × 8','2 × 2'], в:0, р:'НОД(12, 8) = 4.' },
      { ф:'Плита 18 × 12. Самый большой квадрат?', о:['6 × 6','12 × 12','3 × 3'], в:0, р:'НОД(18, 12) = 6.' },
      { ф:'Плита 20 × 15. Самый большой квадрат?', о:['5 × 5','15 × 15','10 × 10'], в:0, р:'НОД(20, 15) = 5.' },
      { ф:'Плита 24 × 15: сколько квадратов 3 × 3 в остатке?', о:['два','один','три'], в:0, р:'Остаток 6 × 3 — это два квадрата 3 × 3.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · алгоритм Евклида',
    задания:[
      { ф:'НОД(24, 15) = ?', о:['3','5','1'], в:0, р:'Цепочка делений даёт 3.' },
      { ф:'НОД(48, 18) = ?', о:['6','3','12'], в:0, р:'48:18 = 2 (12); 18:12 = 1 (6); 12:6 = 2 (0).' },
      { ф:'НОД(100, 75) = ?', о:['25','5','50'], в:0, р:'100:75 = 1 (25); 75:25 = 3 (0).' },
      { ф:'НОД(91, 35) = ?', о:['7','13','1'], в:0, р:'91:35 = 2 (21); 35:21 = 1 (14); 21:14 = 1 (7); 14:7 = 2 (0).' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · разбор ошибок',
    задания:[
      { ф:'Ученик сказал: НОД(18, 24) = 3. Что не так?', о:['Есть больший общий делитель — 6','Ничего, всё верно','3 не делит 18'], в:0, р:'3 — общий делитель, но наибольший равен 6.' },
      { ф:'НОД(48, 35) = 1. Это ошибка?', о:['нет, числа взаимно простые','да, ошибка'], в:0, р:'Общих делителей нет.' },
      { ф:'Можно ли взять НОД = произведение чисел?', о:['нет','да'], в:0, р:'Произведение почти всегда больше НОД.' },
      { ф:'Что быстрее для 1071 и 462?', о:['алгоритм Евклида','разложение на множители'], в:0, р:'Евклид — три деления.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r379T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r379TNext('${ключ}')`)}</div>` : '');
  }
  function F13(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F14(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F15(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L379={
    id: ID, title:'НОД: алгоритм Евклида', ico:'🧱',
    src:'Математика · 5–6 класс · НОД', subj:'math',
    explain: [
      'В мастерской мозаики лежит каменная плита 24 на 15. Её нужно разрезать на одинаковые квадраты так, чтобы не осталось отходов. Квадрат 1 на 1 подойдёт всегда, но он слишком мелкий: хочется взять самый большой. Оказывается, сторона этого квадрата и есть наибольший общий делитель сторон плиты — в нашей задаче НОД(24, 15).',

      'Попробуем резать. Первым берём самый большой возможный квадрат: его сторона равна меньшей стороне плиты, то есть 15. Отрезаем квадрат 15 на 15 — и от плиты остаётся полоса 9 на 15, ведь 24 − 15 = 9. Заметьте: мы не гадаем, а просто всегда берём самый большой квадрат, который помещается.',

      'Теперь работаем с полосой 9 на 15. В ней помещается квадрат 9 на 9 — отрезаем его, остаётся 9 на 6. В остатке помещается квадрат 6 на 6 — отрезаем, остаётся 6 на 3. И вот здесь удача: полоса 6 на 3 закрывается двумя квадратами 3 на 3, и отходов не остаётся вовсе. Значит, самый большой квадрат для всей плиты — 3 на 3.',

      'Соберём, что вышло: плита 24 на 15 разрезалась на один квадрат 15 на 15, один квадрат 9 на 9, один квадрат 6 на 6 и два квадрата 3 на 3. Отходов нет, и все квадраты одного размера в каждом шаге. Сторона последнего квадрата — 3 — и есть НОД(24, 15) = 3. Геометрическая картинка дала ответ без всякой арифметики.',

      'Тот же путь можно пройти вычитанием, и именно так делал Евклид в своих «Началах». Из большего вычитаем меньшее: 24 − 15 = 9. Теперь у нас пара 15 и 9: 15 − 9 = 6. Дальше 9 − 6 = 3, потом 6 − 3 = 3 и наконец 3 − 3 = 0. Как только получился ноль, ответ — последнее ненулевое число, то есть 3. Этот способ называют алгоритмом Евклида, а древние греки — антанерезисом, взаимным вычитанием.',

      'Вычитать одно и то же много раз утомительно. Заметили: замена пары «24 и 15» на пару «15 и 9» — это то же, что деление с остатком. В самом деле, 24 : 15 = 1 и остаток 9: одно деление сразу отбрасывает все целые пятнадцатки. Продолжаем: 15 : 9 = 1 (остаток 6), 9 : 6 = 1 (остаток 3), 6 : 3 = 2 (остаток 0). Последний ненулевой остаток — 3 — это НОД. Так алгоритм работает в разы быстрее.',

      'Почему же замена пары разрешена? Посмотрим на равенство 24 = 1 · 15 + 9. Любой общий делитель чисел 24 и 15 делит и их разность, то есть 9. И наоборот: любой общий делитель 15 и 9 делит 24. Значит, у пар «24 и 15» и «15 и 9» один и тот же набор общих делителей, а потому и одинаковый наибольший общий делитель. На картинке с шестерёнками это видно как совпадение зубьев: НОД — тот момент, когда метки снова встречаются.',

      'Алгоритм особенно выручает на больших числах. Найдём НОД чисел 1071 и 462. Делим: 1071 : 462 = 2 и остаток 147. Дальше 462 : 147 = 3 и остаток 21. И наконец 147 : 21 = 7 и остаток 0. Ответ — 21, и понадобилось всего три деления. Разложить 1071 на множители куда труднее: пришлось бы перебирать делители. Для больших чисел алгоритм Евклида — самый быстрый путь.',

      'В книге VII «Начал» Евклид описывает этот способ так: если меньшее непрерывно вычитается из большего и остаток никогда не измеряет предыдущее, пока не останется единица, то числа взаимно просты. Проверим на числах 48 и 35: вычитание доходит до единицы за девять шагов. Значит, общих делителей, кроме единицы, у них нет — такие числа называют взаимно простыми. И это не ошибка, а законный ответ: НОД(48, 35) = 1.',

      'А теперь самое неожиданное. Тот же алгоритм раскладывает музыкальный ритм. Пусть нужно разместить три удара на восемь долей как можно ровнее. Алгоритм Евклида распределит их так: удар, два пропуска, удар, два пропуска, удар, один пропуск. Промежутки получаются 3, 3 и 2 — они различаются не больше чем на единицу, а значит, это самая ровная раскладка. Запись x..x..x. — знаменитый ритм тресильо, который встречается в музыке Латинской Америки и Африки. Один и тот же алгоритм считает и мозаику, и музыку.',

      'Соберём всё вместе. Есть три способа найти наибольший общий делитель: разрезать плиту на самые большие квадраты, вычитать меньшее из большего или делить с остатком. Самый быстрый — третий. Важно помнить две вещи: искать именно наибольший общий делитель, а не первый попавшийся, и не путать делители с кратными. И ещё: НОД может оказаться равным единице — тогда числа взаимно простые.',

      'Пройти практику: пять уровней про квадраты, алгоритм Евклида, большие числа и взаимно простые. В конце — выходной билет: придумай пару чисел с НОД, равным единице, и пару с НОД, равным пяти. Дальше ждут три тренажёра.',

      'Первый тренажёр — квадраты: по сторонам плиты находим сторону самого большого квадрата.',

      'Второй тренажёр — алгоритм Евклида: делим с остатком и находим НОД.',

      'Третий тренажёр — разбор ошибок: здесь спрятаны типичные неверные решения, и нужно объяснить, что в них не так.'
    ],
    check: {
      q:'Плита 24 × 15. Какой самый большой квадрат её замостит?',
      choices:['3 × 3','5 × 5','15 × 15'],
      ans:0,
      exp:'НОД(24, 15) = 3: 15×15, 9×9, 6×6 и два 3×3 закрывают плиту без отходов.'
    },
    tasks: [
      { q:'НОД(48, 18) = ?', kind:'choice',
        choices:['6','3'], ans:0,
        hints:['48 : 18 = 2 и остаток 12.','18 : 12 = 1 (6); 12 : 6 = 2 (0).'],
        sol:'6.' },
      { q:'Чему равен НОД(1071, 462)?', kind:'choice',
        choices:['21','7'], ans:0,
        hints:['1071 : 462 = 2 и остаток 147.','462 : 147 = 3 (21); 147 : 21 = 7 (0).'],
        sol:'21.' },
      { q:'НОД(48, 35) = 1. Что это значит?', kind:'choice',
        choices:['Числа взаимно простые','В расчёте ошибка'], ans:0,
        hints:['Общих делителей нет.','Единица — законный ответ.'],
        sol:'Числа взаимно простые.' },
      { q:'Ученик получил НОД(18, 24) = 3. Что не так?', kind:'choice',
        choices:['Есть больший общий делитель — 6','Ничего, ответ верный'], ans:0,
        hints:['3 делит оба числа, но…','Ищи наибольший.'],
        sol:'Наибольший общий делитель равен 6.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L379.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else if(f===14) сцена=F14(s);
    else сцена=F15(s);

    const ЗАГОЛОВКИ={
      1:['Мастерская','Плита 24 × 15'],
      2:['Первый квадрат','15 × 15 и остаток'],
      3:['Продолжаем','9 × 9, потом 6 × 6'],
      4:['Финал','3 × 3 закрывает ровно'],
      5:['Способ Евклида','Вычитание до нуля'],
      6:['Короче','Деление с остатком'],
      7:['Почему работает','НОД не меняется'],
      8:['Большие числа','1071 и 462'],
      9:['Начала, книга VII','Взаимно простые'],
      10:['Изюминка','Ритм Евклида'],
      11:['Сводка','Три способа'],
      12:['Практика','Пять уровней'],
      13:['Тренажёр 1','Квадраты'],
      14:['Тренажёр 2','Алгоритм Евклида'],
      15:['Тренажёр 3','Разбор ошибок']
    };
    const з=ЗАГОЛОВКИ[f]||['НОД: алгоритм Евклида','Делимость'];
    el.innerHTML = `<div class="s6 l379" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r379Плита=(к)=>{ S().плита=к; chRender(0); };
  window.r379Первый=(к)=>{ S().первый=к; chRender(0); };
  window.r379Дальше=(к)=>{ S().дальше=к; chRender(0); };
  window.r379Финал=(к)=>{ S().финал=к; chRender(0); };
  window.r379Вычит=(к)=>{ S().вычитание=к; chRender(0); };
  window.r379Дел=(к)=>{ S().деление=к; chRender(0); };
  window.r379Поч=(к)=>{ S().почему=к; chRender(0); };
  window.r379Сравн=(к)=>{ S().сравнение=к; chRender(0); };
  window.r379Свиток=(к)=>{ S().свиток=к; chRender(0); };
  window.r379Ритм=(к)=>{ S().ритм=к; chRender(0); };
  window.r379Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r379Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r379Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r379T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r379TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L379; else arr.push(L379);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA379={render:render, L:L379};
})();
