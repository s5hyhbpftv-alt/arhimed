/* ============ РУССКИЙ ЯЗЫК · УРОК 1000 · «ПАРОНИМЫ» ============
   5 класс, раздел «Лексика». Сюжет: мастерская слова — слова-близнецы.

   СВЕРКА С ПРОГРАММОЙ (5 класс): паронимы — слова, близкие по звучанию,
   но разные по значению: «одеть» и «надеть», «эффектный» и «эффективный»,
   «невежа» и «невежда», «адресат» и «адресант», «дипломат» и «дипломант».

   ЭТАЛОН (995, 830, 932, 1032, 996): движение в кадре — это САМА МАТЕМАТИКА
   смысла, то есть значение слова:
     кадр 1  два слова-близнеца: фигуры появляются, значения встают на места;
     кадр 2  одна звуковая волна и два разных значения;
     кадр 3  ОДЕЖДА ЕДЕТ на ребёнка (одеть кого-то) и шапка — на себя
             (надеть что-то): предмет движется, стрелка подписывает вопрос;
     кадр 4  «надеть одежду, одеть Надежду»: слова вспыхивают по очереди;
     кадр 5  прожектор ХОДИТ по эффектному костюму, у эффективной машины
             ВЫРАСТАЕТ результат;
     кадр 6  невежа ТОЛКАЕТ, у невежды книга ПЕРЕВОРАЧИВАЕТСЯ;
     кадр 7  письмо ЕДЕТ от адресанта к адресату;
     кадр 8  диплом РАЗВОРАЧИВАЕТСЯ у дипломанта;
     кадр 9  чаши весов смысла КАЧАЮТСЯ и выбирают слово;
     кадр 10 стрелки СВЯЗЫВАЮТ слово со значением по одной;
     кадр 11 практика из пяти уровней; кадры 12–14 — ТРИ ТРЕНАЖЁРА.

   РИСУНОК ПО СТАНДАРТУ deploy/РИСОВАНИЕ_ФИГУР.md: плоские заливки, тёмная
   обводка #33291e, свет сверху-слева, тень внизу-справа, ОДНА линия земли
   на кадр. Люди нарисованы кодом, эмодзи не используются.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, движение вдоль смысла, ничего за край кадра,
   keyTimes строго в [0;1], prefers-reduced-motion выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 1000;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';
  const ТУШЬ='#1e3128', ОБВОД='#33291e';
  const КОЖА='#f4c9a3', КОЖА_Т='#d9a87e', БУМАГА='#f7f3e6';

  const CSS=`
  #lvis .s6.l1000{gap:14px}
  #lvis .s6.l1000 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l1000 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l1000 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l1000 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l1000 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l1000 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l1000 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l1000 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l1000 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l1000 .правило b{color:${GOLD}}
  #lvis .s6.l1000 .пара{width:100%;display:flex;gap:10px}
  #lvis .s6.l1000 .пара>div{flex:1 1 0;padding:10px 12px;border-radius:12px;background:rgba(255,255,255,.04);
    border:1.5px solid var(--line);text-align:center}
  #lvis .s6.l1000 .пара .сл{font-size:20px;font-weight:600}
  #lvis .s6.l1000 .пара .зн{font-size:16px;color:var(--mut);margin-top:4px;line-height:1.35}
  #lvis .s6.l1000 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l1000 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1000 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l1000 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l1000 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l1000 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l1000 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1000 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l1000 .уровни .точка.сейчас{background:${GOLD};animation:l1000dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l1000dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l1000 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l1000{-webkit-text-size-adjust:100%}
  #lvis .s6.l1000 [data-anim]{animation:l1000rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l1000rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l1000 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l1000 [data-anim]{animation:none!important}
    #lvis .s6.l1000 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l1000 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l1000-style');
      if(!s){ s=document.createElement('style'); s.id='l1000-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ПАРА = (сл1,зн1,сл2,зн2) => A(3,'пара',
    `<div><div class="сл">${сл1}</div><div class="зн">${зн1}</div></div>`+
    `<div><div class="сл">${сл2}</div><div class="зн">${зн2}</div></div>`);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анП = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анД = (путь,длит) =>
    ДВИЖ ? `<animateMotion dur="${длит}" repeatCount="indefinite" path="${путь}"/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const светит = (длит,нач) => ан('opacity','1;0.6;1',длит, нач!=null?'begin="'+нач+'s"':'');
  const проявить = (длит,доля,конец,знач) => ан('opacity',(знач||'0.3')+';'+(знач||'0.3')+';1;1',длит,
    'keyTimes="0;'+кт(доля)+';'+кт(конец)+';1"');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c1000-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#26382e"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#131f18"/>
      </linearGradient>
      <linearGradient id="c1000-земля" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#3a5a3f"/><stop offset="1" stop-color="#22392a"/>
      </linearGradient>
      <radialGradient id="c1000-луч" cx="0.5" cy="0.1" r="0.8" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fff6d8" stop-opacity=".55"/>
        <stop offset="1" stop-color="#fff6d8" stop-opacity="0"/>
      </radialGradient>
      <filter id="c1000-тень" x="-60%" y="-140%" width="220%" height="380%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3.4" result="р"/>
        <feOffset in="р" dx="3" dy="5" result="с"/>
        <feFlood flood-color="#0b1c2a" flood-opacity="0.35" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c1000-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="3" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  /* фон кадра: стена, зерно, рамка и ОДНА линия земли */
  const фон = (высота,земля,сЗемлёй) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c1000-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c1000-шум)" opacity="0.05"/>
     ${земля?`<rect x="0" y="${земля}" width="336" height="${высота-земля}" fill="url(#c1000-земля)"/>
       <line x1="0" y1="${земля}" x2="336" y2="${земля}" stroke="rgba(0,0,0,.28)" stroke-width="1.6"/>`:''}
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||216}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* ЧЕЛОВЕЧЕК: тень → ноги и обувь → туловище → руки → шея → голова →
     волосы → брови → глаза с бликами → рот. Порядок и приёмы — по скилу
     arhimed-drawing: суставы кружками после конечностей, блики в глазах,
     брови дугой (у грубого — с наклоном), одна линия земли на кадр. */
  function человек(x,земля,рост,цветОдежды,нач,лицо){
    const R=рост, кожа=КОЖА, тень=КОЖА_Т, штаны='#3f4a5c', обувь='#2b2f38';
    const y=земля;
    const г=0.145*R;                 /* радиус головы */
    const гЦ=(x, y-0.855*R);
    const плечи=y-0.62*R, таз=y-0.30*R;
    return `<g>
      <ellipse cx="${x}" cy="${y+2}" rx="${(0.3*R).toFixed(1)}" ry="4.5" fill="#0b1c2a" opacity=".32" filter="url(#c1000-тень)"/>
      <line x1="${(x-0.075*R).toFixed(1)}" y1="${таз.toFixed(1)}" x2="${(x-0.095*R).toFixed(1)}" y2="${y}" stroke="${штаны}" stroke-width="${(0.115*R).toFixed(1)}" stroke-linecap="round"/>
      <line x1="${(x+0.075*R).toFixed(1)}" y1="${таз.toFixed(1)}" x2="${(x+0.095*R).toFixed(1)}" y2="${y}" stroke="${штаны}" stroke-width="${(0.115*R).toFixed(1)}" stroke-linecap="round"/>
      <ellipse cx="${(x-0.1*R).toFixed(1)}" cy="${y}" rx="${(0.07*R).toFixed(1)}" ry="${(0.032*R).toFixed(1)}" fill="${обувь}"/>
      <ellipse cx="${(x+0.1*R).toFixed(1)}" cy="${y}" rx="${(0.07*R).toFixed(1)}" ry="${(0.032*R).toFixed(1)}" fill="${обувь}"/>
      <line x1="${(x-0.155*R).toFixed(1)}" y1="${плечи.toFixed(1)}" x2="${(x-0.245*R).toFixed(1)}" y2="${(y-0.36*R).toFixed(1)}" stroke="${цветОдежды}" stroke-width="${(0.095*R).toFixed(1)}" stroke-linecap="round"/>
      <line x1="${(x+0.155*R).toFixed(1)}" y1="${плечи.toFixed(1)}" x2="${(x+0.245*R).toFixed(1)}" y2="${(y-0.36*R).toFixed(1)}" stroke="${цветОдежды}" stroke-width="${(0.095*R).toFixed(1)}" stroke-linecap="round"/>
      <rect x="${(x-0.18*R).toFixed(1)}" y="${плечи.toFixed(1)}" width="${(0.36*R).toFixed(1)}" height="${(таз-плечи).toFixed(1)}" rx="${(0.08*R).toFixed(1)}"
        fill="${цветОдежды}" stroke="${ОБВОД}" stroke-width="1.8"/>
      <path d="M${(x-0.06*R).toFixed(1)} ${плечи.toFixed(1)} L${x} ${(плечи+0.06*R).toFixed(1)} L${(x+0.06*R).toFixed(1)} ${плечи.toFixed(1)}"
        fill="none" stroke="${ОБВОД}" stroke-width="1.4"/>
      <circle cx="${(x-0.245*R).toFixed(1)}" cy="${(y-0.36*R).toFixed(1)}" r="${(0.055*R).toFixed(1)}" fill="${кожа}" stroke="${ОБВОД}" stroke-width="1.2"/>
      <circle cx="${(x+0.245*R).toFixed(1)}" cy="${(y-0.36*R).toFixed(1)}" r="${(0.055*R).toFixed(1)}" fill="${кожа}" stroke="${ОБВОД}" stroke-width="1.2"/>
      <rect x="${(x-0.05*R).toFixed(1)}" y="${(y-0.72*R).toFixed(1)}" width="${(0.1*R).toFixed(1)}" height="${(0.12*R).toFixed(1)}" fill="${тень}"/>
      <circle cx="${x}" cy="${(y-0.855*R).toFixed(1)}" r="${г.toFixed(1)}" fill="${кожа}" stroke="${ОБВОД}" stroke-width="1.8"/>
      <path d="M${(x-г).toFixed(1)} ${(y-0.9*R).toFixed(1)} Q${x} ${(y-1.02*R).toFixed(1)} ${(x+г).toFixed(1)} ${(y-0.9*R).toFixed(1)}
        Q${(x+г*0.5).toFixed(1)} ${(y-0.94*R).toFixed(1)} ${x} ${(y-0.95*R).toFixed(1)}
        Q${(x-г*0.5).toFixed(1)} ${(y-0.94*R).toFixed(1)} ${(x-г).toFixed(1)} ${(y-0.9*R).toFixed(1)} Z"
        fill="#4a3527" stroke="${ОБВОД}" stroke-width="1.2"/>
      ${лицо==='грубо'
        ? `<line x1="${(x-0.09*R).toFixed(1)}" y1="${(y-0.9*R).toFixed(1)}" x2="${(x-0.03*R).toFixed(1)}" y2="${(y-0.93*R).toFixed(1)}" stroke="${ОБВОД}" stroke-width="1.6"/>
           <line x1="${(x+0.09*R).toFixed(1)}" y1="${(y-0.9*R).toFixed(1)}" x2="${(x+0.03*R).toFixed(1)}" y2="${(y-0.93*R).toFixed(1)}" stroke="${ОБВОД}" stroke-width="1.6"/>
           <path d="M${(x-0.06*R).toFixed(1)} ${(y-0.79*R).toFixed(1)} Q${x} ${(y-0.755*R).toFixed(1)} ${(x+0.06*R).toFixed(1)} ${(y-0.79*R).toFixed(1)}"
             fill="none" stroke="${ОБВОД}" stroke-width="1.6"/>`
        : `<path d="M${(x-0.09*R).toFixed(1)} ${(y-0.93*R).toFixed(1)} Q${(x-0.06*R).toFixed(1)} ${(y-0.96*R).toFixed(1)} ${(x-0.03*R).toFixed(1)} ${(y-0.93*R).toFixed(1)}"
             fill="none" stroke="${ОБВОД}" stroke-width="1.6"/>
           <path d="M${(x+0.03*R).toFixed(1)} ${(y-0.93*R).toFixed(1)} Q${(x+0.06*R).toFixed(1)} ${(y-0.96*R).toFixed(1)} ${(x+0.09*R).toFixed(1)} ${(y-0.93*R).toFixed(1)}"
             fill="none" stroke="${ОБВОД}" stroke-width="1.6"/>
           <path d="M${(x-0.06*R).toFixed(1)} ${(y-0.79*R).toFixed(1)} Q${x} ${(y-0.82*R).toFixed(1)} ${(x+0.06*R).toFixed(1)} ${(y-0.79*R).toFixed(1)}"
             fill="none" stroke="${ОБВОД}" stroke-width="1.6"/>`}
      <circle cx="${(x-0.05*R).toFixed(1)}" cy="${(y-0.875*R).toFixed(1)}" r="1.8" fill="${ОБВОД}"/>
      <circle cx="${(x+0.05*R).toFixed(1)}" cy="${(y-0.875*R).toFixed(1)}" r="1.8" fill="${ОБВОД}"/>
      <circle cx="${(x-0.05*R-0.6).toFixed(1)}" cy="${(y-0.875*R-0.6).toFixed(1)}" r="0.6" fill="#fff"/>
      <circle cx="${(x+0.05*R-0.6).toFixed(1)}" cy="${(y-0.875*R-0.6).toFixed(1)}" r="0.6" fill="#fff"/>
      ${нач!=null?проявить('10s',нач,нач+0.08):''}
    </g>`;
  }

  /* КУРТКА и ШАПКА — предметы, которые двигаются в кадре 3 */
  const куртка = (x,y,ш,нач) => `<g>
    <rect x="${x}" y="${y}" width="${ш}" height="${ш*0.8}" rx="6" fill="${GREEN}" stroke="${ОБВОД}" stroke-width="1.8"/>
    <line x1="${x+ш/2}" y1="${y}" x2="${x+ш/2}" y2="${y+ш*0.8}" stroke="${ОБВОД}" stroke-width="1.4"/>
    ${нач!=null?проявить('10s',нач,нач+0.06):''}
  </g>`;
  const шапка = (x,y,ш,нач) => `<g>
    <path d="M${x} ${y} A${ш/2} ${ш*0.6} 0 0 1 ${x+ш} ${y} Z" fill="${RED}" stroke="${ОБВОД}" stroke-width="1.8"/>
    <rect x="${x-3}" y="${y}" width="${ш+6}" height="5" rx="2.5" fill="#c14b3c" stroke="${ОБВОД}" stroke-width="1.2"/>
    ${нач!=null?проявить('10s',нач,нач+0.06):''}
  </g>`;

  /* КОНВЕРТ */
  const конверт = (x,y,ш,нач) => `<g>
    <rect x="${x}" y="${y}" width="${ш}" height="${ш*0.66}" rx="3" fill="${БУМАГА}" stroke="${ОБВОД}" stroke-width="1.6"/>
    <path d="M${x} ${y} L${x+ш/2} ${y+ш*0.38} L${x+ш} ${y}" fill="none" stroke="${ОБВОД}" stroke-width="1.4"/>
    ${нач!=null?проявить('10s',нач,нач+0.06):''}
  </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Слова-близнецы */
  function F1(s){
    const в = s.близнецы;
    const земля=178;
    return ВОПРОС('Почему слова «одеть» и «надеть» называют паронимами?') +
      `<div class="pic">${свг(`
        ${фон(216,земля,true)}
        ${т(168,22,'Слова-близнецы',14,GOLD,true)}
        ${человек(88,земля,96,'#5aa3cd',0.05,'добро')}
        ${человек(248,земля,96,'#6aa34e',0.15,'добро')}
        <rect x="30" y="44" width="116" height="30" rx="8" fill="rgba(255,255,255,.06)" stroke="${GOLD}" stroke-width="1.6">
          ${проявить('10s',0.3,0.36)}</rect>
        ${т(88,64,'одеть',20,GOLD,true)}
        <rect x="190" y="44" width="116" height="30" rx="8" fill="rgba(255,255,255,.06)" stroke="${GOLD}" stroke-width="1.6">
          ${проявить('10s',0.45,0.51)}</rect>
        ${т(248,64,'надеть',20,GOLD,true)}
        <g>${ан('opacity','.35;.75;.35','5s')}
          <rect x="26" y="40" width="124" height="38" rx="10" fill="none" stroke="${BLUE}" stroke-width="2"/>
          <rect x="186" y="40" width="124" height="38" rx="10" fill="none" stroke="${GREEN}" stroke-width="2"/>
        </g>
        <g>${проявить('10s',0.6,0.68)}
          ${т(168,206,'похожи по звучанию — разные по значению',12,ИНК)}</g>
      `,216)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='похожи'?'hit':(в?'miss':''), 'Они похожи по звучанию, но значат разное', "r1000Близ('похожи')")}
        ${BTN(4, в==='одно'?'miss':'', 'Это одно и то же слово с двумя формами', "r1000Близ('одно')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='похожи',
        в==='похожи' ? 'Верно: паронимы звучат похоже, но значения у них разные — их легко перепутать.'
                    : 'Нет: «одеть» и «надеть» — два разных слова, а не формы одного.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Паронимы</b> — слова, близкие по звучанию, но разные по значению.');
  }

  /* 2. Одна звуковая волна — два значения */
  function F2(s){
    const в = s.волна;
    return ВОПРОС('Что общего у паронимов и чем они различаются?') +
      `<div class="pic">${свг(`
        ${фон(216)}
        ${т(168,22,'Похоже звучат — разное значат',14,GOLD,true)}
        <g>${ан('opacity','1;0.5;1','4s')}
          <path d="M20 76 Q36 48 52 76 T84 76 T116 76 T148 76 T180 76 T212 76 T244 76 T276 76 T308 76"
            fill="none" stroke="${BLUE}" stroke-width="2.6"/></g>
        ${т(168,50,'звучание',12,МУТ)}
        <rect x="28" y="104" width="128" height="86" rx="12" fill="rgba(255,255,255,.05)" stroke="${BLUE}" stroke-width="1.6">
          ${проявить('10s',0.2,0.28)}</rect>
        ${т(92,132,'одеть',20,BLUE,true)}
        ${т(92,160,'кого-то',14,ИНК)}
        ${т(92,180,'ребёнка, куклу',12,МУТ)}
        <rect x="180" y="104" width="128" height="86" rx="12" fill="rgba(255,255,255,.05)" stroke="${GREEN}" stroke-width="1.6">
          ${проявить('10s',0.45,0.53)}</rect>
        ${т(244,132,'надеть',20,GREEN,true)}
        ${т(244,160,'что-то',14,ИНК)}
        ${т(244,180,'шапку, пальто',12,МУТ)}
      `,216)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='звучание'?'hit':(в?'miss':''), 'Похожее звучание, но разное значение', "r1000Волна('звучание')")}
        ${BTN(4, в==='корень'?'miss':'', 'Одинаковый корень и одно значение', "r1000Волна('корень')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='звучание',
        в==='звучание' ? 'Верно: общее у паронимов — звучание, а различает их значение. Смотри на смысл, а не на звук.'
                      : 'Нет: корни у них могут быть разными, а значение всегда разное — в этом и ловушка.') : ЖДЁТ()) +
      ПРАВИЛО('Паронимы различают <b>по значению</b>: сначала смысл, потом звук.');
  }

  /* 3. Одеть кого-то / надеть что-то: предмет едет */
  function F3(s){
    const в = s.одеть;
    const земля=180;
    return ВОПРОС('Мама надевает куртку на сына. Как сказать правильно?') +
      `<div class="pic">${свг(`
        ${фон(220,земля,true)}
        ${т(168,22,'Предмет едет к своему месту',14,GOLD,true)}
        ${человек(58,земля,92,'#c9a6f2',0.05,'добро')}
        ${человек(132,земля,78,'#5aa3cd',0.12,'добро')}
        ${т(96,208,'мама одевает сына',12,МУТ)}
        <g>${анТ('0 0;0 0;-34 12;-34 12;0 0','10s','keyTimes="0;0.12;0.5;0.86;1"')}
          ${куртка(166,112,32,0.1)}
        </g>
        <g>${проявить('10s',0.55,0.63)}${т(48,66,'кого?',14,GOLD,true)}</g>
        <line x1="76" y1="72" x2="112" y2="86" stroke="${GOLD}" stroke-width="1.6" stroke-dasharray="5 4">
          ${проявить('10s',0.5,0.58)}</line>
        ${человек(272,земля,92,'#6aa34e',0.3,'добро')}
        <g>${анТ('0 0;0 0;0 0;-14 32;-14 32;0 0','10s','keyTimes="0;0.2;0.5;0.68;0.9;1"')}
          ${шапка(264,58,16,0.35)}
        </g>
        ${т(272,208,'мальчик надел шапку',12,МУТ)}
        <g>${проявить('10s',0.72,0.8)}${т(316,104,'что?',14,GREEN,true)}</g>
      `,220)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='одеть'?'hit':(в?'miss':''), 'Мама одевает сына: одеть можно кого-то', "r1000Одеть('одеть')")}
        ${BTN(4, в==='надеть'?'miss':'', 'Мама надевает сына: надеть можно кого угодно', "r1000Одеть('надеть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='одеть',
        в==='одеть' ? 'Верно: одевают кого-то — сына, дочку, куклу. Надевают что-то — куртку, шапку, перчатки.'
                    : 'Нет: надеть сына нельзя, его можно только одеть. Надевают предмет: шапку, куртку.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Одеть кого-то</b>, <b>надеть что-то</b>: одеть сына, надеть шапку.');
  }

  /* 4. Мнемоника */
  function F4(s){
    const в = s.мнемоника;
    return ВОПРОС('Какая подсказка помогает не путать «надеть» и «одеть»?') +
      `<div class="pic">${свг(`
        ${фон(216)}
        ${т(168,22,'Подсказка на всю жизнь',14,GOLD,true)}
        <rect x="26" y="52" width="284" height="52" rx="12" fill="rgba(255,255,255,.05)" stroke="${GOLD}" stroke-width="1.8">
          ${проявить('10s',0.1,0.18)}</rect>
        ${т(148,84,'надеть одежду',24,ИНК,true)}
        <rect x="26" y="116" width="284" height="52" rx="12" fill="rgba(255,255,255,.05)" stroke="${GREEN}" stroke-width="1.8">
          ${проявить('10s',0.3,0.38)}</rect>
        ${т(148,148,'одеть Надежду',24,ИНК,true)}
        <g>${ан('opacity','1;0.6;1','6s','begin="0s"')}${т(278,84,'что?',12,GOLD,true)}</g>
        <g>${ан('opacity','1;0.6;1','6s','begin="3s"')}${т(278,148,'кого?',12,GREEN,true)}</g>
        <g>${проявить('10s',0.55,0.63)}
          ${т(168,196,'одежда — предмет, Надежда — человек',14,ИНК)}</g>
      `,216)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='надетьодежду'?'hit':(в?'miss':''), '«Надеть одежду, одеть Надежду»', "r1000Мнем('надетьодежду')")}
        ${BTN(4, в==='наоборот'?'miss':'', '«Одеть одежду, надеть Надежду»', "r1000Мнем('наоборот')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='надетьодежду',
        в==='надетьодежду' ? 'Верно: предмет надевают, человека одевают. Подсказка держит это в голове без правила.'
                           : 'Нет: получится «одеть одежду» — а одежду как раз надевают.') : ЖДЁТ()) +
      ПРАВИЛО('Запомни: <b>надеть одежду, одеть Надежду</b>.');
  }

  /* 5. Эффектный / эффективный: прожектор и результат */
  function F5(s){
    const в = s.эффект;
    const земля=186;
    return ВОПРОС('Костюм произвёл впечатление, а станок дал результат. Какие слова подходят?') +
      `<div class="pic">${свг(`
        ${фон(224,земля,true)}
        ${т(168,22,'Впечатление и результат',14,GOLD,true)}
        <g>${анТ('0 0;0 0;56 0;56 0;0 0','10s','keyTimes="0;0.1;0.5;0.86;1"')}
          <path d="M52 26 L104 26 L128 176 L28 176 Z" fill="url(#c1000-луч)" opacity=".5"/>
        </g>
        ${человек(78,земля,104,'#c9a6f2',0.05,'добро')}
        ${т(78,64,'эффектный',16,GOLD,true)}
        ${т(78,210,'производит впечатление',12,МУТ)}
        <rect x="196" y="140" width="104" height="46" rx="8" fill="#7f8b96" stroke="${ОБВОД}" stroke-width="1.8">
          ${проявить('10s',0.2,0.28)}</rect>
        <circle cx="222" cy="186" r="9" fill="#4d565e" stroke="${ОБВОД}" stroke-width="1.4"/>
        <circle cx="276" cy="186" r="9" fill="#4d565e" stroke="${ОБВОД}" stroke-width="1.4"/>
        ${Array.from({length:6},(_,i)=>`<rect x="${222+i*13}" y="${120-i*16}" width="12" height="${14+i*16}" rx="2"
            fill="${GREEN}" stroke="${ОБВОД}" stroke-width="1.4">
            ${ан('y',(126-i*11)+';'+(126-i*11)+';'+(126-i*11-26)+';'+(126-i*11-26),'10s','keyTimes="0;'+кт(0.3+i*0.07)+';'+кт(0.48+i*0.07)+';1"')}
          </rect>`).join('')}
        ${т(248,64,'эффективный',16,GREEN,true)}
        ${т(248,210,'даёт результат',12,МУТ)}
      `,224)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='верно'?'hit':(в?'miss':''), 'Костюм эффектный, станок эффективный', "r1000Эфф('верно')")}
        ${BTN(4, в==='наоборот'?'miss':'', 'Костюм эффективный, станок эффектный', "r1000Эфф('наоборот')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='верно',
        в==='верно' ? 'Верно: эффектный — тот, что производит впечатление; эффективный — тот, что даёт хороший результат.'
                    : 'Нет: впечатление производит костюм, а результат даёт станок. Значит, костюм эффектный, станок эффективный.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Эффектный</b> — впечатляющий, <b>эффективный</b> — дающий результат.');
  }

  /* 6. Невежа / невежда */
  function F6(s){
    const в = s.невежа;
    const земля=180;
    return ВОПРОС('Один грубит, другой не знает таблицы умножения. Кто невежа, а кто невежда?') +
      `<div class="pic">${свг(`
        ${фон(218,земля,true)}
        ${т(168,22,'Грубость и незнание',14,GOLD,true)}
        ${человек(72,земля,100,'#e08b6a',0.05,'грубо')}
        <g>${анТ('0 0;0 0;16 0;16 0;0 0','10s','keyTimes="0;0.12;0.4;0.8;1"')}
          <line x1="106" y1="146" x2="132" y2="146" stroke="${ОБВОД}" stroke-width="3.4" stroke-linecap="round"/>
        </g>
        ${т(88,206,'невежа: грубит',12,RED,true)}
        ${человек(272,земля,96,'#6aa34e',0.3,'добро')}
        <g>${анП('-14 272 126;10 272 126;-14 272 126','6s')}
          <rect x="260" y="118" width="24" height="16" rx="2" fill="${БУМАГА}" stroke="${ОБВОД}" stroke-width="1.6"/>
          <line x1="263" y1="124" x2="281" y2="124" stroke="${МУТ}" stroke-width="1.2"/>
          <line x1="263" y1="128" x2="281" y2="128" stroke="${МУТ}" stroke-width="1.2"/>
        </g>
        ${т(268,206,'невежда: не знает',12,BLUE,true)}
        <g>${проявить('10s',0.66,0.74)}
          ${т(84,50,'невежа: грубит',12,RED)}
          ${т(250,50,'невежда: не знает',12,BLUE)}</g>
        ${Array.from({length:3},(_,i)=>`<g>${светит('6s',i*0.5)}
          ${т(96+i*14,120,'!',14,RED,true)}</g>`).join('')}
        ${Array.from({length:3},(_,i)=>`<g>${светит('6s',0.3+i*0.5)}
          ${т(256+i*12,116,'?',14,BLUE,true)}</g>`).join('')}
      `,218)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='грубиян'?'hit':(в?'miss':''), 'Грубиян — невежа, незнающий — невежда', "r1000Нев('грубиян')")}
        ${BTN(4, в==='наоборот'?'miss':'', 'Грубиян — невежда, незнающий — невежа', "r1000Нев('наоборот')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='грубиян',
        в==='грубиян' ? 'Верно: невежа — грубый, невоспитанный; невежда — мало знающий, необразованный.'
                     : 'Нет: «вежа» — от «ведать, знать»? Наоборот: невежа грубит, невежда не знает. Запомни по букве «д» — знание.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Невежа</b> — грубиян, <b>невежда</b> — необразованный человек.');
  }

  /* 7. Адресат / адресант: письмо едет */
  function F7(s){
    const в = s.письмо;
    const земля=176;
    return ВОПРОС('Письмо отправила бабушка, получил внук. Кто здесь адресант?') +
      `<div class="pic">${свг(`
        ${фон(214,земля,true)}
        ${т(168,22,'Письмо едет по адресу',14,GOLD,true)}
        ${человек(58,земля,96,'#c9a6f2',0.05,'добро')}
        ${т(58,202,'отправила',12,МУТ)}
        ${человек(284,земля,96,'#5aa3cd',0.15,'добро')}
        ${т(284,202,'получил',12,МУТ)}
        <path d="M92 128 Q168 74 250 128" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6" stroke-dasharray="6 5">
          ${проявить('10s',0.2,0.3)}</path>
        <g>${анД('M0 0 Q76 -54 158 0','10s')}
          ${конверт(78,118,28,0.2)}
        </g>
        <g>${проявить('10s',0.6,0.7)}
          ${т(96,56,'адресант — отправляет',12,GOLD,true)}
          ${т(250,56,'адресат — получает',12,GREEN,true)}</g>
      `,214)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='бабушка'?'hit':(в?'miss':''), 'Бабушка: адресант — тот, кто отправляет', "r1000Письмо('бабушка')")}
        ${BTN(4, в==='внук'?'miss':'', 'Внук: адресант — тот, кто получает', "r1000Письмо('внук')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='бабушка',
        в==='бабушка' ? 'Верно: адресант отправляет, адресат получает. Отправила бабушка — она и адресант.'
                     : 'Нет: получатель — это адресат. Адресант — тот, кто написал и отправил, то есть бабушка.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Адресант</b> — отправитель, <b>адресат</b> — получатель.');
  }

  /* 8. Дипломат / дипломант */
  function F8(s){
    const в = s.диплом;
    const земля=180;
    return ВОПРОС('Кто из них дипломант: сотрудник посольства или победитель конкурса?') +
      `<div class="pic">${свг(`
        ${фон(218,земля,true)}
        ${т(168,22,'Портфель и диплом',14,GOLD,true)}
        ${человек(76,земля,104,'#3f5b7a',0.05,'добро')}
        <rect x="96" y="152" width="34" height="24" rx="5" fill="#6b4a2a" stroke="${ОБВОД}" stroke-width="1.6">
          ${проявить('10s',0.2,0.28)}</rect>
        ${т(76,206,'дипломат: посольство',12,МУТ)}
        ${человек(268,земля,104,'#5f9b6a',0.3,'добро')}
        <rect x="206" y="132" width="0" height="44" rx="3" fill="${БУМАГА}" stroke="${ОБВОД}" stroke-width="1.6">
          ${ан('width','0;0;62;62','10s','keyTimes="0;0.45;0.72;1"')}
        </rect>
        <rect x="206" y="132" width="0" height="44" rx="3" fill="none" stroke="${GOLD}" stroke-width="1.2">
          ${ан('width','0;0;62;62','10s','keyTimes="0;0.45;0.72;1"')}
        </rect>
        <g>${проявить('10s',0.62,0.7)}${т(264,206,'дипломант: диплом',12,GREEN,true)}</g>
      `,218)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='победитель'?'hit':(в?'miss':''), 'Дипломант — победитель конкурса, награждённый дипломом', "r1000Дип('победитель')")}
        ${BTN(4, в==='посольство'?'miss':'', 'Дипломант — сотрудник посольства', "r1000Дип('посольство')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='победитель',
        в==='победитель' ? 'Верно: дипломант получил диплом. Работник посольства — дипломат, у него дипломатическая служба.'
                        : 'Нет: сотрудник посольства — дипломат. А дипломант — тот, кого наградили дипломом.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Дипломат</b> — работник посольства, <b>дипломант</b> — награждённый дипломом.');
  }

  /* 9. Весы смысла: как выбрать слово */
  function F9(s){
    const в = s.весы;
    return ВОПРОС('Как проверить, какое слово из пары подходит?') +
      `<div class="pic">${свг(`
        ${фон(216)}
        ${т(168,22,'Весы смысла',14,GOLD,true)}
        <line x1="168" y1="112" x2="168" y2="180" stroke="${ОБВОД}" stroke-width="3"/>
        <rect x="140" y="180" width="56" height="8" rx="4" fill="${ОБВОД}"/>
        <g>${анП('-7 168 112;0 168 112;7 168 112;0 168 112;-7 168 112','10s')}
          <line x1="72" y1="112" x2="264" y2="112" stroke="${ОБВОД}" stroke-width="3" stroke-linecap="round"/>
          <circle cx="168" cy="112" r="5" fill="${GOLD}" stroke="${ОБВОД}" stroke-width="1.4"/>
          <path d="M72 112 L60 142 L84 142 Z" fill="none" stroke="${ОБВОД}" stroke-width="1.6"/>
          <path d="M264 112 L252 142 L276 142 Z" fill="none" stroke="${ОБВОД}" stroke-width="1.6"/>
          <g>${анТ('0 0;0 0;0 8;0 8;0 0','10s')}
            <rect x="42" y="142" width="60" height="26" rx="6" fill="${GOLD}" stroke="${ОБВОД}" stroke-width="1.6"/>
            ${т(72,160,'смысл',12,ТУШЬ,true)}</g>
          <g>${анТ('0 0;0 0;0 -8;0 -8;0 0','10s')}
            <rect x="234" y="142" width="60" height="26" rx="6" fill="rgba(255,255,255,.07)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
            ${т(264,160,'звук',12,МУТ)}</g>
        </g>
        <g>${проявить('10s',0.5,0.6)}
          ${т(168,200,'сначала смотрим на значение слова',12,ИНК)}</g>
      `,216)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='смысл'?'hit':(в?'miss':''), 'Подставить значение: кому или что, впечатление или результат', "r1000Весы('смысл')")}
        ${BTN(4, в==='звук'?'miss':'', 'Выбрать то, что привычнее звучит', "r1000Весы('звук')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='смысл',
        в==='смысл' ? 'Верно: проверка одна — подставить смысл. Одеть можно кого-то, надеть — что-то; эффектный — впечатляет, эффективный — работает.'
                    : 'Нет: привычное звучание и есть ловушка паронимов. Проверяем смыслом.') : ЖДЁТ()) +
      ПРАВИЛО('Проверка паронима — <b>подставить значение</b>, а не слушать звучание.');
  }

  /* 10. Собери пары: стрелки рисуются по одной */
  function F10(s){
    const в = s.пары;
    const строки=[['одеть','кого-то'],['надеть','что-то'],['эффектный','впечатление'],
                  ['эффективный','результат'],['адресант','отправляет'],['адресат','получает']];
    return ВОПРОС('Какая пара слов различается значением «кого-то» и «что-то»?') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${т(168,22,'Собери пары',14,GOLD,true)}
        ${строки.map(([сл,зн],i)=>{
          const y=44+i*30;
          return `<g>
            <rect x="18" y="${y}" width="120" height="24" rx="7" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.3">
              ${проявить('10s',0.06*i,0.06*i+0.05)}</rect>
            ${т(78,y+17,сл,14,ИНК,true)}
            <rect x="198" y="${y}" width="120" height="24" rx="7" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.3">
              ${проявить('10s',0.06*i+0.03,0.06*i+0.08)}</rect>
            ${т(258,y+17,зн,14,МУТ,true)}
            <path d="M140 ${y+12} C168 ${y+12} 170 ${y+12} 196 ${y+12}" fill="none" stroke="${GOLD}" stroke-width="1.8" stroke-dasharray="6 4">
              ${проявить('10s',0.05*i+0.2,0.05*i+0.26)}</path>
            <g>${светит('7s',i*0.35)}<circle cx="168" cy="${y+12}" r="3.4" fill="${GOLD}"/></g>
          </g>`;
        }).join('')}
      `,232)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='одетьнадеть'?'hit':(в?'miss':''), '«Одеть» — кого-то, «надеть» — что-то', "r1000Пары('одетьнадеть')")}
        ${BTN(4, в==='адрес'?'miss':'', '«Адресант» — кого-то, «адресат» — что-то', "r1000Пары('адрес')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='одетьнадеть',
        в==='одетьнадеть' ? 'Верно: одевают кого-то, надевают что-то. У остальных пар различие в другом: впечатление и результат, отправка и получение.'
                          : 'Нет: адресант и адресат различаются отправкой и получением, а «кого-то» и «что-то» — это одеть и надеть.') : ЖДЁТ()) +
      ПРАВИЛО('В каждой паре значение своё: <b>кого-то и что-то</b>, впечатление и результат, отправляет и получает.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Бабушка … внука в тёплое пальто.', варианты:[
        {т:'одела', ок:true,  почему:'Одевают кого-то: одела внука.'},
        {т:'надела', ок:false, почему:'Надевают что-то: надела пальто. А внука — одевают.'}]},
    { вопрос:'Он … шапку и вышел на улицу.', варианты:[
        {т:'надел', ок:true,  почему:'Надевают предмет: надел шапку.'},
        {т:'одел', ок:false, почему:'Одеть можно кого-то, а шапку — надеть.'}]},
    { вопрос:'Спектакль был … : зрители долго хлопали.', варианты:[
        {т:'эффектным', ок:true,  почему:'Впечатление — значит эффектный.'},
        {т:'эффективным', ок:false, почему:'Эффективный — дающий результат, а здесь про впечатление.'}]},
    { вопрос:'Этот способ решения самый … : время экономится вдвое.', варианты:[
        {т:'эффективный', ок:true,  почему:'Есть результат — значит эффективный.'},
        {т:'эффектный', ок:false, почему:'Эффектный — про впечатление, а здесь про результат.'}]},
    { вопрос:'Мальчик не читал книг и многого не знает. Кто он?', варианты:[
        {т:'невежда', ок:true,  почему:'Невежда — мало знающий.'},
        {т:'невежа', ок:false, почему:'Невежа — грубый. А здесь про незнание.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Сначала подставляем значение: кого-то или что-то, впечатление или результат, отправляет или получает.') +
        `<div class="ask">${BTN(4,'','пройти заново','r1000Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r1000Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Проверка одна: <b>подставь значение</b> — кого-то или что-то, впечатление или результат.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · одеть и надеть',
    задания:[
      { ф:'Мама … дочку на прогулку.', о:['одела','надела'], в:0, р:'Одевают кого-то.' },
      { ф:'Он … куртку и вышел.', о:['надел','одел'], в:0, р:'Надевают что-то.' },
      { ф:'Бабушка … внука в комбинезон.', о:['одела','надела'], в:0, р:'Одела внука.' },
      { ф:'… , пожалуйста, перчатки: холодно.', о:['надеть','одеть'], в:0, р:'Надеть перчатки — это предмет.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · впечатление и незнание',
    задания:[
      { ф:'Фейерверк был очень … .', о:['эффектным','эффективным'], в:0, р:'Впечатление — эффектный.' },
      { ф:'Упражнение оказалось … : результат сразу.', о:['эффективным','эффектным'], в:0, р:'Результат — эффективный.' },
      { ф:'Он нагрубил бабушке. Кто он?', о:['невежа','невежда'], в:0, р:'Грубость — невежа.' },
      { ф:'Она не знает, сколько будет семь на восемь. Кто она?', о:['невежда','невежа'], в:0, р:'Незнание — невежда.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · адресат и дипломант',
    задания:[
      { ф:'Кто такой адресант?', о:['тот, кто отправляет','тот, кто получает'], в:0, р:'Адресант отправляет.' },
      { ф:'Кто такой адресат?', о:['тот, кто получает','тот, кто отправляет'], в:0, р:'Адресат получает.' },
      { ф:'Кто такой дипломант?', о:['награждённый дипломом','работник посольства'], в:0, р:'Дипломант получил диплом.' },
      { ф:'Кто такой дипломат?', о:['работник посольства','награждённый дипломом'], в:0, р:'Дипломат служит в посольстве.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r1000T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r1000TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L1000={
    id: ID, title:'Паронимы', ico:'👥',
    src:'Русский язык · 5 класс · Лексика', subj:'rus',
    explain: [
      'В языке есть слова, которые звучат почти одинаково, а значат совсем разное. Их называют паронимами: «одеть» и «надеть», «эффектный» и «эффективный», «невежа» и «невежда». Слушаешь — будто одно и то же слово, а смысл разный. Из-за этого в сочинениях и появляются ошибки: слово звучит знакомо, и рука сама его пишет.',

      'Паронимы похожи по звучанию, но различаются значением. Они могут быть родственными («эффектный» и «эффективный») или просто созвучными («адресат» и «адресант»). Различает их всегда одно — смысл. Поэтому проверка у нас будет одна: подставить значение и посмотреть, подходит оно или нет. Частая ошибка — выбирать слово по привычке, «как чаще слышал». Как проверить себя: произнеси фразу целиком и подумай, о чём она.',

      'Самая известная пара — «одеть» и «надеть». Одевают кого-то: мама одевает сына, куклу, малыша. Надевают что-то: шапку, куртку, перчатки, очки. Если сомневаешься, задай вопрос: одеваю кого? надеваю что? Частая ошибка — сказать «надеть сына» или «одеть шапку». Как проверить себя: после слова должен стоять либо человек, либо предмет, и он подскажет выбор.',

      'Для этой пары есть подсказка, которую помнят всю жизнь: «надеть одежду, одеть Надежду». Одежда — предмет, её надевают; Надежда — человек, её одевают. Подсказка работает как рифма, поэтому держится в памяти без правила. Частая ошибка — переставить слова местами и получить «одеть одежду». Как проверить себя: вспомни рифму и подставь свой предмет или имя.',

      'Следующая пара — «эффектный» и «эффективный». Эффектный — тот, который производит впечатление: эффектный костюм, эффектный выход, эффектный фейерверк. Эффективный — тот, который даёт результат: эффективный способ, эффективное лекарство, эффективная работа. Частая ошибка — назвать эффектным то, что хорошо работает. Как проверить себя: спроси, о впечатлении речь или о результате.',

      'Пара «невежа» и «невежда» различается всего одной буквой, а значения далеки. Невежа — грубый, невоспитанный человек: он нагрубил, толкнул, не поздоровался. Невежда — мало знающий, необразованный: он не читал книг и не знает простых вещей. Подсказка: слово «невежда» связано со знанием, в нём есть буква «д», как в слове «знать» — нет, проще запомнить по смыслу. Частая ошибка — назвать грубияна невеждой.',

      'Пара «адресант» и «адресат» пришла из почты. Адресант — тот, кто отправляет письмо, адресат — тот, кому оно адресовано. Запомнить помогает вторая буква: адресАнт — отправляет, адресАт — принимает. Частая ошибка — назвать отправителя адресатом. Как проверить себя: спроси, кто действует — тот, кто пишет, или тот, кто читает.',

      'Ещё одна пара — «дипломат» и «дипломант». Дипломат — работник посольства, у него дипломатическая служба. Дипломант — тот, кого наградили дипломом: победитель конкурса, выпускник. Частая ошибка — назвать победителя конкурса дипломатом. Как проверить себя: есть ли в ситуации посольство и служба — если нет, речь о дипломе.',

      'Как же выбирать слово? Единственный надёжный способ — подставить значение. Представь две чаши весов: на одной смысл, на другой звук. У паронимов звук почти одинаковый, поэтому весы должны качнуться в сторону смысла. Частая ошибка — выбирать «на слух». Как проверить себя: подставь значение и посмотри, не получилась ли бессмыслица.',

      'Пройти практику: пять уровней — одеть и надеть, эффектный и эффективный, невежа и невежда, адресат и адресант. Соберём правило: паронимы близки по звучанию и различны по значению; одеть можно кого-то, надеть — что-то; эффектный производит впечатление, эффективный даёт результат; невежа грубит, невежда не знает; адресант отправляет, адресат получает; дипломат служит, дипломант награждён.',

      'Разберём каждую пару отдельно и потренируемся на трёх тренажёрах: сначала «одеть» и «надеть», потом впечатление и незнание, затем почта и диплом.',

      'Первый тренажёр — про пару «одеть» и «надеть». В каждом предложении есть подсказка: если дальше стоит человек — одеваем, если предмет — надеваем.',

      'Второй тренажёр — про впечатление и незнание: «эффектный» или «эффективный», «невежа» или «невежда». Здесь важно не спешить и каждый раз задавать себе вопрос о смысле.',

      'Третий тренажёр — про почту и диплом: адресант и адресат, дипломат и дипломант. Проверяй себя по действию: кто отправляет, кто получает, кто служит, кто награждён.'
    ],
    check: {
      q:'Мама … сына в тёплую куртку.',
      choices:['одела','надела'],
      ans:0,
      exp:'Одевают кого-то: мама одела сына. Куртку при этом надевают.'
    },
    tasks: [
      { q:'Бабушка … внука в тёплое пальто.', kind:'choice',
        choices:['одела','надела'], ans:0,
        hints:['Одевают кого-то.','Дальше стоит человек — внук.'],
        sol:'Одела внука.' },
      { q:'Он … шапку и вышел на улицу.', kind:'choice',
        choices:['надел','одел'], ans:0,
        hints:['Надевают что-то.','Шапка — предмет.'],
        sol:'Надел шапку.' },
      { q:'Спектакль был … : зрители долго хлопали.', kind:'choice',
        choices:['эффектным','эффективным'], ans:0,
        hints:['Речь о впечатлении.','Впечатление — эффектный.'],
        sol:'Эффектным.' },
      { q:'Мальчик не читал книг и многого не знает. Кто он?', kind:'choice',
        choices:['невежда','невежа'], ans:0,
        hints:['Дело не в грубости, а в знании.','Незнание — невежда.'],
        sol:'Невежда.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L1000.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Слова-близнецы','Похожи, а значат разное'],
      2:['Что такое паронимы','Звук и значение'],
      3:['Одеть и надеть','Кого-то или что-то'],
      4:['Подсказка','Надеть одежду, одеть Надежду'],
      5:['Эффектный','Впечатление и результат'],
      6:['Невежа и невежда','Грубость и незнание'],
      7:['Адресат','Кто отправляет и кто получает'],
      8:['Дипломат','Посольство и диплом'],
      9:['Весы смысла','Как выбрать слово'],
      10:['Пары','Слово и значение'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','Одеть и надеть'],
      13:['Тренажёр 2','Впечатление и незнание'],
      14:['Тренажёр 3','Адресат и дипломант']
    };
    const з=ЗАГОЛОВКИ[f]||['Паронимы','Слова, похожие по звучанию'];
    el.innerHTML = `<div class="s6 l1000" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r1000Близ=(к)=>{ S().близнецы=к; chRender(0); };
  window.r1000Волна=(к)=>{ S().волна=к; chRender(0); };
  window.r1000Одеть=(к)=>{ S().одеть=к; chRender(0); };
  window.r1000Мнем=(к)=>{ S().мнемоника=к; chRender(0); };
  window.r1000Эфф=(к)=>{ S().эффект=к; chRender(0); };
  window.r1000Нев=(к)=>{ S().невежа=к; chRender(0); };
  window.r1000Письмо=(к)=>{ S().письмо=к; chRender(0); };
  window.r1000Дип=(к)=>{ S().диплом=к; chRender(0); };
  window.r1000Весы=(к)=>{ S().весы=к; chRender(0); };
  window.r1000Пары=(к)=>{ S().пары=к; chRender(0); };
  window.r1000Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r1000Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r1000T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r1000TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L1000; else arr.push(L1000);
  })();
  window.MA1000={render:render, L:L1000};
})();
