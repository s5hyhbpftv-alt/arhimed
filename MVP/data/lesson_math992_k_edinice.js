/* ====== МАТЕМАТИКА · УРОК 992 · «ПРИВЕДЕНИЕ К ЕДИНИЦЕ» ====================
   4 класс. Тема была заглушкой «готовится» в MVP/data/soon_lessons.js.

   ПОЧЕМУ СТУПЕНЬКА. Из «за 5 дней 60 мер» в «за 8 дней сколько?» нельзя
   перепрыгнуть — между ними обязательно лежит ОДИН день. В этом весь приём, и
   ребёнок должен увидеть, что единица — не формальность, а ступенька, на
   которую надо спуститься, чтобы потом подняться выше. Поэтому центральный
   кадр урока — лесенка из трёх столбиков настоящей высоты: 60, потом низкий
   12, потом 96. Вниз идёт стрелка «: 5», вверх — «× 8».

   Мера воды здесь не значок, а мерный сосуд с двенадцатью делениями: один
   день — один сосуд. В песочнице ребёнок сам выставляет число дней и видит,
   как ряд сосудов растёт, а цена одного дня не меняется.

   СЮЖЕТ ПОД МИР 4 КЛАССА В «ПУТИ» («Открытое море»): штурманский расчёт
   припасов на последний переход до Сиракуз. Лист штурмана висит над каждым
   кадром: три расчёта с отметками и запас в трюме.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     вода:   60 : 5 = 12; 12 × 8 = 96; проверка 96 : 8 = 12 ✓
             в трюме 100, нужно 96, останется 4 ✓
     сухари: 18 : 6 = 3;  3 × 9 = 27 ✓
     двойное: 72 : (6 × 4) = 3; 3 × (9 × 5) = 3 × 45 = 135 ✓
     ремень: 28 : 4 = 7;  7 × 7 = 49 ✓
     кратный случай: 10 дней вдвое больше пяти, 60 × 2 = 120 = 12 × 10 ✓
     ловушка: «на 3 дня больше — значит 63» ✗, день стоит 12, а не 1

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md: одно главное движение на кадр и одна
   вспышка результата. prefers-reduced-motion выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 992;
  const GOLD='#ffd76a', GREEN='#8fd1a8', RED='#e86a5a', СИНЬ='#9db7ff',
        ВОДА='#7fd1ff', ДЕРЕВО='#a97c40', ХЛЕБ='#d9a85e';
  const ИНК='#f4f6ff', МУТ='#b9c3df', ЛИНИЯ='#4a5c8f', ОБВОД='#141a2b';

  /* ---------- ЛИСТ ШТУРМАНА: ход квеста ---------- */
  const В_ТРЮМЕ = 100, НУЖНО = 96;
  const РАСЧЁТЫ = [
    {ключ:'вода',   имя:'Вода на 8 дней',      итог:'96 мер'},
    {ключ:'сухари', имя:'Сухари на 9 человек', итог:'27 штук'},
    {ключ:'ремень', имя:'Ремень на 7 вёсел',   итог:'49 локтей'}
  ];
  const сосчитан = (s,к) => !!s['расч_'+к];

  const CSS=`
  #lvis .s6.l992{gap:14px}
  #lvis .s6.l992 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l992 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l992 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#1f2740,#151a2c);border:1.5px solid var(--line)}
  #lvis .s6.l992 .карт.задача{border-color:${СИНЬ}}
  #lvis .s6.l992 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l992 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l992 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l992 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l992 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${СИНЬ};
    background:linear-gradient(180deg,rgba(157,183,255,.14),rgba(157,183,255,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l992 .правило b{color:${СИНЬ}}

  #lvis .s6.l992 .лист{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(157,183,255,.10),rgba(157,183,255,.03));
    border:1.5px solid rgba(157,183,255,.4);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l992 .лист .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l992 .лист .шапка b{font-size:24px;letter-spacing:0;text-transform:none;color:${СИНЬ};
    font-variant-numeric:tabular-nums}
  #lvis .s6.l992 .лист .шапка b.хватит{color:${GREEN}}
  #lvis .s6.l992 .лист ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l992 .лист li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l992 .лист li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;
    border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l992 .лист li.есть{color:${ИНК}}
  #lvis .s6.l992 .лист li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,209,168,.14)}
  #lvis .s6.l992 .лист li span{margin-left:auto;font-variant-numeric:tabular-nums;color:var(--mut);font-size:16px}
  #lvis .s6.l992 .лист li.есть span{color:${GREEN}}

  #lvis .s6.l992 .рычаг{display:flex;align-items:center;gap:10px;width:100%;padding:12px;border-radius:16px;
    background:rgba(127,209,255,.08);border:1.5px solid rgba(127,209,255,.34)}
  #lvis .s6.l992 .рычаг .сколько{flex:1;min-width:0;text-align:center;font-size:16px;line-height:1.25;color:${ИНК}}
  #lvis .s6.l992 .рычаг .сколько b{display:block;font-size:32px;color:${ВОДА};font-variant-numeric:tabular-nums}
  #lvis .s6.l992 .рычаг button{width:56px;height:56px;flex:0 0 56px;border-radius:14px;cursor:pointer;
    border:1px solid rgba(127,209,255,.5);background:rgba(127,209,255,.14);color:${ВОДА};
    font:inherit;font-size:24px;line-height:1;touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l992 .рычаг button:active{transform:translateY(2px);background:rgba(127,209,255,.26)}
  #lvis .s6.l992 .рычаг button:disabled{opacity:.35;cursor:not-allowed}

  #lvis .s6.l992 .дело{width:100%;min-height:60px;padding:16px;border-radius:16px;cursor:pointer;
    font:inherit;font-size:20px;line-height:1.25;text-align:center;color:#12172a;
    border:0;background:linear-gradient(180deg,${СИНЬ},#5b74c4);
    box-shadow:0 3px 0 rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.4);
    transition:transform 120ms ease, box-shadow 180ms ease}
  #lvis .s6.l992 .дело:active{transform:translateY(2px);box-shadow:0 1px 0 rgba(0,0,0,.42)}
  #lvis .s6.l992 .дело.сделано{background:rgba(143,209,168,.16);color:${GREEN};
    box-shadow:none;border:1.5px solid ${GREEN}}
  #lvis .s6.l992 .дело:disabled{background:rgba(255,255,255,.07);color:var(--mut);
    box-shadow:none;border:1.5px solid rgba(255,255,255,.16);cursor:not-allowed}

  #lvis .s6.l992 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l992 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l992 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l992 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l992 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l992 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center}

  #lvis .s6.l992 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l992 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l992 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l992 .уровни .точка.сейчас{background:${СИНЬ};animation:l992dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l992dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l992 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l992{-webkit-text-size-adjust:100%}
  #lvis .s6.l992 [data-anim]{animation:l992rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l992rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l992 [data-anim]{animation:none!important}
    #lvis .s6.l992 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l992 .ask button,#lvis .s6.l992 .рычаг button,#lvis .s6.l992 .дело{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l992-style');
      if(!s){ s=document.createElement('style'); s.id='l992-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Расчёт</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ДЕЛО = (текст,on,сделано,можно) =>
    `<button type="button" data-anim style="--i:3" class="дело${сделано?' сделано':''}"
       ${можно===false&&!сделано?'disabled':''} onclick="${on}">${текст}</button>`;

  const ЛИСТ = (s) => {
    const вода = сосчитан(s,'вода');
    return A(0,'лист',
      `<div class="шапка"><span>${вода?'Запас':'В трюме'}</span><b class="${вода?'хватит':''}">${
        вода ? ('нужно '+НУЖНО+' · останется '+(В_ТРЮМЕ-НУЖНО)) : (В_ТРЮМЕ+' мер')}</b></div>
       <ul>${РАСЧЁТЫ.map(р=>{
         const есть = сосчитан(s,р.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${р.имя}
           <span>${есть?р.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анРаз = (имя,значения,длит,задержка) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" fill="freeze"
              ${задержка?'begin="'+задержка+'"':''}
              calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';
  const анТ = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}"
              repeatCount="indefinite" calcMode="spline" keyTimes="${keyTimes}"
              keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const очередь = (номер,всего,длит) => {
    if(!ДВИЖ) return '';
    const д = 1/(всего+1), a = кт(номер*д), b = кт(номер*д+д*0.5), c = кт(номер*д+д*0.9);
    return анК('opacity','0.6;0.6;1;1;0.6',длит,'0;'+a+';'+b+';'+c+';1');
  };
  const рамкаВсп = (x0,y0,x1,y1,цвет,длит,доля) => ДВИЖ ? `<rect x="${x0}" y="${y0}"
      width="${x1-x0}" height="${y1-y0}" rx="10" fill="none" stroke="${цвет||СИНЬ}"
      stroke-width="2.2" opacity="0">
      ${анК('opacity','0;0;0.95;0',длит,'0;'+кт(доля)+';'+кт(доля+0.1)+';1')}
    </rect>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c992-фон" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#28314e"/><stop offset="0.55" stop-color="#1b2238"/>
        <stop offset="1" stop-color="#101423"/>
      </linearGradient>
      <linearGradient id="c992-небо" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#3a4770"/><stop offset="1" stop-color="#6d7fa8"/>
      </linearGradient>
      <linearGradient id="c992-море" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#7fb3d1"/><stop offset="1" stop-color="#1e3f63"/>
      </linearGradient>
      <linearGradient id="c992-вода" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c7ecff"/><stop offset="0.35" stop-color="#7fd1ff"/>
        <stop offset="1" stop-color="#2b6d99"/>
      </linearGradient>
      <linearGradient id="c992-стекло" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="rgba(255,255,255,.22)"/><stop offset="0.35" stop-color="rgba(255,255,255,.05)"/>
        <stop offset="1" stop-color="rgba(0,0,0,.25)"/>
      </linearGradient>
      <linearGradient id="c992-дуб" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c79a5a"/><stop offset="0.6" stop-color="#a97c40"/>
        <stop offset="1" stop-color="#6d4a22"/>
      </linearGradient>
      <linearGradient id="c992-карта" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#efe3c4"/><stop offset="1" stop-color="#cdbc93"/>
      </linearGradient>
      <linearGradient id="c992-хлеб" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#e8c283"/><stop offset="1" stop-color="#b07f3e"/>
      </linearGradient>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||246}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c992-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+16, в=к+10;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g>
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,13,24,.88)" stroke="${цвет||СИНЬ}" stroke-width="1.2"/>
      ${т(cx,y-2,текст,к,цвет||СИНЬ,true)}
    </g>`;
  };

  /* ---------------- МЕРНЫЙ СОСУД --------------------------------------------
     Единица урока. Двенадцать делений — двенадцать мер, один сосуд — один день.
     Делений ровно столько, сколько мер, поэтому их можно пересчитать. */
  const ДЕЛЕНИЙ = 12;
  const мерка = (x,низ,высота,ширина,залито,цвет,обвод) => {
    const В = высота||56, Ш = ширина||20, з = Math.max(0,Math.min(ДЕЛЕНИЙ,залито==null?ДЕЛЕНИЙ:залито));
    const верх = низ-В, hВоды = В*з/ДЕЛЕНИЙ;
    return `<g>
      <rect x="${x-Ш/2}" y="${верх}" width="${Ш}" height="${В}" rx="3"
        fill="rgba(10,20,34,.75)" stroke="${ОБВОД}" stroke-width="1.2"/>
      ${з>0?`<rect x="${x-Ш/2+1.4}" y="${(низ-hВоды).toFixed(1)}" width="${Ш-2.8}" height="${hВоды.toFixed(1)}"
        rx="2" fill="${цвет||'url(#c992-вода)'}"/>`:''}
      ${Array.from({length:ДЕЛЕНИЙ-1},(_,i)=>{
        const крупное = (i+1)%3===0;
        return `<line x1="${x-Ш/2+1.4}" y1="${(верх+В*(i+1)/ДЕЛЕНИЙ).toFixed(1)}"
        x2="${x+Ш/2-1.4}" y2="${(верх+В*(i+1)/ДЕЛЕНИЙ).toFixed(1)}"
        stroke="rgba(14,24,42,${крупное?0.8:0.45})" stroke-width="${крупное?1.2:0.8}"/>`;}).join('')}
      <rect x="${x-Ш/2}" y="${верх}" width="${Ш}" height="${В}" rx="3" fill="url(#c992-стекло)"/>
      <rect x="${x-Ш/2}" y="${верх}" width="${Ш}" height="${В}" rx="3" fill="none"
        stroke="${обвод||ОБВОД}" stroke-width="${обвод?1.8:1.2}"/>
      <rect x="${x-Ш/2-2}" y="${верх-4}" width="${Ш+4}" height="5" rx="2"
        fill="${ДЕРЕВО}" stroke="${ОБВОД}" stroke-width="1"/>
    </g>`;
  };

  /* ---------------- СТОЛБИК ВЫСОТЫ ------------------------------------------
     Лесенка через единицу: три столбика настоящих пропорций (60, 12, 96). */
  const столбик = (x,низ,высотаПикс,ширина,цвет,залив) => `<g>
    <rect x="${x-ширина/2}" y="${(низ-высотаПикс).toFixed(1)}" width="${ширина}" height="${высотаПикс.toFixed(1)}"
      rx="4" fill="${залив||'rgba(157,183,255,.18)'}" stroke="${цвет||СИНЬ}" stroke-width="1.6"/>
  </g>`;

  /* стрелка между ступеньками */
  const стрелка = (x0,y0,x1,y1,цвет,текст) => {
    const dx=x1-x0, dy=y1-y0, д=Math.sqrt(dx*dx+dy*dy)||1;
    const ux=dx/д, uy=dy/д, nx=-uy, ny=ux;
    const кx=x1-ux*9, кy=y1-uy*9;
    return `<g>
      <path d="M${x0} ${y0} L${кx.toFixed(1)} ${кy.toFixed(1)}" fill="none" stroke="${цвет||GOLD}"
        stroke-width="2" stroke-linecap="round"/>
      <path d="M${x1} ${y1} L${(кx+nx*5).toFixed(1)} ${(кy+ny*5).toFixed(1)}
               L${(кx-nx*5).toFixed(1)} ${(кy-ny*5).toFixed(1)} z" fill="${цвет||GOLD}"/>
      ${текст?подпись((x0+x1)/2,(y0+y1)/2+5,текст,цвет||GOLD,16):''}
    </g>`;
  };

  /* ---------------- МОРЯК И СУХАРЬ ------------------------------------------ */
  const моряк = (x,низ,м) => {
    const мм=м||1;
    return `<g transform="translate(${x} ${низ}) scale(${мм})">
      <path d="M-9 0 q0 -17 9 -17 q9 0 9 17 z" fill="#5b74c4" stroke="${ОБВОД}" stroke-width="${(1.2/мм).toFixed(2)}"/>
      <circle cx="0" cy="-23" r="6.4" fill="#e4c096" stroke="${ОБВОД}" stroke-width="${(1.2/мм).toFixed(2)}"/>
      <path d="M-6.6 -25.5 q6.6 -4.6 13.2 0 l0 -1.6 q-6.6 -3.6 -13.2 0 z" fill="#f2f4ff"/>
    </g>`;
  };
  const сухарь = (x,y,r) => `<g>
    <circle cx="${x}" cy="${y}" r="${r}" fill="url(#c992-хлеб)" stroke="#7a5424" stroke-width="0.8"/>
    ${[[-0.4,-0.3],[0.4,-0.2],[0,0.4]].map(п=>`<circle cx="${(x+п[0]*r).toFixed(1)}" cy="${(y+п[1]*r).toFixed(1)}"
      r="${(r*0.15).toFixed(1)}" fill="rgba(90,60,20,.45)"/>`).join('')}
  </g>`;

  /* ---------------- СЕТКА ПОРЦИЙ --------------------------------------------
     Двойное приведение: столбцы — люди, строки — дни, клетка — одна порция. */
  const сетка = (x0,y0,кол,ряд,шx,шy,цвет,залив) => `<g>
    ${Array.from({length:кол*ряд},(_,i)=>{
      const c=i%кол, r=Math.floor(i/кол);
      return `<rect x="${(x0+c*шx).toFixed(1)}" y="${(y0+r*шy).toFixed(1)}" width="${(шx-1.6).toFixed(1)}"
        height="${(шy-1.6).toFixed(1)}" rx="1.6" fill="${залив||'rgba(217,168,94,.3)'}"
        stroke="${цвет||ХЛЕБ}" stroke-width="0.8"/>`;
    }).join('')}
  </g>`;

  /* ---------------- ПОЛОСА С ДЕЛЕНИЯМИ --------------------------------------
     Ремень: длина делится на равные куски, по куску на весло. */
  const полоса = (x,y,ед,всего,кусок,цвет) => {
    const В=24, частей=Math.round(всего/кусок);
    return `<g>
      <rect x="${x}" y="${y}" width="${(всего*ед).toFixed(1)}" height="${В}" rx="3"
        fill="rgba(169,124,64,.5)" stroke="${ОБВОД}" stroke-width="1.2"/>
      ${Array.from({length:частей-1},(_,i)=>`<line x1="${(x+(i+1)*кусок*ед).toFixed(1)}" y1="${y}"
        x2="${(x+(i+1)*кусок*ед).toFixed(1)}" y2="${y+В}" stroke="${цвет||GOLD}" stroke-width="1.8"
        stroke-dasharray="4 3"/>`).join('')}
      ${Array.from({length:частей},(_,i)=>т((x+(i+0.5)*кусок*ед),y+17,String(кусок),14,'#f2e6cc',true)).join('')}
    </g>`;
  };

  const судно = (x,y,м) => {
    const мм=м||1;
    return `<g transform="translate(${x} ${y}) scale(${мм})">
      <path d="M-32 0 q5 14 32 14 q27 0 32 -14 z" fill="url(#c992-дуб)" stroke="${ОБВОД}" stroke-width="${(1.3/мм).toFixed(2)}"/>
      <rect x="-33" y="-5" width="66" height="5" rx="2" fill="#8a5f30" stroke="${ОБВОД}" stroke-width="${(1/мм).toFixed(2)}"/>
      <line x1="0" y1="-5" x2="0" y2="-48" stroke="${ОБВОД}" stroke-width="${(2.2/мм).toFixed(2)}"/>
      <path d="M3 -46 q20 16 0 31 z" fill="#f4f0e2" stroke="${ОБВОД}" stroke-width="${(1/мм).toFixed(2)}"/>
      <path d="M-3 -43 q-15 13 0 25 z" fill="#dfd8c4" stroke="${ОБВОД}" stroke-width="${(1/мм).toFixed(2)}"/>
    </g>`;
  };

  /* ================= КАДРЫ ================= */

  /* 1. Лист штурмана: постановка */
  function F1(s){
    const ход = ДВИЖ ? `<g>
      ${анТ('0 0;0 0;154 -36;154 -36;0 0','9s','0;0.08;0.55;0.86;1')}
      <circle cx="146" cy="140" r="5" fill="${GOLD}"/>
      <circle cx="146" cy="140" r="9" fill="none" stroke="${GOLD}" stroke-width="1.2" opacity=".5"/>
    </g>` : `<circle cx="146" cy="140" r="5" fill="${GOLD}"/>`;
    return ЛИСТ(s) +
      ЗАДАЧА('До Сиракуз ещё <b>8 дней</b> хода. Позади <b>5 дней</b>, и за них команда извела <b>60 мер</b> воды. В трюме осталось 100 мер. Хватит ли?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="80" fill="url(#c992-небо)"/>
        <rect x="0" y="80" width="336" height="90" fill="url(#c992-море)"/>
        <rect x="0" y="170" width="336" height="78" fill="url(#c992-фон)"/>
        <path d="M264 92 q14 -20 30 -4 q12 -10 22 4 z" fill="#3c4a3a" stroke="${ОБВОД}" stroke-width="1"/>
        ${судно(94,128,1.3)}
        <path d="M146 140 L300 104" fill="none" stroke="${GOLD}" stroke-width="1.6"
          stroke-dasharray="6 5" opacity=".7"/>
        ${ход}
        <rect x="0" y="0" width="336" height="248" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${подпись(168,26,'Лист штурмана',СИНЬ,16)}
        ${подпись(168,196,'позади 5 дней · ушло 60 мер',GOLD,14)}
        <g>${проявить('9s',0.5,0.62)}${подпись(168,228,'впереди 8 дней · в трюме 100 мер',ВОДА,14)}</g>
      `,248)}</div>` +
      СКАЗ('Загвоздка','Из «за 5 дней — 60 мер» прямо в «за 8 дней» не перепрыгнуть: 8 не делится на 5 без остатка. Между ними обязательно лежит <b>один день</b>.') +
      ПРАВИЛО('Сначала узнаём про <b>одного</b>, потом про <b>всех</b>. В этом весь приём.');
  }

  /* 2. Песочница: один день — одна мерка */
  function F2(s){
    const н = Math.max(1, Math.min(10, s.дней==null?5:s.дней));
    const x0 = 168 - (н-1)*14;
    return ЛИСТ(s) +
      ЗАДАЧА('Мера воды — не значок, а мерный сосуд с двенадцатью делениями. <b>Один день — один сосуд.</b> Выставь число дней и смотри, что делается с запасом.') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${подпись(168,26,'Один день — одна мерка',СИНЬ,14)}
        ${подпись(168,62,'в каждой 12 мер',ВОДА,14)}
        ${Array.from({length:н},(_,i)=>`<g>${очередь(i,н,'7s')}
          ${мерка(x0+i*28,176,56,20,12)}
          ${т(x0+i*28,196,String(i+1),14,МУТ)}
        </g>`).join('')}
        ${н===1?рамкаВсп(150,112,186,182,GOLD,'6s',0.16):''}
        ${подпись(168,230, н===1?'вот она — единица: 12 мер':('12 × '+н+' = '+(12*н)+' мер'),
          н===1?GOLD:GOLD, 20)}
      `,252)}</div>` +
      A(3,'рычаг',
        `<button type="button" onclick="r992Дней(-1)" ${н>1?'':'disabled'} aria-label="убрать день">−</button>
         <span class="сколько">дней пути<b>${н}</b></span>
         <button type="button" onclick="r992Дней(1)" ${н<10?'':'disabled'} aria-label="добавить день">+</button>`) +
      СКАЗ('Смотри', н===1
        ? 'Один сосуд — <b>12 мер</b>. Это и есть та самая единица, через которую считают всё остальное.'
        : 'Сосудов '+н+', и в каждом ровно 12. Сколько бы дней ни было, <b>цена одного дня не меняется</b> — меняется только их число.') +
      ПРАВИЛО('<b>Одна мерка — один день.</b> Зная её, узнаёшь любое число дней.');
  }

  /* 3. Ступенька через единицу. Главный кадр урока. */
  function F3(s){
    const в = s.о1, верно = в==='ok';
    const М = 0.95, база = 196;
    return ЛИСТ(s) +
      ЗАДАЧА('Вот вся суть. Спускаемся с пяти дней <b>на один</b>, а оттуда поднимаемся на восемь. Через единицу — как по ступеньке.') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${подпись(168,26,'Вниз к одному, вверх ко многим',СИНЬ,14)}
        <line x1="30" y1="${база}" x2="306" y2="${база}" stroke="rgba(255,255,255,.2)" stroke-width="1.4"/>
        ${столбик(66,база,60*М,46,GOLD,'rgba(255,215,106,.2)')}
        ${столбик(168,база,12*М,46,ВОДА,'rgba(127,209,255,.28)')}
        ${столбик(270,база,96*М,46, верно?GREEN:МУТ, верно?'rgba(143,209,168,.22)':'rgba(255,255,255,.07)')}
        ${т(66,131,'60',20,GOLD,true)}
        ${т(168,177,'12',20,ВОДА,true)}
        ${т(270,97,верно?'96':'?',20,верно?GREEN:МУТ,true)}
        <g>${очередь(0,2,'7s')}${стрелка(89,140,145,182,GOLD,': 5')}</g>
        <g>${очередь(1,2,'7s')}${стрелка(191,182,247,107,верно?GREEN:СИНЬ,'× 8')}</g>
        ${т(66,216,'5 дней',14,МУТ)}
        ${т(168,216,'1 день',14,ВОДА)}
        ${т(270,216,'8 дней',14,МУТ)}
        ${верно?рамкаВсп(243,100,297,200,GREEN,'7s',0.2):''}
        ${подпись(168,246, верно?'60 : 5 = 12 · 12 × 8 = 96':'сначала вниз, потом вверх',
          верно?GREEN:МУТ, 14)}
      `,256)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), '96 мер — двенадцать раз по восемь', "r992О1('ok')")}
        ${BTN(5, в==='no'?'miss':'', '63 меры — на три дня больше', "r992О1('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Спуск: 60 : 5 = 12 — столько уходит за один день. Подъём: 12 × 8 = 96 — столько уйдёт за восемь. Низкий столбик посередине и есть та ступенька.'
              : 'Нет: три лишних дня стоят не три меры, а три раза по двенадцать. Сначала спустись к одному: 60 : 5 = 12, потом поднимись: 12 × 8 = 96.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('<b>Делим на количество — умножаем на новое количество.</b>');
  }

  /* 4. Ловушка: прибавить дни вместо того, чтобы умножить */
  function F4(s){
    const в = s.о2, верно = в==='ok';
    const x0 = 91;
    return ЛИСТ(s) +
      ЗАДАЧА('Самая частая ошибка в этой теме: «дней стало на три больше — значит, и мер на три больше, 63». Почему так нельзя?') +
      `<div class="pic">${свг(`
        ${фон(262)}
        ${подпись(168,26,'Ловушка',СИНЬ,16)}
        ${подпись(50,64,'так неверно',RED,14)}
        ${Array.from({length:8},(_,i)=>`<g>${проявить('6s',i<5?0.3:0.5,i<5?0.42:0.62)}
          ${мерка(x0+i*22,116,40,16, i<5?12:1, i<5?'url(#c992-вода)':RED, i<5?null:RED)}
          ${i>=5?т(x0+i*22,108,'1',14,RED,true):''}</g>`).join('')}
        ${подпись(168,142,'5 полных и 3 по одной — 63',RED,14)}
        ${подпись(50,170,'так верно',GREEN,14)}
        ${Array.from({length:8},(_,i)=>`<g>${очередь(i,8,'7s')}
          ${мерка(x0+i*22,220,40,16,12)}</g>`).join('')}
        ${верно?рамкаВсп(80,176,256,226,GREEN,'7s',0.2):''}
        ${подпись(168,252,'8 дней по 12 — это 96',GREEN,14)}
      `,262)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), 'Каждый новый день стоит 12 мер, а не одну', "r992О2('ok')")}
        ${BTN(5, в==='no'?'miss':'', 'Дней на самом деле не 8, а 9', "r992О2('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Наверху видно сразу: у трёх последних сосудов налито по капле. А день — это целый сосуд, двенадцать мер. Три дня стоят 3 × 12 = 36, и 60 + 36 = 96.'
              : 'Нет, дней ровно восемь. Беда в другом: прибавили по одной мере за день вместо двенадцати. День — это целый сосуд.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Прибавлять можно <b>меры</b>, а не дни. День сам по себе не мера.');
  }

  /* 5. Проверка обратным действием */
  function F5(s){
    const разл = !!s.разложено;
    const x0 = 77;
    return ЛИСТ(s) +
      ЗАДАЧА('Ответ 96 надо проверить. Проверка здесь простая: разложим 96 мер обратно на 8 дней и посмотрим, выйдет ли по 12.') +
      `<div class="pic">${свг(`
        ${фон(236)}
        ${подпись(168,26,'Проверка обратным действием',СИНЬ,14)}
        ${подпись(168,66,'96 мер · разложим на 8 дней',GOLD,14)}
        ${Array.from({length:8},(_,i)=>`<g>${разл?'':очередь(i,8,'7s')}
          ${мерка(x0+i*26,150,52,18, разл?12:0)}</g>`).join('')}
        ${разл?рамкаВсп(64,94,272,156,GREEN,'6s',0.16):''}
        ${разл?подпись(168,192,'96 : 8 = 12 ✓',GREEN,20)
              :подпись(168,192,'по скольку выйдет?',МУТ,20)}
        ${т(168,220, разл?'столько же, сколько было — расчёт верен':'нажми кнопку под рисунком',14,МУТ)}
      `,236)}</div>` +
      ДЕЛО(разл ? '✓ Разложено: по 12 мер на день' : 'Разложить 96 обратно на 8 дней', "r992Разложить()", разл, true) +
      СКАЗ('Зачем', разл
        ? 'Сошлось: 96 : 8 = 12 — ровно та же единица, с которой начинали. Если бы вышло другое число, значит, где-то ошибка.'
        : 'Обратное действие — самая быстрая проверка. Умножал — подели, делил — умножь.') +
      ПРАВИЛО('Проверка через <b>обратное действие</b>: умножал — подели обратно.');
  }

  /* 6. Второй расчёт: сухари на человека */
  function F6(s){
    const в = s.о3, верно = в==='ok';
    const x0 = 58;
    return ЛИСТ(s) +
      ЗАДАЧА('Второй расчёт. Сейчас на судне <b>6 человек</b>, и в день они съедают <b>18 сухарей</b>. В Сиракузах на борт возьмут ещё троих — станет девять. Сколько сухарей понадобится в день?') +
      `<div class="pic">${свг(`
        ${фон(254)}
        ${подпись(168,26,'Сухари на день',СИНЬ,16)}
        ${подпись(168,62,'18 сухарей на 6 человек',GOLD,14)}
        ${Array.from({length:6},(_,i)=>`<g>${очередь(i,6,'7s')}
          ${сухарь(x0+i*44-8,110,5)}${сухарь(x0+i*44+8,110,5)}${сухарь(x0+i*44,98,5)}
          ${моряк(x0+i*44,152,1)}</g>`).join('')}
        ${подпись(168,186, верно?'18 : 6 = 3 — одному':'на одного — сколько?', верно?ВОДА:МУТ, 14)}
        ${верно?Array.from({length:9},(_,i)=>`<g>${анРаз('opacity','0.55;1','380ms',(0.06*i).toFixed(2)+'s')}
          <rect x="${48+i*30-12}" y="202" width="24" height="20" rx="4"
            fill="rgba(217,168,94,.22)" stroke="${ХЛЕБ}" stroke-width="1.2"/>
          ${т(48+i*30,217,'3',14,ХЛЕБ,true)}</g>`).join('')
          :т(168,214,'а на девятерых?',14,МУТ)}
        ${подпись(168,244, верно?'3 × 9 = 27 сухарей':'сначала — на одного', верно?GREEN:МУТ, 14)}
      `,254)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '27 сухарей', "r992О3('ok')")}
        ${BTN(5, в==='no'?'miss':'', '21 сухарь', "r992О3('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Спуск: 18 : 6 = 3 — столько съедает один. Подъём: 3 × 9 = 27. Ступенька та же, груз другой.'
              : 'Нет: 21 — это 18 да ещё три, по сухарю на каждого нового. Но каждый ест по три: 18 : 6 = 3, и 3 × 9 = 27.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Единица бывает не только «на день», но и <b>«на одного»</b>.');
  }

  /* 7. Двойное приведение: и на одного, и на день */
  function F7(s){
    const в = s.о4, верно = в==='ok';
    return ЛИСТ(s) +
      ЗАДАЧА('А теперь сразу две величины. <b>6 человек за 4 дня</b> съели <b>72 сухаря</b>. Сколько съедят <b>9 человек за 5 дней</b>?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${подпись(168,26,'Двойное приведение',СИНЬ,16)}
        ${подпись(76,84,'6 × 4 = 24',GOLD,14)}
        ${подпись(250,84,'9 × 5 = 45',СИНЬ,14)}
        <g>${очередь(0,2,'7s')}${сетка(26,96,6,4,17,13,ХЛЕБ)}</g>
        <g>${очередь(1,2,'7s')}${сетка(192,96,9,5,13,10, верно?GREEN:МУТ,
          верно?'rgba(143,209,168,.22)':'rgba(255,255,255,.07)')}</g>
        ${т(76,168,'72 сухаря',14,МУТ)}
        ${т(250,166, верно?'135 сухарей':'? сухарей',14, верно?GREEN:МУТ)}
        ${верно?рамкаВсп(188,92,313,150,GREEN,'7s',0.2):''}
        ${подпись(168,200,'одна клетка — одна порция',ВОДА,14)}
        ${подпись(168,238, верно?'72 : 24 = 3 · 3 × 45 = 135':'сколько порций — столько клеток',
          верно?GREEN:GOLD, 14)}
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), '135 сухарей', "r992О4('ok')")}
        ${BTN(5, в==='no'?'miss':'', '90 сухарей', "r992О4('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Клетка — это один человек на один день, то есть одна порция. Слева их 6 × 4 = 24, и на них 72 сухаря: 72 : 24 = 3 в порции. Справа порций 9 × 5 = 45, и 3 × 45 = 135.'
              : 'Нет. Считай клетками: 6 × 4 = 24 порции на 72 сухаря, значит порция — 3 сухаря. Справа 9 × 5 = 45 порций, а 3 × 45 = 135.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Двух величин не пугайся: сведи их в <b>одну клетку</b> — один за один день.');
  }

  /* 8. Третий расчёт: ремень на вёсла */
  function F8(s){
    const в = s.о5, верно = в==='ok';
    return ЛИСТ(s) +
      ЗАДАЧА('Последний расчёт. На обмотку <b>4 вёсел</b> ушло <b>28 локтей</b> ремня. В Сиракузах поставят <b>7 вёсел</b>. Сколько ремня заказать?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        ${подпись(168,26,'Ремень на вёсла',СИНЬ,16)}
        ${подпись(168,62,'28 локтей на 4 весла',GOLD,14)}
        <g>${очередь(0,2,'7s')}${полоса(84,82,6,28,7,GOLD)}</g>
        ${т(76,99,'4 весла',14,МУТ,false,'end')}
        ${подпись(168,136, верно?'28 : 4 = 7 — на одно весло':'на одно весло — сколько?',
          верно?ВОДА:МУТ, 14)}
        ${т(168,160,'7 вёсел',14,МУТ)}
        <g>${очередь(1,2,'7s')}${полоса(21,170,6,49,7, верно?GREEN:МУТ)}</g>
        ${верно?рамкаВсп(17,166,319,198,GREEN,'7s',0.2):''}
        ${подпись(168,226, верно?'7 × 7 = 49 локтей':'сначала — на одно', верно?GREEN:МУТ, 14)}
      `,238)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '49 локтей', "r992О5('ok')")}
        ${BTN(5, в==='no'?'miss':'', '31 локоть', "r992О5('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Ремень разрезали на 4 равных куска по 7 локтей: 28 : 4 = 7. На семь вёсел нужно семь таких кусков: 7 × 7 = 49.'
              : 'Нет: 31 — это 28 да ещё три локтя, по локтю на новое весло. Но на каждое весло идёт целый кусок в 7 локтей: 7 × 7 = 49.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Приём один и тот же, <b>что вода, что сухари, что ремень</b>.');
  }

  /* 9. Когда единицу искать не надо */
  function F9(s){
    const в = s.о6, верно = в==='ok';
    const Е = 1.9;
    return ЛИСТ(s) +
      ЗАДАЧА('Иногда через единицу идти не обязательно. Если дней стало ровно <b>вдвое</b> больше, то и воды нужно ровно вдвое больше. Сколько уйдёт за 10 дней?') +
      `<div class="pic">${свг(`
        ${фон(236)}
        ${подпись(168,26,'Когда единицу искать не надо',СИНЬ,14)}
        <g>${очередь(0,2,'7s')}
          <rect x="76" y="90" width="${(60*Е).toFixed(1)}" height="24" rx="4"
            fill="rgba(255,215,106,.3)" stroke="${GOLD}" stroke-width="1.4"/>
          ${т(133,107,'60 мер',16,'#0f1729',true)}</g>
        ${т(68,107,'5 дней',14,МУТ,false,'end')}
        <g>${очередь(1,2,'7s')}
          <rect x="76" y="140" width="${(120*Е).toFixed(1)}" height="24" rx="4"
            fill="${верно?'rgba(143,209,168,.28)':'rgba(255,255,255,.08)'}"
            stroke="${верно?GREEN:МУТ}" stroke-width="1.4"/>
          ${т(190,157, верно?'120 мер':'? мер',16, верно?'#0f1729':МУТ,true)}</g>
        ${т(68,157,'10 дней',14,МУТ,false,'end')}
        ${верно?рамкаВсп(72,136,308,168,GREEN,'7s',0.2):''}
        ${подпись(168,192,'дней вдвое больше — и воды вдвое',GOLD,14)}
        ${подпись(168,222, верно?'60 × 2 = 120 · и 12 × 10 = 120':'а через единицу выйдет то же?',
          верно?GREEN:МУТ, 14)}
      `,236)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '120 мер', "r992О6('ok')")}
        ${BTN(5, в==='no'?'miss':'', '65 мер', "r992О6('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно, и проверить можно обоими путями: коротким — 60 × 2 = 120, и длинным, через единицу, — 12 × 10 = 120. Сошлось.'
              : 'Нет: пять лишних дней — это не пять мер. Коротко: дней вдвое больше, значит 60 × 2 = 120. Через единицу: 12 × 10 = 120.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Короткий путь годится, когда <b>делится нацело</b>. Не делится — иди через единицу.');
  }

  /* 10. Итог расчёта */
  function F10(s){
    const всё = РАСЧЁТЫ.every(р=>сосчитан(s,р.ключ));
    return ЛИСТ(s) +
      ЗАДАЧА(всё
        ? 'Лист штурмана закрыт: вода, сухари и ремень сосчитаны. Воды нужно 96 мер, в трюме 100 — идём.'
        : 'В листе сосчитано не всё — вернись и добей расчёты. А пока посмотри, чем кончается переход.') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="84" fill="url(#c992-небо)"/>
        <rect x="0" y="84" width="336" height="86" fill="url(#c992-море)"/>
        <rect x="0" y="170" width="336" height="78" fill="url(#c992-фон)"/>
        <path d="M252 92 q18 -26 38 -6 q14 -12 26 6 z" fill="#3c4a3a" stroke="${ОБВОД}" stroke-width="1"/>
        ${судно(150,136,1.5)}
        <rect x="0" y="0" width="336" height="248" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${рамкаВсп(96,58,204,166,СИНЬ,'8s',0.2)}
        ${подпись(168,26,'Расчёт сошёлся',СИНЬ,16)}
        ${подпись(168,196,'вода 96 · сухари 27 · ремень 49',GOLD,14)}
        ${подпись(168,228,'в трюме 100 — останется 4',GREEN,14)}
      `,248)}</div>` +
      СКАЗ('Итог перехода','Три расчёта — одна ступенька. 60 : 5 = 12, потом 12 × 8 = 96. 18 : 6 = 3, потом 3 × 9 = 27. 28 : 4 = 7, потом 7 × 7 = 49. Каждый раз сначала про одного, потом про всех.') +
      ПРАВИЛО('<b>Раздели на количество — получишь единицу. Умножь единицу — получишь новое количество.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'За 4 дня сожгли 20 свечей. Сколько уйдёт за 7 дней?',
      варианты:[{т:'35 свечей',ок:true},{т:'23 свечи',ок:false}],
      разбор:'20 : 4 = 5 свечей в день, 5 × 7 = 35. Три лишних дня стоят 15 свечей, а не три.' },
    { вопрос:'Три бочки весят 96 кг. Сколько весят пять таких бочек?',
      варианты:[{т:'160 кг',ок:true},{т:'98 кг',ок:false}],
      разбор:'96 : 3 = 32 кг одна бочка, 32 × 5 = 160.' },
    { вопрос:'За 6 часов прошли 42 мили. Сколько пройдут за 9 часов тем же ходом?',
      варианты:[{т:'63 мили',ок:true},{т:'45 миль',ок:false}],
      разбор:'42 : 6 = 7 миль в час, 7 × 9 = 63.' },
    { вопрос:'За 5 дней ушло 60 мер. Можно ли сказать, что за 6 дней уйдёт 61?',
      варианты:[{т:'Нельзя: выйдет 72',ок:true},{т:'Можно: день прибавили — меру прибавили',ок:false}],
      разбор:'День стоит 60 : 5 = 12 мер, а не одну. 12 × 6 = 72.' },
    { вопрос:'4 человека за 3 дня съели 36 сухарей. Сколько съедят 5 человек за 2 дня?',
      варианты:[{т:'30 сухарей',ок:true},{т:'45 сухарей',ок:false}],
      разбор:'Порций было 4 × 3 = 12, значит порция 36 : 12 = 3. Новых порций 5 × 2 = 10, и 3 × 10 = 30.' }
  ];

  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r992Reset()")}</div>` +
        ПРАВИЛО('Сначала про <b>одного</b>, потом про <b>всех</b>.');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к,
        выбран===к ? (в.ок?'hit':'miss') : '',
        в.т, "r992Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Сколько на один', задания:[
    {q:'45 мер на 5 дней. Сколько на один день?', в:0, варианты:['9','40'], раз:'45 : 5 = 9.'},
    {q:'56 локтей на 8 вёсел. Сколько на одно?', в:1, варианты:['48','7'], раз:'56 : 8 = 7.'},
    {q:'72 сухаря на 6 человек. Сколько одному?', в:0, варианты:['12','66'], раз:'72 : 6 = 12.'},
    {q:'100 кг на 4 бочки. Сколько весит одна?', в:1, варианты:['96','25'], раз:'100 : 4 = 25.'}
  ]};
  const Т2 = { имя:'Сколько на много', задания:[
    {q:'Одному нужно 8. Сколько нужно семерым?', в:0, варианты:['56','15'], раз:'8 × 7 = 56.'},
    {q:'В день уходит 15 мер. Сколько за 4 дня?', в:1, варианты:['19','60'], раз:'15 × 4 = 60.'},
    {q:'24 меры за 3 дня. Сколько за 5 дней?', в:0, варианты:['40','26'], раз:'24 : 3 = 8, потом 8 × 5 = 40.'},
    {q:'54 мили за 9 часов. Сколько за 12 часов?', в:1, варианты:['57','72'], раз:'54 : 9 = 6, потом 6 × 12 = 72.'}
  ]};
  const Т3 = { имя:'Ловушки и двойное', задания:[
    {q:'За 5 дней 60 мер. Сколько за 6 дней?', в:1, варианты:['61','72'], раз:'День стоит 12 мер: 12 × 6 = 72.'},
    {q:'6 человек за 2 дня съели 36. Сколько съест один за день?', в:0, варианты:['3','6'],
     раз:'Порций 6 × 2 = 12, значит 36 : 12 = 3.'},
    {q:'3 человека за 4 дня — 36 сухарей. Сколько 5 человек за 3 дня?', в:1, варианты:['36','45'],
     раз:'Порция 36 : 12 = 3; новых порций 5 × 3 = 15; 3 × 15 = 45.'},
    {q:'За 2 часа 14 миль. Сколько за 6 часов?', в:0, варианты:['42','18'],
     раз:'Часов втрое больше — и миль втрое: 14 × 3 = 42. Или через единицу: 7 × 6 = 42.'}
  ]};

  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r992T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r992TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L992 = {
    id: ID,
    title: 'Приведение к единице',
    ico: '🧭',
    src: 'Начальная школа · 4 класс · Приведение к единице',
    subj: 'jun',
    explain: [
      'Лист штурмана: позади 5 дней и 60 мер воды, впереди 8 дней, в трюме 100 мер. Из пяти дней в восемь не перепрыгнуть — между ними лежит один день.',
      'Мера воды — мерный сосуд с двенадцатью делениями: один день — один сосуд. Сколько бы дней ни было, цена одного дня не меняется.',
      'Ступенька через единицу: вниз 60 : 5 = 12, вверх 12 × 8 = 96. Низкий столбик посередине и есть единица.',
      'Ловушка: «на три дня больше — значит на три меры больше». Нет: день стоит целый сосуд, 3 × 12 = 36, и выходит 96.',
      'Проверка обратным действием: 96 : 8 = 12 — ровно та единица, с которой начинали.',
      'Сухари: 18 на 6 человек, значит 18 : 6 = 3 одному, и 3 × 9 = 27 на девятерых. Единица бывает не «на день», а «на одного».',
      'Двойное приведение: 6 человек за 4 дня — это 24 порции на 72 сухаря, порция 3. Девять человек за 5 дней — 45 порций, 3 × 45 = 135.',
      'Ремень: 28 локтей на 4 весла, значит 7 на одно, и 7 × 7 = 49 на семь. Приём один, груз разный.',
      'Когда делится нацело, можно короче: дней вдвое больше — и воды вдвое, 60 × 2 = 120. Через единицу выходит то же самое.',
      'Итог: три расчёта — одна ступенька. Сначала про одного, потом про всех.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: сколько на один.',
      'Тренажёр 2: сколько на много.',
      'Тренажёр 3: ловушки и двойное приведение.'
    ],
    check: {
      q: 'За 5 дней ушло 60 мер воды. Сколько уйдёт за 8 дней тем же расходом?',
      choices: ['96 мер','63 меры','75 мер'],
      ans: 0,
      exp: 'Сначала на один день: 60 : 5 = 12. Потом на восемь: 12 × 8 = 96.'
    },
    tasks: [
      { q:'За 5 дней ушло 60 мер воды. Сколько уйдёт за 8 дней?', kind:'choice',
        choices:['96 мер','63 меры'], ans:0,
        hints:['Сначала узнай, сколько уходит за ОДИН день.','60 : 5 = 12, дальше умножай.'],
        sol:'60 : 5 = 12; 12 × 8 = 96.' },
      { q:'6 человек съедают 18 сухарей в день. Сколько съедят девять?', kind:'choice',
        choices:['27 сухарей','21 сухарь'], ans:0,
        hints:['Сколько съедает один?','18 : 6 = 3, дальше умножай на 9.'],
        sol:'18 : 6 = 3; 3 × 9 = 27.' },
      { q:'За 5 дней ушло 60 мер. Сколько уходит за ОДИН день?', kind:'choice',
        choices:['12 мер','60 мер'], ans:0,
        hints:['Это и есть ступенька приёма.','Дели на количество дней.'],
        sol:'60 : 5 = 12.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L992.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1);
    else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);

    const ЗАГОЛОВКИ={
      1:'Лист штурмана', 2:'Один день — одна мерка', 3:'Ступенька через единицу',
      4:'Ловушка', 5:'Проверка', 6:'Сухари на день', 7:'Двойное приведение',
      8:'Ремень на вёсла', 9:'Короткий путь', 10:'Расчёт сошёлся', 11:'Практика',
      12:'Тренажёр 1', 13:'Тренажёр 2', 14:'Тренажёр 3'
    };
    el.innerHTML = `<div class="s6 l992" data-frame="${f}">
        <h2>${ЗАГОЛОВКИ[f]||'Приведение к единице'}</h2>
        ${сцена}
      </div>`;
  }

  window.r992Дней =(д)=>{ const s=S();
    s.дней=Math.max(1,Math.min(10,(s.дней==null?5:s.дней)+д)); chRender(0); };
  window.r992О1   =(к)=>{ const s=S(); s.о1=к; if(к==='ok') s.расч_вода=true;   chRender(0); };
  window.r992О2   =(к)=>{ S().о2=к; chRender(0); };
  window.r992О3   =(к)=>{ const s=S(); s.о3=к; if(к==='ok') s.расч_сухари=true; chRender(0); };
  window.r992О4   =(к)=>{ S().о4=к; chRender(0); };
  window.r992О5   =(к)=>{ const s=S(); s.о5=к; if(к==='ok') s.расч_ремень=true; chRender(0); };
  window.r992О6   =(к)=>{ S().о6=к; chRender(0); };
  window.r992Разложить=()=>{ const s=S(); s.разложено=!s.разложено; chRender(0); };
  window.r992Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r992Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r992T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=(s[ключ+'Шаг']||0)%набор.задания.length;
    const з=набор.задания[шаг];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r992TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L992; else arr.push(L992);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA992={render:render, L:L992};
})();
