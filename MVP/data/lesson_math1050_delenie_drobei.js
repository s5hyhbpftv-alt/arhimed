/* ============ МАТЕМАТИКА · УРОК 1050 · «ДЕЛЕНИЕ ДРОБЕЙ: ОБРАТНЫЕ ЧИСЛА» ============
   Техзадание: школа № 1517, диагностика 5 → 6 класс, блок 1 —
   «...умножение и деление дробей, взаимно-обратные дроби».
   Сюжет: кондитерская — от торта осталось 3/4, куски режут по 1/8 долей:
   деление отвечает на вопрос «сколько таких кусков помещается».

   ПО 12 ПРАВИЛАМ АУДИТА ЭКСПЕРТОВ:
     1) ответ не виден до выбора — и правило, и РИСУНОК (нарезка торта,
        подсчёт кусков, итоговые панели появляются только после ответа);
     2) верный вариант не первый — детерминированный сдвиг порядка;
     3) три варианта, неверные — реальные ошибки (перевернули делимое,
        перевернули оба числа, «при делении всегда меньше»);
     4) затухание: кадр 4 «я» (3/4 : 1/8 целиком), кадр 5 «мы» (шаг пропущен),
        кадр 6 «ты» (2/3 : 4/9 сам);
     5) интерливинг практики: 1–2 новая тема, 3 — прошлый урок (сокращение и
        НОД), 4 — смешанная (поделить и сократить), 5 — обёртка (порции в
        кондитерской);
     6) разминка в кадре 1 — сокращение 12/18 из урока 1039;
     7) самообъяснение — кадры 7 и 9: выбирается ПРИЧИНА;
     8) ошибка как данные — третий тренажёр: чужая запись, назови сбой;
     9) вариативность: 3/4 : 1/8 → 6 : 1/2 → 2/3 : 4/9 → 1 1/2 : 3/4 →
        не-пример 6 : 2 (здесь частное меньше делимого);
    10) я — мы — ты: кадры 4, 5, 6;
    11) выходной билет после пяти уровней;
    12) машинные проверки: qa_layout.py (включая переполнение кнопок).

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ (дроби через Fraction):
     обратные: 3/4 ↔ 4/3, 5 ↔ 1/5, 1/7 ↔ 7, 1 ↔ 1, 2/5 ↔ 5/2; у нуля нет
     3/4 : 1/8 = 3/4 · 8 = 6        проверка 6 · 1/8 = 3/4 ✓
     6 : 1/2 = 12                    проверка 12 · 1/2 = 6 ✓
     2/3 : 4/9 = 2/3 · 9/4 = 3/2     проверка 3/2 · 4/9 = 2/3 ✓
     1/2 : 3 = 1/6                   проверка 1/6 · 3 = 1/2 ✓
     3 : 1/4 = 12                    1 1/2 : 3/4 = 2      5/8 : 5/8 = 1
     сравнение: 6 : 1/2 = 12 (БОЛЬШЕ 6), но 6 : 2 = 3 (меньше 6)
     порции: 3/4 торта по 1/8 → 6 кусков; 3 л по 1/4 л → 12 стаканов
     ловушка «перевернули делимое»: 4/3 · 1/8 = 1/6 (а верно 6) ✗
     ловушка «перевернули оба»: 4/3 · 8 = 32/3 ✗

   СТАНДАРТ: deploy/АНИМАЦИЯ_УРОКОВ.md, deploy/РИСОВАНИЕ_ФИГУР.md.
   Движение — сама математика: торт режется на доли, куски выкладываются в
   ряд и считаются, дробь переворачивается. reduced-motion глушит SMIL. */
(function(){
  'use strict';

  const ID = 1050;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  const CSS=`
  #lvis .s6.l1050{gap:14px}
  #lvis .s6.l1050 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l1050 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l1050 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l1050 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l1050 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l1050 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l1050 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l1050 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l1050 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l1050 .правило b{color:${GOLD}}
  #lvis .s6.l1050 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l1050 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;
    font-size:16px;font-weight:600;line-height:1.35;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1050 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l1050 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l1050 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l1050 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l1050 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l1050 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l1050 .уровни .точка.сейчас{background:${GOLD};animation:l1050dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l1050dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l1050 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l1050{-webkit-text-size-adjust:100%}
  #lvis .s6.l1050 [data-anim]{animation:l1050rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l1050rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l1050 [data-anim]{animation:none!important}
    #lvis .s6.l1050 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l1050 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l1050-style');
      if(!s){ s=document.createElement('style'); s.id='l1050-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(String(ID)) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ПРАВИЛО2 = (ответ, до, после) => (ответ!=null) ? ПРАВИЛО(после) : ПРАВИЛО(до);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анВК = (значения,длит,keyTimes,центр,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c1050-фон" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#3a2a33"/><stop offset="0.45" stop-color="#281d24"/>
        <stop offset="1" stop-color="#150f13"/>
      </linearGradient>
      <linearGradient id="c1050-корж" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#e8c48c"/><stop offset="0.45" stop-color="#c99a5c"/>
        <stop offset="1" stop-color="#8a6132"/>
      </linearGradient>
      <linearGradient id="c1050-крем" x1="0" y1="0" x2="0.4" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fff3f8"/><stop offset="0.5" stop-color="#f2c9dd"/>
        <stop offset="1" stop-color="#c07f9c"/>
      </linearGradient>
      <linearGradient id="c1050-кусок" x1="0" y1="0" x2="0.4" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffe6b8"/><stop offset="0.5" stop-color="#dcae6c"/>
        <stop offset="1" stop-color="#9c7136"/>
      </linearGradient>
      <linearGradient id="c1050-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".6"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".22"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".2"/>
      </linearGradient>
      <linearGradient id="c1050-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <radialGradient id="c1050-свет" cx="0.26" cy="0.12" r="0.8" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffe9d0" stop-opacity=".2"/>
        <stop offset="0.6" stop-color="#ffe9d0" stop-opacity=".05"/>
        <stop offset="1" stop-color="#000000" stop-opacity=".34"/>
      </radialGradient>
      <filter id="c1050-тень" x="-60%" y="-60%" width="220%" height="220%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3" result="р"/>
        <feOffset in="р" dx="3" dy="5" result="с"/>
        <feFlood flood-color="#140a10" flood-opacity="0.45" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c1050-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c1050-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c1050-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="url(#c1050-свет)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(14,8,12,.86)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#140a10" opacity=".34" filter="url(#c1050-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c1050-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  const бумага = (x,y,ш,в,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="6" fill="#140a10" opacity=".36" filter="url(#c1050-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="url(#c1050-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="none" filter="url(#c1050-шум)" opacity=".1"/>
  </g>`;

  /* ТОРТ: круг, разрезанный на доли; закрашено «съедено/осталось» */
  function торт(x,y,r,долей,закрашено,нач,показыватьДоли){
    const части=[];
    for(let i=0;i<долей;i++){
      const а1=-Math.PI/2+(i/долей)*2*Math.PI, а2=-Math.PI/2+((i+1)/долей)*2*Math.PI;
      const x1=x+r*Math.cos(а1), y1=y+r*Math.sin(а1);
      const x2=x+r*Math.cos(а2), y2=y+r*Math.sin(а2);
      const большая=(2*Math.PI/долей)>Math.PI?1:0;
      const внутри=i<закрашено;
      части.push(`<path d="M${x} ${y} L${x1.toFixed(1)} ${y1.toFixed(1)} A${r} ${r} 0 ${большая} 1 ${x2.toFixed(1)} ${y2.toFixed(1)} Z"
        fill="url(#c1050-${внутри?'кусок':'корж'})" stroke="rgba(60,35,15,.55)" stroke-width="1.2"
        opacity="${внутри?1:0.55}"/>`);
    }
    const линии=показыватьДоли?части.join(''):'';
    return `<g>
      <ellipse cx="${x+4}" cy="${y+r+8}" rx="${r*1.05}" ry="9" fill="#140a10" opacity=".45" filter="url(#c1050-тень)"/>
      <circle cx="${x}" cy="${y}" r="${r}" fill="url(#c1050-корж)" stroke="${ОБВОД}" stroke-width="1.6"/>
      ${линии}
      <circle cx="${x}" cy="${y}" r="${r*0.72}" fill="url(#c1050-крем)" opacity=".5"/>
      <circle cx="${x}" cy="${y}" r="${r}" fill="none" stroke="${ОБВОД}" stroke-width="1.8">
        ${нач!=null?проявить('15s',нач,нач+0.05):''}</circle>
      <circle cx="${x}" cy="${y}" r="${r}" fill="none" filter="url(#c1050-шум)" opacity=".07"/>
      ${/* вишня */''}
      <circle cx="${x}" cy="${y-r*0.45}" r="5" fill="#b8324a" stroke="rgba(60,10,20,.5)" stroke-width="1"/>
      <circle cx="${x-1.6}" cy="${y-r*0.45-1.6}" r="1.6" fill="#fff" opacity=".8"/>
    </g>`;
  }

  /* РЯД КУСКОВ: сколько кусков поместилось */
  const рядКусков = (x,y,количество,размер,нач,цвет) => Array.from({length:количество},(_,i)=>
    `<g>${проявить('16s',нач+i*0.04,нач+i*0.04+0.04)}
      <rect x="${x+i*(размер+4)}" y="${y}" width="${размер}" height="${размер}" rx="4"
        fill="url(#c1050-кусок)" stroke="rgba(60,35,15,.5)" stroke-width="1.1"/>
      ${т(x+i*(размер+4)+размер/2,y+размер/2+4,String(i+1),12,цвет||'#3a2410',true)}</g>`).join('');

  /* ДРОБЬ в рамке */
  const дробь = (x,y,верх,низ,цвет,нач,кегль) => {
    const к=кегль||20;
    return `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
      ${т(x,y-6,верх,к,цвет||ИНК,true)}
      <line x1="${x-24}" y1="${y+2}" x2="${x+24}" y2="${y+2}" stroke="${цвет||ИНК}" stroke-width="2"/>
      ${т(x,y+26,низ,к,цвет||ИНК,true)}
    </g>`;
  };

  /* ПЕРЕВЁРНУТАЯ ДРОБЬ: символ переворота */
  const переворот = (x,y,нач) => `<g>${проявить('15s',нач,нач+0.05)}
    <path d="M${x-14} ${y} q14 -14 28 0" fill="none" stroke="${GOLD}" stroke-width="2"/>
    <path d="M${x+14} ${y} l-6 -5 l0 10 z" fill="${GOLD}"/>
    <path d="M${x+14} ${y+12} q-14 14 -28 0" fill="none" stroke="${GOLD}" stroke-width="2"/>
    <path d="M${x-14} ${y+12} l6 5 l0 -10 z" fill="${GOLD}"/>
  </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Разминка (сокращение, урок 1039) + сюжет */
  function F1(s){
    const в = s.разминка;
    return A(2,'cap','Разминка: прошлый урок про сокращение') +
      ВОПРОС('Сократи дробь 12/18.') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Кондитерская: торт и порции',14,GOLD,true)}
        ${торт(120,104,54,8,6,0.05,true)}
        ${подпись(120,196,'осталось 3/4 торта',МУТ,12)}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${стекло(24,208,288,32,0.5)}
          ${т(168,229,'12 : 6 = 2,  18 : 6 = 3 → 2/3',12,ИНК,true)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.4,0.5)}
          ${стекло(24,208,288,32,0.4)}
          ${т(168,229,'вспомним: делим верх и низ на НОД',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='дветрети'?'hit':(в?'miss':''), '2/3: НОД(12, 18) = 6', "r1050Разминка('дветрети')")}
        ${BTN(4, в==='шестьдевятых'?'miss':'', '6/9: разделил только на 2', "r1050Разминка('шестьдевятых')")}
        ${BTN(5, в==='половина'?'miss':'', '1/2 — так короче', "r1050Разминка('половина')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='дветрети',
        в==='дветрети' ? 'Верно: 12 : 6 = 2 и 18 : 6 = 3, значит 12/18 = 2/3. Сегодня мы будем делить дроби.'
                       : 'Нет: 6/9 ещё сократима, а 1/2 не равна 12/18. Делим на НОД 6 и получаем 2/3.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'На прошлом уроке мы сокращали дроби через НОД.',
                 '12/18 = <b>2/3</b>. Сегодня — деление дробей.');
  }

  /* 2. Смысл деления на дробь */
  function F2(s){
    const в = s.смысл;
    return ВОПРОС('Осталось 3/4 торта. Куски режут по 1/8. Сколько кусков получится?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Деление спрашивает: сколько помещается',14,GOLD,true)}
        ${торт(74,96,46,8,6,0.05,true)}
        ${подпись(74,164,'3/4 торта',МУТ,12)}
        ${в!=null?рядКусков(140,74,6,20,0.3,GOLD):''}
        ${в==null?`<g>${проявить('15s',0.3,0.4)}
          ${стекло(140,74,170,44,0.3)}
          ${т(225,100,'по 1/8 каждый',12,МУТ)}</g>`:''}
        <g>${проявить('15s',0.46,0.56)}
          ${стекло(24,190,288,46,0.46)}
          ${т(168,210,'делим 3/4 на 1/8 —',12,ИНК,true)}
          ${т(168,228,'считаем, сколько восьмых долей уместится',12,МУТ)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), 'Шесть кусков: 6 · 1/8 = 3/4', "r1050Смысл('шесть')")}
        ${BTN(4, в==='три'?'miss':'', 'Три куска: 3/4 близко к 3', "r1050Смысл('три')")}
        ${BTN(5, в==='две'?'miss':'', 'Два куска: 1/8 меньше 3/4', "r1050Смысл('две')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: в 3/4 помещается шесть восьмых долей. Проверка умножением: 6 · 1/8 = 6/8 = 3/4.'
                    : 'Нет: посчитай доли на рисунке. В 3/4 умещается шесть восьмых, потому что 6 · 1/8 = 3/4.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Подумай, сколько долей помещается в остатке.',
                 '3/4 : 1/8 = <b>6</b> кусков.');
  }

  /* 3. Взаимно обратные числа — самообъяснение */
  function F3(s){
    const в = s.обратные;
    return ВОПРОС('Почему числа 3/4 и 4/3 называют взаимно обратными?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Взаимно обратные числа',14,GOLD,true)}
        <g>${проявить('15s',0.05,0.15)}
          ${дробь(96,58,'3','4',BLUE,0.05,20)}
          ${переворот(140,60,0.1)}
          ${дробь(190,58,'4','3',GOLD,0.14,20)}</g>
        ${в!=null?`<g>${проявить('15s',0.3,0.42)}
          ${бумага(24,104,288,42,0.3,-0.4)}
          ${т(168,124,'3/4 · 4/3 = 12/12 = 1',16,'#2b2110',true)}
          ${т(168,140,'произведение всегда равно единице',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${стекло(24,156,288,44,0.5)}
          ${т(168,176,'5 ↔ 1/5,  1/7 ↔ 7,  1 ↔ 1',14,ИНК,true)}
          ${т(168,192,'а у нуля обратного числа нет',12,RED)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.68,0.8)}
          ${подпись(168,228,'переворачиваем: верх и низ меняются местами',GREEN,12)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.3,0.4)}
          ${стекло(24,156,288,44,0.3)}
          ${т(168,182,'чем эти числа особенны?',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='единица'?'hit':(в?'miss':''), 'Их произведение равно 1: 3/4 · 4/3 = 1', "r1050Обрат('единица')")}
        ${BTN(4, в==='сумма'?'miss':'', 'Их сумма равна 1: 3/4 + 4/3 = 1', "r1050Обрат('сумма')")}
        ${BTN(5, в==='похожи'?'miss':'', 'У них одинаковые цифры, просто стоят наоборот', "r1050Обрат('похожи')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='единица',
        в==='единица' ? 'Верно: взаимно обратные числа дают в произведении единицу. Сумма здесь не равна единице: 3/4 + 4/3 = 25/12.'
                      : 'Нет: сумма не равна единице. Главное свойство обратных чисел — произведение равно 1.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Посмотри, что даёт произведение этих чисел.',
                 'Взаимно обратные: <b>произведение равно 1</b>.');
  }

  /* 4. «Я»: правило */
  function F4(s){
    const в = s.я;
    return A(2,'cap','Я решаю: 3/4 : 1/8') +
      ВОПРОС('Как разделить 3/4 на 1/8?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Деление — это умножение на обратную',14,GOLD,true)}
        ${в!=null?`<g>${проявить('15s',0.06,0.18)}
          ${бумага(24,38,288,42,0.06,-0.4)}
          ${т(168,58,'3/4 : 1/8 = 3/4 · 8/1',16,'#2b2110',true)}
          ${т(168,74,'переворачиваем ДЕЛИТЕЛЬ',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.26,0.38)}
          ${т(120,110,'3 · 8',14,ИНК,true)}
          <line x1="86" y1="118" x2="154" y2="118" stroke="${ИНК}" stroke-width="1.6"/>
          ${т(120,138,'4 · 1',14,ИНК,true)}
          ${т(160,126,'=',20,ИНК,true)}
          ${т(210,126,'24/4 = 6',20,GREEN,true)}</g>`:''}
        ${в!=null?рядКусков(140,158,6,20,0.42,GOLD):''}
        <g>${проявить('15s',0.6,0.7)}
          ${стекло(24,214,288,30,0.6)}
          ${т(168,234,'проверка: 6 · 1/8 = 3/4',12,МУТ)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='умножить'?'hit':(в?'miss':''), 'Умножить на обратную дробь: 3/4 · 8/1 = 6', "r1050Я('умножить')")}
        ${BTN(4, в==='делитьниз'?'miss':'', 'Разделить знаменатели: 4 : 8 — так короче', "r1050Я('делитьниз')")}
        ${BTN(5, в==='перевернутьвсё'?'miss':'', 'Перевернуть обе дроби и умножить', "r1050Я('перевернутьвсё')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='умножить',
        в==='умножить' ? 'Верно: деление заменяем умножением на обратную дробь. Переворачиваем только делитель: 1/8 → 8/1. Получаем 24/4 = 6.'
                       : 'Нет: знаменатели делить нельзя, а переворачивать надо только делитель. 3/4 · 8/1 = 6.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Вспомни, что делают с делителем при делении.',
                 'Деление: <b>умножаем на обратную делителю</b> → 6.');
  }

  /* 5. «Мы»: 6 : 1/2, шаг пропущен */
  function F5(s){
    const в = s.мы;
    return A(2,'cap','Мы решаем вместе: шаг пропущен') +
      ВОПРОС('6 : 1/2 = 6 · ? — что впишем вместо вопроса?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Дописываем шаг',14,GOLD,true)}
        ${в!=null?`<g>${проявить('15s',0.06,0.18)}
          ${бумага(24,38,288,42,0.06,-0.4)}
          ${т(168,58,'6 : 1/2 = 6 · 2/1',16,'#2b2110',true)}
          ${т(168,74,'обратная к 1/2 — это 2/1',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.26,0.38)}
          ${т(168,110,'6 · 2 = 12',20,GREEN,true)}</g>`:''}
        ${в!=null?рядКусков(84,138,8,20,0.4,GOLD):''}
        ${в==null?`<g>${проявить('15s',0.3,0.4)}
          ${стекло(24,190,288,30,0.3)}
          ${т(168,210,'на какое число умножить?',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='два'?'hit':(в?'miss':''), 'На 2: 6 · 2 = 12 половинок', "r1050Мы('два')")}
        ${BTN(4, в==='половина'?'miss':'', 'На 1/2: 6 · 1/2 = 3', "r1050Мы('половина')")}
        ${BTN(5, в==='шесть'?'miss':'', 'На 6: 6 · 6 = 36', "r1050Мы('шесть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='два',
        в==='два' ? 'Верно: обратная к 1/2 — это 2, поэтому 6 : 1/2 = 6 · 2 = 12. Проверка: 12 · 1/2 = 6.'
                  : 'Нет: умножать надо на обратную к делителю. Обратная к 1/2 — это 2, значит 6 · 2 = 12.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Найди число, обратное делителю.',
                 '6 : 1/2 = 6 · 2 = <b>12</b>.');
  }

  /* 6. «Ты»: 2/3 : 4/9 */
  function F6(s){
    const в = s.ты;
    return A(2,'cap','Теперь ты: раздели сам') +
      ВОПРОС('Раздели 2/3 на 4/9.') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Решаем самостоятельно',14,GOLD,true)}
        ${в!=null?`<g>${проявить('15s',0.06,0.18)}
          ${бумага(24,38,288,42,0.06,-0.4)}
          ${т(168,58,'2/3 : 4/9 = 2/3 · 9/4',16,'#2b2110',true)}
          ${т(168,74,'= 18/12 = 3/2',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.26,0.4)}
          ${дробь(120,112,'2','3',МУТ,0.26,20)}
          ${т(150,136,'·',20,ИНК,true)}
          ${дробь(200,112,'9','4',GOLD,0.32,20)}
          ${т(240,136,'=',20,ИНК,true)}
          ${дробь(288,112,'3','2',GREEN,0.4,20)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.28,0.38)}
          ${стекло(24,190,288,30,0.28)}
          ${т(168,210,'переверни делитель и умножь',12,МУТ)}</g>`:''}
        <g>${проявить('15s',0.6,0.7)}
          ${стекло(24,214,288,30,0.6)}
          ${т(168,234,'проверка: 3/2 · 4/9 = 2/3',12,МУТ)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тривторых'?'hit':(в?'miss':''), '3/2: 2/3 · 9/4 = 18/12 = 3/2', "r1050Ты('тривторых')")}
        ${BTN(4, в==='восемьдвадцатьседьмых'?'miss':'', '8/27: перемножил как есть, не переворачивая', "r1050Ты('восемьдвадцатьседьмых')")}
        ${BTN(5, в==='шестьвосьмых'?'miss':'', '6/8: перевернул делимое вместо делителя', "r1050Ты('шестьвосьмых')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тривторых',
        в==='тривторых' ? 'Верно: 2/3 · 9/4 = 18/12, сокращаем на 6 и получаем 3/2. Проверка: 3/2 · 4/9 = 12/18 = 2/3.'
                        : 'Нет: делитель надо перевернуть. 2/3 · 9/4 = 18/12 = 3/2.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Переверни именно делитель.',
                 '2/3 : 4/9 = <b>3/2</b>.');
  }

  /* 7. Почему так: проверка умножением — самообъяснение */
  function F7(s){
    const в = s.почему;
    return ВОПРОС('Почему деление можно заменить умножением на обратную дробь?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Проверяем умножением',14,GOLD,true)}
        <g>${проявить('15s',0.05,0.15)}
          ${бумага(24,40,288,40,0.05,-0.4)}
          ${т(168,64,'6 : 1/2 = 12',20,'#2b2110',true)}</g>
        ${в!=null?`<g>${проявить('15s',0.24,0.36)}
          ${бумага(24,90,288,40,0.24,0.4)}
          ${т(168,114,'проверка: 12 · 1/2 = 6',16,'#2b2110',true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.46,0.58)}
          ${стекло(24,142,288,44,0.46)}
          ${т(168,162,'частное, умноженное на делитель,',12,ИНК,true)}
          ${т(168,178,'даёт делимое',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.64,0.76)}
          ${подпись(168,212,'поэтому «перевернуть и умножить» работает',GREEN,12)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.3,0.4)}
          ${стекло(24,142,288,44,0.3)}
          ${т(168,168,'вспомни, как проверяют деление',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='проверка'?'hit':(в?'miss':''), 'Частное, умноженное на делитель, даёт делимое', "r1050Поч('проверка')")}
        ${BTN(4, в==='договор'?'miss':'', 'Так просто договорились считать', "r1050Поч('договор')")}
        ${BTN(5, в==='быстрее'?'miss':'', 'Потому что умножение считать быстрее', "r1050Поч('быстрее')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='проверка',
        в==='проверка' ? 'Верно: деление проверяют умножением. Если 6 : 1/2 = 12, то 12 · 1/2 должно дать 6 — и действительно даёт.'
                      : 'Нет: это не договорённость. Частное, умноженное на делитель, обязано дать делимое — отсюда и правило.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Подумай, чем проверяют деление.',
                 'Деление проверяем умножением: <b>частное · делитель = делимое</b>.');
  }

  /* 8. Ловушка 1: перевернули не то */
  function F8(s){
    const в = s.ловушка1;
    return ВОПРОС('Ученик решал 3/4 : 1/8 и перевернул делимое: 4/3 · 1/8 = 1/6. Что не так?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Ловушка: перевернули не то число',14,GOLD,true)}
        <g>${проявить('15s',0.05,0.15)}
          ${бумага(24,40,288,42,0.05,-0.5)}
          ${т(168,60,'4/3 · 1/8 = 1/6',16,'#2b2110',true)}
          ${т(168,76,'перевернули ДЕЛИМОЕ',12,RED)}</g>
        ${в!=null?`<g>${проявить('15s',0.24,0.36)}
          ${бумага(24,92,288,42,0.24,0.4)}
          ${т(168,112,'верно: 3/4 · 8/1 = 6',16,'#2b2110',true)}
          ${т(168,128,'переворачиваем ДЕЛИТЕЛЬ',12,'#2a6b3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.46,0.58)}
          ${стекло(24,144,288,44,0.46)}
          ${т(168,164,'проверка ложного ответа:',12,ИНК,true)}
          ${т(168,180,'1/6 · 1/8 = 1/48, а не 3/4',12,RED)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.64,0.76)}
          ${подпись(168,212,'проверка сразу показывает ошибку',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='делитель'?'hit':(в?'miss':''), 'Переворачивать надо делитель 1/8, а не делимое', "r1050Лов1('делитель')")}
        ${BTN(4, в==='верно'?'miss':'', 'Ответ 1/6 верный, просто записан иначе', "r1050Лов1('верно')")}
        ${BTN(5, в==='оба'?'miss':'', 'Надо было перевернуть обе дроби', "r1050Лов1('оба')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='делитель',
        в==='делитель' ? 'Верно: переворачиваем только делитель. 3/4 · 8/1 = 6, и проверка 6 · 1/8 = 3/4 это подтверждает.'
                       : 'Нет: 1/6 не равно 6, а проверка даёт 1/48 вместо 3/4. Переворачивать нужно делитель.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Проверь, какое число переворачивают.',
                 'Переворачиваем <b>только делитель</b>.');
  }

  /* 9. Ловушка 2: «деление всегда уменьшает» */
  function F9(s){
    const в = s.ловушка2;
    return ВОПРОС('6 : 1/2 = 12, а 6 : 2 = 3. Почему в первом случае получилось больше?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Деление не всегда уменьшает',14,GOLD,true)}
        <g>${проявить('15s',0.05,0.15)}
          ${бумага(24,40,288,42,0.05,-0.5)}
          ${т(168,60,'6 : 2 = 3',16,'#2b2110',true)}
          ${т(168,76,'на 2 части — каждая меньше',12,'#6a5a3a')}</g>
        ${в!=null?`<g>${проявить('15s',0.24,0.36)}
          ${бумага(24,92,288,42,0.24,0.4)}
          ${т(168,112,'6 : 1/2 = 12',16,'#2b2110',true)}
          ${т(168,128,'половинок в шести — двенадцать',12,'#2a6b3a')}</g>`:''}
        ${в!=null?рядКусков(84,150,8,20,0.44,GOLD):''}
        ${в!=null?`<g>${проявить('15s',0.66,0.78)}
          ${подпись(168,206,'чем меньше делитель, тем больше частное',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='меньше'?'hit':(в?'miss':''), 'Делитель меньше единицы, поэтому частное больше делимого', "r1050Лов2('меньше')")}
        ${BTN(4, в==='ошибка'?'miss':'', 'Здесь ошибка: деление обязано уменьшать', "r1050Лов2('ошибка')")}
        ${BTN(5, в==='нуль'?'miss':'', 'Потому что 1/2 почти ноль', "r1050Лов2('нуль')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='меньше',
        в==='меньше' ? 'Верно: делим на число меньше единицы — частное растёт. В шести метрах умещается двенадцать половинок.'
                     : 'Нет: ошибки нет. Когда делят на дробь меньше единицы, частное становится больше делимого.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Сравни делитель с единицей.',
                 'Делим на дробь <b>меньше 1</b> — частное <b>больше</b> делимого.');
  }

  /* 10. Смешанные числа и целые */
  function F10(s){
    const в = s.смешанные;
    return ВОПРОС('Раздели 1 1/2 на 3/4. Сколько получится?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Смешанные числа и целые',14,GOLD,true)}
        ${в!=null?`<g>${проявить('15s',0.06,0.18)}
          ${бумага(24,38,288,44,0.06,-0.4)}
          ${т(168,58,'1 1/2 = 3/2',16,'#2b2110',true)}
          ${т(168,74,'сначала превращаем в неправильную дробь',12,'#6a5a3a')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.26,0.38)}
          ${дробь(120,116,'3','2',МУТ,0.26,20)}
          ${т(150,140,':',20,ИНК,true)}
          ${дробь(190,116,'3','4',GOLD,0.32,20)}
          ${т(228,140,'=',20,ИНК,true)}
          ${т(268,140,'2',20,GREEN,true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.46,0.58)}
          ${стекло(24,164,288,42,0.46)}
          ${т(168,184,'3/2 · 4/3 = 12/6 = 2',14,ИНК,true)}
          ${т(168,200,'целые числа тоже переворачиваем: 3 → 1/3',12,МУТ)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.3,0.4)}
          ${стекло(24,164,288,42,0.3)}
          ${т(168,190,'как записать 1 1/2 одной дробью?',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='два'?'hit':(в?'miss':''), '2: 3/2 · 4/3 = 12/6 = 2', "r1050Смеш('два')")}
        ${BTN(4, в==='полтора'?'miss':'', '1 1/2: деление не изменило число', "r1050Смеш('полтора')")}
        ${BTN(5, в==='девятьвосьмых'?'miss':'', '9/8: перемножил как есть', "r1050Смеш('девятьвосьмых')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='два',
        в==='два' ? 'Верно: превращаем 1 1/2 в 3/2, переворачиваем 3/4 в 4/3 и получаем 12/6 = 2. Проверка: 2 · 3/4 = 3/2.'
                  : 'Нет: смешанное число сначала записываем дробью: 1 1/2 = 3/2. Тогда 3/2 · 4/3 = 2.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Преврати смешанное число в дробь.',
                 '1 1/2 : 3/4 = <b>2</b>.');
  }

  /* 11. Сводка */
  function F11(s){
    const в = s.сводка;
    return ВОПРОС('Как разделить дробь на дробь?') +
      `<div class="pic">${свг(`
        ${фон(266)}
        ${т(168,24,'Порядок действий',14,GOLD,true)}
        ${['запиши смешанные числа дробями','переверни ДЕЛИТЕЛЬ (обратная дробь)','замени деление умножением','сократи и посчитай','проверь умножением']
          .map((с,i)=>`<g>${проявить('15s',0.05+i*0.08,0.14+i*0.08)}
            <rect x="24" y="${38+i*32}" width="288" height="26" rx="7"
              fill="${i===1?'rgba(255,215,106,.16)':'rgba(255,255,255,.06)'}" stroke="${i===1?GOLD:ЛИНИЯ}" stroke-width="1.1"/>
            ${т(168,55+i*32,(i+1)+'. '+с,12,ИНК,true)}</g>`).join('')}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${подпись(168,214,'деление на дробь меньше 1 увеличивает',GOLD,12)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.7,0.82)}
          ${стекло(24,226,288,28,0.7)}
          ${т(168,244,'у нуля обратного числа нет',RED)}</g>`:''}
      `,266)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='перевернуть'?'hit':(в?'miss':''), 'Перевернуть делитель и умножить, потом проверить', "r1050Свод('перевернуть')")}
        ${BTN(4, в==='числители'?'miss':'', 'Разделить числители и знаменатели по отдельности', "r1050Свод('числители')")}
        ${BTN(5, в==='ничего'?'miss':'', 'Ничего не менять: просто перемножить дроби', "r1050Свод('ничего')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='перевернуть',
        в==='перевернуть' ? 'Верно: переворачиваем делитель, умножаем, сокращаем и обязательно проверяем умножением. Так ошибку видно сразу.'
                          : 'Нет: перемножать как есть нельзя, а делить числители с знаменателями — не правило. Переворачиваем делитель.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Выбери правило деления.',
                 'Деление дробей: <b>умножить на обратную делителю</b>.');
  }

  /* 12. Практика: интерливинг + выходной билет */
  const УРОВНИ=[
    { тип:'новая', вопрос:'Раздели 3/4 на 1/8.', варианты:[
        {т:'6', ок:true,  почему:'3/4 · 8/1 = 6; проверка 6 · 1/8 = 3/4.'},
        {т:'1/6', ок:false, почему:'Это 4/3 · 1/8 — перевёрнуто делимое.'},
        {т:'3/32', ок:false, почему:'Так получилось бы при умножении 3/4 · 1/8.'}]},
    { тип:'новая', вопрос:'Раздели 6 на 1/2.', варианты:[
        {т:'12', ок:true,  почему:'6 · 2 = 12; проверка 12 · 1/2 = 6.'},
        {т:'3', ок:false, почему:'Это 6 · 1/2 — умножение вместо деления.'},
        {т:'1/12', ок:false, почему:'1/12 — это 1/2 : 6, а не 6 : 1/2.'}]},
    { тип:'прошлая', вопрос:'Вспомним прошлый урок: сократи 24/36.', варианты:[
        {т:'2/3', ок:true,  почему:'НОД(24, 36) = 12.'},
        {т:'12/18', ок:false, почему:'Разделил только на 2 — ещё сократима.'},
        {т:'4/6', ок:false, почему:'Разделил на 6 — не до конца.'}]},
    { тип:'смешанная', вопрос:'Раздели 2/3 на 4/9 и сократи ответ.', варианты:[
        {т:'3/2', ок:true,  почему:'2/3 · 9/4 = 18/12 = 3/2.'},
        {т:'8/27', ок:false, почему:'Перемножил, не перевернув делитель.'},
        {т:'6/12', ок:false, почему:'Перевернул делимое вместо делителя.'}]},
    { тип:'обёртка', вопрос:'В бутыли 3 л, стаканы по 1/4 л. Сколько стаканов?', варианты:[
        {т:'12', ок:true,  почему:'3 · 4 = 12; проверка 12 · 1/4 = 3.'},
        {т:'3/4', ок:false, почему:'Это 3 · 1/4 — умножение вместо деления.'},
        {т:'1/12', ок:false, почему:'Обратный порядок: это 1/4 : 3.'}]}
  ];
  function F12(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Выходной билет: придумай задачу, где деление на дробь даёт целый ответ, и проверь её умножением.') +
        РАЗБОР(true,'Например: 2 : 1/4 = 8, проверка 8 · 1/4 = 2. Или 3/4 : 1/8 = 6 (порции торта).') +
        `<div class="ask">${BTN(4,'','пройти заново','r1050Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const порядок=сдвиг(ур.варианты, и+1);
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length+' · '+ур.тип) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + порядок.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r1050Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(порядок[выбран] && порядок[выбран].ок, (порядок[выбран]||{}).почему||'') : ЖДЁТ()) +
      ПРАВИЛО('Деление дробей: <b>перевернуть делитель и умножить</b>.');
  }

  /* 13–15. Тренажёры */
  const Т1={
    имя:'Тренажёр 1 · обратные числа',
    задания:[
      { ф:'Назови обратное к 3/4.', о:['4/3','3/4','1/3'], в:0, р:'Произведение 3/4 · 4/3 = 1.' },
      { ф:'Назови обратное к 5.', о:['1/5','5','1'], в:0, р:'5 · 1/5 = 1.' },
      { ф:'Назови обратное к 1/7.', о:['7','1/7','1'], в:0, р:'1/7 · 7 = 1.' },
      { ф:'У какого числа нет обратного?', о:['у нуля','у единицы','у десяти'], в:0, р:'0 · x = 1 невозможно.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · делим дроби',
    задания:[
      { ф:'3/4 : 1/8 = ?', о:['6','1/6','3/32'], в:0, р:'3/4 · 8 = 6.' },
      { ф:'6 : 1/2 = ?', о:['12','3','1/12'], в:0, р:'6 · 2 = 12.' },
      { ф:'2/3 : 4/9 = ?', о:['3/2','8/27','6/12'], в:0, р:'2/3 · 9/4 = 18/12 = 3/2.' },
      { ф:'1/2 : 3 = ?', о:['1/6','3/2','6'], в:0, р:'1/2 · 1/3 = 1/6.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · разбор чужой записи',
    задания:[
      { ф:'Ученик решал 3/4 : 1/8 как 4/3 · 1/8 = 1/6. Что не так?', о:['перевернул делимое вместо делителя','всё верно','надо было перевернуть оба'], в:0, р:'Верно 3/4 · 8/1 = 6.' },
      { ф:'Ученик из 6 : 1/2 получил 3. Что не так?', о:['он умножил вместо деления: верно 12','всё верно','надо было 1/12'], в:0, р:'6 · 2 = 12.' },
      { ф:'Ученик сказал, что деление всегда уменьшает число. Что не так?', о:['деление на дробь меньше 1 увеличивает','всё верно','уменьшает только на 2'], в:0, р:'6 : 1/2 = 12.' },
      { ф:'Ученик решал 1 1/2 : 3/4 и перемножил 1 1/2 · 3/4 = 9/8. Что не так?', о:['надо перевернуть делитель и взять 3/2','всё верно','надо было 1 1/2 · 4/3 = 5/3'], в:0, р:'3/2 · 4/3 = 2.' }
    ]
  };

  /* сдвиг вариантов: верный ответ не всегда первый */
  function сдвиг(варианты, ключ){
    const н=варианты.length; if(н<2) return варианты;
    const к=((ключ*7+3)%н);
    return варианты.slice(к).concat(варианты.slice(0,к));
  }

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const порядок=сдвиг(з.о.map((о,к)=>({о,к})), шаг+1).map(х=>х.о);
    const верный=порядок.indexOf(з.о[з.в]);
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===верный;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + порядок.map((о,к)=>
        BTN(4+к, готово&&к===верный?'hit':(готово&&к===отв?'miss':''), о, `r1050T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r1050TNext('${ключ}')`)}</div>` : '');
  }
  function F13(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F14(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F15(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L1050={
    id: ID, title:'Деление дробей: обратные числа', ico:'🍰',
    src:'Математика · 5–6 класс · Дроби', subj:'math',
    explain: [
      'Начнём с разминки: на прошлом уроке мы сокращали дроби через наибольший общий делитель, и 12/18 превратилось в 2/3. Сегодня новый вопрос. В кондитерской осталось три четверти торта, а куски режут по одной восьмой. Сколько кусков получится? Это задача на деление дробей, и она отвечает на вопрос «сколько таких долей помещается».',

      'Посмотрим на торт. Три четверти — это шесть восьмых долей, значит получится шесть кусков. Запишем это делением: 3/4 разделить на 1/8 равно 6. Деление на дробь спрашивает не «сколько будет, если разделить», а «сколько раз эта доля укладывается». Поэтому ответ может оказаться больше того числа, которое мы делили.',

      'Чтобы делить дроби, нужно познакомиться с взаимно обратными числами. Возьмём 3/4 и перевернём: получится 4/3. Произведение 3/4 и 4/3 равно единице: 12/12 = 1. Именно это свойство и делает числа взаимно обратными: их произведение всегда равно единице. Обратное к числу 5 — это 1/5, обратное к 1/7 — это 7, а обратное к единице — сама единица. У нуля обратного числа нет, потому что ноль, умноженный на что угодно, не даст единицу.',

      'Теперь правило деления: разделить на дробь — значит умножить на обратную к ней. Решим 3/4 разделить на 1/8. Переворачиваем делитель: 1/8 превращается в 8/1. Умножаем: 3/4 · 8/1 = 24/4 = 6. Обратите внимание: переворачиваем только делитель, делимое остаётся как было. Проверим умножением: 6 · 1/8 = 6/8 = 3/4 — всё сходится.',

      'Решим вместе: 6 разделить на 1/2. Обратное к 1/2 — это 2, поэтому деление заменяется умножением: 6 · 2 = 12. Смысл ответа простой: в шести целых умещается двенадцать половинок. Заметьте, что получилось больше шести — и это правильно, ведь мы делили на число меньше единицы.',

      'Попробуй сам: раздели 2/3 на 4/9. Переворачиваем делитель: 4/9 превращается в 9/4. Умножаем: 2/3 · 9/4 = 18/12. Теперь сокращаем: НОД(18, 12) = 6, значит 18/12 = 3/2. Проверим: 3/2 · 4/9 = 12/18 = 2/3 — верно. Если у тебя получилось 8/27, значит дроби перемножили, не перевернув делитель.',

      'Почему правило вообще работает? Вспомним, как проверяют деление: частное, умноженное на делитель, должно дать делимое. Проверим на нашем примере: если 6 разделить на 1/2 равно 12, то 12 · 1/2 обязано дать 6 — и действительно даёт. Именно это требование и приводит к правилу «перевернуть делитель и умножить»: только так произведение вернёт исходное число.',

      'Разберём первую ловушку. Ученик решал 3/4 разделить на 1/8 и перевернул делимое: получилось 4/3 · 1/8 = 4/24 = 1/6. Проверим его ответ умножением: 1/6 · 1/8 = 1/48, а должно получиться 3/4. Ответ неверный. Переворачивать нужно именно делитель: 1/8 превращается в 8/1, и тогда 3/4 · 8/1 = 6.',

      'Вторая ловушка — привычка думать, что деление всегда уменьшает. Сравним: 6 разделить на 2 равно 3, это меньше шести; а 6 разделить на 1/2 равно 12, это больше шести. Противоречия нет: во втором случае мы спрашиваем, сколько половинок умещается в шести целых, а их двенадцать. Правило простое: если делитель меньше единицы, частное больше делимого.',

      'Отдельно про смешанные числа. Разделим 1 1/2 на 3/4. Сначала превратим смешанное число в дробь: 1 1/2 = 3/2. Теперь перевернём делитель: 3/4 превращается в 4/3. Умножаем: 3/2 · 4/3 = 12/6 = 2. Проверка: 2 · 3/4 = 3/2 — верно. Целые числа тоже переворачиваются: 3 — это 3/1, а обратное к нему 1/3.',

      'Соберём порядок действий. Первое: запиши смешанные числа обыкновенными дробями. Второе: переверни делитель — найди обратную дробь. Третье: замени деление умножением. Четвёртое: сократи и посчитай. Пятое: проверь умножением — частное, умноженное на делитель, должно дать делимое. И помни: у нуля обратного числа нет, а при делении на дробь меньше единицы ответ становится больше.',

      'Пройти практику: пять уровней. Первые два — деление дробей и целых, третий вернёт нас к прошлому уроку про сокращение, четвёртый смешает темы, а пятый спрячет деление в задачу про стаканы. В конце — выходной билет: придумай задачу, где деление на дробь даёт целый ответ.',

      'Первый тренажёр — взаимно обратные числа.',

      'Второй тренажёр — деление дробей и целых чисел.',

      'Третий тренажёр — разбор чужой записи: в каждой задаче ученик ошибся, и нужно назвать, что именно он сделал не так.'
    ],
    check: {
      q:'Раздели 3/4 на 1/8.',
      choices:['6','1/6','3/32'],
      ans:0,
      exp:'Переворачиваем делитель: 3/4 · 8/1 = 24/4 = 6. Проверка: 6 · 1/8 = 3/4.'
    },
    tasks: [
      { q:'Раздели 6 на 1/2.', kind:'choice',
        choices:['12','3'], ans:0,
        hints:['Обратное к 1/2 — это 2.','6 · 2.'],
        sol:'12.' },
      { q:'Раздели 2/3 на 4/9.', kind:'choice',
        choices:['3/2','8/27'], ans:0,
        hints:['Переверни делитель.','2/3 · 9/4 = 18/12.'],
        sol:'3/2.' },
      { q:'Какое число обратно к 5?', kind:'choice',
        choices:['1/5','5'], ans:0,
        hints:['Произведение должно дать 1.','5 · 1/5 = 1.'],
        sol:'1/5.' },
      { q:'Ученик из 6 : 1/2 получил 3. Что не так?', kind:'choice',
        choices:['Он умножил вместо деления: верно 12','Всё верно'], ans:0,
        hints:['Делим на дробь меньше 1.','6 · 2 = 12.'],
        sol:'Верно 12.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L1050.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else if(f===14) сцена=F14(s);
    else сцена=F15(s);

    const ЗАГОЛОВКИ={
      1:['Разминка','Сокращение и торт'],
      2:['Смысл деления','Сколько долей помещается'],
      3:['Обратные числа','Произведение равно 1'],
      4:['Я решаю','3/4 : 1/8'],
      5:['Мы решаем','6 : 1/2, шаг пропущен'],
      6:['Ты решаешь','2/3 : 4/9 сам'],
      7:['Почему так','Проверка умножением'],
      8:['Ловушка','Перевернули не то'],
      9:['Ловушка','Деление не всегда уменьшает'],
      10:['Смешанные числа','1 1/2 : 3/4'],
      11:['Сводка','Порядок действий'],
      12:['Практика','Пять уровней'],
      13:['Тренажёр 1','Обратные числа'],
      14:['Тренажёр 2','Делим дроби'],
      15:['Тренажёр 3','Разбор чужой записи']
    };
    const з=ЗАГОЛОВКИ[f]||['Деление дробей','Обратные числа'];
    el.innerHTML = `<div class="s6 l1050" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r1050Разминка=(к)=>{ S().разминка=к; chRender(0); };
  window.r1050Смысл=(к)=>{ S().смысл=к; chRender(0); };
  window.r1050Обрат=(к)=>{ S().обратные=к; chRender(0); };
  window.r1050Я=(к)=>{ S().я=к; chRender(0); };
  window.r1050Мы=(к)=>{ S().мы=к; chRender(0); };
  window.r1050Ты=(к)=>{ S().ты=к; chRender(0); };
  window.r1050Поч=(к)=>{ S().почему=к; chRender(0); };
  window.r1050Лов1=(к)=>{ S().ловушка1=к; chRender(0); };
  window.r1050Лов2=(к)=>{ S().ловушка2=к; chRender(0); };
  window.r1050Смеш=(к)=>{ S().смешанные=к; chRender(0); };
  window.r1050Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r1050Pick=(уровень,вариант)=>{
    const s=S();
    const порядок=сдвиг(УРОВНИ[уровень].варианты, уровень+1);
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(порядок[вариант] && порядок[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r1050Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r1050T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    const порядок=сдвиг(з.о.map((о,к)=>({о,к})), шаг+1).map(х=>х.о);
    const верный=порядок.indexOf(з.о[з.в]);
    s[ключ+'Ответ']=вариант;
    if(вариант===верный) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r1050TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L1050; else arr.push(L1050);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA1050={render:render, L:L1050};
})();
