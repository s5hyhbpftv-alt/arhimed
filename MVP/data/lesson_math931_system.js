/* ============ МАТЕМАТИКА · УРОК 931 · «НАТУРАЛЬНЫЕ ЧИСЛА: СИСТЕМЫ СЧИСЛЕНИЯ» ============
   5 класс. Сюжет: склад — считаем товар пучками, коробками и ящиками.

   СВЕРКА С ПЕТЕРСОНОМ (5 класс, глава «Натуральные числа»):
     • десятичная система счисления, разряды и классы;
     • запись и чтение многозначных чисел, сравнение чисел;
     • разрядные слагаемые; позиционность: значение цифры зависит от места.
   Источники в репозитории: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   КАКИЕ ТЕХНИКИ ОБУЧЕНИЯ ПРИМЕНЕНЫ (и почему):
     1. Конкретное → модель → символ (CRA). Сначала пучки и коробки на складе,
        потом разрядная таблица, и только затем запись числа цифрами.
     2. Обмен десятками как главное действие темы: 10 палочек → 1 пучок,
        10 пучков → 1 коробка. Обмен показан анимацией, а не словами.
     3. Вариативность: одна и та же цифра в разных разрядах — разное значение.
        Меняем ровно одно: место цифры.
     4. Обучение на ошибках: ноль внутри числа, ноль на конце, «пустой» разряд.
        Неверные варианты — это реальные детские ошибки, а не случайный вздор.
     5. Проговаривание вслух: чтение числа по классам, счёт разрядов.
     6. Показ решения (worked example) → неполное решение, которое ребёнок
        заканчивает сам: в практике уровни устроены так.
     7. Немедленный разбор после каждого ответа и счёт верных — формирующее
        оценивание.
     8. Подписной элемент темы один: разрядная таблица (классы-этажи,
        разряды-комнаты). Она повторяется только внутри этой темы.

   ДВЕНАДЦАТЬ КАДРОВ, у каждого своя композиция и свой приём:
     1. склад — почему считают десятками;        2. десяток: обмен 10 → 1;
     3. сотня и тысяча — коробки и ящики;        4. разрядная таблица;
     5. значение цифры зависит от места;         6. классы: этажи таблицы;
     7. чтение числа по классам;                 8. ноль внутри и на конце;
     9. разрядные слагаемые;                    10. сравнение по разрядам;
    11. практика — пять уровней;                12. тренажёр — четыре уровня.

   АНИМАЦИЯ ОБЪЯСНЯЕТ СМЫСЛ: палочки съезжаются в пучок (обмен), цифра
   переезжает из разряда в разряд и меняет значение, нули встают на пустые
   разряды по очереди. При prefers-reduced-motion анимаций нет вовсе —
   кадр сразу показывает конечное состояние.

   СТАНДАРТ: кегли 12/14/16/20/24/32, основной текст 20, подпись 16, цели
   от 48 px, движение только вертикальное или вдоль оси, кривые
   cubic-bezier(.23,1,.32,1), проверка на 390/768/1200 px. */
(function(){
  'use strict';

  const ID = 931;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';

  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);

  const CSS=`
  #lvis .s6.l931{gap:14px}
  #lvis .s6.l931 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l931 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l931 .герой{width:100%;display:flex;gap:12px;align-items:flex-start;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l931 .герой .лицо{flex:none;width:64px;height:64px}
  #lvis .s6.l931 .герой .лицо svg{width:100%;height:100%;display:block}
  #lvis .s6.l931 .герой .речь{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l931 .герой .приём{margin-top:6px;font-size:16px;line-height:1.4;color:var(--mut)}
  #lvis .s6.l931 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l931 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l931 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l931 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l931 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l931 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l931 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l931 .правило b{color:${GOLD}}
  #lvis .s6.l931 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l931 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l931 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l931 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l931 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l931 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l931 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l931 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l931 .уровни .точка.сейчас{background:${GOLD};animation:l931dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l931dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l931 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l931 [data-anim]{animation:l931rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l931rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l931 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l931 [data-anim]{animation:none!important}
    #lvis .s6.l931 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l931 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l931-style');
      if(!s){ s=document.createElement('style'); s.id='l931-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';

  /* Невидимых состояний нет: всё либо пульсирует, либо едет, но остаётся видимым. */
  const пульс = (длит,нач) => ан('opacity','1;0.35;1',длит, нач!=null?'begin="'+нач+'s"':'');
  const появление = (длит,нач) => ан('opacity','1;1;0.3;1',длит,'keyTimes="0;0.55;0.75;1"'+(нач!=null?' begin="'+нач+'s"':''));
  const кольцо = (x,y,r,цвет,длит,нач) =>
    `<circle cx="${x}" cy="${y}" r="${r}" fill="none" stroke="${цвет}" stroke-width="2">`+
      ан('opacity','0;0.9;0',длит,'keyTimes="0;0.5;1"'+(нач!=null?' begin="'+нач+'s"':''))+
      ан('r',r+';'+(r+9)+';'+r,длит,'keyTimes="0;0.5;1"'+(нач!=null?' begin="'+нач+'s"':''))+
    `</circle>`;
  const летит = (x0,y0,x1,y1,длит,нач) =>
    `<circle cx="${x0}" cy="${y0}" r="5" fill="${GOLD}">`+
      анТ('0 0;0 0;'+(x1-x0).toFixed(1)+' '+(y1-y0).toFixed(1)+';0 0',длит,
          'keyTimes="0;0.08;0.55;1"'+(нач!=null?' begin="'+нач+'s"':''))+
    `</circle>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c931-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22362c"/><stop offset="1" stop-color="#152219"/>
      </linearGradient>
      <linearGradient id="c931-палка" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#e2c48a"/><stop offset="1" stop-color="#a97f42"/>
      </linearGradient>
      <linearGradient id="c931-пучок" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#ffd76a"/><stop offset="1" stop-color="#c9971f"/>
      </linearGradient>
      <linearGradient id="c931-короб" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#7fd1ff"/><stop offset="1" stop-color="#3f86b8"/>
      </linearGradient>
      <linearGradient id="c931-ящик" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#a9e8c1"/><stop offset="1" stop-color="#4f9b76"/>
      </linearGradient>
      <filter id="c931-тень" x="-30%" y="-30%" width="160%" height="160%">
        <feGaussianBlur stdDeviation="2.2"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||186}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* палочка */
  const палка = (x,y,поворот) => `
    <g transform="rotate(${поворот||0} ${x} ${y})">
      <rect x="${x-2.2}" y="${y-22}" width="4.4" height="34" rx="2.2" fill="url(#c931-палка)" stroke="#5e4318" stroke-width="0.9"/>
    </g>`;

  /* пучок из десяти палочек */
  const пучок = (x,y) => `
    <g>
      <rect x="${x-13}" y="${y-24}" width="26" height="40" rx="7" fill="url(#c931-пучок)" stroke="#8a5f05" stroke-width="1.6"/>
      ${[0,1,2].map(i=>`<line x1="${x-8+i*8}" y1="${y-19}" x2="${x-8+i*8}" y2="${y+11}" stroke="#8a5f05" stroke-width="1.2" opacity=".55"/>`).join('')}
      ${т(x,y+4,'10',16,'#2a2118',true)}
    </g>`;

  /* коробка (сотня) и ящик (тысяча) */
  const короб = (x,y,подпись,цвет) => `
    <g>
      <rect x="${x-24}" y="${y-24}" width="48" height="44" rx="7" fill="${цвет}" opacity=".92" stroke="#245a80" stroke-width="1.6"/>
      <line x1="${x-24}" y1="${y-6}" x2="${x+24}" y2="${y-6}" stroke="#245a80" stroke-width="1.2" opacity=".6"/>
      ${т(x,y+4,подпись,16,'#0d2733',true)}
    </g>`;

  /* разрядная таблица: классы-этажи, разряды-комнаты */
  function таблица(x,y,классы,значения,подсветка,анимировать){
    /* классы: ['тысяч','единиц']; значения по три цифры на класс, слева направо */
    const шРазр=34, шКл=102;
    let s=`<rect x="${x}" y="${y}" width="${шКл}" height="86" rx="10" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
    let cx=x;
    классы.forEach((кл,клИ)=>{
      s+=`<rect x="${cx}" y="${y}" width="${66}" height="20" rx="6" fill="rgba(255,215,106,.14)" stroke="${GOLD}" stroke-width="1.2"/>`;
      s+=т(cx+шКл/2,y+15,кл,12,GOLD,true);
      ['сотни','десятки','единицы'].forEach((разр,рИ)=>{
        const значение=значения[клИ*3+рИ];
        const подсв=подсветка===клИ*3+рИ;
        s+=`<rect x="${cx+рИ*шРазр}" y="${y+22}" width="${шРазр}" height="64" fill="${подсв?'rgba(127,209,255,.18)':'none'}"
          stroke="${ЛИНИЯ}" stroke-width="1"/>`;
        s+=т(cx+рИ*шРазр+шРазр/2,y+42,разр.slice(0,3),12,подсв?BLUE:МУТ);
        s+=`<g>${анимировать?пульс('6s',(клИ*3+рИ)*0.5):''}
          ${т(cx+рИ*шРазр+шРазр/2,y+72,значение,20,подсв?BLUE:ИНК,true)}</g>`;
      });
      cx+=шКл;
    });
    return s;
  }

  /* ================= КАДРЫ ================= */

  /* 1. Склад: почему десятками. Анимация — лестница упаковки:
        сначала пульсирует пучок, потом коробка, потом ящик. */
  function F1(s){
    const в = s.склад;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="200" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,22,'На складе много палочек',14,GOLD,true)}
      ${Array.from({length:10},(_,i)=>палка(40+i*11,68,0)).join('')}
      ${т(100,100,'считать по одной долго',12,МУТ)}
      <g>${пульс('6s',0)}${пучок(250,68)}</g>
      ${т(250,100,'один десяток',12,GOLD)}
      <g>${пульс('6s',2)}${короб(96,150,'100','url(#c931-короб)')}</g>
      ${т(96,186,'коробка — это 10 пучков',12,МУТ)}
      <g>${пульс('6s',4)}${короб(248,150,'1000','url(#c931-ящик)')}</g>
      ${т(248,186,'ящик — это 10 коробок',12,МУТ)}
    `,200);
    return ВОПРОС('Товара на складе очень много. Как считать быстрее и надёжнее?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='десятками'?'hit':(в?'miss':''), 'Считать десятками: десять палочек — один пучок', "r931Why('десятками')")}
        ${BTN(4, в==='поодной'?'miss':'', 'Считать по одной, но очень внимательно', "r931Why('поодной')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='десятками',
        в==='десятками' ? 'Верно: собрав десять палочек в пучок, ты считаешь пучками — их в десять раз меньше.'
                        : 'Нет: по одной считать можно, но на большом складе собьёшься. Люди договорились считать десятками.') : ЖДЁТ()) +
      ПРАВИЛО('Считать договорились <b>десятками</b>: десять единиц — один десяток.');
  }

  /* 2. Обмен: 10 → 1. Анимация осмысленная: палочки отсчитываются по одной,
        затем от них к пучку идёт переход — пучок вспыхивает кольцом. */
  function F2(s){
    const в = s.обмен;
    const палочки = Array.from({length:10},(_,i)=>{
      const x=56+i*9;
      return `<g>${ан('opacity','1;0.35;1','3.6s','begin="'+(i*0.3).toFixed(1)+'s"')}
        ${палка(x,92,0)}</g>`;
    }).join('');
    const рис = свг(`
      <rect x="0" y="0" width="336" height="196" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="196" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,22,'Десять палочек — это один пучок',14,GOLD,true)}
      ${палочки}
      ${т(96,132,'было 10 единиц',12,МУТ)}
      <g>${анТ('0 0;0 0;104 0;104 0;0 0','3.6s','keyTimes="0;0.18;0.5;0.82;1"')}
        <circle cx="176" cy="92" r="6" fill="${GOLD}" opacity=".9"/>
      </g>
      ${пучок(272,92)}
      <circle cx="272" cy="92" r="26" fill="none" stroke="${GOLD}" stroke-width="2">
        ${ан('opacity','0;0;0.9;0','3.6s','keyTimes="0;0.5;0.68;0.9"')}${ан('r','26;26;34;26','3.6s','keyTimes="0;0.5;0.68;0.9"')}
      </circle>
      ${т(272,140,'стало 1 десяток',12,GOLD)}
      ${т(168,178,'единиц не стало меньше — изменилась только запись',12,ИНК)}
    `,196);
    return ВОПРОС('Ты собрал десять палочек в пучок. Сколько теперь десятков?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='один'?'hit':(в?'miss':''), 'Один десяток — и ни одной свободной единицы', "r931Ten('один')")}
        ${BTN(4, в==='десять'?'miss':'', 'Десять десятков', "r931Ten('десять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='один',
        в==='один' ? 'Верно: десять единиц — это ровно один десяток, свободных единиц не остаётся.'
                   : 'Нет: десять палочек дают один десяток. Десять десятков получится из ста палочек.') : ЖДЁТ()) +
      ПРАВИЛО('<b>10 единиц = 1 десяток.</b> Такой обмен — основа десятичной записи.');
  }

  /* 3. Сотня и тысяча. Анимация — десять пучков по очереди уходят в коробку. */
  function F3(s){
    const в = s.сотня;
    const пучки=[];
    for(let ряд=0;ряд<2;ряд++) for(let i=0;i<5;i++){
      const x=40+i*34, y=64+ряд*40;
      пучки.push(`<g>${пульс('5s', (ряд*5+i)*0.35)}${пучок(x,y)}</g>`);
    }
    const полёт=[0,1].map(ряд=>[0,1,2,3,4].map(i=>{
      const x=40+i*34, y=64+ряд*40, н=(ряд*5+i);
      return летит(x,y,282,86,'5s',0.2+н*0.35);
    }).join('')).join('');
    const рис = свг(`
      <rect x="0" y="0" width="336" height="200" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,22,'Десять пучков собирают в коробку',14,GOLD,true)}
      ${пучки.join('')}
      ${т(108,150,'десять пучков — по сто палочек',12,МУТ)}
      ${т(232,86,'=',20,GOLD,true)}
      ${полёт}
      ${короб(282,86,'100','url(#c931-короб)')}
      ${кольцо(282,86,30,GOLD,'5s',4.2)}
      ${т(282,124,'одна сотня',12,GOLD)}
      ${т(168,186,'десять таких коробок — уже тысяча',12,ИНК)}
    `,200);
    return ВОПРОС('Десять коробок по сто палочек — сколько это палочек?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тысяча'?'hit':(в?'miss':''), 'Тысяча: 10 раз по 100 — это 1000', "r931Hund('тысяча')")}
        ${BTN(4, в==='сто'?'miss':'', 'Сто десять', "r931Hund('сто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тысяча',
        в==='тысяча' ? 'Верно: десять сотен — это тысяча. Так и появились знакомые 1000, 10 000, 100 000.'
                     : 'Нет: десять коробок по сто дают ровно тысячу: 10 · 100 = 1000.') : ЖДЁТ()) +
      ПРАВИЛО('<b>10 единиц = 1 десяток, 10 десятков = 1 сотня, 10 сотен = 1 тысяча.</b>');
  }

  /* 4. Разрядная таблица. Анимация — цифры встают в свои комнаты по очереди. */
  function F4(s){
    const в = s.таблица;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Разрядная таблица числа 4 305',14,GOLD,true)}
      ${таблица(66,38,['кл. тысяч','кл. единиц'],['4','3','0','5'], в==='тысячи'?0:(в==='единицы'?5:null), true)}
      ${кольцо(117,110,24,BLUE,'6s',2)}
      ${т(168,152,'каждая цифра стоит в своём разряде',12,МУТ)}
      ${т(168,176,'слева — старшие разряды, справа — младшие',12,МУТ)}
    `,190);
    return ВОПРОС('В числе 4 305 что означает цифра 3?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сотни'?'hit':(в?'miss':''), 'Три сотни: она стоит в разряде сотен', "r931Tab('сотни')")}
        ${BTN(4, в==='тройки'?'miss':'', 'Просто три, без разряда', "r931Tab('тройки')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сотни',
        в==='сотни' ? 'Верно: цифра стоит в разряде сотен класса единиц, значит это три сотни — 300.'
                    : 'Нет: у каждой цифры есть разряд. Тройка стоит в разряде сотен, значит это 300.') : ЖДЁТ()) +
      ПРАВИЛО('Значение цифры зависит от <b>разряда</b>: 3 в разряде сотен — это 300.');
  }

  /* 5. Вариативность: подсветка едет по строкам — одно число, разные разряды. */
  function F5(s){
    const в = s.позиция;
    const строки=[['3 000','тысячи'],['300','сотни'],['30','десятки'],['3','единицы']];
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Одна цифра — разные значения',14,GOLD,true)}
      ${строки.map(([ч,р],i)=>{
        const y=54+i*32;
        return `<g>
          <rect x="40" y="${y-20}" width="256" height="28" rx="8" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.2"/>
          ${т(96,y,ч,20,ИНК,true)}
          ${т(232,y,'разряд: '+р,12,МУТ)}
        </g>`;
      }).join('')}
      <g>${анТ('0 0;0 0;0 32;0 32;0 64;0 64;0 96;0 96;0 0','8s',
             'keyTimes="0;0.06;0.2;0.3;0.44;0.54;0.68;0.82;1"')}
        <rect x="36" y="30" width="264" height="32" rx="9" fill="rgba(127,209,255,.16)" stroke="${BLUE}" stroke-width="1.8"/>
      </g>
      ${т(168,182,'место цифры меняет её значение в десять раз',12,МУТ)}
    `,190);
    return ВОПРОС('Цифра 3 стоит в разряде десятков. Какое число она обозначает?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тридцать'?'hit':(в?'miss':''), '30: десятки в десять раз больше единиц', "r931Pos('тридцать')")}
        ${BTN(4, в==='три'?'miss':'', '3: цифра всегда значит саму себя', "r931Pos('три')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тридцать',
        в==='тридцать' ? 'Верно: в разряде десятков тройка значит тридцать. Каждый шаг влево увеличивает значение в десять раз.'
                      : 'Нет: место важно. В разряде десятков тройка — это 30, а не 3.') : ЖДЁТ()) +
      ПРАВИЛО('Каждый разряд слева в <b>десять раз</b> больше соседнего справа — потому система и называется десятичной.');
  }

  /* 6. Классы: этажи. Анимация — этажи вспыхивают снизу вверх: единицы, тысячи, миллионы. */
  function F6(s){
    const в = s.классы;
    const этажи=[['единиц','',152],['тысяч','',104],['миллионов','',56]];
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Классы — этажи таблицы',14,GOLD,true)}
      ${этажи.map(([н,_,y],i)=>{
        const нач=[0,2,4][i];
        return `<g>
          <rect x="30" y="${y-24}" width="276" height="38" rx="9" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
          <rect x="30" y="${y-24}" width="276" height="38" rx="9" fill="rgba(255,215,106,.14)" stroke="${GOLD}" stroke-width="2">
            ${ан('opacity','0;0.9;0','6s','keyTimes="0;0.4;0.8" begin="'+нач+'s"')}
          </rect>
          ${т(96,y+1,'класс '+н,16,ИНК,true)}
          ${т(258,y+1,'в нём 3 цифры',12,МУТ)}
        </g>`;
      }).join('')}
      ${т(168,184,'по три разряда в каждом классе',12,МУТ)}
    `,190);
    return ВОПРОС('Сколько разрядов в одном классе?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), 'Три: сотни, десятки, единицы', "r931Class('три')")}
        ${BTN(4, в==='десять'?'miss':'', 'Десять — по числу цифр', "r931Class('десять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: в каждом классе ровно три разряда — сотни, десятки и единицы. Классы идут по три цифры.'
                  : 'Нет: цифр десять, а разрядов в классе три. Классы читают тройками справа налево.') : ЖДЁТ()) +
      ПРАВИЛО('Класс — это <b>три разряда</b>: сотни, десятки, единицы. Классы: единиц, тысяч, миллионов.');
  }

  /* 7. Чтение по классам. Анимация — рамка чтения идёт слева направо. */
  function F7(s){
    const в = s.чтение;
    const классы=[['4','миллионов',60],['305','тысяч',168],['128','единиц',276]];
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Читаем по классам справа налево',14,GOLD,true)}
      ${т(168,74,'4 305 128',24,ИНК,true)}
      ${классы.map(([ц,кл,x])=>`
        <g>
          <rect x="${x-52}" y="96" width="104" height="42" rx="9" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
          ${т(x,124,ц,20,GOLD,true)}
          ${т(x,156,кл,12,МУТ)}
        </g>`).join('')}
      <g>${анТ('0 0;0 0;108 0;108 0;216 0;216 0;0 0','7s',
             'keyTimes="0;0.08;0.3;0.4;0.62;0.76;1"')}
        <rect x="8" y="94" width="104" height="46" rx="10" fill="none" stroke="${GOLD}" stroke-width="2.4"/>
      </g>
      ${т(168,184,'четыре миллиона триста пять тысяч сто двадцать восемь',12,МУТ)}
    `,190);
    return ВОПРОС('С какого класса начинают читать многозначное число?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='старшего'?'hit':(в?'miss':''), 'Со старшего класса — миллионов', "r931Read('старшего')")}
        ${BTN(4, в==='младшего'?'miss':'', 'С младшего класса — единиц', "r931Read('младшего')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='старшего',
        в==='старшего' ? 'Верно: читаем слева направо, то есть со старшего класса: миллионы, тысячи, единицы.'
                       : 'Нет: цифры разбивают справа, но читают слева — со старшего класса.') : ЖДЁТ()) +
      ПРАВИЛО('Разбивай число на классы <b>справа налево</b>, а читай — <b>слева направо</b>.');
  }

  /* 8. Ловушка: ноль держит разряд. Анимация — нули пульсируют по очереди. */
  function F8(s){
    const в = s.ноль;
    const строки=[['405','четыреста пять'],['4 005','четыре тысячи пять'],['4 050','четыре тысячи пятьдесят']];
    const цифры=(ч,нач)=>{
      /* рисуем по цифрам, чтобы нули могли пульсировать */
      let cx=46, и=0;
      return [...ч].map(симв=>{
        if(симв===' '){ cx+=8; return ''; }
        const этоНоль = симв==='0';
        const г=`<g>${этоНоль?пульс('5s',нач+(и++)*0.25):''}
          ${т(cx,0,симв,20,этоНоль?GOLD:BLUE,true)}</g>`;
        cx+=15; return г;
      }).join('');
    };
    const рис = свг(`
      <rect x="0" y="0" width="336" height="200" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Ноль держит пустой разряд',14,GOLD,true)}
      ${строки.map(([ч,сл],i)=>{
        const y=62+i*40;
        return `<g transform="translate(0 ${y})">
          <rect x="20" y="-26" width="296" height="38" rx="9" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.3"/>
          ${цифры(ч, i*1.2)}
          ${т(132,-1,сл,12,ИНК,false,'start')}
        </g>`;
      }).join('')}
      ${т(168,190,'без нуля пятёрка уедет в десятки — число изменится',12,МУТ)}
    `,200);
    return ВОПРОС('Как записать цифрами «четыре тысячи пять»?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='4005'?'hit':(в?'miss':''), '4 005: пустые разряды заполняем нулями', "r931Zero('4005')")}
        ${BTN(4, в==='45'?'miss':'', '45: лишние нули не нужны', "r931Zero('45')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='4005',
        в==='4005' ? 'Верно: четыре тысячи пишем в классе тысяч, а пять — в разряде единиц. Пустые разряды держат нули.'
                   : 'Нет: без нулей пятёрка попадёт в разряд десятков и получится сорок пять. Нужно 4 005.') : ЖДЁТ()) +
      ПРАВИЛО('Ноль <b>держит место</b> пустого разряда: 4 005 — это четыре тысячи пять.');
  }

  /* 9. Разрядные слагаемые */
  function F9(s){
    const в = s.слагаемые;
    const откр = s.разбор||0;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="186" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="186" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Разбираем число на разрядные слагаемые',14,GOLD,true)}
      ${таблица(66,38,['кл. тысяч','кл. единиц'],['4','3','0','5'],откр>0?0:null,true)}
      ${откр>0?т(168,148,'4 305 = 4 000 + 300 + 5',20,GOLD,true):т(168,148,'4 305 = ?',20,МУТ,true)}
      ${откр>0?[[83,110],[161,110],[277,110]].map(([x,y],i)=>
        летит(x,y, 70+i*98-x, 148-y, '5s', i*0.6)).join(''):''}
      ${откр>1?т(168,176,'ноль в разряде десятков — слагаемого нет',12,МУТ):т(168,176,'каждое слагаемое — своя цифра со своими нулями',12,МУТ)}
    `,186);
    if(откр>=2){
      return ВОПРОС('Число 4 305 разложили на разрядные слагаемые. Какое слагаемое пропущено?') +
        `<div class="pic">${рис}</div>` +
        РАЗБОР(true,'Верно: в разряде десятков стоит ноль, поэтому слагаемого с десятками нет — его не пишут.') +
        ПРАВИЛО('Разрядные слагаемые: <b>4 305 = 4 000 + 300 + 5</b>. Нулевое слагаемое не записывают.');
    }
    return ВОПРОС('Какое первое действие при разборе числа на разрядные слагаемые?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">${BTN(3,'', откр===0?'показать разряды числа':'показать разрядные слагаемые', 'r931Parts()')}</div>` +
      (откр>0 ? РАЗБОР(true,'Порядок простой: сначала назови разряд каждой цифры, потом запиши её со своими нулями и сложи.') : ЖДЁТ()) +
      ПРАВИЛО('Каждая цифра превращается в <b>своё слагаемое</b> с нужным числом нулей.');
  }

  /* 10. Сравнение по разрядам. Анимация — курсор идёт слева направо
        и останавливается на первом различающемся разряде. */
  function F10(s){
    const в = s.сравнение;
    const цифры=(ч,y,цвет)=>[...ч].map((симв,i)=>
      симв===' '?'' : т(96+i*26,y,симв,20,цвет,true)).join('');
    const рис = свг(`
      <rect x="0" y="0" width="336" height="196" fill="url(#c931-стена)"/>
      <rect x="0" y="0" width="336" height="196" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Сравниваем по старшим разрядам',14,GOLD,true)}
      ${цифры('4 305',78,ИНК)}
      ${цифры('4 350',124,ИНК)}
      ${т(60,86,'4 305',12,МУТ,false,'end')}
      ${т(60,132,'4 350',12,МУТ,false,'end')}
      <g>${анТ('0 0;0 0;26 0;26 0;52 0;52 0;0 0','7s','keyTimes="0;0.08;0.3;0.42;0.64;0.8;1"')}
        <path d="M96 44 L88 34 L104 34 Z" fill="${GOLD}"/>
      </g>
      <rect x="132" y="56" width="30" height="86" rx="8" fill="rgba(255,215,106,.16)" stroke="${GOLD}" stroke-width="1.8">
        ${ан('opacity','0.35;1;0.35','3.4s')}
      </rect>
      ${т(168,164,'сотни равны, а десятки 0 и 5 — здесь числа и разошлись',12,ИНК)}
      ${т(168,186,'7 004 > 6 999: тысяч 7 > 6, дальше смотреть не нужно',12,МУТ)}
    `,196);
    return ВОПРОС('Какое число больше: 7 004 или 6 999?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='первое'?'hit':(в?'miss':''), '7 004: сравниваем с самого старшего разряда', "r931Cmp('первое')")}
        ${BTN(4, в==='второе'?'miss':'', '6 999: у него больше цифр 9', "r931Cmp('второе')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='первое',
        в==='первое' ? 'Верно: тысяч семь против шести — этого уже достаточно. Младшие разряды не смотрим.'
                     : 'Нет: девятки в младших разрядах ничего не решают. Сравнение начинают со старшего разряда.') : ЖДЁТ()) +
      ПРАВИЛО('Сравнение идёт <b>со старшего разряда</b>: не совпал — дальше смотреть не нужно.');
  }

  /* 11. Практика: пять уровней */
  const УРОВНИ=[
    { вопрос:'Сколько десятков в числе 250?', варианты:[
        {т:'25 десятков', ок:true,  почему:'250 = 25 · 10, значит в нём ровно двадцать пять десятков.'},
        {т:'2 десятка', ок:false, почему:'Двойка стоит в разряде сотен — это две сотни, а десятков 25.'}]},
    { вопрос:'Что означает цифра 8 в числе 6 814?', варианты:[
        {т:'8 сотен', ок:true,  почему:'Восьмёрка стоит в разряде сотен класса единиц — это 800.'},
        {т:'8 тысяч', ок:false, почему:'В разряде тысяч стоит шестёрка, а восьмёрка — в сотнях.'}]},
    { вопрос:'Как записать цифрами «три тысячи семь»?', варианты:[
        {т:'3 007', ок:true,  почему:'Три тысячи и семь единиц; пустые сотни и десятки держат нули.'},
        {т:'37', ок:false, почему:'Без нулей семёрка попадёт в разряд десятков — получится тридцать семь.'}]},
    { вопрос:'Разложи 5 402 на разрядные слагаемые.', варианты:[
        {т:'5 000 + 400 + 2', ок:true,  почему:'Пять тысяч, четыре сотни и две единицы; нулевое слагаемое не пишут.'},
        {т:'5 000 + 400 + 0 + 2', ок:false, почему:'Ноль слагаемым не записывают — он ничего не добавляет.'}]},
    { вопрос:'Какое число больше: 3 099 или 3 100?', варианты:[
        {т:'3 100', ок:true,  почему:'Тысячи и сотни равны, а десятков больше у 3 100: 9 против 0.'},
        {т:'3 099', ок:false, почему:'Больше цифр девяток — не значит больше число. Сравниваем по разрядам.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало решать?') +
        РАЗБОР(true,'Разрядная таблица: каждая цифра знает своё место, а пустое место держит ноль.') +
        `<div class="ask">${BTN(4,'','пройти заново','r931Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r931Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Определи разряд цифры — и ответ станет очевидным. Пустой разряд держит <b>ноль</b>.');
  }

  /* 12. Тренажёр */
  const ТРЕНАЖЁР=[
    { ф:'Сколько разрядов в одном классе?', о:['три','десять','два'], в:0, р:'в классе три разряда: сотни, десятки, единицы.' },
    { ф:'Что означает цифра 7 в числе 3 725?', о:['7 сотен','7 десятков','7 тысяч'], в:0,
      р:'семёрка стоит в разряде сотен — это 700.' },
    { ф:'Как записать «девять тысяч двадцать»?', о:['9 020','9 200','920'], в:0,
      р:'девять тысяч, два десятка; сотни и единицы держат нули.' },
    { ф:'Какое число больше: 5 908 или 5 890?', о:['5 908','5 890','равны'], в:0,
      р:'тысячи и сотни равны, десятков больше у 5 908.' }
  ];
  function F12(s){
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length, з=ТРЕНАЖЁР[и], отв=s.тОтвет, готово=отв!=null, верно=отв===з.в;
    return ТОЧКИ(ТРЕНАЖЁР.length, и, и) +
      A(2,'cap','Уровень '+(и+1)+' из '+ТРЕНАЖЁР.length) +
      ВОПРОС(з.ф) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(3+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r931Train(${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${s.тВерно||0} · ошибок: ${s.тОшибки||0} · всего: ${ТРЕНАЖЁР.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий уровень','r931NextQ()')}</div>` : '');
  }

  const L931={
    id: ID, title:'Натуральные числа: системы счисления', ico:'🔢',
    src:'Математика · 5 класс · Натуральные числа', subj:'math',
    explain: [
      'На складе тысячи мелких палочек. Если считать по одной, легко сбиться, поэтому люди договорились упаковывать: десять палочек — пучок, десять пучков — коробка, десять коробок — ящик. Так считать быстрее: вместо тысячи палочек перед нами один ящик. Эта привычка упаковывать по десять и лежит в основе десятичной системы счисления — той, которой мы пользуемся каждый день.',

      'Главное действие темы — обмен. Десять единиц можно обменять на один десяток, и наоборот. После обмена количество предметов не меняется: изменилась только запись. Именно поэтому в записи числа не бывает цифры 10 в одном разряде: набралось десять — переносим в следующий разряд. Частая ошибка — думать, что при обмене «стало меньше». Как проверить себя: посчитай палочки до обмена и после — их столько же.',

      'Дальше обмен продолжается: десять десятков — это сотня, десять сотен — тысяча, десять тысяч — десяток тысяч. Каждый следующий разряд ровно в десять раз больше соседнего справа. Отсюда и название — десятичная система. Частая ошибка — считать, что «сотня — это сто единиц, а не десять десятков»: верно и то, и другое, потому что сто и есть десять десятков. Как проверить себя: переведи соседний разряд в младший и сравни.',

      'Разрядная таблица — главный инструмент темы. В ней у каждой цифры своя комната, а комнаты объединены в разряды: сотни, десятки, единицы. Записывая число, мы просто расставляем цифры по комнатам. Число 4 305 читается по таблице так: четыре в разряде тысяч, три в сотнях, ноль в десятках, пять в единицах. Частая ошибка — читать цифры подряд и не смотреть на разряд. Как проверить себя: назови разряд каждой цифры по очереди.',

      'Значение цифры зависит от места. Одна и та же тройка может значить три тысячи, триста, тридцать или три — смотря где она стоит. Каждый шаг влево увеличивает значение в десять раз. Частая ошибка — считать, что цифра «всегда значит саму себя». Как проверить себя: подпиши над каждой цифрой её разряд и прочитай число разрядными слагаемыми.',

      'Разряды объединены в классы по три: класс единиц, класс тысяч, класс миллионов, класс миллиардов. Класс — это как этаж дома числа. В каждом классе ровно три разряда: сотни, десятки, единицы. Частая ошибка — путать разряд и класс: разрядов много, а в классе их всегда три. Как проверить себя: разбей число на тройки справа и назови каждый класс.',

      'Читают многозначные числа по классам. Сначала разбивают цифры на тройки справа налево, а потом читают слева направо, называя класс. 4 305 128 — это четыре миллиона триста пять тысяч сто двадцать восемь. Частая ошибка — читать слева направо, не разбив на классы, и терять, где кончаются тысячи. Как проверить себя: поставь пробелы между классами и прочитай вслух.',

      'Ноль — особенная цифра: сам по себе он не значит ничего, но держит пустой разряд. Без нулей пятёрка в числе 4 005 уехала бы в разряд десятков, и получилось бы сорок пять. Сравни три записи: 405 — четыреста пять, 4 005 — четыре тысячи пять, 4 050 — четыре тысячи пятьдесят. Частая ошибка — выбрасывать нули «как лишние». Как проверить себя: прочитай запись вслух и посмотри, то ли число получилось.',

      'Любое число можно разложить на разрядные слагаемые: 4 305 = 4 000 + 300 + 5. Каждая цифра превращается в слагаемое со своим числом нулей, а нулевые слагаемые не пишут. Частая ошибка — писать «+ 0» для пустого разряда или терять нули. Как проверить себя: сложи слагаемые и получи исходное число.',

      'Сравнивают числа по старшим разрядам. Сначала тысячи, потом сотни, потом десятки и единицы: как только разряды не совпали, ответ готов. 7 004 больше, чем 6 999, хотя девяток во втором числе больше. Частая ошибка — сравнивать по количеству «больших цифр» или по последней цифре. Как проверить себя: выпиши числа в разрядную таблицу и сравнивай по столбцам слева направо.',

      'Проверим на пяти числах: сколько десятков в 250, что значит 8 в 6 814, как записать «три тысячи семь», как разложить 5 402 и что больше — 3 099 или 3 100. Перед каждым ответом называй разряд. Дальше — тренажёр со счётом.',

      'Соберём главное. Десятичная система — это упаковка по десять: 10 единиц дают десяток, 10 десятков — сотню, 10 сотен — тысячу. Значение цифры зависит от разряда, разряды объединены в классы по три, ноль держит пустой разряд, а число можно разложить на разрядные слагаемые и сравнить по старшим разрядам.'
    ],
    check: {
      q:'Что означает цифра 8 в числе 6 814?',
      choices:['8 сотен','8 тысяч','8 десятков'],
      ans:0,
      exp:'Восьмёрка стоит в разряде сотен класса единиц — значит, это 800.'
    },
    tasks: [
      { q:'Сколько десятков в числе 250? Запиши ответ числом.', kind:'unit', ans:25, tol:0,
        hints:['Раздели число на 10.','250 = 25 · 10.'],
        sol:'25 десятков.' },
      { q:'Как записать цифрами «три тысячи семь»?', kind:'choice',
        choices:['3 007','37','3 700'], ans:0,
        hints:['Пустые разряды держат нули.','Три тысячи и семь единиц.'],
        sol:'3 007.' },
      { q:'Разложи 5 402 на разрядные слагаемые.', kind:'choice',
        choices:['5 000 + 400 + 2','5 000 + 400 + 0 + 2','5 000 + 40 + 2'], ans:0,
        hints:['Нулевое слагаемое не пишут.','Сотни — 400, десятков нет.'],
        sol:'5 402 = 5 000 + 400 + 2.' },
      { q:'Какое число больше: 3 099 или 3 100?', kind:'choice',
        choices:['3 100','3 099','они равны'], ans:0,
        hints:['Сравнивай по старшим разрядам.','Тысячи и сотни равны, сравни десятки.'],
        sol:'3 100 больше: тысячи и сотни равны, а десятков у него десять против девяти.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L931.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else сцена=F12(s);

    const ЗАГОЛОВКИ={
      1:['Склад','Зачем считать десятками'],
      2:['Обмен','10 единиц — 1 десяток'],
      3:['Разряды растут','Сотня и тысяча'],
      4:['Таблица','Дом числа'],
      5:['Место цифры','Значение зависит от разряда'],
      6:['Классы','Этажи по три разряда'],
      7:['Чтение','Тройки справа, чтение слева'],
      8:['Ноль','Держит пустой разряд'],
      9:['Слагаемые','Разбираем 4 305'],
      10:['Сравнение','Со старшего разряда'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр','Проверь себя']
    };
    const з=ЗАГОЛОВКИ[f]||['Система счисления','Десятичная запись'];
    el.innerHTML = `<div class="s6 l931" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r931Why=(к)=>{ S().склад=к; chRender(0); };
  window.r931Ten=(к)=>{ S().обмен=к; chRender(0); };
  window.r931Hund=(к)=>{ S().сотня=к; chRender(0); };
  window.r931Tab=(к)=>{ S().таблица=к; chRender(0); };
  window.r931Pos=(к)=>{ S().позиция=к; chRender(0); };
  window.r931Class=(к)=>{ S().классы=к; chRender(0); };
  window.r931Read=(к)=>{ S().чтение=к; chRender(0); };
  window.r931Zero=(к)=>{ S().ноль=к; chRender(0); };
  window.r931Parts=()=>{ const s=S(); s.разбор=(s.разбор||0)>=2?0:(s.разбор||0)+1; chRender(0); };
  window.r931Cmp=(к)=>{ S().сравнение=к; chRender(0); };
  window.r931Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r931Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r931Train=(к)=>{
    const s=S();
    if(s.тОтвет!=null) return;
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length;
    s.тОтвет=к;
    if(к===ТРЕНАЖЁР[и].в) s.тВерно=(s.тВерно||0)+1; else s.тОшибки=(s.тОшибки||0)+1;
    chRender(0);
  };
  window.r931NextQ=()=>{ const s=S(); s.тНомер=((s.тНомер||0)+1)%ТРЕНАЖЁР.length; s.тОтвет=null; chRender(0); };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L931; else arr.push(L931);
  })();
  window.MA931={render:render, L:L931};
})();
