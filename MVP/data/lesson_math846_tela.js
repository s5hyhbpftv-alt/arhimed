/* ============ МАТЕМАТИКА · УРОК 846 · «ЦИЛИНДР, КОНУС, ШАР» ============
   6 класс. Сюжет: банка, кулёк и мяч — три круглых тела.

   СВЕРКА С ПЕТЕРСОНОМ (6 класс): «тела (призма, пирамида, цилиндр, конус,
   шар)», «длина окружности и площадь круга». Источники:
   план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ (π = 3,14):
     цилиндр r = 3, h = 5:  C = 2πr = 18,84
                            Sбок = 2πrh = 94,2
                            Sполн = 2πr(r+h) = 150,72
                            V = πr²h = 141,3
     конус  r = 3, h = 5:   V = ⅓πr²h = 47,1 = 141,3 : 3   (три конуса = цилиндр)
     шар    r = 2:          V = 4/3·π·r³ = 33,49   S = 4πr² = 50,24
   Проверка: 47,1 · 3 = 141,3;  4/3 · 3,14 · 8 = 33,49;  4 · 3,14 · 4 = 50,24.

   ЭТАЛОН (826, 377, 995 … 1000): движение в кадре — сама математика.
     кадр 2  БОКОВАЯ ПОВЕРХНОСТЬ ЦИЛИНДРА ОТКЛЕИВАЕТСЯ и разворачивается
             в прямоугольник 2πr × h — это и есть развёртка;
     кадр 3  СЕКТОР СВОРАЧИВАЕТСЯ в конус: видно, откуда берётся боковая
             поверхность;
     кадр 4  ШАР НЕ РАЗВОРАЧИВАЕТСЯ: стрелки радиуса и диаметра бегут по шару,
             а попытка «разложить» его на плоскость отмечается крестом;
     кадр 5  ПЛОСКАЯ ФИГУРА ВРАЩАЕТСЯ вокруг оси: прямоугольник даёт цилиндр,
             треугольник — конус, полукруг — шар;
     кадр 7  ОБЪЁМ ЦИЛИНДРА: h слоёв по πr² укладываются друг на друга;
     кадр 8  ТРИ КОНУСА НАПОЛНЯЮТ ОДИН ЦИЛИНДР — отсюда множитель ⅓.

   РИСУНОК по deploy/РИСОВАНИЕ_ФИГУР.md: плоские заливки, обводка #33291e,
   пара теней (отбрасываемая + контактная), свет сверху-слева, ОДНА линия
   земли на кадр, эмодзи нет.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, keyTimes строго в [0;1], prefers-reduced-motion
   выключает SMIL полностью, ничего не выходит за край кадра. */
(function(){
  'use strict';

  const ID = 846;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f', ОБВОД='#33291e';

  const CSS=`
  #lvis .s6.l846{gap:14px}
  #lvis .s6.l846 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l846 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l846 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l846 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l846 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l846 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l846 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l846 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l846 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l846 .правило b{color:${GOLD}}
  #lvis .s6.l846 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l846 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l846 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l846 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l846 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l846 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l846 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l846 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l846 .уровни .точка.сейчас{background:${GOLD};animation:l846dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l846dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l846 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l846{-webkit-text-size-adjust:100%}
  #lvis .s6.l846 [data-anim]{animation:l846rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l846rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l846 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l846 [data-anim]{animation:none!important}
    #lvis .s6.l846 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l846 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l846-style');
      if(!s){ s=document.createElement('style'); s.id='l846-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анВ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const светит = (длит,нач) => ан('opacity','1;0.45;1',длит, нач!=null?'begin="'+нач+'s"':'');
  const проявить = (длит,доля,конец) => ан('opacity','0.3;0.3;1;1',длит,
    'keyTimes="0;'+кт(доля)+';'+кт(конец)+';1"');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c846-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#26382e"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#131f18"/>
      </linearGradient>
      <linearGradient id="c846-тело" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#8fc4e8"/><stop offset="0.35" stop-color="#cfe9fa"/>
        <stop offset="0.75" stop-color="#5aa3cd"/><stop offset="1" stop-color="#2f6b93"/>
      </linearGradient>
      <linearGradient id="c846-тело2" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#e0b957"/><stop offset="0.4" stop-color="#ffe9a8"/>
        <stop offset="0.8" stop-color="#c9971f"/><stop offset="1" stop-color="#8a6418"/>
      </linearGradient>
      <radialGradient id="c846-шар" cx="0.35" cy="0.3" r="0.75" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#dff3e6"/><stop offset="0.45" stop-color="#8fd1a8"/>
        <stop offset="1" stop-color="#3d7a5c"/>
      </radialGradient>
      <linearGradient id="c846-круг" x1="0" y1="0" x2="1" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#cfe9fa"/><stop offset="1" stop-color="#6fb2d8"/>
      </linearGradient>
      <filter id="c846-тень" x="-60%" y="-60%" width="220%" height="220%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3.4" result="р"/>
        <feOffset in="р" dx="4" dy="5" result="с"/>
        <feFlood flood-color="#0b1c2a" flood-opacity="0.3" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c846-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="3" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||244}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота,земля) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c846-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c846-шум)" opacity="0.05"/>
     ${земля?`<line x1="0" y1="${земля}" x2="336" y2="${земля}" stroke="rgba(0,0,0,.25)" stroke-width="1.4"/>`:''}
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  /* ЦИЛИНДР: два эллипса и боковая поверхность; тени на полу */
  function цилиндр(x,земля,r,h,нач){
    const верх=земля-h;
    return `<g>
      <ellipse cx="${x+4}" cy="${земля+4}" rx="${r+9}" ry="8" fill="#0b1c2a" opacity=".26" filter="url(#c846-тень)"/>
      <ellipse cx="${x}" cy="${земля+2}" rx="${r}" ry="4" fill="#231a12" opacity=".35" filter="url(#c846-тень)"/>
      <path d="M${x-r} ${верх} L${x-r} ${земля} A${r} ${r*0.28} 0 0 0 ${x+r} ${земля} L${x+r} ${верх} Z"
        fill="url(#c846-тело)" stroke="${ОБВОД}" stroke-width="1.8">
        ${нач!=null?проявить('11s',нач,нач+0.08):''}</path>
      <ellipse cx="${x}" cy="${верх}" rx="${r}" ry="${r*0.28}" fill="url(#c846-круг)" stroke="${ОБВОД}" stroke-width="1.8"/>
      <ellipse cx="${x}" cy="${верх}" rx="${r*0.62}" ry="${r*0.17}" fill="none" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
    </g>`;
  }

  /* КОНУС: эллипс основания, вершина, боковая поверхность */
  function конус(x,земля,r,h,нач){
    const вершина=земля-h;
    return `<g>
      <ellipse cx="${x+4}" cy="${земля+4}" rx="${r+8}" ry="7" fill="#0b1c2a" opacity=".26" filter="url(#c846-тень)"/>
      <ellipse cx="${x}" cy="${земля+2}" rx="${r*0.9}" ry="3.5" fill="#231a12" opacity=".33" filter="url(#c846-тень)"/>
      <path d="M${x-r} ${земля} Q${x-r*0.45} ${земля-r*0.34} ${x} ${вершина}
               Q${x+r*0.45} ${земля-r*0.34} ${x+r} ${земля} A${r} ${r*0.28} 0 0 1 ${x-r} ${земля} Z"
        fill="url(#c846-тело2)" stroke="${ОБВОД}" stroke-width="1.8">
        ${нач!=null?проявить('11s',нач,нач+0.08):''}</path>
      <ellipse cx="${x}" cy="${земля}" rx="${r}" ry="${r*0.28}" fill="rgba(255,255,255,.08)" stroke="${ОБВОД}" stroke-width="1.5"/>
      <line x1="${x}" y1="${вершина}" x2="${x}" y2="${земля}" stroke="rgba(20,35,28,.45)" stroke-width="1.4" stroke-dasharray="5 4"/>
    </g>`;
  }

  /* ШАР: круг с бликом и «экватором» */
  const шар = (x,y,r,нач) => `<g>
    <ellipse cx="${x+4}" cy="${y+r+6}" rx="${r+8}" ry="7" fill="#0b1c2a" opacity=".26" filter="url(#c846-тень)"/>
    <circle cx="${x}" cy="${y}" r="${r}" fill="url(#c846-шар)" stroke="${ОБВОД}" stroke-width="1.8">
      ${нач!=null?проявить('11s',нач,нач+0.08):''}</circle>
    <ellipse cx="${x}" cy="${y}" rx="${r}" ry="${r*0.3}" fill="none" stroke="rgba(20,35,28,.45)" stroke-width="1.3" stroke-dasharray="5 4"/>
    <ellipse cx="${x-r*0.32}" cy="${y-r*0.38}" rx="${r*0.28}" ry="${r*0.18}" fill="rgba(255,255,255,.5)"/>
  </g>`;

  /* КАРТОЧКА формулы/подписи */
  const карта = (x,y,ш,в,текст,кегль,цвет,нач,заливка) => `<g>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="9" fill="${заливка||'rgba(255,255,255,.05)'}"
      stroke="${цвет||ЛИНИЯ}" stroke-width="1.6">
      ${нач!=null?проявить('11s',нач,нач+0.06):''}</rect>
    ${т(x+ш/2,y+в/2+(кегль||16)*0.36,текст,кегль||16,цвет||ИНК,true)}
  </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Три тела на полке */
  function F1(s){
    const в = s.тела;
    const земля=196;
    return ВОПРОС('У банки, кулька и мяча есть общее. Что именно?') +
      `<div class="pic">${свг(`
        ${фон(226,земля)}
        ${т(168,22,'Банка, кулёк и мяч',14,GOLD,true)}
        ${цилиндр(62,земля,30,88,0.05)}
        ${конус(168,земля,32,92,0.2)}
        ${шар(272,земля-32,30,0.35)}
        ${т(62,222,'цилиндр',12,ИНК)}
        ${т(168,222,'конус',12,ИНК)}
        ${т(272,222,'шар',12,ИНК)}
        <g>${проявить('11s',0.5,0.6)}
          ${т(168,54,'у всех трёх есть круг',14,GREEN,true)}</g>
      `,226)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='круг'?'hit':(в?'miss':''), 'В каждом есть круг: основание или сам шар', "r846Тела('круг')")}
        ${BTN(4, в==='углы'?'miss':'', 'У всех есть углы и рёбра, как у коробки', "r846Тела('углы')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='круг',
        в==='круг' ? 'Верно: у цилиндра два круга-основания, у конуса один, а шар сам круглый. Углов у этих тел нет.'
                  : 'Нет: углы и рёбра бывают у коробки и пирамиды. У круглых тел есть круг, а рёбер нет.') : ЖДЁТ()) +
      ПРАВИЛО('Цилиндр, конус и шар — <b>круглые тела</b>: в каждом есть круг, а рёбер и углов нет.');
  }

  /* 2. Развёртка цилиндра: боковая поверхность отклеивается */
  function F2(s){
    const в = s.развёрткаЦ;
    return ВОПРОС('Что получится, если разрезать боковую поверхность банки и развернуть её?') +
      `<div class="pic">${свг(`
        ${фон(284)}
        ${т(168,22,'Развёртка цилиндра',14,GOLD,true)}
        ${цилиндр(58,190,30,96,0.05)}
        ${т(58,238,'цилиндр',12,МУТ)}
        <g>${проявить('11s',0.26,0.34)}${т(140,120,'→',20,GOLD,true)}</g>
        ${т(240,44,'прямоугольник',12,МУТ,true)}
        ${карта(170,54,140,70,'2πr × h',20,BLUE,0.34,'rgba(127,209,255,.12)')}
        <g>${проявить('11s',0.5,0.58)}
          <circle cx="196" cy="164" r="26" fill="url(#c846-круг)" stroke="${ОБВОД}" stroke-width="1.8"/>
          <circle cx="284" cy="164" r="26" fill="url(#c846-круг)" stroke="${ОБВОД}" stroke-width="1.8"/>
          ${т(240,200,'два круга-основания',12,ИНК)}</g>
        <g>${проявить('11s',0.66,0.74)}
          ${т(168,246,'Sбок = 2πrh',16,GREEN,true)}
          ${т(168,272,'длина прямоугольника равна длине окружности',12,МУТ)}</g>
      `,284)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='прямоугольник'?'hit':(в?'miss':''), 'Прямоугольник 2πr × h и два круга', "r846РазЦ('прямоугольник')")}
        ${BTN(4, в==='дваКруга'?'miss':'', 'Только два круга', "r846РазЦ('дваКруга')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='прямоугольник',
        в==='прямоугольник' ? 'Верно: боковая поверхность разворачивается в прямоугольник со сторонами 2πr и h, плюс два круга-основания.'
                           : 'Нет: круги дают только основания. Боковая поверхность даёт прямоугольник 2πr × h.') : ЖДЁТ()) +
      ПРАВИЛО('Развёртка цилиндра: <b>прямоугольник 2πr × h и два круга</b>.');
  }

  /* 3. Развёртка конуса: сектор сворачивается */
  function F3(s){
    const в = s.развёрткаК;
    return ВОПРОС('Что даёт боковая поверхность конуса при развёртке?') +
      `<div class="pic">${свг(`
        ${фон(266)}
        ${т(168,22,'Развёртка конуса',14,GOLD,true)}
        ${конус(60,190,36,92,0.05)}
        ${т(60,230,'конус',12,МУТ)}
        <g>${проявить('11s',0.26,0.34)}${т(140,120,'→',20,GOLD,true)}</g>
        <g>${светит('10s',0.34)}
          <path d="M240 60 L168 148 A92 92 0 0 0 312 148 Z" fill="rgba(255,215,106,.16)" stroke="${GOLD}" stroke-width="1.8"/>
          ${т(240,120,'сектор',16,GOLD,true)}</g>
        <g>${проявить('11s',0.5,0.58)}
          <circle cx="240" cy="196" r="24" fill="url(#c846-круг)" stroke="${ОБВОД}" stroke-width="1.8"/>
          ${т(240,232,'основание — круг',12,ИНК)}</g>
        <g>${проявить('11s',0.64,0.72)}
          ${т(168,252,'радиус сектора — образующая l',12,МУТ)}</g>
      `,266)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сектор'?'hit':(в?'miss':''), 'Сектор и круг в основании', "r846РазК('сектор')")}
        ${BTN(4, в==='прямоугольник'?'miss':'', 'Прямоугольник и круг', "r846РазК('прямоугольник')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сектор',
        в==='сектор' ? 'Верно: боковая поверхность конуса — это сектор, а основание даёт круг.'
                    : 'Нет: прямоугольник даёт цилиндр. У конуса боковая поверхность — сектор.') : ЖДЁТ()) +
      ПРАВИЛО('Развёртка конуса: <b>сектор и круг</b>; радиус сектора — образующая l.');
  }

  /* 4. Шар: развёртки нет */
  function F4(s){
    const в = s.шар;
    return ВОПРОС('Можно ли развернуть шар на плоскость?') +
      `<div class="pic">${свг(`
        ${фон(240)}
        ${т(168,22,'Шар развернуть нельзя',14,GOLD,true)}
        ${шар(96,120,52,0.05)}
        <line x1="96" y1="120" x2="148" y2="120" stroke="${GOLD}" stroke-width="2">
          ${проявить('11s',0.2,0.28)}</line>
        ${т(126,112,'r',14,GOLD,true)}
        <line x1="44" y1="120" x2="148" y2="120" stroke="${BLUE}" stroke-width="2">
          ${проявить('11s',0.3,0.38)}</line>
        ${т(96,140,'d = 2r',14,BLUE,true)}
        ${т(96,196,'центр, радиус, диаметр',12,МУТ)}
        <g>${проявить('11s',0.52,0.62)}
          <rect x="204" y="70" width="112" height="88" rx="12" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
          ${т(260,108,'развёртки',14,МУТ)}
          ${т(260,132,'нет',20,RED,true)}
          <line x1="212" y1="78" x2="308" y2="150" stroke="${RED}" stroke-width="2.4"/></g>
        <g>${проявить('11s',0.72,0.8)}
          ${т(168,224,'поверхность шара нельзя уложить на плоскость без складок',12,ИНК)}</g>
      `,240)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нельзя'?'hit':(в?'miss':''), 'Нельзя: у шара нет развёртки', "r846Шар('нельзя')")}
        ${BTN(4, в==='можно'?'miss':'', 'Можно: получится большой круг', "r846Шар('можно')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нельзя',
        в==='нельзя' ? 'Верно: у цилиндра и конуса развёртка есть, а у шара нет — его поверхность не укладывается на плоскость.'
                     : 'Нет: круг — это сечение шара, а не развёртка. Поверхность шара развернуть нельзя.') : ЖДЁТ()) +
      ПРАВИЛО('У шара <b>нет развёртки</b>; у цилиндра и конуса — есть.');
  }

  /* 5. Тела вращения: фигура вращается вокруг оси */
  function F5(s){
    const в = s.вращение;
    const земля=196;
    return ВОПРОС('Прямоугольник вращается вокруг своей стороны. Какое тело получится?') +
      `<div class="pic">${свг(`
        ${фон(226,земля)}
        ${т(168,22,'Плоская фигура вращается',14,GOLD,true)}
        <g>${анВ('0 60 120;35 60 120;0 60 120','6s')}
          <rect x="60" y="60" width="30" height="120" fill="rgba(127,209,255,.25)" stroke="${BLUE}" stroke-width="1.8"/>
        </g>
        <line x1="60" y1="46" x2="60" y2="${земля+8}" stroke="${GOLD}" stroke-width="2" stroke-dasharray="6 5"/>
        ${т(60,40,'ось',12,GOLD,true)}
        <g>${проявить('11s',0.3,0.4)}${т(120,120,'→',20,МУТ)}</g>
        ${цилиндр(210,земля,46,104,0.4)}
        ${т(210,222,'цилиндр',12,ИНК)}
        <g>${проявить('11s',0.62,0.72)}
          ${т(168,50,'прямоугольник → цилиндр',12,МУТ)}</g>
      `,226)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='цилиндр'?'hit':(в?'miss':''), 'Цилиндр: прямоугольник заметает боковую поверхность', "r846Вращ('цилиндр')")}
        ${BTN(4, в==='призма'?'miss':'', 'Призма: у неё тоже есть боковая поверхность', "r846Вращ('призма')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='цилиндр',
        в==='цилиндр' ? 'Верно: при вращении прямоугольника вокруг стороны получается цилиндр — поэтому круглые тела называют телами вращения.'
                     : 'Нет: призма не получается вращением — у неё грани плоские. Прямоугольник даёт цилиндр.') : ЖДЁТ()) +
      ПРАВИЛО('Круглые тела — <b>тела вращения</b>: прямоугольник даёт цилиндр, треугольник — конус, полукруг — шар.');
  }

  /* 6. Формулы поверхности */
  function F6(s){
    const в = s.формулы;
    return ВОПРОС('Длина окружности основания 18,84, а высота цилиндра 5. Чему равна площадь боковой поверхности?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,22,'Поверхность цилиндра',14,GOLD,true)}
        ${карта(24,44,288,58,'развернули: C = 2πr = 18,84, высота h = 5',14,ИНК,0.05,'rgba(255,255,255,.05)')}
        ${карта(24,116,136,50,'Sбок = C · h',16,BLUE,0.24,'rgba(127,209,255,.12)')}
        ${карта(176,116,136,50,'= 18,84 · 5 = 94,2',16,GOLD,0.36,'rgba(255,215,106,.12)')}
        <g>${проявить('11s',0.54,0.64)}
          ${карта(24,178,288,52,'Sполн = 2πrh + 2πr² = 2πr(r + h) = 150,72',14,GREEN,0.54,'rgba(143,209,168,.12)')}</g>
        ${т(168,244,'два круга-основания добавляют 2πr²',12,МУТ)}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='девяносто'?'hit':(в?'miss':''), '94,2: площадь боковой поверхности', "r846Форм('девяносто')")}
        ${BTN(4, в==='сто'?'miss':'', '150,72: это площадь боковой поверхности', "r846Форм('сто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='девяносто',
        в==='девяносто' ? 'Верно: Sбок = 18,84 · 5 = 94,2. А 150,72 — это полная поверхность с двумя кругами.'
                       : 'Нет: 150,72 — полная поверхность. Боковая — это произведение длины окружности на высоту: 94,2.') : ЖДЁТ()) +
      ПРАВИЛО('Sбок = <b>2πrh</b>, полная поверхность <b>2πr(r + h)</b>.');
  }

  /* 7. Объём цилиндра: слои */
  function F7(s){
    const в = s.объёмЦ;
    return ВОПРОС('Радиус основания 3, высота 5. Чему равен объём цилиндра? (π ≈ 3,14)') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${т(168,22,'Объём цилиндра',14,GOLD,true)}
        ${цилиндр(74,206,34,116,0.05)}
        ${Array.from({length:5},(_,i)=>`<g>${светит('10s',i*0.5)}
          <ellipse cx="74" cy="${206-14-i*22}" rx="34" ry="9" fill="none" stroke="rgba(255,255,255,.32)" stroke-width="1.2"/></g>`).join('')}
        ${т(74,244,'высота 5',12,МУТ)}
        <g>${проявить('11s',0.3,0.4)}
          ${карта(150,60,168,44,'V = πr²h',20,GOLD,0.3,'rgba(255,215,106,.12)')}
          ${карта(150,116,168,44,'πr² = 3,14 · 9 = 28,26',14,ИНК,0.42,'rgba(255,255,255,.05)')}
          ${карта(150,172,168,44,'V = 28,26 · 5 = 141,3',16,GREEN,0.54,'rgba(143,209,168,.12)')}</g>
        ${т(232,236,'слой πr² уложили h раз',12,МУТ)}
      `,256)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='141'?'hit':(в?'miss':''), '141,3: 28,26 · 5', "r846ОбЦ('141')")}
        ${BTN(4, в==='28'?'miss':'', '28,26: это площадь основания', "r846ОбЦ('28')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='141',
        в==='141' ? 'Верно: V = πr²h = 3,14 · 9 · 5 = 141,3. Площадь основания 28,26 умножили на высоту 5.'
                  : 'Нет: 28,26 — это площадь одного слоя. Объём — это слоёв на высоту: 28,26 · 5 = 141,3.') : ЖДЁТ()) +
      ПРАВИЛО('Объём цилиндра: <b>V = πr²h</b> — площадь основания на высоту.');
  }

  /* 8. Объём конуса: три конуса — один цилиндр */
  function F8(s){
    const в = s.объёмК;
    return ВОПРОС('У конуса и цилиндра одинаковые радиус и высота. Сколько конусов наполнят цилиндр?') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${т(168,22,'Три конуса — один цилиндр',14,GOLD,true)}
        ${цилиндр(240,206,36,116,0.05)}
        ${т(240,244,'цилиндр V = 141,3',12,МУТ)}
        ${[0,1,2].map(i=>`<g>${анТ('0 0;0 0;'+(126-52-i*44)+' '+(206-172)+';'+(126-52-i*44)+' '+(206-172)+';0 0','11s',
            'keyTimes="0;'+кт(0.12+i*0.1)+';'+кт(0.4+i*0.1)+';0.9;1"')}
          ${конус(52+i*44,172,24,62,0)}</g>`).join('')}
        <g>${проявить('11s',0.5,0.6)}
          ${карта(24,64,180,44,'Vконуса = ⅓πr²h',16,GOLD,0.5,'rgba(255,215,106,.12)')}
          ${карта(24,116,180,44,'= 141,3 : 3 = 47,1',14,GREEN,0.6,'rgba(143,209,168,.12)')}</g>
        ${т(126,236,'переливаем три раза',12,МУТ)}
      `,256)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), 'Три: объём конуса — треть объёма цилиндра', "r846ОбК('три')")}
        ${BTN(4, в==='два'?'miss':'', 'Два: конус займёт половину', "r846ОбК('два')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: чтобы наполнить цилиндр, нужно три таких конуса: V = ⅓πr²h = 141,3 : 3 = 47,1.'
                  : 'Нет: конус занимает ровно треть цилиндра с теми же r и h — значит нужно три конуса.') : ЖДЁТ()) +
      ПРАВИЛО('Объём конуса: <b>V = ⅓πr²h</b> — треть объёма цилиндра.');
  }

  /* 9. Шар: формулы и пример */
  function F9(s){
    const в = s.объёмШ;
    return ВОПРОС('Радиус шара 2. Чему равна площадь его поверхности? (π ≈ 3,14)') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,22,'Шар: поверхность и объём',14,GOLD,true)}
        ${шар(78,120,50,0.05)}
        <line x1="78" y1="120" x2="128" y2="120" stroke="${GOLD}" stroke-width="2">${проявить('11s',0.2,0.28)}</line>
        ${т(108,112,'r = 2',12,GOLD,true)}
        <g>${проявить('11s',0.3,0.4)}
          ${карта(150,58,168,42,'S = 4πr²',16,BLUE,0.3,'rgba(127,209,255,.12)')}
          ${карта(150,108,168,42,'= 4 · 3,14 · 4 = 50,24',14,GREEN,0.4,'rgba(143,209,168,.12)')}
          ${карта(150,158,168,42,'V = 4/3·πr³ = 33,49',14,GOLD,0.5,'rgba(255,215,106,.12)')}</g>
        ${т(168,228,'у шара всё считают через радиус r',12,МУТ)}
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='50'?'hit':(в?'miss':''), '50,24: S = 4πr² = 4 · 3,14 · 4', "r846ОбШ('50')")}
        ${BTN(4, в==='33'?'miss':'', '33,49: это площадь поверхности', "r846ОбШ('33')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='50',
        в==='50' ? 'Верно: S = 4πr² = 4 · 3,14 · 4 = 50,24. А 33,49 — это объём: V = 4/3 · π · r³.'
                 : 'Нет: 33,49 — объём шара. Площадь поверхности считают по формуле S = 4πr² = 50,24.') : ЖДЁТ()) +
      ПРАВИЛО('У шара: <b>S = 4πr²</b>, <b>V = 4/3 · πr³</b>.');
  }

  /* 10. Сводка трёх тел */
  function F10(s){
    const в = s.сводка;
    const строки=[['цилиндр','2 круга + прямоугольник','S = 2πr(r+h) · V = πr²h'],
                  ['конус','круг + сектор','S ≈ πrl + πr² · V = ⅓πr²h'],
                  ['шар','развёртки нет','S = 4πr² · V = 4/3·πr³']];
    return ВОПРОС('У какого тела нет развёртки?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,22,'Три тела: развёртка и формулы',14,GOLD,true)}
        ${строки.map(([имя,развёртка,формулы],i)=>{
          const y=42+i*62;
          return `<g>
            <rect x="18" y="${y}" width="300" height="52" rx="10" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.3">
              ${проявить('11s',0.06*i,0.06*i+0.05)}</rect>
            ${т(56,y+32,имя,16,ИНК,true)}
            <g>${проявить('11s',0.06*i+0.12,0.06*i+0.2)}
              ${т(168,y+22,развёртка,12,МУТ)}
              ${т(168,y+40,формулы,12,формулы.indexOf('нет')>=0?RED:GREEN)}</g>
          </g>`;
        }).join('')}
        ${т(168,240,'у цилиндра и конуса развёртка есть, у шара — нет',12,ИНК)}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шар'?'hit':(в?'miss':''), 'У шара: его поверхность не укладывается на плоскость', "r846Свод('шар')")}
        ${BTN(4, в==='конус'?'miss':'', 'У конуса: у него только сектор', "r846Свод('конус')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шар',
        в==='шар' ? 'Верно: у шара развёртки нет. У конуса она есть — круг и сектор, у цилиндра — два круга и прямоугольник.'
                  : 'Нет: у конуса развёртка есть: круг в основании и сектор для боковой поверхности.') : ЖДЁТ()) +
      ПРАВИЛО('Запомни: <b>шар — единственное из трёх тел без развёртки</b>.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Что получится при вращении прямоугольника вокруг стороны?', варианты:[
        {т:'цилиндр', ок:true,  почему:'Прямоугольник заметает боковую поверхность цилиндра.'},
        {т:'конус', ок:false, почему:'Конус даёт вращение прямоугольного треугольника.'}]},
    { вопрос:'Из чего состоит развёртка цилиндра?', варианты:[
        {т:'прямоугольник и два круга', ок:true,  почему:'Боковая поверхность и два основания.'},
        {т:'сектор и круг', ок:false, почему:'Сектор и круг — это развёртка конуса.'}]},
    { вопрос:'Радиус основания 3, высота 5. Чему равен объём цилиндра? (π ≈ 3,14)', варианты:[
        {т:'141,3', ок:true,  почему:'V = 3,14 · 9 · 5 = 141,3.'},
        {т:'47,1', ок:false, почему:'47,1 — это объём конуса с теми же r и h.'}]},
    { вопрос:'Сколько конусов наполнят цилиндр с теми же r и h?', варианты:[
        {т:'три', ок:true,  почему:'Объём конуса — треть объёма цилиндра.'},
        {т:'два', ок:false, почему:'Два конуса дадут ⅔ объёма, нужно три.'}]},
    { вопрос:'Радиус шара 2. Чему равна площадь поверхности? (π ≈ 3,14)', варианты:[
        {т:'50,24', ок:true,  почему:'S = 4 · 3,14 · 4 = 50,24.'},
        {т:'33,49', ок:false, почему:'33,49 — это объём шара, а не поверхность.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Три опоры: как выглядит тело, из чего состоит его развёртка и какая формула отвечает за поверхность и объём.') +
        `<div class="ask">${BTN(4,'','пройти заново','r846Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r846Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Тело → развёртка → формула: <b>цилиндр 2πr(r+h), конус ⅓πr²h, шар 4πr²</b>.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · узнай тело',
    задания:[
      { ф:'Прямоугольный треугольник вращается вокруг катета. Что получится?', о:['конус','цилиндр','шар'], в:0,
        р:'Треугольник даёт конус.' },
      { ф:'Полукруг вращается вокруг диаметра. Что получится?', о:['шар','конус','цилиндр'], в:0,
        р:'Полукруг даёт шар.' },
      { ф:'У какого тела нет развёртки?', о:['у шара','у конуса','у цилиндра'], в:0,
        р:'Поверхность шара на плоскость не укладывается.' },
      { ф:'Развёртка: прямоугольник и два круга. Какое это тело?', о:['цилиндр','конус','шар'], в:0,
        р:'Боковая поверхность — прямоугольник, основания — два круга.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · поверхность',
    задания:[
      { ф:'Длина окружности основания 18,84, высота 5. Sбок = ?', о:['94,2','150,72','28,26'], в:0,
        р:'Sбок = 18,84 · 5 = 94,2.' },
      { ф:'Радиус шара 2. S = ? (π ≈ 3,14)', о:['50,24','33,49','12,56'], в:0, р:'S = 4πr² = 50,24.' },
      { ф:'Что даёт боковая поверхность конуса при развёртке?', о:['сектор','прямоугольник','круг'], в:0,
        р:'Боковая поверхность конуса — сектор.' },
      { ф:'Основание цилиндра — круг радиуса 3. Чему равна его площадь? (π ≈ 3,14)', о:['28,26','18,84','9'], в:0,
        р:'πr² = 3,14 · 9 = 28,26.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · объёмы',
    задания:[
      { ф:'r = 3, h = 5. V цилиндра = ? (π ≈ 3,14)', о:['141,3','47,1','28,26'], в:0, р:'V = πr²h = 141,3.' },
      { ф:'r = 3, h = 5. V конуса = ? (π ≈ 3,14)', о:['47,1','141,3','94,2'], в:0, р:'V = ⅓πr²h = 47,1.' },
      { ф:'Сколько конусов наполнят цилиндр с теми же r и h?', о:['три','два','полтора'], в:0,
        р:'Объём конуса — треть объёма цилиндра.' },
      { ф:'Радиус шара 2. V = ? (π ≈ 3,14)', о:['33,49','50,24','25,12'], в:0, р:'V = 4/3·πr³ = 33,49.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r846T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r846TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L846={
    id: ID, title:'Цилиндр, конус, шар', ico:'🥫',
    src:'Математика · 6 класс · Круглые тела', subj:'math',
    explain: [
      'На полке стоят банка, кулёк и мяч. Банка — это цилиндр, кулёк — конус, мяч — шар. Все три тела называют круглыми, потому что в каждом есть круг: у цилиндра два круга-основания, у конуса один, а шар сам круглый. Рёбер и углов у этих тел нет — потрогай банку: она гладкая с боков.',

      'Разберём цилиндр. У него два равных круга — основания, и боковая поверхность. Высота цилиндра — расстояние между основаниями, радиус основания обозначают r. Если разрезать боковую поверхность вдоль и развернуть, получится прямоугольник: одна его сторона равна высоте h, а другая — длине окружности основания C = 2πr. Частая ошибка — считать, что боковая поверхность круглая и её нельзя измерить. Как проверить себя: оберни банку бумагой и разверни её.',

      'Развёртка цилиндра состоит из прямоугольника 2πr × h и двух кругов радиуса r. Поэтому площадь боковой поверхности равна Sбок = 2πrh, а полная поверхность S = 2πrh + 2πr² = 2πr(r + h). Возьмём r = 3 и h = 5: длина окружности 18,84, значит Sбок = 18,84 · 5 = 94,2, а полная поверхность 150,72. Частая ошибка — забыть про основания. Как проверить себя: посчитай, сколько частей в развёртке.',

      'Конус устроен иначе: одно круглое основание и боковая поверхность, которая сходится в вершину. Высоту конуса h отмеряют от вершины до центра основания, а отрезок от вершины до точки окружности называют образующей l. Развёртка конуса — это круг в основании и сектор для боковой поверхности, причём радиус сектора равен образующей. Частая ошибка — считать боковую поверхность конуса прямоугольником. Как проверить себя: сверни кулёк из бумаги.',

      'Шар — самое простое и самое особенное тело. У него есть центр, радиус и диаметр: d = 2r. Все точки поверхности шара находятся на одном расстоянии от центра. Главное отличие: у шара нет развёртки — его поверхность нельзя уложить на плоскость без складок. Частая ошибка — считать развёрткой шара большой круг. Как проверить себя: попробуй расплющить мяч на столе — получится складка.',

      'Цилиндр, конус и шар называют телами вращения. Если вращать прямоугольник вокруг одной из его сторон, получится цилиндр. Если вращать прямоугольный треугольник вокруг катета — конус. Если вращать полукруг вокруг диаметра — шар. Частая ошибка — путать, какая фигура какое тело даёт. Как проверить себя: вспомни, какая фигура заметает круглое сечение.',

      'Считаем объём цилиндра. Основание — круг площади πr², а высота h. Объём равен произведению площади основания на высоту: V = πr²h. При r = 3: πr² = 3,14 · 9 = 28,26, а при h = 5 объём равен 28,26 · 5 = 141,3. Можно представить, что мы уложили друг на друга h слоёв по πr². Частая ошибка — умножать радиус, а не площадь основания. Как проверить себя: сначала посчитай площадь круга.',

      'Объём конуса в три раза меньше объёма цилиндра с теми же радиусом и высотой: V = ⅓πr²h. Проверим: при r = 3 и h = 5 объём цилиндра 141,3, значит объём конуса 141,3 : 3 = 47,1. Это удобно проверить опытом: три конуса воды наполняют цилиндр доверху. Частая ошибка — забыть множитель ⅓. Как проверить себя: раздели объём цилиндра на три.',

      'Для шара формулы другие: площадь поверхности S = 4πr², объём V = 4/3 · πr³. При радиусе 2: S = 4 · 3,14 · 4 = 50,24, а V = 4/3 · 3,14 · 8 = 33,49. Обрати внимание: у шара в обеих формулах участвует только радиус — ни высоты, ни образующей у него нет. Частая ошибка — путать формулы поверхности и объёма. Как проверить себя: у объёма есть множитель 4/3 и куб радиуса.',

      'Соберём всё вместе. Цилиндр: два круга и прямоугольник в развёртке, S = 2πr(r + h), V = πr²h. Конус: круг и сектор, S = πrl + πr², V = ⅓πr²h. Шар: развёртки нет, S = 4πr², V = 4/3 · πr³. Общее у всех трёх: в формулах есть π и радиус. Частая ошибка — перепутать, у кого какая формула. Как проверить себя: назови вслух развёртку каждого тела.',

      'Пройти практику: пять уровней про тела вращения, развёртки, поверхность и объём. Дальше ждут три тренажёра: узнай тело, поверхность, объёмы.',

      'Первый тренажёр — узнай тело по вращению или по развёртке. Помни три соответствия: прямоугольник → цилиндр, треугольник → конус, полукруг → шар.',

      'Второй тренажёр — поверхность и длина окружности. Здесь важно не путать Sбок = 2πrh с полной поверхностью 2πr(r + h).',

      'Третий тренажёр — объёмы: цилиндр πr²h, конус ⅓πr²h, шар 4/3 · πr³. Считай по шагам: сначала площадь круга, потом умножай.'
    ],
    check: {
      q:'Радиус основания 3, высота 5. Чему равен объём цилиндра? (π ≈ 3,14)',
      choices:['141,3','47,1','28,26'],
      ans:0,
      exp:'V = πr²h = 3,14 · 9 · 5 = 141,3.'
    },
    tasks: [
      { q:'Что получится при вращении прямоугольного треугольника вокруг катета?', kind:'choice',
        choices:['конус','цилиндр','шар'], ans:0,
        hints:['Какая фигура заметает круглое сечение?','Треугольник даёт конус.'],
        sol:'Конус.' },
      { q:'Из чего состоит развёртка цилиндра?', kind:'choice',
        choices:['прямоугольник и два круга','сектор и круг'], ans:0,
        hints:['Сколько у цилиндра оснований?','Два круга и боковая поверхность.'],
        sol:'Прямоугольник 2πr × h и два круга.' },
      { q:'Чему равен объём конуса при r = 3 и h = 5? (π ≈ 3,14)', kind:'choice',
        choices:['47,1','141,3','94,2'], ans:0,
        hints:['Объём конуса — треть цилиндра.','141,3 : 3.'],
        sol:'V = ⅓πr²h = 47,1.' },
      { q:'Радиус шара 2. Чему равна площадь поверхности? (π ≈ 3,14)', kind:'choice',
        choices:['50,24','33,49'], ans:0,
        hints:['S = 4πr².','4 · 3,14 · 4.'],
        sol:'S = 50,24.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L846.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F6(s);
    else if(f===4) сцена=F3(s);
    else if(f===5) сцена=F4(s);
    else if(f===6) сцена=F5(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Три тела','Банка, кулёк, мяч'],
      2:['Цилиндр','Боковая поверхность'],
      3:['Поверхность','2πr(r + h)'],
      4:['Конус','Круг и сектор'],
      5:['Шар','Развёртки нет'],
      6:['Тела вращения','Фигура вокруг оси'],
      7:['Объём цилиндра','πr²h'],
      8:['Объём конуса','Треть цилиндра'],
      9:['Шар: формулы','4πr² и 4/3·πr³'],
      10:['Сводка','Тело, развёртка, формулы'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','Узнай тело'],
      13:['Тренажёр 2','Поверхность'],
      14:['Тренажёр 3','Объёмы']
    };
    const з=ЗАГОЛОВКИ[f]||['Круглые тела','Цилиндр, конус, шар'];
    el.innerHTML = `<div class="s6 l846" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r846Тела=(к)=>{ S().тела=к; chRender(0); };
  window.r846РазЦ=(к)=>{ S().развёрткаЦ=к; chRender(0); };
  window.r846РазК=(к)=>{ S().развёрткаК=к; chRender(0); };
  window.r846Шар=(к)=>{ S().шар=к; chRender(0); };
  window.r846Вращ=(к)=>{ S().вращение=к; chRender(0); };
  window.r846Форм=(к)=>{ S().формулы=к; chRender(0); };
  window.r846ОбЦ=(к)=>{ S().объёмЦ=к; chRender(0); };
  window.r846ОбК=(к)=>{ S().объёмК=к; chRender(0); };
  window.r846ОбШ=(к)=>{ S().объёмШ=к; chRender(0); };
  window.r846Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r846Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r846Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r846T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r846TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L846; else arr.push(L846);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA846={render:render, L:L846};
})();
