/* ============ МАТЕМАТИКА · 6 КЛАСС · УРОК 649 · «УМНОЖЕНИЕ И ДЕЛЕНИЕ РАЦИОНАЛЬНЫХ ЧИСЕЛ» ============

   Место в программе Петерсона. Шестой класс, школьная линия «Рациональные
   числа»: после координатной прямой и модуля идёт умножение и деление
   положительных и отрицательных чисел, и только потом — все действия с
   рациональными числами. На полке «Математика» уже стоят школьные уроки
   6 класса 193–199 (проценты, отношения, окружность, углы, уравнения),
   но правила знаков при умножении и делении среди них нет: grep по MVP/data
   не находит ни одного листа со словами «минус на минус», «правило знаков»,
   «(−4) · (−6)», «деление отрицательных», а замер по window.ARH_LESSONS в
   живом приложении это подтверждает. Тема в приложении новая.

   О номере. Задание владельца: id 649, проверить занятость. Проверено дважды.
   Первое — grep по MVP/data и MVP/js: под номерами 601–646 стоят листы
   (601–631 волны vis_cw/vis_dw/vis_ew/vis_kl/vis_23_393, 632–646 отдельные
   листы «Пути Мишутки», «Мастерских Сиракуз» и олимпиадные). Второе — замер
   window.ARH_LESSONS в живом приложении: всего 477 уроков, максимум 646,
   уроков 647, 648 и 649 нет, дублей id нет. Значит, 649 в момент работы
   свободен.

   Устройство — как у школьных уроков 6 класса (193–199), а не как у бумажного
   листа «Мастерских Сиракуз». Урок зарегистрирован в window.ARH_LESSONS без
   поля group: только subj:'math', поэтому он встаёт на полку «Математика»,
   а не на «Мастерские Сиракуз» и не на «Путь Мишутки» (subjOf() в
   MVP/js/lessons.js возвращает L.group, если поле есть, и L.subj иначе).
   Рисунок и интерактив рисует свой виджет window.VISKW[649] — тот же
   диспетчер, что у 193–199: состояние лежит в CHS, перерисовка через chRender.

   Усиленное объяснение — требование владельца, и оно сделано не общими
   словами, а в каждом из восьми заданий:
     • разбор по шагам «Шаг 1. Смотрим на знаки: минус на минус. Шаг 2. По
       правилу получаем плюс. Шаг 3. Умножаем модули: 4 · 6 = 24. Шаг 4.
       Проверка: 24 : (−6) = −4 — вернулись к исходному»;
     • «частая ошибка» — правильно посчитать модули и потерять знак; думать,
       что минус на минус даёт минус;
     • «как проверить себя» — обратным действием (умножение делением,
       деление умножением);
     • «второй способ» — через смысл, без правила («долг по 6 пять раз»);
     • таблица знаков — на умножение И на деление, отдельными кадрами;
     • блок «почему минус на минус даёт плюс» — на примере «убрать долг».

   Рисунки — вектор, своими функциями внутри листа: таблица знаков, числовая
   ось с прыжками, долг частями, сетка модулей, термометр-ступеньки, цепочка
   множителей. Эмодзи внутри SVG нет ни одного; клетки, стрелки, галочки и
   крестики нарисованы прямоугольниками, кругами и путями. Контраст пар
   «#e8e0cc на #0b1712» — 14,1 : 1, «#d9a441 на #0b1712» — 8,3 : 1,
   «#cbb89a на #0b1712» — 9,6 : 1, «#e86a5a на #0b1712» — 5,9 : 1,
   «#ffb3a7 на #1c3327» — 7,9 : 1, «#8fd1a8 на #1c3327» — 7,6 : 1 — всё выше
   порога 4,5 : 1.

   Кегли — только по шкале проекта: 12 · 14 · 16 · 20 · 24. Цели касания —
   не меньше 44 px, зазоры — не меньше 8 px.
*/
(function(){
  const ID = 649;
  if(window.VISKW && window.VISKW[ID]) return;      /* защита от двойного подключения */
  window.VISKW = window.VISKW || {};
  window.ARH_LESSONS = window.ARH_LESSONS || [];

  /* ================= ПАЛИТРА ПРОЕКТА ================= */
  const F = "Georgia,'Times New Roman',serif";
  const DEEP = '#0b1712', CARDB = '#1c3327', PANEL = '#14261d',
        GOLD = '#d9a441', IVORY = '#e8e0cc', MUTE = '#cbb89a',
        RED = '#e86a5a', REDT = '#ffb3a7', GREEN = '#8fd1a8',
        LINE = 'rgba(217,164,65,.62)', HAIR = 'rgba(217,164,65,.22)',
        FILL = 'rgba(217,164,65,.07)', FLAT = 'rgba(217,164,65,.18)',
        RFILL = 'rgba(232,106,90,.13)', GFILL = 'rgba(143,209,168,.13)';

  /* ================= CSS ЛИСТА =================
     Свой каркас, а не .wk-frame из vis_wk.js: лист должен выглядеть одинаково
     независимо от порядка подключения файлов. Ширина 352 px — как у школьных
     уроков 6 класса. Отступы подобраны так, чтобы холст рисунка в 320 единиц
     получал не меньше 320 px по ширине: тогда «16» в рисунке читается как
     16 px, а не мельче (замер масштаба — в снимки/racional_umnozh_проверка.mjs). */
  const CSS =
    '#lvis .m6-frame{box-sizing:border-box;width:100%;max-width:352px;margin:0 auto;'+
      "font-family:Georgia,'Times New Roman',serif;color:#e8e0cc;"+
      'background:linear-gradient(180deg,rgba(26,52,40,.96),rgba(15,30,23,.98));'+
      'border:1px solid #4a6b56;border-radius:18px;padding:8px;display:flex;flex-direction:column;gap:10px;'+
      'box-shadow:0 6px 20px rgba(0,0,0,.35);-webkit-font-smoothing:antialiased;text-rendering:geometricPrecision}'+
    '#lvis .m6-hero{width:100%;border-radius:12px;background:linear-gradient(180deg,#14261d,#0b1712);'+
      'border:1px solid rgba(217,164,65,.28);padding:4px;overflow:hidden}'+
    '#lvis .m6-hero svg{display:block;width:100%;height:auto}'+
    '#lvis .m6-step{font-size:12px;letter-spacing:.1em;text-transform:uppercase;color:#cbb89a}'+
    '#lvis .m6-title{font-size:20px;font-weight:600;line-height:1.15;color:#d9a441;letter-spacing:-.01em}'+
    '#lvis .m6-note{font-size:16px;line-height:1.55;color:#e8e0cc}'+
    '#lvis .m6-note.mut{color:#cbb89a}'+
    '#lvis .m6-row{display:flex;flex-wrap:wrap;gap:8px;align-items:stretch}'+
    '#lvis .m6-row>.m6-btn{flex:1 1 calc(50% - 4px);min-width:88px}'+
    '#lvis .m6-row>p{flex:1 1 100%}'+
    '#lvis .m6-btn{min-height:46px;min-width:44px;padding:12px 14px;border-radius:12px;border:1px solid #4a6b56;'+
      'background:rgba(255,255,255,.08);color:#ffe9c9;font-family:inherit;font-size:16px;font-weight:600;'+
      'line-height:1.25;cursor:pointer;touch-action:manipulation;'+
      'transition:transform 140ms cubic-bezier(.23,1,.32,1),border-color 160ms ease,background 160ms ease}'+
    '#lvis .m6-btn:hover{border-color:#d9a441}'+
    '#lvis .m6-btn:active{transform:translateY(2px)}'+
    '#lvis .m6-btn:focus-visible{outline:3px solid #d9a441;outline-offset:2px}'+
    '#lvis .m6-btn.good{border-color:#8fd1a8;background:rgba(143,209,168,.16);color:#d9f2e2}'+
    '#lvis .m6-btn.bad{border-color:#e86a5a;background:rgba(232,106,90,.16);color:#ffb3a7}'+
    '#lvis .m6-btn.ghost{background:none;color:#8fd1a8;border-color:rgba(143,209,168,.45);font-weight:400}'+
    '#lvis .m6-box{border-radius:12px;padding:10px 12px;border:1px solid rgba(217,164,65,.38);background:rgba(217,164,65,.06)}'+
    '#lvis .m6-lab{font-size:14px;letter-spacing:.06em;text-transform:uppercase;color:#d9a441;margin-bottom:8px}'+
    '#lvis .m6-txt{font-size:16px;line-height:1.55;color:#e8e0cc}'+
    '#lvis .m6-txt b{color:#ffd76a}'+
    '#lvis .m6-box.err{border-color:rgba(232,106,90,.6);background:rgba(232,106,90,.12)}'+
    '#lvis .m6-box.err .m6-lab{color:#ffb3a7}'+
    '#lvis .m6-box.err .m6-txt{color:#f7ded9}'+
    '#lvis .m6-box.self{border-color:rgba(143,209,168,.6);background:rgba(143,209,168,.12)}'+
    '#lvis .m6-box.self .m6-lab{color:#9fe0b8}'+
    '#lvis .m6-steps{margin:0;padding-left:22px;font-size:16px;line-height:1.55;color:#e8e0cc}'+
    '#lvis .m6-steps li{margin-bottom:8px}'+
    '#lvis .m6-steps li:last-child{margin-bottom:0}'+
    '#lvis .m6-score{font-size:16px;color:#cbb89a;font-variant-numeric:tabular-nums}'+
    /* Речь Архимеда в общем каркасе набрана 13,5 px — для этого листа поднимаем
       её до 16 px: правило висит на :has() и действует только пока открыт
       m6-frame, чужие уроки оно не задевает. */
    '.card:has(#lvis .m6-frame) .arch{font-size:16px;line-height:1.55}'+
    '.card:has(#lvis .m6-frame) .arch .who{font-size:12px;letter-spacing:1.2px}'+
    '@media (prefers-reduced-motion: reduce){#lvis .m6-frame *{animation:none!important;transition:none!important}}';
  function css(){
    try{
      let e = document.getElementById('m649-style');
      if(!e){ e = document.createElement('style'); e.id = 'm649-style'; document.head.appendChild(e); }
      if(e.textContent !== CSS) e.textContent = CSS;
    }catch(err){}
  }

  /* ================= SVG-ХЕЛПЕРЫ ================= */
  const esc = s => String(s == null ? '' : s)
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');

  const sv = (inner, h) => '<svg viewBox="0 0 320 ' + h + '" width="100%" style="display:block;max-width:100%;'+
    'font-variant-numeric:tabular-nums" role="img">' + inner + '</svg>';

  const tx = (x, y, s, size, fill, anchor, w) =>
    '<text x="' + x + '" y="' + y + '" text-anchor="' + (anchor || 'middle') + '" font-family="' + F + '"'+
    ' font-size="' + (size || 16) + '" fill="' + (fill || IVORY) + '"' + (w ? ' font-weight="bold"' : '') + '>' + s + '</text>';

  const rect = (x, y, w, h, fill, stroke, rx, sw) =>
    '<rect x="' + x + '" y="' + y + '" width="' + w + '" height="' + h + '" rx="' + (rx == null ? 6 : rx) + '"'+
    ' fill="' + (fill || 'none') + '"' + (stroke ? ' stroke="' + stroke + '" stroke-width="' + (sw || 1.2) + '"' : '') + '/>';

  const line = (x1, y1, x2, y2, c, w, dash) =>
    '<line x1="' + x1 + '" y1="' + y1 + '" x2="' + x2 + '" y2="' + y2 + '" stroke="' + (c || LINE) + '"'+
    ' stroke-width="' + (w || 1.4) + '"' + (dash ? ' stroke-dasharray="' + dash + '"' : '') + '/>';

  const circ = (cx, cy, r, fill, stroke, w) =>
    '<circle cx="' + cx + '" cy="' + cy + '" r="' + r + '" fill="' + (fill || 'none') + '"'+
    (stroke ? ' stroke="' + stroke + '" stroke-width="' + (w || 1.2) + '"' : '') + '/>';

  /* галочка и крестик нарисованы путями, а не значками шрифта */
  const галка = (cx, cy, c) =>
    '<path d="M' + (cx - 6) + ' ' + cy + ' l4.5 5 l8.5 -11" fill="none" stroke="' + (c || GOLD) + '"'+
    ' stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"/>';
  const крест = (cx, cy, r, c) => line(cx - r, cy - r, cx + r, cy + r, c || RED, 2) + line(cx + r, cy - r, cx - r, cy + r, c || RED, 2);
  /* стрелка вниз между плашками */
  const вниз = (x, y1, y2, c) =>
    line(x, y1, x, y2 - 6, c || GOLD, 1.6) + '<path d="M' + x + ' ' + y2 + ' l-5 -8 h10 z" fill="' + (c || GOLD) + '"/>';
  /* стрелка вправо (от x1 к x2 на высоте y) */
  const вправо = (x1, x2, y, c, w) =>
    line(x1, y, x2 - 6, y, c || GOLD, w || 2.4) + '<path d="M' + x2 + ' ' + y + ' l-8 -5 v10 z" fill="' + (c || GOLD) + '"/>';
  /* стрелка влево */
  const влево = (x1, x2, y, c, w) =>
    line(x1, y, x2 + 6, y, c || GOLD, w || 2.4) + '<path d="M' + x2 + ' ' + y + ' l8 -5 v10 z" fill="' + (c || GOLD) + '"/>';
  /* дуга-прыжок над осью: слева направо */
  const дугаВправо = (xa, xb, y) =>
    '<path d="M' + xa + ' ' + y + ' Q' + ((xa + xb) / 2) + ' ' + (y - 26) + ' ' + xb + ' ' + y + '" fill="none" stroke="' + GREEN + '" stroke-width="2.2"/>';
  const дугаВлево = (xa, xb, y) =>
    '<path d="M' + xa + ' ' + y + ' Q' + ((xa + xb) / 2) + ' ' + (y - 26) + ' ' + xb + ' ' + y + '" fill="none" stroke="' + RED + '" stroke-width="2.2"/>';
  /* плашка с текстом: y — верх, h — высота */
  const плашка = (y, h, текст, c, size) =>
    rect(16, y, 288, h, FILL, c || GOLD, 10, 1.4) + tx(160, y + h / 2 + (size || 16) * 0.36, текст, size || 16, c || GOLD, 'middle', 1);
  /* заголовок кадра внутри рисунка */
  const шапка = (t, c) => tx(160, 22, t, 14, c || MUTE);

  /* ================= СОСТОЯНИЕ ================= */
  function st6(){
    const k = (typeof lidKey === 'function') ? lidKey(ID) : ('l' + ID);
    if(typeof CHS === 'undefined') window.CHS = {};
    if(!CHS[k]) CHS[k] = { ans:{}, показ:{} };
    if(!CHS[k].ans) CHS[k].ans = {};
    if(!CHS[k].показ) CHS[k].показ = {};
    return CHS[k];
  }
  const открыто = z => { const s = st6(); return s.ans[z] != null || !!s.показ[z]; };

  /* ================= ВОСЕМЬ ЗАДАНИЙ =================
     У каждого — разбор по шагам, частая ошибка, как проверить себя обратным
     действием и второй способ через смысл. Это и есть «усиленное объяснение». */
  const ЗАДАНИЯ = {
    1: {
      q:'Сколько получится: (−4) · (−6)?',
      opts:['−24', '24', '−10'], ans:1,
      шаги:[
        'Смотрим на знаки: минус на минус.',
        'По правилу получаем плюс.',
        'Умножаем модули: 4 · 6 = 24.',
        'Проверка: 24 : (−6) = −4 — вернулись к исходному.'],
      ошибка:'Правильно посчитать модули 4 · 6 = 24 и потерять знак — записать −24. Или решить, что минус на минус даёт минус: это главная ошибка всей темы.',
      проверка:'Делим ответ на второй множитель: 24 : (−6) = −4. Получили исходное число — значит, умножение выполнено верно.',
      способ:'Через смысл: «убрать долг 4 рубля шесть раз». Долг падал шесть раз по 4 рубля — стало на 24 рубля больше, то есть +24.'
    },
    2: {
      q:'Сколько получится: (−3) · 7?',
      opts:['−21', '21', '−4'], ans:0,
      шаги:[
        'Смотрим на знаки: минус и плюс — знаки разные.',
        'По правилу получаем минус.',
        'Умножаем модули: 3 · 7 = 21.',
        'Проверка: (−21) : 7 = −3 — вернулись к исходному.'],
      ошибка:'Сложить 3 и 7 и получить −10. Здесь умножение, а не сложение: модули перемножаем, 3 · 7 = 21.',
      проверка:'Делим результат на второй множитель: (−21) : 7 = −3. Вернулись к −3 — ответ верный.',
      способ:'Через смысл: долг 3 рубля повторили 7 раз — долг стал 21 рубль, то есть −21. Долг растёт, значит знак минус.'
    },
    3: {
      q:'Не считая, определи знак: (+5) · (−8).',
      opts:['плюс', 'минус', 'нуль'], ans:1,
      шаги:[
        'Считаем минусы в записи: он один.',
        'Один минус — нечётное число.',
        'По правилу нечётное число минусов даёт минус.',
        'Проверка модулей: 5 · 8 = 40, значит (+5) · (−8) = −40.'],
      ошибка:'Смотреть на модули и решать по «большему числу»: 5 меньше 8, поэтому думать, что знак будет минус. А если бы было (+9) · (−2), тот же ответ был бы с плюсом. Знак решают знаки, а не размер чисел.',
      проверка:'Умножь результат на второй множитель по модулю: 40 : 8 = 5 — вернулись к первому числу. Модули считаются верно.',
      способ:'Через числовую ось: умножение на отрицательное число разворачивает направление, и прыжок идёт влево — значит минус.'
    },
    4: {
      q:'Сколько получится: (−42) : (−6)?',
      opts:['−7', '7', '−36'], ans:1,
      шаги:[
        'Смотрим на знаки: минус делится на минус.',
        'Правило знаков при делении то же, что при умножении: получаем плюс.',
        'Делим модули: 42 : 6 = 7.',
        'Проверка: 7 · (−6) = −42 — вернулись к делимому.'],
      ошибка:'Решить, что при делении правило знаков другое, и ответить −7. Таблица знаков одна на оба действия: минус на минус — плюс.',
      проверка:'Умножаем частное на делитель: 7 · (−6) = −42. Получили делимое — деление выполнено верно.',
      способ:'Через смысл: сколько долгов по 6 рублей укладывается в долг 42 рубля? Семь. Долг на долг даёт плюс, ответ 7.'
    },
    5: {
      q:'Сколько получится: 36 : (−4)?',
      opts:['−9', '9', '−32'], ans:0,
      шаги:[
        'Смотрим на знаки: плюс делится на минус — знаки разные.',
        'По правилу получаем минус.',
        'Делим модули: 36 : 4 = 9.',
        'Проверка: (−9) · (−4) = 36 — вернулись к делимому.'],
      ошибка:'Потерять минус и ответить 9. Модули действительно дают 9, но знаки разные — значит, ответ −9.',
      проверка:'Умножаем частное на делитель: (−9) · (−4) = 36. Вернулись к 36 — ответ верный.',
      способ:'Через смысл: 36 рублей раздали долями по 4 рубля, но каждая доля — новый долг. Вышло 9 долгов, ответ −9.'
    },
    6: {
      q:'Сколько получится: (−2) · (−3) · (−1) · 5?',
      opts:['−30', '30', '−12'], ans:0,
      шаги:[
        'Считаем минусы: их три — нечётное число.',
        'По правилу нечётное число минусов даёт минус.',
        'Перемножаем модули: 2 · 3 · 1 · 5 = 30.',
        'Проверка по шагам: (−2)·(−3) = +6, 6·(−1) = −6, (−6)·5 = −30.'],
      ошибка:'Перемножить модули и взять плюс «потому что минусов много». Считать надо не количество, а чётность: три минуса — нечётно — знак минус.',
      проверка:'Идём обратно: −30 : 5 = −6, −6 : (−1) = 6, 6 : (−3) = −2. Вернулись к первому множителю — ответ верный.',
      способ:'Парами: (−2)·(−3) = +6, потом 6·(−1) = −6, потом −6·5 = −30. Тот же ответ, но видно, где знак переворачивается.'
    },
    7: {
      q:'Температура каждый час меняется на −2°. Какой будет изменение через 5 часов?',
      opts:['10°', '−10°', '−7°'], ans:1,
      шаги:[
        'Изменение за час — минус 2, таких часов пять.',
        'Повторение — это умножение: (−2) · 5.',
        'Знаки разные, значит минус; модули 2 · 5 = 10.',
        'Проверка: (−10) : 5 = −2 — снова изменение за один час.'],
      ошибка:'Прибавить 2 · 5 = 10 и получить +10: понижение записывают минусом, поэтому (−2) · 5 = −10.',
      проверка:'Делим ответ на число часов: (−10) : 5 = −2 — вернулись к изменению за час. Значит, изменение за 5 часов найдено верно.',
      способ:'Сложением: (−2) + (−2) + (−2) + (−2) + (−2) = −10. Дольше, но видно, что это пять одинаковых понижений.'
    },
    8: {
      q:'Долг 72 рубля разделили на 6 равных частей. Чему равна одна часть?',
      opts:['12 рублей', '−66 рублей', '−12 рублей'], ans:2,
      шаги:[
        'Долг записываем отрицательным числом: −72.',
        'Делим на 6 равных частей: (−72) : 6.',
        'Знаки разные, значит минус; модули 72 : 6 = 12.',
        'Проверка: (−12) · 6 = −72 — вернулись ко всему долгу.'],
      ошибка:'Разделить 72 на 6 и получить +12. Части долга тоже отрицательные: каждая часть — это долг 12 рублей.',
      проверка:'Умножаем часть на число частей: (−12) · 6 = −72. Получили весь долг — деление выполнено верно.',
      способ:'Через смысл: шесть частей по −12 рублей собираются в −72. Долг не исчез, он просто разложен на равные куски.'
    }
  };

  /* ================= РИСУНКИ ================= */

  /* 1. Минус — это направление */
  function рНаправление(){
    const h = 226;
    let s = шапка('минус — это направление');
    s += tx(86, 56, 'минус — влево', 14, RED);
    s += tx(234, 56, 'плюс — вправо', 14, GREEN);
    s += влево(152, 40, 78, RED);
    s += вправо(168, 280, 78, GREEN);
    s += line(32, 96, 288, 96, LINE, 1.8);
    for(let i = -6; i <= 6; i++){
      const x = 160 + i * 20;
      s += line(x, 90, x, 102, HAIR, 1.2);
    }
    [-6, -4, -2, 0, 2, 4, 6].forEach(v => {
      const x = 160 + v * 20;
      s += tx(x, 120, String(v).replace('-', '−'), 12, MUTE);
    });
    s += rect(16, 138, 140, 46, GFILL, GREEN, 10, 1.4);
    s += tx(86, 168, '+ прибыль', 16, GREEN, 'middle', 1);
    s += rect(164, 138, 140, 46, RFILL, RED, 10, 1.4);
    s += tx(234, 168, '− долг', 16, REDT, 'middle', 1);
    s += tx(160, 208, 'знак показывает, куда идти', 14, MUTE);
    return sv(s, h);
  }

  /* 2. Таблица знаков — умножение и деление */
  function рЗнаки(){
    const h = 352;
    let s = шапка('таблица знаков');
    /* Первый столбец — подписи строк. Текст в нём выровнен по левому краю
       (x = 44), а галочка стоит левее (x = 28): в прежней раскладке галочка
       упиралась в слово «плюс» — на снимке это было видно. */
    const колон = [16, 96, 202], шир = [80, 106, 104];
    const ячейка = (i, y, hh, текст, c, hi, size) =>
      rect(колон[i], y, шир[i], hh, hi ? FLAT : FILL, hi ? GOLD : LINE, 6, hi ? 1.8 : 1.1) +
      tx(i === 0 ? 44 : колон[i] + шир[i] / 2, y + hh / 2 + (size || 16) * 0.36, текст, size || 16, c || IVORY, i === 0 ? 'start' : 'middle', hi ? 1 : 0);

    s += tx(160, 44, 'умножение', 14, GOLD, 'middle', 1);
    /* «множители» от x = 44 вылезали за ячейку шириной 80 — заголовок по её середине */
    s += rect(колон[0], 52, шир[0], 30, FILL, LINE, 6, 1.1) + tx(колон[0] + шир[0] / 2, 52 + 15 + 12 * 0.36, 'множители', 12, MUTE, 'middle', 0);
    s += ячейка(1, 52, 30, 'плюс', MUTE, false, 14);
    s += ячейка(2, 52, 30, 'минус', MUTE, false, 14);
    s += ячейка(0, 82, 46, 'плюс', MUTE, false, 14);
    s += ячейка(1, 82, 46, '+ · + = +', GREEN, false);
    s += ячейка(2, 82, 46, '+ · − = −', REDT, false);
    s += галка(28, 105, GOLD);
    s += ячейка(0, 128, 46, 'минус', MUTE, false, 14);
    s += ячейка(1, 128, 46, '− · + = −', REDT, false);
    s += ячейка(2, 128, 46, '− · − = +', GOLD, true);
    s += галка(28, 151, GOLD);

    s += tx(160, 200, 'деление', 14, GOLD, 'middle', 1);
    s += ячейка(0, 208, 30, 'делимое', MUTE, false, 12);
    s += ячейка(1, 208, 30, 'плюс', MUTE, false, 14);
    s += ячейка(2, 208, 30, 'минус', MUTE, false, 14);
    s += ячейка(0, 238, 46, 'плюс', MUTE, false, 14);
    s += ячейка(1, 238, 46, '+ : + = +', GREEN, false);
    s += ячейка(2, 238, 46, '+ : − = −', REDT, false);
    s += галка(28, 261, GOLD);
    s += ячейка(0, 284, 46, 'минус', MUTE, false, 14);
    s += ячейка(1, 284, 46, '− : + = −', REDT, false);
    s += ячейка(2, 284, 46, '− : − = +', GOLD, true);
    s += галка(28, 307, GOLD);
    return sv(s, h);
  }

  /* 3. Правило знаков словами */
  function рПравило(){
    const h = 240;
    let s = шапка('два шага в любом примере');
    s += rect(16, 36, 288, 54, FILL, GOLD, 10, 1.4);
    s += tx(160, 60, '1. Смотрим на знаки', 16, GOLD, 'middle', 1);
    s += tx(160, 80, 'чётно минусов — плюс, нечётно — минус', 12, MUTE);
    s += rect(16, 98, 288, 54, FILL, GOLD, 10, 1.4);
    s += tx(160, 122, '2. Считаем модули', 16, GOLD, 'middle', 1);
    s += tx(160, 142, 'перемножаем или делим, знак уже известен', 12, MUTE);
    s += rect(16, 162, 140, 30, GFILL, GREEN, 8, 1.4);
    s += tx(86, 182, 'одинаковые → +', 14, GREEN);
    s += rect(164, 162, 140, 30, RFILL, RED, 8, 1.4);
    s += tx(234, 182, 'разные → −', 14, REDT);
    s += rect(16, 200, 288, 30, FLAT, GOLD, 8, 1.6);
    s += tx(160, 220, 'минус на минус — это плюс', 16, GOLD, 'middle', 1);
    return sv(s, h);
  }

  /* 4. Задание 1: сетка модулей 4 × 6 */
  function рЗадание1(){
    const h = 244, ok = открыто(1);
    let s = шапка('(−4) · (−6)');
    for(let r = 0; r < 4; r++) for(let c = 0; c < 6; c++){
      s += circ(100 + c * 20, 56 + r * 20, 5, FILL, GOLD, 1.1);
    }
    s += tx(160, 142, 'модули: 4 ряда по 6 кружков', 12, MUTE);
    s += rect(16, 156, 288, 46, ok ? GFILL : FILL, ok ? GREEN : GOLD, 10, 1.6);
    s += tx(160, 186, ok ? '(−4) · (−6) = +24' : '(−4) · (−6) = ?', 20, ok ? GREEN : GOLD, 'middle', 1);
    s += tx(160, 224, ok ? 'минус · минус = плюс' : 'сначала знаки, потом модули', 14, ok ? GOLD : MUTE);
    return sv(s, h);
  }

  /* 5. Четыре шага разбора — цепочкой */
  function рШаги(){
    const h = 324;
    let s = шапка('четыре шага разбора');
    const ш = [
      ['Шаг 1. Знаки: − и −', GOLD],
      ['Шаг 2. Правило: это плюс', GREEN],
      ['Шаг 3. Модули: 4 · 6 = 24', GOLD],
      ['Шаг 4. Проверка: 24 : (−6) = −4', IVORY]
    ];
    ш.forEach((x, i) => {
      const y = 38 + i * 66;
      s += rect(16, y, 288, 44, FILL, x[1], 10, 1.4);
      s += tx(160, y + 28, x[0], 16, x[1]);
      if(i < 3) s += вниз(160, y + 46, y + 64);
    });
    s += rect(16, 290, 288, 26, GFILL, GREEN, 8, 1.4);
    s += tx(160, 308, 'вернулись к −4 — значит, верно', 12, GREEN);
    return sv(s, h);
  }

  /* 6. Задание 2: сетка 3 × 7 */
  function рЗадание2(){
    const h = 226, ok = открыто(2);
    let s = шапка('(−3) · 7');
    for(let r = 0; r < 3; r++) for(let c = 0; c < 7; c++){
      s += circ(84 + c * 22, 52 + r * 22, 5, FILL, GOLD, 1.1);
    }
    s += tx(160, 140, 'модули: 3 ряда по 7 кружков', 12, MUTE);
    s += rect(16, 152, 288, 44, ok ? RFILL : FILL, ok ? RED : GOLD, 10, 1.6);
    s += tx(160, 181, ok ? '(−3) · 7 = −21' : '(−3) · 7 = ?', 20, ok ? REDT : GOLD, 'middle', 1);
    s += tx(160, 210, ok ? 'минус · плюс = минус' : 'знаки разные — какой знак?', 12, MUTE);
    return sv(s, h);
  }

  /* 7. Задание 3: только знак результата */
  function рЗнакРезультата(){
    const h = 258, ok = открыто(3);
    let s = шапка('какой знак у результата?');
    const строки = [['(+5) · (−8)', '−'], ['(−7) · (−2)', '+'], ['(−9) · 4', '−']];
    строки.forEach((r, i) => {
      const y = 40 + i * 52;
      s += rect(16, y, 190, 42, FILL, LINE, 10, 1.2);
      s += tx(111, y + 28, r[0], 16, IVORY);
      s += rect(216, y, 88, 42, ok ? (r[1] === '+' ? GFILL : RFILL) : FLAT,
        ok ? (r[1] === '+' ? GREEN : RED) : GOLD, 10, ok ? 1.6 : 1.2);
      s += tx(260, y + 28, ok ? (r[1] === '+' ? 'плюс' : 'минус') : '?', 16,
        ok ? (r[1] === '+' ? GREEN : REDT) : GOLD, 'middle', 1);
    });
    s += rect(16, 200, 288, 44, GFILL, GREEN, 10, 1.4);
    s += tx(160, 220, 'минусов нечётно → минус', 14, GREEN);
    s += tx(160, 237, 'минусов чётно → плюс', 14, GREEN);
    return sv(s, h);
  }

  /* 8. Задание 4: деление отрицательных, 42 по 6 */
  function рДеление(){
    const h = 264, ok = открыто(4);
    let s = шапка('(−42) : (−6)');
    for(let r = 0; r < 6; r++){
      s += rect(90, 40 + r * 18, 140, 16, FILL, LINE, 4, 1);
      for(let c = 0; c < 7; c++) s += circ(102 + c * 18, 48 + r * 18, 3.4, GOLD);
    }
    s += tx(160, 172, '42 разделили на 6 равных рядов', 12, MUTE);
    s += rect(16, 186, 288, 44, ok ? GFILL : FILL, ok ? GREEN : GOLD, 10, 1.6);
    s += tx(160, 215, ok ? '(−42) : (−6) = 7' : '(−42) : (−6) = ?', 20, ok ? GREEN : GOLD, 'middle', 1);
    s += tx(160, 246, ok ? 'минус : минус = плюс' : 'правило то же, что при умножении', 12, MUTE);
    return sv(s, h);
  }

  /* 9. Задание 5: 36 : (−4), девять долей по 4 */
  function рДеление2(){
    const h = 248, ok = открыто(5);
    let s = шапка('36 : (−4)');
    for(let i = 0; i < 9; i++){
      const x = 24 + i * 30;
      s += rect(x, 42, 28, 28, FILL, GOLD, 6, 1.2);
      s += circ(x + 10, 52, 3.4, GOLD) + circ(x + 18, 52, 3.4, GOLD);
      s += circ(x + 10, 60, 3.4, GOLD) + circ(x + 18, 60, 3.4, GOLD);
      s += tx(x + 14, 90, String(i + 1), 12, MUTE);
    }
    s += tx(160, 120, '9 долей по 4 — это все 36', 12, MUTE);
    s += rect(16, 134, 288, 44, ok ? RFILL : FILL, ok ? RED : GOLD, 10, 1.6);
    s += tx(160, 163, ok ? '36 : (−4) = −9' : '36 : (−4) = ?', 20, ok ? REDT : GOLD, 'middle', 1);
    s += tx(160, 196, ok ? 'знаки разные — минус' : 'каждая доля — новый долг', 14, ok ? REDT : MUTE);
    s += rect(16, 210, 288, 30, FILL, LINE, 8, 1.2);
    s += tx(160, 230, 'модули 36 : 4 = 9', 14, IVORY);
    return sv(s, h);
  }

  /* 10. Задание 6: цепочка множителей */
  function рЦепочка(){
    const h = 274, ok = открыто(6);
    let s = шапка('цепочка: (−2) · (−3) · (−1) · 5');
    const мн = ['−2', '−3', '−1', '5'];
    const тек = ok ? ['−2', '+6', '−6', '−30'] : ['−2', '?', '?', '?'];
    const цв = ok ? [REDT, GREEN, REDT, REDT] : [MUTE, GOLD, GOLD, GOLD];
    for(let i = 0; i < 4; i++){
      const x = 13 + i * 76;
      s += rect(x, 40, 66, 40, FILL, LINE, 8, 1.2);
      s += tx(x + 33, 66, мн[i], 16, IVORY);
      if(i < 3) s += tx(x + 71, 66, '·', 12, MUTE);
      s += вниз(x + 33, 82, 100, HAIR);
      s += rect(x, 100, 66, 36, FILL, цв[i], 8, 1.2);
      s += tx(x + 33, 124, тек[i], 16, цв[i]);
    }
    s += rect(16, 154, 288, 42, FILL, ok ? RED : GOLD, 10, 1.4);
    s += tx(160, 172, ok ? 'минусов три — нечётно' : 'считаем минусы', 14, ok ? REDT : GOLD);
    s += tx(160, 189, ok ? 'значит, знак минус' : 'чётно или нечётно?', 12, MUTE);
    s += rect(16, 206, 288, 44, ok ? RFILL : FILL, ok ? RED : GOLD, 10, 1.6);
    s += tx(160, 235, ok ? '(−2) · (−3) · (−1) · 5 = −30' : '(−2) · (−3) · (−1) · 5 = ?', 14, ok ? REDT : GOLD, 'middle', 1);
    return sv(s, h);
  }

  /* 11. Числовая ось с прыжками — ключевой рисунок темы */
  function рОсь(){
    const h = 300;
    let s = шапка('числовая ось: прыжки по 6');
    const x0 = 160, шаг = 28;
    /* верхний ряд: умножение на −6 разворачивает влево */
    s += tx(160, 46, '· (−6): пять прыжков влево', 14, RED);
    for(let i = 0; i < 5; i++){
      const a = x0 - i * шаг, b = x0 - (i + 1) * шаг;
      s += дугаВлево(a, b, 86);
      s += circ(b, 86, 4, RED);
    }
    s += line(20, 86, 300, 86, HAIR, 1.4);
    s += circ(x0, 86, 4, GOLD);
    ['0', '−6', '−12', '−18', '−24', '−30'].forEach((t, i) => {
      s += tx(x0 - i * шаг, 108, t, 12, i ? REDT : GOLD);
    });
    s += tx(160, 140, 'остановились на −30', 14, REDT);
    /* нижний ряд: −(−6) идёт вправо */
    s += tx(160, 176, '−(−6): прыжки вправо', 14, GREEN);
    for(let i = 0; i < 5; i++){
      const a = x0 + i * шаг, b = x0 + (i + 1) * шаг;
      s += дугаВправо(a, b, 216);
      s += circ(b, 216, 4, GREEN);
    }
    s += line(20, 216, 300, 216, HAIR, 1.4);
    s += circ(x0, 216, 4, GOLD);
    ['0', '6', '12', '18', '24', '30'].forEach((t, i) => {
      s += tx(x0 + i * шаг, 238, t, 12, i ? GREEN : GOLD);
    });
    s += tx(160, 268, 'минус перед множителем разворачивает', 14, MUTE);
    s += tx(160, 288, 'прыжок — и он идёт в другую сторону', 14, MUTE);
    return sv(s, h);
  }

  /* 12. Задание 7: температура ступеньками */
  function рТемпература(){
    const h = 266, ok = открыто(7);
    let s = шапка('температура: −2° каждый час');
    const y = t => 56 - t * 10;
    for(let k = 0; k <= 5; k++){
      const x = 64 + k * 44;
      s += line(x, 50, x, 166, HAIR, 1);
      s += tx(x, 182, String(k), 12, MUTE);
    }
    [0, -2, -4, -6, -8, -10].forEach(t => {
      const yy = y(t);
      s += line(48, yy, 296, yy, HAIR, 1);
      s += tx(30, yy + 4, String(t).replace('-', '−') + '°', 12, MUTE);
    });
    let d = '';
    for(let k = 0; k < 6; k++){
      const px = 64 + k * 44, py = y(-2 * k);
      d += (k ? ' L' : 'M') + px + ' ' + py;
    }
    s += '<path d="' + d + '" fill="none" stroke="' + GOLD + '" stroke-width="2.6"/>';
    /* Подписи «−2°» у каждой ступеньки убраны: на снимке они налезали друг на
       друга и на линию графика, а сам шаг в 2° уже назван в заголовке кадра. */
    for(let k = 0; k < 6; k++) s += circ(64 + k * 44, y(-2 * k), 4.4, GOLD);
    s += tx(160, 200, 'каждый час — на 2° ниже', 12, MUTE);
    s += rect(16, 212, 288, 44, ok ? RFILL : FILL, ok ? RED : GOLD, 10, 1.6);
    s += tx(160, 241, ok ? '(−2) · 5 = −10°' : '(−2) · 5 = ?', 20, ok ? REDT : GOLD, 'middle', 1);
    return sv(s, h);
  }

  /* 13. Задание 8 и второй способ: долг частями */
  function рДолг(){
    const h = 256, ok = открыто(8);
    let s = шапка('долг 72 рубля и 6 равных частей');
    s += rect(16, 40, 288, 48, RFILL, RED, 8, 1.4);
    for(let i = 0; i < 6; i++){
      const x = 16 + i * 48;
      if(i) s += line(x, 40, x, 88, RED, 1.2);
      s += tx(x + 24, 70, '−12', 16, REDT);
    }
    s += tx(160, 110, 'шесть частей по −12', 12, MUTE);
    s += rect(16, 126, 288, 44, ok ? RFILL : FILL, ok ? RED : GOLD, 10, 1.6);
    s += tx(160, 155, ok ? '(−72) : 6 = −12' : '(−72) : 6 = ?', 20, ok ? REDT : GOLD, 'middle', 1);
    s += rect(16, 182, 288, 34, GFILL, GREEN, 8, 1.4);
    s += tx(160, 204, 'проверка: (−12) · 6 = −72', 14, GREEN);
    s += tx(160, 238, 'долг не исчез — он разложен на равные части', 12, MUTE);
    return sv(s, h);
  }

  /* 14. Почему минус на минус даёт плюс */
  function рПочему(){
    const h = 288;
    let s = шапка('почему минус на минус даёт плюс');
    for(let i = 0; i < 5; i++){
      const x = 16 + i * 58;
      s += rect(x, 38, 54, 40, RFILL, RED, 8, 1.4);
      s += tx(x + 27, 63, '−6', 16, REDT);
      s += tx(x + 27, 94, 'долг', 12, MUTE);
    }
    s += tx(160, 122, 'пять долгов по 6 — это −30', 14, REDT);
    s += line(10, 108, 310, 108, RED, 1.2, '6 5');
    s += вниз(160, 132, 158);
    s += rect(16, 158, 288, 52, GFILL, GREEN, 10, 1.6);
    s += tx(160, 180, 'эти долги убрали', 16, GREEN, 'middle', 1);
    s += tx(160, 200, 'долг исчез — стало на 30 больше', 12, GREEN);
    s += rect(16, 220, 288, 52, FILL, GOLD, 10, 1.4);
    s += tx(160, 242, '(−5) · (−6) = +30', 16, GOLD, 'middle', 1);
    s += tx(160, 262, '«убрать» — минус, «долг» — минус', 12, MUTE);
    return sv(s, h);
  }

  /* 15. Второй способ: долг по 6 пять раз */
  function рВторой(){
    const h = 232;
    let s = шапка('без правила — через смысл');
    s += tx(160, 52, 'долг по 6 пять раз', 14, GOLD);
    s += tx(160, 82, '(−6) + (−6) + (−6) + (−6) + (−6)', 14, IVORY);
    s += tx(160, 116, '= −30', 20, REDT, 'middle', 1);
    s += вниз(160, 126, 150, HAIR);
    s += rect(16, 150, 288, 44, FILL, GOLD, 10, 1.4);
    s += tx(160, 172, '5 · (−6) = −30', 16, GOLD, 'middle', 1);
    s += tx(160, 186, 'сложение одинаковых слагаемых', 12, MUTE);
    s += tx(160, 212, 'правило и смысл дают один ответ', 14, MUTE);
    return sv(s, h);
  }

  /* 16. Частые ошибки */
  function рОшибки(){
    const h = 210;
    let s = шапка('две самые частые ошибки');
    s += rect(16, 36, 288, 76, RFILL, RED, 10, 1.6);
    s += tx(30, 58, 'ошибка 1', 12, REDT, 'start', 1);
    s += tx(160, 82, '4 · 6 = 24, а пишут −24', 16, IVORY);
    s += tx(160, 102, 'посчитали модули и потеряли знак', 12, MUTE);
    s += rect(16, 122, 288, 76, RFILL, RED, 10, 1.6);
    s += tx(30, 144, 'ошибка 2', 12, REDT, 'start', 1);
    s += tx(160, 168, 'думают, что минус на минус — минус', 14, IVORY);
    s += tx(160, 188, 'два одинаковых знака дают плюс', 12, MUTE);
    return sv(s, h);
  }

  /* 17. Как проверить себя обратным действием */
  function рПроверка(){
    const h = 232;
    let s = шапка('проверка обратным действием');
    s += rect(16, 36, 288, 44, FILL, GOLD, 10, 1.4);
    s += tx(160, 65, '(−4) · (−6) = 24', 16, GOLD, 'middle', 1);
    s += вниз(160, 84, 106);
    s += rect(16, 106, 288, 44, FILL, GOLD, 10, 1.4);
    s += tx(160, 135, '24 : (−6) = −4', 16, GOLD, 'middle', 1);
    s += '<path d="M30 148 L30 52" fill="none" stroke="' + GREEN + '" stroke-width="1.6" stroke-dasharray="5 4"/>';
    s += '<path d="M30 52 l-5 8 h10 z" fill="' + GREEN + '"/>';
    s += tx(160, 176, 'вернулись к −4', 14, GREEN);
    s += rect(16, 192, 288, 34, GFILL, GREEN, 8, 1.4);
    s += tx(160, 214, 'вернулись — значит, ответ верный', 14, GREEN);
    return sv(s, h);
  }

  /* 18. Умножение и деление — обратные действия */
  function рОбратные(){
    const h = 206;
    let s = шапка('одно правило на два действия');
    s += rect(16, 36, 288, 42, FILL, GOLD, 10, 1.4);
    s += tx(160, 63, '7 · (−6) = −42', 16, GOLD, 'middle', 1);
    s += вниз(160, 82, 106);
    s += rect(16, 106, 288, 42, FILL, GOLD, 10, 1.4);
    s += tx(160, 133, '(−42) : (−6) = 7', 16, GOLD, 'middle', 1);
    s += tx(160, 172, 'таблица знаков у них общая', 14, MUTE);
    s += tx(160, 190, 'минус на минус — плюс в обоих случаях', 12, MUTE);
    return sv(s, h);
  }

  /* 19. Шпаргалка */
  function рШпаргалка(){
    const h = 276;
    let s = шапка('шпаргалка');
    s += rect(16, 36, 288, 40, FILL, GOLD, 10, 1.2);
    s += tx(160, 62, 'сначала знак, потом модули', 16, GOLD);
    s += rect(16, 84, 288, 40, GFILL, GREEN, 10, 1.2);
    s += tx(160, 110, 'минусов чётно → плюс', 16, GREEN);
    s += rect(16, 132, 288, 40, RFILL, RED, 10, 1.2);
    s += tx(160, 158, 'минусов нечётно → минус', 16, REDT);
    const мини = ['+ · + = +', '+ · − = −', '− · − = +'];
    мини.forEach((t, i) => {
      const x = 16 + i * 100;
      s += rect(x, 184, 88, 36, FILL, i === 2 ? GOLD : LINE, 8, i === 2 ? 1.6 : 1.1);
      s += tx(x + 44, 208, t, 12, i === 2 ? GOLD : IVORY);
    });
    s += rect(16, 232, 288, 34, FILL, LINE, 8, 1.2);
    s += tx(160, 254, 'проверка — обратным действием', 14, IVORY);
    return sv(s, h);
  }

  /* 20. Финал */
  function рФинал(){
    const h = 236;
    let s = шапка('правило собрано');
    const ш = ['1. Знак по таблице', '2. Модули перемножить', '3. Проверить обратным действием'];
    ш.forEach((t, i) => {
      const y = 38 + i * 56;
      s += rect(16, y, 288, 44, FILL, GOLD, 10, 1.4);
      s += tx(160, y + 29, t, 16, GOLD);
      if(i < 2) s += вниз(160, y + 46, y + 54, HAIR);
    });
    s += rect(16, 206, 288, 26, GFILL, GREEN, 8, 1.4);
    s += tx(160, 224, 'дальше — проверка себя и три задачи', 12, GREEN);
    return sv(s, h);
  }

  /* ================= КАДРЫ ================= */
  const СЦЕНЫ = [
    { т:'Минус — это направление', рис:рНаправление,
      подпись:'Минус показывает направление: −6 — это долг 6 рублей, +6 — прибыль. Умножение повторяет число, поэтому у результата есть и размер, и направление.' },
    { т:'Таблица знаков', рис:рЗнаки,
      подпись:'Одна таблица на умножение и на деление. Плюс на плюс — плюс, плюс на минус — минус, минус на минус — плюс.',
      блок:['Золотом отмечены клетки «минус на минус — плюс»: это и есть клетка, из-за которой чаще всего ошибаются.'] },
    { т:'Два шага в любом примере', рис:рПравило,
      подпись:'Сначала знак — потом модули. Так модули не мешают знаку, а знак не мешает счёту.' },
    { т:'Задание 1. Минус на минус', рис:рЗадание1, з:1,
      подпись:'Сначала посмотри на знаки, потом считай модули. Ответь — и откроется разбор из четырёх шагов.' },
    { т:'Четыре шага разбора', рис:рШаги,
      подпись:'Шаг 1. Смотрим на знаки: минус на минус. Шаг 2. По правилу получаем плюс. Шаг 3. Умножаем модули: 4 · 6 = 24. Шаг 4. Проверка: 24 : (−6) = −4 — вернулись к исходному.',
      блок:['Обрати внимание: проверка — это не «ещё раз посчитать», а обратное действие. Если вернулись к исходному числу, ответ верный.'] },
    { т:'Задание 2. Разные знаки', рис:рЗадание2, з:2,
      подпись:'Здесь знаки разные: минус и плюс. Модули 3 · 7 = 21, а знак — минус.' },
    { т:'Задание 3. Только знак', рис:рЗнакРезультата, з:3,
      подпись:'Иногда спрашивают не ответ, а только знак. Тогда модули не нужны вовсе: считаем минусы — чётно или нечётно.' },
    { т:'Задание 4. Деление отрицательных', рис:рДеление, з:4,
      подпись:'Правило знаков при делении точно такое же, как при умножении. Минус на минус — плюс.' },
    { т:'Задание 5. Деление с разными знаками', рис:рДеление2, з:5,
      подпись:'36 : (−4): модули дают 9, знаки разные — значит, ответ −9. Проверка умножением вернёт 36.' },
    { т:'Задание 6. Цепочка умножений', рис:рЦепочка, з:6,
      подпись:'В цепочке идём по одному действию или сразу считаем минусы: три минуса — нечётно — знак минус.' },
    { т:'Числовая ось с прыжками', рис:рОсь,
      подпись:'Умножить на отрицательное число — значит развернуться. Прыжки по −6 уводят влево, прыжки по −(−6) — вправо.',
      блок:['Ось показывает то же правило глазами: знак множителя решает, в какую сторону идут одинаковые по длине прыжки.'] },
    { т:'Задание 7. Температура', рис:рТемпература, з:7,
      подпись:'Понижение на 2 градуса каждый час — это повторение: (−2) · 5. Ответ −10°, то есть похолодало на 10 градусов.' },
    { т:'Задание 8. Долг частями', рис:рДолг, з:8,
      подпись:'Долг 72 рубля разделили на 6 равных частей. Части долга тоже отрицательные: (−72) : 6 = −12.' },
    { т:'Почему минус на минус даёт плюс', рис:рПочему,
      подпись:'«Убрать долг» — это и есть два минуса: минус (долг) и минус (убрали). Убрали пять долгов по 6 — долг исчез, стало на 30 больше.',
      блок:['Поэтому (−5) · (−6) = +30. Два «нет» в одном действии дают «да»: не иметь долга — значит иметь прибыль.'] },
    { т:'Второй способ: через смысл', рис:рВторой,
      подпись:'Долг по 6 пять раз — это пять раз по −6. Складываем: (−6) + (−6) + (−6) + (−6) + (−6) = −30. То же самое, что 5 · (−6).',
      блок:['Правило и смысл всегда дают один ответ. Если они разошлись — где-то потерян знак.'] },
    { т:'Частые ошибки', рис:рОшибки,
      подпись:'Обе ошибки лечатся одним порядком работы: сначала знаки, потом модули. Никогда наоборот.' },
    { т:'Как проверить себя', рис:рПроверка,
      подпись:'Умножение проверяем делением, деление — умножением. Вернулись к исходному числу — ответ верный.',
      блок:['24 : (−6) = −4. Получили −4 — тот самый множитель, с которого начали.'] },
    { т:'Обратные действия', рис:рОбратные,
      подпись:'Умножение и деление — обратные действия, поэтому и таблица знаков у них одна и та же.' },
    { т:'Шпаргалка', рис:рШпаргалка,
      подпись:'Три строки, которые стоит запомнить: сначала знак, потом модули, в конце проверка обратным действием.' },
    { т:'Финал: правило собрано', рис:рФинал,
      подпись:'Сначала знак по таблице, потом модули, потом проверка обратным действием. Жми «Понял! Проверю себя» — там вопрос с ловушкой.' }
  ];

  /* ================= HTML-ХЕЛПЕРЫ ================= */
  const кадр = inner => '<div class="m6-frame"><div class="m6-step">математика · 6 класс</div>' + inner + '</div>';
  const рЗаголовок = t => '<div class="m6-title">' + esc(t) + '</div>';
  const рТекст = (t, mut) => '<div class="m6-note' + (mut ? ' mut' : '') + '">' + esc(t) + '</div>';
  const рРяд = (...x) => '<div class="m6-row">' + x.join('') + '</div>';
  const рКнопка = (t, on, cls) => '<button type="button" class="m6-btn' + (cls ? ' ' + cls : '') + '" onclick="' + on + '">' + esc(t) + '</button>';
  const рПлашка = (lab, body, cls) =>
    '<div class="m6-box' + (cls ? ' ' + cls : '') + '">' + (lab ? '<div class="m6-lab">' + esc(lab) + '</div>' : '') +
    '<div class="m6-txt">' + body + '</div></div>';
  const рСписок = arr => '<ol class="m6-steps">' + arr.map(x => '<li>' + esc(x) + '</li>').join('') + '</ol>';

  function рСчёт(){
    const s = st6();
    let ok = 0, no = 0, всего = 0;
    for(const z in ЗАДАНИЯ){
      всего++;
      if(s.ans[z] == null) continue;
      if(s.ans[z] === ЗАДАНИЯ[z].ans) ok++; else no++;
    }
    return '<div class="m6-score">верно: ' + ok + ' · ошибок: ' + no + ' · разобрано: ' + (ok + no) + ' из ' + всего + '</div>';
  }

  function рЗадание(z){
    const s = st6(), T = ЗАДАНИЯ[z], ответ = s.ans[z], видно = открыто(z);
    let h = '<div class="m6-title" style="font-size:16px;color:#e8e0cc;line-height:1.5">' + esc(T.q) + '</div>';
    const кнопки = T.opts.map((o, i) => {
      let cls = '';
      if(видно){ if(i === T.ans) cls = ' good'; else if(i === ответ) cls = ' bad'; }
      else if(i === ответ) cls = ' good';
      return '<button type="button" class="m6-btn' + cls + '" onclick="m6Pick(' + z + ',' + i + ')">' + esc(o) + '</button>';
    }).join('');
    h += '<div class="m6-row" data-m6="варианты">' + кнопки + '</div>';
    if(!видно){
      h += рРяд(рКнопка('показать разбор', 'm6Показ(' + z + ')', 'ghost'));
      return h;
    }
    if(ответ != null){
      h += ответ === T.ans
        ? рПлашка('верно', 'Ответ ' + esc(T.opts[T.ans]) + ' — правильный.', 'self')
        : рПлашка('не так', 'Правильный ответ: <b>' + esc(T.opts[T.ans]) + '</b>. Ниже — полный разбор, разбери его до конца.', 'err');
    }
    h += рПлашка('разбор по шагам', рСписок(T.шаги));
    h += рПлашка('частая ошибка', esc(T.ошибка), 'err');
    h += рПлашка('как проверить себя', esc(T.проверка), 'self');
    h += рПлашка('второй способ', esc(T.способ));
    h += рРяд(рКнопка('пройти заново', 'm6Сброс(' + z + ')', 'ghost'));
    h += рСчёт();
    return h;
  }

  /* ================= ВИДЖЕТ ================= */
  function виджет(el){
    css();
    try{
      const шаг = (typeof LV !== 'undefined' && LV && LV.step != null) ? LV.step : 0;
      const с = СЦЕНЫ[шаг] || СЦЕНЫ[0];
      const s = st6();
      let h = рЗаголовок(с.т);
      h += '<div class="m6-hero">' + с.рис(s) + '</div>';
      h += рТекст(с.подпись);
      if(с.блок) с.блок.forEach(x => { h += рТекст(x, true); });
      if(с.з) h += рЗадание(с.з);
      else h += рСчёт();
      el.innerHTML = кадр(h);
    }catch(e){
      try{ el.innerHTML = ''; }catch(_){}
      try{ console.error('урок 649, виджет:', e); }catch(_){}
    }
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  const крас = () => { if(typeof chRender === 'function') chRender(0); };
  window.m6Pick = function(z, i){
    const s = st6();
    if(s.ans[z] != null) return;
    if(!ЗАДАНИЯ[z] || i == null || i < 0 || i >= ЗАДАНИЯ[z].opts.length) return;
    s.ans[z] = i;
    крас();
  };
  window.m6Показ = function(z){ st6().показ[z] = 1; крас(); };
  window.m6Сброс = function(z){ const s = st6(); delete s.ans[z]; delete s.показ[z]; крас(); };

  window.VISKW[ID] = виджет;

  /* ================= ДАННЫЕ УРОКА ================= */
  const ОБЪЯСНЕНИЕ = [
    'Минус — это не «плохое число», а направление: −6 — долг 6 рублей, +6 — прибыль. Умножение повторяет число несколько раз, поэтому у результата есть и размер, и направление. Сегодня научимся находить оба.',
    'Вся тема держится на одном правиле: плюс на плюс даёт плюс, плюс на минус — минус, минус на минус — плюс. Посмотри на таблицу: она одна и для умножения, и для деления. Держи её перед глазами весь урок.',
    'Короткая формула правила: перемножаем модули — получаем размер, смотрим на знаки — получаем направление. Два одинаковых знака дают плюс, два разных — минус. Больше запоминать нечего.',
    'Задание 1. (−4) · (−6). Минус на минус — самый частый случай ошибки. Сначала смотри на знаки, потом считай модули. Ответь и посмотри разбор из четырёх шагов.',
    'Разбор по шагам: шаг 1 — смотрим на знаки (минус на минус); шаг 2 — по правилу получаем плюс; шаг 3 — умножаем модули 4 · 6 = 24; шаг 4 — проверяем обратным действием 24 : (−6) = −4. Вернулись к −4 — значит, верно.',
    'Задание 2. (−3) · 7. Знаки разные: минус и плюс. Модули 3 · 7 = 21, знак — минус. Проверим делением: (−21) : 7 = −3 — вернулись к −3.',
    'Задание 3. Иногда спрашивают не ответ, а только знак. Считаем минусы: нечётное число минусов — результат отрицательный, чётное — положительный. Модули при этом не важны вовсе.',
    'Задание 4. Деление отрицательных: (−42) : (−6). Правило знаков при делении точно такое же, как при умножении: минус на минус даёт плюс. Модули: 42 : 6 = 7.',
    'Задание 5. Деление с разными знаками: 36 : (−4). Модули 36 : 4 = 9, знаки разные — значит, ответ −9. Проверка: (−9) · (−4) = 36.',
    'Задание 6. Цепочка: (−2) · (−3) · (−1) · 5. Идём по одному действию: (−2)·(−3) = +6, потом 6·(−1) = −6, потом −6·5 = −30. Либо сразу считаем минусы: их три — нечётно — значит минус.',
    'Числовая ось показывает то же самое глазами. Умножить на отрицательное число — значит развернуться: одинаковые по длине прыжки идут в другую сторону. Посмотри на прыжки по −6 и по +6.',
    'Задание 7. Температура каждый час меняется на −2°, то есть понижается на 2 градуса. За 5 часов: (−2) · 5 = −10°. Похолодало на 10 градусов.',
    'Задание 8. Долг 72 рубля разделили на 6 равных частей. Части долга тоже отрицательные: (−72) : 6 = −12 рублей. Проверка: (−12) · 6 = −72.',
    'Почему минус на минус даёт плюс? «Убрать долг» — это два минуса: минус (долг) и минус (убрали). Убрали пять долгов по 6 — долг исчез, стало на 30 больше. Значит, (−5) · (−6) = +30.',
    'Второй способ — через смысл, без правила. «Долг по 6 пять раз» — это пять раз по −6: (−6) + (−6) + (−6) + (−6) + (−6) = −30. То же самое, что 5 · (−6). Правило и смысл дают один ответ.',
    'Две самые частые ошибки: посчитать модули правильно и потерять знак (4 · 6 = 24, а пишут −24) и решить, что минус на минус даёт минус. Обе лечатся одним порядком: сначала знак, потом модули.',
    'Как проверить себя — обратным действием. Умножение проверяем делением, деление — умножением. 24 : (−6) = −4 — вернулись к исходному множителю. Вернулись — значит, ответ верный.',
    'Умножение и деление — обратные действия, поэтому и правило знаков у них одно. 7 · (−6) = −42 и (−42) : (−6) = 7. Одна таблица знаков на оба действия.',
    'Шпаргалка: модули перемножаем или делим, знаки считаем отдельно. Минусов чётное число — плюс, нечётное — минус. В конце — проверка обратным действием.',
    'Финал: правило собрано. Сначала знак, потом модули, потом проверка. Жми «Понял! Проверю себя» — там вопрос с ловушкой.'
  ];

  const УРОК = {
    id: ID,
    title: 'Умножение и деление рациональных чисел',
    ico: '±',
    src: 'Математика · 6 класс · Рациональные числа',
    subj: 'math',                     /* group НЕ ставим: урок идёт на полку «Математика» */
    explain: ОБЪЯСНЕНИЕ,
    check: {
      q: 'Не выполняя умножения, определи знак произведения: (−7) · (−3) · (−2).',
      choices: ['минус: минусов три — нечётное число',
                'плюс: минусов больше одного',
                'плюс: числа большие по модулю',
                'минус: среди множителей есть двойка'],
      ans: 0,
      exp: 'Минусов три — нечётное число, значит результат отрицательный. Проверка по шагам: (−7)·(−3) = +21, потом 21·(−2) = −42. Модули тут ни при чём: знак решают только знаки.'
    },
    tasks: [
      { q:'В шлюзе уровень воды меняется на −4 см каждый час. Каким будет изменение уровня за 6 часов? Запиши ответ числом сантиметров, понижение — со знаком минус.',
        kind:'unit', ans:-24, tol:0,
        hints:['Изменение за один час — минус 4. Таких часов шесть.',
               'Нужно умножение: (−4) · 6.',
               'Модули 4 · 6 = 24, знаки разные — минус. Ответ: −24.'],
        sol:'(−4) · 6 = −24, то есть уровень понизился на 24 см.' },
      { q:'Мишутка записал: (−8) · (−5) = −40. Верно ли это?',
        kind:'choice',
        choices:['Неверно: минус на минус даёт плюс, правильно 40',
                 'Верно: модули 8 · 5 = 40, и знак минус',
                 'Неверно: правильно 13',
                 'Верно: оба множителя отрицательные'],
        ans:0, tol:0,
        hints:['Посмотри на знаки множителей.',
               'Минус на минус даёт плюс.',
               'Правильно 40, а не −40.'],
        trap:'Знаки одинаковые — значит, результат положительный. Мишутка потерял знак: проверка (−?) · (−5) = 40 даёт 8, а не −8.',
        sol:'(−8) · (−5) = +40: знак потерян.' },
      { q:'Долг 96 рублей разделили на 8 равных частей. Чему равна одна часть? Запиши ответ со знаком.',
        kind:'unit', ans:-12, tol:0,
        hints:['Долг записывают отрицательным числом: −96.',
               'Делим: (−96) : 8.',
               'Модули 96 : 8 = 12, знаки разные — минус. Ответ: −12.'],
        sol:'(−96) : 8 = −12 рублей.' }
    ]
  };

  /* Регистрация: только добавление, если урока с таким id ещё нет. */
  (function(){
    const уже = window.ARH_LESSONS.some(x => x && x.id === ID);
    if(!уже) window.ARH_LESSONS.push(УРОК);
  })();
})();
