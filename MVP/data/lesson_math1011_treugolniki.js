/* ============ МАТЕМАТИКА · УРОК 1011 · «ВИДЫ ТРЕУГОЛЬНИКОВ» ============
   6 класс. Остроугольный, прямоугольный, тупоугольный; равносторонний,
   равнобедренный, разносторонний.

   СВЕРКА С ПЕТЕРСОНОМ (6 класс): виды треугольников, сумма углов
   треугольника, равнобедренный и равносторонний треугольники, элементы
   прямоугольного треугольника; 7 класс — соотношения между сторонами и углами.
   Источники: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   ГЛАВНОЕ ПРАВИЛО ЭТОГО УРОКА: треугольник СТРОИТСЯ ПО СВОИМ УГЛАМ.
   Координаты вершин считаются по теореме синусов (функция треуг), поэтому
   подпись «60°» стоит ровно там, где нарисован угол 60°. Проверено обратным
   измерением: углы, восстановленные из координат, совпадают с заданными.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     сумма углов: 60+60+60 = 180; 90+60+30 = 180; 120+30+30 = 180;
                  80+60+40 = 180; 45+45+90 = 180; 40+70+70 = 180
     равносторонний: 180 : 3 = 60 у каждого угла, все стороны равны
     прямоугольный 3-4-5: 3² + 4² = 9 + 16 = 25 = 5² (Пифагор);
       гипотенуза 5 больше катетов 4 и 3; углы 90°, 53,13°, 36,87°
     равнобедренный: равные стороны против равных углов (40° при вершине,
       70° и 70° при основании)
     против большего угла лежит большая сторона (120° против стороны 1,73)

   МАТЕРИАЛЫ (справочник svg-creator + deploy/РИСОВАНИЕ_ФИГУР.md):
     треугольник — бумага с зерном и мягкой тенью, обводка #33291e;
     линейка и угольник — металл с блеском; стеклянные панели для выводов.

   АНИМАЦИЯ: SMIL calcMode="spline", keySplines="0.23 1 0.32 1".
     кадр 3 — ТРИ УГЛА СЪЕЗЖАЮТСЯ НА ОДНУ ПРЯМУЮ и вместе дают 180°;
     кадр 8 — УГОЛЬНИК ПРИКЛАДЫВАЕТСЯ к каждому углу и проверяет прямой;
     дуги равных углов и засечки равных сторон появляются по очереди.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, keyTimes строго в [0;1], reduced-motion выключает
   SMIL полностью, ничего не выходит за край кадра, подписи короткие. */
(function(){
  'use strict';

  const ID = 1011;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const РОЗ='#f2a0b8', ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  const CSS=`
  #lvis .s6.l1011{gap:14px}
  #lvis .s6.l1011 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l1011 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l1011 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l1011 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l1011 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l1011 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l1011 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l1011 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l1011 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l1011 .правило b{color:${GOLD}}
  #lvis .s6.l1011 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l1011 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1011 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l1011 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l1011 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l1011 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l1011 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1011 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l1011 .уровни .точка.сейчас{background:${GOLD};animation:l1011dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l1011dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l1011 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l1011{-webkit-text-size-adjust:100%}
  #lvis .s6.l1011 [data-anim]{animation:l1011rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l1011rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l1011 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l1011 [data-anim]{animation:none!important}
    #lvis .s6.l1011 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l1011 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l1011-style');
      if(!s){ s=document.createElement('style'); s.id='l1011-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ: КРИВЫЕ ПРОЕКТА ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анВК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c1011-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2a3f33"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#111c15"/>
      </linearGradient>
      <linearGradient id="c1011-бумага" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffd98a"/><stop offset="0.45" stop-color="#f2b64a"/>
        <stop offset="1" stop-color="#c07f1c"/>
      </linearGradient>
      <linearGradient id="c1011-бумага2" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#a8dcff"/><stop offset="0.45" stop-color="#5fa8d8"/>
        <stop offset="1" stop-color="#2b6d99"/>
      </linearGradient>
      <linearGradient id="c1011-бумага3" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffb3a0"/><stop offset="0.45" stop-color="#e8735a"/>
        <stop offset="1" stop-color="#a83c28"/>
      </linearGradient>
      <linearGradient id="c1011-металл" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#5d6a76"/><stop offset="0.18" stop-color="#e8eef4"/>
        <stop offset="0.5" stop-color="#9fb0bd"/><stop offset="0.8" stop-color="#57636f"/>
        <stop offset="1" stop-color="#434d57"/>
      </linearGradient>
      <linearGradient id="c1011-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".7"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".3"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".25"/>
      </linearGradient>
      <filter id="c1011-тень" x="-70%" y="-70%" width="240%" height="240%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3.2" result="р"/>
        <feOffset in="р" dx="3" dy="5" result="с"/>
        <feFlood flood-color="#0a1a24" flood-opacity="0.4" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c1011-контакт" x="-70%" y="-150%" width="240%" height="400%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.3" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#1d1508" flood-opacity="0.4" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c1011-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.1"/>
      </filter>
      <filter id="c1011-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c1011-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c1011-шум)" opacity="0.05"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  /* ВЕРШИНЫ ПО УГЛАМ: теорема синусов. Углы[0] при A, [1] при B, [2] при C,
     сторона AB = длина; поворот вокруг точки (x,y). */
  function вершины(углы, x, y, длина, поворот){
    const A=углы[0]*Math.PI/180, B=углы[1]*Math.PI/180, C=углы[2]*Math.PI/180;
    const AB=длина, AC=длина*Math.sin(B)/Math.sin(C);
    const баз=[[0,0],[AB,0],[AC*Math.cos(A),-AC*Math.sin(A)]];
    const c=Math.cos(поворот||0), s=Math.sin(поворот||0);
    // центрируем по среднему вершин, чтобы фигура не уезжала при повороте
    const цх=(баз[0][0]+баз[1][0]+баз[2][0])/3, цу=(баз[0][1]+баз[1][1]+баз[2][1])/3;
    return баз.map(([px,py])=>{
      const dx=px-цх, dy=py-цу;
      return [x+dx*c-dy*s, y+dx*s+dy*c];
    });
  }

  /* ТРЕУГОЛЬНИК: бумага с зерном, тень, обводка; подписи углов — только те,
     которые действительно нарисованы */
  function треуг(P,материал,углы,подписи,нач,засечки,прямой){
    const [A,B,C]=P;
    const центр=[(A[0]+B[0]+C[0])/3,(A[1]+B[1]+C[1])/3];
    const d=`M${A[0].toFixed(1)} ${A[1].toFixed(1)} L${B[0].toFixed(1)} ${B[1].toFixed(1)} L${C[0].toFixed(1)} ${C[1].toFixed(1)} Z`;
    const стороны=[[A,B,'AB'],[B,C,'BC'],[C,A,'CA']];
    return `<g>
      <path d="${d}" transform="translate(2,4)" fill="#08171f" opacity=".34" filter="url(#c1011-тень)"/>
      <path d="${d}" fill="url(#c1011-${материал||'бумага'})" stroke="${ОБВОД}" stroke-width="2.4">
        ${нач!=null?проявить('12s',нач,нач+0.06):''}</path>
      ${(засечки||[]).map(([ном,кол])=>{
        const [p,q]=стороны[ном];
        const mx=(p[0]+q[0])/2, my=(p[1]+q[1])/2;
        const dx=q[0]-p[0], dy=q[1]-p[1], len=Math.hypot(dx,dy);
        const nx=-dy/len*6, ny=dx/len*6;
        return Array.from({length:кол},(_,i)=>`<g>${анК('opacity','0.3;0.3;1;1','12s',
            '0;'+кт(0.3+i*0.06)+';'+кт(0.38+i*0.06)+';1')}
          <line x1="${(mx+nx*0.5+(i-0.5)*5*nx/6).toFixed(1)}" y1="${(my+ny*0.5+(i-0.5)*5*ny/6).toFixed(1)}"
            x2="${(mx-nx*0.5+(i-0.5)*5*nx/6).toFixed(1)}" y2="${(my-ny*0.5+(i-0.5)*5*ny/6).toFixed(1)}"
            stroke="#7dffc0" stroke-width="2.4"/></g>`).join('');
      }).join('')}
      ${прямой!=null?(()=>{
        const [p,q,r]=[P[прямой==='A'?0:прямой==='B'?1:2], null, null];
        const v=P[прямой==='A'?0:прямой==='B'?1:2];
        const a=P[(прямой==='A'?1:прямой==='B'?2:0)], b=P[(прямой==='A'?2:прямой==='B'?0:1)];
        const u1=[(a[0]-v[0])/Math.hypot(a[0]-v[0],a[1]-v[1]),(a[1]-v[1])/Math.hypot(a[0]-v[0],a[1]-v[1])];
        const u2=[(b[0]-v[0])/Math.hypot(b[0]-v[0],b[1]-v[1]),(b[1]-v[1])/Math.hypot(b[0]-v[0],b[1]-v[1])];
        const к=13;
        const p1=[v[0]+u1[0]*к,v[1]+u1[1]*к], p3=[v[0]+u2[0]*к,v[1]+u2[1]*к];
        const p2=[v[0]+(u1[0]+u2[0])*к,v[1]+(u1[1]+u2[1])*к];
        return `<g>${проявить('12s',0.34,0.42)}
          <path d="M${p1[0].toFixed(1)} ${p1[1].toFixed(1)} L${p2[0].toFixed(1)} ${p2[1].toFixed(1)} L${p3[0].toFixed(1)} ${p3[1].toFixed(1)}"
            fill="none" stroke="${GOLD}" stroke-width="1.8"/></g>`;
      })():''}
      ${(подписи||[]).map(([кто,текст,цвет]) => {
        const i = кто==='A'?0:кто==='B'?1:2;
        const p=P[i];
        const dx=(центр[0]-p[0]), dy=(центр[1]-p[1]), len=Math.hypot(dx,dy)||1;
        return подпись(p[0]+dx/len*20, p[1]+dy/len*20+4, текст, цвет||GOLD, 12);
      }).join('')}
    </g>`;
  }

  const бумага = (x,y,ш,в,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="6" fill="#08171f" opacity=".36" filter="url(#c1011-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="url(#c1011-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('12s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="none" filter="url(#c1011-шум)" opacity=".08"/>
    <line x1="${x+5}" y1="${y+5}" x2="${x+ш-5}" y2="${y+5}" stroke="rgba(255,255,255,.5)" stroke-width="1.2"/>
  </g>`;

  /* ПОДПИСЬ НА ПОДЛОЖКЕ: тёмная плашка + светлый жирный текст.
     Так подпись читается и на светлой фигуре, и на тёмном фоне. */
  const подпись = (x,y,текст,цвет,кегль) => {
    const ш=String(текст).length*(кегль||13)*0.62+14, в=(кегль||13)+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.82)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,кегль||13,цвет||GOLD,true)}
    </g>`;
  };

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#08171f" opacity=".32" filter="url(#c1011-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c1011-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('12s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  /* УГОЛЬНИК: металлический прямой угол для проверки */
  const угольник = (x,y,сторона,поворот) => `<g transform="rotate(${поворот||0} ${x} ${y})">
    <path d="M${x} ${y} L${x+сторона} ${y} L${x} ${y-сторона} Z" fill="url(#c1011-металл)" opacity=".9"
      stroke="rgba(40,45,50,.6)" stroke-width="1.2"/>
    <path d="M${x} ${y} L${x+сторона*0.72} ${y} L${x} ${y-сторона*0.72} Z" fill="none" stroke="rgba(255,255,255,.4)" stroke-width="1"/>
  </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Три треугольника: чем отличаются */
  function F1(s){
    const в = s.сюжет;
    return ВОПРОС('Три треугольника на столе. Чем они отличаются друг от друга?') +
      `<div class="pic">${свг(`
        ${фон(254)}
        ${т(168,24,'Три треугольника',14,GOLD,true)}
        ${треуг(вершины([60,60,60],62,124,70,0),'бумага2',null,null,0.05)}
        ${треуг(вершины([90,60,30],168,126,68,0),'бумага3',null,null,0.18)}
        ${треуг(вершины([120,30,30],266,126,64,0),'бумага',null,null,0.3)}
        ${т(62,158,'все углы острые',12,МУТ)}
        ${т(168,158,'есть прямой угол',12,МУТ)}
        ${т(266,158,'есть тупой угол',12,МУТ)}
        <g>${проявить('12s',0.5,0.6)}
          ${бумага(20,168,296,44,0.5,-0.4)}
          ${т(168,190,'углы разные — значит и названия разные',14,'#2b2110',true)}
          ${т(168,206,'а сумма углов у всех одна',12,'#7a6a44')}</g>
        <g>${проявить('12s',0.72,0.82)}
          ${т(168,240,'значит, различать нужно по углам',12,ИНК)}</g>
      `,254)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='углы'?'hit':(в?'miss':''), 'Углами: острые, прямой или тупой', "r1011Сюжет('углы')")}
        ${BTN(4, в==='цвет'?'miss':'', 'Цветом бумаги', "r1011Сюжет('цвет')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='углы',
        в==='углы' ? 'Верно: треугольники различают по углам и по сторонам. Здесь показаны три вида по углам.'
                   : 'Нет: цвет — это оформление. Различие в углах: острые, прямой, тупой.') : ЖДЁТ()) +
      ПРАВИЛО('Треугольники различают <b>по углам</b> и <b>по сторонам</b>.');
  }

  /* 2. Виды по углам */
  function F2(s){
    const в = s.по_углам;
    return ВОПРОС('У треугольника два острых угла и один прямой. Как он называется?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Виды по углам',14,GOLD,true)}
        ${треуг(вершины([60,60,60],60,120,66,0),'бумага2',null,[['A','60°',BLUE],['B','60°',BLUE],['C','60°',BLUE]],0.05)}
        ${треуг(вершины([90,60,30],168,122,66,0),'бумага3',null,[['A','90°',GOLD]],0.2, null,'A')}
        ${треуг(вершины([120,30,30],266,122,62,0),'бумага',null,[['A','120°',RED]],0.34)}

        <g>${проявить('12s',0.5,0.6)}
          ${стекло(16,166,304,44,0.5)}
          ${т(168,186,'остроугольный: все углы меньше 90°',12,ИНК)}
          ${т(168,202,'прямоугольный: один угол равен 90°',12,ИНК)}</g>
        <g>${проявить('12s',0.72,0.82)}
          ${т(168,234,'тупоугольный: один угол больше 90°',14,RED,true)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='прямоугольный'?'hit':(в?'miss':''), 'Прямоугольный: есть угол 90°', "r1011Угл('прямоугольный')")}
        ${BTN(4, в==='остроугольный'?'miss':'', 'Остроугольный: все углы острые', "r1011Угл('остроугольный')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='прямоугольный',
        в==='прямоугольный' ? 'Верно: если есть прямой угол, треугольник прямоугольный. Остроугольный — когда все три угла острые.'
                            : 'Нет: остроугольный — это когда ВСЕ три угла острые. Здесь есть прямой, значит прямоугольный.') : ЖДЁТ()) +
      ПРАВИЛО('Один прямой угол → <b>прямоугольный</b>; один тупой → <b>тупоугольный</b>.');
  }

  /* 3. Сумма углов: три угла съезжаются на прямую */
  function F3(s){
    const в = s.сумма;
    return ВОПРОС('Чему равна сумма углов треугольника?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Три угла вместе дают 180°',14,GOLD,true)}
        ${треуг(вершины([80,60,40],104,130,96,0),'бумага2',null,[['A','80°',BLUE],['B','60°',GREEN],['C','40°',GOLD]],0.05)}
        <g>${проявить('12s',0.24,0.34)}
          <line x1="30" y1="180" x2="306" y2="180" stroke="${ЛИНИЯ}" stroke-width="2.4"/>
          <line x1="30" y1="174" x2="30" y2="186" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
          <line x1="306" y1="174" x2="306" y2="186" stroke="${ЛИНИЯ}" stroke-width="1.6"/></g>
        ${/* три угла едут на прямую и укладываются в 180° */''}
        <g>${анТК('0 0;0 0;38 100;38 100;0 0','13s','0;'+кт(0.3)+';'+кт(0.6)+';0.9;1')}
          <path d="M30 176 A46 46 0 0 1 74 146 L30 176 Z" fill="rgba(127,209,255,.35)" stroke="${BLUE}" stroke-width="1.6"/></g>
        <g>${анТК('0 0;0 0;128 104;128 104;0 0','13s','0;'+кт(0.38)+';'+кт(0.68)+';0.9;1')}
          <path d="M120 176 A60 60 0 0 1 172 152 L120 176 Z" fill="rgba(143,209,168,.35)" stroke="${GREEN}" stroke-width="1.6"/></g>
        <g>${анТК('0 0;0 0;218 108;218 108;0 0','13s','0;'+кт(0.46)+';'+кт(0.76)+';0.9;1')}
          <path d="M210 176 A52 52 0 0 1 258 158 L210 176 Z" fill="rgba(255,215,106,.35)" stroke="${GOLD}" stroke-width="1.6"/></g>
        <g>${проявить('13s',0.78,0.88)}
          ${т(168,214,'80° + 60° + 40° = 180°',20,GREEN,true)}
          ${т(168,238,'развёрнутый угол — половина полного круга',12,МУТ)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сто восемьдесят'?'hit':(в?'miss':''), '180°: три угла укладываются в развёрнутый угол', "r1011Сумм('сто восемьдесят')")}
        ${BTN(4, в==='сто'?'miss':'', '100°: у каждого треугольника по-разному', "r1011Сумм('сто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сто восемьдесят',
        в==='сто восемьдесят' ? 'Верно: сумма углов любого треугольника равна 180°. Проверка: 80 + 60 + 40 = 180.'
                              : 'Нет: сумма углов одна и та же у всех треугольников — 180°. 80 + 60 + 40 = 180.') : ЖДЁТ()) +
      ПРАВИЛО('Сумма углов треугольника всегда <b>180°</b>.');
  }

  /* 4. Виды по сторонам */
  function F4(s){
    const в = s.по_сторонам;
    return ВОПРОС('У треугольника две стороны равны. Как он называется?') +
      `<div class="pic">${свг(`
        ${фон(268)}
        ${т(168,24,'Виды по сторонам',14,GOLD,true)}
        ${треуг(вершины([70,55,55],62,122,68,0),'бумага',null,null,0.05,[[0,3],[2,3],[1,1]])}
        ${треуг(вершины([60,60,60],170,122,68,0),'бумага2',null,null,0.2,[[0,2],[1,2],[2,2]])}
        ${треуг(вершины([80,60,40],268,122,64,0),'бумага3',null,null,0.34,[[0,1],[1,1],[2,1]])}
        ${т(62,188,'две равные',12,GOLD,true)}
        ${т(170,188,'три равные',12,BLUE,true)}
        ${т(268,188,'все разные',12,GREEN,true)}
        <g>${проявить('12s',0.5,0.6)}
          ${стекло(16,196,304,44,0.5)}
          ${т(168,216,'засечки одинаковой длины — равные стороны',12,ИНК)}
          ${т(168,232,'одна засечка, две или три',12,МУТ)}</g>
        <g>${проявить('12s',0.72,0.82)}
          ${т(168,256,'равнобедренный: две равные стороны',14,GOLD,true)}</g>
      `,268)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='равнобедренный'?'hit':(в?'miss':''), 'Равнобедренный: две стороны равны', "r1011Стор('равнобедренный')")}
        ${BTN(4, в==='равносторонний'?'miss':'', 'Равносторонний: все стороны равны', "r1011Стор('равносторонний')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='равнобедренный',
        в==='равнобедренный' ? 'Верно: две равные стороны — равнобедренный; три равные — равносторонний.'
                             : 'Нет: равносторонний — когда равны ВСЕ три стороны. Две равные стороны дают равнобедренный.') : ЖДЁТ()) +
      ПРАВИЛО('Две равные стороны → <b>равнобедренный</b>, три → <b>равносторонний</b>.');
  }

  /* 5. Равнобедренный: равные стороны и равные углы */
  function F5(s){
    const в = s.равнобедренный;
    return ВОПРОС('У равнобедренного треугольника угол при вершине 40°. Чему равны углы при основании?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,24,'Равные стороны — равные углы',14,GOLD,true)}
        ${треуг(вершины([40,70,70],104,130,92,0),'бумага2',null,[['A','40°',GOLD],['B','70°',GREEN],['C','70°',GREEN]],0.06,[[2,2],[0,2]])}
        <g>${проявить('12s',0.3,0.4)}
          ${бумага(190,58,132,74,0.3,1)}
          ${т(256,84,'180 − 40 = 140',12,'#5a4a2a')}
          ${т(256,108,'140 : 2 = 70',16,'#2b2110',true)}</g>
        <g>${проявить('12s',0.56,0.66)}
          ${стекло(20,168,296,42,0.56)}
          ${т(168,188,'стороны CA и AB равны — равны и углы C и B',12,ИНК)}
          ${т(168,204,'по 70° каждый',12,GREEN,true)}</g>
        <g>${проявить('12s',0.76,0.86)}
          ${т(168,236,'проверка: 40 + 70 + 70 = 180',14,GREEN,true)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='семьдесят'?'hit':(в?'miss':''), '70° каждый: (180 − 40) : 2 = 70', "r1011Равн('семьдесят')")}
        ${BTN(4, в==='сто сорок'?'miss':'', '140° каждый: 180 − 40', "r1011Равн('сто сорок')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='семьдесят',
        в==='семьдесят' ? 'Верно: два угла при основании равны, значит (180 − 40) : 2 = 70°.'
                        : 'Нет: 140° — это сумма двух углов при основании. Каждый из них равен 70°.') : ЖДЁТ()) +
      ПРАВИЛО('В равнобедренном углы при основании <b>равны</b>.');
  }

  /* 6. Равносторонний: все углы по 60 */
  function F6(s){
    const в = s.равносторонний;
    return ВОПРОС('Чему равен каждый угол равностороннего треугольника?') +
      `<div class="pic">${свг(`
        ${фон(246)}
        ${т(168,24,'Равносторонний: все по 60°',14,GOLD,true)}
        ${треуг(вершины([60,60,60],112,132,96,0),'бумага3',null,[['A','60°',BLUE],['B','60°',BLUE],['C','60°',BLUE]],0.06,[[0,3],[1,3],[2,3]])}
        <g>${проявить('12s',0.3,0.4)}
          ${бумага(212,60,110,72,0.3,1)}
          ${т(267,86,'180 : 3',16,'#2b2110',true)}
          ${т(267,110,'= 60°',16,'#2f6b4a',true)}</g>
        <g>${проявить('12s',0.56,0.66)}
          ${стекло(20,166,296,42,0.56)}
          ${т(168,186,'все стороны равны — значит и все углы равны',12,ИНК)}
          ${т(168,202,'а сумма 180°, поэтому каждый по 60°',12,МУТ)}</g>
        <g>${проявить('12s',0.76,0.86)}
          ${т(168,234,'равносторонний всегда остроугольный',14,GREEN,true)}</g>
      `,246)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шестьдесят'?'hit':(в?'miss':''), '60°: 180 : 3 = 60', "r1011Равны('шестьдесят')")}
        ${BTN(4, в==='девяносто'?'miss':'', '90°: как у квадрата', "r1011Равны('девяносто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шестьдесят',
        в==='шестьдесят' ? 'Верно: три равных угла в сумме дают 180°, значит каждый равен 60°.'
                         : 'Нет: 90° дало бы в сумме 270°. Три равных угла по 60° дают 180°.') : ЖДЁТ()) +
      ПРАВИЛО('У равностороннего каждый угол <b>60°</b>.');
  }

  /* 7. Прямоугольный: катеты и гипотенуза */
  function F7(s){
    const в = s.прямоугольный;
    return ВОПРОС('В прямоугольном треугольнике со сторонами 3, 4 и 5 какая сторона самая длинная?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Катеты и гипотенуза',14,GOLD,true)}
        ${треуг(вершины([90,53.13,36.87],92,152,86,0),'бумага2',null,null,0.05,null,'A')}
        ${т(102,146,'катет 3',12,'#2b2110',true)}
        ${т(146,122,'катет 4',12,МУТ)}
        ${т(56,108,'гипотенуза 5',12,GOLD,true)}
        <g>${проявить('12s',0.34,0.44)}
          ${бумага(196,52,124,74,0.34,1)}
          ${т(258,78,'3² + 4² = 9 + 16',12,'#5a4a2a')}
          ${т(258,102,'= 25 = 5²',16,'#2b2110',true)}</g>
        <g>${проявить('12s',0.58,0.68)}
          ${стекло(20,182,296,42,0.58)}
          ${т(168,202,'гипотенуза лежит против прямого угла',12,ИНК)}
          ${т(168,218,'и всегда самая длинная: 5 > 4 > 3',12,GREEN,true)}</g>
        <g>${проявить('12s',0.78,0.88)}
          ${т(168,244,'катеты короче гипотенузы',12,МУТ)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пять'?'hit':(в?'miss':''), 'Гипотенуза 5: она против прямого угла', "r1011Прям('пять')")}
        ${BTN(4, в==='четыре'?'miss':'', 'Катет 4: он длиннее катета 3', "r1011Прям('четыре')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пять',
        в==='пять' ? 'Верно: против прямого угла лежит гипотенуза, и она самая длинная — 5. Проверка: 3² + 4² = 5².'
                   : 'Нет: катет 4 длиннее катета 3, но гипотенуза 5 ещё длиннее: 3² + 4² = 25 = 5².') : ЖДЁТ()) +
      ПРАВИЛО('Гипотенуза — <b>против прямого угла</b>, и она самая длинная.');
  }

  /* 8. Проверка угольником */
  function F8(s){
    const в = s.проверка;
    return ВОПРОС('Как проверить, есть ли в треугольнике прямой угол?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,24,'Прикладываем угольник',14,GOLD,true)}
        ${треуг(вершины([90,60,30],118,152,104,0),'бумага2',null,null,0.05,null,'A')}
        ${/* угольник едет к каждой вершине */''}
        <g>${анТК('0 0;0 0;-62 0;-62 0;0 0','13s','0;'+кт(0.2)+';'+кт(0.5)+';0.9;1')}
          ${угольник(118,152,26,0)}</g>
        <g>${проявить('12s',0.5,0.6)}
          ${бумага(20,196,296,42,0.5,-0.4)}
          ${т(168,216,'совпал с углом — значит угол прямой',12,'#2b2110')}
          ${т(168,232,'не совпал — угол острый или тупой',12,'#7a6a44')}</g>
        <g>${проявить('12s',0.72,0.82)}
          ${т(168,180,'так же проверяют стороны: линейкой',12,ИНК)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='угольник'?'hit':(в?'miss':''), 'Приложить угольник к каждому углу', "r1011Пров('угольник')")}
        ${BTN(4, в==='наглаз'?'miss':'', 'Посмотреть на глаз, без инструмента', "r1011Пров('наглаз')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='угольник',
        в==='угольник' ? 'Верно: прямой угол проверяют угольником или линейкой с прямым углом: приложил — и видно.'
                       : 'Нет: на глаз легко ошибиться, особенно у тупого угла. Нужен угольник.') : ЖДЁТ()) +
      ПРАВИЛО('Прямой угол проверяют <b>угольником</b>, стороны — линейкой.');
  }

  /* 9. Ловушка: виды независимы */
  function F9(s){
    const в = s.ловушка;
    return ВОПРОС('Может ли равнобедренный треугольник быть прямоугольным?') +
      `<div class="pic">${свг(`
        ${фон(270)}
        ${т(168,24,'Виды независимы друг от друга',14,GOLD,true)}
        ${треуг(вершины([90,45,45],78,126,66,0),'бумага',null,[['A','90°',GOLD],['B','45°',BLUE],['C','45°',BLUE]],0.05,[[2,2],[0,2]],'A')}
        ${треуг(вершины([120,30,30],212,126,66,0),'бумага3',null,[['A','120°',RED],['B','30°',BLUE],['C','30°',BLUE]],0.24,[[2,2],[0,2]])}
        ${т(78,190,'90°, 45°, 45°',14,GREEN,true)}
        ${т(212,190,'120°, 30°, 30°',14,GREEN,true)}
        <g>${проявить('12s',0.48,0.58)}
          ${стекло(16,198,304,44,0.48)}
          ${т(168,218,'две классификации независимы',12,ИНК)}
          ${т(168,234,'оба сразу и прямоугольные, и равнобедренные',12,МУТ)}</g>
        <g>${проявить('12s',0.72,0.82)}
          ${т(168,258,'а равносторонний всегда остроугольный',14,GREEN,true)}</g>
      `,270)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='может'?'hit':(в?'miss':''), 'Может: 45°, 45° и 90° — и равнобедренный, и прямоугольный', "r1011Лов('может')")}
        ${BTN(4, в==='неможет'?'miss':'', 'Не может: прямоугольный всегда разносторонний', "r1011Лов('неможет')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='может',
        в==='может' ? 'Верно: 45°, 45° и 90° — стороны две равны, угол прямой. Оба названия подходят сразу.'
                    : 'Нет: может. Возьмите 45°, 45° и 90°: два угла равны, значит две стороны равны, а угол прямой.') : ЖДЁТ()) +
      ПРАВИЛО('Классификации <b>независимы</b>: треугольник может быть сразу двух видов.');
  }

  /* 10. Сводка */
  function F10(s){
    const в = s.сводка;
    return ВОПРОС('По какому признаку треугольник называют прямоугольным?') +
      `<div class="pic">${свг(`
        ${фон(254)}
        ${т(168,24,'Две классификации',14,GOLD,true)}
        ${бумага(14,38,150,190,0.05,-1)}
        ${бумага(172,38,150,190,0.2,1)}
        ${т(89,64,'по углам',14,'#2b2110',true)}
        ${т(247,64,'по сторонам',14,'#2b2110',true)}
        ${[['остроугольный','все углы < 90°'],['прямоугольный','есть 90°'],['тупоугольный','есть > 90°']]
          .map(([имя,что],i)=>`<g>${проявить('12s',0.1+i*0.08,0.18+i*0.08)}
            ${т(89,96+i*40,имя,12,'#2b2110',true)}
            ${т(89,112+i*40,что,12,'#7a6a44')}</g>`).join('')}
        ${[['разносторонний','все стороны разные'],['равнобедренный','две равны'],['равносторонний','все равны']]
          .map(([имя,что],i)=>`<g>${проявить('12s',0.3+i*0.08,0.38+i*0.08)}
            ${т(247,96+i*40,имя,12,'#2b2110',true)}
            ${т(247,112+i*40,что,12,'#7a6a44')}</g>`).join('')}
        <g>${проявить('12s',0.66,0.76)}
          ${т(168,246,'названия не мешают друг другу',12,GREEN,true)}</g>
      `,254)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='угол'?'hit':(в?'miss':''), 'По углу: есть прямой угол 90°', "r1011Свод('угол')")}
        ${BTN(4, в==='сторона'?'miss':'', 'По сторонам: все стороны разные', "r1011Свод('сторона')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='угол',
        в==='угол' ? 'Верно: «прямоугольный» говорит про угол. Про стороны говорят «разносторонний», «равнобедренный», «равносторонний».'
                   : 'Нет: про стороны — другие слова. «Прямоугольный» — это про угол 90°.') : ЖДЁТ()) +
      ПРАВИЛО('Название по углам и название по сторонам <b>складываются</b>.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Все три угла меньше 90°. Какой это треугольник?', варианты:[
        {т:'остроугольный', ок:true,  почему:'Все углы острые.'},
        {т:'тупоугольный', ок:false, почему:'Тупоугольный — когда есть угол больше 90°.'}]},
    { вопрос:'Две стороны равны. Как называется?', варианты:[
        {т:'равнобедренный', ок:true,  почему:'Две равные стороны.'},
        {т:'равносторонний', ок:false, почему:'Равносторонний — когда равны все три.'}]},
    { вопрос:'Угол при вершине равнобедренного 40°. Углы при основании?', варианты:[
        {т:'по 70°', ок:true,  почему:'(180 − 40) : 2 = 70.'},
        {т:'по 140°', ок:false, почему:'140 — это их сумма.'}]},
    { вопрос:'Стороны 3, 4, 5. Какая самая длинная?', варианты:[
        {т:'5 — гипотенуза', ок:true,  почему:'3² + 4² = 5².'},
        {т:'4 — катет', ок:false, почему:'4 длиннее 3, но 5 ещё длиннее.'}]},
    { вопрос:'Может ли равнобедренный быть тупоугольным?', варианты:[
        {т:'да: 120°, 30°, 30°', ок:true,  почему:'Два угла равны, один тупой.'},
        {т:'нет, никогда', ок:false, почему:'Может: 30 + 30 + 120 = 180.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Две опоры: углы сравниваем с 90°, стороны — засечками. Сумма углов всегда 180°.') +
        `<div class="ask">${BTN(4,'','пройти заново','r1011Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r1011Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('По углам: <b>остроугольный · прямоугольный · тупоугольный</b>.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · по углам',
    задания:[
      { ф:'Углы 60°, 60°, 60°. Какой треугольник?', о:['остроугольный','прямоугольный','тупоугольный'], в:0, р:'Все углы острые.' },
      { ф:'Углы 90°, 45°, 45°. Какой?', о:['прямоугольный','остроугольный','тупоугольный'], в:0, р:'Есть угол 90°.' },
      { ф:'Углы 120°, 30°, 30°. Какой?', о:['тупоугольный','прямоугольный','остроугольный'], в:0, р:'Есть угол больше 90°.' },
      { ф:'Чему равна сумма углов треугольника?', о:['180°','360°','90°'], в:0, р:'Всегда 180°.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · по сторонам',
    задания:[
      { ф:'Все три стороны равны. Как называется?', о:['равносторонний','равнобедренный','разносторонний'], в:0, р:'Три равные стороны.' },
      { ф:'Стороны 5, 5, 8. Как называется?', о:['равнобедренный','равносторонний','разносторонний'], в:0, р:'Две равные стороны.' },
      { ф:'Стороны 4, 5, 6. Как называется?', о:['разносторонний','равнобедренный','равносторонний'], в:0, р:'Все стороны разные.' },
      { ф:'Чему равен каждый угол равностороннего?', о:['60°','90°','45°'], в:0, р:'180 : 3 = 60.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · смешанные и ловушки',
    задания:[
      { ф:'Может ли равнобедренный быть прямоугольным?', о:['да: 45°, 45°, 90°','нет'], в:0, р:'Два угла равны и есть прямой.' },
      { ф:'Угол при вершине 40°, треугольник равнобедренный. Углы при основании?', о:['по 70°','по 140°','по 50°'], в:0, р:'(180 − 40) : 2 = 70.' },
      { ф:'Стороны 3, 4, 5. Какой это треугольник?', о:['прямоугольный','тупоугольный','остроугольный'], в:0, р:'3² + 4² = 5².' },
      { ф:'Равносторонний может быть тупоугольным?', о:['нет, он всегда остроугольный','да'], в:0, р:'Все углы по 60° — острые.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r1011T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r1011TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L1011={
    id: ID, title:'Виды треугольников', ico:'📐',
    src:'Математика · 6 класс · Геометрия', subj:'math',
    explain: [
      'На столе три треугольника. У первого все углы острые, у второго есть прямой угол, у третьего — тупой. Углы разные, поэтому и названия разные. При этом сумма углов у всех трёх одинакова. Треугольники различают двумя способами: по углам и по сторонам, и эти два способа не мешают друг другу.',

      'Разберём виды по углам. Если все три угла меньше 90°, треугольник называют остроугольным. Если один из углов равен 90°, это прямоугольный треугольник. Если один угол больше 90°, треугольник тупоугольный. Заметьте: острых углов в треугольнике всегда хотя бы два, а прямой или тупой может быть только один. Частая ошибка — называть тупоугольный треугольник «остроугольным» из-за двух острых углов. Как проверить себя: найди самый большой угол и сравни его с 90°.',

      'Почему двух прямых углов быть не может? Потому что сумма углов треугольника равна 180°. Это один из главных фактов геометрии. Проверим на разных треугольниках: 60 + 60 + 60 = 180, 90 + 60 + 30 = 180, 120 + 30 + 30 = 180, 80 + 60 + 40 = 180. Если бы в треугольнике было два прямых угла, их сумма уже дала бы 180°, и третьему углу не осталось бы места. Частая ошибка — забывать про этот факт. Как проверить себя: сложи все три угла.',

      'Теперь виды по сторонам. Если все три стороны разной длины, треугольник разносторонний. Если две стороны равны — равнобедренный. Если равны все три — равносторонний. На чертежах равные стороны отмечают одинаковыми засечками: одна засечка, две или три. Частая ошибка — путать равнобедренный и равносторонний. Как проверить себя: посчитай, сколько сторон одинаковой длины.',

      'У равнобедренного треугольника есть важное свойство: углы при основании равны. Пусть угол при вершине равен 40°. Тогда на два угла при основании остаётся 180 − 40 = 140°, а так как они равны, каждый равен 140 : 2 = 70°. Проверка: 40 + 70 + 70 = 180. Частая ошибка — делить 140 пополам не подумав или отвечать 140°. Как проверить себя: сумма всех трёх углов должна дать 180°.',

      'Равносторонний треугольник — частный случай равнобедренного. У него равны все три стороны, значит равны и все три угла. Сумма равна 180°, поэтому каждый угол равен 180 : 3 = 60°. Отсюда важный вывод: равносторонний треугольник всегда остроугольный, тупоугольным он быть не может. Частая ошибка — считать, что у равностороннего углы по 90°. Как проверить себя: три раза по 60° дают 180°.',

      'Разберём прямоугольный треугольник. Стороны, которые образуют прямой угол, называют катетами, а сторону напротив прямого угла — гипотенузой. Гипотенуза всегда самая длинная. Проверим на известном треугольнике 3, 4, 5: 3² + 4² = 9 + 16 = 25, а 5² = 25 — равенство выполняется, и это прямоугольный треугольник. Частая ошибка — считать гипотенузой самый короткий катет. Как проверить себя: гипотенуза лежит против прямого угла.',

      'Как проверить виды на практике? Прямой угол проверяют угольником: прикладывают его к углу, и если стороны совпали, угол прямой. Если угол меньше прямого — он острый, если больше — тупой. Стороны измеряют линейкой и сравнивают. Частая ошибка — определять на глаз: тупой угол легко принять за прямой. Как проверить себя: используй инструмент.',

      'Полезно помнить, что две классификации независимы. Равнобедренный треугольник может быть прямоугольным: углы 45°, 45° и 90°. Может быть и тупоугольным: 30°, 30° и 120°. А равносторонний всегда остроугольный, потому что все его углы по 60°. Частая ошибка — думать, что у треугольника может быть только одно название. Как проверить себя: назови вид и по углам, и по сторонам.',

      'Соберём всё вместе. По углам: остроугольный — все углы меньше 90°, прямоугольный — есть угол 90°, тупоугольный — есть угол больше 90°. По сторонам: разносторонний — все стороны разные, равнобедренный — две равны, равносторонний — все три равны. Сумма углов всегда 180°, у равнобедренного углы при основании равны, у равностороннего каждый угол 60°, а в прямоугольном гипотенуза самая длинная. Частая ошибка — перепутать, о чём говорит название. Как проверить себя: спроси, про углы это или про стороны.',

      'Пройти практику: пять уровней про виды по углам, виды по сторонам, равнобедренный и прямоугольный треугольники. Дальше ждут три тренажёра.',

      'Первый тренажёр — виды по углам: сравниваем самый большой угол с 90°.',

      'Второй тренажёр — виды по сторонам: считаем, сколько сторон одинаковой длины.',

      'Третий тренажёр — смешанные задачи и ловушки: помним, что классификации независимы.'
    ],
    check: {
      q:'У треугольника два острых угла и один прямой. Как он называется?',
      choices:['прямоугольный','остроугольный','тупоугольный'],
      ans:0,
      exp:'Если один угол равен 90°, треугольник прямоугольный.'
    },
    tasks: [
      { q:'Угол при вершине равнобедренного 40°. Чему равны углы при основании?', kind:'choice',
        choices:['по 70°','по 140°'], ans:0,
        hints:['180 − 40 = 140.','140 : 2 = 70.'],
        sol:'По 70°.' },
      { q:'Стороны 3, 4, 5. Какой это треугольник?', kind:'choice',
        choices:['прямоугольный','тупоугольный'], ans:0,
        hints:['3² + 4² = 9 + 16.','25 = 5².'],
        sol:'Прямоугольный.' },
      { q:'Может ли равнобедренный треугольник быть тупоугольным?', kind:'choice',
        choices:['да: 120°, 30°, 30°','нет'], ans:0,
        hints:['Сумма должна быть 180°.','30 + 30 + 120 = 180.'],
        sol:'Да.' },
      { q:'Чему равен каждый угол равностороннего треугольника?', kind:'choice',
        choices:['60°','90°'], ans:0,
        hints:['Три равных угла в сумме 180°.','180 : 3 = 60.'],
        sol:'60°.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L1011.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Три треугольника','Чем отличаются'],
      2:['По углам','Острый, прямой, тупой'],
      3:['Сумма углов','180°'],
      4:['По сторонам','Засечки равных сторон'],
      5:['Равнобедренный','Углы при основании'],
      6:['Равносторонний','Каждый по 60°'],
      7:['Прямоугольный','Катеты и гипотенуза'],
      8:['Проверка','Угольник и линейка'],
      9:['Виды независимы','Может быть сразу двух видов'],
      10:['Две классификации','Сводка'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','По углам'],
      13:['Тренажёр 2','По сторонам'],
      14:['Тренажёр 3','Смешанные и ловушки']
    };
    const з=ЗАГОЛОВКИ[f]||['Виды треугольников','Геометрия'];
    el.innerHTML = `<div class="s6 l1011" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r1011Сюжет=(к)=>{ S().сюжет=к; chRender(0); };
  window.r1011Угл=(к)=>{ S().по_углам=к; chRender(0); };
  window.r1011Сумм=(к)=>{ S().сумма=к; chRender(0); };
  window.r1011Стор=(к)=>{ S().по_сторонам=к; chRender(0); };
  window.r1011Равн=(к)=>{ S().равнобедренный=к; chRender(0); };
  window.r1011Равны=(к)=>{ S().равносторонний=к; chRender(0); };
  window.r1011Прям=(к)=>{ S().прямоугольный=к; chRender(0); };
  window.r1011Пров=(к)=>{ S().проверка=к; chRender(0); };
  window.r1011Лов=(к)=>{ S().ловушка=к; chRender(0); };
  window.r1011Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r1011Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r1011Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r1011T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r1011TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L1011; else arr.push(L1011);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA1011={render:render, L:L1011};
})();
