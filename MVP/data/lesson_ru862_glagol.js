/* ============ РУССКИЙ ЯЗЫК · УРОК 862 · «ГЛАГОЛ: ЧТО ДЕЛАЕТ СЛОВО» ============
   3 класс, «Морфология». Была заглушкой в soon_lessons.js — теперь урок.
   Продолжение 861: лодка «Чайка» дошла до мыса, где живут слова-действия.
   Рисунки — общая библиотека моря MVP/data/ris_more.js (window.РМ).

   СЮЖЕТ. «Мыс действий». На мысу стоит маяк, а у смотрителя правило: на этом
   берегу всё делается только по слову. Скажешь предмет — ничего не случится;
   скажешь действие — парус поднимется, якорь упадёт, чайка взлетит.

   РУКАМИ: команды на палубе — касаешься слова, и если это глагол, действие
   происходит на рисунке (парус ползёт вверх, якорь уходит под воду, чайка
   срывается с мачты, юнга машет); часы времени — вчера / сегодня / завтра, и
   сцена с глаголом меняется; окончания прошедшего времени для лодки,
   корабля и облака.

   ПРОВЕРЕНО ПО ПРОГРАММЕ 3 КЛАССА (Канакина, Горецкий):
     глагол обозначает действие предмета и отвечает на вопросы что делать?
     что сделать? что делает? что сделал? что будет делать?;
     времена: прошедшее (плыл), настоящее (плывёт), будущее (поплывёт,
     будет плыть);
     число: чайка летит — чайки летят; рыбы плещутся;
     в прошедшем времени глагол изменяется по родам: корабль плыл, лодка
     плыла, облако плыло;
     не с глаголами пишется раздельно: не спал;
     неопределённая форма отвечает на вопрос что делать? (грести, плыть);
     плавание, бег, пловец — существительные (что? кто?), плавать — глагол;
     «Ветер надул паруса, и лодка помчалась к маяку»: глаголы надул,
     помчалась (2). */
(function(){
  'use strict';

  const ID = 862;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f', ОБВОД='#33291e';
  const ЧЕРНИЛА='#2e2416';

  const ДЕЛА = [
    {ключ:'команды', имя:'Отдать команды словами', итог:'4 действия'},
    {ключ:'время',   имя:'Запустить часы времени', итог:'три времени'},
    {ключ:'род',     имя:'Подобрать окончания',    итог:'три рода'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const КОМАНДЫ = [
    {с:'поднимает', гл:'парус'},
    {с:'парус',     почему:'«Парус» — это <b>что?</b> Предмет. Предмет сам ничего не делает.'},
    {с:'бросает',   гл:'якорь'},
    {с:'синий',     почему:'«Синий» отвечает на вопрос <b>какой?</b> — это признак, а не действие.'},
    {с:'летит',     гл:'чайка'},
    {с:'якорь',     почему:'«Якорь» — <b>что?</b> Это предмет, а не действие.'},
    {с:'машет',     гл:'юнга'},
    {с:'быстро',    почему:'«Быстро» отвечает на вопрос <b>как?</b> Оно говорит, как делают, но само — не действие.'}
  ];
  const ГЛАГОЛЫ1 = КОМАНДЫ.map((к,i)=>к.гл?i:-1).filter(i=>i>=0);
  const ВРЕМЕНА = [
    {ключ:'вчера',   слово:'плыл',     время:'прошедшее', вопрос:'что делал?'},
    {ключ:'сегодня', слово:'плывёт',   время:'настоящее', вопрос:'что делает?'},
    {ключ:'завтра',  слово:'поплывёт', время:'будущее',   вопрос:'что сделает?'}
  ];
  const РОДЫ = [
    {кто:'корабль', род:'он',  верно:0},
    {кто:'лодка',   род:'она', верно:1},
    {кто:'облако',  род:'оно', верно:2}
  ];
  const ОКОНЧ = ['плыл','плыла','плыло'];

  const CSS=`
  #lvis .s6.l862{gap:14px}
  #lvis .s6.l862 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l862 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l862 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l862 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l862 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l862 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l862 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l862 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l862 .карт .текст b{color:${GOLD}}
  #lvis .s6.l862 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l862 .правило b{color:${GOLD}}
  #lvis .s6.l862 .журнал{width:100%;padding:12px 14px;border-radius:6px 16px 16px 6px;
    background:linear-gradient(90deg,#efe2c0,#dcc79a);border-left:7px solid #7a4a24;display:flex;flex-direction:column;gap:7px;color:${ЧЕРНИЛА}}
  #lvis .s6.l862 .журнал .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:#6a4a24}
  #lvis .s6.l862 .журнал .шапка b{font-size:18px;letter-spacing:0;text-transform:none;color:#7a2a10;font-family:Georgia,serif}
  #lvis .s6.l862 .журнал .шапка b.готово{color:#1a6a3a}
  #lvis .s6.l862 .журнал ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l862 .журнал li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:#6a5236}
  #lvis .s6.l862 .журнал li i{flex:0 0 22px;width:22px;height:22px;border-radius:50%;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;border:1.5px solid #a88a5a;color:#8a6a3a}
  #lvis .s6.l862 .журнал li.есть{color:${ЧЕРНИЛА}}
  #lvis .s6.l862 .журнал li.есть i{border-color:#1a4a6a;background:#2a6a9a;color:#fff}
  #lvis .s6.l862 .журнал li span{margin-left:auto;white-space:nowrap;font-size:14px;color:#8a6a3a;font-family:Georgia,serif}
  #lvis .s6.l862 .журнал li.есть span{color:#1a6a3a;font-weight:700}
  #lvis .s6.l862 .буйки{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;width:100%}
  #lvis .s6.l862 .буйки button{min-height:56px;border-radius:28px;cursor:pointer;font:inherit;font-size:19px;font-weight:700;
    font-family:Georgia,serif;border:2px solid #e86a5a;background:linear-gradient(180deg,#fff6ea,#f0dcc0);color:${ЧЕРНИЛА};
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),opacity 200ms}
  #lvis .s6.l862 .буйки button:active{transform:scale(.96)}
  #lvis .s6.l862 .буйки button.пойман{opacity:.35;border-color:${GREEN};text-decoration:line-through}
  #lvis .s6.l862 .буйки button.мимо{border-color:${RED};animation:l862нет 360ms cubic-bezier(.23,1,.32,1)}
  @keyframes l862нет{0%,100%{transform:none}25%{transform:translateX(-6px)}75%{transform:translateX(6px)}}
  #lvis .s6.l862 .ряд{display:grid;gap:8px;width:100%}
  #lvis .s6.l862 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:18px;font-weight:700;
    border:1.5px solid rgba(127,209,255,.5);background:rgba(127,209,255,.1);color:${ИНК};padding:6px 4px;font-family:Georgia,serif;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l862 .ряд button:active{transform:translateY(2px)}
  #lvis .s6.l862 .ряд button.вкл{border-color:${GOLD};background:rgba(255,215,106,.22);color:${GOLD}}
  #lvis .s6.l862 .случай{width:100%;display:flex;flex-direction:column;gap:6px}
  #lvis .s6.l862 .случай .что{font-size:19px;color:var(--ink);font-family:Georgia,serif}
  #lvis .s6.l862 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l862 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:17px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l862 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l862 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l862 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l862 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-size:18px}
  #lvis .s6.l862 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-size:18px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l862 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l862 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l862 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l862 .уровни .точка.сейчас{background:${GOLD};animation:l862dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l862dot{0%,100%{opacity:1}50%{opacity:.55}}
  #lvis .s6.l862 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l862{-webkit-text-size-adjust:100%}
  #lvis .s6.l862 [data-anim]{animation:l862rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l862rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l862 [data-anim]{animation:none!important}
    #lvis .s6.l862 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l862 button{transition:none!important;animation:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l862-style');
      if(!s){ s=document.createElement('style'); s.id='l862-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Судовой журнал</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,f) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, 'r862Отв('+f+','+к+')')).join('')}</div>`;
  const ЖУРНАЛ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'журнал',
      `<div class="шапка"><span>Мыс действий</span><b class="${всё?'готово':''}">${
        всё?'мыс пройден':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}<span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.45;0.45;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const вырасти = (длит,доля) => анК('opacity','0.15;0.15;1;1',длит,'0;'+кт(доля)+';'+кт(доля+0.06)+';1');
  const сдвигРаз = (из,в,длит,задержка) => ДВИЖ ?
    `<animateTransform attributeName="transform" type="translate" from="${из}" to="${в}" dur="${длит}" begin="${(задержка||0).toFixed(2)}s"
       fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';
  const масштабРаз = (из,в,длит) => ДВИЖ ?
    `<animateTransform attributeName="transform" type="scale" from="${из}" to="${в}" dur="${длит}" fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}" additive="sum"/>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  const Р = () => window.РМ;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <filter id="c862-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="1.5" dy="2" stdDeviation="1.6" flood-color="#0b1c2a" flood-opacity=".45"/>
      </filter>
      <linearGradient id="c862-цепь" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#5a5e68"/><stop offset="0.5" stop-color="#b8bcc6"/><stop offset="1" stop-color="#4a4e56"/>
      </linearGradient>
      <linearGradient id="c862-палуба" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#b88450"/><stop offset="1" stop-color="#7a4e24"/>
      </linearGradient>
      <radialGradient id="c862-фонарь" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#fff4c0" stop-opacity=".85"/><stop offset="1" stop-color="#ffb040" stop-opacity="0"/>
      </radialGradient>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${Р().defs()}${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c862-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(14,26,20,.92)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  /* парус на мачте корабля (в координатах корабля): поднимается снизу вверх */
  const поднятыйПарус = (свежий) => `<g transform="translate(0 -106)"><g>${свежий?масштабРаз('1 0.05','1 1','1.2s'):''}
      <path d="M4 0 Q60 46 6 99 Z" fill="url(#рм-парус)" stroke="${ОБВОД}" stroke-width="1.3"/>
      ${[0.3,0.55,0.8].map(t0=>`<path d="M5 ${(99*t0).toFixed(1)} q28 -4 ${(46*(1-Math.abs(t0-0.5))).toFixed(1)} -2" stroke="#c8b48a" stroke-width=".8" fill="none"/>`).join('')}
      <path d="M-4 10 Q-44 52 -6 86 Z" fill="#efe2c4" stroke="${ОБВОД}" stroke-width="1.2"/></g></g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Команды на палубе */
  function F1(s){
    const Н=300, М=Р(), сдел=Array.isArray(s.сдел1)?s.сдел1:[], ош=s.ош1, свеж=s.свеж1, всё=ГЛАГОЛЫ1.every(i=>сдел.includes(i));
    const есть=(гл)=>сдел.some(i=>КОМАНДЫ[i].гл===гл), новое=(гл)=>свеж!=null&&КОМАНДЫ[свеж].гл===гл;
    const X=168, Y=210, м=0.95;
    const якорьВнизу=есть('якорь');
    const цепь = якорьВнизу
      ? `<path d="M${X-74*м} ${Y-6*м} Q${X-86*м} ${Y+20} ${X-84*м} ${Y+36}" stroke="url(#c862-цепь)" stroke-width="2.4" stroke-dasharray="3 2" fill="none"/>`
      : `<path d="M${X-74*м} ${Y-6*м} L${X-80*м} ${Y+6}" stroke="url(#c862-цепь)" stroke-width="2.4" stroke-dasharray="3 2"/>`;
    const якорь = якорьВнизу
      ? `<g>${новое('якорь')?сдвигРаз('0 -58','0 0','1s',0):''}<g opacity=".85">${М.якорь(X-84*м,Y+72,0.7)}</g></g>
         ${новое('якорь')&&ДВИЖ?[0,1,2].map(k=>`<ellipse cx="${X-84*м}" cy="${Y+26}" rx="4" ry="1.4" fill="none" stroke="#fff" stroke-width="1.6" opacity="0"><animate attributeName="rx" values="4;${26+k*10}" dur="1.2s" begin="${(0.7+k*0.2).toFixed(1)}s" fill="freeze"/><animate attributeName="opacity" values="0.9;0" dur="1.2s" begin="${(0.7+k*0.2).toFixed(1)}s" fill="freeze"/></ellipse>`).join(''):''}`
      : М.якорь(X-80*м,Y+40,0.7);
    const чайкаЛетит=есть('чайка');
    const чайка = чайкаЛетит
      ? `<g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 0;-120 -70;-240 -40" dur="6s" ${новое('чайка')?'fill="freeze"':'repeatCount="indefinite"'}/>`:''}${М.чайка(X+6,Y-118*м,0.9,{влево:true})}</g>`
      : М.чайка(X+4,Y-116*м,0.8,{летит:false});
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Лодка «Чайка» пришла к мысу. Смотритель маяка предупреждает: «Здесь всё делается только <b>по слову</b>. Скажешь, что делать, — и сделается. Скажешь предмет — ничего не будет». Касайся слов: найди четыре слова-<b>действия</b>.') +
      `<div class="pic">${свг(`
        ${М.небо(336,110,{солнце:[46,36,11],облака:[[150,26,0.6,10],[270,58,0.45,-6]]})}
        ${М.утёс(300,112,0.9)}${М.маяк(300,56,0.34,false)}
        ${М.море(110,Н-110,336,{дорожка:46})}
        ${цепь}${якорь}
        ${М.юнга(X-40,Y+14,0.5,{поза:есть('юнга')?'машет':'стоит',безНог:true})}
        ${М.парусник(X,Y,м,{парус:0,качка:false,имя:'Чайка',якорь:false})}
        ${есть('парус')?`<g transform="translate(${X} ${Y}) scale(${м})">${поднятыйПарус(новое('парус'))}</g>`:''}
        ${чайка}
        ${подпись(256,Н-14,'действий: '+сдел.length+' из 4',всё?GREEN:ИНК,13)}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="буйки">${КОМАНДЫ.map((к,i)=>BTN(3+(i%5),сдел.includes(i)?'пойман':(ош===i?'мимо':''),к.с,'r862Слово('+i+')')).join('')}</div>` +
      (всё ? РАЗБОР(true,'Поднимает, бросает, летит, машет — каждое слово <b>что-то делает</b>, и на рисунке это случилось. Они отвечают на вопрос <b>что делает?</b>') :
        ош!=null ? РАЗБОР(false,КОМАНДЫ[ош].почему) :
        СКАЗ('Подсказка','Спроси: «что делает?» Если слово отвечает — это действие, и оно сработает.')) +
      (всё ? ПРАВИЛО('<b>Глагол</b> — часть речи, которая обозначает <b>действие</b> предмета и отвечает на вопросы <b>что делает? что делать? что сделал?</b>') : '');
  }

  /* 2. Вопрос глагола */
  function F2(s){
    const Н=260, в=s.ответ2, ок=в===0, М=Р();
    const водоросль=(x,в2,цв)=>`<path d="M${x} ${Н} q-8 -${в2*0.3} 0 -${в2*0.55} q8 -${в2*0.25} 0 -${в2*0.45}" stroke="${цв}" stroke-width="4" fill="none" stroke-linecap="round">${ДВИЖ?`<animateTransform attributeName="transform" type="rotate" values="-5 ${x} ${Н};5 ${x} ${Н};-5 ${x} ${Н}" dur="${(3+x%3).toFixed(0)}s" repeatCount="indefinite"/>`:''}</path>`;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('У мыса вода прозрачная, видно дно. Смотритель показывает рыбку и спрашивает: «Что она делает?» Какое слово отвечает на вопрос <b>что делает?</b>') +
      `<div class="pic">${свг(`
        <g><rect x="0" y="0" width="336" height="${Н}" fill="url(#рм-море)"/></g>
        ${[0,1,2,3].map(k=>`<path d="M${k*90-30} 0 l40 ${Н}" stroke="#e8f6ff" stroke-width="18" opacity=".05" data-декор="1"/>`).join('')}
        <path d="M0 ${Н-16} q80 -20 160 -6 q90 14 176 -8 V${Н} H0z" fill="#6a5a3c" data-декор="1"/>
        ${водоросль(24,80,'#3f8a4a')}${водоросль(40,56,'#5aa85a')}${водоросль(300,70,'#3f8a4a')}${водоросль(316,48,'#5aa85a')}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="40 0;-40 6;40 0" dur="7s" repeatCount="indefinite"/>`:''}${М.ёрш(180,110,1.3)}</g>
        ${[0,1,2].map(k=>`<circle cx="${140+k*6}" cy="100" r="${1.6+k*0.8}" fill="none" stroke="#e8f6ff" stroke-width="1" opacity=".8">${анЛин('cy','100;20',(2+k*0.5).toFixed(1)+'s')}${анЛин('opacity','0.8;0','2.4s')}</circle>`).join('')}
        ${т(168,190,'рыбка что делает?',16,ИНК,true,undefined,'#0f3f6c')}
        ${ок?подпись(168,Н-30,'плывёт — отвечает на «что делает?»',GREEN,13):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['плывёт','пловец','плавучий'],0,в,2) +
      (в==null ? СКАЗ('Вопрос','Какое слово отвечает на вопрос «что делает?»') :
        РАЗБОР(ок,['Рыбка что делает? — <b>плывёт</b>. Это глагол: он называет действие.',
          '«Пловец» — <b>кто?</b> Это существительное: тот, кто плавает. А действие — <b>плывёт</b>.',
          '«Плавучий» — <b>какой?</b> Это признак. Действие — <b>плывёт</b>.'][в])) +
      (ок ? ПРАВИЛО('Глагол узнают по вопросу: <b>что делает? что сделал? что будет делать?</b>') : '');
  }

  /* 3. Часы времени */
  function F3(s){
    const Н=280, М=Р(), к=s.время3||'сегодня', вид=Array.isArray(s.видел3)?s.видел3:[], все=вид.length>=3, в=s.ответ3, ок=в===2;
    const в3=ВРЕМЕНА.find(x=>x.ключ===к);
    const сцена = к==='вчера'
      ? `${М.небо(336,120,{ночь:true})}<g><rect x="0" y="0" width="336" height="120" fill="#1a2438" opacity=".5"/></g>
         <path d="M40 60 q10 -20 34 -16 q12 -16 34 -6 q18 -6 26 10 q14 2 10 16 H40z" fill="#4a5468" stroke="${ОБВОД}" stroke-width=".8"/>
         <path d="M96 76 l-8 16 h8 l-6 18 l18 -24 h-8 l6 -10z" fill="#ffe46a" stroke="${ОБВОД}" stroke-width=".7">${анЛин('opacity','1;0;0;1;0','1.6s')}</path>
         ${М.море(120,Н-120,336,{ночь:true})}
         <g transform="rotate(-8 168 200)">${М.парусник(168,196,0.8,{имя:'Чайка'})}</g>
         ${Array.from({length:14},(_,k)=>`<line x1="${20+k*23}" y1="${20+(k%4)*10}" x2="${12+k*23}" y2="${44+(k%4)*10}" stroke="#a8c8e8" stroke-width="1.2" opacity=".7" data-декор="1">${анЛин('transform','0','1s')}</line>`).join('')}`
      : к==='сегодня'
      ? `${М.небо(336,120,{солнце:[270,40,12],облака:[[70,34,0.6,10]]})}${М.берег(120,{x1:190})}${М.море(120,Н-120,336,{дорожка:270})}
         <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="-20 0;30 0;-20 0" dur="10s" repeatCount="indefinite"/>`:''}${М.парусник(168,200,0.8,{имя:'Чайка'})}</g>`
      : `${М.небо(336,120,{закат:true,солнце:[70,108,16]})}${М.море(120,Н-120,336,{дорожка:70})}
         ${М.утёс(290,122,0.9)}${М.маяк(290,68,0.36,true)}
         <g opacity=".75">${М.парусник(150,200,0.8,{имя:'Чайка'})}</g>
         <path d="M210 190 q30 -10 60 -34" stroke="#fff" stroke-width="2" stroke-dasharray="4 5" fill="none" opacity=".8">${анЛин('stroke-dashoffset','0;-18','1.2s')}</path>`;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('В маяке висят старинные <b>часы времени</b>. Повернёшь стрелку — и увидишь, что было вчера, что есть сегодня, что будет завтра. Глагол при этом меняется! Посмотри все три времени.') +
      `<div class="pic">${свг(`
        ${сцена}
        <g filter="url(#c862-тень)"><rect x="62" y="8" width="212" height="48" rx="10" fill="rgba(14,26,20,.88)" stroke="${GOLD}" stroke-width="1.4"/></g>
        ${т(168,28,в3.ключ+' · '+в3.вопрос,12,МУТ,true)}
        <g>${ДВИЖ?`<animate attributeName="opacity" from="0" to="1" dur="0.5s" fill="freeze"/>`:''}${т(168,49,'«Чайка» '+в3.слово,17,GOLD,true)}</g>
        ${подпись(168,Н-12,в3.время+' время',ИНК,13)}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="ряд" style="grid-template-columns:repeat(3,1fr)">${ВРЕМЕНА.map((x,i)=>BTN(3+i,к===x.ключ?'вкл':'',x.ключ,"r862Время('"+x.ключ+"')")).join('')}</div>` +
      (!все ? СКАЗ('Попробуй','Нажми «вчера», «сегодня» и «завтра» и смотри, как меняется слово.') :
        ОТВЕТЫ('три',['прошедшее','настоящее','будущее'],2,в,3) +
        (в==null ? СКАЗ('Вопрос','Какое время у глагола «поплывёт»?') :
          РАЗБОР(ок,['Прошедшее — то, что уже было: <b>плыл</b>. А «поплывёт» — что сделает? Это ещё не случилось: <b>будущее</b>.',
            'Настоящее — то, что происходит сейчас: <b>плывёт</b>. «Поплывёт» — завтра, это <b>будущее</b>.',
            '«Поплывёт» — <b>что сделает?</b> Это будет потом: будущее время.'][в]))) +
      (ок ? ПРАВИЛО('Глаголы изменяются по <b>временам</b>: прошедшее — <b>плыл</b>, настоящее — <b>плывёт</b>, будущее — <b>поплывёт</b> (будет плыть).') : '');
  }

  /* 4. Число */
  function F4(s){
    const Н=260, в=s.ответ4, ок=в===1, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('С маяка видно море. Одна чайка <b>летит</b>, а вот стая — чайки <b>летят</b>. Глагол меняется вместе с числом! В сети у рыбаков бьётся много рыб. Как сказать: рыбы в сети …?') +
      `<div class="pic">${свг(`
        ${М.небо(336,130,{солнце:[296,34,11],облака:[[80,40,0.6,8]]})}
        ${М.море(130,Н-130,336,{дорожка:296})}
        ${М.чайка(60,70,1,{дрейф:'10 -4'})}
        ${[[190,50],[222,72],[250,44],[276,70],[212,98]].map(([x,y],k)=>М.чайка(x,y,0.7,{темп:0.8+k*0.1,дрейф:'-8 -3'})).join('')}
        ${т(60,108,'чайка летит',13,'#1a3a5a',true,undefined,'#e8f4fa')}${т(236,122,'чайки летят',13,'#1a3a5a',true,undefined,'#e8f4fa')}
        ${М.лодка(110,Н-50,0.9,{})}
        <path d="M150 ${Н-58} q40 30 80 0" stroke="#e8dcc0" stroke-width="1" fill="rgba(232,220,192,.25)"/>
        ${[0,1,2,3].map(k=>`<g>${М.качать('0 0;0 -5;0 0',(0.6+k*0.13).toFixed(2)+'s')}<ellipse cx="${164+k*16}" cy="${Н-46}" rx="8" ry="3.4" fill="url(#рм-рыба)" stroke="${ОБВОД}" stroke-width=".6"/></g>`).join('')}
        ${т(200,Н-12,'рыбы в сети …',14,ИНК,true,undefined,'#0f3f6c')}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['плещется','плещутся'],1,в,4) +
      (в==null ? СКАЗ('Вопрос','Рыбы в сети … — какое слово?') :
        РАЗБОР(ок,['«Плещется» — так говорят об одной рыбе. Рыб много: рыбы <b>плещутся</b>.',
          'Рыб много — глагол тоже во <b>множественном числе</b>: рыбы плещутся, чайки летят.'][в])) +
      (ок ? ПРАВИЛО('Глаголы изменяются по <b>числам</b>: чайка летит — чайки летят, рыба плещется — рыбы плещутся.') : '');
  }

  /* 5. Род в прошедшем */
  function F5(s){
    const Н=270, М=Р(), реш=s.реш5||{}, все=РОДЫ.every((_,i)=>реш[i]!=null), верно=все&&РОДЫ.every((р,i)=>реш[i]===р.верно);
    const ярлык=(x,y,i)=>{ const р=РОДЫ[i], в=реш[i], сл = в!=null ? ОКОНЧ[в] : 'плыл_';
      return подпись(x,y,р.кто+' '+сл, в==null?ИНК:(в===р.верно?GREEN:RED),13); };
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Смотритель записывает в журнал, кто вчера проплыл мимо мыса. Слово одно — <b>плыл</b>, но конец у него меняется. Подставь: <b>он</b> плыл, <b>она</b> плыла, <b>оно</b> плыло. Подбери окончание к каждому.') +
      `<div class="pic">${свг(`
        ${М.небо(336,120,{солнце:[300,30,10],облака:[[70,40,0.9,16]]})}
        ${М.море(120,Н-120,336,{})}
        ${М.парусник(90,206,0.6,{имя:''})}${М.лодка(250,228,0.75,{})}
        ${ярлык(82,78,2)}${ярлык(90,Н-12,0)}${ярлык(250,Н-12,1)}
        ${рамка(Н)}
      `,Н)}</div>` +
      РОДЫ.map((р,i)=>`<div class="случай">${A(3+i,'что',р.кто+' ('+р.род+')')}<div class="ask три">${ОКОНЧ.map((v,j)=>
        BTN(4+i,реш[i]===j?(j===р.верно?'hit':'miss'):'',v,'r862Род('+i+','+j+')')).join('')}</div></div>`).join('') +
      (!все ? СКАЗ('Подсказка','Скажи: «он», «она» или «оно»? Корабль — он, лодка — она, облако — оно.') :
        РАЗБОР(верно, верно ? 'Корабль <b>плыл</b>, лодка <b>плыла</b>, облако <b>плыло</b>. Окончание подсказывает род.' :
          'Проверь: он — <b>плыл</b>, она — <b>плыла</b>, оно — <b>плыло</b>.')) +
      (верно ? ПРАВИЛО('В <b>прошедшем времени</b> глагол изменяется по <b>родам</b>: он плыл, она плыла, оно плыло.') : '');
  }

  /* 6. Не с глаголами */
  function F6(s){
    const Н=270, в=s.ответ6, ок=в===0, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Ночью юнгу поставили на вахту. Утром смотритель спросил: «Спал?» — «Ни минуты!» Юнга записывает в журнал: «Юнга (не)спал всю ночь». Как написать?') +
      `<div class="pic">${свг(`
        ${М.небо(336,130,{ночь:true,луна:[290,34]})}
        ${М.море(130,Н-130,336,{ночь:true})}
        ${М.маяк(290,128,0.46,true)}
        <g><rect x="0" y="${Н-56}" width="210" height="56" fill="url(#c862-палуба)"/></g>
        ${Array.from({length:5},(_,k)=>`<line x1="${k*44}" y1="${Н-56}" x2="${k*44}" y2="${Н}" stroke="${ОБВОД}" stroke-width=".7" opacity=".4"/>`).join('')}
        <path d="M0 ${Н-56} H210" stroke="#e8b878" stroke-width="3"/>
        <circle cx="130" cy="${Н-100}" r="40" fill="url(#c862-фонарь)">${анЛин('r','36;44;36','2s')}</circle>
        ${М.юнга(100,Н-26,0.72,{поза:'стоит'})}
        <g transform="translate(130 ${Н-84})"><line x1="0" y1="-14" x2="0" y2="-6" stroke="${ОБВОД}"/><rect x="-6" y="-6" width="12" height="16" rx="2" fill="#ffe7a0" stroke="${ОБВОД}" stroke-width=".8"/></g>
        ${ок?подпись(110,40,'не спал — раздельно',GREEN,13):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['не спал','неспал'],0,в,6) +
      (в==null ? СКАЗ('Вопрос','Как написать: «юнга (не)спал»?') :
        РАЗБОР(ок,['<b>Не</b> с глаголами пишется <b>раздельно</b>: не спал, не видел, не устал.',
          'Слитно не пишут: частица <b>не</b> с глаголами — отдельное слово. Правильно: <b>не спал</b>.'][в])) +
      (ок ? ПРАВИЛО('<b>Не с глаголами пишется раздельно</b>: не спал, не знает, не поплывёт.') : '');
  }

  /* 7. Неопределённая форма */
  function F7(s){
    const Н=270, в=s.ответ7, ок=в===0, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Капитан вывешивает приказ на день: «<b>Грести</b> к маяку. <b>Поднять</b> якорь. <b>Плыть</b> вдоль берега». В приказе глаголы стоят в особой, <b>начальной</b> форме. На какой вопрос отвечает слово «грести»?') +
      `<div class="pic">${свг(`
        ${М.небо(336,110,{солнце:[40,30,10],облака:[[250,40,0.6,-8]]})}
        ${М.море(110,Н-110,336,{дорожка:40})}
        ${М.лист(186,24,140,112,{линии:3})}
        ${т(256,50,'Приказ',14,'#7a2a10',true)}${т(256,78,'грести',15,ЧЕРНИЛА,true)}${т(256,100,'поднять',15,ЧЕРНИЛА,true)}${т(256,122,'плыть',15,ЧЕРНИЛА,true)}
        <g filter="url(#c862-тень)"><rect x="96" y="146" width="110" height="10" fill="url(#c862-палуба)" stroke="${ОБВОД}" stroke-width="1"/></g>
        ${[104,150,196].map(x=>`<rect x="${x-3}" y="156" width="6" height="30" fill="#5a3a18" stroke="${ОБВОД}" stroke-width=".6"/>`).join('')}
        ${[0,1,2,3,4].map(k=>`<line x1="${96+k*22}" y1="146" x2="${96+k*22}" y2="156" stroke="${ОБВОД}" stroke-width=".6" opacity=".5"/>`).join('')}
        ${М.капитан(150,148,0.62,{})}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 0;24 0;0 0" dur="5s" repeatCount="indefinite"/>`:''}
          ${М.лодка(110,Н-44,0.85,{внутри:М.юнга(0,16,0.5,{безНог:true})})}
          <g>${ДВИЖ?`<animateTransform attributeName="transform" type="rotate" values="-20 110 ${Н-48};12 110 ${Н-48};-20 110 ${Н-48}" dur="1.4s" repeatCount="indefinite"/>`:''}${М.весло(110,Н-48,0.7,24)}</g></g>
        ${ок?подпись(168,Н-10,'грести — что делать?',GREEN,13):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['что делать?','что делает?','что сделал?'],0,в,7) +
      (в==null ? СКАЗ('Вопрос','На какой вопрос отвечает слово «грести»?') :
        РАЗБОР(ок,['Грести — <b>что делать?</b> Так глагол записан в словаре: без времени и без лица. Это <b>неопределённая форма</b>.',
          '«Что делает?» — у слова «гребёт». А «грести» отвечает на вопрос <b>что делать?</b>',
          '«Что сделал?» — у слова «грёб». «Грести» — <b>что делать?</b>'][в])) +
      (ок ? ПРАВИЛО('<b>Неопределённая форма</b> глагола отвечает на вопросы <b>что делать?</b> или <b>что сделать?</b>: грести, поднять, плыть. Так глаголы записаны в словаре.') : '');
  }

  /* 8. Глагол или нет */
  function F8(s){
    const Н=260, в=s.ответ8, ок=в===1, М=Р();
    const брызги = [0,1,2,3,4].map(k=>`<circle cx="${150+k*9}" cy="${190-(k%2)*6}" r="2.2" fill="#e8f6ff">${анЛин('cy',`${192-(k%2)*6};${174-(k%3)*6};${192-(k%2)*6}`,(0.8+k*0.15).toFixed(2)+'s')}</circle>`).join('');
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга прыгнул за борт. Смотритель кричит с маяка три слова: «<b>Плавание</b>! <b>Плавать</b>! <b>Пловец</b>!» Все три похожи, но действие называет только одно. Какое слово — глагол?') +
      `<div class="pic">${свг(`
        ${М.небо(336,110,{солнце:[296,32,11],облака:[[90,34,0.6,10]]})}
        ${М.утёс(40,112,0.8)}
        ${М.море(110,Н-110,336,{дорожка:296})}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 0;30 0;0 0" dur="4s" repeatCount="indefinite"/>`:''}
          <ellipse cx="170" cy="192" rx="36" ry="6" fill="#e8f6ff" opacity=".35"/>
          <g transform="translate(170 192) rotate(-70)">${М.юнга(0,40,0.55,{шапка:false,поза:'стоит'})}</g>
          ${брызги}</g>
        ${[['плавание','что?'],['плавать',ок?'что делать?':'?'],['пловец','кто?']].map(([w,q],k)=>`<g filter="url(#c862-тень)"><rect x="${22+k*102}" y="${Н-40}" width="92" height="32" rx="6" fill="${ок&&k===1?'#e6f5ea':'url(#рм-бумага)'}" stroke="${ок&&k===1?GREEN:ОБВОД}" stroke-width="${ок&&k===1?2:1}"/></g>
          ${т(68+k*102,Н-26,w,13,ЧЕРНИЛА,true)}${т(68+k*102,Н-13,q,11,k===1&&ок?'#1a6a3a':'#7a5a3a',true)}`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['плавание','плавать','пловец'],1,в,8) +
      (в==null ? СКАЗ('Вопрос','Какое слово — глагол?') :
        РАЗБОР(ок,['«Плавание» — <b>что?</b> Это существительное, название действия как предмета. Глагол — <b>плавать</b> (что делать?).',
          '<b>Плавать</b> — что делать? Это глагол. А «плавание» — что?, «пловец» — кто? — существительные.',
          '«Пловец» — <b>кто?</b> Это человек, существительное. Глагол — <b>плавать</b>.'][в])) +
      (ок ? ПРАВИЛО('Похожие слова бывают разными частями речи. Решает <b>вопрос</b>: что? кто? — существительное, что делать? — глагол.') : '');
  }

  /* 9. Глаголы в строке журнала */
  function F9(s){
    const Н=270, в=s.ответ9, ок=в===0, М=Р();
    const строка1=['Ветер','надул','паруса,','и'], строка2=['лодка','помчалась','к','маяку.'], гл=['надул','помчалась'];
    const ряд=(слова,y)=>{ let x=24; return слова.map(w=>{ const ш=w.length*11.4+10, g=`<g>${ок&&гл.includes(w)?`<rect x="${x-4}" y="${y-22}" width="${ш}" height="32" rx="6" fill="rgba(143,209,168,.35)" stroke="${GREEN}"/>`:''}${т(x,y,w,20,ЧЕРНИЛА,false,'start')}</g>`; x+=ш+2; return g; }).join(''); };
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Вечером юнга записывает в журнал: «<b>Ветер надул паруса, и лодка помчалась к маяку.</b>» Сколько в этой строке глаголов?') +
      `<div class="pic">${свг(`
        ${М.небо(336,112,{закат:true,солнце:[70,96,14]})}
        ${М.море(112,40,336,{дорожка:70})}
        ${М.маяк(290,112,0.4,true)}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 0;40 -2;0 0" dur="8s" repeatCount="indefinite"/>`:''}${М.парусник(150,116,0.42,{имя:''})}</g>
        ${М.лист(12,134,312,Н-146,{})}
        ${ряд(строка1,180)}${ряд(строка2,222)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['2','3','1'],0,в,9) +
      (в==null ? СКАЗ('Вопрос','Сколько глаголов в строке?') :
        РАЗБОР(ок,['Ветер что сделал? — <b>надул</b>. Лодка что сделала? — <b>помчалась</b>. Глаголов два.',
          'Три — если посчитать «ветер» или «паруса». Но это <b>что?</b> — существительные. Глаголов два: <b>надул</b>, <b>помчалась</b>.',
          'Есть ещё один: лодка что сделала? — <b>помчалась</b>. Всего два.'][в])) +
      (ок ? ПРАВИЛО('Глагол в предложении ищут вопросом от предмета: ветер <b>что сделал?</b> — надул; лодка <b>что сделала?</b> — помчалась.') : '');
  }

  /* 10. Маяк зажёгся */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=300, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА(всё
        ? 'Смотритель улыбается: «Ты понял главное. Предмет называют, а <b>действие</b> — делают». Он говорит одно слово — «зажгись!» — и на вершине маяка вспыхивает огонь. «Чайка» уходит дальше вдоль берега.'
        : 'Мыс ещё не пройден — вернись к делам в списке. Вот что получится в конце.') +
      `<div class="pic">${свг(`
        ${М.небо(336,130,{ночь:true,луна:[60,34]})}
        ${М.море(130,Н-130,336,{ночь:true})}
        ${М.утёс(270,132,1)}${М.маяк(270,72,0.52,true)}
        ${М.парусник(110,188,0.7,{имя:'Чайка'})}
        ${[['что делает? — глагол',GOLD],['плыл · плывёт · поплывёт',BLUE],['не спал — раздельно',GREEN]].map(([t0,ц],i)=>`<g>${проявить('9s',0.1+i*0.18,0.16+i*0.18)}${подпись(168,Н-70+i*28,t0,ц,13)}</g>`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','<b>Глагол</b> обозначает действие и отвечает на вопросы <b>что делать? что делает? что сделал?</b> Глаголы изменяются по временам (плыл, плывёт, поплывёт) и по числам (летит — летят), а в прошедшем времени — по родам (плыл, плыла, плыло). Начальная форма отвечает на вопрос что делать? Не с глаголами пишется раздельно.') +
      ПРАВИЛО('<b>Что делает? — значит, глагол.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Какое слово — глагол?', варианты:[{т:'светит',ок:true},{т:'свет',ок:false}], разбор:'Что делает? — светит.' },
    { вопрос:'Какое время у глагола «шумел»?', варианты:[{т:'настоящее',ок:false},{т:'прошедшее',ок:true}], разбор:'Что делал? — уже было.' },
    { вопрос:'Лодка … у берега (стоять, прошедшее время).', варианты:[{т:'стояла',ок:true},{т:'стоял',ок:false}], разбор:'Она — стояла.' },
    { вопрос:'Как написать: «(не)боится»?', варианты:[{т:'неболится',ок:false},{т:'не боится',ок:true}], разбор:'Не с глаголами — раздельно.' },
    { вопрос:'Какая форма отвечает на вопрос «что делать?»', варианты:[{т:'рисовать',ок:true},{т:'рисует',ок:false}], разбор:'Рисовать — начальная форма.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r862Reset()")}</div>` +
        ПРАВИЛО('<b>Что делает? — значит, глагол.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r862Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Найди глагол', задания:[
    {q:'Какое слово — глагол?', в:1, варианты:['шум','шумит'], раз:'Что делает? — шумит.'},
    {q:'Какое слово — глагол?', в:0, варианты:['прыгать','прыжок'], раз:'Что делать? — прыгать.'},
    {q:'Какое слово — глагол?', в:1, варианты:['бег','бежать'], раз:'Бег — что?, бежать — что делать?'},
    {q:'Какое слово — глагол?', в:0, варианты:['светит','светлый'], раз:'Светлый — какой?'}
  ]};
  const Т2 = { имя:'Время и число', задания:[
    {q:'Какое время: «нашёл»?', в:0, варианты:['прошедшее','будущее'], раз:'Что сделал? — уже было.'},
    {q:'Какое время: «споёт»?', в:1, варианты:['настоящее','будущее'], раз:'Что сделает? — будет.'},
    {q:'Волны … (шуметь, много).', в:1, варианты:['шумит','шумят'], раз:'Много волн — шумят.'},
    {q:'Какое время: «читает»?', в:0, варианты:['настоящее','прошедшее'], раз:'Что делает? — сейчас.'}
  ]};
  const Т3 = { имя:'Род и не', задания:[
    {q:'Туча … (плыть, прошедшее).', в:1, варианты:['плыл','плыла'], раз:'Она — плыла.'},
    {q:'Солнце … (светить, прошедшее).', в:0, варианты:['светило','светил'], раз:'Оно — светило.'},
    {q:'Как написать: «(не)знаю»?', в:1, варианты:['незнаю','не знаю'], раз:'Не с глаголами — раздельно.'},
    {q:'Ветер … (дуть, прошедшее).', в:0, варианты:['дул','дула'], раз:'Он — дул.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r862T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r862TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L862 = {
    id: ID,
    title: 'Глагол: что делает слово',
    ico: '⛵',
    src: 'Русский язык · 3 класс · Морфология', subj: 'rus',
    explain: [
      'На мысу всё делается по слову: поднимает, бросает, летит, машет — слова-действия. Это глаголы.',
      'Глагол отвечает на вопрос что делает?: рыбка плывёт.',
      'Глаголы изменяются по временам: плыл — плывёт — поплывёт.',
      'Глаголы изменяются по числам: чайка летит — чайки летят; рыбы плещутся.',
      'В прошедшем времени — по родам: корабль плыл, лодка плыла, облако плыло.',
      'Не с глаголами пишется раздельно: не спал.',
      'Начальная форма отвечает на вопрос что делать?: грести, поднять, плыть.',
      'Плавание — что?, пловец — кто?, плавать — что делать? — глагол.',
      '«Ветер надул паруса, и лодка помчалась к маяку»: глаголов два.',
      'Итог: что делает? — значит, глагол.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: найди глагол.',
      'Тренажёр 2: время и число.',
      'Тренажёр 3: род и не.'
    ],
    check: {
      q: 'Какое слово — глагол?',
      choices: ['парус','поднимает','синий'],
      ans: 1,
      exp: 'Поднимает — что делает? Это глагол.'
    },
    tasks: [
      { q:'На какой вопрос отвечает глагол?', kind:'choice', choices:['кто? что?','что делает?','какой?'], ans:1, tol:0,
        hints:['Глагол называет действие.'], sol:'Что делает?' },
      { q:'Какое время у глагола «поплывёт»?', kind:'choice', choices:['прошедшее','настоящее','будущее'], ans:2, tol:0,
        hints:['Что сделает?'], sol:'Будущее.' },
      { q:'Сколько глаголов в предложении «Ветер надул паруса, и лодка помчалась к маяку»?', kind:'unit', ans:2, tol:0,
        hints:['Ветер что сделал? Лодка что сделала?'], sol:'Два: надул, помчалась.' }
    ]
  };

  function render(el){
    if(!window.РМ){ el.innerHTML=''; return; }
    css();
    const s = S();
    const step = Math.max(0, Math.min(L862.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    s.свеж1=null;
    const ЗАГОЛОВКИ={1:'Команды на палубе',2:'Что делает?',3:'Часы времени',4:'Одна и много',5:'Он, она, оно',
      6:'Ночная вахта',7:'Приказ капитана',8:'Три похожих слова',9:'Вечерняя запись',10:'Маяк зажёгся',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l862" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Глагол'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r862Слово=(i)=>{ const s=S(); const с=Array.isArray(s.сдел1)?s.сдел1.slice():[];
    if(с.includes(i)) return;
    if(КОМАНДЫ[i].гл){ с.push(i); s.сдел1=с; s.ош1=null; s.свеж1=i; if(ГЛАГОЛЫ1.every(k=>с.includes(k))) s.дело_команды=true; }
    else s.ош1=i;
    chRender(0); };
  window.r862Время=(к)=>{ const s=S(); s.время3=к; const в=Array.isArray(s.видел3)?s.видел3.slice():[]; if(!в.includes(к)) в.push(к); s.видел3=в; chRender(0); };
  window.r862Род=(i,j)=>{ const s=S(); const р=Object.assign({},s.реш5||{}); р[i]=j; s.реш5=р;
    if(РОДЫ.every((x,k)=>р[k]===x.верно)) s.дело_род=true; chRender(0); };
  window.r862Отв=(f,к)=>{ const s=S(); s['ответ'+f]=к; if(f===3&&к===2) s.дело_время=true; chRender(0); };
  window.r862Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r862Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r862T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r862TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L862; else arr.push(L862); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.RU862={render:render, L:L862};
})();
