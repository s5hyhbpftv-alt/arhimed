/* ============ РУССКИЙ ЯЗЫК · УРОК 1051 · «СИНОНИМЫ, АНТОНИМЫ, ОМОНИМЫ» ============
   5 класс, раздел «Лексика». Сюжет: слова бывают друзьями, противниками и
   близнецами — учимся их различать.

   СВЕРКА С ПРОГРАММОЙ (5 класс, «Лексика»): синонимы — слова, близкие по
   значению; антонимы — слова с противоположным значением; омонимы — слова,
   которые звучат и пишутся одинаково, а значения имеют разные. Продолжение
   линии уроков 877–883 (речь, текст, фонетика, орфоэпия, шипящие) и 1000
   (паронимы).

   ПРОВЕРЕННЫЕ ПРИМЕРЫ (школьный курс):
     синонимы: смелый — храбрый — отважный; большой — огромный; идти — шагать;
     антонимы: день — ночь, холодный — горячий, друг — враг, громко — тихо;
     омонимы: ключ (родник / от двери / скрипичный), коса (девичья / инструмент /
       песчаная), лук (растение / оружие), норка (животное / маленькая нора),
       брак (супружество / испорченная работа);
     НЕ омонимы, а многозначные слова: ножка (стула, гриба, ребёнка) — есть общий
       признак «маленькая опора»; крыло (птицы, самолёта) — общий признак;
     ловушка: «смелый — трусливый» — это АНТОНИМЫ, а не синонимы;
     не у каждого слова есть антоним: «стол», «тетрадь» — антонима нет.

   ЭТАЛОН (уроки 877–883, 1000): движение в кадре — это сам смысл.
     кадр 1  три пары слов расходятся по трём дорожкам;
     кадр 2  синонимы съезжаются к одной карточке смысла;
     кадр 3  антонимы расходятся по концам одной шкалы;
     кадр 4  омонимы вырастают из одного слова в разные предметы;
     кадр 5  сравнение: «лук» — два разных предмета, «ножка» — один признак;
     кадры 6–8 «я» → «мы» (пропуск) → «ты»;
     кадр 9  самообъяснение: выбирается ПРИЧИНА;
     кадр 10 ловушка: антоним принимают за синоним;
     кадр 11 практика; кадры 12–14 три тренажёра.

   ПО 12 ПРАВИЛАМ АУДИТА ЭКСПЕРТОВ: ответ не виден до выбора (и рисунок тоже),
   верный вариант не первый (детерминированный сдвиг), три варианта с реальными
   ошибками, затухание «я — мы — ты», интерливинг практики (уровень 3 — паронимы
   из урока 1000), разминка из прошлого урока, самообъяснение причины, разбор
   чужой записи в третьем тренажёре, выходной билет.

   РИСУНОК по deploy/РИСОВАНИЕ_ФИГУР.md: тёплые заливки, тёмная обводка,
   свет сверху-слева, тень внизу-справа, ничего за краем кадра.
   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: SMIL с кривой 0.23 1 0.32 1,
   reduced-motion выключает движение полностью. */
(function(){
  'use strict';

  const ID = 1051;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';
  const ОБВОД='#33291e', БУМАГА='#f7f3e6';

  const CSS=`
  #lvis .s6.l1051{gap:14px}
  #lvis .s6.l1051 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l1051 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l1051 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l1051 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l1051 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l1051 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l1051 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l1051 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l1051 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l1051 .правило b{color:${GOLD}}
  #lvis .s6.l1051 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l1051 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;
    font-size:16px;font-weight:600;line-height:1.35;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1051 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l1051 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l1051 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l1051 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l1051 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l1051 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l1051 .уровни .точка.сейчас{background:${GOLD};animation:l1051dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l1051dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l1051 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l1051{-webkit-text-size-adjust:100%}
  #lvis .s6.l1051 [data-anim]{animation:l1051rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l1051rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l1051 [data-anim]{animation:none!important}
    #lvis .s6.l1051 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l1051 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l1051-style');
      if(!s){ s=document.createElement('style'); s.id='l1051-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(String(ID)) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ПРАВИЛО2 = (ответ, до, после) => (ответ!=null) ? ПРАВИЛО(после) : ПРАВИЛО(до);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c1051-фон" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#26382e"/><stop offset="0.45" stop-color="#1a281f"/>
        <stop offset="1" stop-color="#101a14"/>
      </linearGradient>
      <linearGradient id="c1051-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <linearGradient id="c1051-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".6"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".22"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".2"/>
      </linearGradient>
      <linearGradient id="c1051-дерево" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#c69a67"/><stop offset="0.5" stop-color="#96683c"/>
        <stop offset="1" stop-color="#5c3f20"/>
      </linearGradient>
      <radialGradient id="c1051-свет" cx="0.26" cy="0.12" r="0.8" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffe9b0" stop-opacity=".18"/>
        <stop offset="0.6" stop-color="#ffe9b0" stop-opacity=".05"/>
        <stop offset="1" stop-color="#000000" stop-opacity=".32"/>
      </radialGradient>
      <filter id="c1051-тень" x="-60%" y="-60%" width="220%" height="220%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="2.6" result="р"/>
        <feOffset in="р" dx="3" dy="4" result="с"/>
        <feFlood flood-color="#0c1410" flood-opacity="0.45" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c1051-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c1051-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c1051-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="url(#c1051-свет)"/>
     <line x1="20" y1="${высота-18}" x2="316" y2="${высота-18}" stroke="${ОБВОД}" stroke-width="2" opacity=".5"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,18,13,.86)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#0c1410" opacity=".34" filter="url(#c1051-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c1051-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  /* КАРТОЧКА СЛОВА: бумага, тёмная обводка, слово крупно */
  const карточка = (x,y,ш,в,слово,цвет,нач,кегль) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#0c1410" opacity=".38" filter="url(#c1051-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c1051-бумага)" stroke="${ОБВОД}" stroke-width="1.5">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="none" filter="url(#c1051-шум)" opacity=".08"/>
    ${т(x+ш/2,y+в/2+6,слово,кегль||18,цвет||'#2b2110',true)}
  </g>`;

  /* СТРЕЛКА */
  const стрелкаГ = (x1,y,x2,цвет,нач) => `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
    <line x1="${x1}" y1="${y}" x2="${x2}" y2="${y}" stroke="${цвет||GOLD}" stroke-width="2.4"/>
    <path d="M${x2} ${y} l-9 -6 l0 12 z" fill="${цвет||GOLD}"/></g>`;

  /* ШКАЛА для антонимов: полоса с двумя концами */
  const шкала = (x,y,ш,левое,правое,нач) => `<g>${проявить('15s',нач,нач+0.05)}
    <defs><linearGradient id="c1051-шкала" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
      <stop offset="0" stop-color="#5fa8d8"/><stop offset="0.5" stop-color="#e8dcc8"/>
      <stop offset="1" stop-color="#e8735a"/></linearGradient></defs>
    <rect x="${x}" y="${y}" width="${ш}" height="14" rx="7" fill="url(#c1051-шкала)" stroke="${ОБВОД}" stroke-width="1.3"/>
    ${т(x+2,y+34,левое,16,'#bfe6ff',true,'start')}
    ${т(x+ш-2,y+34,правое,16,'#ffd0c4',true,'end')}
  </g>`;

  /* ПРЕДМЕТ: ключ (гаечный/дверной) — рисуем просто и узнаваемо */
  const ключДверной = (x,y,нач,цвет) => `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
    <circle cx="${x}" cy="${y-14}" r="8" fill="none" stroke="${цвет||'#ffd76a'}" stroke-width="3.4"/>
    <rect x="${x-2}" y="${y-8}" width="4" height="26" rx="2" fill="${цвет||'#ffd76a'}"/>
    <rect x="${x+2}" y="${y+10}" width="8" height="4" rx="1.6" fill="${цвет||'#ffd76a'}"/>
    <rect x="${x+2}" y="${y+16}" width="6" height="4" rx="1.6" fill="${цвет||'#ffd76a'}"/>
  </g>`;

  const ключРодник = (x,y,нач) => `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
    <path d="M${x-16} ${y+14} q16 -22 32 0" fill="none" stroke="#7fd1ff" stroke-width="3.4" stroke-linecap="round"/>
    <path d="M${x-10} ${y+20} q10 -14 20 0" fill="none" stroke="#a8dcff" stroke-width="2.6" stroke-linecap="round"/>
    <circle cx="${x}" cy="${y-8}" r="3" fill="#bfe6ff"/>
  </g>`;

  const ключСкрипичный = (x,y,нач) => `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
    <path d="M${x} ${y+16} q-12 -10 0 -20 q12 -10 0 -20" fill="none" stroke="${GOLD}" stroke-width="3.2" stroke-linecap="round"/>
    <circle cx="${x}" cy="${y+16}" r="3.4" fill="${GOLD}"/>
    <circle cx="${x}" cy="${y-24}" r="2.6" fill="${GOLD}"/>
  </g>`;

  const лукОвощ = (x,y,нач) => `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
    <ellipse cx="${x}" cy="${y+6}" rx="14" ry="16" fill="#e8d3a8" stroke="${ОБВОД}" stroke-width="1.4"/>
    <path d="M${x} ${y-10} q-8 -10 -4 -18 M${x} ${y-10} q8 -10 4 -18 M${x} ${y-10} q0 -12 0 -20"
      fill="none" stroke="#8fd1a8" stroke-width="2.4" stroke-linecap="round"/></g>`;

  const лукОружие = (x,y,нач) => `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
    <path d="M${x-16} ${y+12} q16 -26 32 0" fill="none" stroke="url(#c1051-дерево)" stroke-width="4"/>
    <line x1="${x-16}" y1="${y+12}" x2="${x+16}" y2="${y+12}" stroke="#e8dcc8" stroke-width="1.6"/>
    <line x1="${x}" y1="${y-8}" x2="${x}" y2="${y+14}" stroke="#e8dcc8" stroke-width="2"/></g>`;

  /* НОЖКА: стул и гриб — показываем общий признак «опора» */
  const ножкаСтула = (x,y,нач) => `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
    <rect x="${x-14}" y="${y-22}" width="28" height="6" rx="3" fill="url(#c1051-дерево)" stroke="${ОБВОД}" stroke-width="1.2"/>
    <rect x="${x-10}" y="${y-16}" width="4" height="18" fill="url(#c1051-дерево)"/>
    <rect x="${x+6}" y="${y-16}" width="4" height="18" fill="url(#c1051-дерево)"/></g>`;

  const гриб = (x,y,нач) => `<g>${нач!=null?проявить('15s',нач,нач+0.05):''}
    <path d="M${x-16} ${y-10} q16 -20 32 0 z" fill="#e8735a" stroke="${ОБВОД}" stroke-width="1.3"/>
    <rect x="${x-5}" y="${y-10}" width="10" height="18" rx="3" fill="#f7f3e6" stroke="${ОБВОД}" stroke-width="1.2"/></g>`;

  /* полоска бумаги для сводки */
  const бумагаПолоса = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="7" fill="#0c1410" opacity=".38" filter="url(#c1051-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="7" fill="url(#c1051-бумага)" stroke="${ОБВОД}" stroke-width="1.4">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="7" fill="none" filter="url(#c1051-шум)" opacity=".08"/>
  </g>`;

  /* 12. Практика: интерливинг + выходной билет */
  const УРОВНИ=[
    { тип:'новая', вопрос:'Храбрый и отважный — это…', варианты:[
        {т:'синонимы', ок:true,  почему:'Значение близкое.'},
        {т:'антонимы', ок:false, почему:'Антонимы — противоположные: храбрый и трусливый.'},
        {т:'омонимы', ок:false, почему:'Омонимы пишутся одинаково — здесь слова разные.'}]},
    { тип:'новая', вопрос:'Начало и конец — это…', варианты:[
        {т:'антонимы', ок:true,  почему:'Значения противоположны.'},
        {т:'синонимы', ок:false, почему:'Синонимы близки по значению.'},
        {т:'омонимы', ок:false, почему:'Омонимы — одинаковые по записи слова.'}]},
    { тип:'прошлая', вопрос:'Вспомним прошлый урок: как правильно?', варианты:[
        {т:'надеть шапку, одеть ребёнка', ок:true,  почему:'Надеть — на себя, одеть — кого-то.'},
        {т:'одеть шапку, надеть ребёнка', ок:false, почему:'Это перепутанные паронимы.'},
        {т:'оба варианта равны', ок:false, почему:'Паронимы различаются значением.'}]},
    { тип:'смешанная', вопрос:'Коса (причёска), коса (инструмент), коса (песчаная) — это…', варианты:[
        {т:'омонимы', ок:true,  почему:'Запись одна, значения не связаны.'},
        {т:'одно многозначное слово', ок:false, почему:'Общего признака у трёх значений нет.'},
        {т:'антонимы', ок:false, почему:'Антонимы противоположны по значению.'}]},
    { тип:'обёртка', вопрос:'Редактор правит текст. Слово «идут» повторилось трижды. Что он подберёт?', варианты:[
        {т:'синонимы: шагают, движутся, направляются', ок:true,  почему:'Синонимы убирают повтор.'},
        {т:'антонимы: стоят, замерли', ок:false, почему:'Антонимы изменят смысл.'},
        {т:'омонимы — одинаковые слова', ок:false, почему:'Это, наоборот, усилит повтор.'}]}
  ];
  function F12(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Выходной билет: придумай своё слово-омоним и два его разных значения.') +
        РАЗБОР(true,'Например, «брак»: супружество и испорченная работа. Или «мир»: вселенная и покой. Главное — значения не связаны.') +
        `<div class="ask">${BTN(4,'','пройти заново','r1051Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const порядок=сдвиг(ур.варианты, и+1);
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length+' · '+ур.тип) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + порядок.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r1051Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(порядок[выбран] && порядок[выбран].ок, (порядок[выбран]||{}).почему||'') : ЖДЁТ()) +
      ПРАВИЛО('Смотри на <b>значение</b>: близкое, противоположное или разное.');
  }

  /* 13–15. Тренажёры */
  const Т1={
    имя:'Тренажёр 1 · синонимы и антонимы',
    задания:[
      { ф:'Синоним к слову «идти».', о:['шагать','стоять','бежать назад'], в:0, р:'Шагать — то же значение.' },
      { ф:'Антоним к слову «холодный».', о:['горячий','прохладный','мокрый'], в:0, р:'Противоположное значение.' },
      { ф:'Антоним к слову «друг».', о:['враг','товарищ','сосед'], в:0, р:'Враг — противоположность.' },
      { ф:'Есть ли антоним у слова «стол»?', о:['нет','есть: стул','есть: шкаф'], в:0, р:'Антонима у «стола» нет.' }
    ]
  };

  /* ================= КАДРЫ ================= */

  /* 1. Разминка (паронимы, урок 1000) + сюжет */
  function F1(s){
    const в = s.разминка;
    return A(2,'cap','Разминка: прошлый урок про паронимы') +
      ВОПРОС('Какое слово подходит: «… шапку» или «… ребёнка»?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Слова-друзья, противники и близнецы',14,GOLD,true)}
        ${карточка(40,52,110,44,'надеть',GOLD,0.05,16)}
        ${карточка(186,52,110,44,'одеть',BLUE,0.12,16)}
        ${в!=null?`<g>${проявить('15s',0.3,0.42)}
          ${подпись(95,120,'надеть — на себя',GOLD,12)}
          ${подпись(241,120,'одеть — кого-то',BLUE,12)}</g>`:''}
        <g>${проявить('15s',0.5,0.6)}
          ${стекло(24,140,288,42,0.5)}
          ${т(168,166,'паронимы звучат похоже, но значат разное',12,ИНК,true)}</g>
        ${в!=null?`<g>${проявить('15s',0.7,0.8)}
          ${подпись(168,206,'сегодня — синонимы, антонимы и омонимы',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='надеть'?'hit':(в?'miss':''), 'Шапку надевают, ребёнка одевают', "r1051Разминка('надеть')")}
        ${BTN(4, в==='одеть'?'miss':'', 'Наоборот: шапку одевают, ребёнка надевают', "r1051Разминка('одеть')")}
        ${BTN(5, в==='оба'?'miss':'', 'Оба слова подходят одинаково', "r1051Разминка('оба')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='надеть',
        в==='надеть' ? 'Верно: надеть можно что-то на себя, а одеть — кого-то другого. Запомним: «надеть одежду, одеть Надежду».'
                     : 'Нет: надеть — на себя (надеть шапку), одеть — кого-то (одеть ребёнка). Это паронимы.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Вспомним паронимы: слова похожи, а значения разные.',
                 'Надеть — на себя, одеть — кого-то. Дальше — три новых понятия.');
  }

  /* 2. Синонимы */
  function F2(s){
    const в = s.синонимы;
    return ВОПРОС('Смелый, храбрый, отважный — как называются такие слова?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Синонимы: слова-друзья',14,GOLD,true)}
        ${карточка(16,52,92,44,'смелый',GOLD,0.05,16)}
        ${карточка(122,52,92,44,'храбрый',GOLD,0.12,16)}
        ${карточка(228,52,92,44,'отважный',GOLD,0.19,16)}
        ${в!=null?`<g>${проявить('15s',0.3,0.42)}
          ${стрелкаГ(108,74,122,GREEN,0.3)}${стрелкаГ(214,74,228,GREEN,0.36)}</g>`:''}
        <g>${проявить('15s',0.46,0.56)}
          ${стекло(24,116,288,40,0.46)}
          ${т(168,140,'значение одно: «не боится»',14,ИНК,true)}</g>
        ${в!=null?`<g>${проявить('15s',0.62,0.72)}
          ${стекло(24,168,288,40,0.62)}
          ${т(168,192,'но оттенки разные: отважный — особенно',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.78,0.88)}
          ${подпись(168,230,'синонимы близки по значению',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='синонимы'?'hit':(в?'miss':''), 'Синонимы: близкие по значению слова', "r1051Син('синонимы')")}
        ${BTN(4, в==='антонимы'?'miss':'', 'Антонимы: слова с обратным значением', "r1051Син('антонимы')")}
        ${BTN(5, в==='омонимы'?'miss':'', 'Омонимы: одинаковые слова с разными значениями', "r1051Син('омонимы')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='синонимы',
        в==='синонимы' ? 'Верно: синонимы называют одно и то же по-разному. Значение общее, а оттенки отличаются.'
                      : 'Нет: антонимы — это противоположные слова (смелый — трусливый), а здесь значение общее. Значит, синонимы.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Подумай, одинаковое значение или противоположное.',
                 'Синонимы — слова, <b>близкие по значению</b>.');
  }

  /* 3. Антонимы */
  function F3(s){
    const в = s.антонимы;
    return ВОПРОС('День и ночь, холодный и горячий — как называются такие пары?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Антонимы: слова-противники',14,GOLD,true)}
        ${шкала(30,60,276,'день','ночь',0.06)}
        ${в!=null?`<g>${проявить('15s',0.28,0.4)}
          ${шкала(30,116,276,'холодный','горячий',0.28)}
          ${шкала(30,172,276,'друг','враг',0.36)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.3,0.4)}
          ${стекло(24,140,288,40,0.3)}
          ${т(168,164,'слова стоят на разных концах смысла',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.56,0.66)}
          ${стекло(24,184,288,40,0.56)}
          ${т(168,208,'не у каждого слова есть антоним: «стол» — нет',12,ИНК,true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.72,0.82)}
          ${подпись(168,238,'антонимы противоположны по значению',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='антонимы'?'hit':(в?'miss':''), 'Антонимы: противоположные по значению', "r1051Ант('антонимы')")}
        ${BTN(4, в==='синонимы'?'miss':'', 'Синонимы: близкие по значению', "r1051Ант('синонимы')")}
        ${BTN(5, в==='омонимы'?'miss':'', 'Омонимы: одинаково пишутся', "r1051Ант('омонимы')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='антонимы',
        в==='антонимы' ? 'Верно: антонимы — слова с противоположным значением. Они стоят на разных концах одной шкалы.'
                       : 'Нет: синонимы близки по значению (смелый — храбрый), а день и ночь — противоположны. Это антонимы.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Сравни значения: они совпадают или спорят?',
                 'Антонимы — слова с <b>противоположным значением</b>.');
  }

  /* 4. Омонимы */
  function F4(s){
    const в = s.омонимы;
    return ВОПРОС('Ключ от двери, ключ-родник и скрипичный ключ — что это?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Омонимы: слова-близнецы',14,GOLD,true)}
        ${карточка(140,44,56,32,'ключ',GOLD,0.05,16)}
        ${ключДверной(70,116,0.16)}
        ${ключРодник(168,116,0.24)}
        ${ключСкрипичный(266,120,0.32)}
        ${в!=null?`<g>${проявить('15s',0.42,0.52)}
          ${т(70,166,'от двери',12,ИНК,true)}
          ${т(168,166,'родник',12,ИНК,true)}
          ${т(266,166,'скрипичный',12,ИНК,true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.6,0.7)}
          ${стекло(24,186,288,40,0.6)}
          ${т(168,210,'звучат и пишутся одинаково — а значения разные',12,ИНК,true)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.4,0.5)}
          ${стекло(24,186,288,40,0.4)}
          ${т(168,212,'одно слово — сколько значений?',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='омонимы'?'hit':(в?'miss':''), 'Омонимы: пишутся одинаково, значения разные', "r1051Омо('омонимы')")}
        ${BTN(4, в==='синонимы'?'miss':'', 'Синонимы: слова близки по значению', "r1051Омо('синонимы')")}
        ${BTN(5, в==='многозначное'?'miss':'', 'Это одно многозначное слово с оттенками', "r1051Омо('многозначное')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='омонимы',
        в==='омонимы' ? 'Верно: ключ от двери, ключ-родник и скрипичный ключ — разные слова, которые случайно пишутся одинаково. Это омонимы.'
                      : 'Нет: значения совсем не связаны между собой, поэтому это не оттенки одного слова, а омонимы.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Разберись, связаны ли значения между собой.',
                 'Омонимы — <b>одинаковая запись, разные значения</b>.');
  }

  /* 5. Омонимы и многозначные слова */
  function F5(s){
    const в = s.различие;
    return ВОПРОС('«Лук» (растение и оружие) и «ножка» (стула и гриба) — в чём разница?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Омонимы или многозначное слово?',14,GOLD,true)}
        ${лукОвощ(70,104,0.05)}
        ${лукОружие(150,104,0.12)}
        ${ножкаСтула(232,104,0.2)}
        ${гриб(292,104,0.28)}
        ${в!=null?`<g>${проявить('15s',0.4,0.5)}
          ${подпись(110,170,'лук: связи нет',GOLD,12)}
          ${подпись(262,170,'ножка: общий признак',BLUE,12)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.56,0.66)}
          ${стекло(24,186,288,44,0.56)}
          ${т(168,206,'лук — омонимы:',12,ИНК,true)}
          ${т(168,222,'ножка — одно многозначное слово',12,МУТ)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.36,0.46)}
          ${стекло(24,186,288,44,0.36)}
          ${т(168,212,'есть ли у значений что-то общее?',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='связь'?'hit':(в?'miss':''), 'У «лука» связи нет, а у «ножки» есть общий признак — опора', "r1051Разл('связь')")}
        ${BTN(4, в==='всёомонимы'?'miss':'', 'И то и другое — омонимы, раз значения разные', "r1051Разл('всёомонимы')")}
        ${BTN(5, в==='всёмного'?'miss':'', 'И то и другое — многозначные слова', "r1051Разл('всёмного')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='связь',
        в==='связь' ? 'Верно: у «лука» значения не связаны — это омонимы. У «ножки» общий признак: маленькая опора. Значит, это одно многозначное слово.'
                    : 'Нет: у «ножки» стула и гриба есть общий признак — маленькая опора, поэтому это многозначное слово, а не омонимы.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Проверь, есть ли у значений общий признак.',
                 'Общий признак есть — <b>многозначное слово</b>; нет — <b>омонимы</b>.');
  }

  /* 6. «Я»: как подобрать синоним */
  function F6(s){
    const в = s.я;
    return A(2,'cap','Я показываю: подбираю синоним') +
      ВОПРОС('Подбери синоним к слову «большой».') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Как искать синоним',14,GOLD,true)}
        ${/* исходное слово видно всегда (это условие задачи, а не ответ) */''}
        <g>${проявить('15s',0.04,0.14)}
          ${карточка(120,44,96,40,'большой',GOLD,0.04,16)}
          ${стекло(24,100,288,40,0.1)}
          ${т(168,124,'ищем слово с тем же значением',12,МУТ)}</g>
        ${в!=null?`<g>${проявить('15s',0.06,0.16)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.24,0.36)}
          ${стекло(24,100,288,40,0.24)}
          ${т(168,124,'шаг 1: объясни значение словами',12,ИНК,true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.42,0.54)}
          ${стекло(24,148,288,40,0.42)}
          ${т(168,172,'шаг 2: «такой, что многого не хватает»',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.6,0.72)}
          ${карточка(40,196,80,36,'огромный',GREEN,0.6,14)}
          ${карточка(128,196,80,36,'громадный',GREEN,0.66,14)}
          ${карточка(216,196,80,36,'крупный',GREEN,0.72,14)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='огромный'?'hit':(в?'miss':''), 'Огромный: значение то же, звучит иначе', "r1051Я('огромный')")}
        ${BTN(4, в==='маленький'?'miss':'', 'Маленький: тоже про размер', "r1051Я('маленький')")}
        ${BTN(5, в==='большойдом'?'miss':'', 'Большой дом: словосочетание с тем же словом', "r1051Я('большойдом')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='огромный',
        в==='огромный' ? 'Верно: огромный и громадный называют то же самое, что и большой. Это синонимы.'
                       : 'Нет: «маленький» — противоположное слово (антоним), а «большой дом» — просто словосочетание. Синоним — огромный.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Ищи слово с тем же значением, но другим звучанием.',
                 'Синоним к «большой» — <b>огромный</b>.');
  }

  /* 7. «Мы»: антоним, шаг пропущен */
  function F7(s){
    const в = s.мы;
    return A(2,'cap','Мы делаем вместе: один шаг пропущен') +
      ВОПРОС('Антоним к слову «громко» — это «…»? Впиши шаг.') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Антоним — на другом конце шкалы',14,GOLD,true)}
        ${шкала(30,60,276,'громко','?',0.06)}
        ${в!=null?`<g>${проявить('15s',0.26,0.38)}
          ${т(306,44,'тихо',16,'#ffd0c4',true,'end')}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.44,0.56)}
          ${стекло(24,124,288,40,0.44)}
          ${т(168,148,'шаг: найди слово с обратным значением',12,ИНК,true)}</g>`:''}
        <g>${проявить('15s',0.6,0.7)}
          ${стекло(24,176,288,44,0.6)}
          ${т(168,196,'если слово не имеет пары-противника,',12,МУТ)}
          ${т(168,212,'антонима у него нет: «стол», «тетрадь»',12,ИНК,true)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тихо'?'hit':(в?'miss':''), 'Тихо: противоположное значение', "r1051Мы('тихо')")}
        ${BTN(4, в==='громкий'?'miss':'', 'Громкий: то же значение, другая форма', "r1051Мы('громкий')")}
        ${BTN(5, в==='быстро'?'miss':'', 'Быстро: тоже про звук и скорость', "r1051Мы('быстро')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тихо',
        в==='тихо' ? 'Верно: тихо — противоположно громко. Это антонимы; «громкий» — просто другая форма того же слова.'
                   : 'Нет: «громкий» — форма того же слова, а «быстро» про другое. Антоним — тихо.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Найди слово с противоположным значением.',
                 'Антоним к «громко» — <b>тихо</b>.');
  }

  /* 8. «Ты»: омонимы сам */
  function F8(s){
    const в = s.ты;
    return A(2,'cap','Теперь ты: определи сам') +
      ВОПРОС('Норка — животное и норка — маленькая нора. Что это?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Решаем самостоятельно',14,GOLD,true)}
        ${карточка(120,44,96,40,'норка',GOLD,0.05,16)}
        ${в!=null?`<g>${проявить('15s',0.24,0.36)}
          <ellipse cx="96" cy="132" rx="26" ry="16" fill="#96683c" stroke="${ОБВОД}" stroke-width="1.4"/>
          <circle cx="76" cy="124" r="9" fill="#96683c" stroke="${ОБВОД}" stroke-width="1.3"/>
          <circle cx="238" cy="132" r="20" fill="#3a2c18" stroke="${ОБВОД}" stroke-width="1.4"/>
          <ellipse cx="238" cy="134" rx="12" ry="9" fill="#101a14"/></g>`:''}
        ${в!=null?`<g>${проявить('15s',0.44,0.56)}
          ${подпись(96,176,'зверёк',GOLD,12)}
          ${подпись(238,176,'жилище',GOLD,12)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.6,0.7)}
          ${стекло(24,196,288,36,0.6)}
          ${т(168,218,'значения не связаны между собой',12,ИНК,true)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.36,0.46)}
          ${стекло(24,196,288,36,0.36)}
          ${т(168,220,'что связывает эти значения?',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='омонимы'?'hit':(в?'miss':''), 'Омонимы: одинаково пишутся, значения не связаны', "r1051Ты('омонимы')")}
        ${BTN(4, в==='многозначное'?'miss':'', 'Одно многозначное слово: зверёк живёт в норке', "r1051Ты('многозначное')")}
        ${BTN(5, в==='антонимы'?'miss':'', 'Антонимы: зверёк и жилище — противоположности', "r1051Ты('антонимы')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='омонимы',
        в==='омонимы' ? 'Верно: маленький зверёк и маленькая нора — разные слова, которые пишутся одинаково. Это омонимы.'
                      : 'Нет: сходство только в записи, а значения не связаны: зверёк не «маленькая нора». Это омонимы.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Проверь связь значений.',
                 'Норка (зверёк) и норка (нора) — <b>омонимы</b>.');
  }

  /* 9. Самообъяснение: почему ключ — омоним, а ножка — нет */
  function F9(s){
    const в = s.почему;
    return ВОПРОС('Почему «ключ» — омоним, а «ножка» — многозначное слово?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Причина в связи значений',14,GOLD,true)}
        ${карточка(30,48,120,38,'ключ',GOLD,0.05,16)}
        ${карточка(186,48,120,38,'ножка',BLUE,0.12,16)}
        ${в!=null?`<g>${проявить('15s',0.28,0.4)}
          ${т(90,112,'родник ≠ от двери',12,МУТ)}
          ${т(246,112,'опора — общее',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.46,0.58)}
          ${стекло(24,132,288,44,0.46)}
          ${т(168,152,'у «ключа» значения никак не связаны —',12,ИНК,true)}
          ${т(168,168,'у «ножки» есть общий признак',12,ИНК,true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.64,0.74)}
          ${подпись(168,206,'поэтому первое — омонимы, второе — одно слово',GREEN,12)}</g>`:''}
        ${в==null?`<g>${проявить('15s',0.34,0.44)}
          ${стекло(24,132,288,44,0.34)}
          ${т(168,160,'вспомни, что мы проверяем',12,МУТ)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='признак'?'hit':(в?'miss':''), 'У «ножки» есть общий признак, у «ключа» его нет', "r1051Поч('признак')")}
        ${BTN(4, в==='длина'?'miss':'', 'Слова разной длины, поэтому и ведут себя по-разному', "r1051Поч('длина')")}
        ${BTN(5, в==='звук'?'miss':'', 'Просто одни слова короче звучат, другие длиннее', "r1051Поч('звук')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='признак',
        в==='признак' ? 'Верно: многозначные слова связаны общим признаком (ножка — маленькая опора). У омонимов такой связи нет.'
                      : 'Нет: дело не в длине и не в звучании. Проверяем связь значений: есть общий признак или нет.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Подумай, что общего у значений.',
                 'Проверка: <b>общий признак есть — многозначное; нет — омонимы</b>.');
  }

  /* 10. Ловушка: антоним приняли за синоним */
  function F10(s){
    const в = s.ловушка;
    return ВОПРОС('Ученик сказал: «смелый и трусливый — синонимы». Что не так?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Ловушка: спорят или совпадают?',14,GOLD,true)}
        ${карточка(30,52,120,44,'смелый',GOLD,0.05,16)}
        ${карточка(186,52,120,44,'трусливый',RED,0.12,16)}
        ${в!=null?`<g>${проявить('15s',0.28,0.4)}
          ${стрелкаГ(150,74,186,RED,0.28)}
          ${подпись(168,120,'значения противоположны',RED,12)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.44,0.56)}
          ${стекло(24,140,288,42,0.44)}
          ${т(168,160,'синонимы: смелый — храбрый — отважный',12,ИНК,true)}
          ${т(168,176,'антонимы: смелый — трусливый',12,МУТ)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.62,0.72)}
          ${подпись(168,214,'сначала проверь значение, потом называй',GREEN,12)}</g>`:''}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='антонимы'?'hit':(в?'miss':''), 'Это антонимы: значения противоположны', "r1051Лов('антонимы')")}
        ${BTN(4, в==='верно'?'miss':'', 'Всё верно, это синонимы', "r1051Лов('верно')")}
        ${BTN(5, в==='омонимы'?'miss':'', 'Это омонимы: слова звучат похоже', "r1051Лов('омонимы')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='антонимы',
        в==='антонимы' ? 'Верно: смелый и трусливый — противоположные по значению слова, то есть антонимы. Синонимы к «смелому» — храбрый, отважный.'
                       : 'Нет: синонимы называют одно и то же, а здесь значения спорят. Смелый и трусливый — антонимы.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Сравни значения слов, прежде чем называть их.',
                 'Смелый и трусливый — <b>антонимы</b>, а не синонимы.');
  }

  /* 11. Сводка */
  function F11(s){
    const в = s.сводка;
    return ВОПРОС('Как различить синонимы, антонимы и омонимы?') +
      `<div class="pic">${свг(`
        ${фон(266)}
        ${т(168,24,'Три понятия рядом',14,GOLD,true)}
        ${стекло(14,38,308,44,0.05)}
        ${т(168,62,'синонимы: значение близкое',14,GREEN,true)}
        ${т(168,76,'смелый — храбрый — отважный',12,МУТ)}
        ${стекло(14,88,308,44,0.2)}
        ${т(168,112,'антонимы: значение противоположное',14,RED,true)}
        ${т(168,126,'день — ночь, друг — враг',12,МУТ)}
        ${стекло(14,138,308,44,0.35)}
        ${т(168,162,'омонимы: запись одна, значения разные',14,GOLD,true)}
        ${т(168,176,'ключ, коса, норка, лук, брак',12,МУТ)}
        ${в!=null?`<g>${проявить('15s',0.5,0.62)}
          ${бумагаПолоса(14,192,308,40,0.5)}
          ${т(168,212,'есть общий признак? многозначное слово',12,'#2b2110',true)}
          ${т(168,226,'нет общего признака? омонимы',12,'#2b2110',true)}</g>`:''}
        ${в!=null?`<g>${проявить('15s',0.7,0.82)}
          ${подпись(168,252,'сначала значение — потом название',GREEN,12)}</g>`:''}
      `,266)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='значение'?'hit':(в?'miss':''), 'Смотрим на значение: близкое, противоположное или разное', "r1051Свод('значение')")}
        ${BTN(4, в==='длина'?'miss':'', 'Смотрим на длину слов: короткие и длинные', "r1051Свод('длина')")}
        ${BTN(5, в==='буквы'?'miss':'', 'Смотрим на первые буквы слов', "r1051Свод('буквы')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='значение',
        в==='значение' ? 'Верно: всё решает значение. Близкое — синонимы, противоположное — антонимы, разное при одной записи — омонимы.'
                       : 'Нет: длина и буквы ничего не решают. Смотрим только на значение слов.') : ЖДЁТ()) +
      ПРАВИЛО2(в,'Реши, на что смотреть в первую очередь.',
                 'Различаем по значению: <b>близкое — противоположное — разное</b>.');
  }

  const Т2={
    имя:'Тренажёр 2 · омонимы',
    задания:[
      { ф:'Ключ (родник) и ключ (от двери) — это…', о:['омонимы','многозначное слово','синонимы'], в:0, р:'Значения не связаны.' },
      { ф:'Ножка стула и ножка гриба — это…', о:['одно многозначное слово','омонимы','антонимы'], в:0, р:'Общий признак — маленькая опора.' },
      { ф:'Лук (растение) и лук (оружие) — это…', о:['омонимы','многозначное слово','антонимы'], в:0, р:'Ничего общего у значений нет.' },
      { ф:'Мир (вселенная) и мир (покой) — это…', о:['омонимы','синонимы','многозначное слово'], в:0, р:'Разные значения, одна запись.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · разбор чужой записи',
    задания:[
      { ф:'Ученик назвал «смелый — трусливый» синонимами. Что не так?', о:['это антонимы: значения противоположны','всё верно','это омонимы'], в:0, р:'Синонимы: смелый — храбрый.' },
      { ф:'Ученик считает, что «ключ» — многозначное слово. Что не так?', о:['значения не связаны, это омонимы','всё верно','это синонимы'], в:0, р:'Общего признака нет.' },
      { ф:'Ученик написал «одеть шапку». Что не так?', о:['надеть шапку: надеть — на себя','всё верно','надо «надеть ребёнка»'], в:0, р:'Одеть — кого-то.' },
      { ф:'Ученик сказал, что у слова «тетрадь» есть антоним. Что не так?', о:['у «тетради» антонима нет','всё верно','антоним — «ручка»'], в:0, р:'Противоположного значения нет.' }
    ]
  };

  /* сдвиг вариантов: верный ответ не всегда первый */
  function сдвиг(варианты, ключ){
    const н=варианты.length; if(н<2) return варианты;
    const к=((ключ*7+3)%н);
    return варианты.slice(к).concat(варианты.slice(0,к));
  }

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const порядок=сдвиг(з.о.map((о,к)=>({о,к})), шаг+1).map(х=>х.о);
    const верный=порядок.indexOf(з.о[з.в]);
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===верный;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + порядок.map((о,к)=>
        BTN(4+к, готово&&к===верный?'hit':(готово&&к===отв?'miss':''), о, `r1051T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r1051TNext('${ключ}')`)}</div>` : '');
  }
  function F13(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F14(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F15(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L1051={
    id: ID, title:'Синонимы, антонимы, омонимы', ico:'📚',
    src:'Русский язык · 5 класс · Лексика', subj:'rus',
    explain: [
      'Начнём с разминки по прошлому уроку: мы разбирали паронимы — слова, которые звучат похоже, а значат разное. «Надеть» мы говорим про себя, «одеть» — про кого-то другого. Сегодня познакомимся ещё с тремя понятиями. Оказывается, слова умеют дружить, спорить и даже выглядеть как близнецы, означая при этом совершенно разное.',

      'Первое понятие — синонимы. Это слова, близкие по значению: смелый, храбрый, отважный. Все три говорят об одном: человек не боится. Но оттенки всё же разные: «отважный» звучит особенно торжественно. Синонимы позволяют не повторять одно и то же слово в тексте. Сравните: «большой дом, огромный дом, громадный дом» — смысл один, а речь богаче.',

      'Второе понятие — антонимы. Это слова с противоположным значением: день и ночь, холодный и горячий, друг и враг, громко и тихо. Их удобно представлять на одной шкале: два конца, а между ними — все оттенки. Важно помнить: антоним есть не у каждого слова. У слов «стол», «тетрадь», «окно» антонима нет, потому что противоположного значения у них не бывает.',

      'Третье понятие — омонимы. Это слова, которые пишутся и звучат одинаково, а значения имеют совершенно разные. Самый известный пример — ключ: ключ от двери, ключ-родник и скрипичный ключ. Ничего общего между этими предметами нет, просто так совпало. Другие примеры: коса (девичья, инструмент, песчаная), норка (зверёк и жилище), брак (супружество и испорченная работа).',

      'Здесь важно не перепутать омонимы с многозначными словами. Сравним. Лук — это растение и оружие: связи между ними нет, значит, это омонимы. А ножка — у стула, у гриба, у ребёнка: во всех случаях это маленькая опора, общий признак есть, значит, это одно многозначное слово. Проверка простая: спроси себя, есть ли у значений что-то общее.',

      'Научимся подбирать синоним. Возьмём слово «большой». Первый шаг — объяснить значение своими словами: такой, которого много, который занимает много места. Второй шаг — вспомнить слова с тем же значением: огромный, громадный, крупный. Обратите внимание: «маленький» сюда не подходит — это противоположное слово, антоним. Синоним и антоним легко перепутать, если не проверить значение.',

      'Теперь вместе подберём антоним к слову «громко». Мысленно поставим слово на шкалу и поищем его на другом конце: громко — тихо. А вот «громкий» не подходит: это просто другая форма того же слова, а не противоположное значение. И ещё раз о важном: если у слова нет пары-противника, значит, антонима у него нет — так бывает со словами «стол», «тетрадь», «карандаш».',

      'Попробуй сам. Норка — это маленький зверёк, и норка — это маленькая нора. Омонимы это или многозначное слово? Проверим связь: зверёк не является «маленькой норой», общего признака нет. Значит, перед нами омонимы. Если бы значения были связаны общим признаком, как у ножки стула и ножки гриба, мы назвали бы это многозначным словом.',

      'Разберёмся, почему так. У многозначного слова значения выросли из одного: сначала была ножка человека, потом ножка стула — потому что она тоже маленькая опора. У омонимов такой истории нет: ключ-родник и ключ от двери возникли независимо друг от друга. Поэтому и правило звучит так: есть общий признак — многозначное слово, нет общего признака — омонимы.',

      'Теперь ловушка, в которую попадают часто. Ученик сказал: «смелый и трусливый — синонимы». Это ошибка. Синонимы называют одно и то же: смелый, храбрый, отважный. А смелый и трусливый — противоположные по значению, то есть антонимы. Вывод простой: прежде чем назвать слова синонимами, проверь, совпадают значения или спорят.',

      'Соберём всё вместе. Синонимы — слова, близкие по значению: смелый, храбрый, отважный. Антонимы — слова с противоположным значением: день и ночь, друг и враг. Омонимы — слова, которые пишутся одинаково, а значения имеют разные: ключ, коса, норка, лук. И отдельно: если у значений есть общий признак, это многозначное слово, а не омонимы.',

      'Пройти практику: пять уровней. Первые два — синонимы и антонимы, третий вернёт нас к прошлому уроку про паронимы, четвёртый смешает темы, а пятый спрячет задачу в работу редактора. В конце — выходной билет: придумай слово-омоним и два его разных значения.',

      'Первый тренажёр — синонимы и антонимы: подбираем слова по значению.',

      'Второй тренажёр — омонимы: решаем, омонимы это или многозначное слово.',

      'Третий тренажёр — разбор чужой записи: в каждой задаче ученик ошибся, и нужно объяснить, что именно он перепутал.'
    ],
    check: {
      q:'Смелый, храбрый, отважный — это…',
      choices:['синонимы','антонимы','омонимы'],
      ans:0,
      exp:'Значение близкое: все три слова говорят «не боится». Это синонимы.'
    },
    tasks: [
      { q:'День и ночь — это…', kind:'choice',
        choices:['антонимы','синонимы'], ans:0,
        hints:['Сравни значения.','Они противоположны.'],
        sol:'Антонимы.' },
      { q:'Ключ (родник) и ключ (от двери) — это…', kind:'choice',
        choices:['омонимы','многозначное слово'], ans:0,
        hints:['Есть ли общий признак?','Значения не связаны.'],
        sol:'Омонимы.' },
      { q:'Ножка стула и ножка гриба — это…', kind:'choice',
        choices:['одно многозначное слово','омонимы'], ans:0,
        hints:['Что общего у значений?','Маленькая опора.'],
        sol:'Многозначное слово.' },
      { q:'Ученик назвал «смелый — трусливый» синонимами. Что не так?', kind:'choice',
        choices:['Это антонимы','Всё верно'], ans:0,
        hints:['Совпадают значения или спорят?','Синонимы: смелый, храбрый.'],
        sol:'Это антонимы.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L1051.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else if(f===14) сцена=F14(s);
    else сцена=F15(s);

    const ЗАГОЛОВКИ={
      1:['Разминка','Паронимы и три новых понятия'],
      2:['Синонимы','Слова-друзья'],
      3:['Антонимы','Слова-противники'],
      4:['Омонимы','Слова-близнецы'],
      5:['Омонимы или нет','Ищем общий признак'],
      6:['Я показываю','Как подобрать синоним'],
      7:['Мы делаем','Антоним, шаг пропущен'],
      8:['Ты делаешь','Омонимы сам'],
      9:['Почему так','Причина в связи значений'],
      10:['Ловушка','Антоним вместо синонима'],
      11:['Сводка','Три понятия рядом'],
      12:['Практика','Пять уровней'],
      13:['Тренажёр 1','Синонимы и антонимы'],
      14:['Тренажёр 2','Омонимы'],
      15:['Тренажёр 3','Разбор чужой записи']
    };
    const з=ЗАГОЛОВКИ[f]||['Лексика','Синонимы, антонимы, омонимы'];
    el.innerHTML = `<div class="s6 l1051" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r1051Разминка=(к)=>{ S().разминка=к; chRender(0); };
  window.r1051Син=(к)=>{ S().синонимы=к; chRender(0); };
  window.r1051Ант=(к)=>{ S().антонимы=к; chRender(0); };
  window.r1051Омо=(к)=>{ S().омонимы=к; chRender(0); };
  window.r1051Разл=(к)=>{ S().различие=к; chRender(0); };
  window.r1051Я=(к)=>{ S().я=к; chRender(0); };
  window.r1051Мы=(к)=>{ S().мы=к; chRender(0); };
  window.r1051Ты=(к)=>{ S().ты=к; chRender(0); };
  window.r1051Поч=(к)=>{ S().почему=к; chRender(0); };
  window.r1051Лов=(к)=>{ S().ловушка=к; chRender(0); };
  window.r1051Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r1051Pick=(уровень,вариант)=>{
    const s=S();
    const порядок=сдвиг(УРОВНИ[уровень].варианты, уровень+1);
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(порядок[вариант] && порядок[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r1051Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r1051T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    const порядок=сдвиг(з.о.map((о,к)=>({о,к})), шаг+1).map(х=>х.о);
    const верный=порядок.indexOf(з.о[з.в]);
    s[ключ+'Ответ']=вариант;
    if(вариант===верный) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r1051TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L1051; else arr.push(L1051);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA1051={render:render, L:L1051};
})();
