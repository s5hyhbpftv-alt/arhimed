/* ====== ИНФОРМАТИКА · УРОК 541 · «РЕКУРСИЯ: ЗАДАЧА ВНУТРИ ЗАДАЧИ» ============================
   5–6 класс, курс «С нуля». В 525 рекурсия мелькнула во фракталах, в 513 были
   помощники, в 539 — стек. Здесь рекурсия — главная тема.

   ГЛАВНАЯ МЫСЛЬ. Рекурсия — решать задачу через ту же задачу, только
   поменьше, пока не дойдёшь до самого простого случая (базы). Без базы
   рекурсия не кончается. Ханойская башня: n дисков = перенести n − 1,
   переложить большой, снова перенести n − 1: 1, 3, 7, 15 ходов. Вызовы
   копятся в стопке (стеке) и раскручиваются обратно: 4! = 24. Ветвящаяся
   рекурсия — дерево; поиск во вложенных папках тоже рекурсия.

   СЮЖЕТ. «Сокровищница Гиерона». Архимеду принесли сундук в сундуке; жрецы
   храма Аполлона показывают башню из золотых дисков; писарь учится считать
   ступени, свитки и папки с делами.

   РУКАМИ: открывать вложенные сундуки до ключа; решить Ханойскую башню из
   двух и из трёх дисков касанием стержней (большой на маленький не ляжет).

   ВСЕ ОТВЕТЫ ПРОВЕРЕНЫ:
     S(5) = 5 + 4 + 3 + 2 + 1 + 0 = 15; ханой: 2 диска — 3 хода, 3 — 7, 4 — 15;
     4! = 4·3·2·1 = 24; дерево глубины 3 по две ветки — 8 кончиков;
     папки: корень + 3 + (2 + 1) = 7. */
(function(){
  'use strict';

  const ID = 541;
  const GOLD='#ffd76a', GREEN='#8fe0b0', RED='#ff8a78', ЯНТАРЬ='#f0b050', ЛАЗУРЬ='#8fd0f0';
  const ИНК='#f6f1e8', МУТ='#bdb3a4', ЛИНИЯ='#5a4a66';

  const ДЕЛА = [
    {ключ:'сундук', имя:'Добраться до ключа', итог:'5 сундуков'},
    {ключ:'башня',  имя:'Перенести башню за 7 ходов', итог:'7 ходов'},
    {ключ:'стопка', имя:'Раскрутить стопку вызовов', итог:'4! = 24'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const CSS=`
  #lvis .s6.l541{gap:14px}
  #lvis .s6.l541 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l541 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l541 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#262036,#161222);border:1.5px solid var(--line)}
  #lvis .s6.l541 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l541 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l541 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l541 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l541 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l541 .карт .текст b{color:${GOLD}}
  #lvis .s6.l541 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l541 .правило b{color:${GOLD}}
  #lvis .s6.l541 .лист{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.04));
    border:1.5px solid rgba(255,215,106,.5);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l541 .лист .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l541 .лист .шапка b{font-size:18px;letter-spacing:0;text-transform:none;color:${GOLD}}
  #lvis .s6.l541 .лист .шапка b.готово{color:${GREEN}}
  #lvis .s6.l541 .лист ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l541 .лист li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l541 .лист li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l541 .лист li.есть{color:${ИНК}}
  #lvis .s6.l541 .лист li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,224,176,.14)}
  #lvis .s6.l541 .лист li span{margin-left:auto;white-space:nowrap;font-size:14px;color:var(--mut)}
  #lvis .s6.l541 .лист li.есть span{color:${GREEN}}
  #lvis .s6.l541 .ряд{display:grid;gap:8px;width:100%}
  #lvis .s6.l541 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:16px;font-weight:700;
    border:1.5px solid rgba(255,215,106,.5);background:rgba(255,215,106,.1);color:${ИНК};padding:6px 4px;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l541 .ряд button:active{transform:translateY(2px);background:rgba(255,215,106,.24)}
  #lvis .s6.l541 .ряд button:disabled{opacity:.4;cursor:default}
  #lvis .s6.l541 .ряд button.вкл{border-color:${GOLD};background:rgba(255,215,106,.28)}
  #lvis .s6.l541 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l541 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l541 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l541 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l541 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l541 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-size:17px}
  #lvis .s6.l541 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-variant-numeric:tabular-nums;font-size:18px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l541 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l541 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l541 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l541 .уровни .точка.сейчас{background:${GOLD};animation:l541dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l541dot{0%,100%{opacity:1}50%{opacity:.55}}
  #lvis .s6.l541 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l541{-webkit-text-size-adjust:100%}
  #lvis .s6.l541 [data-anim]{animation:l541rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l541rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l541 [data-anim]{animation:none!important}
    #lvis .s6.l541 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l541 button{transition:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l541-style');
      if(!s){ s=document.createElement('style'); s.id='l541-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,выкл) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}"${выкл?' disabled':''}>${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Сокровищница Гиерона</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,f) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, 'r541Отв('+f+','+к+')')).join('')}</div>`;
  const ЛИСТ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'лист',
      `<div class="шапка"><span>Тайны сокровищницы</span><b class="${всё?'готово':''}">${
        всё?'все тайны открыты':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}<span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };
  const ходов = (n) => n+' ход'+(n%10===1&&n%100!==11?'':((n%10>=2&&n%10<=4)&&!(n%100>=12&&n%100<=14)?'а':'ов'));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.45;0.45;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const вырасти = (длит,доля) => анК('opacity','0.12;0.12;1;1',длит,'0;'+кт(доля)+';'+кт(доля+0.06)+';1');
  const сдвигРаз = (из,в,длит,задержка) => ДВИЖ ?
    `<animateTransform attributeName="transform" type="translate" from="${из}" to="${в}" dur="${длит}" begin="${(задержка||0).toFixed(2)}s"
       fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c541-зал" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2a2238"/><stop offset="1" stop-color="#15101e"/>
      </linearGradient>
      <linearGradient id="c541-мрамор" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f0ebe0"/><stop offset="1" stop-color="#b8b0a0"/>
      </linearGradient>
      <linearGradient id="c541-бронза" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#6a4410"/><stop offset="0.45" stop-color="#e0b060"/><stop offset="1" stop-color="#5a3a0a"/>
      </linearGradient>
      <linearGradient id="c541-диск" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#fff0a8"/><stop offset="0.4" stop-color="#f0c040"/><stop offset="1" stop-color="#8a5a08"/>
      </linearGradient>
      <linearGradient id="c541-сундук" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#9a5a2a"/><stop offset="1" stop-color="#4a2410"/>
      </linearGradient>
      <radialGradient id="c541-сияние" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#ffe8a0" stop-opacity=".6"/><stop offset="1" stop-color="#ffb040" stop-opacity="0"/>
      </radialGradient>
      <linearGradient id="c541-папирус" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f2e2b8"/><stop offset="1" stop-color="#d4b67c"/>
      </linearGradient>
      <filter id="c541-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="0" dy="2" stdDeviation="1.8" flood-color="#000" flood-opacity=".55"/>
      </filter>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const зал = (Н) => `<rect x="0" y="0" width="336" height="${Н}" fill="url(#c541-зал)"/>
    ${[18,318].map(x=>`<rect x="${x-10}" y="0" width="20" height="${Н}" fill="rgba(240,235,224,.08)" data-декор="1"/>`).join('')}`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c541-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(14,10,20,.92)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* Ханойская башня */
  const СТЕРЖНИ = [60,168,276];
  function башня(s,f,n,Н){
    const х=s['х'+f]||{с:[Array.from({length:n},(_,i)=>n-i),[],[]],ход:0,выбран:null};
    const низ=Н-44, вд=18;
    let g=`<g filter="url(#c541-тень)"><rect x="12" y="${низ}" width="312" height="16" rx="4" fill="url(#c541-мрамор)"/></g>`;
    СТЕРЖНИ.forEach((x,k)=>{ g+=`<rect x="${x-4}" y="${низ-110}" width="8" height="110" rx="3" fill="url(#c541-бронза)"/>`; });
    СТЕРЖНИ.forEach((x,k)=>{
      х.с[k].forEach((d,j)=>{
        const верх = j===х.с[k].length-1, поднят = верх && х.выбран===k;
        const ш=30+d*22, y=низ-(j+1)*вд-(поднят?40:0), свеж = s.упал && s.упал.f===f && s.упал.k===k && верх;
        g+=`<g>${свеж?сдвигРаз('0 -50','0 0','0.4s',0):''}<g filter="url(#c541-тень)"><rect x="${x-ш/2}" y="${y}" width="${ш}" height="${вд-2}" rx="8" fill="url(#c541-диск)" stroke="#6a4408" stroke-width="1"/></g>
          ${поднят?`<rect x="${x-ш/2-4}" y="${y-4}" width="${ш+8}" height="${вд+6}" rx="10" fill="none" stroke="${GOLD}" stroke-width="2">${анЛин('opacity','1;0.3;1','1s')}</rect>`:''}</g>`;
      });
      g+=`<g onclick="r541Стержень(${f},${k})" style="cursor:pointer"><rect x="${x-54}" y="${низ-160}" width="108" height="${176}" fill="transparent"/></g>`;
      g+=т(x,низ+32,['I','II','III'][k],13,МУТ,true);
    });
    return g;
  }
  function кнопкиБашни(s,f,n){
    const х=s['х'+f]||{с:[],выбран:null};
    return `<div class="ряд" style="grid-template-columns:repeat(3,1fr)">${[0,1,2].map(k=>BTN(3+k,х.выбран===k?'вкл':'','Стержень '+['I','II','III'][k],'r541Стержень('+f+','+k+')')).join('')}</div>
      <div class="ряд">${BTN(6,'','Начать заново','r541Башня('+f+')')}</div>`;
  }

  /* ================= КАДРЫ ================= */

  /* 1. Сундук в сундуке */
  function F1(s){
    const Н=280, откр=s.откр1||0, в=s.ответ1, ключ=откр>=5, ок=в===0;
    let g='';
    for(let i=0;i<5;i++){
      const ш=250-i*44, в2=150-i*24, x=168-ш/2, y=Н-40-в2, открыт=i<откр, свеж=i===откр-1&&s.открыл;
      g+=`<g filter="url(#c541-тень)"><rect x="${x}" y="${y}" width="${ш}" height="${в2}" rx="6" fill="url(#c541-сундук)" stroke="#2a1408" stroke-width="1.4" opacity="${открыт?0.55:1}"/></g>`;
      if(!открыт){ g+=`<rect x="${x}" y="${y}" width="${ш}" height="${Math.min(22,в2*0.25)}" rx="6" fill="#b86a34" stroke="#2a1408"/>
        <circle cx="168" cy="${y+в2/2}" r="${6-i*0.6}" fill="#e0b060" stroke="#6a4408"/>`; break; }
      else if(свеж && ДВИЖ) g+=`<g opacity="1">${сдвигРаз('0 0','0 -40','0.6s',0)}<animate attributeName="opacity" from="1" to="0" dur="0.6s" fill="freeze"/><rect x="${x}" y="${y}" width="${ш}" height="${Math.min(22,в2*0.25)}" rx="6" fill="#b86a34"/></g>`;
    }
    if(ключ) g+=`<circle cx="168" cy="${Н-60}" r="40" fill="url(#c541-сияние)" data-декор="1"/>
      <g transform="translate(168 ${Н-62})"><circle cx="-12" cy="0" r="8" fill="none" stroke="${GOLD}" stroke-width="4"/><path d="M-4 0 h24 v8 M14 0 v6" stroke="${GOLD}" stroke-width="4" fill="none"/></g>`;
    return ЛИСТ(s) +
      ЗАДАЧА('Царь Гиерон прислал Архимеду сундук с запиской: «Ключ от сокровищницы — внутри». Архимед открыл — а там сундук поменьше. Правило простое: <b>открой сундук и ищи ключ в том, что внутри</b>. Открывай, пока не найдёшь.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${g}
        ${т(168,24,ключ?'ключ найден в пятом сундуке':'открыто сундуков: '+откр,13,ключ?GREEN:МУТ,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      (!ключ ? `<div class="ряд">${BTN(3,'','Открыть сундук №'+(откр+1),'r541Открыть()')}</div>` + СКАЗ('Попробуй','Жми «Открыть» — в каждом сундуке что-то есть.') :
        ОТВЕТЫ('',['Сундуки становятся меньше, а в самом маленьком лежит ключ, а не сундук','В правиле сказано: «сундуков ровно пять»'],0,в,1) +
        (в==null ? СКАЗ('Вопрос','Правило «открой и ищи внутри» говорит одно и то же каждый раз. Почему оно всё-таки закончилось?') :
          РАЗБОР(ок,['Каждый раз задача та же, но <b>меньше</b>. А в самом маленьком сундуке — не сундук, а ключ: это <b>простейший случай</b>, на нём всё и остановилось.',
            'В правиле ни слова о числе сундуков: оно одно и то же для любого. Остановил его <b>ключ</b> в самом маленьком сундуке.'][в]))) +
      (ок ? ПРАВИЛО('<b>Рекурсия</b> — решать задачу через ту же задачу, только <b>меньше</b>. Нужен <b>простейший случай</b> (база), на котором всё останавливается.') : '');
  }

  /* 2. Лестница: сумма */
  function F2(s){
    const Н=260, в=s.ответ2, ок=в===0;
    const ст=[5,4,3,2,1];
    return ЛИСТ(s) +
      ЗАДАЧА('Писарь считает сумму ступеней храма: <b>S(5) = 5 + 4 + 3 + 2 + 1</b>. Архимед подсказывает: «S(5) — это 5 + S(4). А S(4) — это 4 + S(3). И так до <b>S(0) = 0</b>». Чему равно S(5)?') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${ст.map((n,i)=>{ const x=30+i*56, h=n*28;
          return `<g>${вырасти('9s',0.05+i*0.12)}<g filter="url(#c541-тень)"><rect x="${x}" y="${Н-40-h}" width="50" height="${h}" rx="3" fill="url(#c541-мрамор)"/></g>
            ${т(x+25,Н-48-h+22,String(n),16,'#3a2a14',true)}
            ${т(x+25,Н-20,'S('+n+')',11,МУТ,true)}</g>`; }).join('')}
        ${т(168,28,ок?'S(5) = 5 + 4 + 3 + 2 + 1 + 0 = 15':'S(n) = n + S(n − 1),  S(0) = 0',13,ок?GREEN:GOLD,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['15','5','10'],0,в,2) +
      (в==null ? СКАЗ('Вопрос','Чему равно S(5)?') :
        РАЗБОР(ок,['Раскручиваем: S(5) = 5 + S(4) = 5 + 4 + S(3) = … = 5 + 4 + 3 + 2 + 1 + 0 = <b>15</b>.',
          '5 — только первое слагаемое. Дальше ещё S(4) = 4 + S(3)… Всего <b>15</b>.',
          '10 = 4 + 3 + 2 + 1 — это S(4). А S(5) = 5 + S(4) = <b>15</b>.'][в])) +
      (ок ? ПРАВИЛО('Рекурсия: <b>шаг</b> (S(n) = n + S(n − 1)) уменьшает задачу, <b>база</b> (S(0) = 0) её останавливает.') : '');
  }

  /* 3. Забыли базу */
  function F3(s){
    const Н=240, в=s.ответ3, ок=в===0;
    return ЛИСТ(s) +
      ЗАДАЧА('Молодой писарь получил приказ: «<b>Перепиши это письмо и отдай следующему писарю</b>». Следующий сделал то же самое, и ещё один… Через неделю во дворце не осталось ни одного свободного писаря.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${[0,1,2,3,4].map(i=>{ const x=34+i*58, y=110+(i%2)*10;
          return `<g>${вырасти('6s',0.05+i*0.12)}<g filter="url(#c541-тень)"><rect x="${x-20}" y="${y-30}" width="40" height="54" rx="4" fill="url(#c541-папирус)"/></g>
            ${[0,1,2].map(k=>`<line x1="${x-12}" y1="${y-18+k*10}" x2="${x+12}" y2="${y-18+k*10}" stroke="#8a6a3a" stroke-width="1.4"/>`).join('')}
            ${i<4?т(x+31,y+4,'→',16,GOLD,true):''}</g>`; }).join('')}
        ${т(322,122,'…',22,МУТ,true,'end')}
        ${подпись(168,Н-14, ок?'нет базы — нет конца':'когда это кончится?', ок?RED:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Условия, когда остановиться','Бумаги и чернил'],0,в,3) +
      (в==null ? СКАЗ('Вопрос','Чего не хватает этому приказу?') :
        РАЗБОР(ок,['В приказе есть «шаг» (перепиши и передай), но нет <b>базы</b>: «если ты десятый — остановись». Такая рекурсия не кончается никогда.',
          'Бумага кончится, но и тогда писари будут ждать. Нужно <b>условие остановки</b>: без него рекурсия бесконечна.'][в])) +
      (ок ? ПРАВИЛО('Рекурсия без базы — бесконечная. Программа «падает»: у компьютера кончается память под незаконченные дела.') : '');
  }

  /* 4. Башня из двух дисков */
  function F4(s){
    const Н=230, х=s.х4, готово = х && х.с[2].length===2, в=s.ответ4, ок=в===0;
    return ЛИСТ(s) +
      ЗАДАЧА('В храме Аполлона жрецы переносят золотые диски. Правила: за ход — <b>один</b> диск, с верхушки; <b>большой нельзя класть на маленький</b>. Перенеси башню из двух дисков со стержня I на стержень III. Коснись стержня, откуда брать, потом — куда класть.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${башня(s,4,2,Н)}
        ${т(168,24,'ходов: '+(х?х.ход:0),14,готово?GREEN:ИНК,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      кнопкиБашни(s,4,2) +
      (х&&х.ошибка ? РАЗБОР(false,х.ошибка) : '') +
      (!готово ? СКАЗ('Попробуй','Маленький диск — на стержень II, большой — на III, маленький — сверху.') :
        ОТВЕТЫ('три',['3','2','4'],0,в,4) +
        (в==null ? СКАЗ('Вопрос','Башня перенесена за '+ходов(х.ход)+'. За сколько ходов меньше всего можно перенести два диска?') :
          РАЗБОР(ок,['Маленький в сторону, большой на место, маленький сверху — <b>3 хода</b>. Меньше нельзя: большой диск должен сдвинуться, а маленький — уйти и вернуться.',
            'Двумя ходами не выйдет: маленький диск мешает большому, его надо убрать и потом вернуть — <b>3 хода</b>.',
            'Четыре — лишний ход. Хватает <b>трёх</b>.'][в]))) +
      (ок ? ПРАВИЛО('Башня из 2 дисков: убери верхний в сторону, переложи нижний, верни верхний — <b>3 хода</b>.') : '');
  }

  /* 5. Башня из трёх дисков */
  function F5(s){
    const Н=230, х=s.х5, готово = х && х.с[2].length===3;
    return ЛИСТ(s) +
      ЗАДАЧА('Теперь три диска. Подсказка Архимеда: «Верхние два диска — это та же задача, которую ты уже решил! Перенеси их на средний стержень, переложи большой на III, и снова перенеси два сверху». Сумей уложиться в <b>7 ходов</b>.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${башня(s,5,3,Н)}
        ${т(168,24,'ходов: '+(х?х.ход:0)+(готово?' — башня перенесена':''),14,готово?(х.ход===7?GREEN:ЯНТАРЬ):ИНК,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      кнопкиБашни(s,5,3) +
      (х&&х.ошибка ? РАЗБОР(false,х.ошибка) : '') +
      (!готово ? СКАЗ('Подсказка','Сначала перенеси два верхних диска на стержень II (3 хода), потом большой — на III.') :
        РАЗБОР(х.ход===7, х.ход===7 ? 'Ровно <b>7 ходов</b>: 3 (два диска на II) + 1 (большой на III) + 3 (два диска на III).' :
          'Башня перенесена за '+ходов(х.ход)+'. Можно за <b>7</b>: 3 + 1 + 3. Нажми «Начать заново» и попробуй по подсказке.')) +
      (готово && х.ход===7 ? ПРАВИЛО('Башня из n дисков = башня из <b>n − 1</b> (в сторону) + 1 ход + башня из <b>n − 1</b> (на место). Задача решена через себя саму.') : '');
  }

  /* 6. Сколько ходов для 4 дисков */
  function F6(s){
    const Н=240, в=s.ответ6, ок=в===0;
    const ряд=[[1,1],[2,3],[3,7],[4,ок?15:'?']];
    return ЛИСТ(s) +
      ЗАДАЧА('Жрецы записали: 1 диск — 1 ход, 2 — 3 хода, 3 — 7 ходов. По легенде, у храма есть башня из <b>64 дисков</b>, и когда жрецы её перенесут, наступит конец света. Сначала посчитай для <b>4 дисков</b>.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${ряд.map(([n,h],i)=>{ const x=36+i*76, hh=h==='?'?20:Math.max(8,h*8);
          return `<g>${вырасти('8s',0.05+i*0.15)}<g filter="url(#c541-тень)"><rect x="${x}" y="${Н-56-hh}" width="56" height="${hh}" rx="4" fill="${h==='?'?'rgba(255,255,255,.1)':'url(#c541-диск)'}"/></g>
            ${т(x+28,Н-62-hh,String(h),16,h==='?'?МУТ:GOLD,true)}${т(x+28,Н-34,n+' д.',12,МУТ,true)}</g>`; }).join('')}
        ${т(168,28,ок?'7 + 1 + 7 = 15':'ходы(n) = ходы(n − 1) + 1 + ходы(n − 1)',12,ок?GREEN:GOLD,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['15','14','16'],0,в,6) +
      (в==null ? СКАЗ('Вопрос','Сколько ходов нужно для 4 дисков?') :
        РАЗБОР(ок,['Три диска в сторону — 7 ходов, большой — 1, три диска обратно — 7: <b>15</b>. Для 64 дисков выйдет больше 18 миллиардов миллиардов ходов — по ходу в секунду это сотни миллиардов лет.',
          '14 = 7 + 7 — забыли ход самого большого диска. <b>7 + 1 + 7 = 15</b>.',
          '16 — это удвоение 8. Но 3 диска — это 7 ходов, а не 8: 7 + 1 + 7 = <b>15</b>.'][в])) +
      (ок ? ПРАВИЛО('Каждый новый диск больше чем удваивает работу: 1, 3, 7, 15, 31… Рекурсия коротко <b>записывается</b>, но может долго <b>работать</b>.') : '');
  }

  /* 7. Стопка вызовов: 4! */
  function F7(s){
    const Н=300, в=s.ответ7, ок=в===0;
    const выз=[['4! = 4 · 3!','24'],['3! = 3 · 2!','6'],['2! = 2 · 1!','2'],['1! = 1','1']];
    return ЛИСТ(s) +
      ЗАДАЧА('Архимед считает, сколькими способами можно рассадить 4 гостей: <b>4! = 4 · 3 · 2 · 1</b> (читается «четыре факториал»). Рекурсивно: 4! = 4 · 3!, 3! = 3 · 2!… Каждый незаконченный расчёт ложится свитком в <b>стопку</b>, пока не дойдём до 1! = 1. Потом стопка раскручивается.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${выз.map(([ф,р],i)=>{ const y=40+i*56;
          return `<g>${вырасти('10s',0.04+i*0.1)}<g filter="url(#c541-тень)"><rect x="${30+i*14}" y="${y}" width="${200-i*28}" height="42" rx="6" fill="url(#c541-папирус)"/></g>
            ${т(130,y+27,ф,15,'#3a2410',true)}</g>
            <g>${ок?проявить('10s',0.5+(3-i)*0.1,0.56+(3-i)*0.1):анЛин('opacity','0;0','1s')}${т(290,y+27,'= '+р,17,GREEN,true)}</g>`; }).join('')}
        ${т(290,24,'обратно ↑',11,МУТ,true)}${т(130,24,'вниз ↓ (стопка растёт)',11,МУТ,true)}
        ${подпись(168,Н-12, ок?'1 → 2 → 6 → 24':'дошли до базы 1! = 1 — что дальше?', ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['24','10','16'],0,в,7) +
      (в==null ? СКАЗ('Вопрос','Чему равно 4!?') :
        РАЗБОР(ок,['Раскручиваем стопку снизу: 1! = 1, 2! = 2 · 1 = 2, 3! = 3 · 2 = 6, 4! = 4 · 6 = <b>24</b>.',
          '10 = 4 + 3 + 2 + 1 — это сумма, а факториал — <b>произведение</b>: 4 · 3 · 2 · 1 = 24.',
          '16 = 4 · 4. Но множители уменьшаются: 4 · 3 · 2 · 1 = <b>24</b>.'][в])) +
      (ок ? ПРАВИЛО('Незаконченные вызовы копятся в <b>стеке</b> (урок 539), а на базе раскручиваются обратно: 1 → 2 → 6 → 24.') : '');
  }

  /* 8. Ветвящаяся рекурсия */
  function F8(s){
    const Н=270, в=s.ответ8, ок=в===0;
    let ветви='';
    const ветка=(x,y,дл,уг,гл)=>{
      const x2=x+дл*Math.sin(уг), y2=y-дл*Math.cos(уг);
      ветви+=`<g>${вырасти('8s',0.05+гл*0.18)}<line x1="${x.toFixed(1)}" y1="${y.toFixed(1)}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}" stroke="${гл===3?GREEN:'#a07040'}" stroke-width="${6-гл*1.3}" stroke-linecap="round"/></g>`;
      if(гл===3){ ветви+=`<g>${вырасти('8s',0.7)}<circle cx="${x2.toFixed(1)}" cy="${y2.toFixed(1)}" r="6" fill="${ок?GOLD:GREEN}"/></g>`; return; }
      ветка(x2,y2,дл*0.68,уг-0.5,гл+1); ветка(x2,y2,дл*0.68,уг+0.5,гл+1);
    };
    ветка(168,Н-30,70,0,0);
    return ЛИСТ(s) +
      ЗАДАЧА('В саду Архимед показывает правило дерева: «Нарисуй ствол, а на его конце — <b>два дерева поменьше</b>». Каждое маленькое дерево делает то же. База: на третьем ветвлении вместо деревьев — листья. Сколько листьев?') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${ветви}
        ${т(168,24,ок?'2 · 2 · 2 = 8 листьев':'ствол → 2 → 4 → ?',13,ок?GREEN:GOLD,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['8','6','3'],0,в,8) +
      (в==null ? СКАЗ('Вопрос','Сколько листьев на дереве?') :
        РАЗБОР(ок,['Каждая ветка делится на две: 1 → 2 → 4 → <b>8</b>. Как у кранов в журнале (урок 536): удвоение на каждом шаге.',
          '6 = 2 + 2 + 2 — сложили, а ветки <b>удваиваются</b>: 2 · 2 · 2 = 8.',
          '3 — это число ветвлений, а не листьев. Листьев 2 · 2 · 2 = <b>8</b>.'][в])) +
      (ок ? ПРАВИЛО('Если задача вызывает себя <b>дважды</b>, работа растёт как дерево: 2, 4, 8, 16… Так рисуют фракталы (урок 525).') : '');
  }

  /* 9. Папки в папках */
  function F9(s){
    const Н=270, в=s.ответ9, ок=в===0;
    const папка=(x,y,имя,поздн)=>`<g>${вырасти('9s',поздн)}<g filter="url(#c541-тень)"><path d="M${x-26} ${y-14} h16 l5 -6 h31 v34 h-52 z" fill="#e0b060" stroke="#6a4408"/></g>${т(x,y+8,имя,10,'#3a2408',true)}</g>`;
    const линия=(x1,y1,x2,y2)=>`<path d="M${x1} ${y1+20} V${(y1+y2)/2} H${x2} V${y2-20}" stroke="#8a7a9a" stroke-width="1.6" fill="none"/>`;
    return ЛИСТ(s) +
      ЗАДАЧА('Прошло две тысячи лет. В компьютере потерялся файл «чертёж.png». Поиск действует рекурсивно: «Загляни в папку. Для каждой папки внутри — <b>сделай то же самое</b>». Сколько папок он проверит, включая главную?') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${линия(168,40,70,120)}${линия(168,40,168,120)}${линия(168,40,266,120)}
        ${линия(70,120,36,210)}${линия(70,120,104,210)}${линия(168,120,168,210)}
        ${папка(168,40,'Архимед',0.05)}
        ${папка(70,120,'Чертежи',0.2)}${папка(168,120,'Письма',0.25)}${папка(266,120,'Счета',0.3)}
        ${папка(36,210,'Машины',0.45)}${папка(104,210,'Башни',0.5)}${папка(168,210,'Египет',0.55)}
        ${ок?`<circle cx="104" cy="210" r="30" fill="none" stroke="${GREEN}" stroke-width="2.4"/>`:''}
        ${т(250,210,ок?'1 + 3 + 3 = 7':'',13,GREEN,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['7','4','3'],0,в,9) +
      (в==null ? СКАЗ('Вопрос','Сколько папок проверит поиск, если заглянет в каждую?') :
        РАЗБОР(ок,['Главная — 1, в ней 3 папки, а внутри них ещё 2 + 1: всего <b>7</b>. Файл нашёлся в «Башнях».',
          '4 — это главная и три папки внутри. Но поиск заглядывает и глубже: ещё 3 папки. Всего <b>7</b>.',
          '3 — только папки второго уровня. Всего 1 + 3 + 3 = <b>7</b>.'][в])) +
      (ок ? ПРАВИЛО('Вложенные папки, ветви дерева, узоры фракталов — всё, что «похоже само на себя», удобно обходить <b>рекурсией</b>.') : '');
  }

  /* 10. Итог */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=250;
    return ЛИСТ(s) +
      ЗАДАЧА(всё
        ? 'Ключ найден, башня перенесена, стопка раскручена. Архимед: «Большую задачу не обязательно решать целиком. Достаточно знать, как свести её к <b>такой же, но поменьше</b>, — и где остановиться».'
        : 'Тайны ещё не раскрыты — вернись к делам в списке. Вот что получится в конце.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${[0,1,2,3].map(i=>{ const ш=120-i*26, в2=80-i*16; return `<rect x="${86-ш/2}" y="${140-в2/2}" width="${ш}" height="${в2}" rx="6" fill="none" stroke="${GOLD}" stroke-width="${2.4-i*0.4}" opacity="${1-i*0.15}"/>`; }).join('')}
        <circle cx="86" cy="140" r="5" fill="${GOLD}"/>
        ${[3,2,1].map((d,j)=>`<rect x="${252-(30+d*14)/2}" y="${178-(j+1)*14}" width="${30+d*14}" height="12" rx="6" fill="url(#c541-диск)"/>`).join('')}
        <rect x="200" y="178" width="104" height="8" rx="3" fill="url(#c541-мрамор)"/>
        ${[['шаг: та же задача, но меньше',GOLD],['база: где остановиться',GREEN]].map(([t,ц],i)=>`<g>${проявить('8s',0.1+i*0.2,0.16+i*0.2)}${подпись(168,34+i*30,t,ц,12)}</g>`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','<b>Рекурсия</b> — решить задачу через ту же задачу поменьше. Ей нужен <b>шаг</b> (сундук внутри, S(n) = n + S(n − 1)) и <b>база</b> (ключ, S(0) = 0). Без базы она бесконечна. Ханойская башня: 1, 3, 7, 15 ходов. Вызовы копятся в стеке и раскручиваются: 4! = 24. Двойной вызов растёт деревом.') +
      ПРАВИЛО('<b>Шаг уменьшает, база останавливает.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Что нужно рекурсии, чтобы остановиться?', варианты:[{т:'много памяти',ок:false},{т:'база — самый простой случай',ок:true}], разбор:'Без базы рекурсия бесконечна.' },
    { вопрос:'S(n) = n + S(n − 1), S(0) = 0. Чему равно S(3)?', варианты:[{т:'6',ок:true},{т:'3',ок:false}], разбор:'3 + 2 + 1 + 0.' },
    { вопрос:'Ханойская башня из 2 дисков: сколько ходов меньше всего?', варианты:[{т:'4',ок:false},{т:'3',ок:true}], разбор:'1 + 1 + 1.' },
    { вопрос:'3! = ?', варианты:[{т:'6',ок:true},{т:'9',ок:false}], разбор:'3 · 2 · 1.' },
    { вопрос:'Ветка делится на 2, ветвлений 2. Сколько кончиков?', варианты:[{т:'3',ок:false},{т:'4',ок:true}], разбор:'2 · 2.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r541Reset()")}</div>` +
        ПРАВИЛО('<b>Шаг уменьшает, база останавливает.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r541Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Шаг и база', задания:[
    {q:'«Чтобы спуститься с n ступеней, шагни вниз и спустись с n − 1». Где остановиться?', в:0, варианты:['на 0 ступеней','на 100 ступенях'], раз:'0 ступеней — база.'},
    {q:'Рекурсия без базы…', в:0, варианты:['не кончится','кончится сама'], раз:'Ей нечем остановиться.'},
    {q:'S(4) = 4 + S(3), а S(3) = 6. S(4) = ?', в:1, варианты:['7','10'], раз:'4 + 6.'},
    {q:'Каждый следующий вызов задачи должен быть…', в:0, варианты:['меньше','больше'], раз:'Иначе до базы не дойти.'}
  ]};
  const Т2 = { имя:'Ханойская башня', задания:[
    {q:'1 диск — сколько ходов?', в:0, варианты:['1','2'], раз:'Просто переложить.'},
    {q:'3 диска — сколько ходов меньше всего?', в:1, варианты:['8','7'], раз:'3 + 1 + 3.'},
    {q:'4 диска?', в:0, варианты:['15','16'], раз:'7 + 1 + 7.'},
    {q:'Можно класть большой диск на маленький?', в:0, варианты:['нет','да'], раз:'Главное правило башни.'}
  ]};
  const Т3 = { имя:'Счёт рекурсией', задания:[
    {q:'4! = ?', в:0, варианты:['24','16'], раз:'4 · 3 · 2 · 1.'},
    {q:'5! = 5 · 4! = ?', в:1, варианты:['100','120'], раз:'5 · 24.'},
    {q:'Дерево: 3 ветвления по 2 ветки. Листьев?', в:0, варианты:['8','6'], раз:'2 · 2 · 2.'},
    {q:'Папка, в ней 2 папки, в каждой ещё по 2. Всего папок?', в:0, варианты:['7','5'], раз:'1 + 2 + 4.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r541T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r541TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L541 = {
    id: ID,
    title: 'Рекурсия: задача внутри задачи',
    ico: '🪆',
    src: 'Информатика · 5–6 класс · С нуля: рекурсия',
    subj: 'inf',
    explain: [
      'Сундук в сундуке: правило «открой и ищи внутри» одно и то же, но сундуки меньше, а в самом маленьком — ключ. Это рекурсия и её база.',
      'S(5) = 5 + S(4), …, S(0) = 0: шаг уменьшает задачу, база останавливает. S(5) = 15.',
      'Приказ «перепиши и передай» без условия остановки — бесконечная рекурсия.',
      'Ханойская башня из 2 дисков: 3 хода.',
      'Башня из 3 дисков: 2 диска в сторону, большой, 2 диска на место — 7 ходов.',
      'Ходы растут: 1, 3, 7, 15 — для 4 дисков 15.',
      'Вызовы копятся в стопке и раскручиваются: 4! = 24.',
      'Двойной вызов растёт деревом: 2 · 2 · 2 = 8 листьев.',
      'Поиск во вложенных папках — рекурсия: 7 папок.',
      'Итог: шаг уменьшает, база останавливает.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: шаг и база.',
      'Тренажёр 2: Ханойская башня.',
      'Тренажёр 3: счёт рекурсией.'
    ],
    check: {
      q: 'S(n) = n + S(n − 1), S(0) = 0. Чему равно S(4)?',
      choices: ['4','10','24'],
      ans: 1,
      exp: '4 + 3 + 2 + 1 + 0 = 10.'
    },
    tasks: [
      { q:'Сколько ходов меньше всего нужно для Ханойской башни из 3 дисков?', kind:'unit', ans:7, tol:0,
        hints:['2 диска — 3 хода.','3 + 1 + 3.'], sol:'7.' },
      { q:'Чему равно 4! = 4 · 3 · 2 · 1?', kind:'unit', ans:24, tol:0,
        hints:['Перемножь по шагам.'], sol:'24.' },
      { q:'Чего не хватает рекурсии, которая не кончается?', kind:'choice', choices:['базы','шага','памяти'], ans:0, tol:0,
        hints:['Где остановиться?'], sol:'Базы — простейшего случая.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L541.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    s.упал=null; s.открыл=false;
    const ЗАГОЛОВКИ={1:'Сундук в сундуке',2:'Ступени храма',3:'Приказ без конца',4:'Башня из двух',5:'Башня из трёх',
      6:'Легенда о 64 дисках',7:'Стопка вызовов',8:'Дерево в саду',9:'Папки в папках',10:'Тайны открыты',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l541" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Рекурсия'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  const ДИСКОВ = {4:2, 5:3};
  const нов = (n) => ({с:[Array.from({length:n},(_,i)=>n-i),[],[]],ход:0,выбран:null,ошибка:null});
  window.r541Стержень=(f,k)=>{ const s=S(); const n=ДИСКОВ[f]; const х=s['х'+f]||нов(n); х.ошибка=null;
    if(х.с[2].length===n){ s['х'+f]=х; chRender(0); return; }
    if(х.выбран==null){ if(х.с[k].length) х.выбран=k; else х.ошибка='На стержне '+['I','II','III'][k]+' нет дисков — бери с другого.'; }
    else if(х.выбран===k){ х.выбран=null; }
    else {
      const из=х.с[х.выбран], d=из[из.length-1], в=х.с[k], верх=в[в.length-1];
      if(верх!=null && верх<d){ х.ошибка='Большой диск нельзя класть на маленький. Выбери другой стержень.'; }
      else { из.pop(); в.push(d); х.ход++; х.выбран=null; s.упал={f:f,k:k};
        if(f===5 && в.length===3 && k===2 && х.ход===7) s.дело_башня=true; }
    }
    s['х'+f]=х; chRender(0); };
  window.r541Башня=(f)=>{ const s=S(); s['х'+f]=нов(ДИСКОВ[f]); chRender(0); };
  window.r541Открыть=()=>{ const s=S(); s.откр1=Math.min(5,(s.откр1||0)+1); s.открыл=true; chRender(0); };
  window.r541Отв=(f,к)=>{ const s=S(); s['ответ'+f]=к;
    if(f===1&&к===0) s.дело_сундук=true; if(f===7&&к===0) s.дело_стопка=true; chRender(0); };
  window.r541Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r541Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r541T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r541TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L541; else arr.push(L541); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.IN541={render:render, L:L541};
})();
