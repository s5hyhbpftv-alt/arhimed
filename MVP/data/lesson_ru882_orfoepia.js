/* ============ РУССКИЙ ЯЗЫК · УРОК 882 · «ОРФОЭПИЯ: КАК ГОВОРИТЬ ПРАВИЛЬНО» ============
   5 класс, ФРП, II четверть, раздел «Фонетика. Орфоэпия».

   СВЕРКА С УЧЕБНИКОМ (Ладыженская, 5 класс, рабочая программа):
     • «Орфоэпия. Произносительные нормы литературного языка.
        Орфоэпические словари»;
     • «Умение соблюдать основные правила литературного произношения»;
     • «произносить гласные и согласные перед гласным е»;
     • «Умение находить справки о произношении слов в различных словарях
        (в том числе орфоэпических)»;
     • «Соблюдение правильного ударения в глаголах, при произношении которых
        допускаются ошибки (начать, понять; начал, понял; начала, поняла;
        повторит, облегчит и др.)»; «Умение правильно ставить ударение
        в краткой форме прилагательных (труден, трудна, трудно)».

   СЮЖЕТ: радиостудия «Говорим правильно». Ребёнок — помощник диктора:
   проверяет тексты, ставит ударения, готовит выпуск.

   ЧТО СДЕЛАНО ДЛЯ ПОНЯТНОСТИ (просьба владельца «ещё более понятнее
   и осмысленно»):
     • на каждом кадре есть правило ОДНОЙ СТРОКОЙ в отдельной рамке;
     • рядом всегда стоят «верно» и «неверно» — их легко сравнить;
     • у каждой ошибки объяснено, ПОЧЕМУ так говорят неправильно;
     • есть приём «скажи вслух три раза» — проговаривание вслух;
     • сквозной сюжет: от знакомства со студией до готового выпуска.

   СТАНДАРТ: заголовок 24, текст кадра 20, подпись 16, служебное 14; в SVG
   кегли только 12/14/16/20/24/32; цели касания от 48 px; движение только
   вертикальное и прозрачность; есть prefers-reduced-motion. */
(function(){
  'use strict';

  const ID = 882;

  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';

  const CSS=`
  #lvis .s6.l882{gap:14px}
  #lvis .s6.l882 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l882 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l882 .rule{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.4;color:var(--ink)}
  #lvis .s6.l882 .rule b{color:${GOLD}}
  #lvis .s6.l882 .sheet{width:100%;padding:16px 14px;border-radius:16px;background:linear-gradient(180deg,#22362c,#17261e);
    border:1.5px solid var(--line);display:flex;flex-direction:column;gap:8px}
  #lvis .s6.l882 .sheet p{margin:0;font-size:20px;line-height:1.5}
  #lvis .s6.l882 .pair{display:flex;gap:10px;width:100%;flex-wrap:wrap}
  #lvis .s6.l882 .pair .half{flex:1 1 44%;min-width:140px;padding:14px 12px;border-radius:14px;display:flex;
    flex-direction:column;gap:6px;align-items:center;background:linear-gradient(180deg,#22362c,#17261e)}
  #lvis .s6.l882 .pair .half.да{border:1.6px solid ${GREEN}}
  #lvis .s6.l882 .pair .half.нет{border:1.6px solid ${RED}}
  #lvis .s6.l882 .pair .half b{font-size:24px;line-height:1.2}
  #lvis .s6.l882 .pair .half.да b{color:${GREEN}}
  #lvis .s6.l882 .pair .half.нет b{color:${RED};text-decoration:line-through}
  #lvis .s6.l882 .pair .half i{font-style:normal;font-size:14px;color:var(--mut);text-align:center;line-height:1.35}
  #lvis .s6.l882 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l882 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left}
  #lvis .s6.l882 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l882 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l882 button[disabled]{opacity:.45}
  #lvis .s6.l882 .verdict{font-size:16px;line-height:1.45}
  #lvis .s6.l882 .verdict.ok{color:var(--ok)}
  #lvis .s6.l882 .verdict.no{color:var(--no)}
  #lvis .s6.l882 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l882 .steps{display:flex;flex-direction:column;gap:8px;width:100%}
  #lvis .s6.l882 .steps .st{display:flex;gap:12px;align-items:center;padding:12px 14px;border-radius:14px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line);min-height:56px;font-size:20px;color:var(--mut)}
  #lvis .s6.l882 .steps .st.on{border-color:var(--gold);color:var(--ink)}
  #lvis .s6.l882 .steps .st .n{flex:none;width:34px;height:34px;border-radius:10px;display:flex;align-items:center;
    justify-content:center;font-size:16px;font-weight:700;color:#17261e;background:linear-gradient(180deg,#ffd76a,#d9a441)}
  /* карточки задания: вопрос и разбор — отдельными карточками, вопрос всегда первый */
  #lvis .s6.l882 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l882 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l882 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l882 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l882 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l882 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l882 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l882 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l882 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l882 .уровни .точка.сейчас{background:${GOLD}}
  #lvis .s6.l882 [data-anim]{animation:l882rise .42s cubic-bezier(.23,1,.32,1) both;animation-delay:calc(var(--i,0)*70ms)}
  #lvis .s6.l882[data-frame="3"] [data-anim],#lvis .s6.l882[data-frame="8"] [data-anim]{animation-name:l882pop}
  #lvis .s6.l882[data-frame="12"] [data-anim]{animation-name:l882pop}
  @keyframes l882rise{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:none}}
  @keyframes l882pop{0%{opacity:0;transform:scale(.95)}70%{transform:scale(1.03)}100%{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l882 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){ #lvis .s6.l882 [data-anim]{animation:none!important} }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l882-style');
      if(!s){ s=document.createElement('style'); s.id='l882-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  /* правило одной строкой — на каждом кадре */
  const ПРАВИЛО = (текст) => `<div class="rule">${текст}</div>`;

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c882-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22362c"/><stop offset="1" stop-color="#152219"/>
      </linearGradient>
      <linearGradient id="c882-плита" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2c4536"/><stop offset="1" stop-color="#1b2f22"/>
      </linearGradient>
      <radialGradient id="c882-микро" cx="0.4" cy="0.3" r="0.8">
        <stop offset="0" stop-color="#e8eef4"/><stop offset="0.6" stop-color="#9fb2c4"/>
        <stop offset="1" stop-color="#4a5560"/>
      </radialGradient>
      <radialGradient id="c882-свет" cx="0.5" cy="0.4" r="0.6">
        <stop offset="0" stop-color="#ffe9a8" stop-opacity="0.85"/>
        <stop offset="1" stop-color="#ffe9a8" stop-opacity="0"/>
      </radialGradient>
      <filter id="c882-тень" x="-30%" y="-30%" width="160%" height="160%">
        <feGaussianBlur stdDeviation="2.4"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||176}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* микрофон */
  const микрофон = (x,y,м) => {
    const к=м||1;
    return `<g transform="translate(${x} ${y}) scale(${к})">
      <rect x="-11" y="-30" width="22" height="38" rx="11" fill="url(#c882-микро)" stroke="#3a4750" stroke-width="1.4"/>
      ${[-22,-14,-6].map(дy=>`<line x1="-7" y1="${дy}" x2="7" y2="${дy}" stroke="#5a6b7a" stroke-width="1.1" opacity=".7"/>`).join('')}
      <path d="M-16 -6 q16 14 32 0" fill="none" stroke="#5a6b7a" stroke-width="2"/>
      <path d="M0 6 L0 18" stroke="#5a6b7a" stroke-width="2.4"/>
      <path d="M-12 26 L0 18 L12 26" fill="none" stroke="#5a6b7a" stroke-width="2.2"/>
    </g>`;
  };

  /* слово с ударным слогом */
  const слогиСлово = (x,y,слоги,ударный) => {
    const общаяШ=слоги.reduce((а,с)=>а+с.length*11+14,0);
    let cx=x-общаяШ/2+8;
    return слоги.map((с,i)=>{
      const ш=с.length*11+14;
      const акц=i===ударный;
      const г=`<g>
        <rect x="${cx}" y="${y-22}" width="${ш}" height="40" rx="9" fill="url(#c882-плита)"
          stroke="${акц?GOLD:ЛИНИЯ}" stroke-width="${акц?2.4:1.5}"/>
        ${т(cx+ш/2,y+6,с,20,акц?GOLD:ИНК,акц)}
        ${акц?`<path d="M${cx+ш/2-7} ${y-30} L${cx+ш/2+7} ${y-30}" stroke="${GOLD}" stroke-width="3" stroke-linecap="round"/>`:''}
      </g>`;
      cx += ш+8;
      return г;
    }).join('');
  };

  /* пара «верно / неверно» в SVG */
  const пара = (x,y,верно,неверно) => `
    <g>
      <rect x="${x}" y="${y}" width="140" height="40" rx="10" fill="rgba(143,209,168,.12)" stroke="${GREEN}" stroke-width="2"/>
      ${т(x+70,y+26,верно,20,GREEN,true)}
      ${т(x+70,y+58,'так говорят',12,МУТ)}
      <rect x="${x+166}" y="${y}" width="140" height="40" rx="10" fill="rgba(232,106,90,.12)" stroke="${RED}" stroke-width="2"/>
      ${т(x+236,y+26,неверно,20,RED,true)}
      ${т(x+236,y+58,'так не говорят',12,МУТ)}
    </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Сюжет: студия «Говорим правильно» */
  function F1(s){
    const в = s.студия;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c882-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${микрофон(96,96,1.5)}
      <circle cx="96" cy="70" r="46" fill="url(#c882-свет)"/>
      <rect x="176" y="40" width="136" height="96" rx="10" fill="url(#c882-плита)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(244,66,'Табло студии',12,МУТ)}
      ${т(244,96,'звонИт',16,GOLD,true)}
      ${т(244,120,'каталОг',16,GOLD,true)}
      ${т(168,164,'диктор говорит по норме, а не как придётся',12,ИНК)}
      ${т(168,24,'Радиостудия «Говорим правильно»',14,GOLD,true)}
    `,176);
    return `<div class="pic">${рис}</div>
      ${ПРАВИЛО('<b>Орфоэпия</b> — наука о правильном произношении. Норма — одна, и её проверяют по словарю.')}
      <div class="ask">
        ${BTN(1, в==='норма'?'hit':(в?'miss':''), 'Норму произношения проверяют по словарю', "r882Studio('норма')")}
        ${BTN(2, в==='какговорят'?'miss':'', 'Как говорят вокруг — так и правильно', "r882Studio('какговорят')")}
      </div>
      ${в!=null?A(3,'verdict '+(в==='норма'?'ok':'no'),
        в==='норма' ? '✅ Верно: у слова одна литературная норма, и она записана в орфоэпическом словаре.'
                   : '❌ Нет: вокруг говорят по-разному, и часто с ошибкой. Норма живёт в словаре.') : ''}`;
  }

  /* 2. Как найти ударение — приём «позови слово» */
  function F2(s){
    const в = s.позови;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c882-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Позови слово вслух',14,GOLD,true)}
      ${слогиСлово(168,86,['звон','о','к'],в==='второй'?1:(в==='первый'?0:1))}
      ${т(168,132,'звонОк — сила голоса на «о»',12,ИНК)}
      ${т(168,154,'так же проверяем: звонИт, каталОг, щавЕль',12,МУТ)}
    `,176);
    const ответ = в==null?'':(в==='второй'
      ? A(3,'verdict ok','✅ Верно: ударение на «о» — звонОк. Приём простой: позови слово, и сильный слог услышишь сам.')
      : A(3,'verdict no','❌ Нет: позови слово вслух — сильнее звучит «о» во втором слоге.'));
    return `<div class="pic">${рис}</div>
      ${ПРАВИЛО('Приём первый: <b>позови слово</b>, будто зовёшь его издалека. Ударный слог прозвучит громче.')}
      <div class="ask">
        ${BTN(1, в==='второй'?'hit':(в?'miss':''), 'Ударение на второй слог: звонОк', "r882Call('второй')")}
        ${BTN(2, в==='первый'?'miss':'', 'Ударение на первый слог: звОнок', "r882Call('первый')")}
      </div>
      ${ответ}`;
  }

  /* 3. Почему ошибки появляются */
  function F3(s){
    const в = s.причина;
    return `${A(0,'cap','Ошибки в ударении появляются по двум причинам. Первая — мы слышим слово от других и повторяем. Вторая — в русском языке ударение свободное: оно может стоять на любом слоге, и его нельзя вычислить по правилу.')}
      ${A(1,'pair','<div class="half да"><b>звонИт</b><i>норма: ударение на И</i></div>'+
                    '<div class="half нет"><b>звОнит</b><i>бытовая ошибка: слышали так</i></div>')}
      ${A(2,'pair','<div class="half да"><b>каталОг</b><i>норма: ударение на О</i></div>'+
                    '<div class="half нет"><b>катАлог</b><i>бытовая ошибка</i></div>')}
      ${ПРАВИЛО('Значит, каждое спорное слово надо <b>проверять</b>, а не вспоминать «на слух».')}
      <div class="ask">
        ${BTN(3, в==='проверять'?'hit':(в?'miss':''), 'Спорные слова проверяют по словарю', "r882Cause('проверять')")}
        ${BTN(4, в==='слух'?'miss':'', 'Достаточно вспомнить, как говорят знакомые', "r882Cause('слух')")}
      </div>
      ${в!=null?A(5,'verdict '+(в==='проверять'?'ok':'no'),
        в==='проверять' ? '✅ Верно: слух обманывает, а словарь — нет.'
                       : '❌ Нет: знакомые тоже ошибаются. Проверяем по словарю.') : ''}`;
  }

  /* 4. Ударение меняет смысл */
  function F4(s){
    const в = s.смысл;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c882-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Ударение меняет смысл',14,GOLD,true)}
      ${слогиСлово(96,74,['мУ','ка'],0)}
      ${т(96,112,'мука для теста',12,ИНК)}
      ${слогиСлово(250,74,['му','кА'],1)}
      ${т(250,112,'мука — страдание',12,ИНК)}
      ${слогиСлово(96,146,['Ат','лас'],0)}
      ${т(96,170,'атлас — сборник карт',12,МУТ)}
      ${слогиСлово(250,146,['ат','лАс'],1)}
      ${т(250,170,'атлас — гладкая ткань',12,МУТ)}
    `,180);
    const ответ = в==null?'':(в==='ткань'
      ? A(2,'verdict ok','✅ Верно: атлАс — это ткань, Атлас — сборник карт. Ударение меняет значение.')
      : A(2,'verdict no','❌ Нет: сборник карт — это Атлас, ударение на первый слог.'));
    return `<div class="pic">${рис}</div>
      ${ПРАВИЛО('Ударение может <b>менять смысл</b> слова: зАмок — замОк, мУка — мукА, Атлас — атлАс.')}
      <div class="ask">
        ${BTN(1, в==='ткань'?'hit':(в?'miss':''), 'атлАс — это ткань', "r882Sense('ткань')")}
        ${BTN(2, в==='карты'?'miss':'', 'атлАс — это сборник карт', "r882Sense('карты')")}
      </div>
      ${ответ}`;
  }

  /* 5. Глаголы */
  const ГЛАГОЛЫ=[
    { в:'звонИт', н:'звОнит', п:'ударение на И' },
    { в:'повторИт', н:'повтОрит', п:'ударение на И' },
    { в:'началА', н:'нАчала', п:'ударение на последний слог' },
    { в:'понялА', н:'пОняла', п:'ударение на последний слог' },
    { в:'облегчИт', н:'облЕгчит', п:'ударение на И' }
  ];
  function F5(s){
    const и=(s.глагол||0)%ГЛАГОЛЫ.length, з=ГЛАГОЛЫ[и];
    const отв=s.глОтв, готово=отв!=null, верно=отв===0;
    return `${A(0,'cap','Эти глаголы произносят неправильно чаще всего. Сравни две записи и выбери норму.')}
      ${A(1,'pair','<div class="half да"><b>'+з.в+'</b><i>'+з.п+'</i></div>'+
                    '<div class="half нет"><b>'+з.н+'</b><i>так говорить неверно</i></div>')}
      ${A(2,'rule', 'Скажи вслух три раза: <b>'+з.в+'</b>, '+з.в+', '+з.в+'.')}
      <div class="ask">
        ${BTN(3, готово?(верно?'hit':'miss'):'', 'Правильно: '+з.в, 'r882Verb(0)')}
        ${BTN(4, готово?(!верно?'hit':'miss'):'', 'Правильно: '+з.н, 'r882Verb(1)')}
      </div>
      ${готово?A(5,'verdict '+(верно?'ok':'no'),(верно?'✅ Верно: ':'❌ Не так: ')+'норма — '+з.в+'.') : A(5,'verdict','Выбери норму.')}
      ${готово?`<div class="ask">${BTN(6,'','следующий глагол','r882VerbNext()')}</div>`:''}`;
  }

  /* 6. Существительные */
  const СУЩ=[
    { в:'тОрты', н:'тортЫ' },
    { в:'каталОг', н:'катАлог' },
    { в:'договОр', н:'дОговор' },
    { в:'щавЕль', н:'щАвель' },
    { в:'свЁкла', н:'свеклА' },
    { в:'портфЕль', н:'пОртфель' }
  ];
  function F6(s){
    const и=(s.сущ||0)%СУЩ.length, з=СУЩ[и];
    const отв=s.сущОтв, готово=отв!=null, верно=отв===0;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="150" fill="url(#c882-стена)"/>
      <rect x="0" y="0" width="336" height="150" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Словарик диктора: существительные',14,GOLD,true)}
      ${СУЩ.map((х,i)=>{
        const кол=i%3, ряд=Math.floor(i/3);
        const x=62+кол*106, y=48+ряд*46;
        const актив=i===и;
        return `<g>
          <rect x="${x-50}" y="${y}" width="100" height="34" rx="9"
            fill="${актив?'rgba(255,215,106,.16)':'rgba(255,255,255,.04)'}"
            stroke="${актив?GOLD:ЛИНИЯ}" stroke-width="${актив?2:1.4}"/>
          ${т(x,y+23,х.в,16,актив?GOLD:ИНК,актив)}
        </g>`;
      }).join('')}
      ${т(168,142,'все шесть слов — с ударением по норме',12,МУТ)}
    `,150);
    return `<div class="pic">${рис}</div>
      ${ПРАВИЛО('Запомни: <b>тОрты, каталОг, договОр, щавЕль, свЁкла, портфЕль</b> — ударение как в словарике.')}
      <div class="ask">
        ${BTN(1, готово?(верно?'hit':'miss'):'', 'Правильно: '+з.в, 'r882Noun(0)')}
        ${BTN(2, готово?(!верно?'hit':'miss'):'', 'Правильно: '+з.н, 'r882Noun(1)')}
      </div>
      ${готово?A(3,'verdict '+(верно?'ok':'no'),(верно?'✅ Верно: ':'❌ Не так: ')+'норма — '+з.в+'.') : A(3,'verdict','Выбери норму.')}
      ${готово?`<div class="ask">${BTN(4,'','следующее слово','r882NounNext()')}</div>`:''}`;
  }

  /* 7. Прилагательные и краткие формы */
  const ПРИЛ=[
    { в:'красИвее', н:'красивЕе', п:'ударение на И' },
    { в:'труднА', н:'трУдна', п:'в краткой форме ударение на конце' },
    { в:'свЕтла', н:'светлА', п:'здесь ударение на первый слог' },
    { в:'удОбнее', н:'удобнЕе', п:'ударение на О' }
  ];
  function F7(s){
    const и=(s.прил||0)%ПРИЛ.length, з=ПРИЛ[и];
    const отв=s.прОтв, готово=отв!=null, верно=отв===0;
    return `${A(0,'cap','Краткая форма прилагательного часто переносит ударение: трУден — но труднА, труднО, труднЫ.')}
      ${A(1,'pair','<div class="half да"><b>'+з.в+'</b><i>'+з.п+'</i></div>'+
                    '<div class="half нет"><b>'+з.н+'</b><i>так говорить неверно</i></div>')}
      ${ПРАВИЛО('Проверяй краткие формы: <b>труден — трудна — трудно</b>. Ударение уходит на конец.')}
      <div class="ask">
        ${BTN(2, готово?(верно?'hit':'miss'):'', 'Правильно: '+з.в, 'r882Adj(0)')}
        ${BTN(3, готово?(!верно?'hit':'miss'):'', 'Правильно: '+з.н, 'r882Adj(1)')}
      </div>
      ${готово?A(4,'verdict '+(верно?'ok':'no'),(верно?'✅ Верно: ':'❌ Не так: ')+'норма — '+з.в+'.') : A(4,'verdict','Выбери норму.')}
      ${готово?`<div class="ask">${BTN(5,'','следующее слово','r882AdjNext()')}</div>`:''}`;
  }

  /* 8. Особые слова: что, конечно, скучно */
  const ОСОБЫЕ=[
    { с:'что', произн:'[што]', почем:'пишем «что», а произносим [што]' },
    { с:'конечно', произн:'[канешна]', почем:'в слове «конечно» звучит [ш]' },
    { с:'скучно', произн:'[скушна]', почем:'«чн» произносится как [шн]' },
    { с:'яичница', произн:'[йиишница]', почем:'тоже [шн] внутри слова' }
  ];
  function F8(s){
    const и=(s.особое||0)%ОСОБЫЕ.length, з=ОСОБЫЕ[и];
    const отв=s.осОтв, готово=отв!=null;
    const верно = готово && (отв===0);
    return `${A(0,'cap','Есть слова, которые пишутся не так, как произносятся. Это не ошибка, а норма.')}
      ${A(1,'sheet','<p>Пишем: <b style="color:'+GOLD+'">'+з.с+'</b></p>')}
      <div class="ask">
        ${BTN(2, готово?((отв===0)?'hit':'miss'):'', 'Произносим '+з.произн, 'r882Spec(0)')}
        ${BTN(3, готово?((отв===1)?'hit':'miss'):'', 'Произносим так же, как пишем', 'r882Spec(1)')}
      </div>
      ${готово ? A(4,'verdict ok','✅ Верно: '+з.почем+'. Такова норма литературного произношения.')
               : A(4,'verdict','Выбери ответ.')}
      ${готово?`<div class="ask">${BTN(5,'','другое слово','r882SpecNext()')}</div>`:''}`;
  }

  /* 9. Согласные перед е */
  function F9(s){
    const в = s.передЕ;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="164" fill="url(#c882-стена)"/>
      <rect x="0" y="0" width="336" height="164" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Согласный перед буквой е',14,GOLD,true)}
      ${пара(12,50,'кофе','кофэ')}
      ${пара(12,116,'академия','акадэмия')}
      ${т(168,158,'в заимствованных словах согласный бывает твёрдым',12,МУТ)}
    `,164);
    const ответ = в==null?'':(в==='мягко'
      ? A(3,'verdict ok','✅ Верно: в слове «академия» согласный [д\u2019] мягкий — такова норма.')
      : A(3,'verdict no','❌ Нет: «академия» произносят с мягким [д\u2019]. А вот «кофе» — с твёрдым [ф].'));
    return `<div class="pic">${рис}</div>
      ${ПРАВИЛО('Перед <b>е</b> согласный бывает мягким и твёрдым. Это тоже норма, и её проверяют по словарю.')}
      <div class="ask">
        ${BTN(1, в==='мягко'?'hit':(в?'miss':''), 'В слове «академия» согласный мягкий', "r882E('мягко')")}
        ${BTN(2, в==='твёрдо'?'miss':'', 'В слове «академия» согласный твёрдый', "r882E('твёрдо')")}
      </div>
      ${ответ}`;
  }

  /* 10. Три шага проверки */
  const ШАГИ10=[
    ['Услышал слово','заметь, где ты слышал его: у диктора, у учителя, в записи'],
    ['Проверил по словарю','орфоэпический словарь показывает ударение и произношение'],
    ['Записал в свой словарик','спорные слова выписывают и повторяют вслух']
  ];
  function F10(s){
    const открыто = s.шаги||0;
    const готово = открыто===ШАГИ10.length;
    return `${A(0,'cap','Вот порядок, по которому диктор проверяет любое спорное слово.')}
      <div class="steps">${ШАГИ10.map(([к,ч],и)=>
        `<div class="st${и<открыто?' on':''}"><span class="n">${и+1}</span><span>${и<открыто?к+' — '+ч:'шаг не открыт'}</span></div>`).join('')}</div>
      <div class="ask">${BTN(1,'', готово?'пройти заново':'следующий шаг', 'r882Steps()')}</div>
      ${готово?A(2,'verdict ok','✅ Три шага: услышал — проверил — записал. Так рождается привычка говорить правильно.'):''}`;
  }

  /* 11. Свой словарик: распредели слова */
  const СЛОВА11=[
    { с:'звонИт', г:'глагол' },
    { с:'каталОг', г:'существительное' },
    { с:'красИвее', г:'прилагательное' },
    { с:'повторИт', г:'глагол' },
    { с:'договОр', г:'существительное' },
    { с:'труднА', г:'прилагательное' }
  ];
  function F11(s){
    const отв = s.словарик || {};
    const верных = СЛОВА11.filter((х,и)=>отв[и]===х.г).length;
    return `${A(0,'cap','Разложи слова по полкам словарика: глагол, существительное, прилагательное.')}
      <div class="sheet">${СЛОВА11.map((х,и)=>
        `<p>${х.с} — <b style="color:${отв[и]===х.г?GREEN:(отв[и]?RED:GOLD)}">${отв[и]||'выбери часть речи'}</b></p>`).join('')}</div>
      <div class="ask">${СЛОВА11.map((х,и)=>BTN(1+и,'','куда: '+х.с,`r882Word(${и})`)).join('')}</div>
      <p class="score">разложено верно: ${верных} из ${СЛОВА11.length}</p>
      ${верных===СЛОВА11.length?A(8,'verdict ok','✅ Словарик собран: у каждого слова отмечена часть речи и ударение.'):''}`;
  }

  /* 12. Тренажёр */
  const ТРЕНАЖЁР=[
    { ф:'Как проверить ударение в спорном слове?', о:['по орфоэпическому словарю','как говорят знакомые','по числу слогов'], в:0,
      р:'норма записана в орфоэпическом словаре.' },
    { ф:'Как правильно?', о:['звонИт','звОнит','оба варианта верны'], в:0,
      р:'литературная норма — звонИт.' },
    { ф:'«атлАс» — это…', о:['ткань','сборник карт','оба значения'], в:0,
      р:'Атлас — сборник карт, атлАс — ткань.' },
    { ф:'Как произносят слово «конечно»?', о:['[канешна]','[канечно]','[конешно]'], в:0,
      р:'сочетание «чн» здесь произносится как [шн].' }
  ];
  function F12(s){
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length, з=ТРЕНАЖЁР[и], отв=s.тОтвет, готово=отв!=null, верно=отв===з.в;
    const точки = Array.from({length:ТРЕНАЖЁР.length},(_,к)=>
      `<span class="точка ${к<и?'пройдено':(к===и?'сейчас':'')}"></span>`).join('');
    return `${A(0,'уровни',точки)}
      ${A(1,'cap','Уровень '+(и+1)+' из '+ТРЕНАЖЁР.length)}
      ${A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+з.ф+'</div>')}
      <div class="ask">${з.о.map((о,к)=>BTN(3+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r882Train(${к})`)).join('')}</div>
      ${готово
        ? A(8,'карт '+(верно?'верно':'ошибка'),
            '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+
            (верно?'✅ ':'❌ ')+з.р+'</div>')
        : A(8,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше.</div>')}
      <p class="score">верно: ${s.тВерно||0} · ошибок: ${s.тОшибки||0} · всего: ${ТРЕНАЖЁР.length}</p>
      ${готово ? `<div class="ask">${BTN(9,'','следующий уровень','r882NextQ()')}</div>` : ''}`;
  }

  const ВОПРОСЫ={
    1:['Что такое орфоэпия?',[
      {к:'a', т:'наука о правильном произношении', ок:1, fb:'верно: орфоэпия учит говорить по норме'},
      {к:'b', т:'наука о правильном написании', ок:0, fb:'написание изучает орфография'}]],

    2:['Как найти ударный слог?',[
      {к:'a', т:'позвать слово', ок:1, fb:'верно: звонОк'},
      {к:'b', т:'посчитать буквы', ок:0, fb:'счётом букв ударение не найти'}]],

    3:['Почему в ударении так часто ошибаются?',[
      {к:'a', т:'ударение свободное, и его повторяют на слух', ок:1, fb:'верно: правило не подскажет, нужен словарь'},
      {к:'b', т:'в русском языке нет ударения', ок:0, fb:'ударение есть, оно просто свободное'}]],

    4:['Может ли ударение изменить смысл?',[
      {к:'a', т:'да: зАмок и замОк', ок:1, fb:'верно'},
      {к:'b', т:'нет', ок:0, fb:'мУка и мукА — разные слова'}]],

    5:['Как правильно?',[
      {к:'a', т:'повторИт', ок:1, fb:'верно: ударение на И'},
      {к:'b', т:'повтОрит', ок:0, fb:'норма — повторИт'}]],

    6:['Как правильно?',[
      {к:'a', т:'тОрты', ок:1, fb:'верно'},
      {к:'b', т:'тортЫ', ок:0, fb:'норма — тОрты'}]],

    7:['Какая форма верна?',[
      {к:'a', т:'трудна', ок:1, fb:'верно: в краткой форме ударение на конце'},
      {к:'b', т:'трУдна', ок:0, fb:'трУден, но труднА'}]],

    8:['Как произносят слово «скучно»?',[
      {к:'a', т:'[скушна]', ок:1, fb:'верно: «чн» здесь читается как [шн]'},
      {к:'b', т:'[скучно]', ок:0, fb:'норма — [скушна]'}]],

    9:['Как звучит согласный перед е в слове «кофе»?',[
      {к:'a', т:'твёрдо: [кофэ]', ок:1, fb:'верно'},
      {к:'b', т:'мягко: [коф\u2019е]', ок:0, fb:'в слове «кофе» согласный твёрдый'}]],

    10:['С чего начинают проверку слова?',[
      {к:'a', т:'замечают, где услышали слово', ок:1, fb:'верно: услышал — проверил — записал'},
      {к:'b', т:'сразу пишут в тетрадь', ок:0, fb:'сначала проверяют по словарю'}]],

    11:['Куда записывают спорные слова?',[
      {к:'a', т:'в свой словарик и повторяют вслух', ок:1, fb:'верно'},
      {к:'b', т:'никуда, они запомнятся сами', ок:0, fb:'сами не запомнятся — нужна запись'}]],

  };

  /* Карточка вопроса: показывается первой, до рисунка и вариантов.
     Варианты ответа у каждого кадра свои — здесь только вопрос. */
  function pred(f, st){
    const в = ВОПРОСЫ[f]; if(!в) return '';
    return A(0,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+в[0]+'</div>');
  }

  const L882={
    id: ID, title:'Орфоэпия: как говорить правильно', ico:'🎙',
    src:'Русский язык · 5 класс · Фонетика и графика', subj:'rus',
    explain: [
      'Представь радиостудию «Говорим правильно». Диктор читает текст по норме — так, как принято в литературном языке. Наука об этом называется орфоэпия: она учит не писать, а произносить. Норма у слова одна, но в быту её часто нарушают, поэтому норму проверяют по орфоэпическому словарю. В словаре рядом со словом стоит знак ударения и, если нужно, подсказка о произношении звуков.',

      'Первый приём — позвать слово. Скажи «звонок» так, будто зовёшь его издалека: звонОк! Сильный слог слышно сразу. Этот приём работает всегда: он опирается на то, как устроено ударение — один слог произносится громче и длиннее остальных. Частая ошибка — искать ударение «по правилу»: правила нет, ударение в русском языке свободное. Как проверить себя: позови слово вслух, а потом сверь с ответом в словаре.',

      'Откуда берутся ошибки в ударении? По двум причинам. Первая: мы слышим слово от других и повторяем услышанное, а вокруг говорят по-разному. Вторая: ударение в русском языке свободное — оно может стоять на любом слоге и даже переходить при изменении слова, поэтому «вычислить» его нельзя. Сравни: правильно звонИт — неправильно звОнит; правильно каталОг — неправильно катАлог. Частая ошибка — доверять слуху. Как проверить себя: если сомневаешься, открой словарь, а не спрашивай знакомых.',

      'Ударение может менять смысл слова. «зАмок» — старинная крепость, «замОк» — запор на двери. «мУка» — то, из чего пекут хлеб, «мукА» — страдание. «Атлас» — сборник географических карт, «атлАс» — гладкая блестящая ткань. Частая ошибка — считать ударение мелочью: иногда от него зависит, какое слово перед нами. Как проверить себя: поставь ударение по-разному и посмотри, получаются ли разные значения.',

      'Отдельно запомним глаголы: в них ошибаются чаще всего. Норма: звонИт и позвонИшь, повторИт, началА, понялА, облегчИт. Обрати внимание: в форме прошедшего времени женского рода ударение часто уходит на конец слова — началА, понялА, взялА. Частая ошибка — говорить звОнит, повтОрит, нАчала. Приём для запоминания: скажи правильный вариант вслух три раза подряд. Как проверить себя: открой словарь и найди форму, которую собираешься сказать.',

      'Существительные, которые стоит запомнить сразу: тОрты, каталОг, договОр, щавЕль, свЁкла, портфЕль. Заметь: во множественном числе «тОрты» ударение остаётся на первом слоге, а не переходит на конец. Частая ошибка — говорить тортЫ, катАлог, дОговор. Как проверить себя: выпиши эти шесть слов в свой словарик и проговаривай их вслух по одному.',

      'У прилагательных тоже есть ловушки. Сравнительная степень: красИвее (не красивЕе), удобнее с ударением на О — удОбнее. Краткие формы переносят ударение: трУден, но труднА, труднО, труднЫ; свЕтлый, но светлА. Частая ошибка — сохранять ударение полной формы в краткой. Как проверить себя: произнеси обе формы рядом — трУден и труднА — и услышь разницу.',

      'Есть слова, которые пишутся не так, как произносятся, и это норма, а не ошибка. «что» произносим [што], «конечно» — [канешна], «скучно» — [скушна], «яичница» — [йиишница]. Сочетание «чн» в этих словах читается как [шн]. Частая ошибка — читать так, как написано: [конечно] вместо [канешна]. Как проверить себя: посмотри в орфоэпическом словаре, там произношение указано в квадратных скобках.',

      'Согласный перед буквой е бывает мягким и твёрдым — и это тоже норма, которую надо знать. В слове «кофе» согласный твёрдый: [кофэ]. В слове «академия» согласный мягкий: [акадэмия] — нет, мягкий: [акад\u2019емия]. Проверить просто: в заимствованных словах согласный часто остаётся твёрдым, но не всегда. Частая ошибка — произносить все заимствованные слова одинаково. Как проверить себя: словарь помечает твёрдое произношение значком.',

      'Чтобы говорить правильно, диктор пользуется тремя шагами. Первый: заметить, где ты услышал слово, — у диктора, у учителя, в записи. Второй: проверить по орфоэпическому словарю. Третий: записать спорное слово в свой словарик и проговаривать его вслух. Частая ошибка — проверить и забыть: без записи и повторения привычка не закрепится. Как проверить себя: через день проговори свой словарик — вспомнишь ли норму.',

      'Соберём свой словарик по частям речи. Глаголы: звонИт, повторИт, началА, понялА, облегчИт. Существительные: тОрты, каталОг, договОр, щавЕль, свЁкла, портфЕль. Прилагательные: красИвее, удОбнее, труднА. Особые слова: [што], [канешна], [скушна]. Распредели слова по полкам на кадре и проверь себя. Как проверить себя: назови по три слова из каждой группы, не подглядывая.',

      'Соберём главное. Орфоэпия — наука о правильном произношении; норма одна и записана в орфоэпическом словаре. Ударение в русском языке свободное, поэтому его проверяют, а не вычисляют: приём «позови слово» подскажет ударный слог. Ударение может менять смысл. Запомни глаголы звонИт, повторИт, началА; существительные тОрты, каталОг, договОр; прилагательные красИвее, труднА; особые слова [што], [канешна], [скушна]. Проверка идёт тремя шагами: услышал — проверил — записал. Дальше тренажёр со счётом.'
    ],
    check: {
      q:'Как правильно сказать?',
      choices:['звонИт','звОнит','звОнит и звонИт одинаково верны'],
      ans:0,
      exp:'Литературная норма — звонИт: ударение на И. Так указано в орфоэпическом словаре.'
    },
    tasks: [
      { q:'Как правильно?', kind:'choice', choices:['каталОг','катАлог','кАталог'], ans:0,
        hints:['Смотри орфоэпический словарь.','Ударение падает на последний слог.'],
        sol:'каталОг — ударение на О.' },
      { q:'Как правильно?', kind:'choice', choices:['тОрты','тортЫ','тОртЫ'], ans:0,
        hints:['Во множественном числе ударение не переходит на конец.','Норма — на первый слог.'],
        sol:'тОрты — ударение на первый слог.' },
      { q:'Как произносят слово «конечно»?', kind:'choice',
        choices:['[канешна]','[канечно]','[конечно]'], ans:0,
        hints:['Сочетание «чн» здесь читается как [шн].','Это норма, а не ошибка.'],
        sol:'[канешна] — такова литературная норма.' },
      { q:'Куда записывают спорные слова?', kind:'choice',
        choices:['в свой словарик и повторяют вслух','никуда','только в тетрадь по литературе'], ans:0,
        hints:['Нужно и записать, и проговорить.','Привычка закрепляется повторением.'],
        sol:'В свой словарик — и проговаривать вслух.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L882.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else сцена=F12(s);

    const ЗАГОЛОВКИ={
      1:['Студия','Кто задаёт норму'],
      2:['Приём','Позови слово'],
      3:['Причины','Откуда берутся ошибки'],
      4:['Смысл','зАмок и замОк'],
      5:['Глаголы','Где ошибаются чаще всего'],
      6:['Существительные','Словарик диктора'],
      7:['Прилагательные','Краткая форма'],
      8:['Особые слова','[што] и [канешна]'],
      9:['Перед е','Твёрдый или мягкий'],
      10:['Проверка','Три шага'],
      11:['Словарик','Разложи по полкам'],
      12:['Тренажёр','Проверь себя']
    };
    const [кикер, заголовок]=ЗАГОЛОВКИ[f]||['Орфоэпия','Как говорить правильно'];

    el.innerHTML = `<div class="s6 l882" data-frame="${f}">
        <h2>${заголовок}</h2>
        ${pred(f, s)}
        ${сцена}
      </div>`;
  }

  window.r882Ask=(f,k)=>{ S()['в'+f]=k; chRender(0); };
  window.r882Studio=(к)=>{ S().студия=к; chRender(0); };
  window.r882Call=(к)=>{ S().позови=к; chRender(0); };
  window.r882Cause=(к)=>{ S().причина=к; chRender(0); };
  window.r882Sense=(к)=>{ S().смысл=к; chRender(0); };
  window.r882Verb=(к)=>{ const s=S(); if(s.глОтв==null) s.глОтв=к; chRender(0); };
  window.r882VerbNext=()=>{ const s=S(); s.глагол=((s.глагол||0)+1)%ГЛАГОЛЫ.length; s.глОтв=null; chRender(0); };
  window.r882Noun=(к)=>{ const s=S(); if(s.сущОтв==null) s.сущОтв=к; chRender(0); };
  window.r882NounNext=()=>{ const s=S(); s.сущ=((s.сущ||0)+1)%СУЩ.length; s.сущОтв=null; chRender(0); };
  window.r882Adj=(к)=>{ const s=S(); if(s.прОтв==null) s.прОтв=к; chRender(0); };
  window.r882AdjNext=()=>{ const s=S(); s.прил=((s.прил||0)+1)%ПРИЛ.length; s.прОтв=null; chRender(0); };
  window.r882Spec=(к)=>{ const s=S(); if(s.осОтв==null) s.осОтв=к; chRender(0); };
  window.r882SpecNext=()=>{ const s=S(); s.особое=((s.особое||0)+1)%ОСОБЫЕ.length; s.осОтв=null; chRender(0); };
  window.r882E=(к)=>{ S().передЕ=к; chRender(0); };
  window.r882Steps=()=>{ const s=S(); s.шаги=(s.шаги||0)>=ШАГИ10.length?0:(s.шаги||0)+1; chRender(0); };
  window.r882Word=(и)=>{
    const s=S(); s.словарик=s.словарик||{};
    const порядок=['глагол','существительное','прилагательное'];
    const сейчас=порядок.indexOf(s.словарик[и]);
    s.словарик[и]=порядок[(сейчас+1)%порядок.length];
    chRender(0);
  };
  window.r882Train=(к)=>{
    const s=S();
    if(s.тОтвет!=null) return;
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length;
    s.тОтвет=к;
    if(к===ТРЕНАЖЁР[и].в) s.тВерно=(s.тВерно||0)+1; else s.тОшибки=(s.тОшибки||0)+1;
    chRender(0);
  };
  window.r882NextQ=()=>{ const s=S(); s.тНомер=((s.тНомер||0)+1)%ТРЕНАЖЁР.length; s.тОтвет=null; chRender(0); };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L882; else arr.push(L882);
  })();
  window.RU882={render:render, L:L882};
})();
