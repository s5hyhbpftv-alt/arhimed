/* ====== МАТЕМАТИКА · УРОК 367 · «ЗАДАЧИ НА РАБОТУ» ==========================
   4 класс, раздел «Работа и производительность».

   ПОДХОД ЗДЕСЬ ДРУГОЙ, чем в уроках 365 и 356. Там каждый кадр был устроен
   одинаково: вопрос и две кнопки. Ребёнок к четвёртому кадру уже знает форму
   и отвечает не думая. В этом уроке на каждом шаге СВОЙ способ действия:

     ПЕСОЧНИЦА  — кнопками «−» и «+» ребёнок сам меняет часы и число мастеров
                  и сразу видит, сколько деталей выйдет (кадры 2, 10);
     СТАВКА     — сначала предсказать результат, и только потом увидеть его
                  (кадр 4); ставка запоминается и сравнивается с правдой;
     ГОНКА      — две полосы заполняют общий заказ, видно момент встречи (5);
     ОТЧЁТ      — три строки, одна с ошибкой: её надо найти нажатием (8);
     КОНСТРУКТОР— собрать верную формулу из карточек (7);
     ВЫБОР      — обычный вопрос, но только там, где он уместен.

   Сюжет: мастерская Архимеда в Сиракузах делает 120 деталей для водяного
   винта. Мастер Гелон делает 12 деталей в час, мастер Дамон — 8.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     Гелон один: 120 : 12 = 10 ч · Дамон один: 120 : 8 = 15 ч
     вместе: 12 + 8 = 20 деталей в час → 120 : 20 = 6 ч
     сверка: за 6 ч Гелон 72, Дамон 48, вместе 72 + 48 = 120 ✓
     по часам вместе: 20, 40, 60, 80, 100, 120
     втроём (ученик 4 в час): 24 в час → 120 : 24 = 5 ч
     за 3 часа вдвоём: 20 · 3 = 60 деталей — ровно половина
     обратная: 120 за 6 ч вдвоём → общая 20, значит второй 20 − 12 = 8
     ЛОВУШКИ: сложить время 10 + 15 = 25 ч и взять среднее (10+15):2 = 12,5 ч —
       оба больше, чем работа поодиночке, то есть заведомо абсурд.

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md: кривая 0.23 1 0.32 1, состояние
   всегда видимо (минимум 0.55), текст не масштабируется, кольцо только вокруг
   предмета, prefers-reduced-motion выключает SMIL полностью. Кроме того, в
   песочнице и гонке движение зависит от действия ребёнка, а не крутится само:
   это и есть «движение как объяснение». */
(function(){
  'use strict';

  const ID = 367;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a',
        SAND='#e8c48a', ОГОНЬ='#ff9d5c', МЕДЬ='#d98b4a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  /* мастера: цвет, имя, скорость */
  const ГЕЛОН = {имя:'Гелон', цвет:BLUE, скорость:12};
  const ДАМОН = {имя:'Дамон', цвет:МЕДЬ, скорость:8};
  const УЧЕНИК= {имя:'Ученик', цвет:GREEN, скорость:4};
  const ЗАКАЗ = 120;

  const CSS=`
  #lvis .s6.l367{gap:14px}
  #lvis .s6.l367 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l367 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l367 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l367 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l367 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l367 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l367 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l367 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l367 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l367 .правило b{color:${GOLD}}

  /* ── пульт песочницы: ребёнок крутит число сам ── */
  #lvis .s6.l367 .пульт{display:flex;align-items:center;gap:10px;width:100%;padding:12px;border-radius:16px;
    background:rgba(255,215,106,.08);border:1.5px solid rgba(255,215,106,.35)}
  #lvis .s6.l367 .пульт .имя{flex:1;min-width:0;font-size:16px;line-height:1.3;color:${ИНК}}
  #lvis .s6.l367 .пульт .имя b{display:block;font-size:24px;color:${GOLD};font-variant-numeric:tabular-nums}
  #lvis .s6.l367 .пульт button{width:56px;height:56px;flex:0 0 56px;border-radius:14px;cursor:pointer;
    border:1px solid rgba(255,215,106,.5);background:rgba(255,215,106,.14);color:${GOLD};
    font:inherit;font-size:24px;line-height:1;touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l367 .пульт button:active{transform:translateY(2px);background:rgba(255,215,106,.26)}
  #lvis .s6.l367 .пульт button:disabled{opacity:.35;cursor:not-allowed}

  #lvis .s6.l367 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l367 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l367 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l367 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l367 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l367 .ask button.ставка{border-color:${GOLD}}

  /* ── карточки конструктора формулы ── */
  #lvis .s6.l367 .формулы{display:flex;flex-direction:column;gap:10px;width:100%}
  #lvis .s6.l367 .формулы button{width:100%;min-height:56px;padding:14px;border-radius:14px;cursor:pointer;
    font:inherit;font-size:20px;line-height:1.3;text-align:center;color:${ИНК};
    border:1.5px solid rgba(255,255,255,.18);background:rgba(255,255,255,.05);
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l367 .формулы button:active{transform:translateY(2px)}
  #lvis .s6.l367 .формулы button.hit{border-color:var(--ok);background:rgba(143,209,168,.12)}
  #lvis .s6.l367 .формулы button.miss{border-color:var(--no);background:rgba(232,106,90,.1)}

  /* ── строки отчёта: найди ошибку ── */
  #lvis .s6.l367 .отчёт{display:flex;flex-direction:column;gap:8px;width:100%}
  #lvis .s6.l367 .отчёт button{display:flex;align-items:center;gap:12px;width:100%;min-height:56px;padding:12px 14px;
    border-radius:12px;cursor:pointer;font:inherit;font-size:16px;line-height:1.3;text-align:left;color:${ИНК};
    border:1.5px solid rgba(255,255,255,.16);background:rgba(255,255,255,.04);
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l367 .отчёт button:active{transform:translateY(2px)}
  #lvis .s6.l367 .отчёт button .ном{flex:0 0 32px;height:32px;border-radius:9px;display:flex;align-items:center;
    justify-content:center;background:rgba(255,255,255,.08);color:${МУТ};font-size:16px}
  #lvis .s6.l367 .отчёт button.hit{border-color:var(--ok);background:rgba(143,209,168,.12)}
  #lvis .s6.l367 .отчёт button.miss{border-color:var(--no);background:rgba(232,106,90,.1)}

  #lvis .s6.l367 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l367 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l367 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l367 .уровни .точка.сейчас{background:${GOLD};animation:l367dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l367dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l367 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l367{-webkit-text-size-adjust:100%}
  #lvis .s6.l367 [data-anim]{animation:l367rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l367rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l367 [data-anim]{animation:none!important}
    #lvis .s6.l367 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l367 .ask button,#lvis .s6.l367 .пульт button,
    #lvis .s6.l367 .формулы button,#lvis .s6.l367 .отчёт button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l367-style');
      if(!s){ s=document.createElement('style'); s.id='l367-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Мастерская</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  /* «24 детали», а не «24 деталей»: живой проход показал помарку в песочнице */
  const деталей = (n) => {
    const м10=Math.abs(n)%10, м100=Math.abs(n)%100;
    if(м10===1 && м100!==11) return 'деталь';
    if(м10>=2 && м10<=4 && (м100<10 || м100>=20)) return 'детали';
    return 'деталей';
  };
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* пульт: минус, значение, плюс */
  const ПУЛЬТ = (подпись,значение,меньше,больше,можноМеньше,можноБольше) =>
    A(3,'пульт',
      `<button type="button" onclick="${меньше}" ${можноМеньше?'':'disabled'} aria-label="меньше">−</button>
       <span class="имя">${подпись}<b>${значение}</b></span>
       <button type="button" onclick="${больше}" ${можноБольше?'':'disabled'} aria-label="больше">+</button>`);

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анРаз = (имя,значения,длит,keyTimes) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" fill="freeze"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}"/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c367-фон" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2f4136"/><stop offset="0.5" stop-color="#1f3228"/>
        <stop offset="1" stop-color="#121d16"/>
      </linearGradient>
      <linearGradient id="c367-горн" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#ffd76a"/><stop offset="0.5" stop-color="#ff9d5c"/>
        <stop offset="1" stop-color="#c2410c"/>
      </linearGradient>
      <linearGradient id="c367-медь" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f0c48a"/><stop offset="0.5" stop-color="#d98b4a"/>
        <stop offset="1" stop-color="#8a4f22"/>
      </linearGradient>
      <linearGradient id="c367-синь" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#bfe6ff"/><stop offset="0.5" stop-color="#7fd1ff"/>
        <stop offset="1" stop-color="#2b6d99"/>
      </linearGradient>
      <linearGradient id="c367-полоса" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#8fd1a8"/><stop offset="1" stop-color="#4f9e74"/>
      </linearGradient>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||236}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c367-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+16, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.86)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* ДЕТАЛЬ винта — шестерёнка с зубцами */
  const деталь = (x,y,r,цвет) => {
    const зубцы = Array.from({length:8},(_,i)=>{
      const a=i*Math.PI/4, s=Math.sin(a), c=Math.cos(a);
      return `<rect x="${(x+s*r-1.8).toFixed(1)}" y="${(y-c*r-1.8).toFixed(1)}" width="3.6" height="3.6" rx="1"
        fill="${цвет}" opacity=".9"/>`;
    }).join('');
    return `<g>${зубцы}
      <circle cx="${x}" cy="${y}" r="${r-1}" fill="none" stroke="${цвет}" stroke-width="2"/>
      <circle cx="${x}" cy="${y}" r="${(r/3).toFixed(1)}" fill="${цвет}" opacity=".5"/>
    </g>`;
  };

  /* МАСТЕР у наковальни: фигура, молот, искры */
  const мастер = (x,y,м,искры) => `<g>
    <circle cx="${x}" cy="${y-30}" r="9" fill="${м.цвет}" opacity=".9" stroke="${ОБВОД}" stroke-width="1"/>
    <path d="M${x-10} ${y} l4 -20 h12 l4 20 z" fill="${м.цвет}" opacity=".55" stroke="${ОБВОД}" stroke-width="1"/>
    <line x1="${x+9}" y1="${y-18}" x2="${x+22}" y2="${y-28}" stroke="${МУТ}" stroke-width="2.4"/>
    <rect x="${x+20}" y="${y-34}" width="10" height="7" rx="2" fill="${МУТ}" stroke="${ОБВОД}" stroke-width="1"/>
    <rect x="${x-16}" y="${y}" width="32" height="7" rx="2" fill="#4a5560" stroke="${ОБВОД}" stroke-width="1"/>
    ${искры?`<g>${анК('opacity','0;1;0','1.6s','0;0.3;1')}
      ${[[-6,-6],[2,-9],[8,-4]].map(d=>`<circle cx="${x+d[0]}" cy="${y+d[1]}" r="1.8" fill="${ОГОНЬ}"/>`).join('')}
    </g>`:''}
    ${т(x,y+22,м.имя,14,м.цвет)}
    ${т(x,y+40,м.скорость+' в час',14,МУТ)}
  </g>`;

  /* ПОЛОСА заказа: сколько деталей сделано */
  /* Полоса умеет ДОРАСТАТЬ от прежней доли к новой за один прогон (fill=freeze,
     не цикл): когда ребёнок жмёт «+ час», прибавку видно глазом, а не только
     в числе. Это движение-объяснение, а не украшение. */
  const полоса = (x,y,ш,в,доля,цвет,подпись_,отДоли) => {
    const к = Math.max(0,Math.min(1,доля))*ш;
    const от = (отДоли==null) ? null : Math.max(0,Math.min(1,отДоли))*ш;
    return `<g>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="${в/2}" fill="rgba(255,255,255,.07)"
      stroke="rgba(255,255,255,.18)" stroke-width="1.2"/>
    <rect x="${x}" y="${y}" width="${от!=null?от.toFixed(1):к.toFixed(1)}" height="${в}" rx="${в/2}" fill="${цвет}" opacity=".9">
      ${от!=null ? анРаз('width', от.toFixed(1)+';'+к.toFixed(1), '620ms', '0;1') : ''}
    </rect>
    ${подпись_?т(x+ш+6,y+в-4,подпись_,14,цвет,true,'start'):''}
  </g>`;};

  /* ================= КАДРЫ ================= */

  /* 1. Мастерская: заказ на 120 деталей */
  function F1(s){
    return ЗАДАЧА('В мастерской Архимеда делают водяной винт. Нужно <b>120 деталей</b>. За работу берутся два мастера.') +
      `<div class="pic">${свг(`
        ${фон(238)}
        ${т(168,26,'Мастерская Сиракуз',14,GOLD,true)}
        <g>${проявить('7s',0.1,0.2)}
          <path d="M28 196 h64 l-6 -26 h-52 z" fill="url(#c367-горн)" opacity=".55" stroke="${ОБВОД}" stroke-width="1.2"/>
          ${т(60,214,'горн',14,МУТ)}</g>
        ${мастер(150,170,ГЕЛОН,true)}
        ${мастер(260,170,ДАМОН,true)}
        <g>${проявить('7s',0.34,0.44)}
          ${[0,1,2,3,4,5].map((i)=>деталь(56+i*42,76,13,i<3?SAND:МУТ)).join('')}</g>
        <g>${проявить('7s',0.6,0.7)}${подпись(168,116,'заказ: 120 деталей',GOLD,16)}</g>
      `,238)}</div>` +
      СКАЗ('Что известно','Гелон делает <b>12 деталей в час</b>, Дамон — <b>8 деталей в час</b>. Сколько деталей за час — это и есть <b>производительность</b>.') +
      ПРАВИЛО('Производительность — <b>сколько работы за один час</b>.');
  }

  /* 2. ПЕСОЧНИЦА: сам меняешь часы и смотришь, сколько сделает Гелон */
  function F2(s){
    const ч = Math.max(1, Math.min(10, s.часыГелона||1));
    const сделано = ГЕЛОН.скорость*ч;
    const готово = сделано>=ЗАКАЗ;
    return ЗАДАЧА('Покрути часы: сколько деталей успеет Гелон? Он делает 12 деталей в час.') +
      `<div class="pic">${свг(`
        ${фон(214)}
        ${т(168,26,'Гелон работает один',14,BLUE,true)}
        ${мастер(58,110,ГЕЛОН,true)}
        <g>
          ${Array.from({length:Math.min(12,ч)},(_,i)=>
            деталь(126+(i%6)*34, 86+Math.floor(i/6)*40, 13, BLUE)).join('')}
        </g>
        ${полоса(28,170,232,16,сделано/ЗАКАЗ,'url(#c367-синь)',null)}
        ${т(28,202,'сделано '+сделано+' из 120',14,ИНК,false,'start')}
        ${готово?подпись(252,202,'весь заказ!',GREEN,14):''}
      `,214)}</div>` +
      ПУЛЬТ('часов работы', ч, "r367Часы(-1)", "r367Часы(1)", ч>1, ч<10) +
      СКАЗ('Смотри', 'За '+ч+' ч Гелон сделает <b>12 × '+ч+' = '+сделано+'</b> '+деталей(сделано)+'. Весь заказ он закроет за <b>10 часов</b>: 120 : 12 = 10.') +
      ПРАВИЛО('Работа = <b>производительность × время</b>.');
  }

  /* 3. Второй мастер: он медленнее */
  function F3(s){
    return ЗАДАЧА('А Дамон делает 8 деталей в час. Сколько часов ему нужно на те же 120 деталей?') +
      `<div class="pic">${свг(`
        ${фон(228)}
        ${т(168,26,'Один и тот же заказ',14,GOLD,true)}
        ${мастер(56,90,ГЕЛОН,false)}
        ${полоса(112,74,180,16,1,'url(#c367-синь)','10 ч')}
        ${мастер(56,180,ДАМОН,false)}
        ${полоса(112,164,180,16,1,'url(#c367-медь)','15 ч')}
        <g>${проявить('8s',0.5,0.6)}
          ${т(202,112,'120 : 12 = 10',14,BLUE)}
          ${т(202,202,'120 : 8 = 15',14,МЕДЬ)}</g>
      `,228)}</div>` +
      СКАЗ('Вывод','Гелон быстрее: ему нужно 10 часов, Дамону — 15. <b>Чем больше делаешь за час, тем меньше времени нужно.</b>') +
      ПРАВИЛО('Время = <b>работа : производительность</b>.');
  }

  /* 4. СТАВКА: угадай, сколько выйдет вместе */
  function F4(s){
    const ставка = s.ставка;
    const варианты = [
      {к:'25', т:'25 часов — сложим 10 и 15'},
      {к:'12', т:'12 с половиной — возьмём среднее'},
      {к:'6',  т:'6 часов — меньше, чем у каждого'}
    ];
    return ЗАДАЧА('Теперь они берутся за заказ <b>вместе</b>. Сделай ставку: за сколько часов управятся?') +
      `<div class="pic">${свг(`
        ${фон(200)}
        ${т(168,26,'Ставка до ответа',14,GOLD,true)}
        ${мастер(92,110,ГЕЛОН,true)}
        ${мастер(244,110,ДАМОН,true)}
        <g>${проявить('6s',0.3,0.4)}
          ${подпись(168,80,'вместе — это как?',GOLD,16)}</g>
        <g>${проявить('6s',0.6,0.7)}
          ${т(168,186,ставка?('твоя ставка: '+ставка+' ч'):'выбери вариант внизу',14,ставка?GOLD:МУТ)}</g>
      `,200)}</div>` +
      `<div class="ask">` +
      варианты.map((в,к)=>BTN(3+к, ставка===в.к?'ставка':'', в.т, "r367Ставка('"+в.к+"')")).join('') +
      `</div>` +
      (ставка ? СКАЗ('Ставка принята','Запомнили: <b>'+ставка+' ч</b>. На следующем шаге пустим мастеров работать и посмотрим, что выйдет на самом деле.')
              : СКАЗ('Подумай','Не считай пока — просто прикинь. Потом проверим.')) +
      ПРАВИЛО('Сначала прикидка, <b>потом счёт</b>. Так проверяют себя взрослые инженеры.');
  }

  /* 5. ГОНКА: полосы растут и встречаются на шести часах */
  function F5(s){
    const шаг = Math.max(0, Math.min(6, s.гонка||0));
    /* от какого часа дорастать: запоминаем предыдущее значение пульта */
    const прежний = Math.max(0, Math.min(6, s.гонкаБыло==null?шаг:s.гонкаБыло));
    const гелон = ГЕЛОН.скорость*шаг, дамон = ДАМОН.скорость*шаг, вместе = гелон+дамон;
    const ставка = s.ставка;
    return ЗАДАЧА('Пускаем обоих. Нажимай «+ час» и смотри, как наполняется заказ.') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${т(168,24,'Час за часом',14,GOLD,true)}
        ${т(28,58,'Гелон',14,BLUE,false,'start')}
        ${полоса(92,44,190,14,гелон/ЗАКАЗ,'url(#c367-синь)',null,прежний*ГЕЛОН.скорость/ЗАКАЗ)}
        ${т(288,56,String(гелон),14,BLUE,true,'start')}
        ${т(28,98,'Дамон',14,МЕДЬ,false,'start')}
        ${полоса(92,84,190,14,дамон/ЗАКАЗ,'url(#c367-медь)',null,прежний*ДАМОН.скорость/ЗАКАЗ)}
        ${т(288,96,String(дамон),14,МЕДЬ,true,'start')}
        <line x1="28" y1="118" x2="308" y2="118" stroke="rgba(255,255,255,.16)" stroke-width="1.2"/>
        ${т(28,146,'вместе',14,GREEN,false,'start')}
        ${полоса(92,130,190,20,вместе/ЗАКАЗ,'url(#c367-полоса)',null,прежний*(ГЕЛОН.скорость+ДАМОН.скорость)/ЗАКАЗ)}
        ${т(288,146,String(вместе),16,GREEN,true,'start')}
        ${т(168,182,'прошло часов: '+шаг,16,ИНК)}
        ${вместе>=ЗАКАЗ?подпись(168,214,'заказ закрыт за 6 часов',GREEN,16):т(168,212,'осталось '+(ЗАКАЗ-вместе)+' деталей',14,МУТ)}
      `,232)}</div>` +
      ПУЛЬТ('часов прошло', шаг, "r367Гонка(-1)", "r367Гонка(1)", шаг>0, шаг<6) +
      (вместе>=ЗАКАЗ
        ? РАЗБОР(ставка==='6',
            ставка==='6' ? 'Ставка сошлась: вместе они закрыли заказ за 6 часов. Гелон сделал 72 детали, Дамон — 48, вместе ровно 120.'
            : (ставка ? 'На самом деле 6 часов, а ставка была «'+ставка+'». Это не ошибка — так и проверяют догадку. Гелон сделал 72, Дамон 48, вместе 120.'
                      : 'Вышло 6 часов: Гелон сделал 72 детали, Дамон — 48, вместе ровно 120.'))
        : СКАЗ('Идёт работа','За каждый час прибавляется <b>12 + 8 = 20</b> деталей.')) +
      ПРАВИЛО('Вместе всегда <b>быстрее</b>, чем поодиночке.');
  }

  /* 6. Почему складываются производительности */
  function F6(s){
    const в = s.почему;
    return ЗАДАЧА('Почему вместе получилось 6 часов, а не 25 и не 12 с половиной?') +
      `<div class="pic">${свг(`
        ${фон(226)}
        ${т(168,26,'Складываем работу за час',14,GOLD,true)}
        <g>${проявить('8s',0.12,0.22)}
          <rect x="26" y="52" width="122" height="46" rx="10" fill="rgba(127,209,255,.14)" stroke="${BLUE}" stroke-width="1.6"/>
          ${т(87,74,'Гелон',14,BLUE)}
          ${т(87,92,'12 за час',16,ИНК,true)}</g>
        <g>${проявить('8s',0.3,0.4)}
          <rect x="188" y="52" width="122" height="46" rx="10" fill="rgba(217,139,74,.16)" stroke="${МЕДЬ}" stroke-width="1.6"/>
          ${т(249,74,'Дамон',14,МЕДЬ)}
          ${т(249,92,'8 за час',16,ИНК,true)}</g>
        ${т(168,84,'+',24,GOLD,true)}
        <g>${проявить('8s',0.52,0.62)}
          <rect x="70" y="118" width="196" height="46" rx="10" fill="rgba(143,209,168,.16)" stroke="${GREEN}" stroke-width="2"/>
          ${т(168,146,'20 деталей за час',16,GREEN,true)}</g>
        <g>${проявить('8s',0.74,0.84)}
          ${т(168,192,'120 : 20 = 6 часов',20,GOLD,true)}</g>
      `,226)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='скорости'?'hit':(в?'miss':''), 'Складывают детали за час, а не часы', "r367Почему('скорости')")}
        ${BTN(4, в==='часы'?'miss':'', 'Складывают часы каждого мастера', "r367Почему('часы')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='скорости',
        в==='скорости' ? 'Верно: за один час мастерская делает 12 + 8 = 20 деталей. Складывают именно работу за час, а время потом находят делением.'
                       : 'Нет: если сложить часы, выйдет 25 — больше, чем нужно даже одному Гелону. Складывают детали за час: 12 + 8 = 20.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Вместе: <b>производительности складываются</b>.');
  }

  /* 7. КОНСТРУКТОР: собери верную формулу */
  function F7(s){
    const в = s.формула;
    const карточки = [
      {к:'верх', т:'время = работа : (p₁ + p₂)', ок:true},
      {к:'сумма', т:'время = t₁ + t₂', ок:false},
      {к:'среднее', т:'время = (t₁ + t₂) : 2', ок:false}
    ];
    return ЗАДАЧА('Выбери карточку, на которой записан верный способ найти время совместной работы.') +
      `<div class="pic">${свг(`
        ${фон(186)}
        ${т(168,26,'Три записи — верна одна',14,GOLD,true)}
        <g>${проявить('7s',0.16,0.26)}
          ${т(168,64,'p — сколько деталей за час',14,ИНК)}
          ${т(168,88,'t — сколько часов',14,ИНК)}
          ${т(168,112,'A — вся работа',14,ИНК)}</g>
        <g>${проявить('7s',0.56,0.66)}
          ${подпись(168,156,'A = 120 · p₁ = 12 · p₂ = 8',GOLD,14)}</g>
      `,186)}</div>` +
      `<div class="формулы">` +
      карточки.map((к,i)=>
        `<button type="button" data-anim style="--i:${3+i}" class="${в===к.к?(к.ок?'hit':'miss'):''}"
           onclick="r367Формула('${к.к}')">${к.т}</button>`).join('') +
      `</div>` +
      (в!=null ? РАЗБОР(в==='верх',
        в==='верх' ? 'Верно: сначала складывают производительности, потом делят всю работу на сумму. 120 : (12 + 8) = 6 часов.'
        : (в==='сумма' ? 'Нет: 10 + 15 = 25 часов — это больше, чем у каждого по отдельности. Вместе не может быть дольше!'
                       : 'Нет: среднее (10 + 15) : 2 = 12,5 ч — тоже больше, чем 10 часов у одного Гелона. Так не бывает.')) : СКАЗ('Ответ','Нажми на карточку.')) +
      ПРАВИЛО('<b>t = A : (p₁ + p₂)</b> — работа делится на общую производительность.');
  }

  /* 8. ОТЧЁТ: найди строку с ошибкой */
  function F8(s){
    const в = s.отчёт;
    const строки = [
      {т:'За 1 час вместе — 20 деталей', ок:true},
      {т:'За 3 часа вместе — 60 деталей', ок:true},
      {т:'За 5 часов вместе — 90 деталей', ок:false}
    ];
    return ЗАДАЧА('Ученик записал отчёт мастерской. В одной строке ошибка — найди её.') +
      `<div class="pic">${свг(`
        ${фон(196)}
        ${т(168,26,'Отчёт за день',14,GOLD,true)}
        ${[1,2,3,4,5,6].map((ч,i)=>{
          const y=52+i*22, д=20*ч;
          return `<g>${проявить('9s',0.1+i*0.1,0.16+i*0.1)}
            ${т(40,y,ч+' ч',14,МУТ,false,'start')}
            ${полоса(76,y-11,150,13,д/ЗАКАЗ,'url(#c367-полоса)',null)}
            ${т(238,y,String(д),14,ч===5?GOLD:ИНК,ч===5,'start')}
          </g>`;}).join('')}
      `,196)}</div>` +
      `<div class="отчёт">` +
      строки.map((с,i)=>
        `<button type="button" data-anim style="--i:${3+i}" class="${в===i?(с.ок?'miss':'hit'):''}"
           onclick="r367Отчёт(${i})"><span class="ном">${i+1}</span>${с.т}</button>`).join('') +
      `</div>` +
      (в!=null ? РАЗБОР(!строки[в].ок,
        !строки[в].ок ? 'Верно, ошибка здесь: за 5 часов выйдет 20 × 5 = 100 деталей, а не 90. Остальные строки сходятся.'
                      : 'Эта строка верная: '+строки[в].т.toLowerCase()+' — сходится с 20 деталями в час. Ищи дальше.') : СКАЗ('Ответ','Нажми на строку, в которой видишь ошибку.')) +
      ПРАВИЛО('Проверка отчёта — это <b>умножение производительности на время</b>.');
  }

  /* 9. Часть работы: сколько сделано за 3 часа */
  function F9(s){
    const в = s.часть;
    return ЗАДАЧА('Прошло 3 часа совместной работы. Какая часть заказа готова?') +
      `<div class="pic">${свг(`
        ${фон(206)}
        ${т(168,26,'Половина пути',14,GOLD,true)}
        ${полоса(28,60,280,26,0.5,'url(#c367-полоса)',null)}
        <line x1="168" y1="52" x2="168" y2="94" stroke="${GOLD}" stroke-width="2" stroke-dasharray="4 3"/>
        ${т(96,78,'60 деталей',14,'#0d1f16',true)}
        ${т(240,78,'осталось 60',14,МУТ)}
        <g>${проявить('8s',0.4,0.5)}
          ${т(168,124,'20 × 3 = 60',20,GOLD,true)}</g>
        <g>${проявить('8s',0.64,0.74)}
          ${т(168,158,'это ровно половина заказа',14,ИНК)}
          ${т(168,182,'значит впереди ещё 3 часа',14,МУТ)}</g>
      `,206)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='60'?'hit':(в?'miss':''), '60 деталей — половина', "r367Часть('60')")}
        ${BTN(4, в==='36'?'miss':'', '36 деталей — только Гелон', "r367Часть('36')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='60',
        в==='60' ? 'Верно: вместе они делают 20 деталей в час, за 3 часа — 20 × 3 = 60. Это половина заказа.'
                 : 'Нет: 36 — это только Гелон (12 × 3). Но работают оба, значит 20 × 3 = 60.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Сделано = <b>общая производительность × время</b>.');
  }

  /* 10. ПЕСОЧНИЦА: добавляем мастеров */
  function F10(s){
    const н = Math.max(1, Math.min(3, s.мастеров||2));
    const бригада = [ГЕЛОН,ДАМОН,УЧЕНИК].slice(0,н);
    const общая = бригада.reduce((a,м)=>a+м.скорость,0);
    const часы = ЗАКАЗ/общая;
    const красиво = Number.isInteger(часы) ? часы+' ч' : (Math.floor(часы)+' ч '+Math.round((часы%1)*60)+' мин');
    return ЗАДАЧА('Добавь в бригаду ученика — он делает 4 детали в час. Что изменится?') +
      `<div class="pic">${свг(`
        ${фон(214)}
        ${т(168,24,'Бригада',14,GOLD,true)}
        ${бригада.map((м,i)=>мастер(76+i*96,108,м,true)).join('')}
        ${полоса(28,164,232,16,общая/24,'url(#c367-полоса)',null)}
        ${т(28,198,'вместе '+общая+' '+деталей(общая)+' в час',14,GREEN,false,'start')}
        ${подпись(276,198,красиво,GOLD,14)}
      `,214)}</div>` +
      ПУЛЬТ('мастеров в бригаде', н, "r367Мастеров(-1)", "r367Мастеров(1)", н>1, н<3) +
      СКАЗ('Смотри','Скорости складываются: '+бригада.map(м=>м.скорость).join(' + ')+' = <b>'+общая+'</b> '+деталей(общая)+' в час. Заказ закроют за <b>120 : '+общая+' = '+красиво+'</b>.') +
      ПРАВИЛО('Каждый новый работник <b>прибавляет свою скорость</b>.');
  }

  /* 11. Обратная задача */
  function F11(s){
    const в = s.обратная;
    return ЗАДАЧА('Известно: вдвоём заказ из 120 деталей закрыли за 6 часов, и Гелон делает 12 в час. Сколько делает второй мастер?') +
      `<div class="pic">${свг(`
        ${фон(214)}
        ${т(168,26,'Обратный ход',14,GOLD,true)}
        <g>${проявить('8s',0.12,0.22)}
          <rect x="26" y="50" width="284" height="38" rx="10" fill="rgba(255,215,106,.14)" stroke="${GOLD}" stroke-width="1.6"/>
          ${т(168,75,'120 : 6 = 20 деталей в час у двоих',16,GOLD)}</g>
        <g>${проявить('8s',0.42,0.52)}
          <rect x="40" y="108" width="112" height="40" rx="10" fill="rgba(127,209,255,.14)" stroke="${BLUE}" stroke-width="1.6"/>
          ${т(96,133,'Гелон 12',16,BLUE,true)}</g>
        <g>${проявить('8s',0.56,0.66)}
          <rect x="184" y="108" width="112" height="40" rx="10" fill="none" stroke="${МЕДЬ}" stroke-width="2" stroke-dasharray="5 4"/>
          ${т(240,133,'?',24,МЕДЬ,true)}</g>
        <g>${проявить('8s',0.76,0.86)}
          ${т(168,182,'20 − 12 = 8 деталей в час',20,GREEN,true)}</g>
      `,214)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='8'?'hit':(в?'miss':''), '8 деталей в час: 20 − 12', "r367Обратная('8')")}
        ${BTN(4, в==='20'?'miss':'', '20 деталей в час: 120 : 6', "r367Обратная('20')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='8',
        в==='8' ? 'Верно: 120 : 6 = 20 — это скорость двоих вместе. Вычитаем известную: 20 − 12 = 8 деталей в час.'
                : 'Нет: 20 — это общая скорость обоих. Скорость второго: 20 − 12 = 8.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Из работы и времени добывают <b>общую производительность</b>, потом вычитают известную.');
  }

  /* 12. Ловушки рядом */
  function F12(s){
    const в = s.ловушка;
    return ЗАДАЧА('Два ученика решили задачу по-своему. Кто прав и почему?') +
      `<div class="pic">${свг(`
        ${фон(226)}
        <g>${проявить('8s',0.08,0.18)}
          <rect x="22" y="40" width="292" height="52" rx="10" fill="rgba(232,106,90,.12)" stroke="${RED}" stroke-width="1.6"/>
          ${т(168,64,'10 + 15 = 25 часов',20,RED,true)}
          ${т(168,84,'дольше, чем у одного Гелона',14,МУТ)}</g>
        <g>${проявить('8s',0.34,0.44)}
          <rect x="22" y="102" width="292" height="52" rx="10" fill="rgba(232,106,90,.12)" stroke="${RED}" stroke-width="1.6"/>
          ${т(168,126,'(10 + 15) : 2 = 12,5 часа',20,RED,true)}
          ${т(168,146,'тоже дольше, чем 10 часов',14,МУТ)}</g>
        <g>${проявить('8s',0.62,0.72)}
          <rect x="22" y="164" width="292" height="46" rx="10" fill="rgba(143,209,168,.14)" stroke="${GREEN}" stroke-width="2"/>
          ${т(168,192,'120 : 20 = 6 часов',20,GREEN,true)}</g>
      `,226)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='меньше'?'hit':(в?'miss':''), 'Вместе должно выйти МЕНЬШЕ 10 часов', "r367Ловушка('меньше')")}
        ${BTN(4, в==='среднее'?'miss':'', 'Среднее подходит: 12,5 часа', "r367Ловушка('среднее')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='меньше',
        в==='меньше' ? 'Верно, и это отличная самопроверка: вдвоём не может быть дольше, чем одному. Если ответ больше 10 часов — где-то ошибка.'
                     : 'Нет: 12,5 часа больше, чем 10 часов у одного Гелона. Помощник не может замедлить работу.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Проверка на здравый смысл: <b>вместе — быстрее самого быстрого</b>.');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Мастер делает 15 деталей в час. Сколько за 4 часа?',
      варианты:[{т:'60 деталей',ок:true},{т:'19 деталей',ок:false}],
      разбор:'15 × 4 = 60. Работа = производительность × время.' },
    { вопрос:'Нужно 90 деталей, мастер делает 9 в час. Сколько часов?',
      варианты:[{т:'10 часов',ок:true},{т:'81 час',ок:false}],
      разбор:'90 : 9 = 10 часов.' },
    { вопрос:'Двое делают 7 и 5 деталей в час. Сколько вместе за час?',
      варианты:[{т:'12 деталей',ок:true},{т:'2 детали',ок:false}],
      разбор:'7 + 5 = 12: производительности складываются.' },
    { вопрос:'Заказ 120 деталей, вместе делают 20 в час. За сколько часов?',
      варианты:[{т:'6 часов',ок:true},{т:'100 часов',ок:false}],
      разбор:'120 : 20 = 6 часов.' },
    { вопрос:'Один справился бы за 10 ч, другой за 15 ч. Может ли вместе выйти 12 часов?',
      варианты:[{т:'Нет: вместе всегда быстрее',ок:true},{т:'Да, это среднее',ок:false}],
      разбор:'Вместе быстрее самого быстрого: меньше 10 часов. Верный ответ — 6 часов.' }
  ];

  function F13(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r367Reset()")}</div>` +
        ПРАВИЛО('A = p × t · t = A : p · вместе <b>p₁ + p₂</b>.');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к,
        выбран===к ? (в.ок?'hit':'miss') : '',
        в.т, "r367Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Работа за время', задания:[
    {q:'10 деталей в час, 6 часов. Сколько деталей?', в:0, варианты:['60','16'], раз:'10 × 6 = 60.'},
    {q:'8 деталей в час, 5 часов. Сколько деталей?', в:1, варианты:['13','40'], раз:'8 × 5 = 40.'},
    {q:'Нужно 72 детали, делает 9 в час. Сколько часов?', в:0, варианты:['8 часов','63 часа'], раз:'72 : 9 = 8.'},
    {q:'Сделал 45 деталей за 5 часов. Сколько в час?', в:1, варианты:['40','9'], раз:'45 : 5 = 9.'}
  ]};
  const Т2 = { имя:'Вместе', задания:[
    {q:'6 и 4 детали в час. Вместе за час?', в:0, варианты:['10','2'], раз:'6 + 4 = 10.'},
    {q:'Вместе 20 в час, заказ 100. За сколько часов?', в:1, варианты:['5 часов? нет, 2000','5 часов'], раз:'100 : 20 = 5.'},
    {q:'Втроём 12 + 8 + 4. Сколько в час?', в:0, варианты:['24','12'], раз:'12 + 8 + 4 = 24.'},
    {q:'Вместе 15 в час, работали 4 часа. Сколько сделали?', в:1, варианты:['19','60'], раз:'15 × 4 = 60.'}
  ]};
  const Т3 = { имя:'Ловушки', задания:[
    {q:'Один за 6 ч, другой за 12 ч. Вместе — дольше 6 часов?', в:1,
      варианты:['Да','Нет: вместе всегда быстрее'], раз:'Вместе быстрее самого быстрого, то есть меньше 6 часов.'},
    {q:'Можно ли сложить 10 ч и 15 ч и получить время вместе?', в:1,
      варианты:['Да','Нет: складывают детали за час'], раз:'Складывают производительности: 12 + 8 = 20, затем 120 : 20 = 6.'},
    {q:'Вместе 20 в час. Сколько за 5 часов?', в:0,
      варианты:['100 деталей','90 деталей'], раз:'20 × 5 = 100. В отчёте ученика было 90 — это и была ошибка.'},
    {q:'Заказ 120 за 6 ч вдвоём, один делает 12. Второй?', в:0,
      варианты:['8 в час','20 в час'], раз:'120 : 6 = 20 общая; 20 − 12 = 8.'}
  ]};

  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r367T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r367TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L367 = {
    id: ID,
    title: 'Задачи на работу',
    ico: '🔨',
    src: 'Начальная школа · 4 класс · Работа и производительность',
    subj: 'jun',
    explain: [
      'В мастерской Архимеда нужно сделать 120 деталей для водяного винта. Гелон делает 12 деталей в час, Дамон — 8. Сколько работы за час — это производительность.',
      'Покрути часы сам: за 1 час Гелон сделает 12 деталей, за 5 часов — 60, а весь заказ закроет за 10 часов.',
      'Дамону на те же 120 деталей нужно 15 часов: 120 : 8 = 15. Чем больше делаешь за час, тем меньше времени нужно.',
      'Сделай ставку: за сколько часов они управятся вместе? Сначала прикидка, потом счёт.',
      'Пускаем обоих: за каждый час прибавляется 12 + 8 = 20 деталей, и за 6 часов заказ закрыт. Гелон сделал 72, Дамон — 48.',
      'Складывают не часы, а детали за час: 12 + 8 = 20, и только потом 120 : 20 = 6.',
      'Верная формула: время = вся работа : сумма производительностей.',
      'Проверка отчёта: за 5 часов выйдет 20 × 5 = 100 деталей, а не 90 — вот где ошибка.',
      'За 3 часа совместной работы готово 20 × 3 = 60 деталей — ровно половина заказа.',
      'Добавили ученика с 4 деталями в час: вместе 24 в час, и заказ закрывается за 5 часов.',
      'Обратный ход: 120 : 6 = 20 — это общая скорость; значит второй мастер делает 20 − 12 = 8 в час.',
      'Ловушки: 10 + 15 = 25 ч и (10 + 15) : 2 = 12,5 ч — оба ответа больше, чем у одного Гелона, а вместе всегда быстрее.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: работа, время и производительность.',
      'Тренажёр 2: совместная работа.',
      'Тренажёр 3: ловушки.'
    ],
    check: {
      q: 'Двое делают 7 и 5 деталей в час. Сколько деталей выйдет за час вместе?',
      choices: ['12 деталей','2 детали','35 деталей'],
      ans: 0,
      exp: 'Производительности складываются: 7 + 5 = 12 деталей в час.'
    },
    tasks: [
      { q:'Мастер делает 15 деталей в час. Сколько деталей за 4 часа?', kind:'choice',
        choices:['60','19'], ans:0,
        hints:['Работа = производительность × время.','15 × 4.'],
        sol:'60 деталей.' },
      { q:'Заказ 120 деталей, вместе делают 20 в час. За сколько часов закроют?', kind:'choice',
        choices:['6 часов','100 часов'], ans:0,
        hints:['Время = работа : общая производительность.','120 : 20.'],
        sol:'6 часов.' },
      { q:'Один справился бы за 10 ч, другой за 15 ч. Может ли вместе выйти 12 часов?', kind:'choice',
        choices:['Нет: вместе быстрее самого быстрого','Да, это среднее'], ans:0,
        hints:['Помощник не может замедлить работу.','Ответ должен быть меньше 10 часов.'],
        sol:'Нет, вместе выйдет 6 часов.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L367.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else if(f===14) сцена=тренажёр(s,'т1',Т1,1);
    else if(f===15) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);

    const ЗАГОЛОВКИ={
      1:'Заказ на 120 деталей',
      2:'Крути часы сам',
      3:'Второй мастер',
      4:'Сделай ставку',
      5:'Гонка по часам',
      6:'Почему складывают',
      7:'Собери формулу',
      8:'Найди ошибку в отчёте',
      9:'Половина заказа',
      10:'Добавь мастера',
      11:'Обратный ход',
      12:'Две ловушки',
      13:'Практика',
      14:'Тренажёр 1',
      15:'Тренажёр 2',
      16:'Тренажёр 3'
    };
    el.innerHTML = `<div class="s6 l367" data-frame="${f}">
        <h2>${ЗАГОЛОВКИ[f]||'Задачи на работу'}</h2>
        ${сцена}
      </div>`;
  }

  window.r367Часы     =(д)=>{ const s=S(); s.часыГелона=Math.max(1,Math.min(10,(s.часыГелона||1)+д)); chRender(0); };
  window.r367Гонка    =(д)=>{ const s=S(); s.гонкаБыло=s.гонка||0;
    s.гонка=Math.max(0,Math.min(6,(s.гонка||0)+д)); chRender(0); };
  window.r367Мастеров =(д)=>{ const s=S(); s.мастеров=Math.max(1,Math.min(3,(s.мастеров||2)+д)); chRender(0); };
  window.r367Ставка   =(к)=>{ S().ставка=к; chRender(0); };
  window.r367Почему   =(к)=>{ S().почему=к; chRender(0); };
  window.r367Формула  =(к)=>{ S().формула=к; chRender(0); };
  window.r367Отчёт    =(и)=>{ S().отчёт=и; chRender(0); };
  window.r367Часть    =(к)=>{ S().часть=к; chRender(0); };
  window.r367Обратная =(к)=>{ S().обратная=к; chRender(0); };
  window.r367Ловушка  =(к)=>{ S().ловушка=к; chRender(0); };
  window.r367Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r367Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r367T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=(s[ключ+'Шаг']||0)%набор.задания.length;
    const з=набор.задания[шаг];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r367TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L367; else arr.push(L367);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA367={render:render, L:L367};
})();
