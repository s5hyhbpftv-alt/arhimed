/* ============ РУССКИЙ ЯЗЫК · УРОК 887 · «СИНОНИМЫ, АНТОНИМЫ, ОМОНИМЫ» ============
   5 класс, «Лексика». Продолжение линии 877–883 (речь, текст, фонетика,
   орфоэпия, шипящие) и 1000 (паронимы).

   ФОРМАТ — КАК У РУССКИХ УРОКОВ (vis_ru.js): window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT: .kicker (номер и тема), <h2>, .lead,
   .cap, .row, .card/.nm/.ex, .tag, .pair, .split/.word, .score. Текст переносится
   сам, поэтому крупный кегль не ломает кадр; SVG-холста и эмодзи нет.
   Последний кадр — тренажёр на массиве game.

   ПРОВЕРЕННЫЕ ПРИМЕРЫ (школьный курс):
     синонимы: смелый — храбрый — отважный; большой — огромный; идти — шагать;
     антонимы: день — ночь, холодный — горячий, друг — враг, громко — тихо;
       у слов «стол», «тетрадь» антонима нет;
     омонимы: ключ (родник / от двери / скрипичный), коса (девичья / инструмент /
       песчаная), норка (зверёк / жилище), лук (растение / оружие),
       брак (супружество / испорченная работа);
     НЕ омонимы: ножка (стула, гриба, ребёнка) — общий признак «маленькая опора»,
       это многозначное слово;
     ловушка: «смелый — трусливый» — АНТОНИМЫ, а не синонимы.
   СВЕРКА с прошлым уроком: паронимы «надеть / одеть» (урок 1000). */
(function(){
  'use strict';

  const ID = 887;
  const R = window.RUKIT;
  if(!R){ return; }
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  /* helpers каркаса — те же, что в уроках 619/620 */
  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const row   = (...xs)=>`<div class="row">${xs.join('')}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;

  /* ── тренажёр: восемь заданий по кругу ── */
  const GAME=[
    ['смелый, храбрый, отважный — это…','синонимы','Синонимы: значение близкое — «не боится».','антонимы'],
    ['день и ночь — это…','антонимы','Антонимы: значения противоположны.','синонимы'],
    ['ключ-родник и ключ от двери — это…','омонимы','Омонимы: пишутся одинаково, значения разные.','синонимы'],
    ['ножка стула и ножка гриба — это…','многозначное слово','Есть общий признак: маленькая опора.','омонимы'],
    ['норка (зверёк) и норка (нора) — это…','омонимы','Значения не связаны — это омонимы.','многозначное слово'],
    ['лук (растение) и лук (оружие) — это…','омонимы','Общего признака у значений нет.','многозначное слово'],
    ['начало и конец — это…','антонимы','Противоположные значения.','синонимы'],
    ['есть ли антоним у слова «стол»?','нет','У «стола» антонима нет.','есть: стул']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    return `${kick('10 · Тренажёр')}
      ${word(it[0],'выбери название')}
      ${готово ? (верно ? R.NOTE('ok','верно',it[2]) : R.NOTE('no','исправить',it[2]))
               : `<p class="verdict">Сначала подумай: значения совпадают, спорят или одинаково пишутся?</p>`}
      <div class="row">${варианты.map(о=>
        `<button type="button" class="chip" style="flex:1 1 40%;font-size:20px"
           onclick="ru887Game('${esc(о)}')">${esc(о)}</button>`).join('')}</div>
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее задание.':'Сначала вопрос, потом название.')}`;
  }

  /* ── кадры ── */
  window.RULESSON({id:ID, game:GAME, frames:[
    ()=>`${kick('01 · Разминка из прошлого урока')}
      ${h2('Слова похожи, а значат разное')}
      ${lead('Мы уже знаем паронимы: «надеть шапку» — на себя, «одеть ребёнка» — кого-то. Сегодня три других понятия: слова-друзья, слова-противники и слова-близнецы.')}
      ${pair(card('надеть','на себя: надеть шапку, надеть пальто'),card('одеть','кого-то: одеть ребёнка, одеть куклу'))}
      ${cap('Паронимы звучат похоже, но значат разное. А бывает, что слова близки по значению — или спорят.')}`,

    ()=>`${kick('02 · Синонимы')}
      ${h2('Слова-друзья: значение одно')}
      ${lead('Смелый, храбрый, отважный — все три говорят одно: человек не боится. Так говорят по-разному об одном и том же.')}
      ${row(chip('смелый'),chip('храбрый'),chip('отважный'))}
      ${cards([['общее','значение одно: «не боится»'],['разное','оттенки: «отважный» звучит торжественнее']])}
      ${cap('Синонимы нужны, чтобы не повторять одно слово: большой дом, огромный дом, громадный дом.')}`,

    ()=>`${kick('03 · Антонимы')}
      ${h2('Слова-противники: значение обратное')}
      ${lead('День и ночь, холодный и горячий, друг и враг. Это два конца одной шкалы.')}
      ${cards([['день — ночь','время суток'],['холодный — горячий','температура'],['друг — враг','отношение людей']])}
      ${cap('Антоним есть не у каждого слова: у слов «стол», «тетрадь», «окно» противоположного значения не бывает.')}`,

    ()=>`${kick('04 · Омонимы')}
      ${h2('Слова-близнецы: запись одна, значения разные')}
      ${lead('Ключ от двери, ключ-родник и скрипичный ключ пишутся одинаково, а общего между ними нет. Это омонимы.')}
      ${row(chip('ключ от двери'),chip('ключ-родник'),chip('скрипичный ключ'))}
      ${cards([['коса','девичья, инструмент, песчаная'],['норка','зверёк и жилище'],['брак','супружество и испорченная работа']])}`,

    ()=>`${kick('05 · Омонимы или многозначное слово')}
      ${h2('Есть ли у значений общий признак')}
      ${lead('Лук — растение и лук — оружие: связи нет, значит омонимы. Ножка стула, ножка гриба, ножка ребёнка — во всех случаях это маленькая опора.')}
      ${pair(card('омонимы','лук, ключ, норка: общего признака нет'),card('многозначное','ножка, крыло: общий признак есть'))}
      ${cap('Проверка одна: спроси себя, что общего у значений. Есть общее — это одно слово, нет — омонимы.')}`,

    ()=>`${kick('06 · Как подобрать синоним')}
      ${h2('Два шага к слову-другу')}
      ${cards([['шаг 1','объясни значение своими словами'],['шаг 2','вспомни слово с тем же значением']])}
      ${lead('«Большой» — такой, который занимает много места. Значит, подойдут: огромный, громадный, крупный.')}
      ${cap('А «маленький» не подойдёт: это противоположное слово, антоним. Синоним и антоним легко перепутать.')}`,

    ()=>`${kick('07 · Антоним на другом конце шкалы')}
      ${h2('Громко — тихо')}
      ${lead('Чтобы найти антоним, поставь слово на шкалу и поищи его на другом конце. Громко — тихо, быстро — медленно, высоко — низко.')}
      ${pair(card('подходит','тихо: противоположное значение'),card('не подходит','громкий: это форма того же слова'))}
      ${cap('Если пары-противника нет, значит, антонима у слова нет.')}`,

    ()=>`${kick('08 · Проверка на омонимы')}
      ${h2('Норка — зверёк и норка — нора')}
      ${lead('Зверёк не является «маленькой норой»: общего признака нет. Значит, перед нами омонимы.')}
      ${cards([['что проверяем','связь значений, а не звучание'],['как звучит вывод','нет связи — омонимы; есть связь — одно слово']])}
      ${cap('Так же проверяются «брак», «мир», «коса».')}`,

    ()=>`${kick('09 · Ловушка')}
      ${h2('Спорят или совпадают?')}
      ${lead('Ученик сказал: «смелый и трусливый — синонимы». Это ошибка: значения спорят, значит это антонимы.')}
      ${pair(card('синонимы','смелый — храбрый — отважный'),card('антонимы','смелый — трусливый'))}
      ${cap('Прежде чем назвать слова синонимами, проверь: значения совпадают или противоположны?')}`,

    (st)=>тренер(st)
  ]});

  /* ── карточка каталога: тексты пояснений и задачи ── */
  const L887={
    id:ID, title:'Синонимы, антонимы, омонимы', ico:'📚',
    src:'Русский язык · 5 класс · Лексика', subj:'rus', group:null,
    explain:[
      'Мы уже знаем паронимы: «надеть шапку» — на себя, «одеть ребёнка» — кого-то. Сегодня три других понятия: слова-друзья, слова-противники и слова-близнецы.',
      'Синонимы — слова, близкие по значению: смелый, храбрый, отважный. Значение одно, а оттенки разные. Синонимы нужны, чтобы не повторять одно и то же слово в тексте.',
      'Антонимы — слова с противоположным значением: день и ночь, холодный и горячий, друг и враг. Они стоят на разных концах одной шкалы. Антоним есть не у каждого слова: у «стола» и «тетради» его нет.',
      'Омонимы — слова, которые пишутся и звучат одинаково, а значения имеют разные: ключ от двери, ключ-родник, скрипичный ключ; коса, норка, брак. Общего между значениями нет.',
      'Важно не путать омонимы с многозначными словами. Лук — растение и оружие: связи нет, это омонимы. Ножка стула и ножка гриба: общий признак «маленькая опора», значит это одно многозначное слово.',
      'Чтобы подобрать синоним, сделай два шага: объясни значение своими словами, потом вспомни слово с тем же значением. К «большому» подойдут огромный, громадный, крупный, а «маленький» — это уже антоним.',
      'Чтобы найти антоним, поставь слово на шкалу и поищи его на другом конце: громко — тихо. «Громкий» не подойдёт: это форма того же слова, а не противоположное значение.',
      'Проверка на омонимы простая: спроси, есть ли у значений общий признак. Норка-зверёк и норка-нора не связаны — омонимы. Если бы связь была, как у ножки стула и гриба, это было бы многозначное слово.',
      'Частая ошибка — назвать антонимы синонимами: «смелый и трусливый» — это антонимы, потому что значения противоположны. Синонимы к «смелому» — храбрый и отважный.',
      'Тренажёр: восемь заданий по кругу. Прочитай пару слов и реши, что это — синонимы, антонимы, омонимы или многозначное слово.'
    ],
    tasks:[
      { q:'Смелый, храбрый, отважный — это…', kind:'choice',
        choices:['синонимы','антонимы'], ans:0,
        hints:['Сравни значения.','Все три значат «не боится».'],
        sol:'Синонимы.' },
      { q:'День и ночь — это…', kind:'choice',
        choices:['антонимы','синонимы'], ans:0,
        hints:['Значения противоположны.','Два конца одной шкалы.'],
        sol:'Антонимы.' },
      { q:'Ключ-родник и ключ от двери — это…', kind:'choice',
        choices:['омонимы','многозначное слово'], ans:0,
        hints:['Есть ли общий признак?','Значения не связаны.'],
        sol:'Омонимы.' },
      { q:'Ученик назвал «смелый — трусливый» синонимами. Что не так?', kind:'choice',
        choices:['Это антонимы','Всё верно'], ans:0,
        hints:['Совпадают значения или спорят?','Синонимы: смелый, храбрый.'],
        sol:'Это антонимы.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){
      const м=arr.findIndex(L=>L && L.id===ID);
      if(м>=0) arr[м]=L887; else arr.push(L887);
    }
  }catch(e){}

  /* ── ход тренажёра ── */
  function игра(ответ){
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    const st=CHS[lk];
    if(st.gRes!=null){                       /* ответ уже дан: любой клик — следующее задание */
      st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null;
    }else{
      const it=GAME[(st.gIdx||0)%GAME.length];
      st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1;
    }
    if(typeof chRender==='function') chRender(0);
  }
  window.ru887Game=function(о){ try{ игра(о); }catch(e){} };
})();
