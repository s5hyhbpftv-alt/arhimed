/* ====== МАТЕМАТИКА · УРОК 388 · «РАЗРЕЗАНИЯ И ПЛОЩАДИ» ========================
   5–6 класс, олимпиадная тема «клетки». Урок переделан с нуля по эталону 1022
   (deploy/ЭТАЛОН_УРОКА.md). Прежний (vis_wk.js, visW388) — 18 кадров текста и
   два вопроса с выбором; этот файл регистрируется поверх него.

   ПОЧЕМУ ПАЛЬЦЕМ ПО ПЛИТЕ. Тема — разрезание, значит ребёнок режет сам:
   ставит квадраты 2×2 на плиту, кладёт доминошки на доску. Выбор из двух
   вариантов тут врёт — по нему не видно, понял он или угадал.

   ГЛАВНАЯ ОШИБКА ТЕМЫ сделана видимой: «площадь делится — значит, режется».
   Плита 5×4 = 20 клеток, 20 : 4 = 5, а квадратов 2×2 выходит только четыре —
   остаётся полоска, и ребёнок видит её сам. Вершина урока — классика
   олимпиад: доска 4×4 без двух противоположных углов. По площади 14 клеток =
   7 доминошек, а не выходит; раскраска доски объясняет почему.

   РАЗБОР НАЗЫВАЕТ СОБСТВЕННУЮ ОШИБКУ: квадрат вылезает за край — какой;
   лёг на занятое; доминошка по диагонали; в рамке угловые плитки посчитаны
   дважды; треугольник посчитан целым прямоугольником.

   СЮЖЕТ: Сиракузы, мастерская мозаики. Говорят, Архимед придумал головоломку
   «стомахион» — квадрат, разрезанный на 14 кусочков: из них складывают разные
   фигуры, и у всех одна и та же площадь. Ход квеста — лист заказов мастерской.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ:
     6×4 = 24, квадрат 2×2 = 4, 24 : 4 = 6 — плита режется ровно на 6.
     5×4 = 20, 20 : 4 = 5, но в ширину 5 : 2 = 2 и 1 в остатке — квадратов 4,
       остаётся полоска 1×4 = 4 клетки.
     4×3 = 12 → две половины 2×3 → столбик 2×6 = 12.
     ступенька: низ 5×2 = 10, над ним 2×3 = 6, всего 16; по-другому — левая
       часть 2×5 = 10 и правая 3×2 = 6; описанный квадрат 5×5 = 25 — ловушка.
     рамка: 5×5 − 3×3 = 25 − 9 = 16; «по 5 на сторону» = 20 — ловушка.
     треугольник на прямоугольнике 6×4: 24 : 2 = 12.
     доска 4×4 без углов (0;0) и (3;3): оба угла тёмные (сумма номеров
       чётная), осталось 8 светлых и 6 тёмных; доминошка закрывает одну
       светлую и одну тёмную — семь доминошек невозможны.

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md. В каждом кадре есть <animate>: без
   него Chrome не заводит таймлайн SVG, где одни animateTransform.
   prefers-reduced-motion выключает SMIL; без движения каждый рисунок
   показывает конечное состояние. */
(function(){
  'use strict';

  const ID = 388;
  const GOLD='#ffd76a', GREEN='#8fd1a8', RED='#e86a5a', ЛАЗУРЬ='#8fd0e6',
        ТЕРРА='#e08a5f', ТЕРРА2='#b8603a';
  const ИНК='#f4ecdd', МУТ='#c2b194', ЛИНИЯ='#6e5c45', ОБВОД='#1a120c',
        ПЛИТА='#3a2f27', ШОВ='rgba(244,236,221,.22)';

  /* ---------- ЛИСТ ЗАКАЗОВ: ход квеста ---------- */
  const ДЕЛА = [
    {ключ:'квадраты',   имя:'Разрезать плиту 6×4 на квадраты 2×2', итог:'6 квадратов'},
    {ключ:'ступенька',  имя:'Посчитать площадь ступеньки',          итог:'16 клеток'},
    {ключ:'доминошки',  имя:'Доказать: доминошками не закрыть',     итог:'доказано'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const CSS=`
  #lvis .s6.l388{gap:14px}
  #lvis .s6.l388 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l388 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l388 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#2e251f,#1c1612);border:1.5px solid var(--line)}
  #lvis .s6.l388 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l388 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l388 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l388 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l388 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l388 .карт .текст b{color:${GOLD}}
  #lvis .s6.l388 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l388 .правило b{color:${GOLD}}

  #lvis .s6.l388 .лист{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(224,138,95,.14),rgba(224,138,95,.04));
    border:1.5px solid rgba(224,138,95,.5);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l388 .лист .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l388 .лист .шапка b{font-size:20px;letter-spacing:0;text-transform:none;color:${ТЕРРА}}
  #lvis .s6.l388 .лист .шапка b.готово{color:${GREEN}}
  #lvis .s6.l388 .лист ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l388 .лист li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l388 .лист li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;
    border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l388 .лист li.есть{color:${ИНК}}
  #lvis .s6.l388 .лист li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,209,168,.14)}
  #lvis .s6.l388 .лист li span{margin-left:auto;white-space:nowrap;font-variant-numeric:tabular-nums;color:var(--mut);font-size:14px}
  #lvis .s6.l388 .лист li.есть span{color:${GREEN}}

  /* ── ряд кнопок-рычагов: ширина и высота, разрез ── */
  #lvis .s6.l388 .рычаги{display:grid;grid-template-columns:1fr 1fr;gap:8px;width:100%}
  #lvis .s6.l388 .рычаги button{min-height:52px;border-radius:14px;cursor:pointer;font:inherit;font-size:16px;
    font-weight:600;border:1.5px solid rgba(224,138,95,.55);background:rgba(224,138,95,.13);color:${ИНК};
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l388 .рычаги button:active{transform:translateY(2px);background:rgba(224,138,95,.26)}
  #lvis .s6.l388 .рычаги button:disabled{opacity:.35;cursor:not-allowed}
  #lvis .s6.l388 .рычаги button.вкл{border-color:${GOLD};background:rgba(255,215,106,.16);color:${GOLD}}

  #lvis .s6.l388 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l388 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l388 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l388 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l388 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l388 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;
    font-variant-numeric:tabular-nums;font-size:18px}
  #lvis .s6.l388 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;
    font-variant-numeric:tabular-nums;font-size:20px}

  #lvis .s6.l388 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l388 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l388 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l388 .уровни .точка.сейчас{background:${GOLD};animation:l388dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l388dot{0%,100%{opacity:1}50%{opacity:.6}}
  #lvis .s6.l388 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l388{-webkit-text-size-adjust:100%}
  #lvis .s6.l388 [data-anim]{animation:l388rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l388rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l388 [data-anim]{animation:none!important}
    #lvis .s6.l388 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l388 .ask button,#lvis .s6.l388 .рычаги button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l388-style');
      if(!s){ s=document.createElement('style'); s.id='l388-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Заказ</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  const ЛИСТ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'лист',
      `<div class="шапка"><span>Заказы мастерской</span><b class="${всё?'готово':''}">${
        всё?'все готовы':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}
           <span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анРаз = (имя,значения,длит) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" fill="freeze"
              calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';
  /* сдвиг группы; рядом — невидимый «завод» с одним <animate>, иначе Chrome
     таймлайн не заводит и предмет стоит (830, 995) */
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const анСдвиг = (значения,длит,keyTimes) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}"
              repeatCount="indefinite" calcMode="spline" keyTimes="${keyTimes}"
              keySplines="${сплайны(keyTimes.split(';').length-1)}"/>${ЗАВОД}` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c388-фон" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#3a2e25"/><stop offset="0.6" stop-color="#241c16"/>
        <stop offset="1" stop-color="#16110d"/>
      </linearGradient>
      <linearGradient id="c388-терра" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#eea27a"/><stop offset="1" stop-color="${ТЕРРА2}"/>
      </linearGradient>
      <linearGradient id="c388-лазурь" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#b6e4f2"/><stop offset="1" stop-color="#4f9bb6"/>
      </linearGradient>
      <linearGradient id="c388-золото" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#ffe7a3"/><stop offset="1" stop-color="#c8912f"/>
      </linearGradient>
      <linearGradient id="c388-дерево" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#9a6a3c"/><stop offset="1" stop-color="#5a3a1c"/>
      </linearGradient>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c388-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  /* плашка-подпись по центру x; ширина — по числу знаков с запасом:
     жирная кириллица в Georgia шире 0,6 кегля на знак (замер снимком) */
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.66+20, в=к+10;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g>
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(16,11,8,.9)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* ---------------- ПЛИТА ПО КЛЕТКАМ -----------------------------------------
     Клетка 44 единицы ≈ 46 px на телефоне 390 — палец попадает (норма 44).
     Клетки рисуются поштучно: у доски без углов нет «лишних» клеток. Мишени
     касания — самый верхний слой, плиткой без щелей; всё нарисованное под
     ними прозрачно для пальца (урок 1022: подсказка глотала тап). */
  const КЛ = 44;
  function плита(о){
    const к = о.кл || КЛ;
    const есть = (i,j) => !(о.нет||[]).some(п=>п[0]===i&&п[1]===j);
    let клетки='', мишени='';
    for(let j=0;j<о.ряд;j++) for(let i=0;i<о.кол;i++){
      if(!есть(i,j)) continue;
      const x=о.x0+i*к, y=о.y0+j*к;
      const цвет = о.раскрас ? ((i+j)%2===0 ? '#2a211b' : '#6b5a48') : ПЛИТА;
      клетки+=`<rect x="${x}" y="${y}" width="${к}" height="${к}" fill="${цвет}" stroke="${ШОВ}" stroke-width="1"/>`;
      if(о.тап) мишени+=`<rect x="${x}" y="${y}" width="${к}" height="${к}" fill="rgba(255,255,255,.001)"
          style="cursor:pointer" onclick="${о.тап}(${i},${j})"/>`;
    }
    const контур = о.контур===false ? '' :
      `<rect x="${о.x0}" y="${о.y0}" width="${о.кол*к}" height="${о.ряд*к}" fill="none" stroke="${МУТ}" stroke-width="2" rx="2"/>`;
    return `<g><g pointer-events="none">${клетки}${о.нет?'':контур}${о.поверх||''}</g>${мишени}</g>`;
  }
  /* кусок плиты: прямоугольник w×h клеток с левого верхнего угла (i,j) */
  const кусок = (x0,y0,i,j,w,h,заливка,к,свежий) => {
    const кк=к||КЛ, x=x0+i*кк+2, y=y0+j*кк+2, W=w*кк-4, H=h*кк-4;
    let швы='';
    for(let a=1;a<w;a++) швы+=`<line x1="${x0+(i+a)*кк}" y1="${y+3}" x2="${x0+(i+a)*кк}" y2="${y+H-3}" stroke="rgba(26,18,12,.35)" stroke-width="1"/>`;
    for(let b=1;b<h;b++) швы+=`<line x1="${x+3}" y1="${y0+(j+b)*кк}" x2="${x+W-3}" y2="${y0+(j+b)*кк}" stroke="rgba(26,18,12,.35)" stroke-width="1"/>`;
    return `<g>${свежий?анРаз('opacity','0.2;1','0.45s'):''}
      <rect x="${x}" y="${y}" width="${W}" height="${H}" rx="6" fill="${заливка||'url(#c388-терра)'}"
        stroke="${ОБВОД}" stroke-width="1.4"/>
      <rect x="${x+3}" y="${y+3}" width="${W-6}" height="3" rx="1.5" fill="rgba(255,255,255,.28)"/>
      ${швы}</g>`;
  };

  /* ================= РАЗРЕЗ НА КВАДРАТЫ 2×2 ==================================
     Тап по свободной клетке кладёт квадрат с этой клеткой в левом верхнем
     углу; тап по лежащему квадрату снимает его. Промах называется: за какой
     край вылез квадрат или на что он лёг. */
  const занята = (набор,i,j) => набор.some(q=>i>=q[0]&&i<q[0]+2&&j>=q[1]&&j<q[1]+2);
  const влезает = (набор,кол,ряд,i,j) => i+1<кол && j+1<ряд &&
    !занята(набор,i,j) && !занята(набор,i+1,j) && !занята(набор,i,j+1) && !занята(набор,i+1,j+1);
  const ещёМожно = (набор,кол,ряд) => {
    for(let j=0;j<ряд-1;j++) for(let i=0;i<кол-1;i++) if(влезает(набор,кол,ряд,i,j)) return true;
    return false;
  };
  function тапКвадрат(s,кл,кол,ряд,i,j){
    const набор = s[кл] || (s[кл]=[]);
    const к = набор.findIndex(q=>i>=q[0]&&i<q[0]+2&&j>=q[1]&&j<q[1]+2);
    if(к>=0){ набор.splice(к,1); s[кл+'Мимо']=null; s[кл+'Свежий']=-1; return; }
    let мимо=null;
    if(i+1>=кол) мимо='право';
    else if(j+1>=ряд) мимо='низ';
    else if(!влезает(набор,кол,ряд,i,j)) мимо='занято';
    if(мимо){ s[кл+'Мимо']=мимо; s[кл+'Промахов']=(s[кл+'Промахов']||0)+1; return; }
    набор.push([i,j]); s[кл+'Мимо']=null; s[кл+'Свежий']=набор.length-1;
  }
  const НАЗВАТЬ_МИМО = {
    право:'От этой клетки квадрат 2×2 вылезет за <b>правый край</b>: справа от неё нет второй клетки. Тапни клетку левее.',
    низ:'От этой клетки квадрат 2×2 вылезет за <b>нижний край</b>: под ней нет второй клетки. Тапни клетку выше.',
    занято:'Квадрат лёг бы на уже вырезанный — <b>две плиты на одном месте не лежат</b>. Найди четыре свободные клетки квадратом.'
  };
  const квадратыНаПлите = (x0,y0,набор,свежий) =>
    набор.map((q,к)=>кусок(x0,y0,q[0],q[1],2,2,'url(#c388-терра)',КЛ,к===свежий)).join('');
  /* клетки, которые не закрыты ни одним квадратом */
  const остаток = (набор,кол,ряд) => {
    const р=[]; for(let j=0;j<ряд;j++) for(let i=0;i<кол;i++) if(!занята(набор,i,j)) р.push([i,j]); return р;
  };

  /* ================= ДОМИНОШКИ НА ДОСКЕ БЕЗ УГЛОВ ============================ */
  const ДОСКА_НЕТ = [[0,0],[3,3]];
  const наДоске = (i,j) => i>=0&&i<4&&j>=0&&j<4 && !ДОСКА_НЕТ.some(п=>п[0]===i&&п[1]===j);
  const подДоминошкой = (дом,i,j) => дом.findIndex(д=>д.some(п=>п[0]===i&&п[1]===j));
  const свободна = (дом,i,j) => наДоске(i,j) && подДоминошкой(дом,i,j)<0;
  const доминошкеЕстьМесто = (дом) => {
    for(let j=0;j<4;j++) for(let i=0;i<4;i++) if(свободна(дом,i,j) && (свободна(дом,i+1,j)||свободна(дом,i,j+1))) return true;
    return false;
  };

  /* ================= КАДРЫ ================= */

  /* 1. Мастерская и ящик Архимеда */
  function F1(s){
    const Н=258;
    /* пол мастерской: плитки шахматкой вдоль низа */
    const пол = Array.from({length:12},(_,i)=>Array.from({length:3},(_,j)=>
      `<rect x="${i*28}" y="${186+j*24}" width="28" height="24" fill="${(i+j)%2?'#5a4636':'#4a392c'}" stroke="rgba(0,0,0,.25)"/>`).join('')).join('');
    /* ящик с кусочками: квадрат 6×6 клеток по 14, разрезанный на части */
    const x0=196, y0=58, к=14;
    const части = [
      {d:`M${x0} ${y0} h${6*к} v${2*к} h-${6*к} z`, з:'url(#c388-терра)'},
      {d:`M${x0} ${y0+2*к} h${3*к} v${4*к} h-${3*к} z`, з:'url(#c388-лазурь)'},
      {d:`M${x0+3*к} ${y0+2*к} h${3*к} v${4*к} z`, з:'url(#c388-золото)'},
      {d:`M${x0+3*к} ${y0+2*к} l${3*к} ${4*к} h-${3*к} z`, з:'url(#c388-терра)'}
    ];
    return ЛИСТ(s) +
      ЗАДАЧА('Сиракузы, мастерская мозаики. Мастер кладёт полы из каменных плит и режет их <b>по клеткам</b>. Говорят, сам Архимед придумал здесь головоломку «стомахион»: квадрат, разрезанный на кусочки, — из них складывают разные фигуры, и <b>у всех одна и та же площадь</b>.') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${пол}
        ${подпись(168,26,'Мастерская мозаики',GOLD,16)}
        <rect x="${x0-10}" y="${y0-10}" width="${6*к+20}" height="${6*к+20}" rx="8" fill="url(#c388-дерево)" stroke="${ОБВОД}" stroke-width="1.6"/>
        ${части.map((ч,i)=>`<path d="${ч.d}" fill="${ч.з}" stroke="${ОБВОД}" stroke-width="1.3"/>`).join('')}
        ${т(x0+3*к,y0+6*к+30,'ящик с кусочками',12,МУТ)}
        ${/* плита 4×3 на верстаке: одна часть выезжает и возвращается */''}
        <rect x="24" y="150" width="148" height="12" rx="3" fill="url(#c388-дерево)" stroke="${ОБВОД}" stroke-width="1.2"/>
        ${плита({x0:40,y0:66,кол:4,ряд:3,кл:28})}
        <g>${анСдвиг('0 0;0 0;26 -14;26 -14;0 0','6s','0;0.2;0.45;0.8;1')}
          ${кусок(40,66,2,0,2,3,'url(#c388-терра)',28)}</g>
        <g>${анК('opacity','0.6;0.6;1;1;0.6','6s','0;0.3;0.45;0.8;1')}
          ${т(96,184,'4 · 3 = 12 клеток',14,GOLD,true)}</g>
        ${подпись(168,248,'площадь считаем клетками',МУТ,14)}
      `,Н)}</div>` +
      СКАЗ('Два слова','<b>Площадь</b> фигуры на клетках — это сколько в ней клеток. Одна клетка — единица площади. Резать можно только по линиям клеток.') +
      ПРАВИЛО('Разрезали, переложили — <b>площадь та же</b>: клетки никуда не делись.');
  }

  /* 2. Площадь прямоугольника: ряды на число в ряду */
  function F2(s){
    const a = s.ширина||4, b = s.высота||3, к=36, Н=290;   /* высота кадра постоянна: под пальцем ничего не прыгает */
    const x0 = Math.round((336-a*к)/2), y0 = 46;
    const ряды = Array.from({length:b},(_,j)=>`<g>${ДВИЖ?анК('opacity','0.25;0.25;1;1','5s','0;'+кт(0.08+j*0.1)+';'+кт(0.16+j*0.1)+';1'):''}
        ${Array.from({length:a},(_,i)=>кусок(x0,y0,i,j,1,1,j%2?'url(#c388-лазурь)':'url(#c388-терра)',к)).join('')}
        ${т(x0-12,y0+j*к+к/2+5,String(j+1),12,МУТ,false,'end')}</g>`).join('');
    return ЛИСТ(s) +
      ЗАДАЧА('Мастер меряет плиту. Меняй ширину и высоту и смотри: клетки ложатся <b>рядами</b>. В каждом ряду столько клеток, какова ширина, а рядов — сколько высота.') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${т(168,26,'Считаем рядами',14,GOLD,true)}
        ${плита({x0,y0,кол:a,ряд:b,кл:к})}
        ${ряды}
        ${т(168,y0+b*к+26,'ширина '+a+', рядов '+b,14,МУТ)}
        ${подпись(168,Н-12,a+' · '+b+' = '+(a*b)+' клеток',GOLD,16)}
      `,Н)}</div>` +
      `<div class="рычаги">
        ${BTN(3,'','ширина −',"r388Размер(-1,0)")}${BTN(3,'','ширина +',"r388Размер(1,0)")}
        ${BTN(4,'','высота −',"r388Размер(0,-1)")}${BTN(4,'','высота +',"r388Размер(0,1)")}
      </div>` +
      ПРАВИЛО('Площадь прямоугольника = <b>ширина · высота</b> (в клетках).');
  }

  /* 3. Режем плиту 6×4 на квадраты 2×2 — сами */
  const П3 = {x0:36, y0:48, кол:6, ряд:4};
  function F3(s){
    const набор = s.кв3||[], Н=268;
    const готово = набор.length===6;
    const тупик = !готово && !ещёМожно(набор,6,4);
    const подсказка = !готово && (s.кв3Промахов||0)>=2
      ? [[0,0],[2,0],[4,0],[0,2],[2,2],[4,2]].filter(q=>!занята(набор,q[0],q[1])).map(q=>
          `<rect x="${П3.x0+q[0]*КЛ+4}" y="${П3.y0+q[1]*КЛ+4}" width="${2*КЛ-8}" height="${2*КЛ-8}" rx="6"
             fill="none" stroke="${GOLD}" stroke-width="2" stroke-dasharray="6 5">
             ${анК('stroke-dashoffset','0;-22','1.4s','0;1')}</rect>`).join('') : '';
    const мимо = s.кв3Мимо;
    return ЛИСТ(s) +
      ЗАДАЧА('Первый заказ: плита 6×4 — порежь её на квадраты 2×2 для узора. <b>Тапни клетку</b> — квадрат ляжет от неё вправо и вниз. Тап по квадрату снимает его.') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${т(168,28,'Плита 6 × 4 — на квадраты 2 × 2',14,GOLD,true)}
        ${плита({...П3, тап:'r388Тап3', поверх: квадратыНаПлите(П3.x0,П3.y0,набор,s.кв3Свежий) + подсказка})}
        ${подпись(168,Н-14, готово?'24 : 4 = 6 квадратов':'квадратов: '+набор.length+' из 6', готово?GREEN:GOLD,16)}
      `,Н)}</div>` +
      (готово
        ? РАЗБОР(true,'Плита 6×4 — это 24 клетки, квадрат 2×2 — 4 клетки. <b>24 : 4 = 6</b>, и квадраты легли ровно, без остатка.')
        : тупик
          ? A(9,'карт ошибка','<span class="метка">Разбор</span><div class="текст">❌ Квадраты стоят со сдвигом: остались полоски шириной в одну клетку, туда квадрат 2×2 не лезет. <b>Начни от угла</b> — квадраты встанут вплотную.</div>') +
            `<div class="ask">${BTN(10,'','Снять все квадраты',"r388Сброс('кв3')")}</div>`
          : мимо ? РАЗБОР(false,НАЗВАТЬ_МИМО[мимо]) + ((s.кв3Промахов||0)>=2?СКАЗ('Подсказка','Пунктиром показано, где квадраты лягут вплотную.'):'')
          : СКАЗ('Как резать','Площадь плиты 6 · 4 = 24 клетки, у квадрата 2 · 2 = 4. Сколько квадратов выйдет — проверь руками.')) +
      ПРАВИЛО('Сколько кусков: <b>площадь плиты : площадь куска</b> — если куски ложатся без остатка.');
  }

  /* 4. Ловушка: 5×4 — по площади пять, а квадратов четыре */
  const П4 = {x0:58, y0:48, кол:5, ряд:4};
  function F4(s){
    const набор = s.кв4||[], Н=268;
    const тупик = !ещёМожно(набор,5,4);
    const полно = тупик && набор.length===4;
    const хвост = полно ? остаток(набор,5,4).map(п=>
      `<rect x="${П4.x0+п[0]*КЛ+3}" y="${П4.y0+п[1]*КЛ+3}" width="${КЛ-6}" height="${КЛ-6}" rx="5"
         fill="rgba(232,106,90,.28)" stroke="${RED}" stroke-width="2">
         ${анК('opacity','1;1;0.6;1','2.4s','0;0.4;0.7;1')}</rect>`).join('') : '';
    const в = s.ответ4;
    const мимо = s.кв4Мимо;
    return ЛИСТ(s) +
      ЗАДАЧА('Вторая плита — 5×4. Это 20 клеток, а 20 : 4 = 5. Значит, пять квадратов 2×2? <b>Проверь руками</b>: режь, пока квадраты помещаются.') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${т(168,28,'Плита 5 × 4: 20 клеток',14,GOLD,true)}
        ${плита({...П4, тап:'r388Тап4', поверх: квадратыНаПлите(П4.x0,П4.y0,набор,s.кв4Свежий) + хвост})}
        ${подпись(168,Н-14, полно?'квадратов 4 и полоска 1 × 4':'квадратов: '+набор.length, полно?RED:GOLD,16)}
      `,Н)}</div>` +
      (полно
        ? `<div class="ask">
            ${BTN(4, в===0?'miss':'', 'Пять: 20 : 4 = 5', "r388Отв4(0)")}
            ${BTN(5, в===1?'hit':'', 'Четыре, и остаётся полоска 1×4', "r388Отв4(1)")}
          </div>` +
          (в==null ? СКАЗ('Вопрос','Сколько квадратов 2×2 вырезается из плиты 5×4?')
           : в===1 ? РАЗБОР(true,'В ширину 5 клеток: два квадрата занимают 4, а пятый столбик — шириной в одну клетку. Квадрат туда не лезет, и остаётся полоска <b>1×4</b>.')
           : РАЗБОР(false,'По площади выходит пять, но квадраты не режутся из воздуха: в ширину 5 : 2 = 2 и 1 в остатке. Посмотри на красную полоску — это и есть «пятый квадрат», только он в одну клетку шириной.'))
        : тупик
          ? A(9,'карт ошибка','<span class="метка">Разбор</span><div class="текст">❌ Квадраты сдвинуты, и места осталось больше, чем нужно. <b>Начни от угла</b>.</div>') +
            `<div class="ask">${BTN(10,'','Снять все квадраты',"r388Сброс('кв4')")}</div>`
          : мимо ? РАЗБОР(false,НАЗВАТЬ_МИМО[мимо])
          : СКАЗ('Как резать','Тапай клетки, пока квадраты помещаются.')) +
      (в!=null ? ПРАВИЛО('Площадь делится — <b>ещё не значит, что режется</b>. Проверяй, укладываются ли куски по сторонам.') : '');
  }

  /* 5. Перекладывание: 4×3 → 2×6 */
  function F5(s){
    const к=30, x0=108, y0=48, Н=268;
    const в = s.ответ5;
    return ЛИСТ(s) +
      ЗАДАЧА('Плиту 4×3 распилили пополам на две части 2×3 и правую переложили вниз. Вышел высокий столбик 2×6. Площадь изменилась?') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${т(168,28,'Переложили — площадь та же?',14,GOLD,true)}
        ${плита({x0,y0,кол:2,ряд:6,кл:к,контур:false})}
        ${/* где часть лежала раньше — пунктир */''}
        <rect x="${x0+2*к}" y="${y0}" width="${2*к}" height="${3*к}" fill="none" stroke="${МУТ}" stroke-width="1.6" stroke-dasharray="5 5"/>
        ${кусок(x0,y0,0,0,2,3,'url(#c388-лазурь)',к)}
        <g>${анСдвиг('60 -90;60 -90;0 0;0 0;60 -90','6s','0;0.15;0.45;0.85;1')}
          ${кусок(x0,y0,0,3,2,3,'url(#c388-терра)',к)}</g>
        <g>${анК('opacity','1;1;0.35;0.35;1','6s','0;0.15;0.3;0.85;1')}
          ${т(250,96,'4 · 3 = 12',14,МУТ,true,'start')}</g>
        <g>${анК('opacity','0.35;0.35;1;1;0.35','6s','0;0.4;0.5;0.85;1')}
          ${т(250,196,'2 · 6 = 12',14,GOLD,true,'start')}</g>
        ${в===1 ? подпись(168,Н-14,'клетки те же — площадь та же',GREEN,14) : подпись(168,Н-14,'было 4 × 3, стало 2 × 6',МУТ,14)}
      `,Н)}</div>` +
      `<div class="ask">
        ${BTN(4, в===0?'miss':'', 'Больше — столбик же выше', "r388Отв5(0)")}
        ${BTN(5, в===1?'hit':'', 'Та же: 12 клеток', "r388Отв5(1)")}
      </div>` +
      (в==null ? СКАЗ('Вопрос','Сравни площади до и после.')
       : в===1 ? РАЗБОР(true,'Ни одной клетки не добавили и не убрали: <b>4 · 3 = 2 · 6 = 12</b>. На этом и держится стомахион Архимеда: кусочки складывают по-разному, а площадь всегда одна.')
       : РАЗБОР(false,'Выше — не значит больше: столбик стал вдвое уже. Посчитай клетки: 2 · 6 = 12, как и 4 · 3 = 12.')) +
      (в!=null ? ПРАВИЛО('Разрезание и перекладывание <b>не меняют площадь</b>.') : '');
  }

  /* 6. Ступенька: режем на прямоугольники */
  function F6(s){
    const к=36, x0=78, y0=44, Н=262;
    const разрез = s.разрез6;           /* 'в' | 'г' | undefined */
    const в = s.ответ6;
    /* ступенька: низ 5×2 (ряды 3–4), над ним 2×3 (столбцы 0–1, ряды 0–2) */
    const нет = []; for(let j=0;j<3;j++) for(let i=2;i<5;i++) нет.push([i,j]);
    let части='', линия='';
    if(разрез==='г'){
      части = кусок(x0,y0,0,0,2,3,'url(#c388-лазурь)',к)+кусок(x0,y0,0,3,5,2,'url(#c388-терра)',к)
        + т(x0+к,y0+1.5*к+6,'6',20,ОБВОД,true) + т(x0+2.5*к,y0+4*к+7,'5 · 2 = 10',16,ОБВОД,true)
        + т(x0+к,y0+1.5*к+24,'2 · 3',12,ОБВОД);
      линия = `<line x1="${x0-8}" y1="${y0+3*к}" x2="${x0+2*к+8}" y2="${y0+3*к}" stroke="${GOLD}" stroke-width="3" stroke-dasharray="7 5">
        ${анК('stroke-dashoffset','0;-24','1.2s','0;1')}</line>`;
    } else if(разрез==='в'){
      части = кусок(x0,y0,0,0,2,5,'url(#c388-лазурь)',к)+кусок(x0,y0,2,3,3,2,'url(#c388-терра)',к)
        + т(x0+к,y0+2.5*к+6,'10',20,ОБВОД,true) + т(x0+к,y0+2.5*к+24,'2 · 5',12,ОБВОД)
        + т(x0+3.5*к,y0+4*к+7,'3 · 2 = 6',16,ОБВОД,true);
      линия = `<line x1="${x0+2*к}" y1="${y0+3*к-8}" x2="${x0+2*к}" y2="${y0+5*к+8}" stroke="${GOLD}" stroke-width="3" stroke-dasharray="7 5">
        ${анК('stroke-dashoffset','0;-24','1.2s','0;1')}</line>`;
    } else {
      части = кусок(x0,y0,0,0,2,3,'url(#c388-золото)',к)+кусок(x0,y0,0,3,5,2,'url(#c388-золото)',к);
      линия = `<rect x="${x0-4}" y="${y0-4}" width="${5*к+8}" height="${5*к+8}" fill="none" stroke="${МУТ}" stroke-width="1.2" stroke-dasharray="4 6" opacity=".6">
        ${анК('opacity','0.6;0.6;0.2;0.6','4s','0;0.4;0.7;1')}</rect>`;
    }
    const разборы = [
      'Это описанный квадрат 5×5 — вместе с пустым углом 3×3. Пустое не кладут: <b>25 − 9 = 16</b>.',
      'Разрезали на два прямоугольника и сложили. Как ни режь — <b>10 + 6 = 16</b>.',
      'Это только нижняя полоса 5×2. Над ней ещё часть 2×3 = 6 клеток: <b>10 + 6 = 16</b>.'
    ];
    return ЛИСТ(s) +
      ЗАДАЧА('Второй заказ: ступенька у входа. Прямоугольника нет — значит, <b>разрежь её на прямоугольники</b> и сложи их площади. Выбери, где резать.') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${т(168,28,'Ступенька',14,GOLD,true)}
        ${плита({x0,y0,кол:5,ряд:5,кл:к,нет,поверх:части+линия})}
        ${подпись(168,Н-12, разрез?'10 + 6 = 16 клеток':'режь по линии клеток', разрез?GREEN:МУТ,14)}
      `,Н)}</div>` +
      `<div class="рычаги">
        ${BTN(3,разрез==='г'?'вкл':'','Резать поперёк',"r388Разрез('г')")}
        ${BTN(3,разрез==='в'?'вкл':'','Резать вдоль',"r388Разрез('в')")}
      </div>` +
      `<div class="ask три">
        ${['25','16','10'].map((v,к_)=>BTN(4+к_, в===к_?(к_===1?'hit':'miss'):'', v, "r388Отв6("+к_+")")).join('')}
      </div>` +
      (в==null ? СКАЗ('Вопрос','Сколько клеток в ступеньке?') : РАЗБОР(в===1, разборы[в])) +
      ПРАВИЛО('Сложная фигура = <b>сумма прямоугольников</b>, на которые её разрезали.');
  }

  /* 7. Рамка: целое минус дырка */
  function F7(s){
    const к=36, x0=78, y0=44, Н=262;
    const в = s.ответ7;
    const кольцо = [];
    for(let i=0;i<5;i++) кольцо.push([i,0]);
    for(let j=1;j<5;j++) кольцо.push([4,j]);
    for(let i=3;i>=0;i--) кольцо.push([i,4]);
    for(let j=3;j>=1;j--) кольцо.push([0,j]);
    const плитки = кольцо.map((п,н)=>`<g>${анК('opacity','0.3;0.3;1;1','8s','0;'+кт(0.03+н*0.035)+';'+кт(0.06+н*0.035)+';1')}
      ${кусок(x0,y0,п[0],п[1],1,1,(п[0]+п[1])%2?'url(#c388-лазурь)':'url(#c388-терра)',к)}</g>`).join('');
    const разборы = [
      'По 5 плиток на каждую из четырёх сторон — это 20, но <b>угловые плитки посчитаны дважды</b>: каждая стоит сразу на двух сторонах. 20 − 4 = 16.',
      'Весь квадрат 5 · 5 = 25, дырка 3 · 3 = 9, в рамке <b>25 − 9 = 16</b>.',
      'Это весь квадрат 5×5 вместе с дыркой. А в дырке плиток нет: <b>25 − 9 = 16</b>.'
    ];
    return ЛИСТ(s) +
      ЗАДАЧА('Вокруг бассейна 3×3 кладут рамку в одну плитку. Снаружи вышел квадрат 5×5. Сколько плиток ушло на рамку?') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${т(168,28,'Рамка вокруг бассейна',14,GOLD,true)}
        ${плита({x0,y0,кол:5,ряд:5,кл:к})}
        <rect x="${x0+к+3}" y="${y0+к+3}" width="${3*к-6}" height="${3*к-6}" rx="6" fill="url(#c388-лазурь)" opacity=".35"
          stroke="${ЛАЗУРЬ}" stroke-width="1.4"/>
        ${т(x0+2.5*к,y0+2.5*к+5,'вода 3 × 3',14,ИНК,true)}
        ${плитки}
        ${подпись(168,Н-12, в===1?'25 − 9 = 16':'снаружи 5 × 5, внутри 3 × 3', в===1?GREEN:МУТ,14)}
      `,Н)}</div>` +
      `<div class="ask три">
        ${['20','16','25'].map((v,к_)=>BTN(4+к_, в===к_?(к_===1?'hit':'miss'):'', v, "r388Отв7("+к_+")")).join('')}
      </div>` +
      (в==null ? СКАЗ('Вопрос','Сколько плиток в рамке?') : РАЗБОР(в===1, разборы[в])) +
      ПРАВИЛО('Иногда проще <b>вычесть</b>: всё целое минус то, чего нет.');
  }

  /* 8. Треугольник — половина прямоугольника */
  function F8(s){
    const к=36, x0=60, y0=48, Н=262;
    const в = s.ответ8;
    const A_=[x0,y0+4*к], B_=[x0+6*к,y0+4*к], C_=[x0,y0], D_=[x0+6*к,y0];
    const разборы = [
      '24 — это весь прямоугольник 6×4. Диагональ режет его на <b>два равных треугольника</b>, наш — половина: 24 : 2 = 12.',
      'Сложили стороны 6 + 4 — так считают путь по краю, а не площадь. Площадь треугольника — <b>половина прямоугольника</b> 6 · 4: 12.',
      'Дополнили до прямоугольника 6 · 4 = 24, и треугольник — ровно <b>половина: 12</b>.'
    ];
    return ЛИСТ(s) +
      ЗАДАЧА('Для узора нужен треугольник: катеты 6 и 4 клетки. Клетки режутся наискосок, и сосчитать их трудно. Как найти площадь?') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${т(168,28,'Треугольник — половина',14,GOLD,true)}
        ${плита({x0,y0,кол:6,ряд:4,кл:к})}
        <path d="M${C_[0]} ${C_[1]} L${D_[0]} ${D_[1]} L${B_[0]} ${B_[1]} Z" fill="url(#c388-лазурь)" stroke="${ОБВОД}" stroke-width="1.4" opacity=".35">
          ${анК('opacity','0.35;0.35;0.85;0.85;0.35','6s','0;0.3;0.45;0.8;1')}</path>
        <path d="M${A_[0]} ${A_[1]} L${B_[0]} ${B_[1]} L${C_[0]} ${C_[1]} Z" fill="url(#c388-терра)" stroke="${ОБВОД}" stroke-width="1.6"/>
        ${т(x0+1.7*к,y0+3*к+6,'наш',14,ОБВОД,true)}
        <g>${анК('opacity','0.4;0.4;1;1;0.4','6s','0;0.35;0.45;0.8;1')}
          ${т(x0+4.4*к,y0+1.2*к+6,'такой же',14,ИНК,true)}</g>
        ${т(x0+3*к,y0+4*к+22,'6',14,МУТ,true)}
        ${т(x0-12,y0+2*к+5,'4',14,МУТ,true)}
        ${подпись(168,Н-12, в===2?'6 · 4 : 2 = 12':'два треугольника = прямоугольник', в===2?GREEN:МУТ,14)}
      `,Н)}</div>` +
      `<div class="ask три">
        ${['24','10','12'].map((v,к_)=>BTN(4+к_, в===к_?(к_===2?'hit':'miss'):'', v, "r388Отв8("+к_+")")).join('')}
      </div>` +
      (в==null ? СКАЗ('Вопрос','Сколько клеток занимает треугольник?') : РАЗБОР(в===2, разборы[в])) +
      ПРАВИЛО('Прямоугольный треугольник на клетках — <b>половина прямоугольника</b>.');
  }

  /* 9. Доминошки на доске без двух углов */
  const П9 = {x0:80, y0:48, кол:4, ряд:4};
  function F9(s){
    const дом = s.дом9||[], Н=268;
    const выбор = s.выбор9;
    const тупик = !доминошкеЕстьМесто(дом);
    const раскрас = !!s.раскрас9;
    const в = s.ответ9;
    const фишки = дом.map((д,к)=>{
      const i=Math.min(д[0][0],д[1][0]), j=Math.min(д[0][1],д[1][1]);
      const w=д[0][0]===д[1][0]?1:2, h=w===1?2:1;
      return кусок(П9.x0,П9.y0,i,j,w,h,'url(#c388-золото)',КЛ,к===s.дом9Свежий);
    }).join('');
    const выделение = выбор ? `<rect x="${П9.x0+выбор[0]*КЛ+3}" y="${П9.y0+выбор[1]*КЛ+3}" width="${КЛ-6}" height="${КЛ-6}" rx="6"
        fill="rgba(255,215,106,.22)" stroke="${GOLD}" stroke-width="2.4">
        ${анК('opacity','1;1;0.6;1','1.6s','0;0.4;0.7;1')}</rect>` : '';
    const углы = ДОСКА_НЕТ.map(п=>`<g>
      <line x1="${П9.x0+п[0]*КЛ+10}" y1="${П9.y0+п[1]*КЛ+10}" x2="${П9.x0+п[0]*КЛ+КЛ-10}" y2="${П9.y0+п[1]*КЛ+КЛ-10}" stroke="${МУТ}" stroke-width="1.6" opacity=".5"/>
      <line x1="${П9.x0+п[0]*КЛ+КЛ-10}" y1="${П9.y0+п[1]*КЛ+10}" x2="${П9.x0+п[0]*КЛ+10}" y2="${П9.y0+п[1]*КЛ+КЛ-10}" stroke="${МУТ}" stroke-width="1.6" opacity=".5"/></g>`).join('');
    const разборы = [
      'Сколько ни пробуй — не выйдет, и это можно <b>доказать</b>, а не проверять вечно. Посмотри на раскраску: светлых 8, тёмных 6.',
      'Доминошка всегда лежит на <b>одной светлой и одной тёмной</b>. Семь доминошек закрыли бы 7 светлых и 7 тёмных, а тёмных всего 6. Значит, нельзя — доказано.',
      '14 — чётное: 14 : 2 = 7, по площади доминошек хватает. Мешает не площадь, а <b>цвет</b>: светлых 8, тёмных 6.'
    ];
    return ЛИСТ(s) +
      ЗАДАЧА('Третий заказ — задача с олимпиады. Доска 4×4 без двух противоположных углов: 14 клеток. Закрой её доминошками 1×2 — их нужно 7. <b>Тапни клетку, потом соседнюю</b> — ляжет доминошка.') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${т(168,28,'Доска без двух углов',14,GOLD,true)}
        ${плита({...П9, нет:ДОСКА_НЕТ, раскрас, тап:'r388Тап9', поверх: фишки+выделение})}
        ${углы}
        ${подпись(168,Н-14, раскрас?'светлых 8, тёмных 6':'доминошек: '+дом.length+' из 7', раскрас?GOLD:(тупик?RED:GOLD),16)}
      `,Н)}</div>` +
      (s.дом9Мимо ? РАЗБОР(false,'Доминошка кладётся на две клетки <b>рядом по стороне</b>, не наискосок. Тапни соседнюю справа, слева, сверху или снизу.') : '') +
      (тупик && !раскрас ? СКАЗ('Не выходит','Осталось '+(14-2*дом.length)+' клетки, и соседних среди них нет. Попробуй ещё — или раскрась доску, как шахматную, и посчитай цвета.') : '') +
      `<div class="рычаги">
        ${BTN(6,раскрас?'вкл':'',раскрас?'Доска раскрашена':'Раскрасить доску',"r388Раскрас()")}
        ${BTN(6,'','Убрать все',"r388Сброс('дом9')")}
      </div>` +
      (раскрас
        ? `<div class="ask">
            ${BTN(8, в===0?'miss':'', 'Не повезло — надо пробовать ещё', "r388Отв9(0)")}
            ${BTN(9, в===1?'hit':'', 'Светлых 8, тёмных 6, а доминошка закрывает одну светлую и одну тёмную', "r388Отв9(1)")}
            ${BTN(10, в===2?'miss':'', 'Клеток 14 — это нечётно', "r388Отв9(2)")}
          </div>` + (в==null ? СКАЗ('Вопрос','Почему семь доминошек не ложатся?') : РАЗБОР(в===1, разборы[в]))
        : '') +
      /* правило — только после ответа: раньше оно подсказывало разгадку */
      (в===1 ? ПРАВИЛО('Раскраска доказывает «нельзя»: <b>доминошка всегда закрывает одну светлую и одну тёмную</b>.') : '');
  }

  /* 10. Итог: мастерская сдаёт заказы */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=262, к=12;
    const фигура = (x0,y0,клетки,заливка,подп,н,cx) => `<g>${проявить('8s',0.08+н*0.16,0.16+н*0.16)}
      ${клетки.map(п=>`<rect x="${x0+п[0]*к}" y="${y0+п[1]*к}" width="${к}" height="${к}" fill="${заливка}" stroke="${ОБВОД}" stroke-width="0.8"/>`).join('')}
      ${т(cx,y0+90,подп,12,GOLD,true)}</g>`;
    const пр = (w,h,дx,дy) => { const р=[]; for(let j=0;j<h;j++) for(let i=0;i<w;i++) р.push([i+(дx||0),j+(дy||0)]); return р; };
    const ступ = [...пр(2,3,0,0),...пр(5,2,0,3)];
    const рамка = []; for(let j=0;j<5;j++) for(let i=0;i<5;i++) if(i===0||j===0||i===4||j===4) рамка.push([i,j]);
    return ЛИСТ(s) +
      ЗАДАЧА(всё
        ? 'Все три заказа сданы: плита порезана, ступенька посчитана, а про доску мастер знает то, чего не знают другие, — её не закрыть, и это доказано.'
        : 'В листе заказов отмечено не всё — вернись и доделай. А вот что мастерская уже умеет.') +
      `<div class="pic">${свг(`
        ${фон(Н)}
        ${т(168,28,'Четыре приёма мастерской',14,GOLD,true)}
        ${фигура(20,52,пр(6,4),'url(#c388-терра)','6 · 4 = 24',0,56)}
        ${фигура(104,52,ступ,'url(#c388-лазурь)','10 + 6 = 16',1,134)}
        ${фигура(182,52,рамка,'url(#c388-золото)','25 − 9 = 16',2,212)}
        <g>${проявить('8s',0.56,0.64)}
          <path d="M262 112 L322 112 L262 64 Z" fill="url(#c388-терра)" stroke="${ОБВОД}" stroke-width="1"/>
          ${т(292,142,'24 : 2 = 12',12,GOLD,true)}</g>
        ${т(56,172,'умножить',12,МУТ)}${т(134,172,'сложить',12,МУТ)}${т(212,172,'вычесть',12,МУТ)}${т(292,172,'половина',12,МУТ)}
        ${подпись(168,212,'а 4 × 4 без двух углов — нельзя',RED,14)}
        ${подпись(168,Н-14,'площадь от разрезания не меняется',GREEN,14)}
      `,Н)}</div>` +
      СКАЗ('Итог','Площадь на клетках считают <b>рядами</b>, режут на прямоугольники и <b>складывают</b>, берут целое и <b>вычитают</b> пустое, треугольник — <b>половина</b> прямоугольника. А чтобы доказать «нельзя», фигуру <b>раскрашивают</b>.') +
      ПРАВИЛО('<b>Разрезали, переложили — площадь та же.</b> Делится площадь — проверь, ложатся ли куски.');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Сколько квадратов 2×2 вырежется из плиты 8×6?',
      варианты:[{т:'14',ок:false},{т:'12',ок:true}],
      разбор:'8 · 6 = 48 клеток, квадрат — 4. Стороны 8 и 6 чётные, квадраты ложатся без остатка: 48 : 4 = 12.' },
    { вопрос:'Плиту 3×7 разрезали на полоски 1×7. Сколько полосок и какая площадь?',
      варианты:[{т:'3 полоски, 21 клетка',ок:true},{т:'7 полосок, 10 клеток',ок:false}],
      разбор:'Рядов три, в каждом 7 клеток: 3 полоски, 3 · 7 = 21.' },
    { вопрос:'Квадрат 6×6, у которого вырезали угол 3×3. Сколько клеток осталось?',
      варианты:[{т:'33',ок:false},{т:'27',ок:true}],
      разбор:'Целое минус вырезанное: 36 − 9 = 27.' },
    { вопрос:'Прямоугольный треугольник — половина прямоугольника 8×5. Какая у него площадь?',
      варианты:[{т:'20',ок:true},{т:'40',ок:false}],
      разбор:'8 · 5 = 40 — это весь прямоугольник, треугольник — половина: 20.' },
    { вопрос:'Можно ли закрыть доминошками доску 3×3?',
      варианты:[{т:'Можно, если постараться',ок:false},{т:'Нельзя: 9 клеток, а доминошка закрывает 2',ок:true}],
      разбор:'Любое число доминошек закрывает чётное число клеток, а 9 — нечётное. Значит, нельзя.' }
  ];

  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r388Reset()")}</div>` +
        ПРАВИЛО('<b>Площадь : площадь куска</b> — если куски ложатся без остатка.');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r388Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Площадь по клеткам', задания:[
    {q:'Прямоугольник 7×3. Сколько в нём клеток?', в:0, варианты:['21','10'], раз:'Три ряда по 7: 3 · 7 = 21. 10 — это сумма сторон, а не площадь.'},
    {q:'Квадрат со стороной 6. Сколько в нём клеток?', в:1, варианты:['12','36'], раз:'6 рядов по 6 клеток: 6 · 6 = 36.'},
    {q:'Две плиты 3×4 положили рядом. Общая площадь?', в:0, варианты:['24','14'], раз:'Каждая по 3 · 4 = 12, вместе 24.'},
    {q:'Ступенька: полоса 4×2, а над ней квадрат 2×2. Площадь?', в:1, варианты:['8','12'], раз:'Режем на две части и складываем: 4 · 2 + 2 · 2 = 8 + 4 = 12.'}
  ]};
  const Т2 = { имя:'Разрезать на части', задания:[
    {q:'Сколько квадратов 2×2 в плите 4×6?', в:0, варианты:['6','12'], раз:'4 · 6 = 24, квадрат — 4 клетки: 24 : 4 = 6. Обе стороны чётные — ложатся без остатка.'},
    {q:'Из плиты 7×2 режут квадраты 2×2. Что выйдет?', в:0, варианты:['3 квадрата и полоска 1×2','Ровно 4 квадрата'], раз:'В длину 7 : 2 = 3 и 1 в остатке: три квадрата, и остаётся полоска 1×2.'},
    {q:'Плиту 4×5 режут на полоски 1×4. Сколько полосок?', в:1, варианты:['4','5'], раз:'Пять рядов по 4 клетки — пять полосок: 20 : 4 = 5.'},
    {q:'Плиту 4×3 переложили в столбик 2×6. Площадь…', в:1, варианты:['стала больше','та же'], раз:'Клетки не добавлялись и не пропадали: 12 = 12.'}
  ]};
  const Т3 = { имя:'Олимпиадные ловушки', задания:[
    {q:'Доска 4×4 без двух противоположных углов. Хватит 7 доминошек?', в:1, варианты:['Да','Нет'], раз:'Светлых клеток 8, тёмных 6, а доминошка закрывает одну светлую и одну тёмную. Семь не лягут.'},
    {q:'Доминошка на шахматной доске всегда закрывает…', в:0, варианты:['светлую и тёмную','две одного цвета'], раз:'Соседние по стороне клетки шахматной доски всегда разного цвета.'},
    {q:'Рамка в одну клетку вокруг квадрата 4×4 (снаружи 6×6). Сколько в ней клеток?', в:0, варианты:['20','24'], раз:'36 − 16 = 20. 24 — это «по 6 на сторону», где угловые посчитаны дважды.'},
    {q:'Можно ли доминошками закрыть доску 5×5?', в:1, варианты:['Можно','Нельзя'], раз:'25 клеток — нечётно, а доминошки закрывают чётное число клеток.'}
  ]};

  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r388T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r388TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L388 = {
    id: ID,
    title: 'Разрезания и площади',
    ico: '▦',
    src: 'Математика · 5–6 класс · Олимп-5: клетки',
    subj: 'math',
    explain: [
      'Мастерская мозаики в Сиракузах. Площадь фигуры на клетках — это сколько в ней клеток. Режем только по линиям клеток, а разрезанное и переложенное сохраняет площадь. Говорят, на этом Архимед построил головоломку «стомахион».',
      'Прямоугольник считают рядами: в каждом ряду столько клеток, какова ширина, а рядов — сколько высота. Площадь равна ширине, умноженной на высоту.',
      'Плита 6 на 4 — это 24 клетки, квадрат 2 на 2 — 4 клетки. Порежь сам: выйдет 24 : 4 = 6 квадратов, ровно, без остатка.',
      'Ловушка. Плита 5 на 4 — 20 клеток, и 20 : 4 = 5. Но квадратов 2 на 2 вырезается только четыре: в ширину пять клеток, и остаётся полоска в одну клетку.',
      'Плиту 4 на 3 распилили на две части 2 на 3 и переложили в столбик 2 на 6. Клетки те же — площадь та же: 12.',
      'Ступенька — не прямоугольник. Режем её на два прямоугольника: 10 и 6 клеток, всего 16. Как ни режь, сумма одна.',
      'Рамка вокруг бассейна: весь квадрат 5 на 5 — 25 клеток, дырка 3 на 3 — 9. В рамке 25 − 9 = 16. Если считать по пять на каждую сторону, угловые плитки попадут в счёт дважды.',
      'Прямоугольный треугольник на клетках — половина прямоугольника. Прямоугольник 6 на 4 — это 24 клетки, треугольник — 12.',
      'Олимпиадная задача: доску 4 на 4 без двух противоположных углов не закрыть доминошками. Раскрась её, как шахматную: светлых 8, тёмных 6, а доминошка закрывает одну светлую и одну тёмную.',
      'Итог: площадь на клетках считают рядами, складывают части, вычитают пустое, треугольник — половина прямоугольника. Раскраска доказывает, что разрезать нельзя.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: площадь по клеткам.',
      'Тренажёр 2: разрезать на части.',
      'Тренажёр 3: олимпиадные ловушки.'
    ],
    check: {
      q: 'Плиту 6×4 разрезали на квадраты 2×2. Сколько квадратов?',
      choices: ['8','6','12','4'],
      ans: 1,
      exp: 'Площадь плиты 24, квадрата 4: 24 : 4 = 6. Обе стороны чётные — квадраты ложатся без остатка.'
    },
    tasks: [
      { q:'Площадь фигуры из 3 полосок по 5 клеток?', kind:'unit', ans:15, tol:0,
        hints:['Каждая полоска — 5 клеток.','3 · 5 = 15.'], sol:'3 · 5 = 15 клеток.' },
      { q:'Сколько квадратов 2×2 вырежется из плиты 5×4?', kind:'choice',
        choices:['4','5'], ans:0,
        hints:['По площади 20 : 4 = 5 — но проверь стороны.','В ширину 5 : 2 = 2 и 1 в остатке.'],
        sol:'4 квадрата, и остаётся полоска 1×4.' },
      { q:'Рамка в одну клетку: снаружи 5×5, внутри пусто 3×3. Сколько клеток в рамке?', kind:'unit', ans:16, tol:0,
        hints:['Весь квадрат минус дырка.','25 − 9.'], sol:'25 − 9 = 16.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L388.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1);
    else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);

    const ЗАГОЛОВКИ={
      1:'Мастерская мозаики', 2:'Считаем рядами', 3:'Режем плиту', 4:'Ловушка: 5 × 4',
      5:'Переложили', 6:'Ступенька', 7:'Рамка', 8:'Треугольник',
      9:'Доска без двух углов', 10:'Заказы сданы', 11:'Практика',
      12:'Тренажёр 1', 13:'Тренажёр 2', 14:'Тренажёр 3'
    };
    el.innerHTML = `<div class="s6 l388" data-frame="${f}">
        <h2>${ЗАГОЛОВКИ[f]||'Разрезания и площади'}</h2>
        ${сцена}
      </div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r388Размер=(dx,dy)=>{ const s=S();
    s.ширина=Math.max(1,Math.min(7,(s.ширина||4)+dx)); s.высота=Math.max(1,Math.min(5,(s.высота||3)+dy)); chRender(0); };
  window.r388Тап3=(i,j)=>{ const s=S(); if((s.кв3||[]).length===6) return; тапКвадрат(s,'кв3',6,4,i,j);
    if((s.кв3||[]).length===6) s.дело_квадраты=true; chRender(0); };
  window.r388Тап4=(i,j)=>{ const s=S(); тапКвадрат(s,'кв4',5,4,i,j); s.ответ4=null; chRender(0); };
  window.r388Сброс=(кл)=>{ const s=S(); s[кл]=[]; s[кл+'Мимо']=null; s[кл+'Свежий']=-1;
    if(кл==='дом9'){ s.выбор9=null; } chRender(0); };
  window.r388Отв4=(к)=>{ S().ответ4=к; chRender(0); };
  window.r388Отв5=(к)=>{ S().ответ5=к; chRender(0); };
  window.r388Разрез=(р)=>{ S().разрез6=р; chRender(0); };
  window.r388Отв6=(к)=>{ const s=S(); s.ответ6=к; if(к===1) s.дело_ступенька=true; chRender(0); };
  window.r388Отв7=(к)=>{ S().ответ7=к; chRender(0); };
  window.r388Отв8=(к)=>{ S().ответ8=к; chRender(0); };
  window.r388Тап9=(i,j)=>{
    const s=S(); const дом=s.дом9||(s.дом9=[]); s.дом9Мимо=false;
    const под=подДоминошкой(дом,i,j);
    if(под>=0){ дом.splice(под,1); s.выбор9=null; s.дом9Свежий=-1; chRender(0); return; }
    const в=s.выбор9;
    if(!в){ s.выбор9=[i,j]; chRender(0); return; }
    if(в[0]===i && в[1]===j){ s.выбор9=null; chRender(0); return; }
    if(Math.abs(в[0]-i)+Math.abs(в[1]-j)===1){ дом.push([[в[0],в[1]],[i,j]]); s.выбор9=null; s.дом9Свежий=дом.length-1; }
    else { if(Math.abs(в[0]-i)===1 && Math.abs(в[1]-j)===1) s.дом9Мимо=true; s.выбор9=[i,j]; }
    chRender(0);
  };
  window.r388Раскрас=()=>{ S().раскрас9=true; chRender(0); };
  window.r388Отв9=(к)=>{ const s=S(); s.ответ9=к; if(к===1) s.дело_доминошки=true; chRender(0); };
  window.r388Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r388Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r388T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=(s[ключ+'Шаг']||0)%набор.задания.length;
    const з=набор.задания[шаг];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r388TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  /* прежний урок 388 (vis_wk.js) регистрируется раньше — этот файл
     подключён после него и перерегистрируется ещё раз на load */
  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L388; else arr.push(L388);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA388={render:render, L:L388};
})();
