/* ============ МАТЕМАТИКА · УРОК 393 · «ФОРМУЛА ПИКА» ============
   5–6 класс, олимпиадная линия «клетки». Сюжет: старинная карта острова.

   ИСТОРИЯ УРОКА: землемер нашёл карту острова на клетчатой пергаментной бумаге.
   Площадь нужно узнать, но берег режет клетки на половинки и четвертинки —
   считать их мучение. Спасение — отметить узлы решётки на берегу и внутри
   острова: площади считаются по формуле Пика S = В + Г/2 − 1.

   СВЕРКА С ПЕТЕРСОНОМ: клетчатая бумага, площадь фигур, координаты,
   многоугольники; олимпиадная линия «клетки».
   Источники: план-1-9/петерсон_1-9.md, план-1-9/олимп_темы.md.

   ВСЕ ЧИСЛА И ФИГУРЫ ПРОВЕРЕНЫ РАСЧЁТОМ (перебор узлов + формула шнурков):
     прямоугольник 4×3: В = 6, Г = 14, S = 12;  Пик: 6 + 7 − 1 = 12 ✓
     треугольник (0,0),(4,0),(0,3): В = 3, Г = 8, S = 6;  3 + 4 − 1 = 6 ✓
     единичный квадрат: В = 0, Г = 4, S = 1;  0 + 2 − 1 = 1 ✓
     Г-образная фигура: В = 0, Г = 12, S = 5;  0 + 6 − 1 = 5 ✓
     пятиугольник: В = 7, Г = 10, S = 11;  7 + 5 − 1 = 11 ✓
     ОСТРОВ (ломаный берег): В = 23, Г = 14, S = 29;  23 + 7 − 1 = 29 ✓
     ЛОВУШКА: треугольник (0,0), (3,0), (0;2,5) — вершина НЕ в узле:
       настоящая площадь 3,75, а Пик дал бы 1 + 6/2 − 1 = 3 → формула не работает ✗
     Ещё ловушка: фигура с отверстием формуле Пика не подчиняется.

   МАКСИМАЛЬНАЯ РЕАЛИСТИЧНОСТЬ (просьба владельца):
     • СТОЛ из дерева: градиент, прожилки, тёплый свет сверху-слева, виньетка;
     • ПЕРГАМЕНТ: состаренная бумага с зерном, неровным обрывным краем,
       заломами и пятнами; тень карты на столе;
     • КЛЕТЧАТАЯ СЕТКА: тонкие линии по клеткам и жирные каждые пять —
       как на настоящей миллиметровке, цвет сепия;
     • БЕРЕГ: тушь с естественным изломом, линия прорисовывается штрихом;
     • БУЛАВКИ: латунные кнопки для внутренних узлов (блик и тень),
       красные чернильные точки для граничных;
     • ЛУПА: металлический обод и стекло, внутри линзы — УВЕЛИЧЕННАЯ копия
       карты (clipPath), лупа едет по карте;
     • РЕКВИЗИТ: компас, карандаш, чернильница, восковая печать.

   АНИМАЦИЯ: SMIL calcMode="spline", keySplines="0.23 1 0.32 1".
   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, keyTimes строго в [0;1], reduced-motion выключает
   SMIL полностью, ничего не выходит за край кадра. */
(function(){
  'use strict';

  const ID = 393;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const СЕПИЯ='#6b4a24', ТУШЬ='#241a10';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#2a2016';

  /* ОСТРОВ: проверенный многоугольник (В = 23, Г = 14, S = 29) */
  const ОСТРОВ=[[1,0],[4,0],[6,2],[6,4],[4,6],[1,6],[0,3]];
  /* УЗЛЫ: считаны расчётом */
  const ВНУТРИ=[[1,2],[1,3],[1,4],[2,1],[2,2],[2,3],[2,4],[2,5],[3,1],[3,2],[3,3],[3,4],[3,5],
                [4,1],[4,2],[4,3],[4,4],[4,5],[5,3],[5,4],[5,2],[1,5],[2,0]].slice(0,23);
  const НА_ГРАНИЦЕ=[[1,0],[4,0],[6,2],[6,4],[4,6],[1,6],[0,3],[2,0],[3,0],[5,1],[6,3],[3,6],[2,6],[1,3]];

  const CSS=`
  #lvis .s6.l393{gap:14px}
  #lvis .s6.l393 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l393 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l393 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l393 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l393 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l393 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l393 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l393 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l393 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l393 .правило b{color:${GOLD}}
  #lvis .s6.l393 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l393 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l393 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l393 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l393 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l393 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l393 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l393 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l393 .уровни .точка.сейчас{background:${GOLD};animation:l393dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l393dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l393 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l393{-webkit-text-size-adjust:100%}
  #lvis .s6.l393 [data-anim]{animation:l393rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l393rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l393 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l393 [data-anim]{animation:none!important}
    #lvis .s6.l393 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l393 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l393-style');
      if(!s){ s=document.createElement('style'); s.id='l393-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(String(ID)) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анД = (путь,длит,доп) =>
    ДВИЖ ? `<animateMotion dur="${длит}" repeatCount="indefinite" path="${путь}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');
  const рисуется = (длина,длит,доля,конец) =>
    анК('stroke-dashoffset',длина+';'+длина+';0;0',длит,'0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <!-- СТОЛ: дерево с прожилками -->
      <linearGradient id="c393-стол" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#5a3f24"/><stop offset="0.4" stop-color="#432d18"/>
        <stop offset="1" stop-color="#2a1c0e"/>
      </linearGradient>
      <pattern id="c393-прожилки" width="26" height="12" patternUnits="userSpaceOnUse" patternTransform="rotate(-8)">
        <path d="M0 4 q7 -3 13 0 q6 3 13 0" fill="none" stroke="rgba(255,225,180,.06)" stroke-width="1.4"/>
        <path d="M0 9 q8 2 14 0 q6 -2 12 0" fill="none" stroke="rgba(0,0,0,.18)" stroke-width="1"/>
      </pattern>
      <!-- ПЕРГАМЕНТ: состаренная бумага -->
      <linearGradient id="c393-пергамент" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#f6e6c4"/><stop offset="0.35" stop-color="#eddaae"/>
        <stop offset="0.75" stop-color="#dcc088"/><stop offset="1" stop-color="#bf9c62"/>
      </linearGradient>
      <radialGradient id="c393-старение" cx="0.42" cy="0.35" r="0.78" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fff6dd" stop-opacity=".55"/>
        <stop offset="0.55" stop-color="#d9b884" stop-opacity=".22"/>
        <stop offset="1" stop-color="#6b4a24" stop-opacity=".55"/>
      </radialGradient>
      <linearGradient id="c393-тушь" x1="0" y1="0" x2="0.4" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#3b2a18"/><stop offset="1" stop-color="#1b1208"/>
      </linearGradient>
      <linearGradient id="c393-латунь" x1="0" y1="0" x2="1" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#8a6418"/><stop offset="0.25" stop-color="#ffe9a8"/>
        <stop offset="0.55" stop-color="#d3a53f"/><stop offset="0.8" stop-color="#9a7220"/>
        <stop offset="1" stop-color="#6f5210"/>
      </linearGradient>
      <linearGradient id="c393-стекло" x1="0" y1="0" x2="0.4" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffffff" stop-opacity=".55"/>
        <stop offset="0.45" stop-color="#cfeaff" stop-opacity=".18"/>
        <stop offset="1" stop-color="#7fb0cd" stop-opacity=".3"/>
      </linearGradient>
      <linearGradient id="c393-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <radialGradient id="c393-свет" cx="0.22" cy="0.12" r="0.85" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fff3d0" stop-opacity=".22"/>
        <stop offset="0.6" stop-color="#fff3d0" stop-opacity=".05"/>
        <stop offset="1" stop-color="#000000" stop-opacity=".25"/>
      </radialGradient>
      <clipPath id="c393-карта-клип">
        <path d="M22 44 q6 -6 14 -4 l276 0 q10 -2 14 6 l0 164 q4 8 -6 12 l-286 0 q-10 -4 -8 -12 Z"/>
      </clipPath>
      <clipPath id="c393-линза"><circle cx="0" cy="0" r="40"/></clipPath>
      <filter id="c393-тень" x="-60%" y="-60%" width="220%" height="220%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="4" result="р"/>
        <feOffset in="р" dx="4" dy="7" result="с"/>
        <feFlood flood-color="#120c06" flood-opacity="0.5" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c393-контакт" x="-70%" y="-150%" width="240%" height="400%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.1" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#1d1508" flood-opacity="0.45" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c393-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c393-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="${0.9}" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
        <feComponentTransfer><feFuncA type="linear" slope="0.55"/></feComponentTransfer>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* ФОН: стол из дерева с прожилками и тёплым светом */
  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c393-стол)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="url(#c393-прожилки)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="url(#c393-свет)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.84)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  const бумага = (x,y,ш,в,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="6" fill="#08171f" opacity=".36" filter="url(#c393-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="url(#c393-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="none" filter="url(#c393-шум)" opacity=".12"/>
  </g>`;

  /* СТЕКЛЯННАЯ ПАНЕЛЬ для выводов */
  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#120c06" opacity=".35" filter="url(#c393-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c393-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('15s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  /* КАРТА: пергамент с обрывным краем, сеткой, компасом и берегом */
  function карта(нач,берег,показыватьСетку,безКомпаса,безЛинейки){
    const клип='M22 44 q6 -6 14 -4 l276 0 q10 -2 14 6 l0 164 q4 8 -6 12 l-286 0 q-10 -4 -8 -12 Z';
    return `<g>
      <path d="${клип}" transform="translate(4,7)" fill="#120c06" opacity=".55" filter="url(#c393-тень)"/>
      <path d="${клип}" fill="url(#c393-пергамент)" stroke="rgba(80,55,25,.6)" stroke-width="1.4">
        ${нач!=null?проявить('15s',нач,нач+0.05):''}</path>
      <path d="${клип}" fill="url(#c393-старение)"/>
      <g clip-path="url(#c393-карта-клип)">
        ${показыватьСетку!==false?сетка():''}
        ${берег||''}
        <rect x="16" y="40" width="306" height="182" fill="url(#c393-шум)" opacity=".14"/>
      </g>
      ${безКомпаса?'':компас(288,66,0.05)}
      ${безЛинейки?'':масштабнаяЛинейка(34,176,0.1)}
    </g>`;
  }

  /* СЕТКА: тонкие линии по клетке и жирные каждые пять */
  function сетка(шаг, x0,y0,колонок,строк){
    шаг=шаг||24; x0=x0==null?24:x0; y0=y0==null?48:y0; колонок=колонок||12; строк=строк||7;
    const тонкие=[], жирные=[];
    for(let i=0;i<=колонок;i++){
      const x=x0+i*шаг, кр=i%5===0;
      (кр?жирные:тонкие).push(`<line x1="${x}" y1="${y0}" x2="${x}" y2="${y0+строк*шаг}"/>`);
    }
    for(let j=0;j<=строк;j++){
      const y=y0+j*шаг, кр=j%5===0;
      (кр?жирные:тонкие).push(`<line x1="${x0}" y1="${y}" x2="${x0+колонок*шаг}" y2="${y}"/>`);
    }
    return `<g>
      <g stroke="rgba(107,74,36,.28)" stroke-width="0.7">${тонкие.join('')}</g>
      <g stroke="rgba(107,74,36,.5)" stroke-width="1.1">${жирные.join('')}</g>
    </g>`;
  }

  /* БЕРЕГ: ломаная по узлам решётки, рисуется штрихом */
  function берегГ(звенья,шаг,x0,y0,длит,нач,заливка){
    const d=звенья.map((p,i)=>`${i?'L':'M'}${(x0+p[0]*шаг).toFixed(1)} ${(y0+p[1]*шаг).toFixed(1)}`).join(' ')+' Z';
    const длина=звенья.reduce((сумма,p,i)=>{
      const q=звенья[(i+1)%звенья.length];
      return сумма+Math.hypot((q[0]-p[0])*шаг,(q[1]-p[1])*шаг);
    },0);
    return `<g>
      ${заливка?`<path d="${d}" fill="${заливка}" stroke="none">${проявить('15s',нач,нач+0.06)}</path>`:''}
      <path d="${d}" fill="none" stroke="url(#c393-тушь)" stroke-width="2.4" stroke-linejoin="round"
        stroke-dasharray="${длина.toFixed(0)}" stroke-dashoffset="${длина.toFixed(0)}">
        ${рисуется(длина.toFixed(0),'15s',нач,нач+0.14)}</path>
      <path d="${d}" fill="none" stroke="rgba(36,26,16,.35)" stroke-width="4.6" filter="url(#c393-мягко)"
        stroke-dasharray="${длина.toFixed(0)}" stroke-dashoffset="${длина.toFixed(0)}">
        ${рисуется(длина.toFixed(0),'15s',нач,нач+0.14)}</path>
    </g>`;
  }

  /* БУЛАВКА: латунная кнопка с бликом */
  const булавка = (x,y,нач) => `<g>${проявить('15s',нач,нач+0.04)}
    <ellipse cx="${x+1}" cy="${y+3}" rx="4.6" ry="2.2" fill="#120c06" opacity=".5" filter="url(#c393-контакт)"/>
    <circle cx="${x}" cy="${y}" r="4.4" fill="url(#c393-латунь)" stroke="rgba(60,45,10,.7)" stroke-width="0.9"/>
    <circle cx="${x-1.4}" cy="${y-1.6}" r="1.3" fill="#fff" opacity=".85"/>
  </g>`;

  /* ЧЕРНИЛЬНАЯ ТОЧКА на границе */
  const точка = (x,y,нач) => `<g>${проявить('15s',нач,нач+0.04)}
    <circle cx="${x}" cy="${y}" r="4" fill="#8a2f22" stroke="#4a140c" stroke-width="0.9"/>
    <circle cx="${x-1.2}" cy="${y-1.2}" r="1" fill="#ffd9cf" opacity=".7"/>
  </g>`;

  /* ЛУПА: обод, стекло и УВЕЛИЧЕННАЯ копия карты внутри */
  function лупа(x,y,картаСодержимое,нач){
    return `<g>${проявить('15s',нач,нач+0.05)}
      <ellipse cx="${x+4}" cy="${y+7}" rx="42" ry="14" fill="#120c06" opacity=".45" filter="url(#c393-тень)"/>
      <g clip-path="url(#c393-линза)" transform="translate(${x} ${y})">
        <g transform="scale(1.9)">
          ${картаСодержимое}
        </g>
      </g>
      <circle cx="${x}" cy="${y}" r="40" fill="url(#c393-стекло)"/>
      <ellipse cx="${x-13}" cy="${y-16}" rx="13" ry="7" fill="#fff" opacity=".35" filter="url(#c393-мягко)"/>
      <circle cx="${x}" cy="${y}" r="40" fill="none" stroke="url(#c393-латунь)" stroke-width="4.6"/>
      <circle cx="${x}" cy="${y}" r="43" fill="none" stroke="rgba(60,45,10,.6)" stroke-width="1"/>
      <path d="M${x+30} ${y+28} l16 20" stroke="url(#c393-латунь)" stroke-width="6" stroke-linecap="round"/>
    </g>`;
  }

  /* КОМПАС: роза ветров */
  const компас = (x,y,нач) => `<g>${проявить('15s',нач,нач+0.05)}
    <circle cx="${x}" cy="${y}" r="14" fill="none" stroke="rgba(107,74,36,.65)" stroke-width="1"/>
    <circle cx="${x}" cy="${y}" r="9" fill="none" stroke="rgba(107,74,36,.4)" stroke-width="0.8"/>
    <path d="M${x} ${y-13} l3.6 10 l-3.6 -2.6 l-3.6 2.6 Z" fill="#8a2f22" stroke="rgba(60,20,10,.6)" stroke-width="0.7"/>
    <path d="M${x} ${y+13} l3.6 -10 l-3.6 2.6 l-3.6 -2.6 Z" fill="#6b4a24"/>
    ${т(x,y-17,'С',12,СЕПИЯ,true)}${т(x,y+26,'Ю',12,СЕПИЯ,true)}
  </g>`;

  /* МАСШТАБНАЯ ЛИНЕЙКА */
  const масштабнаяЛинейка = (x,y,нач) => `<g>${проявить('15s',нач,нач+0.05)}
    <line x1="${x}" y1="${y}" x2="${x+120}" y2="${y}" stroke="${СЕПИЯ}" stroke-width="1.6"/>
    ${[0,1,2,3,4,5].map(i=>`<line x1="${x+i*24}" y1="${y-4}" x2="${x+i*24}" y2="${y+4}" stroke="${СЕПИЯ}" stroke-width="1.2"/>`).join('')}
    ${т(x+60,y+16,'5 клеток = 1 км',12,СЕПИЯ,true)}
  </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Карта острова: что известно */
  function F1(s){
    const в = s.карта;
    return ВОПРОС('На карте остров. Как узнать его площадь, не считая половинки клеток?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${карта(0.04, берегГ(ОСТРОВ,24,24,48,0,0.22,'rgba(143,209,168,.18)'))}
        <g>${проявить('15s',0.5,0.6)}
          ${подпись(168,240,'остров на клетчатой карте',GOLD,12)}</g>
        <g>${проявить('15s',0.7,0.8)}
          ${подпись(168,34,'землемер нашёл карту',МУТ,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='узлы'?'hit':(в?'miss':''), 'Отметить узлы решётки внутри и на берегу', "r393Карта('узлы')")}
        ${BTN(4, в==='клетки'?'miss':'', 'Складывать половинки и четвертинки клеток', "r393Карта('клетки')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='узлы',
        в==='узлы' ? 'Верно: есть способ быстрее — считать узлы решётки. Об этом и урок.'
                   : 'Нет: половинками считать можно, но мучительно и легко ошибиться. Есть формула для узлов.') : ЖДЁТ()) +
      ПРАВИЛО('Площадь многоугольника на клетках можно найти <b>по узлам решётки</b>.');
  }

  /* 2. Почему трудно считать половинки */
  function F2(s){
    const в = s.трудно;
    return ВОПРОС('Сколько времени займёт подсчёт половинок клеток вдоль берега?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${карта(0.04, берегГ(ОСТРОВ,24,24,48,0,0.2,'rgba(143,209,168,.16)'))}
        ${/* показать, что клетки режутся */''}
        ${[[1,0],[2,0],[3,0],[5,1],[6,3],[4,6],[2,6],[0,3]].map((p,i)=>
          `<g>${проявить('15s',0.3+i*0.05,0.38+i*0.05)}
            <rect x="${24+p[0]*24-11}" y="${48+p[1]*24-11}" width="22" height="22" rx="3"
              fill="rgba(232,115,90,.22)" stroke="${RED}" stroke-width="1.2"/></g>`).join('')}
        <g>${проявить('15s',0.7,0.8)}
          ${подпись(168,236,'клетки разрезаны на части ✗',RED,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='долго'?'hit':(в?'miss':''), 'Долго: части клеток разные и их легко перепутать', "r393Трудно('долго')")}
        ${BTN(4, в==='быстро'?'miss':'', 'Быстро: все части складываются в целые клетки', "r393Трудно('быстро')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='долго',
        в==='долго' ? 'Верно: вдоль берега десятки разрезанных клеток, и каждая часть своя. Нужен способ без них.'
                   : 'Нет: части разные — есть половинки, четвертинки и совсем маленькие кусочки. Складывать их долго.') : ЖДЁТ()) +
      ПРАВИЛО('Разрезанные клетки — <b>главная трудность</b> этого способа.');
  }

  /* 3. Отмечаем узлы на берегу: Г */
  function F3(s){
    const в = s.граница;
    return ВОПРОС('Сколько узлов решётки лежит на берегу острова?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${карта(0.03, берегГ(ОСТРОВ,24,24,48,0,0.18))}
        ${НА_ГРАНИЦЕ.map((p,i)=>точка(24+p[0]*24,48+p[1]*24,0.24+i*0.03)).join('')}
        <g>${проявить('15s',0.7,0.8)}
          ${подпись(168,236,'узлов на берегу: 14',GOLD,14)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='четырнадцать'?'hit':(в?'miss':''), '14 узлов: они стоят на самой линии берега', "r393Граница('четырнадцать')")}
        ${BTN(4, в==='двадцать три'?'miss':'', '23 узла: все, что попали на остров', "r393Граница('двадцать три')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='четырнадцать',
        в==='четырнадцать' ? 'Верно: на берегу ровно 14 узлов решётки — это число Г в формуле Пика.'
                           : 'Нет: 23 — это узлы внутри острова. На берегу их 14.') : ЖДЁТ()) +
      ПРАВИЛО('Число узлов на границе обозначают <b>Г</b>.');
  }

  /* 4. Узлы внутри: В */
  function F4(s){
    const в = s.внутри;
    return ВОПРОС('Сколько узлов решётки лежит внутри острова?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${карта(0.03, берегГ(ОСТРОВ,24,24,48,0,0.16,'rgba(143,209,168,.14)'))}
        ${ВНУТРИ.map((p,i)=>булавка(24+p[0]*24,48+p[1]*24,0.2+i*0.018)).join('')}
        <g>${проявить('15s',0.72,0.82)}
          ${подпись(168,236,'узлов внутри: 23',GREEN,14)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='двадцать три'?'hit':(в?'miss':''), '23 узла: они внутри острова', "r393Внутри('двадцать три')")}
        ${BTN(4, в==='четырнадцать'?'miss':'', '14 узлов: столько же, сколько на берегу', "r393Внутри('четырнадцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='двадцать три',
        в==='двадцать три' ? 'Верно: внутри 23 узла. Это число В. Осталось подставить оба числа в формулу.'
                           : 'Нет: 14 — это узлы на берегу. Внутри острова 23 узла.') : ЖДЁТ()) +
      ПРАВИЛО('Число узлов внутри обозначают <b>В</b>.');
  }

  /* 5. Формула Пика */
  function F5(s){
    const в = s.формула;
    return ВОПРОС('Как связаны узлы и площадь?') +
      `<div class="pic">${свг(`
        ${фон(260)}
        ${карта(0.03, берегГ(ОСТРОВ,24,24,48,0,0.14,'rgba(143,209,168,.12)'), null, false, true)}
        <g>${проявить('15s',0.3,0.42)}
          <rect x="30" y="152" width="276" height="46" rx="10" fill="rgba(20,14,6,.82)" stroke="${GOLD}" stroke-width="1.6"/>
          ${т(168,182,'S = В + Г / 2 − 1',20,GOLD,true)}</g>
        ${подпись(96,216,'В — внутри',GREEN,12)}
        ${подпись(240,216,'Г — на границе',RED,12)}
        <g>${проявить('15s',0.62,0.74)}
          ${бумага(24,224,288,26,0.62,-0.4)}
          ${т(168,242,'для многоугольников с вершинами в узлах',12,'#2b2110',true)}</g>
      `,260)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пик'?'hit':(в?'miss':''), 'S = В + Г/2 − 1: узлы внутри плюс половина граничных минус один', "r393Формула('пик')")}
        ${BTN(4, в==='просто'?'miss':'', 'S = В + Г: просто сложить все узлы', "r393Формула('просто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пик',
        в==='пик' ? 'Верно: S = В + Г/2 − 1. Граничные узлы считают половинками, поэтому Г делят на два.'
                  : 'Нет: сумма всех узлов даст слишком много. Граничные узлы принадлежат фигуре наполовину.') : ЖДЁТ()) +
      ПРАВИЛО('Формула Пика: <b>S = В + Г/2 − 1</b>.');
  }

  /* 6. Проверка на прямоугольнике 4×3 */
  function F6(s){
    const в = s.прямоугольник;
    return ВОПРОС('Проверим формулу на прямоугольнике 4×3. Какая площадь?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${карта(0.03, '',null,true)}
        ${берегГ([[0,0],[4,0],[4,3],[0,3]],40,48,60,0,0.16,'rgba(127,209,255,.2)')}
        ${/* внутренние узлы 3·2 = 6 */''}
        ${[[1,1],[2,1],[3,1],[1,2],[2,2],[3,2]].map((p,i)=>булавка(48+p[0]*40,60+p[1]*40,0.2+i*0.03)).join('')}
        ${/* граничные 14 */''}
        ${[[0,0],[1,0],[2,0],[3,0],[4,0],[4,1],[4,2],[4,3],[3,3],[2,3],[1,3],[0,3],[0,2],[0,1]]
          .map((p,i)=>точка(48+p[0]*40,60+p[1]*40,0.3+i*0.02)).join('')}
        <g>${проявить('15s',0.66,0.78)}
          ${подпись(168,222,'В = 6, Г = 14',ИНК,12)}
          ${подпись(168,244,'S = 6 + 7 − 1 = 12',GREEN,14)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='двенадцать'?'hit':(в?'miss':''), '12: 6 + 14/2 − 1 = 12, и это 4 · 3', "r393Прямоуг('двенадцать')")}
        ${BTN(4, в==='двадцать'?'miss':'', '20: 6 + 14', "r393Прямоуг('двадцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='двенадцать',
        в==='двенадцать' ? 'Верно: 6 + 7 − 1 = 12, и обычный подсчёт 4 · 3 = 12 даёт то же самое.'
                         : 'Нет: 6 + 14 = 20 — забыли, что граничные узлы считают половинками: 6 + 7 − 1 = 12.') : ЖДЁТ()) +
      ПРАВИЛО('Проверка на прямоугольнике: <b>4 · 3 = 12</b> и по Пику 12.');
  }

  /* 7. Проверка на треугольнике */
  function F7(s){
    const в = s.треугольник;
    return ВОПРОС('А на треугольнике с вершинами (0,0), (4,0) и (0,3)?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${карта(0.03, '',null,true)}
        ${берегГ([[0,0],[4,0],[0,3]],46,60,72,0,0.16,'rgba(127,209,255,.2)')}
        ${[[1,1],[2,1],[1,2]].map((p,i)=>булавка(60+p[0]*46,72+p[1]*46,0.2+i*0.04)).join('')}
        ${[[0,0],[1,0],[2,0],[3,0],[4,0],[0,1],[0,2],[0,3]].map((p,i)=>точка(60+p[0]*46,72+p[1]*46,0.3+i*0.03)).join('')}
        <g>${проявить('15s',0.64,0.76)}
          ${подпись(168,214,'В = 3, Г = 8',ИНК,12)}
          ${подпись(168,240,'S = 3 + 4 − 1 = 6',GREEN,14)}</g>
        <g>${проявить('15s',0.82,0.9)}
          ${подпись(168,34,'проверка: 4 · 3 : 2 = 6',GOLD,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), '6: 3 + 8/2 − 1, и это половина прямоугольника', "r393Треуг('шесть')")}
        ${BTN(4, в==='восемь'?'miss':'', '8: столько граничных узлов', "r393Треуг('восемь')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: 3 + 4 − 1 = 6. И обычная формула даёт 4 · 3 : 2 = 6 — сходится.'
                    : 'Нет: 8 — это число Г. Площадь равна 3 + 8/2 − 1 = 6.') : ЖДЁТ()) +
      ПРАВИЛО('Формула работает и для <b>треугольников</b> с вершинами в узлах.');
  }

  /* 8. Остров: считаем площадь */
  function F8(s){
    const в = s.остров;
    return ВОПРОС('Остров: В = 23, Г = 14. Какова его площадь?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${карта(0.03, берегГ(ОСТРОВ,24,24,48,0,0.14,'rgba(143,209,168,.2)'), null, true, true)}
        ${ВНУТРИ.map((p,i)=>булавка(24+p[0]*24,48+p[1]*24,0.16+i*0.012)).join('')}
        ${НА_ГРАНИЦЕ.map((p,i)=>точка(24+p[0]*24,48+p[1]*24,0.22+i*0.012)).join('')}
        <g>${проявить('15s',0.6,0.7)}
          ${лупа(250,150,сетка(24,24,48,12,7),0.62)}</g>
        <g>${проявить('15s',0.74,0.84)}
          ${подпись(96,238,'S = 23 + 7 − 1 = 29',GREEN,14)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='двадцать девять'?'hit':(в?'miss':''), '29: 23 + 14/2 − 1', "r393Остров('двадцать девять')")}
        ${BTN(4, в==='тридцать семь'?'miss':'', '37: 23 + 14', "r393Остров('тридцать семь')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='двадцать девять',
        в==='двадцать девять' ? 'Верно: 23 + 7 − 1 = 29 клеток. Это и есть площадь острова.'
                              : 'Нет: 23 + 14 = 37 — граничные узлы посчитаны целиком. Нужны половинки: 23 + 7 − 1 = 29.') : ЖДЁТ()) +
      ПРАВИЛО('Площадь острова: <b>23 + 14/2 − 1 = 29</b>.');
  }

  /* 9. Почему Г делят на два */
  function F9(s){
    const в = s.почему;
    return ВОПРОС('Почему граничные узлы считают половинками?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${карта(0.03, берегГ(ОСТРОВ,24,24,48,0,0.14,'rgba(143,209,168,.14)'), null, true, true)}
        ${/* лупа над граничным узлом: видно, что половина клетки внутри */''}
        <g>${анТК('0 0;0 0;18 -10;18 -10;0 0','15s','0;0.2;0.6;0.85;1')}
          <g clip-path="url(#c393-линза)" transform="translate(96 120)">
            <g transform="scale(2.6) translate(-72 -86)">
              ${сетка(24,24,48,12,7)}
              ${берегГ([[0,0],[4,0],[4,3],[0,3]],24,24,48,0,0,'rgba(127,209,255,.25)')}
            </g>
            <circle cx="0" cy="0" r="40" fill="none" stroke="rgba(255,255,255,.2)"/>
          </g>
          <circle cx="96" cy="120" r="40" fill="url(#c393-стекло)"/>
          <ellipse cx="83" cy="104" rx="13" ry="7" fill="#fff" opacity=".35" filter="url(#c393-мягко)"/>
          <circle cx="96" cy="120" r="40" fill="none" stroke="url(#c393-латунь)" stroke-width="4.4"/>
          <path d="M126 148 l16 20" stroke="url(#c393-латунь)" stroke-width="6" stroke-linecap="round"/>
        </g>
        <g>${проявить('15s',0.4,0.5)}
          ${бумага(196,60,124,104,0.4,1)}
          ${т(258,88,'каждый узел',12,'#5a4a2a')}
          ${т(258,110,'на берегу',12,'#5a4a2a')}
          ${т(258,136,'наполовину',16,'#2b2110',true)}
          ${т(258,156,'внутри',16,'#2b2110',true)}</g>
        <g>${проявить('15s',0.7,0.8)}
          ${подпись(168,238,'поэтому Г : 2, а −1 — поправка на углы',GREEN,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='половина'?'hit':(в?'miss':''), 'Каждый граничный узел принадлежит фигуре наполовину', "r393Почему('половина')")}
        ${BTN(4, в==='целиком'?'miss':'', 'Потому что узлы расположены редко', "r393Почему('целиком')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='половина',
        в==='половина' ? 'Верно: берег делит каждую клетку у узла пополам, поэтому такой узел даёт половину. Отсюда Г/2.'
                       : 'Нет: дело не в частоте. Берег проходит через узел так, что фигуре принадлежит половина клетки.') : ЖДЁТ()) +
      ПРАВИЛО('Граничный узел — <b>половина</b>, поэтому <b>Г/2</b>.');
  }

  /* 10. Ловушка: вершина не в узле */
  function F10(s){
    const в = s.ловушка;
    return ВОПРОС('Вершина треугольника (0,0), (3,0), (0;2,5) не в узле. Сработает ли формула?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${карта(0.03, '',null,true)}
        ${/* треугольник с вершиной между узлами */''}
        ${берегГ([[0,0],[3,0]],40,50,72,0,0.16,'rgba(232,115,90,.22)')}
        <path d="M50 72 L170 72 L50 172 Z" fill="rgba(232,115,90,.22)" stroke="${RED}" stroke-width="0.9"
          stroke-dasharray="5 4">${проявить('15s',0.2,0.3)}</path>
        ${/* узлы внутри и на границе */''}
        ${точка(90,72,0.3)}${точка(130,72,0.34)}${точка(50,112,0.38)}${точка(50,152,0.42)}
        ${булавка(90,112,0.5)}
        <g>${проявить('15s',0.6,0.7)}
          ${подпись(196,96,'настоящая площадь 3,75',ИНК,12)}
          ${подпись(196,126,'по Пику: 1 + 3 − 1 = 3',RED,12)}
          ${подпись(196,156,'не сходится ✗',RED,14)}</g>
        <g>${проявить('15s',0.78,0.88)}
          ${подпись(168,236,'все вершины обязаны лежать в узлах решётки',GOLD,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нет'?'hit':(в?'miss':''), 'Нет: вершины должны лежать в узлах решётки', "r393Лов('нет')")}
        ${BTN(4, в==='да'?'miss':'', 'Да: формула работает для любого треугольника', "r393Лов('да')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нет',
        в==='нет' ? 'Верно: 3,75 против 3 — формула не сработала. Все вершины многоугольника должны быть в узлах решётки.'
                  : 'Нет: Пик даёт 3, а настоящая площадь 3,75. Формула требует вершин в узлах решётки.') : ЖДЁТ()) +
      ПРАВИЛО('Формула Пика верна, только если <b>все вершины — в узлах решётки</b>.');
  }

  /* 11. Сводка: два числа и формула */
  function F11свод(s){
    const в = s.сводка;
    return ВОПРОС('Что нужно сделать, чтобы найти площадь по формуле Пика?') +
      `<div class="pic">${свг(`
        ${фон(260)}
        ${карта(0.03, берегГ(ОСТРОВ,24,24,48,0,0.14,'rgba(143,209,168,.14)'), null, true, true)}
        ${ВНУТРИ.filter((_,i)=>i%3===0).map((p,i)=>булавка(24+p[0]*24,48+p[1]*24,0.2+i*0.03)).join('')}
        ${НА_ГРАНИЦЕ.filter((_,i)=>i%2===0).map((p,i)=>точка(24+p[0]*24,48+p[1]*24,0.28+i*0.03)).join('')}
        <g>${проявить('15s',0.3,0.42)}
          ${стекло(14,150,308,44,0.3)}
          ${т(168,178,'S = В + Г / 2 − 1',20,GOLD,true)}</g>
        <g>${проявить('15s',0.5,0.6)}
          ${стекло(14,200,308,40,0.5)}
          ${т(168,226,'В — узлы внутри, Г — узлы на границе',12,ИНК,true)}</g>
        <g>${проявить('15s',0.68,0.78)}
          ${подпись(168,252,'вершины обязаны быть в узлах решётки',RED,12)}</g>
      `,260)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='два числа'?'hit':(в?'miss':''), 'Посчитать два числа — В и Г — и подставить в формулу', "r393Свод('два числа')")}
        ${BTN(4, в==='клетки'?'miss':'', 'Пересчитать все разрезанные клетки', "r393Свод('клетки')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='два числа',
        в==='два числа' ? 'Верно: считаем узлы внутри и на границе, потом S = В + Г/2 − 1. Разрезанные клетки не нужны.'
                       : 'Нет: именно от них и спасает формула. Считаем только узлы.') : ЖДЁТ()) +
      ПРАВИЛО('Два числа и одна формула: <b>S = В + Г/2 − 1</b>.');
  }

  /* 12. Практика */
  const УРОВНИ=[
    { вопрос:'Что обозначает буква В в формуле Пика?', варианты:[
        {т:'Число узлов внутри фигуры', ок:true,  почему:'В — внутренние узлы.'},
        {т:'Число узлов на границе', ок:false, почему:'На границе — это Г.'}]},
    { вопрос:'Прямоугольник: В = 6, Г = 14. Какова площадь?', варианты:[
        {т:'12', ок:true,  почему:'6 + 7 − 1 = 12.'},
        {т:'20', ок:false, почему:'Забыли поделить Г на два.'}]},
    { вопрос:'Треугольник: В = 3, Г = 8. Какова площадь?', варианты:[
        {т:'6', ок:true,  почему:'3 + 4 − 1 = 6.'},
        {т:'11', ок:false, почему:'Это 3 + 8.'}]},
    { вопрос:'Остров: В = 23, Г = 14. Какова площадь?', варианты:[
        {т:'29', ок:true,  почему:'23 + 7 − 1 = 29.'},
        {т:'37', ок:false, почему:'Г посчитали целиком.'}]},
    { вопрос:'Когда формула Пика не работает?', варианты:[
        {т:'Если вершина не в узле решётки', ок:true,  почему:'Все вершины обязаны быть в узлах.'},
        {т:'Если фигура большая', ок:false, почему:'Размер не мешает.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Считаем два числа: узлы внутри и узлы на берегу. Дальше S = В + Г/2 − 1.') +
        `<div class="ask">${BTN(4,'','пройти заново','r393Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r393Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Считай <b>В</b> и <b>Г</b>, потом S = В + Г/2 − 1.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · узлы',
    задания:[
      { ф:'Узел лежит на берегу. Это В или Г?', о:['Г','В'], в:0, р:'На границе — Г.' },
      { ф:'Узел внутри фигуры. Это В или Г?', о:['В','Г'], в:0, р:'Внутри — В.' },
      { ф:'Прямоугольник 4×3: сколько узлов внутри?', о:['6','12','14'], в:0, р:'3 · 2 = 6.' },
      { ф:'Прямоугольник 4×3: сколько узлов на границе?', о:['14','12','6'], в:0, р:'2·(4+3) = 14.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · формула',
    задания:[
      { ф:'В = 6, Г = 14. Площадь?', о:['12','20','7'], в:0, р:'6 + 7 − 1 = 12.' },
      { ф:'В = 3, Г = 8. Площадь?', о:['6','11','4'], в:0, р:'3 + 4 − 1 = 6.' },
      { ф:'В = 23, Г = 14. Площадь?', о:['29','37','21'], в:0, р:'23 + 7 − 1 = 29.' },
      { ф:'В = 0, Г = 4. Площадь?', о:['1','4','3'], в:0, р:'0 + 2 − 1 = 1.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · обратные и ловушки',
    задания:[
      { ф:'S = 12, В = 6. Чему равно Г?', о:['14','12','7'], в:0, р:'Г = 2·(S − В + 1) = 14.' },
      { ф:'Формула Пика требует, чтобы…', о:['вершины были в узлах','фигура была выпуклой'], в:0, р:'Все вершины — в узлах решётки.' },
      { ф:'Можно ли применять формулу к фигуре с дырой?', о:['нет','да'], в:0, р:'Только к простому многоугольнику.' },
      { ф:'В = 10, Г = 6. Площадь?', о:['12','16','15'], в:0, р:'10 + 3 − 1 = 12.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r393T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r393TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L393={
    id: ID, title:'Формула Пика', ico:'🗺️',
    src:'Математика · 5–6 класс · Олимп-6: клетки', subj:'math',
    explain: [
      'Землемер нашёл старую карту острова, начерченную на клетчатой бумаге. Берег острова идёт по линиям сетки и разрезает клетки на части. Чтобы узнать площадь, можно складывать эти половинки и четвертинки, но их десятки и в них легко запутаться. Есть способ изящнее: считать не клетки, а узлы решётки — точки, где линии сетки пересекаются.',

      'Почему подсчёт частей клеток так неудобен? Вдоль берега клетка может быть разрезана на две половины, на четверти, а то и на совсем маленький кусочек у самого излома. Каждая часть считается отдельно, и одна ошибка портит весь ответ. Именно поэтому математики искали формулу, которая обходится без частей: австрийский математик Георг Пик нашёл её в 1899 году.',

      'Отметим на карте все узлы решётки, которые лежат на самом берегу острова. На нашей карте их ровно четырнадцать. Это число обозначают буквой Г — от слова «граница». Важно: узел считается граничным, только если он лежит точно на линии берега, а не рядом с ней.',

      'Теперь отметим узлы, которые оказались внутри острова, — те, что не попали на берег. Их двадцать три. Это число обозначают буквой В — «внутренние». Вместе два числа — В и Г — полностью описывают фигуру для нашей формулы: больше ничего измерять не нужно.',

      'Формула Пика выглядит так: площадь равна числу внутренних узлов плюс половина числа граничных узлов минус один. Коротко: S = В + Г/2 − 1. Почему граничные узлы берут половинками? Потому что берег проходит через такой узел так, что фигуре принадлежит ровно половина окружающей его клетки. А единица — это поправка: три угловые половинки при сложении дают лишнюю половину, её и вычитают.',

      'Проверим формулу на простой фигуре — прямоугольнике 4 на 3. Внутри у него шесть узлов: три по горизонтали и два по вертикали. На границе четырнадцать узлов: по четыре на длинных сторонах, по три на коротких и четыре в углах. Считаем: 6 + 14/2 − 1 = 6 + 7 − 1 = 12. И обычная формула площади даёт 4 · 3 = 12 — сходится.',

      'Проверим на треугольнике с вершинами в узлах: (0,0), (4,0) и (0,3). Внутри три узла, на границе восемь. По формуле: 3 + 8/2 − 1 = 3 + 4 − 1 = 6. И обычный способ: половина произведения катетов даёт 4 · 3 : 2 = 6. Формула Пика работает и для треугольников, если их вершины лежат в узлах.',

      'Вернёмся к острову. Мы уже сосчитали: внутри 23 узла, на границе 14. Подставляем: 23 + 14/2 − 1 = 23 + 7 − 1 = 29. Значит, площадь острова равна 29 клеткам. Представляете: вместо того чтобы возиться с половинками клеток вдоль всего изломанного берега, мы дважды посчитали точки.',

      'Почему у граничных узлов именно половина? Посмотрим в лупу на один такой узел. Берег проходит точно через узел, и клетка вокруг него делится на две равные части: одна внутри острова, другая снаружи. Значит, каждая такая клетка добавляет к площади ровно половину. Отсюда и деление на два. А вычитаемая единица появляется потому, что при обходе берега углы считаются дважды.',

      'У формулы есть важное условие: все вершины многоугольника должны лежать в узлах решётки. Проверим, что будет иначе. Возьмём треугольник с вершинами (0,0), (3,0) и (0; 2,5) — последняя вершина стоит между узлами. Узлов внутри один, на границе шесть, и формула даёт 1 + 3 − 1 = 3. А настоящая площадь равна 3,75. Формула не сработала. Ещё одна ловушка: если у фигуры есть отверстие, формула тоже не применяется.',

      'Соберём всё вместе. Считаем два числа: В — узлы внутри фигуры, Г — узлы на её границе. Подставляем в формулу Пика: площадь равна В + Г/2 − 1. Помним условия: вершины должны лежать в узлах решётки, а фигура — не иметь отверстий. И помним смысл: каждый внутренний узел даёт целую клетку, каждый граничный — половину.',

      'Пройти практику: пять уровней про узлы, формулу Пика, остров и ловушки. Дальше ждут три тренажёра.',

      'Первый тренажёр — узлы: различаем внутренние и граничные.',

      'Второй тренажёр — формула: подставляем В и Г и считаем площадь.',

      'Третий тренажёр — обратные задачи и ловушки: находим Г по площади и помним про вершины в узлах.'
    ],
    check: {
      q:'Остров: В = 23, Г = 14. Какова его площадь?',
      choices:['29','37','21'],
      ans:0,
      exp:'S = В + Г/2 − 1 = 23 + 7 − 1 = 29.'
    },
    tasks: [
      { q:'Прямоугольник: В = 6, Г = 14. Какова площадь?', kind:'choice',
        choices:['12','20'], ans:0,
        hints:['Г делят на два.','6 + 7 − 1.'],
        sol:'12.' },
      { q:'Треугольник: В = 3, Г = 8. Какова площадь?', kind:'choice',
        choices:['6','11'], ans:0,
        hints:['8 : 2 = 4.','3 + 4 − 1.'],
        sol:'6.' },
      { q:'Когда формула Пика не работает?', kind:'choice',
        choices:['Если вершина не в узле решётки','Если фигура большая'], ans:0,
        hints:['Проверь условие про вершины.','Ловушка с вершиной (0; 2,5).'],
        sol:'Если вершина не в узле решётки.' },
      { q:'S = 12, В = 6. Чему равно Г?', kind:'choice',
        choices:['14','12'], ans:0,
        hints:['Вырази Г из формулы.','Г = 2·(S − В + 1).'],
        sol:'14.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L393.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11свод(s);
    else if(f===12) сцена=F11(s);
    else if(f===13) сцена=F12(s);
    else if(f===14) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Старая карта','Остров на клетках'],
      2:['Трудность','Разрезанные клетки'],
      3:['Узлы на берегу','Число Г'],
      4:['Узлы внутри','Число В'],
      5:['Формула Пика','S = В + Г/2 − 1'],
      6:['Проверка','Прямоугольник 4 × 3'],
      7:['Ещё проверка','Треугольник'],
      8:['Остров','Считаем площадь'],
      9:['Почему половина','Смотрим в лупу'],
      10:['Ловушка','Вершина не в узле'],
      11:['Сводка','Два числа и формула'],
      12:['Практика','Пять уровней'],
      13:['Тренажёр 1','Узлы'],
      14:['Тренажёр 2','Формула'],
      15:['Тренажёр 3','Обратные и ловушки']
    };
    const з=ЗАГОЛОВКИ[f]||['Формула Пика','Клетки'];
    el.innerHTML = `<div class="s6 l393" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r393Карта=(к)=>{ S().карта=к; chRender(0); };
  window.r393Трудно=(к)=>{ S().трудно=к; chRender(0); };
  window.r393Граница=(к)=>{ S().граница=к; chRender(0); };
  window.r393Внутри=(к)=>{ S().внутри=к; chRender(0); };
  window.r393Формула=(к)=>{ S().формула=к; chRender(0); };
  window.r393Прямоуг=(к)=>{ S().прямоугольник=к; chRender(0); };
  window.r393Треуг=(к)=>{ S().треугольник=к; chRender(0); };
  window.r393Остров=(к)=>{ S().остров=к; chRender(0); };
  window.r393Почему=(к)=>{ S().почему=к; chRender(0); };
  window.r393Лов=(к)=>{ S().ловушка=к; chRender(0); };
  window.r393Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r393Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r393Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r393T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r393TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L393; else arr.push(L393);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA393={render:render, L:L393};
})();
