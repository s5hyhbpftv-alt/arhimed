/* ====== МАТЕМАТИКА · УРОК 356 · «ДЕЛИМОСТЬ: ПРИЗНАКИ 2, 3, 5, 9, 10» ========
   3 класс, олимпиадный блок «Олимп-3: признаки делимости».

   Сюжет под мир 3 класса в «Пути» («Вдоль берега»): в порту разгружают судно,
   и груз надо разложить поровну. Боцман не делит — он смотрит на число и
   сразу говорит, сойдётся или нет. Признак делимости и есть его секрет.

   Раньше урок был четырьмя строками текста без единого рисунка.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ (node, деление и сумма цифр):
     4386 : 2 = 2193 ✓ · сумма цифр 21 → на 3 да (4386 : 3 = 1462), на 9 НЕТ
     738  сумма 18 → на 3 да (246) и на 9 да (82)
     321  сумма 6  → на 3 да (107), на 9 нет
     126  сумма 9  → на 9 да (14) и на 3 да (42)
     414  сумма 9  → на 3 да (138) и на 9 да (46)
     870  на 10 да (87), на 3 да (290), на 9 нет (сумма 15)
     235  на 5 да (47), на 10 нет
     128  на 2 да (64) · 77 на 2 нет · 999 на 9 да (111) · 100 на 3 нет
     510  на 10 да (51) · 51* на 10 → * = 0 · 12* на 9 → * = 6 (126)

   МЕХАНИЗМЫ КАДРОВ РАЗНЫЕ:
     1  ДВА ПУТИ: столбик деления против взгляда боцмана — гонка;
     2  ПАРЫ: ящики сходятся по двое, последняя цифра решает;
     3  ПЯТЁРКИ: груз в столбики по пять, хвост числа подсвечен;
     4  НОЛЬ НА КОНЦЕ: на 10 — только ноль, и это 2 и 5 сразу;
     5  РАЗРЯДЫ-МЕШКИ: 10 и 100 делятся сами, остаётся хвост — почему хватает
        последней цифры;
     6  ЦИФРЫ ССЫПАЮТСЯ: из числа собирается сумма цифр;
     7  ДЕВЯТКА: та же сумма, но порог строже, и 9 сильнее 3;
     8  ДЕСЯТЬ = ДЕВЯТЬ + ОДИН: каждый разряд оставляет единицу — почему
        works сумма цифр;
     9  ДЛИННОЕ ЧИСЛО: 4386 проверяется по всем признакам сразу;
     10 ЛЕСТНИЦА 9 → 3: стрелка идёт в одну сторону, обратно перечёркнута;
     11 ПРОПУЩЕННАЯ ЦИФРА: слот перебирает цифры, подходящая встаёт на место;
     12 ПРАКТИКА: пять уровней;
     13–15 ТРЕНАЖЁРЫ: на 2/5/10, на 3/9, хитрые.

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md: кривая проекта 0.23 1 0.32 1,
   одно главное движение на кадр, состояние всегда видимо (минимум 0.55 —
   при 0.3 кадр выглядит пустым, это ошибка 7 стандарта), текст не
   масштабируется, prefers-reduced-motion выключает SMIL полностью,
   кольцо ставится только вокруг предмета, а не поверх подписи (ошибка 4). */
(function(){
  'use strict';

  const ID = 356;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a', SAND='#e8c48a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  const CSS=`
  #lvis .s6.l356{gap:14px}
  #lvis .s6.l356 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l356 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l356 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l356 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l356 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l356 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l356 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l356 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l356 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l356 .правило b{color:${GOLD}}
  #lvis .s6.l356 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l356 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l356 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l356 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l356 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l356 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l356 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l356 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l356 .уровни .точка.сейчас{background:${GOLD};animation:l356dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l356dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l356 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l356{-webkit-text-size-adjust:100%}
  #lvis .s6.l356 [data-anim]{animation:l356rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l356rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l356 [data-anim]{animation:none!important}
    #lvis .s6.l356 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l356 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l356-style');
      if(!s){ s=document.createElement('style'); s.id='l356-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  /* кольцо ставится ТОЛЬКО вокруг предмета (цифры, ящика), не поверх подписи */
  const кольцо = (x,y,r,цвет,длит,доля) => ДВИЖ ? `<circle cx="${x}" cy="${y}" r="${r}" fill="none"
      stroke="${цвет}" stroke-width="2">
      ${анК('r',r+';'+r+';'+(r+8)+';'+r,длит,'0;'+кт(доля)+';'+кт(доля+0.1)+';1')}
      ${анК('opacity','0;0;0.9;0',длит,'0;'+кт(доля)+';'+кт(доля+0.06)+';'+кт(доля+0.16))}
    </circle>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c356-фон" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2a3f33"/><stop offset="0.5" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#111c15"/>
      </linearGradient>
      <linearGradient id="c356-ящик" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c79a5a"/><stop offset="0.55" stop-color="#8a5f30"/>
        <stop offset="1" stop-color="#53381c"/>
      </linearGradient>
      <linearGradient id="c356-мешок" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#9fb0bd"/><stop offset="1" stop-color="#4a5560"/>
      </linearGradient>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||240}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c356-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+16, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.86)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* ЯЩИК груза: доски и обвязка */
  const ящик = (x,y,б,цвет) => {
    const s=б||18;
    return `<g>
      <rect x="${x-s/2}" y="${y-s/2}" width="${s}" height="${s}" rx="2"
        fill="${цвет||'url(#c356-ящик)'}" stroke="${ОБВОД}" stroke-width="1.1"/>
      <line x1="${x-s/2}" y1="${y}" x2="${x+s/2}" y2="${y}" stroke="rgba(0,0,0,.35)" stroke-width="1"/>
    </g>`;
  };

  /* ЦИФРА числа в рамке: рамку можно подсветить, цифру — нет (текст не трогаем) */
  const цифра = (x,y,знак,подсвет,кегль) => `<g>
    <rect x="${x-15}" y="${y-22}" width="30" height="34" rx="6"
      fill="${подсвет?'rgba(255,215,106,.18)':'rgba(255,255,255,.05)'}"
      stroke="${подсвет?GOLD:'rgba(255,255,255,.18)'}" stroke-width="${подсвет?2:1.2}"/>
    ${т(x,y+4,знак,кегль||24,подсвет?GOLD:ИНК,true)}
  </g>`;

  const число = (x,y,строка,подсветIdx,кегль) => {
    const ш=32, старт=x-(строка.length-1)*ш/2;
    return строка.split('').map((з,i)=>цифра(старт+i*ш,y,з,подсветIdx!=null&&i===подсветIdx,кегль)).join('');
  };

  /* ================= КАДРЫ ================= */

  /* 1. Два пути: столбик против взгляда боцмана */
  function F1(s){
    const в = s.зачем;
    return ВОПРОС('В трюме 4 386 ящиков. Надо понять, разложатся ли они поровну на 2 лодки. Как быстрее узнать ответ?') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${т(86,26,'Делить столбиком',14,МУТ,true)}
        ${т(250,26,'Взгляд боцмана',14,GOLD,true)}
        <line x1="168" y1="36" x2="168" y2="214" stroke="rgba(255,255,255,.16)" stroke-width="1.2"/>
        <g>${проявить('9s',0.1,0.2)}
          ${т(86,58,'4386 : 2',16,ИНК)}
          ${т(86,82,'4 : 2 = 2',14,МУТ)}
          ${т(86,102,'3 : 2 = 1 ост.1',14,МУТ)}
          ${т(86,122,'18 : 2 = 9',14,МУТ)}
          ${т(86,142,'6 : 2 = 3',14,МУТ)}
          ${т(86,168,'…четыре шага',14,МУТ)}</g>
        <g>${проявить('9s',0.48,0.58)}
          ${число(250,74,'6',0,24)}
          ${т(250,112,'последняя цифра',14,ИНК)}
          ${т(250,132,'чётная — значит да',14,GREEN)}</g>
        ${кольцо(250,66,24,GOLD,'9s',0.6)}
        <g>${проявить('9s',0.74,0.84)}${подпись(168,204,'один взгляд против четырёх шагов',GOLD,14)}</g>
      `,232)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='признак'?'hit':(в?'miss':''), 'Посмотреть на последнюю цифру', "r356Зачем('признак')")}
        ${BTN(4, в==='столбик'?'miss':'', 'Разделить столбиком и посмотреть остаток', "r356Зачем('столбик')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='признак',
        в==='признак' ? 'Верно: признак делимости отвечает на вопрос «разделится ли», не выполняя деления. Ответ виден сразу.'
                      : 'Так тоже можно, и ответ будет верный — но это четыре шага вместо одного взгляда. Признак быстрее.') : ЖДЁТ()) +
      ПРАВИЛО('Признак делимости говорит <b>разделится или нет</b>, не выполняя деления.');
  }

  /* 2. Пары: признак делимости на 2 */
  function F2(s){
    const в = s.два;
    const ящики = [0,1,2,3,4,5].map(i=>{
      const x=60+i*38, y=118;
      return `<g>${анТК((i%2? '0 0;0 0;-9 0;-9 0' : '0 0;0 0;9 0;9 0'),'8s','0;0.16;0.62;1')}
        ${ящик(x,y,26)}</g>`;
    }).join('');
    return ВОПРОС('Как с одного взгляда понять, что число делится на 2?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,26,'Ящики сходятся по двое',14,GOLD,true)}
        ${ящики}
        <g>${проявить('8s',0.66,0.76)}
          ${т(168,158,'пар получилось ровно три — лишнего нет',14,ИНК)}</g>
        <g>${проявить('8s',0.24,0.34)}
          ${число(168,204,'128',2,24)}</g>
        <g>${проявить('8s',0.8,0.9)}${т(168,236,'решает только последняя цифра: 8 — чётная',14,GREEN)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='последняя'?'hit':(в?'miss':''), 'Посмотреть на последнюю цифру: 0, 2, 4, 6, 8', "r356Два('последняя')")}
        ${BTN(4, в==='сумма'?'miss':'', 'Сложить все цифры и проверить сумму', "r356Два('сумма')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='последняя',
        в==='последняя' ? 'Верно: у 128 последняя цифра 8 — чётная, значит 128 делится на 2 (128 : 2 = 64).'
                        : 'Нет: сумма цифр нужна для 3 и 9. Для 2 хватает последней цифры — у 128 это 8.') : ЖДЁТ()) +
      ПРАВИЛО('На <b>2</b> делятся числа, у которых последняя цифра 0, 2, 4, 6 или 8.');
  }

  /* 3. Пятёрки: столбики груза по пять */
  function F3(s){
    const в = s.пять;
    const столб = (x,n,цвет) => Array.from({length:n},(_,i)=>ящик(x,170-i*20,18,цвет)).join('');
    return ВОПРОС('А на 5? В порту груз складывают столбиками по пять ящиков.') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,24,'Столбики по пять',14,GOLD,true)}
        <g>${проявить('9s',0.08,0.18)}${столб(64,5)}</g>
        <g>${проявить('9s',0.2,0.3)}${столб(112,5)}</g>
        <g>${проявить('9s',0.32,0.42)}${столб(160,5)}</g>
        <g>${проявить('9s',0.44,0.54)}${столб(208,5)}</g>
        <g>${проявить('9s',0.56,0.66)}${столб(256,5)}</g>
        <line x1="40" y1="182" x2="296" y2="182" stroke="${МУТ}" stroke-width="1.4"/>
        <g>${проявить('9s',0.7,0.8)}${т(168,202,'235 ящиков — ровно 47 столбиков',14,ИНК)}</g>
        <g>${проявить('9s',0.82,0.92)}
          ${число(168,228,'235',2,20)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='05'?'hit':(в?'miss':''), 'Последняя цифра 0 или 5', "r356Пять('05')")}
        ${BTN(4, в==='чётная'?'miss':'', 'Последняя цифра чётная', "r356Пять('чётная')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='05',
        в==='05' ? 'Верно: у 235 на конце 5, значит делится на 5 (235 : 5 = 47). Других вариантов нет — только 0 или 5.'
                 : 'Нет: чётность решает вопрос про 2. Для пятёрки смотрят, стоит ли на конце 0 или 5.') : ЖДЁТ()) +
      ПРАВИЛО('На <b>5</b> делятся числа, у которых на конце 0 или 5.');
  }

  /* 4. Ноль на конце: на 10 — и на 2, и на 5 сразу */
  function F4(s){
    const в = s.десять;
    return ВОПРОС('Груз грузят в контейнеры по 10 ящиков. Какие числа подойдут?') +
      `<div class="pic">${свг(`
        ${фон(236)}
        ${т(168,26,'Ноль на конце',14,GOLD,true)}
        <g>${проявить('9s',0.1,0.2)}${число(120,76,'870',2,24)}
          ${т(250,80,'да',20,GREEN,true)}</g>
        <g>${проявить('9s',0.3,0.4)}${число(120,134,'235',2,24)}
          ${т(250,138,'нет',20,RED,true)}</g>
        ${кольцо(152,68,20,GREEN,'9s',0.22)}
        <g>${проявить('9s',0.56,0.66)}
          <rect x="20" y="164" width="296" height="30" rx="8" fill="rgba(143,209,168,.12)" stroke="${GREEN}" stroke-width="1.4"/>
          ${т(168,184,'10 = 2 × 5 — делится и на 2, и на 5',14,ИНК)}</g>
        <g>${проявить('9s',0.78,0.88)}${т(168,220,'870 : 10 = 87, а 235 на 10 не делится',14,МУТ)}</g>
      `,236)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='ноль'?'hit':(в?'miss':''), 'Только те, у которых на конце ноль', "r356Десять('ноль')")}
        ${BTN(4, в==='05'?'miss':'', 'Те, у которых на конце 0 или 5', "r356Десять('05')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='ноль',
        в==='ноль' ? 'Верно: на 10 делятся только числа с нулём на конце. 870 — да, 235 — нет, хотя на 5 оно делится.'
                   : 'Нет: 0 или 5 — это признак пятёрки. Для десятки нужен именно ноль: 235 на 5 делится, а на 10 уже нет.') : ЖДЁТ()) +
      ПРАВИЛО('На <b>10</b> делятся числа с нулём на конце. Это 2 и 5 одновременно.');
  }

  /* 5. Разряды-мешки: почему хватает последней цифры */
  function F5(s){
    const в = s.почему;
    return ВОПРОС('Почему для 2, 5 и 10 хватает одной последней цифры?') +
      `<div class="pic">${свг(`
        ${фон(240)}
        ${т(168,24,'Разряды делятся сами',14,GOLD,true)}
        ${[['1000',56],['100',96],['10',136]].map((p,i)=>`
          <g>${проявить('9s',0.1+i*0.16,0.2+i*0.16)}
            <rect x="26" y="${p[1]-18}" width="140" height="30" rx="8" fill="url(#c356-мешок)" opacity=".5" stroke="${ОБВОД}" stroke-width="1"/>
            ${т(96,p[1]+3,p[0],20,ИНК,true)}
            ${т(248,p[1]+3,'делится на 2, 5 и 10',14,GREEN)}
          </g>`).join('')}
        <g>${проявить('9s',0.62,0.72)}
          <rect x="26" y="158" width="284" height="44" rx="8" fill="rgba(255,215,106,.14)" stroke="${GOLD}" stroke-width="1.6"/>
          ${т(168,176,'остаются только единицы',16,GOLD,true)}
          ${т(168,194,'то есть последняя цифра',14,ИНК)}</g>
        <g>${проявить('9s',0.82,0.92)}${т(168,222,'4386 = 4000 + 300 + 80 + 6 · решает шестёрка',14,ИНК)}</g>
      `,240)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='разряды'?'hit':(в?'miss':''), 'Десятки, сотни и тысячи делятся сами', "r356Почему('разряды')")}
        ${BTN(4, в==='правило'?'miss':'', 'Просто такое правило, объяснения нет', "r356Почему('правило')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='разряды',
        в==='разряды' ? 'Верно: 10, 100 и 1000 делятся и на 2, и на 5, и на 10 без остатка. Значит весь «хвост» числа уже разделился, и остаётся проверить только единицы.'
                      : 'Объяснение есть, и оно простое: 10, 100, 1000 сами делятся на 2, 5 и 10. Поэтому спор решает только последняя цифра.') : ЖДЁТ()) +
      ПРАВИЛО('Все разряды, кроме единиц, <b>уже делятся</b> на 2, 5 и 10.');
  }

  /* 6. Цифры ссыпаются: сумма цифр и тройка */
  function F6(s){
    const в = s.три;
    return ВОПРОС('Теперь 3. Делится ли 321 на 3?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Цифры спускаются в сумму',14,GOLD,true)}
        ${число(168,74,'321',null,24)}
        ${/* цифры спускаются КАЖДАЯ ПО СВОЕЙ вертикали: если сводить их в одну
              точку, они наложатся друг на друга и на плашку — так и вышло в
              первом варианте (ошибка 5 стандарта) */''}
        ${[[136,'3'],[168,'2'],[200,'1']].map((p,i)=>`
          <g>${анТК('0 0;0 0;0 34;0 34','9s','0;'+(0.12+i*0.08).toFixed(2)+';'+(0.46+i*0.08).toFixed(2)+';1')}
            ${т(p[0],100,p[1],20,SAND,true)}</g>`).join('')}
        <g>${проявить('9s',0.66,0.76)}
          <rect x="100" y="150" width="136" height="34" rx="8" fill="rgba(255,215,106,.14)" stroke="${GOLD}" stroke-width="1.6"/>
          ${т(168,173,'3 + 2 + 1 = 6',16,GOLD,true)}</g>
        <g>${проявить('9s',0.8,0.9)}
          ${т(168,208,'6 делится на 3 — значит и 321 делится',14,GREEN)}
          ${т(168,232,'321 : 3 = 107',14,МУТ)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='да'?'hit':(в?'miss':''), 'Да: 3 + 2 + 1 = 6, а 6 делится на 3', "r356Три('да')")}
        ${BTN(4, в==='нет'?'miss':'', 'Нет: последняя цифра 1 — нечётная', "r356Три('нет')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='да',
        в==='да' ? 'Верно: для тройки складывают цифры. 3 + 2 + 1 = 6, шесть делится на три — значит и 321 делится (321 : 3 = 107).'
                 : 'Нет: чётность тут ни при чём, она про двойку. Для тройки складывают цифры: 3 + 2 + 1 = 6.') : ЖДЁТ()) +
      ПРАВИЛО('На <b>3</b> делится число, у которого <b>сумма цифр</b> делится на 3.');
  }

  /* 7. Девятка: та же сумма, но порог строже */
  function F7(s){
    const в = s.девять;
    return ВОПРОС('Та же сумма цифр работает и для 9. Делится ли 321 на 9?') +
      `<div class="pic">${свг(`
        ${фон(236)}
        ${т(168,24,'Один приём — два числа',14,GOLD,true)}
        <g>${проявить('9s',0.08,0.18)}
          ${число(96,78,'321',null,20)}
          ${т(96,112,'сумма 6',14,ИНК)}
          ${т(96,136,'на 3 — да',14,GREEN)}
          ${т(96,158,'на 9 — нет',14,RED)}</g>
        <g>${проявить('9s',0.4,0.5)}
          ${число(244,78,'126',null,20)}
          ${т(244,112,'сумма 9',14,ИНК)}
          ${т(244,136,'на 3 — да',14,GREEN)}
          ${т(244,158,'на 9 — да',14,GREEN)}</g>
        <line x1="168" y1="56" x2="168" y2="170" stroke="rgba(255,255,255,.16)" stroke-width="1.2"/>
        ${кольцо(244,104,22,GREEN,'9s',0.62)}
        <g>${проявить('9s',0.74,0.84)}
          ${т(168,200,'для девятки сумма должна делиться на 9',14,ИНК)}
          ${т(168,224,'126 : 9 = 14',14,МУТ)}</g>
      `,236)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нет'?'hit':(в?'miss':''), 'Нет: сумма 6, она на 9 не делится', "r356Девять('нет')")}
        ${BTN(4, в==='да'?'miss':'', 'Да: раз делится на 3, то и на 9', "r356Девять('да')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нет',
        в==='нет' ? 'Верно: сумма цифр 321 равна 6. Шесть делится на три, но не на девять — значит 321 на 9 не делится.'
                  : 'Нет, так не бывает: на 3 делятся многие числа, которые на 9 не делятся. У 321 сумма 6 — этого мало для девятки.') : ЖДЁТ()) +
      ПРАВИЛО('На <b>9</b> делится число, у которого сумма цифр делится на 9.');
  }

  /* 8. Десять = девять + один: почему работает сумма цифр */
  function F8(s){
    const в = s.почемусумма;
    return ВОПРОС('Почему для 3 и 9 годится именно сумма цифр?') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${т(168,24,'Каждый разряд оставляет единицу',14,GOLD,true)}
        ${[['10 = 9 + 1','9 : 9 = 1',62],['100 = 99 + 1','99 : 9 = 11',102],['1000 = 999 + 1','999 : 9 = 111',142]].map((p,i)=>`
          <g>${проявить('9s',0.1+i*0.18,0.2+i*0.18)}
            <rect x="24" y="${p[2]-20}" width="288" height="32" rx="8" fill="rgba(255,255,255,.05)" stroke="rgba(255,255,255,.16)" stroke-width="1.2"/>
            ${т(108,p[2]+2,p[0],16,ИНК)}
            ${т(246,p[2]+2,p[1],14,GREEN)}
          </g>`).join('')}
        <g>${проявить('9s',0.66,0.76)}
          <rect x="24" y="166" width="288" height="34" rx="8" fill="rgba(255,215,106,.14)" stroke="${GOLD}" stroke-width="1.6"/>
          ${т(168,188,'девятки уходят, остаются цифры',14,GOLD,true)}</g>
        <g>${проявить('9s',0.84,0.92)}${т(168,220,'поэтому и проверяют их сумму',14,ИНК)}</g>
      `,232)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='остаток'?'hit':(в?'miss':''), 'Каждый разряд — это девятки плюс единица', "r356Почемусумма('остаток')")}
        ${BTN(4, в==='совпало'?'miss':'', 'Просто совпадение, запомнить и всё', "r356Почемусумма('совпало')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='остаток',
        в==='остаток' ? 'Верно: 10 = 9 + 1, 100 = 99 + 1, 1000 = 999 + 1. Все девятки делятся и на 9, и на 3, а от каждого разряда остаётся ровно его цифра — их и складывают.'
                      : 'Это не совпадение: 10 = 9 + 1, 100 = 99 + 1. Девятки делятся нацело, а остаются сами цифры — поэтому сумма цифр и решает.') : ЖДЁТ()) +
      ПРАВИЛО('10 = 9 + 1, 100 = 99 + 1: <b>от каждого разряда остаётся его цифра</b>.');
  }

  /* 9. Длинное число: 4386 по всем признакам */
  function F9(s){
    const в = s.длинное;
    const строка = (y,подпись_,ответ,цвет,доля) => `<g>${проявить('9s',доля,доля+0.08)}
      ${т(56,y,подпись_,14,МУТ,false,'start')}
      ${т(280,y,ответ,16,цвет,true,'end')}</g>`;
    return ВОПРОС('Проверь число 4 386: на что оно делится?') +
      `<div class="pic">${свг(`
        ${фон(246)}
        ${число(168,50,'4386',null,20)}
        <g>${проявить('9s',0.14,0.24)}
          ${т(168,84,'сумма цифр: 4 + 3 + 8 + 6 = 21',14,GOLD)}</g>
        ${строка(116,'на 2 — последняя 6','да',GREEN,0.3)}
        ${строка(142,'на 5 — конец не 0 и не 5','нет',RED,0.42)}
        ${строка(168,'на 10 — нет нуля','нет',RED,0.52)}
        ${строка(194,'на 3 — 21 делится на 3','да',GREEN,0.62)}
        ${строка(220,'на 9 — 21 на 9 не делится','нет',RED,0.74)}
      `,246)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='два3'?'hit':(в?'miss':''), 'На 2 и на 3', "r356Длинное('два3')")}
        ${BTN(4, в==='всё'?'miss':'', 'На 2, 3 и 9', "r356Длинное('всё')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='два3',
        в==='два3' ? 'Верно: последняя цифра 6 — делится на 2; сумма цифр 21 делится на 3. На 9 не делится: 21 на 9 не делится. 4386 : 2 = 2193, 4386 : 3 = 1462.'
                   : 'Про 9 не сходится: сумма цифр 21, а она на девять не делится. На 2 и на 3 — да.') : ЖДЁТ()) +
      ПРАВИЛО('Одно число проверяют <b>каждым признаком отдельно</b>.');
  }

  /* 10. Лестница 9 → 3: стрелка в одну сторону */
  function F10(s){
    const в = s.лестница;
    return ВОПРОС('Если число делится на 9, делится ли оно на 3? А наоборот?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        <g>${проявить('9s',0.08,0.18)}
          <rect x="34" y="46" width="112" height="46" rx="10" fill="rgba(143,209,168,.14)" stroke="${GREEN}" stroke-width="1.6"/>
          ${т(90,76,'делится на 9',14,GREEN,true)}</g>
        <g>${проявить('9s',0.24,0.34)}
          <rect x="190" y="46" width="112" height="46" rx="10" fill="rgba(143,209,168,.14)" stroke="${GREEN}" stroke-width="1.6"/>
          ${т(246,76,'делится на 3',14,GREEN,true)}</g>
        <g>${анТК('0 0;0 0;16 0;16 0','9s','0;0.36;0.62;1')}
          <path d="M150 70 h30 l-8 -6 M180 70 l-8 6" fill="none" stroke="${GREEN}" stroke-width="2.4"/></g>
        <g>${проявить('9s',0.66,0.76)}
          <path d="M180 116 h-30 l8 -6 M150 116 l8 6" fill="none" stroke="${RED}" stroke-width="2.4"/>
          <line x1="152" y1="104" x2="180" y2="128" stroke="${RED}" stroke-width="2.4"/>
          ${т(168,150,'обратно — не всегда',14,RED)}</g>
        <g>${проявить('9s',0.82,0.92)}
          ${т(168,186,'321 делится на 3, но не на 9',14,ИНК)}
          ${т(168,212,'126 делится и на 3, и на 9',14,МУТ)}</g>
      `,238)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='одна'?'hit':(в?'miss':''), 'На 9 → значит и на 3. Обратно — не всегда', "r356Лестница('одна')")}
        ${BTN(4, в==='обе'?'miss':'', 'Одно всегда следует из другого', "r356Лестница('обе')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='одна',
        в==='одна' ? 'Верно: девять само делится на три, поэтому всё, что делится на 9, делится и на 3. А обратно нет: у 321 сумма 6 — на 3 делится, на 9 нет.'
                   : 'Нет: в одну сторону работает, в другую — нет. 321 делится на 3, но на 9 не делится.') : ЖДЁТ()) +
      ПРАВИЛО('Делится на <b>9 → значит и на 3</b>. Обратное неверно.');
  }

  /* 11. Пропущенная цифра: слот перебирает */
  function F11(s){
    const в = s.цифра;
    return ВОПРОС('Какую цифру поставить вместо звёздочки в числе 12*, чтобы оно делилось на 9?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        ${т(168,26,'Подбираем цифру',14,GOLD,true)}
        ${/* слот отставлен от соседней цифры: при шаге 32 кольцо захватывало
             её край и рамки читались как налезающие друг на друга */''}
        ${цифра(112,80,'1',false,24)}
        ${цифра(148,80,'2',false,24)}
        <g>
          <rect x="191" y="58" width="30" height="34" rx="6" fill="rgba(255,215,106,.18)" stroke="${GOLD}" stroke-width="2" stroke-dasharray="4 3"/>
          ${/* перебор 0…6: каждая цифра мелькает и гаснет, а 6 остаётся в слоте —
               раньше гасла и она, и под «1 + 2 + 6 = 9» слот стоял пустой; без
               движения цифры лежали стопкой — теперь видна только 6 */''}
          ${[0,1,2,3,4,5,6].map((ц,i)=>`<g opacity="${ц===6?1:0}">${анК('opacity',ц===6?'0;0;1;1;1;1':'0;0;1;1;0;0','9s',
              '0;'+кт(0.08+i*0.1)+';'+кт(0.1+i*0.1)+';'+кт(0.16+i*0.1)+';'+кт(0.18+i*0.1)+';1')}
              ${т(206,84,String(ц),24,ц===6?GREEN:МУТ,true)}</g>`).join('')}
        </g>
        <g>${проявить('9s',0.76,0.86)}
          ${т(168,128,'1 + 2 + 6 = 9',16,GOLD,true)}
          ${т(168,156,'девять делится на девять — подходит',14,GREEN)}</g>
        ${кольцо(206,76,20,GREEN,'9s',0.78)}
        <g>${проявить('9s',0.88,0.94)}
          ${т(168,196,'получилось 126, и 126 : 9 = 14',14,ИНК)}
          ${т(168,220,'другие цифры дают сумму не кратную 9',14,МУТ)}</g>
      `,238)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='6'?'hit':(в?'miss':''), 'Цифра 6: сумма станет 9', "r356Цифра('6')")}
        ${BTN(4, в==='0'?'miss':'', 'Цифра 0: круглое число делится на всё', "r356Цифра('0')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='6',
        в==='6' ? 'Верно: 1 + 2 + 6 = 9, сумма делится на 9 — значит и 126 делится (126 : 9 = 14).'
                : 'Нет: при нуле получится 120, сумма цифр 3 — на 9 не делится. Нужна цифра 6: 1 + 2 + 6 = 9.') : ЖДЁТ()) +
      ПРАВИЛО('Подбор цифры — это подбор <b>нужной суммы</b>.');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Делится ли 738 на 3?',
      варианты:[{т:'Да: 7 + 3 + 8 = 18',ок:true},{т:'Нет',ок:false}],
      разбор:'Сумма цифр 18 делится на 3, значит 738 : 3 = 246.' },
    { вопрос:'А на 9 то же число 738 делится?',
      варианты:[{т:'Да: 18 делится на 9',ок:true},{т:'Нет: 18 на 9 не делится',ок:false}],
      разбор:'18 : 9 = 2, поэтому 738 : 9 = 82.' },
    { вопрос:'Делится ли 4 386 на 9?',
      варианты:[{т:'Нет: сумма 21',ок:true},{т:'Да',ок:false}],
      разбор:'4 + 3 + 8 + 6 = 21, а 21 на 9 не делится. На 3 — делится.' },
    { вопрос:'Какую цифру поставить в 51*, чтобы число делилось на 10?',
      варианты:[{т:'0',ок:true},{т:'5',ок:false}],
      разбор:'На 10 делятся только числа с нулём на конце: 510 : 10 = 51.' },
    { вопрос:'Число 870: на что делится?',
      варианты:[{т:'На 2, 5, 10 и 3',ок:true},{т:'На 2, 5, 10 и 9',ок:false}],
      разбор:'Ноль на конце даёт 2, 5 и 10. Сумма цифр 15 делится на 3, но не на 9.' }
  ];

  function F12(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r356Reset()")}</div>` +
        ПРАВИЛО('2, 5, 10 — <b>по последней цифре</b>. 3 и 9 — <b>по сумме цифр</b>.');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ВОПРОС('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к,
        выбран===к ? (в.ок?'hit':'miss') : '',
        в.т, "r356Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : ЖДЁТ());
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'На 2, 5 и 10', задания:[
    {q:'Делится ли 128 на 2?', в:0, варианты:['Да','Нет'], раз:'Последняя цифра 8 — чётная. 128 : 2 = 64.'},
    {q:'Делится ли 235 на 10?', в:1, варианты:['Да','Нет: на конце 5, а не 0'], раз:'На 10 нужен ноль. На 5 оно делится: 235 : 5 = 47.'},
    {q:'Делится ли 870 на 10?', в:0, варианты:['Да','Нет'], раз:'Ноль на конце. 870 : 10 = 87.'},
    {q:'Делится ли 77 на 2?', в:1, варианты:['Да','Нет: 7 — нечётная'], раз:'Последняя цифра 7, она нечётная.'}
  ]};
  const Т2 = { имя:'На 3 и 9', задания:[
    {q:'Делится ли 321 на 9?', в:1, варианты:['Да','Нет: сумма 6'], раз:'3 + 2 + 1 = 6, на 9 не делится. На 3 — да.'},
    {q:'Делится ли 126 на 9?', в:0, варианты:['Да: сумма 9','Нет'], раз:'1 + 2 + 6 = 9. 126 : 9 = 14.'},
    {q:'Делится ли 4 386 на 3?', в:0, варианты:['Да: сумма 21','Нет'], раз:'21 делится на 3. 4386 : 3 = 1462.'},
    {q:'Делится ли 100 на 3?', в:1, варианты:['Да','Нет: сумма 1'], раз:'1 + 0 + 0 = 1, на 3 не делится.'}
  ]};
  const Т3 = { имя:'Хитрые', задания:[
    {q:'Делится ли 414 на 9?', в:0, варианты:['Да: 4 + 1 + 4 = 9','Нет'], раз:'Сумма 9, значит 414 : 9 = 46. И на 3 тоже: 414 : 3 = 138.'},
    {q:'Число делится на 9. Делится ли оно на 3?', в:0, варианты:['Да, всегда','Не обязательно'], раз:'Девять само делится на три, поэтому да — всегда.'},
    {q:'Число делится на 3. Обязательно ли на 9?', в:1, варианты:['Да, всегда','Нет: например 321'], раз:'У 321 сумма 6: на 3 делится, на 9 нет.'},
    {q:'Делится ли 999 на 9?', в:0, варианты:['Да: сумма 27','Нет'], раз:'9 + 9 + 9 = 27, а 27 : 9 = 3. Значит 999 : 9 = 111.'}
  ]};

  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ВОПРОС(з.q) +
      `<div class="ask">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r356T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r356TNext('"+ключ+"')")}</div>`
        : ЖДЁТ());
  }

  /* ================= СБОРКА ================= */
  const L356 = {
    id: ID,
    title: 'Делимость: признаки 2, 3, 5, 9, 10',
    ico: '⚖️',
    src: 'Начальная школа · 3 класс · Олимп-3: признаки делимости',
    subj: 'jun',
    explain: [
      'Признак делимости отвечает на вопрос «разделится ли», не выполняя деления: один взгляд вместо четырёх шагов столбика.',
      'На 2 делятся числа, у которых последняя цифра 0, 2, 4, 6 или 8: у 128 это 8.',
      'На 5 делятся числа, у которых на конце 0 или 5: 235 : 5 = 47.',
      'На 10 делятся только числа с нулём на конце. Это 2 и 5 сразу, ведь 10 = 2 × 5.',
      'Почему хватает последней цифры: 10, 100 и 1000 сами делятся на 2, 5 и 10 — остаются только единицы.',
      'На 3 делится число, у которого сумма цифр делится на 3: 3 + 2 + 1 = 6, и 321 : 3 = 107.',
      'На 9 — тот же приём, но строже: сумма должна делиться на 9. У 126 сумма 9, и 126 : 9 = 14.',
      'Почему годится сумма цифр: 10 = 9 + 1, 100 = 99 + 1 — девятки уходят, остаются сами цифры.',
      'Число 4 386: на 2 — да, на 3 — да (сумма 21), а на 5, 10 и 9 — нет.',
      'Делится на 9 — значит и на 3. Обратное неверно: 321 делится на 3, но не на 9.',
      'Пропущенную цифру подбирают по нужной сумме: в 12* годится 6, потому что 1 + 2 + 6 = 9.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: на 2, 5 и 10.',
      'Тренажёр 2: на 3 и 9.',
      'Тренажёр 3: хитрые случаи.'
    ],
    check: {
      q: 'Делится ли 414 на 3?',
      choices: ['Да: 4 + 1 + 4 = 9','Нет'],
      ans: 0,
      exp: 'Сумма цифр 9 делится на 3, поэтому 414 : 3 = 138. И на 9 тоже: 414 : 9 = 46.'
    },
    tasks: [
      { q:'Какую цифру поставить вместо * в числе 51*, чтобы оно делилось на 10?', kind:'choice',
        choices:['0','5'], ans:0,
        hints:['На 10 делятся числа с нулём на конце.','510 : 10 = 51.'],
        sol:'0' },
      { q:'Какое число делится на 9?', kind:'choice',
        choices:['126','123'], ans:0,
        hints:['Сложи цифры каждого числа.','1 + 2 + 6 = 9, а 1 + 2 + 3 = 6.'],
        sol:'126' },
      { q:'Делится ли 4 386 на 9?', kind:'choice',
        choices:['Нет: сумма цифр 21','Да'], ans:0,
        hints:['4 + 3 + 8 + 6 = 21.','21 на 9 не делится.'],
        sol:'Нет' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L356.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=тренажёр(s,'т1',Т1,1);
    else if(f===14) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);

    const ЗАГОЛОВКИ={
      1:'Зачем признак',
      2:'На 2 — по парам',
      3:'На 5 — столбиками',
      4:'На 10 — ноль на конце',
      5:'Почему хватает хвоста',
      6:'На 3 — сумма цифр',
      7:'На 9 — строже',
      8:'Почему сумма цифр',
      9:'Проверяем 4 386',
      10:'Девять и три',
      11:'Пропущенная цифра',
      12:'Практика',
      13:'Тренажёр 1',
      14:'Тренажёр 2',
      15:'Тренажёр 3'
    };
    el.innerHTML = `<div class="s6 l356" data-frame="${f}">
        <h2>${ЗАГОЛОВКИ[f]||'Признаки делимости'}</h2>
        ${сцена}
      </div>`;
  }

  window.r356Зачем      =(к)=>{ S().зачем=к; chRender(0); };
  window.r356Два        =(к)=>{ S().два=к; chRender(0); };
  window.r356Пять       =(к)=>{ S().пять=к; chRender(0); };
  window.r356Десять     =(к)=>{ S().десять=к; chRender(0); };
  window.r356Почему     =(к)=>{ S().почему=к; chRender(0); };
  window.r356Три        =(к)=>{ S().три=к; chRender(0); };
  window.r356Девять     =(к)=>{ S().девять=к; chRender(0); };
  window.r356Почемусумма=(к)=>{ S().почемусумма=к; chRender(0); };
  window.r356Длинное    =(к)=>{ S().длинное=к; chRender(0); };
  window.r356Лестница   =(к)=>{ S().лестница=к; chRender(0); };
  window.r356Цифра      =(к)=>{ S().цифра=к; chRender(0); };
  window.r356Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r356Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r356T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=(s[ключ+'Шаг']||0)%набор.задания.length;
    const з=набор.задания[шаг];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r356TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L356; else arr.push(L356);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA356={render:render, L:L356};
})();
