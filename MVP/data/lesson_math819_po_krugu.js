/* ====== МАТЕМАТИКА · УРОК 819 · «ДВИЖЕНИЕ ПО КРУГУ» =======================
   4 класс. Тема была заглушкой «готовится» в MVP/data/soon_lessons.js.

   ПОЧЕМУ ОСТРОВ СВЕРХУ. На прямой дороге «догоняет» и «навстречу» ребёнок уже
   видел. На круге ломается другое: расстояние между двумя лодками не кончается
   никогда, оно наматывается. Поэтому смотреть надо сверху — на круг целиком,
   и тогда видно главное: быстрая лодка догонит медленную не когда пройдёт
   круг сама, а когда ОТЫГРАЕТ у неё целый круг.

   Отыгрывает она по 30 метров в минуту — это разность скоростей. Значит,
   круг в 600 метров отыгрывается за 600 : 30 = 20 минут. Никакой новой
   формулы: то же «вдогонку», только расстояние между ними — длина круга.

   В разные стороны — та же мысль наоборот: сближаются на 90 + 60 = 150 метров
   в минуту, и круг съедается за 600 : 150 = 4 минуты.

   СЮЖЕТ ПОД МИР 4 КЛАССА В «ПУТИ» («Открытое море»): штиль, корабль стоит, и
   команда на двух лодках гребёт вокруг острова. Доска с тремя ответами висит
   над каждым кадром.

   ЯЗЫК. Без «сближения», «относительной скорости» и прочего: отыгрывает,
   догоняет, сходятся. Метры и минуты.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     круг 600 м, быстрая 90 м/мин, медленная 60 м/мин
     в одну сторону: 90 − 60 = 30; 600 : 30 = 20 мин
        проверка: за 20 мин 90 × 20 = 1800 м (3 круга) и 60 × 20 = 1200 м
        (2 круга) — разница ровно один круг ✓
     в разные стороны: 90 + 60 = 150; 600 : 150 = 4 мин
        проверка: 90 × 4 = 360 и 60 × 4 = 240, вместе 600 — целый круг ✓
     встреч за те же 20 минут: 20 : 4 = 5 ✓
     равные скорости: разность 0 — не догонит никогда ✓

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md: главное движение кадра — сами лодки,
   они идут по кругу поворотом вокруг центра острова, и периоды относятся как
   настоящие скорости (2 : 3). prefers-reduced-motion выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 819;
  const GOLD='#ffd76a', GREEN='#8fd1a8', RED='#e86a5a',
        БЫСТРАЯ='#ffd76a', МЕДЛ='#7fd1ff', МОРЕ='#3f8fa8', ЗЕЛЕНЬ='#5f9a52', ПЕСОК='#e0c98a';
  const ИНК='#eef7fa', МУТ='#a8c4cd', ЛИНИЯ='#3f7f93', ОБВОД='#0f2028';

  /* ---------- ЧИСЛА УРОКА ---------- */
  const КРУГ = 600, V1 = 90, V2 = 60;
  const РАЗН = V1 - V2;          /* 30 — столько отыгрывает за минуту */
  const СУММА = V1 + V2;         /* 150 — столько съедают вдвоём навстречу */
  const ДОГОН = КРУГ / РАЗН;     /* 20 минут */
  const ВСТРЕЧА = КРУГ / СУММА;  /* 4 минуты */
  const ВСТРЕЧ = ДОГОН / ВСТРЕЧА;/* 5 встреч за те же 20 минут */

  const ОТВЕТЫ = [
    {ключ:'догон',   имя:'В одну сторону — догонит через', итог:'20 минут'},
    {ключ:'встреча', имя:'Навстречу — сойдутся через',     итог:'4 минуты'},
    {ключ:'сколько', имя:'Встреч за те же 20 минут',       итог:'5'}
  ];
  const найден = (s,к) => !!s['отв_'+к];

  const CSS=`
  #lvis .s6.l819{gap:14px}
  #lvis .s6.l819 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l819 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l819 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#1b3742,#122530);border:1.5px solid var(--line)}
  #lvis .s6.l819 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l819 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l819 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l819 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l819 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l819 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l819 .правило b{color:${GOLD}}

  #lvis .s6.l819 .доска{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(255,215,106,.10),rgba(255,215,106,.03));
    border:1.5px solid rgba(255,215,106,.4);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l819 .доска .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l819 .доска .шапка b{font-size:24px;letter-spacing:0;text-transform:none;color:${GOLD};
    font-variant-numeric:tabular-nums}
  #lvis .s6.l819 .доска ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l819 .доска li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l819 .доска li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;
    border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l819 .доска li.есть{color:${ИНК}}
  #lvis .s6.l819 .доска li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,209,168,.14)}
  #lvis .s6.l819 .доска li span{margin-left:auto;font-variant-numeric:tabular-nums;color:var(--mut);font-size:16px}
  #lvis .s6.l819 .доска li.есть span{color:${GREEN}}

  #lvis .s6.l819 .рычаг{display:flex;align-items:center;gap:10px;width:100%;padding:12px;border-radius:16px;
    background:rgba(255,215,106,.08);border:1.5px solid rgba(255,215,106,.34)}
  #lvis .s6.l819 .рычаг .сколько{flex:1;min-width:0;text-align:center;font-size:16px;line-height:1.25;color:${ИНК}}
  #lvis .s6.l819 .рычаг .сколько b{display:block;font-size:32px;color:${GOLD};font-variant-numeric:tabular-nums}
  #lvis .s6.l819 .рычаг button{width:56px;height:56px;flex:0 0 56px;border-radius:14px;cursor:pointer;
    border:1px solid rgba(255,215,106,.5);background:rgba(255,215,106,.14);color:${GOLD};
    font:inherit;font-size:24px;line-height:1;touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l819 .рычаг button:active{transform:translateY(2px);background:rgba(255,215,106,.26)}
  #lvis .s6.l819 .рычаг button:disabled{opacity:.35;cursor:not-allowed}

  #lvis .s6.l819 .дело{width:100%;min-height:60px;padding:16px;border-radius:16px;cursor:pointer;
    font:inherit;font-size:20px;line-height:1.25;text-align:center;color:#17241a;
    border:0;background:linear-gradient(180deg,${GOLD},#c8912f);
    box-shadow:0 3px 0 rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.4);
    transition:transform 120ms ease, box-shadow 180ms ease}
  #lvis .s6.l819 .дело:active{transform:translateY(2px);box-shadow:0 1px 0 rgba(0,0,0,.42)}
  #lvis .s6.l819 .дело.сделано{background:rgba(143,209,168,.16);color:${GREEN};
    box-shadow:none;border:1.5px solid ${GREEN}}
  #lvis .s6.l819 .дело:disabled{background:rgba(255,255,255,.07);color:var(--mut);
    box-shadow:none;border:1.5px solid rgba(255,255,255,.16);cursor:not-allowed}

  #lvis .s6.l819 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l819 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l819 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l819 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l819 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l819 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center}

  #lvis .s6.l819 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l819 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l819 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l819 .уровни .точка.сейчас{background:${GOLD};animation:l819dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l819dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l819 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l819{-webkit-text-size-adjust:100%}
  #lvis .s6.l819 [data-anim]{animation:l819rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l819rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l819 [data-anim]{animation:none!important}
    #lvis .s6.l819 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l819 .ask button,#lvis .s6.l819 .рычаг button,#lvis .s6.l819 .дело{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l819-style');
      if(!s){ s=document.createElement('style'); s.id='l819-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Задача</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ДЕЛО = (текст,on,сделано,можно) =>
    `<button type="button" data-anim style="--i:3" class="дело${сделано?' сделано':''}"
       ${можно===false&&!сделано?'disabled':''} onclick="${on}">${текст}</button>`;

  const ДОСКА = (s) => A(0,'доска',
    `<div class="шапка"><span>Круг вокруг острова</span><b>${КРУГ} м</b></div>
     <ul>${ОТВЕТЫ.map(о=>{
       const есть = найден(s,о.ключ);
       return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${о.имя}
         <span>${есть?о.итог:'—'}</span></li>`;
     }).join('')}</ul>`);

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анРаз = (имя,значения,длит,задержка) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" fill="freeze"
              ${задержка?'begin="'+задержка+'"':''}
              calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const очередь = (номер,всего,длит) => {
    if(!ДВИЖ) return '';
    const д = 1/(всего+1), a = кт(номер*д), b = кт(номер*д+д*0.5), c = кт(номер*д+д*0.9);
    return анК('opacity','0.6;0.6;1;1;0.6',длит,'0;'+a+';'+b+';'+c+';1');
  };
  const рамкаВсп = (x0,y0,x1,y1,цвет,длит,доля) => ДВИЖ ? `<rect x="${x0}" y="${y0}"
      width="${x1-x0}" height="${y1-y0}" rx="10" fill="none" stroke="${цвет||GOLD}"
      stroke-width="2.2" opacity="0">
      ${анК('opacity','0;0;0.95;0',длит,'0;'+кт(доля)+';'+кт(доля+0.1)+';1')}
    </rect>` : '';
  const кольцо = (x,y,r,цвет,длит,доля) => ДВИЖ ? `<circle cx="${x}" cy="${y}" r="${r}" fill="none"
      stroke="${цвет||GOLD}" stroke-width="2.2" opacity="0">
      ${анК('opacity','0;0;0.95;0',длит,'0;'+кт(доля)+';'+кт(доля+0.1)+';1')}
      ${анК('r',r+';'+r+';'+(r+8)+';'+(r+8),длит,'0;'+кт(доля)+';'+кт(доля+0.16)+';1')}
    </circle>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <radialGradient id="c819-море" cx="0.5" cy="0.45" r="0.75">
        <stop offset="0" stop-color="#4aa3bd"/><stop offset="1" stop-color="#17394a"/>
      </radialGradient>
      <linearGradient id="c819-фон" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#1f3f4c"/><stop offset="0.55" stop-color="#15303c"/>
        <stop offset="1" stop-color="#0c1d25"/>
      </linearGradient>
      <radialGradient id="c819-остров" cx="0.42" cy="0.36" r="0.75">
        <stop offset="0" stop-color="#7cb268"/><stop offset="1" stop-color="#3f6c3a"/>
      </radialGradient>
      <linearGradient id="c819-песок" x1="0" y1="0" x2="1" y2="1">
        <stop offset="0" stop-color="#f0dcab"/><stop offset="1" stop-color="#c8ab6e"/>
      </linearGradient>
      <linearGradient id="c819-быстро" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#ffe9a8"/><stop offset="1" stop-color="#d9a63a"/>
      </linearGradient>
      <linearGradient id="c819-медленно" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#bfe6ff"/><stop offset="1" stop-color="#3f8fbd"/>
      </linearGradient>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||250}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c819-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+16, в=к+10;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g>
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(8,20,26,.9)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* ---------------- КРУГ ВОКРУГ ОСТРОВА -------------------------------------
     Доля — часть круга от старта: 0 — верхняя точка, дальше по часовой. */
  const доляТочка = (cx,cy,r,д) => {
    const a = д*2*Math.PI - Math.PI/2;
    return [cx + r*Math.cos(a), cy + r*Math.sin(a)];
  };
  /* остриё на конце дуги: без него «в одну сторону» и «в разные» на картинке
     неотличимы — обе выглядят как две дуги на круге */
  const остриё = (cx,cy,r,доля,цвет,назад) => {
    const [x,y] = доляТочка(cx,cy,r,доля);
    const угол = доля*360 + (назад?180:0);
    return `<g transform="translate(${x.toFixed(1)} ${y.toFixed(1)}) rotate(${угол.toFixed(1)})">
      <path d="M7 0 L-4 -5 L-4 5 z" fill="${цвет}"/></g>`;
  };
  const дугаДоли = (cx,cy,r,д0,д1,цвет,ш) => {
    const шаг = д1-д0;
    if(шаг <= 0.001) return '';
    if(шаг >= 0.999) return `<circle cx="${cx}" cy="${cy}" r="${r}" fill="none"
      stroke="${цвет}" stroke-width="${ш||7}"/>`;
    const [x0,y0] = доляТочка(cx,cy,r,д0), [x1,y1] = доляТочка(cx,cy,r,д1);
    return `<path d="M${x0.toFixed(1)} ${y0.toFixed(1)} A${r} ${r} 0 ${шаг>0.5?1:0} 1 ${x1.toFixed(1)} ${y1.toFixed(1)}"
      fill="none" stroke="${цвет}" stroke-width="${ш||7}" stroke-linecap="round"/>`;
  };

  /* Остров сверху: неровный берег, песчаная кромка и две пальмы, чтобы кадр
     читался как вид с высоты, а не как пятно. */
  const остров = (cx,cy,r) => `<g>
    <path d="M${cx-r} ${cy+r*0.1}
      q${r*0.1} ${-r*0.8} ${r*0.62} ${-r*0.86}
      q${r*0.5} ${-r*0.1} ${r*0.86} ${r*0.3}
      q${r*0.34} ${r*0.4} ${r*0.12} ${r*0.72}
      q${-r*0.3} ${r*0.44} ${-r*0.9} ${r*0.34}
      q${-r*0.6} ${-r*0.1} ${-r*0.7} ${-r*0.5} z"
      fill="url(#c819-песок)" stroke="${ОБВОД}" stroke-width="1.2"/>
    <path d="M${cx-r*0.78} ${cy+r*0.06}
      q${r*0.1} ${-r*0.6} ${r*0.5} ${-r*0.64}
      q${r*0.4} ${-r*0.06} ${r*0.66} ${r*0.24}
      q${r*0.26} ${r*0.3} ${r*0.08} ${r*0.54}
      q${-r*0.24} ${r*0.34} ${-r*0.7} ${r*0.26}
      q${-r*0.46} ${-r*0.08} ${-r*0.54} ${-r*0.4} z"
      fill="url(#c819-остров)" stroke="rgba(20,50,20,.5)" stroke-width="1"/>
    ${[[-0.28,-0.2],[0.22,0.1]].map(п=>{
      const x=cx+r*п[0], y=cy+r*п[1];
      return `<g><line x1="${x.toFixed(1)}" y1="${y.toFixed(1)}" x2="${x.toFixed(1)}" y2="${(y-10).toFixed(1)}"
        stroke="#6b4a22" stroke-width="2"/>
        <circle cx="${x.toFixed(1)}" cy="${(y-13).toFixed(1)}" r="6" fill="#4f8f42" stroke="rgba(20,50,20,.6)" stroke-width="0.9"/></g>`;
    }).join('')}
  </g>`;

  /* Гребная лодка сверху. Нос смотрит по ходу: доля 0 — вверху, идём вправо. */
  const лодка = (cx,cy,r,доля,цвет,назад) => {
    const [x,y] = доляТочка(cx,cy,r,доля);
    const угол = доля*360 + (назад?180:0);
    return `<g transform="translate(${x.toFixed(1)} ${y.toFixed(1)}) rotate(${угол.toFixed(1)})">
      <line x1="-2" y1="-9" x2="-2" y2="9" stroke="#e8dcc0" stroke-width="1.6" opacity=".85"/>
      <path d="M11 0 q-5 6.4 -11 6.4 q-6.4 0 -6.4 -6.4 q0 -6.4 6.4 -6.4 q6 0 11 6.4 z"
        fill="${цвет==='б'?'url(#c819-быстро)':'url(#c819-медленно)'}"
        stroke="${ОБВОД}" stroke-width="1.2"/>
      <path d="M-4 0 h9" stroke="rgba(30,20,8,.45)" stroke-width="1.2"/>
    </g>`;
  };
  /* Лодка идёт по кругу: поворот всей группы вокруг центра острова.
     Периоды относятся как настоящие скорости — быстрая обгоняет на глазах. */
  const везётПоКругу = (cx,cy,тело,период,назад) => {
    if(!ДВИЖ || !период) return тело;
    const к = назад ? -360 : 360;
    return `<g><animateTransform attributeName="transform" type="rotate"
      values="0 ${cx} ${cy};${к} ${cx} ${cy}" dur="${период}" repeatCount="indefinite"/>${тело}</g>`;
  };
  const идётПоКругу = (cx,cy,r,доля,цвет,период,назад) =>
    везётПоКругу(cx,cy, лодка(cx,cy,r,доля,цвет,назад), период, назад);

  /* Разрыв, наматывающийся на круг: дуга обегает круг целиком.
     ВАЖНО: это ещё и единственный <animate> в таких кадрах. Без него Chrome
     не заводит таймлайн SVG, где есть только animateTransform, и лодки стоят
     (проверено замером положения лодки во времени, глазом это не видно). */
  const наматывается = (cx,cy,r,цвет,длит) => {
    if(!ДВИЖ) return дугаДоли(cx,cy,r,0,0.55,цвет,7);
    const о = (2*Math.PI*r).toFixed(1);
    return `<circle cx="${cx}" cy="${cy}" r="${r}" fill="none" stroke="${цвет}" stroke-width="7"
      stroke-linecap="round" stroke-dasharray="${о}" stroke-dashoffset="${о}"
      transform="rotate(-90 ${cx} ${cy})">
      <animate attributeName="stroke-dashoffset" values="${о};0;0;${о}" dur="${длит||'8s'}"
        keyTimes="0;0.72;0.86;1" calcMode="linear" repeatCount="indefinite"/>
    </circle>`;
  };

  /* Дорожка вокруг острова: пунктир, чтобы читалась как путь, а не как обод */
  const дорожка = (cx,cy,r) => `<g>
    <circle cx="${cx}" cy="${cy}" r="${r}" fill="none" stroke="rgba(255,255,255,.35)"
      stroke-width="2" stroke-dasharray="7 7"/>
    <circle cx="${cx}" cy="${(cy-r).toFixed(1)}" r="3.4" fill="${ИНК}" opacity=".9"/>
  </g>`;

  /* ---------------- ПОЛОСА С ДЕЛЕНИЯМИ --------------------------------------
     Круг, разложенный в прямую: видно, что 600 метров — это двадцать раз по 30. */
  const полоса = (x,y,ширина,частей,цвет,залив) => `<g>
    <rect x="${x}" y="${y}" width="${ширина}" height="24" rx="3"
      fill="${залив||'rgba(255,215,106,.18)'}" stroke="${цвет||GOLD}" stroke-width="1.4"/>
    ${Array.from({length:частей-1},(_,i)=>`<line x1="${(x+ширина*(i+1)/частей).toFixed(1)}" y1="${y+((i+1)%5?8:0)}"
      x2="${(x+ширина*(i+1)/частей).toFixed(1)}" y2="${y+24}"
      stroke="${(i+1)%5?'rgba(255,255,255,.28)':цвет||GOLD}" stroke-width="${(i+1)%5?0.9:1.6}"/>`).join('')}
  </g>`;

  /* столбик скорости: 90 и 60 рядом, разница видна сразу */
  const скорость = (x,y,значение,ед,цвет,подпись_) => `<g>
    <rect x="${x}" y="${y}" width="${(значение*ед).toFixed(1)}" height="20" rx="3"
      fill="${цвет==='б'?'url(#c819-быстро)':'url(#c819-медленно)'}" stroke="${ОБВОД}" stroke-width="1.1"/>
    ${т(x+значение*ед/2, y+15, подпись_, 14, '#12232b', true)}
  </g>`;

  /* ================= КАДРЫ ================= */
  const море = (в) => `<rect x="0" y="0" width="336" height="${в}" fill="url(#c819-море)"/>
    <rect x="0" y="0" width="336" height="${в}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  /* 1. Штиль: две лодки идут по кругу */
  function F1(s){
    return ДОСКА(s) +
      ЗАДАЧА('Полный штиль, корабль стоит. Команда села на две лодки и гребёт вокруг острова <b>в одну сторону</b>. Круг — <b>600 метров</b>. Одна лодка идёт <b>90 метров в минуту</b>, другая <b>60</b>. Через сколько минут быстрая догонит медленную?') +
      `<div class="pic">${свг(`
        ${море(252)}
        ${остров(168,124,42)}
        ${дорожка(168,124,60)}
        ${наматывается(168,124,60,'rgba(255,215,106,.7)','9s')}
        ${идётПоКругу(168,124,60,0,'м','7.5s')}
        ${идётПоКругу(168,124,60,0,'б','5s')}
        ${подпись(168,26,'Штиль у острова',GOLD,16)}
        ${подпись(168,212,'круг — 600 метров',GOLD,14)}
        ${подпись(84,242,'быстрая 90 м/мин',БЫСТРАЯ,14)}
        ${подпись(252,242,'медленная 60',МЕДЛ,14)}
      `,252)}</div>` +
      СКАЗ('В чём подвох','На круге расстояние между лодками <b>никогда не кончается</b> — оно наматывается. Быстрая догонит медленную не тогда, когда пройдёт круг сама, а когда <b>отыграет у неё целый круг</b>.') +
      ПРАВИЛО('Смотри сверху на весь круг, а не на одну лодку.');
  }

  /* 2. Песочница: крути время и смотри на разрыв */
  function F2(s){
    const t = Math.max(0, Math.min(24, s.минут==null?6:s.минут));
    const дБ = (V1*t/КРУГ) % 1, дМ = (V2*t/КРУГ) % 1;
    const разрыв = РАЗН*t, вКругах = разрыв/КРУГ;
    const вместе = разрыв>0 && разрыв % КРУГ === 0;
    return ДОСКА(s) +
      ЗАДАЧА('Покрути время и следи за золотой дугой — это то, что быстрая уже отыграла у медленной.') +
      `<div class="pic">${свг(`
        ${море(256)}
        ${остров(168,132,40)}
        ${дорожка(168,132,58)}
        <g>${проявить('6s',0.4,0.58)}${вКругах>=1 ? дугаДоли(168,132,58,0,1,'rgba(255,215,106,.85)',7)
                     : дугаДоли(168,132,58,дМ,дМ+вКругах,'rgba(255,215,106,.85)',7)}</g>
        ${лодка(168,132,58,дМ,'м')}
        ${лодка(168,132,58,дБ,'б')}
        ${вместе?кольцо(...доляТочка(168,132,58,дБ),16,GREEN,'6s',0.2):''}
        ${подпись(168,26,'Крути время',GOLD,14)}
        ${подпись(168,56,'прошло '+t+' мин',МУТ,14)}
        ${подпись(168,220, вместе ? 'отыграла целый круг — они рядом'
                                  : ('отыграла '+разрыв+' м из 600'),
          вместе?GREEN:GOLD, 14)}
        ${т(88,244,'быстрая '+(V1*t)+' м',14,БЫСТРАЯ)}
        ${т(248,244,'медленная '+(V2*t)+' м',14,МЕДЛ)}
      `,256)}</div>` +
      A(3,'рычаг',
        `<button type="button" onclick="r819Мин(-2)" ${t>0?'':'disabled'} aria-label="убавить время">−</button>
         <span class="сколько">минут прошло<b>${t}</b></span>
         <button type="button" onclick="r819Мин(2)" ${t<24?'':'disabled'} aria-label="прибавить время">+</button>`) +
      СКАЗ('Смотри', вместе
        ? 'Золотая дуга замкнулась: отыграно ровно <b>600 метров</b>, то есть целый круг — и лодки снова борт о борт. Это и есть ответ: <b>20 минут</b>.'
        : (t===0 ? 'Обе на старте, дуги ещё нет.'
                 : 'За '+t+' мин быстрая ушла вперёд на <b>'+разрыв+' м</b>. Пока это меньше круга — значит, ещё не догнала.')) +
      ПРАВИЛО('Разрыв растёт на <b>30 метров каждую минуту</b>, и расти ему до целого круга.');
  }

  /* 3. Догоняет не лодка, а разность. Главный кадр урока. */
  function F3(s){
    const в = s.о1, верно = в==='ok', ЕД = 1.5;
    return ДОСКА(s) +
      ЗАДАЧА('За минуту быстрая проходит 90 метров, медленная 60. Значит, за минуту она отыгрывает у неё <b>30 метров</b>. Отыграть нужно целый круг — 600. Сколько минут на это уйдёт?') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${подпись(168,26,'Отыгрывает разность',GOLD,16)}
        <g>${очередь(0,3,'8s')}${скорость(96,64,V1,ЕД,'б','90')}${т(88,79,'быстрая',14,БЫСТРАЯ,false,'end')}</g>
        <g>${очередь(1,3,'8s')}${скорость(96,92,V2,ЕД,'м','60')}${т(88,107,'медленная',14,МЕДЛ,false,'end')}</g>
        <g>${очередь(2,3,'8s')}
          <rect x="96" y="120" width="${(РАЗН*ЕД).toFixed(1)}" height="20" rx="3"
            fill="rgba(255,215,106,.35)" stroke="${GOLD}" stroke-width="1.4"/>
          ${т(96+РАЗН*ЕД/2,135,'30',14,GOLD,true)}
          ${т(88,135,'разница',14,GOLD,false,'end')}</g>
        ${подпись(168,166,'за минуту отыгрывает 30 м',GOLD,14)}
        ${полоса(28,184,280,20,GOLD, верно?'rgba(143,209,168,.2)':'rgba(255,215,106,.18)')}
        ${[0,5,10,15,20].map(м=>т(28+м*14,220,String(м),14,МУТ)).join('')}
        ${верно?рамкаВсп(24,180,312,212,GREEN,'7s',0.2):''}
        ${подпись(168,246, верно?'600 : 30 = 20 минут':'круг — это двадцать раз по 30',
          верно?GREEN:МУТ, 16)}
      `,256)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '20 минут', "r819О1('ok')")}
        ${BTN(5, в==='no'?'miss':'', '6 с лишним минут', "r819О1('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Разрыв растёт по 30 метров в минуту, а вырасти ему надо до 600. Двадцать раз по 30 — это и есть 600, значит 20 минут. Полоска внизу как раз показывает эти двадцать кусков.'
              : 'Нет: 6 с лишним — это 600 : 90, столько быстрая идёт круг <b>сама</b>. Но медленная за это время тоже уехала. Делить надо на разность: 600 : 30 = 20.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('В одну сторону: <b>круг делят на разность скоростей</b>.');
  }

  /* 4. Ловушка: разделить круг на скорость быстрой */
  function F4(s){
    const в = s.о2, верно = в==='ok';
    const t = КРУГ/V1;                      /* 6 с лишним минут */
    return ДОСКА(s) +
      ЗАДАЧА('Самая частая ошибка: «быстрая проходит круг за 600 : 90, значит через столько и догонит». Посмотри на картинку — быстрая уже на старте, а медленная где?') +
      `<div class="pic">${свг(`
        ${море(248)}
        <g>${проявить('6s',0.42,0.6)}${подпись(168,58,'600 : 90 — так нельзя',RED,14)}</g>
        <path d="M70 34 L266 60" stroke="${RED}" stroke-width="2.2" opacity=".75"/>
        <path d="M70 60 L266 34" stroke="${RED}" stroke-width="2.2" opacity=".75"/>
        ${остров(168,142,38)}
        ${дорожка(168,142,54)}
        ${дугаДоли(168,142,54,(V2*t/КРУГ)%1,1,'rgba(255,215,106,.8)',6)}
        ${лодка(168,142,54,(V2*t/КРУГ)%1,'м')}
        ${лодка(168,142,54,0,'б')}
        ${верно?кольцо(...доляТочка(168,142,54,(V2*t/КРУГ)%1),15,RED,'6s',0.22):''}
        ${подпись(168,26,'Ловушка',GOLD,16)}
        ${подпись(168,232, верно?'медленная ушла на треть круга вперёд':'где будет медленная?',
          верно?GREEN:МУТ, 14)}
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), 'Медленная тоже не стоит на месте', "r819О2('ok')")}
        ${BTN(5, в==='no'?'miss':'', 'Потому что 600 не делится на 90 нацело', "r819О2('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Быстрая вернулась на старт, а медленная за то же время уехала вперёд — догонять ещё и догонять. Поэтому круг делят не на 90, а на 30: столько быстрая отыгрывает за минуту.'
              : 'Нет, дело не в делении нацело. Дело в том, что пока быстрая шла свой круг, медленная тоже гребла. Круг делят на разность: 600 : 30 = 20.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Делить на скорость быстрой можно, только если медленная <b>стоит</b>.');
  }

  /* 5. Проверка: разложить пути на круги */
  function F5(s){
    const разл = !!s.разложено, ЕД = 0.13;
    const деления = (x,y,всего) => разл ? Array.from({length:Math.round(всего/КРУГ)-1},(_,i)=>
      `<line x1="${(x+(i+1)*КРУГ*ЕД).toFixed(1)}" y1="${y}" x2="${(x+(i+1)*КРУГ*ЕД).toFixed(1)}" y2="${y+24}"
        stroke="${ИНК}" stroke-width="2">${анРаз('opacity','0;1','360ms',(0.12*i).toFixed(2)+'s')}</line>`).join('') : '';
    return ДОСКА(s) +
      ЗАДАЧА('Проверим ответ. За 20 минут быстрая прошла 90 × 20 = <b>1800 метров</b>, медленная 60 × 20 = <b>1200</b>. Разложим оба пути на круги по 600 и посмотрим, сходится ли.') +
      `<div class="pic">${свг(`
        ${фон(244)}
        ${подпись(168,26,'Проверка',GOLD,16)}
        ${т(54,92,'быстрая — 1800 м',14,БЫСТРАЯ,false,'start')}
        <g>${разл?'':очередь(0,2,'7s')}
          <rect x="54" y="98" width="${(1800*ЕД).toFixed(1)}" height="24" rx="3"
            fill="url(#c819-быстро)" opacity=".85" stroke="${ОБВОД}" stroke-width="1.2"/></g>
        ${деления(54,98,1800)}
        ${т(54,134,'медленная — 1200 м',14,МЕДЛ,false,'start')}
        <g>${разл?'':очередь(1,2,'7s')}
          <rect x="54" y="140" width="${(1200*ЕД).toFixed(1)}" height="24" rx="3"
            fill="url(#c819-медленно)" opacity=".85" stroke="${ОБВОД}" stroke-width="1.2"/></g>
        ${деления(54,140,1200)}
        ${разл?рамкаВсп(206,94,292,126,GREEN,'6s',0.18):''}
        ${подпись(168,192, разл?'3 круга и 2 круга':'по скольку кругов вышло?', разл?GREEN:МУТ, 14)}
        ${подпись(168,224, разл?'3 − 2 = 1 круг — сошлось':'нажми кнопку под рисунком', разл?GREEN:МУТ, 14)}
      `,244)}</div>` +
      ДЕЛО(разл ? '✓ Разложено: 3 круга и 2 круга' : 'Разложить пути на круги', "r819Разложить()", разл, true) +
      СКАЗ('Зачем', разл
        ? 'Сошлось: быстрая намотала три круга, медленная два, разница — ровно один круг. Значит, 20 минут найдены верно.'
        : 'Если ответ верный, то за это время быстрая должна оторваться ровно на один круг — не больше и не меньше.') +
      ПРАВИЛО('Проверка простая: <b>разница путей должна равняться одному кругу</b>.');
  }

  /* 6. В разные стороны */
  function F6(s){
    const в = s.о3, верно = в==='ok';
    const t = верно ? ВСТРЕЧА : 2;
    const дБ = (V1*t/КРУГ)%1, дМ = 1-((V2*t/КРУГ)%1);
    return ДОСКА(s) +
      ЗАДАЧА('Теперь лодки гребут <b>в разные стороны</b> от одного места. Скорости те же: 90 и 60 метров в минуту. Через сколько минут они сойдутся?') +
      `<div class="pic">${свг(`
        ${море(256)}
        ${остров(168,136,38)}
        ${дорожка(168,136,56)}
        <g>${очередь(0,2,'7s')}${дугаДоли(168,136,56,0,дБ,'rgba(255,215,106,.8)',6)}</g>
        <g>${очередь(1,2,'7s')}${дугаДоли(168,136,56,дМ,1,'rgba(127,209,255,.8)',6)}</g>
        ${лодка(168,136,56,дБ,'б')}
        ${лодка(168,136,56,дМ,'м',true)}
        ${верно?кольцо(...доляТочка(168,136,56,дБ),16,GREEN,'6s',0.22):''}
        ${подпись(168,26,'В разные стороны',GOLD,16)}
        ${подпись(168,56,'гребут навстречу друг другу',МУТ,14)}
        ${подпись(168,216,'сближаются на 90 + 60 = 150 м в минуту',GOLD,14)}
        ${подпись(168,248, верно?'600 : 150 = 4 минуты':'через сколько сойдутся?',
          верно?GREEN:МУТ, 14)}
      `,256)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '4 минуты', "r819О3('ok')")}
        ${BTN(5, в==='no'?'miss':'', '20 минут', "r819О3('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Навстречу круг съедают вдвоём: 90 + 60 = 150 метров в минуту. Значит, 600 : 150 = 4 минуты. Проверка: 90 × 4 = 360 и 60 × 4 = 240, вместе ровно 600 — целый круг.'
              : 'Нет: 20 минут — это когда они гребут в одну сторону. Навстречу они съедают круг вдвоём, по 150 метров в минуту: 600 : 150 = 4.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('В разные стороны: <b>круг делят на сумму скоростей</b>.');
  }

  /* 7. Сводка: когда вычитать, когда складывать */
  function F7(s){
    const в = s.сторона || null;
    const мини = (cx,свои,цвет) => `<g>
      ${остров(cx,138,26)}
      ${дорожка(cx,138,40)}
      ${свои==='одна'
        ? дугаДоли(cx,138,40,0.06,0.44,'rgba(255,215,106,.8)',5)+дугаДоли(cx,138,40,0.56,0.82,'rgba(127,209,255,.8)',5)
        : дугаДоли(cx,138,40,0.06,0.42,'rgba(255,215,106,.8)',5)+дугаДоли(cx,138,40,0.58,0.94,'rgba(127,209,255,.8)',5)}
      ${свои==='одна'
        ? остриё(cx,138,40,0.08,БЫСТРАЯ)+остриё(cx,138,40,0.92,МЕДЛ)
        : остриё(cx,138,40,0.08,БЫСТРАЯ)+остриё(cx,138,40,0.92,МЕДЛ,true)}
      ${лодка(cx,138,40,0.44,'б')}
      ${свои==='одна' ? лодка(cx,138,40,0.82,'м') : лодка(cx,138,40,0.58,'м',true)}
      ${в===свои?кольцо(cx,138,50,цвет,'6s',0.2):''}
    </g>`;
    return ДОСКА(s) +
      ЗАДАЧА('Два случая рядом. Нажми любой круг и посмотри, что со скоростями делают.') +
      `<div class="pic">${свг(`
        ${фон(240)}
        ${подпись(168,26,'Вычитать или складывать',GOLD,16)}
        ${подпись(88,68,'в одну сторону',в==='одна'?GOLD:МУТ,14)}
        ${подпись(248,68,'в разные стороны',в==='разные'?GOLD:МУТ,14)}
        <g>${в?'':очередь(0,2,'7s')}${мини(88,'одна',GOLD)}</g>
        <g>${в?'':очередь(1,2,'7s')}${мини(248,'разные',GOLD)}</g>
        ${т(88,200,'90 − 60 = 30',16,в==='одна'?GOLD:МУТ,true)}
        ${т(248,200,'90 + 60 = 150',16,в==='разные'?GOLD:МУТ,true)}
        ${подпись(168,232, в==='одна'?'600 : 30 = 20 минут'
                        :(в==='разные'?'600 : 150 = 4 минуты':'нажми круг под рисунком'),
          в?GREEN:МУТ, 14)}
      `,240)}</div>` +
      `<div class="ask пара">
        ${BTN(4, в==='одна'?'hit':'', 'В одну сторону', "r819Сторона('одна')")}
        ${BTN(5, в==='разные'?'hit':'', 'В разные стороны', "r819Сторона('разные')")}
      </div>` +
      СКАЗ('Смотри', в==='одна'
        ? 'Догоняют — значит, работает только <b>разница</b> скоростей: быстрая отыгрывает по 30 метров в минуту.'
        : (в==='разные'
          ? 'Сходятся — значит, обе скорости работают <b>вместе</b>: круг съедается по 150 метров в минуту.'
          : 'Нажми одну из двух кнопок.')) +
      ПРАВИЛО('Догоняют — <b>вычитай</b>. Сходятся — <b>складывай</b>. Круг дели на то, что вышло.');
  }

  /* 8. Сколько встреч */
  function F8(s){
    const в = s.о4, верно = в==='ok';
    const X = (м)=> 36 + м*13.2;
    return ДОСКА(s) +
      ЗАДАЧА('Сойдясь, лодки не останавливаются — гребут дальше и сходятся снова, и так каждые <b>4 минуты</b>. Сколько раз они сойдутся за те же <b>20 минут</b>?') +
      `<div class="pic">${свг(`
        ${фон(224)}
        ${подпись(168,26,'Встречи по кругу',GOLD,16)}
        ${подпись(168,58,'сходятся каждые 4 минуты',GOLD,14)}
        <line x1="36" y1="120" x2="300" y2="120" stroke="${МУТ}" stroke-width="1.6"/>
        ${[4,8,12,16,20].map((м,i)=>`<g>${очередь(i,5,'8s')}
          <line x1="${X(м).toFixed(1)}" y1="112" x2="${X(м).toFixed(1)}" y2="128" stroke="${GOLD}" stroke-width="2"/>
          <circle cx="${(X(м)-5).toFixed(1)}" cy="100" r="5" fill="url(#c819-быстро)" stroke="${ОБВОД}" stroke-width="0.9"/>
          <circle cx="${(X(м)+5).toFixed(1)}" cy="100" r="5" fill="url(#c819-медленно)" stroke="${ОБВОД}" stroke-width="0.9"/>
          ${т(X(м),146,String(м),14,МУТ)}</g>`).join('')}
        ${т(36,146,'0',14,МУТ)}
        ${верно?рамкаВсп(32,90,306,134,GREEN,'7s',0.2):''}
        ${подпись(168,182, верно?'20 : 4 = 5 встреч':'сколько раз за 20 минут?', верно?GREEN:МУТ, 16)}
        ${т(168,210,'первая на 4-й минуте, последняя на 20-й',14,МУТ)}
      `,224)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '5 раз', "r819О4('ok')")}
        ${BTN(5, в==='no'?'miss':'', '4 раза', "r819О4('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно: 20 : 4 = 5. Смотри, как сходится: пока в одну сторону быстрая только-только отыграла один круг, навстречу они успели сойтись пять раз.'
              : 'Нет: встречи на 4-й, 8-й, 12-й, 16-й и 20-й минуте — их пять. Посчитай засечки на ленте.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Встречи повторяются <b>через равные промежутки</b>: сколько их — столько раз промежуток уложился во время.');
  }

  /* 9. А если скорости равны */
  function F9(s){
    const в = s.о5, верно = в==='ok';
    return ДОСКА(s) +
      ЗАДАЧА('Последний случай. Обе лодки идут <b>в одну сторону</b> и обе по <b>70 метров в минуту</b>. Одна вышла раньше и ушла на полкруга вперёд. Через сколько минут вторая её догонит?') +
      `<div class="pic">${свг(`
        ${море(246)}
        ${остров(168,130,38)}
        ${дорожка(168,130,56)}
        <g>${проявить('6s',0.45,0.62)}${везётПоКругу(168,130,
          дугаДоли(168,130,56,0,0.5, верно?'rgba(232,106,90,.7)':'rgba(255,255,255,.28)',6),'7s')}</g>
        ${идётПоКругу(168,130,56,0,'м','7s')}
        ${идётПоКругу(168,130,56,0.5,'б','7s')}
        ${подпись(168,26,'А если скорости равны',GOLD,16)}
        ${подпись(168,56,'обе по 70 м/мин',МУТ,14)}
        ${подпись(168,208,'70 − 70 = 0 — отыгрывать нечем',верно?RED:МУТ,14)}
        ${подпись(168,236, верно?'разрыв не меняется никогда':'что будет с разрывом?',
          верно?GREEN:МУТ, 14)}
      `,246)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), 'Никогда: разница скоростей — ноль', "r819О5('ok')")}
        ${BTN(5, в==='no'?'miss':'', 'Через 600 : 70 минут', "r819О5('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Отыгрывать нечем: за минуту обе проходят поровну, и полкруга так и останутся полкругом. Вот почему в этих задачах главное число — <b>разница</b> скоростей: если она ноль, догонять нечем.'
              : 'Нет: 600 : 70 — это время круга, а не догона. Разница скоростей здесь 70 − 70 = 0, значит разрыв не сокращается вообще.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Нет разницы скоростей — <b>нет и догона</b>, сколько ни греби.');
  }

  /* 10. Итог */
  function F10(s){
    const всё = ОТВЕТЫ.every(о=>найден(s,о.ключ));
    return ДОСКА(s) +
      ЗАДАЧА(всё
        ? 'Доска заполнена: все три числа найдены, и все три — из одного круга в 600 метров.'
        : 'На доске записано не всё — вернись и добей ответы. А пока посмотри, чем кончается штиль.') +
      `<div class="pic">${свг(`
        ${море(248)}
        ${остров(168,126,42)}
        ${дорожка(168,126,60)}
        ${идётПоКругу(168,126,60,0,'м','7.5s')}
        ${идётПоКругу(168,126,60,0,'б','5s')}
        ${подпись(168,26,'Штиль кончился',GOLD,16)}
        <g>${проявить('8s',0.42,0.58)}${подпись(168,212,'в одну сторону — 600 : 30 = 20 мин',GREEN,14)}</g>
        <g>${проявить('8s',0.62,0.78)}${подпись(168,238,'навстречу — 600 : 150 = 4 мин',GREEN,14)}</g>
      `,248)}</div>` +
      СКАЗ('Итог','Никакой новой формулы тут нет. Это всё то же «вдогонку» и «навстречу», только расстояние между лодками — <b>длина круга</b>. В одну сторону делим круг на разность скоростей, в разные — на сумму.') +
      ПРАВИЛО('<b>Круг : разность — догонит. Круг : сумма — сойдутся.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Круг 400 м. Одна лодка идёт 80 м/мин, другая 60, обе в одну сторону. Через сколько быстрая догонит?',
      варианты:[{т:'Через 20 минут',ок:true},{т:'Через 5 минут',ок:false}],
      разбор:'Отыгрывает 80 − 60 = 20 м в минуту, а отыграть надо круг: 400 : 20 = 20 минут.' },
    { вопрос:'Круг 300 м. Скорости 70 и 80 м/мин, гребут в разные стороны. Через сколько сойдутся?',
      варианты:[{т:'Через 2 минуты',ок:true},{т:'Через 30 минут',ок:false}],
      разбор:'Навстречу складывают: 70 + 80 = 150 м в минуту. 300 : 150 = 2 минуты.' },
    { вопрос:'Круг 600 м. Гребли навстречу и сошлись через 5 минут. Одна шла 70 м/мин. Какова скорость второй?',
      варианты:[{т:'50 м/мин',ок:true},{т:'120 м/мин',ок:false}],
      разбор:'Вдвоём съедали 600 : 5 = 120 м в минуту. Значит вторая: 120 − 70 = 50.' },
    { вопрос:'Круг 480 м, скорости 90 и 60, обе в одну сторону. Один сказал: 480 : 150 = 3,2 минуты. Верно?',
      варианты:[{т:'Нет: в одну сторону делят на разность, 480 : 30 = 16',ок:true},
                {т:'Верно: 90 и 60 надо сложить',ок:false}],
      разбор:'Складывают, когда гребут навстречу. А тут догоняют — значит, разность: 90 − 60 = 30 и 480 : 30 = 16 минут.' },
    { вопрос:'Лодки сходятся каждые 4 минуты. Сколько раз они сойдутся за 20 минут?',
      варианты:[{т:'5 раз',ок:true},{т:'4 раза',ок:false}],
      разбор:'Встречи на 4-й, 8-й, 12-й, 16-й и 20-й минуте: 20 : 4 = 5.' }
  ];

  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r819Reset()")}</div>` +
        ПРАВИЛО('Догоняют — <b>вычитай</b>. Сходятся — <b>складывай</b>.');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к,
        выбран===к ? (в.ок?'hit':'miss') : '',
        в.т, "r819Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Догоняет', задания:[
    {q:'Круг 600 м, скорости 90 и 60, в одну сторону. Через сколько догонит?', в:0, варианты:['20 мин','4 мин'],
     раз:'90 − 60 = 30; 600 : 30 = 20.'},
    {q:'Круг 400 м, скорости 70 и 50, в одну сторону. Через сколько?', в:1, варианты:['10 мин','20 мин'],
     раз:'70 − 50 = 20; 400 : 20 = 20.'},
    {q:'Круг 900 м, скорости 100 и 70, в одну сторону. Через сколько?', в:0, варианты:['30 мин','9 мин'],
     раз:'100 − 70 = 30; 900 : 30 = 30.'},
    {q:'Круг 240 м, скорости 80 и 60, в одну сторону. Через сколько?', в:1, варианты:['3 мин','12 мин'],
     раз:'80 − 60 = 20; 240 : 20 = 12.'}
  ]};
  const Т2 = { имя:'Сходятся', задания:[
    {q:'Круг 600 м, скорости 90 и 60, в разные стороны. Через сколько сойдутся?', в:0, варианты:['4 мин','20 мин'],
     раз:'90 + 60 = 150; 600 : 150 = 4.'},
    {q:'Круг 800 м, скорости 90 и 110, в разные стороны. Через сколько?', в:1, варианты:['40 мин','4 мин'],
     раз:'90 + 110 = 200; 800 : 200 = 4.'},
    {q:'Круг 450 м, скорости 80 и 70, в разные стороны. Через сколько?', в:0, варианты:['3 мин','45 мин'],
     раз:'80 + 70 = 150; 450 : 150 = 3.'},
    {q:'Круг 360 м, обе по 60, в разные стороны. Через сколько?', в:1, варианты:['6 мин','3 мин'],
     раз:'60 + 60 = 120; 360 : 120 = 3.'}
  ]};
  const Т3 = { имя:'Что делить и на что', задания:[
    {q:'Гребут в одну сторону. На что делят круг?', в:0, варианты:['На разность скоростей','На сумму скоростей'],
     раз:'Догоняют — работает только разница: она и съедает круг.'},
    {q:'Гребут в разные стороны. На что делят круг?', в:1, варианты:['На разность скоростей','На сумму скоростей'],
     раз:'Сходятся — обе скорости работают вместе.'},
    {q:'Круг 600 м, догнал ровно через 20 минут. Какова разность скоростей?', в:0, варианты:['30 м/мин','12 м/мин'],
     раз:'600 : 20 = 30 — столько отыгрывалось каждую минуту.'},
    {q:'Обе лодки идут по 80 м/мин в одну сторону. Через сколько догонит?', в:1, варианты:['Через 7,5 мин','Никогда'],
     раз:'Разность 80 − 80 = 0: отыгрывать нечем, разрыв не меняется.'}
  ]};

  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r819T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r819TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L819 = {
    id: ID,
    title: 'Движение по кругу',
    ico: '🔄',
    src: 'Начальная школа · 4 класс · Движение по кругу',
    subj: 'jun',
    explain: [
      'Штиль. Две лодки гребут вокруг острова в одну сторону: круг 600 метров, скорости 90 и 60 м/мин. На круге расстояние наматывается, поэтому догонит она тогда, когда отыграет целый круг.',
      'Покрути время: золотая дуга — это то, что быстрая уже отыграла. На двадцатой минуте дуга замыкается, и лодки снова рядом.',
      'Главное число — разность: за минуту быстрая отыгрывает 90 − 60 = 30 метров. Круг в 600 метров — это двадцать раз по 30, значит 600 : 30 = 20 минут.',
      'Ловушка: «600 : 90, и готово». Нет: пока быстрая шла свой круг, медленная тоже уехала вперёд.',
      'Проверка: за 20 минут быстрая прошла 1800 м — три круга, медленная 1200 м — два круга. Разница ровно один круг.',
      'В разные стороны круг съедают вдвоём: 90 + 60 = 150 метров в минуту, и 600 : 150 = 4 минуты.',
      'Сводка: догоняют — вычитай скорости, сходятся — складывай. Круг дели на то, что вышло.',
      'Встречи повторяются каждые 4 минуты, значит за 20 минут их будет 20 : 4 = 5.',
      'А если скорости равны, разность нулевая — отыгрывать нечем, и разрыв не изменится никогда.',
      'Итог: новой формулы нет. Это то же «вдогонку» и «навстречу», только расстояние между лодками — длина круга.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: догоняет.',
      'Тренажёр 2: сходятся.',
      'Тренажёр 3: что делить и на что.'
    ],
    check: {
      q: 'Круг 600 м. Лодки идут в одну сторону со скоростями 90 и 60 м/мин. Через сколько минут быстрая догонит медленную?',
      choices: ['Через 20 минут','Через 4 минуты','Через 10 минут'],
      ans: 0,
      exp: 'За минуту быстрая отыгрывает 90 − 60 = 30 м, а отыграть надо целый круг: 600 : 30 = 20 минут.'
    },
    tasks: [
      { q:'Круг 600 м, скорости 90 и 60 м/мин, обе лодки в одну сторону. Через сколько догонит?', kind:'choice',
        choices:['Через 20 минут','Через 4 минуты'], ans:0,
        hints:['Догонит, когда отыграет целый круг.','Отыгрывает по 90 − 60 = 30 м в минуту.'],
        sol:'600 : 30 = 20 минут.' },
      { q:'Тот же круг 600 м и те же скорости, но лодки гребут в разные стороны. Через сколько сойдутся?', kind:'choice',
        choices:['Через 4 минуты','Через 20 минут'], ans:0,
        hints:['Навстречу круг съедают вдвоём.','90 + 60 = 150 м в минуту.'],
        sol:'600 : 150 = 4 минуты.' },
      { q:'Скорости 90 и 60 м/мин, обе в одну сторону. Сколько метров быстрая отыгрывает за минуту?', kind:'choice',
        choices:['30 метров','150 метров'], ans:0,
        hints:['Это и есть разность скоростей.','90 − 60.'],
        sol:'90 − 60 = 30 метров.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L819.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1);
    else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);

    const ЗАГОЛОВКИ={
      1:'Штиль у острова', 2:'Крути время', 3:'Отыгрывает разность',
      4:'Ловушка', 5:'Проверка', 6:'В разные стороны', 7:'Вычитать или складывать',
      8:'Сколько встреч', 9:'Если скорости равны', 10:'Итог', 11:'Практика',
      12:'Тренажёр 1', 13:'Тренажёр 2', 14:'Тренажёр 3'
    };
    el.innerHTML = `<div class="s6 l819" data-frame="${f}">
        <h2>${ЗАГОЛОВКИ[f]||'Движение по кругу'}</h2>
        ${сцена}
      </div>`;
  }

  window.r819Мин  =(д)=>{ const s=S();
    s.минут=Math.max(0,Math.min(24,(s.минут==null?6:s.минут)+д)); chRender(0); };
  window.r819О1   =(к)=>{ const s=S(); s.о1=к; if(к==='ok') s.отв_догон=true;   chRender(0); };
  window.r819О2   =(к)=>{ S().о2=к; chRender(0); };
  window.r819О3   =(к)=>{ const s=S(); s.о3=к; if(к==='ok') s.отв_встреча=true; chRender(0); };
  window.r819О4   =(к)=>{ const s=S(); s.о4=к; if(к==='ok') s.отв_сколько=true; chRender(0); };
  window.r819О5   =(к)=>{ S().о5=к; chRender(0); };
  window.r819Сторона=(к)=>{ const s=S(); s.сторона=(s.сторона===к?null:к); chRender(0); };
  window.r819Разложить=()=>{ const s=S(); s.разложено=!s.разложено; chRender(0); };
  window.r819Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r819Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r819T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=(s[ключ+'Шаг']||0)%набор.задания.length;
    const з=набор.задания[шаг];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r819TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L819; else arr.push(L819);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA819={render:render, L:L819};
})();
