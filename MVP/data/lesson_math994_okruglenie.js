/* ============ МАТЕМАТИКА · УРОК 994 · «ОКРУГЛЕНИЕ НАТУРАЛЬНЫХ ЧИСЕЛ» ============
   5 класс. Сюжет: ярмарка — на ценниках пишут «примерно», а рядом точное число.

   СВЕРКА С ПЕТЕРСОНОМ (5 класс, «Учусь учиться»):
     • округление натуральных чисел, координатная прямая;
     • сравнение и запись чисел, разряды и классы;
     • округление до десятков, сотен, тысяч.
   Источники в репозитории: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   ДВЕНАДЦАТЬ КАДРОВ. У каждого своя композиция (не один и тот же каркас):
     1. ценник «примерно» — зачем округляют;
     2. что значит округлить — маркер едет к ближайшему круглому числу;
     3. кто ближе — сравнение расстояний дугами;
     4. качели — 44 ближе к 40, весы наклоняются;
     5. правило по цифре — решающая цифра пульсирует, стрелка вверх;
     6. до десятков — три случая;
     7. до сотен — хвост обнуляется по одной цифре;
     8. до тысяч — разрядная сетка;
     9. разбор по шагам — 4 680 до тысяч;
     10. где это нужно — прикидка;
     11. практика — пять уровней карточками;
     12. тренажёр — четыре уровня.

   АНИМАЦИЯ ОБЪЯСНЯЕТ СМЫСЛ, А НЕ УКРАШАЕТ (стандарт проекта, раздел «Движение»):
     • маркер числа едет по лучу к ближайшему круглому — 5 с, с паузами;
     • решающая цифра пульсирует прозрачностью (без масштабирования текста);
     • стрелка поднимается вверх, если разряд увеличивается;
     • цифры хвоста превращаются в нули по очереди;
     • качели наклоняются в сторону ближнего числа.
   Все движения вертикальные или вдоль луча, ни одно не выносит элемент за край.
   При prefers-reduced-motion SMIL-анимации НЕ выводятся вовсе: кадр сразу
   показывает конечное состояние (проверяется замером: 0 тегов animate).

   СТАНДАРТ: шкала кеглей 12/14/16/20/24/32, основной текст 20, подпись 16;
   цели касания от 48 px; кривые cubic-bezier(.23,1,.32,1); есть
   prefers-reduced-motion; проверка на 390/768/1200 px. */
(function(){
  'use strict';

  const ID = 994;

  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';

  /* движение включаем только если система не просит его отключить */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);

  const CSS=`
  #lvis .s6.l994{gap:14px}
  #lvis .s6.l994 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l994 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l994 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l994 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l994 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l994 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l994 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l994 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l994 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l994 .правило b{color:${GOLD}}
  #lvis .s6.l994 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l994 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l994 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l994 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l994 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l994 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l994 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l994 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l994 .уровни .точка.сейчас{background:${GOLD};animation:l994dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l994dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l994 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l994 [data-anim]{animation:l994rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l994rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l994 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l994 [data-anim]{animation:none!important}
    #lvis .s6.l994 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l994 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l994-style');
      if(!s){ s=document.createElement('style'); s.id='l994-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  /* анимация выводится только когда движение разрешено */
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';

  const пульс = (длит,нач) => ан('opacity','1;0.35;1',длит, нач!=null?'begin="'+нач+'s"':'');
  const кольцо = (x,y,r,цвет,длит,нач) =>
    `<circle cx="${x}" cy="${y}" r="${r}" fill="none" stroke="${цвет}" stroke-width="2">`+
      ан('opacity','0;0.9;0',длит,'keyTimes="0;0.5;1"'+(нач!=null?' begin="'+нач+'s"':''))+
      ан('r',r+';'+(r+9)+';'+r,длит,'keyTimes="0;0.5;1"'+(нач!=null?' begin="'+нач+'s"':''))+
    `</circle>`;
  const летит = (x0,y0,x1,y1,длит,нач) =>
    `<circle cx="${x0}" cy="${y0}" r="5" fill="${GOLD}">`+
      анТ('0 0;0 0;'+(x1-x0).toFixed(1)+' '+(y1-y0).toFixed(1)+';0 0',длит,
          'keyTimes="0;0.08;0.55;1"'+(нач!=null?' begin="'+нач+'s"':''))+
    `</circle>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c994-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22362c"/><stop offset="1" stop-color="#152219"/>
      </linearGradient>
      <linearGradient id="c994-луч" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#3f7a5f"/><stop offset="1" stop-color="#8fd1a8"/>
      </linearGradient>
      <linearGradient id="c994-ценник" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#fdf6e0"/><stop offset="1" stop-color="#ddcda6"/>
      </linearGradient>
      <filter id="c994-тень" x="-30%" y="-30%" width="160%" height="160%">
        <feGaussianBlur stdDeviation="2.2"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||190}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* числовой луч с числами от до с шагом 1 и маркером, который едет к ближайшему
     круглому числу; возвращает разметку */
  function лучСМаркером(опции){
    const { x0, x1, y, от, до, число, ближнее, подпись } = опции;
    const ш = x1-x0;
    const поз = ч => x0 + ((ч-от)/(до-от))*ш;
    let s = `<line x1="${x0}" y1="${y}" x2="${x1}" y2="${y}" stroke="url(#c994-луч)" stroke-width="3"/>
      <path d="M${x1} ${y} L${x1-10} ${y-5} L${x1-10} ${y+5} Z" fill="#8fd1a8"/>`;
    for(let ч=от; ч<=до; ч++){
      const круглая = ч%10===0;
      const x=поз(ч);
      s += `<line x1="${x.toFixed(1)}" y1="${y-7}" x2="${x.toFixed(1)}" y2="${y+7}" stroke="${ЛИНИЯ}" stroke-width="1.4"/>`;
      if(круглая || ч===число)
        s += т(x.toFixed(1), y+28, String(ч), круглая?16:14, круглая?GOLD:BLUE, true);
    }
    const xч=поз(число), xб=поз(ближнее), сдвиг=xб-xч;
    s += `<g>${анТ('0 0;0 0;'+сдвиг.toFixed(1)+' 0;'+сдвиг.toFixed(1)+' 0;0 0','5s','keyTimes="0;0.12;0.42;0.78;1"')}
        <circle cx="${xч.toFixed(1)}" cy="${y}" r="7" fill="${BLUE}" stroke="#0d2733" stroke-width="1.4"/>
        ${т(xч.toFixed(1),y-16,'наше число',12,BLUE,false)}
      </g>`;
    s += `<circle cx="${xб.toFixed(1)}" cy="${y}" r="10" fill="none" stroke="${GOLD}" stroke-width="2">
        ${ан('opacity','0.15;1;0.15','5s','keyTimes="0;0.45;0.9"')}</circle>`;
    if(подпись) s += т(168, y+52, подпись, 12, МУТ);
    return s;
  }

  /* цифровая сетка числа: цифры, решающая подсвечена и пульсирует */
  function разряды(x,y,цифры,решИндекс,цветРеш){
    const шаг=26;
    return цифры.map((ц,i)=>{
      const решает = i===решИндекс;
      const x0=x+i*шаг;
      return `<g>
        ${решает?`<rect x="${x0-3}" y="${y-22}" width="${шаг-2}" height="34" rx="6" fill="rgba(255,215,106,.16)"
            stroke="${цветРеш||GOLD}" stroke-width="1.8">${ан('opacity','1;0.35;1','1.4s')}</rect>`:''}
        ${т(x0+11, y+2, ц, 20, решает?(цветРеш||GOLD):ИНК, true)}
      </g>`;
    }).join('');
  }

  /* ярлык-ценник «примерно» */
  const ценник = (x,y,ш,текст,подпись,цвет) => `
    <g>
      <rect x="${x+3}" y="${y-19}" width="${ш}" height="38" rx="8" fill="rgba(0,0,0,.4)" filter="url(#c994-тень)"/>
      <rect x="${x}" y="${y-22}" width="${ш}" height="38" rx="8" fill="url(#c994-ценник)"
        stroke="${цвет||'#b8a06a'}" stroke-width="1.6"/>
      ${т(x+ш/2,y+4,текст,20,'#2a2118',true)}
      ${подпись?т(x+ш/2,y+34,подпись,12,МУТ):''}
    </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Ценник «примерно» */
  function F1(s){
    const в = s.зачем;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="186" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="186" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'На ценнике — примерно',14,GOLD,true)}
      <g>${пульс('7s',0)}${ценник(24,66,132,'38 ₽','точная цена')}</g>
      <g>${пульс('7s',3)}${ценник(180,66,132,'≈ 40 ₽','как пишут на ярлыке',GOLD)}</g>
      ${кольцо(246,66,80,GOLD,'7s',1.4)}
      ${т(168,132,'покупателю важно быстро понять порядок цены',12,ИНК)}
      ${т(168,158,'для этого точное число заменяют круглым',12,МУТ)}
    `,186);
    return ВОПРОС('Зачем точное число 38 заменяют на 40?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='быстрее'?'hit':(в?'miss':''), 'Так число короче и понятнее: видно порядок', "r994Why('быстрее')")}
        ${BTN(4, в==='обман'?'miss':'', 'Так продавец завышает цену', "r994Why('обман')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='быстрее',
        в==='быстрее' ? 'Верно: округление заменяет число близким круглым, чтобы быстро понять величину. Точное число при этом никуда не исчезает.'
                     : 'Нет: округление не про обман, а про удобство. «Примерно 40» значит «около сорока».') : ЖДЁТ()) +
      ПРАВИЛО('<b>Округлить</b> — заменить число близким круглым: 38 ≈ 40.');
  }

  /* 2. Что значит округлить */
  function F2(s){
    const в = s.чтоЗначит;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'38 едет по лучу к ближайшему десятку',14,GOLD,true)}
      ${лучСМаркером({x0:40,x1:300,y:80,от:30,до:40,число:38,ближнее:40,подпись:ДВИЖ?'маркер сам находит ближайшее круглое число':'38 ближе к 40'})}
    `,190);
    return ВОПРОС('Что происходит с числом при округлении до десятков?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='ближайшее'?'hit':(в?'miss':''), 'Число заменяют ближайшим круглым', "r994What('ближайшее')")}
        ${BTN(4, в==='любое'?'miss':'', 'Число заменяют любым круглым — какое удобнее', "r994What('любое')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='ближайшее',
        в==='ближайшее' ? 'Верно: 38 стоит почти у сорока — до него две единицы, а до тридцати восемь. Округляем к 40.'
                        : 'Нет: круглое число выбирают не по удобству, а по расстоянию на луче. 38 ближе к 40.') : ЖДЁТ()) +
      ПРАВИЛО('Округляя, выбираем <b>ближайшее</b> круглое число по числовому лучу.');
  }

  /* 3. Кто ближе: дуги расстояний */
  function F3(s){
    const в = s.ктоБлиже;
    const x30=60, x38=232, x40=288;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Сравниваем расстояния',14,GOLD,true)}
      <line x1="40" y1="104" x2="304" y2="104" stroke="url(#c994-луч)" stroke-width="3"/>
      ${т(x30,132,'30',16,GOLD,true)}
      ${т(x38,132,'38',16,BLUE,true)}
      ${т(x40,132,'40',16,GOLD,true)}
      <circle cx="${x38}" cy="104" r="7" fill="${BLUE}"/>
      <path d="M${x30} 74 Q${(x30+x38)/2} 44 ${x38} 74" fill="none" stroke="${МУТ}" stroke-width="2"/>
      ${т((x30+x38)/2,40,'8 единиц',12,МУТ)}
      <path d="M${x38} 74 Q${(x38+x40)/2} 44 ${x40} 74" fill="none" stroke="${GOLD}" stroke-width="2.4">
        ${ан('opacity','1;0.4;1','1.8s')}</path>
      ${т((x38+x40)/2,40,'2 единицы',12,GOLD,true)}
      ${т(168,168,'короче путь до 40 — туда и округляем',12,ИНК)}
    `,190);
    return ВОПРОС('До какого десятка 38 ближе?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сорок'?'hit':(в?'miss':''), 'До 40: расстояние 2 против 8', "r994Near('сорок')")}
        ${BTN(4, в==='тридцать'?'miss':'', 'До 30: оно тоже круглое', "r994Near('тридцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сорок',
        в==='сорок' ? 'Верно: от 38 до 40 всего две единицы, а до 30 — восемь. Ближайшее круглое — 40.'
                    : 'Нет: круглое число выбирают по расстоянию. До 30 целых восемь шагов, до 40 — только два.') : ЖДЁТ()) +
      ПРАВИЛО('Расстояние и есть ответ: <b>ближайшее</b> круглое число и будет округлением.');
  }

  /* 4. Качели: 44 ближе к 40 */
  function F4(s){
    const в = s.качели;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'44 — куда ближе?',14,GOLD,true)}
      <g>
        ${ДВИЖ?'<animateTransform attributeName="transform" type="rotate" values="0 168 120;-8 168 120;0 168 120;8 168 120;0 168 120" dur="6s" repeatCount="indefinite"/>':''}
        <rect x="48" y="116" width="240" height="8" rx="4" fill="#8a5f05"/>
      </g>
      <path d="M168 120 L168 148" stroke="#8a5f05" stroke-width="4"/>
      <path d="M140 152 L196 152 L168 176 Z" fill="#6f4420" stroke="#3a2412" stroke-width="1.4"/>
      ${т(78,104,'40',20,GOLD,true)}
      ${т(258,104,'50',20,GOLD,true)}
      ${т(78,78,'4 шага',12,МУТ)}
      ${т(258,78,'6 шагов',12,МУТ)}
      ${т(168,60,'чаша с 44 наклоняется к 40',14,BLUE,true)}
    `,190);
    return ВОПРОС('44 округляем до десятков. Какое число ближе?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сорок'?'hit':(в?'miss':''), '40: до него 4 единицы, до 50 — 6', "r994Scales('сорок')")}
        ${BTN(4, в==='пятьдесят'?'miss':'', '50: оно больше, значит ближе', "r994Scales('пятьдесят')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сорок',
        в==='сорок' ? 'Верно: качели наклоняются к 40. 44 − 40 = 4, а 50 − 44 = 6 — ближе сорок.'
                    : 'Нет: «больше» не значит «ближе». Считаем шаги: до 50 целых шесть, до 40 — четыре.') : ЖДЁТ()) +
      ПРАВИЛО('Считай шаги до соседних круглых чисел: <b>44 ≈ 40</b>.');
  }

  /* 5. Правило по цифре */
  function F5(s){
    const в = s.правило;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Смотрим на цифру после разряда',14,GOLD,true)}
      ${разряды(128,66,['4','7'],1)}
      ${т(168,104,'до десятков округляем — смотрим на единицы',12,МУТ)}
      <g>${анТ('0 0;0 0;0 -12;0 0;0 0','3.4s','keyTimes="0;0.2;0.5;0.8;1"')}
        <path d="M168 126 L168 148" stroke="${GOLD}" stroke-width="2.4"/>
        <path d="M162 142 L168 152 L174 142" fill="${GOLD}"/>
      </g>
      ${т(232,146,'5–9 → увеличиваем',12,GOLD,true)}
      ${т(96,146,'0–4 → оставляем',12,BLUE,true)}
      ${т(168,178,'цифра-решатель пульсирует',12,МУТ)}
    `,190);
    return ВОПРОС('Какая цифра решает, в какую сторону округлять?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='после'?'hit':(в?'miss':''), 'Первая цифра после нужного разряда', "r994Rule('после')")}
        ${BTN(4, в==='первая'?'miss':'', 'Самая первая цифра числа', "r994Rule('первая')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='после',
        в==='после' ? 'Верно: при округлении до десятков смотрим на единицы, до сотен — на десятки. Эта цифра и решает.'
                    : 'Нет: первая цифра числа при округлении до десятков не меняется. Решает цифра сразу после разряда.') : ЖДЁТ()) +
      ПРАВИЛО('Решает <b>первая цифра после</b> нужного разряда: 0–4 — не меняем, 5–9 — увеличиваем.');
  }

  /* 6. До десятков: три случая */
  const СЛУЧАИ10=[['44','40','0–4: не меняем'],['46','50','5–9: увеличиваем'],['45','50','ровно 5: увеличиваем']];
  function F6(s){
    const в = s.десятки;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Округляем до десятков',14,GOLD,true)}
      ${СЛУЧАИ10.map(([ч,о,п],i)=>{
        const y=54+i*44;
        return `<g>
          <rect x="22" y="${y-24}" width="292" height="38" rx="10" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
          ${т(88,y+2,ч,20,BLUE,true)}
          ${т(126,y+2,'≈',20,МУТ)}
          ${т(170,y+2,о,20,GOLD,true)}
          ${т(262,y+2,п,12,МУТ)}
        </g>`;
      }).join('')}
      <g>${анТ('0 0;0 0;0 44;0 44;0 88;0 88;0 0','8s','keyTimes="0;0.06;0.24;0.36;0.54;0.72;1"')}
        <rect x="18" y="28" width="300" height="42" rx="11" fill="none" stroke="${GOLD}" stroke-width="2.2"/>
      </g>
      ${т(168,184,'единицы решают, десятки отвечают',12,МУТ)}
    `,190);
    return ВОПРОС('Округляем 45 до десятков. Что получится?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пятьдесят'?'hit':(в?'miss':''), '50: цифра 5 — увеличиваем десятки', "r994Ten('пятьдесят')")}
        ${BTN(4, в==='сорок'?'miss':'', '40: пятёрка маленькая, оставляем', "r994Ten('сорок')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пятьдесят',
        в==='пятьдесят' ? 'Верно: по правилу цифра 5 в решающем разряде увеличивает: 45 ≈ 50. Так округляют и 46, и 47.'
                        : 'Нет: пятёрка относится к группе «5–9». Значит, десятки увеличиваем: 45 ≈ 50.') : ЖДЁТ()) +
      ПРАВИЛО('До десятков: смотрим единицы. <b>0–4</b> — десятки те же, <b>5–9</b> — десятки на один больше.');
  }

  /* 7. До сотен: хвост обнуляется */
  function F7(s){
    const в = s.сотни;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Округляем 372 до сотен',14,GOLD,true)}
      <g>
        ${т(94,74,'3',24,ИНК,true)}
        ${т(126,74,'7',24,GOLD,true)}<rect x="112" y="50" width="28" height="34" rx="6" fill="rgba(255,215,106,.16)"
          stroke="${GOLD}" stroke-width="1.8">${ан('opacity','1;0.35;1','1.4s')}</rect>
        ${т(158,74,'2',24,МУТ,true)}
      </g>
      ${т(200,74,'≈',20,МУТ)}
      ${т(248,74,'400',24,GOLD,true)}
      ${т(168,110,'цифра 7 в разряде десятков — значит, увеличиваем сотни',12,ИНК)}
      ${[0,1,2].map(i=>`<g opacity="${ДВИЖ?0.25:1}">
        ${ан('opacity','0.25;1','0.6s','begin="'+(0.4+i*0.5)+'s;fill=\'freeze\'"')}
        <rect x="${62+i*92}" y="132" width="80" height="34" rx="8" fill="rgba(143,209,168,.14)" stroke="${GREEN}" stroke-width="1.4"/>
        ${т(102+i*92,155,['сотни: 4','десятки: 0','единицы: 0'][i],12,GREEN,true)}
      </g>`).join('')}
    `,190);
    return ВОПРОС('372 округляем до сотен. Сколько сотен останется?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='четыре'?'hit':(в?'miss':''), 'Четыре: 372 ≈ 400', "r994Hund('четыре')")}
        ${BTN(4, в==='три'?'miss':'', 'Три: сотни не меняются', "r994Hund('три')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='четыре',
        в==='четыре' ? 'Верно: в разряде десятков стоит 7 — это «5–9», поэтому сотен становится четыре. Младшие разряды обнуляются: 400.'
                     : 'Нет: смотри на десятки. Там 7, значит сотни увеличиваем: 372 ≈ 400.') : ЖДЁТ()) +
      ПРАВИЛО('Округляя до сотен, смотрим десятки; <b>372 ≈ 400</b>. Разряды справа становятся нулями.');
  }

  /* 8. До тысяч */
  function F8(s){
    const в = s.тысячи;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="190" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Округляем до тысяч',14,GOLD,true)}
      ${[[4320,'4000','сотни 3 — оставить'],[4680,'5000','сотни 6 — вверх'],
         [4500,'5000','сотни 5 — вверх']].map(([ч,о,п],i)=>{
        const y=58+i*44;
        return `<g>
          <rect x="20" y="${y-24}" width="296" height="38" rx="10" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
          ${т(84,y+2,String(ч),20,BLUE,true)}
          ${т(122,y+2,'≈',20,МУТ)}
          ${т(168,y+2,о,20,GOLD,true)}
          ${т(258,y+2,п,12,МУТ)}
        </g>`;
      }).join('')}
      <g>${анТ('0 0;0 0;0 44;0 44;0 88;0 88;0 0','8s','keyTimes="0;0.06;0.24;0.36;0.54;0.72;1"')}
        <rect x="16" y="32" width="304" height="42" rx="11" fill="none" stroke="${GOLD}" stroke-width="2.2"/>
      </g>
      ${т(168,186,'тысячи решает разряд сотен',12,МУТ)}
    `,190);
    return ВОПРОС('Округляем 4 680 до тысяч. Что получится?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пять'?'hit':(в?'miss':''), '5 000: сотен шесть — увеличиваем', "r994Thou('пять')")}
        ${BTN(4, в==='четыре'?'miss':'', '4 000: оставляем тысячи', "r994Thou('четыре')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пять',
        в==='пять' ? 'Верно: при округлении до тысяч решает разряд сотен. Там 6, значит тысяч становится пять: 4 680 ≈ 5 000.'
                   : 'Нет: смотри на сотни. Там 6 — это «5–9», поэтому тысяч пять, а не четыре.') : ЖДЁТ()) +
      ПРАВИЛО('До тысяч решает <b>разряд сотен</b>: 4 680 ≈ 5 000, а 4 320 ≈ 4 000.');
  }

  /* 9. Разбор по шагам */
  const ШАГИ=[
    ['Находим разряд','округляем до тысяч — значит смотрим на цифру в разряде сотен'],
    ['Читаем цифру','в числе 4 680 сотен шесть: 4 6 8 0'],
    ['Решаем','6 входит в «5–9» — тысячи увеличиваем с четырёх до пяти'],
    ['Обнуляем хвост','сотни, десятки и единицы записываем нулями'],
    ['Записываем ответ','4 680 ≈ 5 000']
  ];
  function F9(s){
    const откр = s.шаги||0;
    const готово = откр>=ШАГИ.length;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Порядок действий',14,GOLD,true)}
      ${разряды(96,80,['4','6','8','0'],1)}
      ${т(168,124, откр>2 ? '≈ 5 000' : '≈ ?', 24, откр>2?GREEN:GOLD, true)}
      ${т(168,158,'разряд сотен решает, разряды справа обнуляются',12,МУТ)}
    `,176);
    const список = `<div class="ask" style="flex-direction:column">${ШАГИ.map(([к,ч],i)=>
      BTN(10+i, i<откр?'hit':'', (i+1)+'. '+к+(i<откр?' — '+ч:''), `r994Step(${i})`, i>откр)).join('')}</div>`;
    return ВОПРОС('Разбери округление 4 680 до тысяч по шагам. Какой шаг первый?') +
      `<div class="pic">${рис}</div>` + список +
      (готово ? РАЗБОР(true,'Порядок такой: разряд, цифра, решение, нули, запись. Проверка: 4 680 ближе к 5 000, чем к 4 000.')
              : ЖДЁТ()) +
      ПРАВИЛО('Пять шагов: <b>разряд → цифра → решение → нули → запись</b>.');
  }

  /* 10. Где это нужно */
  function F10(s){
    const в = s.прикидка;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="186" fill="url(#c994-стена)"/>
      <rect x="0" y="0" width="336" height="186" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,24,'Прикидка в магазине',14,GOLD,true)}
      ${ценник(16,66,96,'487 ₽',null)}
      ${ценник(120,66,96,'296 ₽',null)}
      ${летит(112,66,258,66,'6s',0)}
      ${летит(216,66,258,66,'6s',0.8)}
      ${т(230,74,'+',20,МУТ)}
      ${ценник(240,66,80,'≈ ?','сумма',GOLD)}
      <g>${пульс('6s',1.6)}${т(168,126,'487 ≈ 500 и 296 ≈ 300',16,ИНК,true)}</g>
      ${т(168,156,'500 + 300 = 800 — примерно столько и выйдет',12,GOLD)}
    `,186);
    return ВОПРОС('Сколько примерно стоят две покупки: 487 ₽ и 296 ₽?') +
      `<div class="pic">${рис}</div>` +
      `<div class="ask">
        ${BTN(3, в==='восемьсот'?'hit':(в?'miss':''), 'Около 800 ₽: 500 + 300', "r994Est('восемьсот')")}
        ${BTN(4, в==='тысяча'?'miss':'', 'Около 1 000 ₽: округляем вверх', "r994Est('тысяча')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='восемьсот',
        в==='восемьсот' ? 'Верно: 487 ≈ 500, 296 ≈ 300, значит вместе около 800 ₽. Точнее — 783 ₽, прикидка близка.'
                       : 'Нет: 487 ближе к 500, а 296 — к 300. Вместе около 800, а не 1000.') : ЖДЁТ()) +
      ПРАВИЛО('Округление даёт <b>прикидку</b>: 487 + 296 ≈ 500 + 300 = 800.');
  }

  /* 11. Практика: пять уровней */
  const УРОВНИ=[
    { вопрос:'Округляем 62 до десятков.', варианты:[
        {т:'60: единицы 2 — не меняем', ок:true,  почему:'Цифра 2 из группы «0–4», десятки остаются: 62 ≈ 60.'},
        {т:'70: десятки увеличиваем', ок:false, почему:'Увеличиваем только при цифрах 5–9, а здесь 2.'}]},
    { вопрос:'Округляем 87 до десятков.', варианты:[
        {т:'90: единицы 7 — увеличиваем', ок:true,  почему:'7 из группы «5–9», поэтому десятки становятся девятью: 87 ≈ 90.'},
        {т:'80: единицы отбрасываем', ок:false, почему:'Отбрасывать можно только 0–4. Семёрка требует увеличить.'}]},
    { вопрос:'Округляем 250 до сотен.', варианты:[
        {т:'300: десятки 5 — увеличиваем', ок:true,  почему:'По правилу цифра 5 увеличивает: 250 ≈ 300.'},
        {т:'200: сотни не меняем', ок:false, почему:'Пятёрка входит в «5–9», значит сотни увеличиваем.'}]},
    { вопрос:'Округляем 7 340 до тысяч.', варианты:[
        {т:'7 000: сотни 3 — не меняем', ок:true,  почему:'При округлении до тысяч решают сотни; здесь 3, значит оставляем 7 000.'},
        {т:'8 000: округляем вверх', ок:false, почему:'Вверх округляем при 5–9 в разряде сотен, а там 3.'}]},
    { вопрос:'Округляем 1 968 до сотен.', варианты:[
        {т:'2 000: десятки 6 — увеличиваем', ок:true,  почему:'Смотрим десятки: 6 из «5–9», сотни растут до 20 сотен — это 2 000.'},
        {т:'1 900: десятки обнуляем', ок:false, почему:'Обнулять можно при 0–4. Шестёрка требует увеличить.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что решало ответ?') +
        РАЗБОР(true,'Два шага: находим нужный разряд и смотрим цифру сразу после него. 0–4 — оставляем, 5–9 — увеличиваем, хвост обнуляем.') +
        `<div class="ask">${BTN(4,'','пройти заново','r994Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r994Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Сначала разряд, потом цифра после него: <b>0–4 оставляем, 5–9 увеличиваем</b>, хвост — нули.');
  }

  /* 12. Тренажёр */
  const ТРЕНАЖЁР=[
    { ф:'Округли 38 до десятков.', о:['40','30','38'], в:0, р:'38 ближе к 40: расстояние 2 против 8.' },
    { ф:'Округли 254 до сотен.', о:['300','200','250'], в:0, р:'десятки 5 — увеличиваем сотни: 254 ≈ 300.' },
    { ф:'Округли 3 120 до тысяч.', о:['3 000','4 000','3 100'], в:0, р:'сотни 1 — из группы «0–4», оставляем 3 000.' },
    { ф:'Сколько примерно: 691 + 208?', о:['около 900','около 800','около 1 000'], в:0, р:'691 ≈ 700 и 208 ≈ 200, вместе около 900.' }
  ];
  function F12(s){
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length, з=ТРЕНАЖЁР[и], отв=s.тОтвет, готово=отв!=null, верно=отв===з.в;
    return ТОЧКИ(ТРЕНАЖЁР.length, и, и) +
      A(2,'cap','Уровень '+(и+1)+' из '+ТРЕНАЖЁР.length) +
      ВОПРОС(з.ф) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(3+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r994Train(${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${s.тВерно||0} · ошибок: ${s.тОшибки||0} · всего: ${ТРЕНАЖЁР.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий уровень','r994NextQ()')}</div>` : '');
  }

  const L994={
    id: ID, title:'Округление натуральных чисел', ico:'🎯',
    src:'Математика · 5 класс · Натуральные числа и действия', subj:'math',
    explain: [
      'На ярмарке на ценнике стоит «≈ 40 ₽», а точная цена — 38 ₽. Зачем заменили число? Чтобы покупатель сразу понял порядок цены: сорок, а не триста. Такая замена точного числа близким круглым и называется округлением. Округление не портит число и не обманывает: точное значение остаётся, а рядом появляется удобное приближённое. На этом уроке разберём, как выбирать, к какому круглому числу округлять, и как делать это по цифре, не пересчитывая весь луч.',

      'Округлить — значит заменить число ближайшим круглым. Круглые числа — те, что оканчиваются нулями: 10, 20, 30, 100, 1 000. Чтобы понять, какое из двух соседних круглых ближе, число ставят на числовой луч. Сорок восемь стоит почти у пятидесяти: до него две единицы, а до сорока — восемь. Значит, 48 ≈ 50. Частая ошибка — округлять «на глаз», не сравнивая расстояния. Как проверить себя: вычти число из правого круглого и из него — левое круглое, меньшее расстояние и будет ответом.',

      'Расстояние удобно видеть на луче: от 38 до 40 две единицы, от 38 до 30 — восемь. Короткая дуга и показывает, куда округлять. Тот же приём работает и для больших чисел: 372 стоит между 300 и 400; до 400 — двадцать восемь, до 300 — семьдесят два. Значит, 372 ≈ 400. Частая ошибка — считать, что всегда округляют вниз. Как проверить себя: посмотри, к какому круглому числу число ближе на прямом луче.',

      'Есть второй способ, короче: смотреть на одну цифру. При округлении до десятков смотрим на разряд единиц, до сотен — на разряд десятков, до тысяч — на разряд сотен. Короче говоря, смотрим на цифру сразу после того разряда, до которого округляем. Если эта цифра 0, 1, 2, 3 или 4 — разряд не меняем. Если 5, 6, 7, 8 или 9 — разряд увеличиваем на один. Цифры правее всегда становятся нулями. Частая ошибка — смотреть не на соседнюю цифру, а на самую большую или на первую. Как проверить себя: назови разряд, до которого округляешь, и его правого соседа.',

      'Разберём случаи до десятков. 44: единицы — 4, значит десятки не меняем, получается 40. 46: единицы — 6, десятки увеличиваем, получается 50. 45: единицы — 5, по правилу увеличиваем, получается 50. Обрати внимание: 44 и 46 стоят по разные стороны от середины между 40 и 50, а 45 — ровно посередине, и правило отправляет его вверх. Частая ошибка — оставлять 45 как 40, потому что «пятёрка не больше пяти». Как проверить себя: проверь по лучу — 45 одинаково далеко от 40 и 50, и договорились округлять вверх.',

      'Теперь до сотен. 372: разряд сотен — 3, а сразу после него стоит 7. Семёрка в группе «5–9», значит сотен становится четыре: 372 ≈ 400. 320: после сотен стоит 2, значит сотни не меняем: 320 ≈ 300. 350: после сотен стоит 5, увеличиваем: 350 ≈ 400. Цифры десятков и единиц в ответе записываем нулями. Частая ошибка — забыть обнулить хвост и написать «≈ 47» вместо 470 или «≈ 4» вместо 400. Как проверить себя: в ответе столько нулей, сколько разрядов ты округлил.',

      'До тысяч работает то же правило, только решает разряд сотен. 4 320: сотен три, значит тысяч остаётся четыре: 4 320 ≈ 4 000. 4 680: сотен шесть — тысяч становится пять: 4 680 ≈ 5 000. 4 500: сотен пять — по правилу увеличиваем: 4 500 ≈ 5 000. Частая ошибка — смотреть на десятки вместо сотен. Как проверить себя: посчитай, сколько сотен в числе, и посмотри именно на эту цифру.',

      'Соберём порядок действий в пять шагов. Первый: найти разряд, до которого округляем. Второй: прочитать цифру сразу после него. Третий: решить — оставить разряд или увеличить. Четвёртый: записать все разряды правее нулями. Пятый: записать ответ со знаком «≈». Для 4 680: разряд — тысячи, цифра после него — 6, решение — увеличить, хвост — три нуля, ответ — 5 000. Частая ошибка — перескакивать шаги и путать, какой разряд увеличивать. Как проверить себя: проговори все пять шагов вслух.',

      'Округление нужно не само по себе, а для прикидки — быстрой оценки. В магазине две покупки: 487 ₽ и 296 ₽. Округлим до сотен: 487 ≈ 500 и 296 ≈ 300. Значит, вместе около 800 ₽, а точнее — 783 ₽. Прикидка отвечает на вопрос «хватит ли денег», не требуя точного счёта. Частая ошибка — округлять только одно слагаемое. Как проверить себя: округли каждое число, потом считай.',

      'И ещё об одном: округлять можно до любого разряда — до десятков, сотен, тысяч, десятков тысяч. Правило всегда одно и то же, меняется только разряд-решатель. Округляя до десятков тысяч, смотрим на тысячи; до миллионов — на сотни тысяч. Частая ошибка — думать, что для больших чисел правило другое. Как проверить себя: назови разряд, до которого округляешь, и скажи, какая цифра решает.',

      'Теперь пройди практику сам: пять уровней — до десятков, сотен и тысяч. На каждом уровне сначала читай вопрос, потом выбирай ответ и смотри разбор: он объясняет, какая цифра решила. Частая ошибка — спешить и не проверять соседнюю цифру. Как проверить себя: перед ответом скажи вслух разряд и цифру после него.',

      'Соберём правило целиком. Округление — замена числа ближайшим круглым. Ближайшее определяют по лучу: считают расстояния до соседних круглых чисел. Быстрее — по цифре: смотрят на разряд сразу после нужного, 0–4 оставляют, 5–9 увеличивают, а все разряды правее записывают нулями. Округление помогает делать прикидку. Дальше — тренажёр со счётом.'
    ],
    check: {
      q:'Округли 87 до десятков.',
      choices:['90','80','87'], ans:0,
      exp:'Единицы — 7, это группа «5–9», значит десятки увеличиваем: 87 ≈ 90.'
    },
    tasks: [
      { q:'Округли 38 до десятков. Запиши ответ числом.', kind:'unit', ans:40, tol:0,
        hints:['Найди соседние круглые числа: 30 и 40.','38 ближе к 40.'],
        sol:'38 ≈ 40.' },
      { q:'Округли 254 до сотен.', kind:'choice',
        choices:['300','200','250'], ans:0,
        hints:['Смотри разряд десятков.','Десятков пять — увеличиваем.'],
        sol:'254 ≈ 300.' },
      { q:'Округли 3 120 до тысяч.', kind:'choice',
        choices:['3 000','4 000','3 100'], ans:0,
        hints:['До тысяч решает разряд сотен.','Сотен одна — группа «0–4».'],
        sol:'3 120 ≈ 3 000.' },
      { q:'Сколько примерно: 691 + 208? Запиши ответ числом.', kind:'unit', ans:900, tol:0,
        hints:['Округли каждое слагаемое.','691 ≈ 700, 208 ≈ 200.'],
        sol:'Около 900.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L994.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else сцена=F12(s);

    const ЗАГОЛОВКИ={
      1:['Ярмарка','Зачем пишут «примерно»'],
      2:['Ближайшее','Маркер находит круглое'],
      3:['Расстояния','Сравниваем шаги'],
      4:['Качели','44 между 40 и 50'],
      5:['Правило','Цифра после разряда'],
      6:['До десятков','Три случая'],
      7:['До сотен','Хвост — нули'],
      8:['До тысяч','Решает разряд сотен'],
      9:['Порядок','Пять шагов'],
      10:['Прикидка','Считаем примерно'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр','Проверь себя']
    };
    const з=ЗАГОЛОВКИ[f]||['Округление','Натуральные числа'];
    el.innerHTML = `<div class="s6 l994" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r994Why=(к)=>{ S().зачем=к; chRender(0); };
  window.r994What=(к)=>{ S().чтоЗначит=к; chRender(0); };
  window.r994Near=(к)=>{ S().ктоБлиже=к; chRender(0); };
  window.r994Scales=(к)=>{ S().качели=к; chRender(0); };
  window.r994Rule=(к)=>{ S().правило=к; chRender(0); };
  window.r994Ten=(к)=>{ S().десятки=к; chRender(0); };
  window.r994Hund=(к)=>{ S().сотни=к; chRender(0); };
  window.r994Thou=(к)=>{ S().тысячи=к; chRender(0); };
  window.r994Step=(и)=>{ const s=S(); if(и===(s.шаги||0)) s.шаги=(s.шаги||0)+1; chRender(0); };
  window.r994Est=(к)=>{ S().прикидка=к; chRender(0); };
  window.r994Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r994Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r994Train=(к)=>{
    const s=S();
    if(s.тОтвет!=null) return;
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length;
    s.тОтвет=к;
    if(к===ТРЕНАЖЁР[и].в) s.тВерно=(s.тВерно||0)+1; else s.тОшибки=(s.тОшибки||0)+1;
    chRender(0);
  };
  window.r994NextQ=()=>{ const s=S(); s.тНомер=((s.тНомер||0)+1)%ТРЕНАЖЁР.length; s.тОтвет=null; chRender(0); };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L994; else arr.push(L994);
  })();
  window.MA994={render:render, L:L994};
})();
