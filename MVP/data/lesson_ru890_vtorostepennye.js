/* ============ РУССКИЙ ЯЗЫК · УРОК 890 · «ВТОРОСТЕПЕННЫЕ ЧЛЕНЫ» ============
   5 класс, «Синтаксис». Продолжение линии 877–891.

   ФОРМАТ — КАК У РУССКИХ УРОКОВ: window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT: .kicker, h2, .lead, .cap, .steps/.st,
   .ex-line, .cases, .rail, .pair/.card/.tag/.row/.split .word.wide, .verdict, .score.

   ИНТЕРАКТИВ В КАДРАХ (состояние в CHS, перерисовка через chRender):
     кадр 1  «показать основу» — предложение подсвечивает подлежащее и сказуемое;
     кадр 2  «убрать основу» — видно, что осталось: второстепенные члены;
     кадр 6  выбор слова — показывается вопрос и вид второстепенного члена;
     кадр 8  переключатель похожих фраз: «играл в футбол» / «играл во дворе»;
     кадр 12 разбор по клику: подлежащее, сказуемое, дополнение, определение,
             обстоятельство.
   Плюс тренажёр из десяти заданий.

   ПРАВИЛА (школьный курс, 5 класс):
     второстепенные члены поясняют главные и не входят в грамматическую основу;
     ДОПОЛНЕНИЕ — вопросы косвенных падежей (кого? чего? кому? чему? кем? чем?
       о ком? о чём?): читает книгу (что?), пишет другу (кому?), гордится братом;
     ОПРЕДЕЛЕНИЕ — какой? чей? который?: осенний лист (какой?), мой друг (чей?),
       третий день (который?);
     ОБСТОЯТЕЛЬСТВО — где? когда? куда? откуда? почему? зачем? как?:
       упал на дорожку (куда?), гуляли вечером (когда?), бежал быстро (как?),
       промок из-за дождя (почему?);
     в тетради: дополнение — пунктир, определение — волнистая линия,
       обстоятельство — точка с тире;
     ЛОВУШКА: «играл в футбол» — дополнение (во что?), а «играл во дворе» —
       обстоятельство (где?); решает вопрос, а не предлог. */
(function(){
  'use strict';

  const ID = 890;
  const R = window.RUKIT;
  if(!R){ return; }
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const row   = (...xs)=>`<div class="row">${xs.join('')}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;
  const пр    = (t)=>`<p class="ex-line">${esc(t)}</p>`;
  const рельс = (стиль,...xs)=>`<div class="rail" style="${стиль||''}">${xs.join('')}</div>`;
  const двa    = (a,b)=>`<div class="cases">${a}${b}</div>`;
  const шаги  = (items)=>`<div class="steps">${items.map(([t,s],i)=>
    `<div class="st"><span class="n">${i+1}</span><span class="t">${esc(t)}<small>${esc(s)}</small></span></div>`).join('')}</div>`;
  const кноп  = (t,on)=>`<button type="button" class="chip" style="flex:1 1 40%;font-size:17px" onclick="${on}">${esc(t)}</button>`;
  const кнопки= (...xs)=>`<div class="row">${xs.join('')}</div>`;

  /* ── данные интерактивных кадров ── */
  const СЛОВА=[
    ['книгу','что? — дополнение'],
    ['другу','кому? — дополнение'],
    ['осенний','какой? — определение'],
    ['мой','чей? — определение'],
    ['на дорожку','куда? — обстоятельство'],
    ['вечером','когда? — обстоятельство'],
    ['быстро','как? — обстоятельство'],
    ['из-за дождя','почему? — обстоятельство']
  ];
  const ПОХОЖИЕ=[
    ['Мальчик играл в футбол.','во что? — дополнение','Вопрос падежа: играл во что? Значит, это дополнение.'],
    ['Мальчик играл во дворе.','где? — обстоятельство','Вопрос места: играл где? Значит, это обстоятельство.'],
    ['Мальчик играл весело.','как? — обстоятельство','Вопрос образа действия: играл как?'],
    ['Мальчик играл с другом.','с кем? — дополнение','Падежный вопрос: играл с кем?']
  ];
  const РАЗБОР=[
    ['Осенний лист упал на дорожку.','лист — подлежащее, упал — сказуемое','осенний — определение (какой?), на дорожку — обстоятельство (куда?)'],
    ['Мальчик читает интересную книгу.','мальчик — подлежащее, читает — сказуемое','интересную — определение (какую?), книгу — дополнение (что?)'],
    ['Вечером дети гуляли в парке.','дети — подлежащее, гуляли — сказуемое','вечером — обстоятельство (когда?), в парке — обстоятельство (где?)'],
    ['Я пишу письмо другу.','я — подлежащее, пишу — сказуемое','письмо — дополнение (что?), другу — дополнение (кому?)']
  ];

  /* ── тренажёр ── */
  const GAME=[
    ['«читает книгу» — какой второстепенный член?','дополнение','Вопрос что? — падежный, значит дополнение.','определение'],
    ['«осенний лист» — какой второстепенный член?','определение','Вопрос какой? — определение.','обстоятельство'],
    ['«упал на дорожку» — какой второстепенный член?','обстоятельство','Вопрос куда? — обстоятельство.','дополнение'],
    ['«гуляли вечером» — какой второстепенный член?','обстоятельство','Вопрос когда? — обстоятельство.','определение'],
    ['«мой друг» — какой второстепенный член?','определение','Вопрос чей? — определение.','дополнение'],
    ['«пишет другу» — какой второстепенный член?','дополнение','Вопрос кому? — дополнение.','обстоятельство'],
    ['«бежал быстро» — какой второстепенный член?','обстоятельство','Вопрос как? — обстоятельство.','определение'],
    ['«играл в футбол» — какой второстепенный член?','дополнение','Вопрос во что? — дополнение, хотя есть предлог.','обстоятельство'],
    ['Входят ли второстепенные члены в грамматическую основу?','нет','Основу образуют только подлежащее и сказуемое.','да'],
    ['Чем подчёркивают дополнение?','пунктиром','Дополнение — пунктир, определение — волнистая, обстоятельство — точка с тире.','волнистой линией']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    const кн = варианты.map(о=>кноп(о,`ru890Game('${esc(о)}')`)).join('');
    const ответ = готово ? (верно ? window.RUFEED.note('ok','верно',it[2]) : window.RUFEED.note('no','исправить',it[2]))
                         : `<p class="verdict">Сначала задай вопрос от главного слова.</p>`;
    return `${kick('16 · Тренажёр')}
      ${word(it[0],'выбери ответ')}
      ${ответ}
      ${кнопки(кн)}
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее задание.':'Сначала вопрос, потом название.')}`;
  }

  window.RULESSON({id:ID, game:GAME, frames:[
    (st)=>{
      const открыто = st.о1!=null;
      const отв = открыто
        ? `<p class="verdict ok">Основа: лист упал. Остальные слова — второстепенные члены.</p>`
        : `<p class="verdict">Нажми кнопку — я покажу грамматическую основу.</p>`;
      const кн = кноп('показать основу','ru890Основа()');
      return `${kick('01 · Разминка')}
        ${h2('От сложного предложения — к членам предложения')}
        ${lead('На прошлом уроке мы считали грамматические основы. Сегодня разберём, из чего состоит предложение внутри части: кроме основы в нём есть второстепенные члены.')}
        ${пр('Осенний лист упал на дорожку.')}
        ${отв}
        ${кнопки(кн)}
        ${pair(card('главные члены','подлежащее и сказуемое'),card('второстепенные','все остальные слова'))}
        ${cap('Второстепенные члены поясняют главные — без них предложение беднее.')}`;
    },

    (st)=>{
      const убрано = st.о2!=null;
      const текст = убрано ? 'Лист упал.' : 'Осенний лист упал на дорожку.';
      const отв = убрано
        ? `<p class="verdict ok">Осталась основа: лист упал. Всё, что убрали, — второстепенные члены.</p>`
        : `<p class="verdict">Нажми — и увидишь, что станется с предложением без второстепенных членов.</p>`;
      return `${kick('02 · Интерактив: убери второстепенные')}
        ${h2('Что останется от предложения')}
        ${lead('Если убрать второстепенные члены, останется грамматическая основа. Смысл сохранится, но станет суше.')}
        ${пр(текст)}
        ${отв}
        ${кнопки(кноп(убрано?'вернуть слова':'убрать второстепенные','ru890Убрать()'))}
        ${двa(card('было','Осенний лист упал на дорожку.'),card('стало','Лист упал.'))}
        ${cap('Второстепенные члены делают мысль точной: какой лист, куда упал.')}`;
    },

    ()=>`${kick('03 · Дополнение')}
      ${h2('Отвечает на вопросы падежей')}
      ${lead('Дополнение — второстепенный член, который отвечает на вопросы косвенных падежей: кого? чего? кому? чему? кем? чем? о ком? о чём?')}
      ${cards([['читает книгу','что? — винительный падеж'],['пишет другу','кому? — дательный падеж'],['гордится братом','кем? — творительный падеж']])}
      ${пр('Мальчик читает книгу. → читает (что?) книгу')}
      ${cap('В тетради дополнение подчёркивают пунктиром: _ _ _ _')}`,

    ()=>`${kick('04 · Определение')}
      ${h2('Отвечает на вопросы какой? чей? который?')}
      ${lead('Определение называет признак предмета. Чаще всего это прилагательное, которое поясняет существительное.')}
      ${cards([['осенний лист','какой?'],['мой друг','чей?'],['третий день','который?']])}
      ${пр('Осенний лист упал. → лист (какой?) осенний')}
      ${cap('В тетради определение подчёркивают волнистой линией: ~~~')}`,

    ()=>`${kick('05 · Обстоятельство')}
      ${h2('Отвечает на вопросы где? когда? как? куда? почему? зачем?')}
      ${lead('Обстоятельство называет место, время, способ или причину действия. Это наречие или существительное с предлогом.')}
      ${cards([['упал на дорожку','куда?'],['гуляли вечером','когда?'],['бежал быстро','как?'],['промок из-за дождя','почему?']])}
      ${пр('Лист упал на дорожку. → упал (куда?) на дорожку')}
      ${cap('В тетради обстоятельство подчёркивают точкой с тире: _._._.')}`,

    (st)=>{
      const к = (st.о6||0);
      const сл = СЛОВА[к];
      const кн = СЛОВА.slice(0,4).map((х,i)=>кноп(х[0],`ru890Слово(${i})`)).join('');
      const кн2 = СЛОВА.slice(4).map((х,i)=>кноп(х[0],`ru890Слово(${i+4})`)).join('');
      return `${kick('06 · Интерактив: определи по вопросу')}
        ${h2('Нажми на слово — увидишь вопрос и вид')}
        ${lead('Второстепенный член узнают по вопросу от главного слова.')}
        ${кнопки(кн)}
        ${кнопки(кн2)}
        ${пр('Выбрано: ' + сл[0])}
        <p class="verdict ok">${esc(сл[1])}</p>
        ${cap('Падежный вопрос — дополнение; какой? чей? — определение; где? когда? как? — обстоятельство.')}`;
    },

    ()=>`${kick('07 · Три вида рядом')}
      ${h2('Дополнение, определение, обстоятельство')}
      ${рельс('flex-wrap:wrap',chip('дополнение: книгу'),chip('определение: осенний'),chip('обстоятельство: вечером'))}
      ${шаги([['дополнение','вопросы падежей: что? кому? кем?'],['определение','вопросы какой? чей? который?'],['обстоятельство','вопросы где? когда? как? куда? почему? зачем?']])}
      ${cap('Сначала вопрос — потом название: так не ошибаются.')}`,

    (st)=>{
      const к = (st.о8||0);
      const п = ПОХОЖИЕ[к];
      const кн = ПОХОЖИЕ.map((х,i)=>кноп('фраза '+(i+1),`ru890Сравни(${i})`)).join('');
      return `${kick('08 · Интерактив: похожие фразы')}
        ${h2('Предлог ничего не решает — решает вопрос')}
        ${lead('Сравним фразы, которые выглядят одинаково: «в футбол» и «во дворе». Обе с предлогом, но вопросы разные.')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">${esc(п[1])}</p>
        ${cap(п[2])}`;
    },

    ()=>`${kick('09 · Как различить')}
      ${h2('Порядок действий')}
      ${шаги([['1 найди основу','подлежащее и сказуемое'],['2 возьми слово','то, которое надо разобрать'],['3 задай вопрос','от слова, к которому оно относится'],['4 назови вид','падеж — дополнение, какой — определение, где — обстоятельство']])}
      ${пр('Мальчик читает интересную книгу. → интересную (какую?) — определение, книгу (что?) — дополнение')}
      ${cap('Четыре шага — и член предложения назван верно.')}`,

    ()=>`${kick('10 · Как подчёркивать')}
      ${h2('Условные знаки в тетради')}
      ${cards([['подлежащее','одна черта: —'],['сказуемое','две черты: ='],['определение','волнистая: ~~~'],['дополнение','пунктир: _ _ _'],['обстоятельство','точка с тире: _._._']])}
      ${пр('Осенний лист упал на дорожку. → ~~~ — = _._._')}
      ${cap('Знак помогает увидеть разбор: по нему сразу понятно, какой это член.')}`,

    ()=>`${kick('11 · Разбор по шагам')}
      ${h2('Синтаксический разбор предложения')}
      ${шаги([['1 найди основу','подчеркни подлежащее и сказуемое'],['2 найди второстепенные','задай вопросы от главных слов'],['3 назови виды','дополнение, определение, обстоятельство'],['4 проверь','каждое слово получило свою роль']])}
      ${пр('Я пишу письмо другу. → я — , пишу = , письмо _ _ _ , другу _ _ _')}
      ${cap('Разбор — это не угадывание, а вопросы по порядку.')}`,

    (st)=>{
      const к = (st.о12||0);
      const п = РАЗБОР[к];
      const кн = РАЗБОР.map((х,i)=>кноп('разбор '+(i+1),`ru890Разбор(${i})`)).join('');
      return `${kick('12 · Интерактив: разбор по клику')}
        ${h2('Нажми на разбор — увидишь члены предложения')}
        ${кнопки(кн)}
        ${пр(п[0])}
        ${cards([['главные члены',п[1]],['второстепенные члены',п[2]]])}
        ${cap('Сравни разборы: в каждом есть и главные, и второстепенные члены.')}`;
    },

    ()=>`${kick('13 · Ловушка')}
      ${h2('Предлог не делает член предложения')}
      ${lead('Многие думают: если есть предлог, это обстоятельство. Это не так: «играл в футбол» — дополнение, потому что вопрос падежа (во что?).')}
      ${двa(пр('играл в футбол → во что? — дополнение'),пр('играл во дворе → где? — обстоятельство'))}
      ${cards([['смотри на вопрос','падежный — дополнение'],['а не на предлог','предлог бывает у обоих']])}
      ${cap('Один и тот же предлог «в» встречается и у дополнения, и у обстоятельства.')}`,

    ()=>`${kick('14 · Сводка')}
      ${h2('Три второстепенных члена')}
      ${шаги([['дополнение','вопросы падежей; читает книгу, пишет другу'],['определение','какой? чей? который?; осенний лист, мой друг'],['обстоятельство','где? когда? как? куда? почему?; вечером, быстро, на дорожку']])}
      ${pair(card('главное','второстепенные члены поясняют главные'),card('проверка','задай вопрос от главного слова'))}
      ${cap('В основу входят только подлежащее и сказуемое.')}`,

    ()=>`${kick('15 · В карман')}
      ${h2('Три фразы, которые стоит унести')}
      ${cards([['падеж — дополнение','читает книгу, пишет другу'],['какой — определение','осенний лист, мой друг'],['где и когда — обстоятельство','упал на дорожку, гуляли вечером']])}
      ${пр('Осенний лист упал на дорожку. → определение, подлежащее, сказуемое, обстоятельство')}
      ${cap('Один вопрос — один член предложения: так и разбираем.')}`,

    (st)=>тренер(st)
  ]});

  /* ── карточка каталога ── */
  const L890={
    id:ID, title:'Второстепенные члены', ico:'🧩',
    src:'Русский язык · 5 класс · Синтаксис', subj:'rus', group:null,
    explain:[
      'Кроме грамматической основы в предложении есть второстепенные члены. Они поясняют главные члены и в основу не входят: «Осенний лист упал на дорожку» — основа «лист упал», остальное второстепенное.',
      'Если убрать второстепенные члены, останется основа: «Лист упал». Смысл сохранится, но пропадут подробности — какой лист и куда упал.',
      'Дополнение отвечает на вопросы косвенных падежей: кого? чего? кому? чему? кем? чем? о ком? о чём? Примеры: читает книгу (что?), пишет другу (кому?), гордится братом (кем?).',
      'В тетради дополнение подчёркивают пунктиром: _ _ _ _.',
      'Определение отвечает на вопросы какой? чей? который? и называет признак предмета: осенний лист (какой?), мой друг (чей?), третий день (который?).',
      'В тетради определение подчёркивают волнистой линией: ~~~.',
      'Обстоятельство отвечает на вопросы где? когда? куда? откуда? почему? зачем? как? Примеры: упал на дорожку (куда?), гуляли вечером (когда?), бежал быстро (как?), промок из-за дождя (почему?).',
      'В тетради обстоятельство подчёркивают точкой с тире: _._._.',
      'Порядок разбора: найти основу, взять слово, задать вопрос от того слова, к которому оно относится, и назвать вид второстепенного члена.',
      'Ловушка: предлог не определяет вид члена предложения. «Играл в футбол» — дополнение (во что?), а «играл во дворе» — обстоятельство (где?).',
      'В основу входят только подлежащее и сказуемое, поэтому второстепенные члены никогда не подчёркиваются как основа.',
      'В уроке есть интерактив: показать основу, убрать второстепенные члены, определить слово по вопросу, сравнить похожие фразы и разобрать предложение по клику.',
      'Три опоры: падежный вопрос — дополнение; какой? чей? — определение; где? когда? как? — обстоятельство.',
      'Синтаксический разбор идёт по шагам: основа, потом второстепенные члены, потом их названия и проверка.',
      'Знаки в тетради помогают запомнить: пунктир, волнистая линия, точка с тире.',
      'Тренажёр: десять заданий по кругу. Определи вид второстепенного члена или ответь на вопрос о разборе.'
    ],
    tasks:[
      { q:'«читает книгу» — какой второстепенный член?', kind:'choice',
        choices:['дополнение','определение'], ans:0,
        hints:['Какой вопрос?','Читает (что?) книгу.'],
        sol:'Дополнение.' },
      { q:'«осенний лист» — какой второстепенный член?', kind:'choice',
        choices:['определение','обстоятельство'], ans:0,
        hints:['Какой вопрос?','Лист (какой?) осенний.'],
        sol:'Определение.' },
      { q:'«упал на дорожку» — какой второстепенный член?', kind:'choice',
        choices:['обстоятельство','дополнение'], ans:0,
        hints:['Какой вопрос?','Упал (куда?) на дорожку.'],
        sol:'Обстоятельство.' },
      { q:'Входят ли второстепенные члены в грамматическую основу?', kind:'choice',
        choices:['нет','да'], ans:0,
        hints:['Что образует основу?','Подлежащее и сказуемое.'],
        sol:'Нет.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){
      const м=arr.findIndex(L=>L && L.id===ID);
      if(м>=0) arr[м]=L890; else arr.push(L890);
    }
  }catch(e){}

  /* ── состояние и обработчики ── */
  const С = ()=>{
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const перерисовать = ()=>{ if(typeof chRender==='function') chRender(0); };

  window.ru890Основа=function(){ const s=С(); s.о1=1; перерисовать(); };
  window.ru890Убрать=function(){ const s=С(); s.о2=(s.о2==null?1:null); перерисовать(); };
  window.ru890Слово=function(i){ const s=С(); s.о6=i; перерисовать(); };
  window.ru890Сравни=function(i){ const s=С(); s.о8=i; перерисовать(); };
  window.ru890Разбор=function(i){ const s=С(); s.о12=i; перерисовать(); };

  window.ru890Game=function(ответ){
    const st=С();
    if(st.gRes!=null){
      st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null;
    }else{
      const it=GAME[(st.gIdx||0)%GAME.length];
      st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1;
    }
    перерисовать();
  };
})();
