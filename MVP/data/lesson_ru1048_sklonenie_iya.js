/* ============ РУССКИЙ ЯЗЫК · УРОК 1048 · «СКЛОНЕНИЕ НА -ИЯ, -ИЙ, -ИЕ» ============
   5 класс, «Морфология. Имя существительное». Продолжение линии 877–1047.

   ФОРМАТ — КАК У РУССКИХ УРОКОВ: window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT (.kicker, h2, .lead, .cap, .steps/.st,
   .ex-line, .cases, .rail, .pair/.card/.tag/.row/.split, .verdict, .score).

   ИНТЕРАКТИВ: кадр 1 «показать окончание»; кадр 3 выбор слова на -ия; кадр 6
   склоняем слово по падежам; кадр 9 ловушка «-ее или -ии»; кадр 13 разбор по
   клику. Плюс тренажёр.

   ПРОВЕРЕННЫЕ СВЕДЕНИЯ (5 класс):
     существительные на -ИЯ (лекция, армия, Мария, станция) в дательном и
       предложном падежах имеют окончание -ИИ: лекции, о лекции; Марии, о Марии;
     существительные на -ИЙ (санаторий, лекторий, гений) в предложном падеже
       имеют окончание -ИИ: о санатории, о гении;
     существительные на -ИЕ (здание, сочинение, внимание) в предложном падеже
       имеют окончание -ИИ: о здании, о сочинении;
     у обычных существительных 1 и 2 склонения в этих падежах пишется -Е:
       о стране, о коне, в окне;
     отсюда правило: видишь -ия, -ий, -ие — в дательном и предложном пиши -ИИ,
       а не -Е. */
(function(){
  'use strict';

  const ID = 1048;
  const R = window.RUKIT;
  if(!R){ return; }
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;
  const пр    = (t)=>`<p class="ex-line">${esc(t)}</p>`;
  const рельс = (стиль,...xs)=>`<div class="rail" style="${стиль||''}">${xs.join('')}</div>`;
  const двa    = (a,b)=>`<div class="cases">${a}${b}</div>`;
  const шаги  = (items)=>`<div class="steps">${items.map(([t,s],i)=>
    `<div class="st"><span class="n">${i+1}</span><span class="t">${esc(t)}<small>${esc(s)}</small></span></div>`).join('')}</div>`;
  const кноп  = (t,on)=>`<button type="button" class="chip" style="flex:1 1 40%;font-size:17px" onclick="${on}">${esc(t)}</button>`;
  const кнопки= (...xs)=>`<div class="row">${xs.join('')}</div>`;

  const СЛОВА=[
    ['лекция','на -ия','И. лекция · Р. лекции · Д. лекции · В. лекцию · Т. лекцией · П. о лекции'],
    ['Мария','на -ия','И. Мария · Р. Марии · Д. Марии · В. Марию · Т. Марией · П. о Марии'],
    ['армия','на -ия','И. армия · Р. армии · Д. армии · В. армию · Т. армией · П. об армии'],
    ['санаторий','на -ий','И. санаторий · Р. санатория · Д. санаторию · В. санаторий · Т. санаторием · П. о санатории'],
    ['гений','на -ий','И. гений · Р. гения · Д. гению · В. гения · Т. гением · П. о гении'],
    ['здание','на -ие','И. здание · Р. здания · Д. зданию · В. здание · Т. зданием · П. о здании'],
    ['сочинение','на -ие','И. сочинение · Р. сочинения · Д. сочинению · В. сочинение · Т. сочинением · П. о сочинении']
  ];
  const ПАДЕЖИ=[['лекция · -ия','о лекции,-ИИ'],['санаторий · -ий','о санатории,-ИИ'],['здание · -ие','о здании,-ИИ'],['страна · обычное','о стране,-Е'],['конь · обычное','о коне,-Е']];
  const ЛОВУШКИ=[
    ['о лекции','верно','Слово на -ия: в предложном падеже -ИИ.'],
    ['о лекцие','ошибка','Писать -Е нельзя: слово на -ия.'],
    ['о санатории','верно','Слово на -ий: в предложном падеже -ИИ.'],
    ['о санаторие','ошибка','Это ошибка: нужно -ИИ.'],
    ['о здании','верно','Слово на -ие: в предложном падеже -ИИ.'],
    ['о здание','ошибка','В предложном падеже будет «о здании».']
  ];
  const РАЗБОР=[
    ['Мы были в лекции? — как правильно?','в лекции','Предложный падеж, слово на -ия → -ИИ.'],
    ['Подошёл к Марие или к Марии?','к Марии','Дательный падеж, слово на -ия → -ИИ.'],
    ['Отдыхали в санаторие или в санатории?','в санатории','Предложный падеж, слово на -ий → -ИИ.'],
    ['Думаю о сочинении или о сочиненье?','о сочинении','Предложный падеж, слово на -ие → -ИИ.'],
    ['Говорили о стране или о страни?','о стране','Обычное 1 склонение → -Е.'],
    ['Вспоминали о гение или о гении?','о гении','Слово на -ий → -ИИ.']
  ];

  const GAME=[
    ['«о лекц_и» — какая буква?','и','Слово на -ия: Д. и П. падежи — ИИ.','е'],
    ['«о стран_» — какая буква?','е','Обычное 1 склонение — Е.','и'],
    ['«о здани_» — какая буква?','и','Слово на -ие — ИИ.','е'],
    ['«к Мари_» — какая буква?','и','Слово на -ия, дательный падеж — ИИ.','е'],
    ['«о санатори_» — какая буква?','и','Слово на -ий — ИИ.','е'],
    ['«в окн_» — какая буква?','е','Обычное 2 склонение — Е.','и'],
    ['«об арми_» — какая буква?','и','Слово на -ия — ИИ.','е'],
    ['«о гени_» — какая буква?','и','Слово на -ий — ИИ.','е'],
    ['«о сочинени_» — какая буква?','и','Слово на -ие — ИИ.','е'],
    ['Какие слова требуют -ИИ?','на -ия, -ий, -ие','Отсюда и правило.','на -а, -я']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    const кн = варианты.map(о=>кноп(о,`ru1048Game('${esc(о)}')`)).join('');
    const ответ = готово ? (верно ? R.NOTE('ok','верно',it[2]) : R.NOTE('no','исправить',it[2]))
                         : `<p class="verdict">Сначала посмотри, на что оканчивается слово.</p>`;
    return `${kick('16 · Тренажёр')}
      ${word(it[0],'выбери букву')}
      ${ответ}
      ${кнопки(кн)}
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее слово.':'Сначала окончание слова, потом буква.')}`;
  }

  const подсказка_1 = cap('Смотри на конец слова: -ия, -ий или -ие — значит пишем -ИИ.');

  window.RULESSON({id:ID, game:GAME, frames:[
    (st)=>{
      const ок = st.о1!=null;
      const отв = ок ? `<p class="verdict ok">«О лекции» — окончание -ИИ, потому что слово оканчивается на -ия.</p>`
                     : `<p class="verdict">Нажми кнопку — покажу, какое окончание здесь нужно.</p>`;
      return `${kick('01 · Разминка')}
        ${h2('Особые окончания у слов на -ия, -ий, -ие')}
        ${lead('Мы разбирали собственные и нарицательные существительные. Теперь посмотрим на окончания: у слов на -ия, -ий, -ие они особенные.')}
        ${двa(пр('о странЕ'),пр('о лекциИ'))}
        ${отв}
        ${кнопки(кноп('показать окончание','ru1048Окончание()'))}
        ${pair(card('обычное слово','о стране, о коне — Е'),card('слово на -ия/-ий/-ие','о лекции, о здании — ИИ'))}
        ${подсказка_1}`;
    },

    ()=>`${kick('02 · Склонения')}
      ${h2('Три склонения и особый случай')}
      ${lead('У обычных существительных в дательном и предложном падежах пишется -Е. А слова на -ия, -ий, -ие — особый случай: у них -ИИ.')}
      ${cards([['1 склонение','страна, земля: о стране'],['2 склонение','конь, окно: о коне, в окне'],['3 склонение','ночь, степь: о ночи'],['особый случай','лекция, санаторий, здание: о лекции, о санатории, о здании']])}
      ${пр('Обрати внимание: слово «ночь» — 3 склонение, там тоже И, но по другому правилу.')}
      ${cap('Особенность в том, что слова на -ия, -ий, -ие не подчиняются общему правилу -Е.')}`,

    (st)=>{
      const к = (st.о3||0);
      const п = СЛОВА[к];
      const кн = СЛОВА.map((х,i)=>кноп(х[0],`ru1048Слово(${i})`)).join('');
      return `${kick('03 · Интерактив: склоняем слово')}
        ${h2('Нажми слово — увидишь все падежи')}
        ${кнопки(кн)}
        ${word(п[0], п[1])}
        <p class="verdict ok">${esc(п[2])}</p>
        ${cards([['дательный','-ИИ'],['предложный','-ИИ'],['остальные падежи','по общим правилам']])}
        ${cap('Ищи в списке дательный и предложный падежи — там и увидишь -ИИ.')}`;
    },

    ()=>`${kick('04 · Слова на -ия')}
      ${h2('Дательный и предложный — ИИ')}
      ${lead('Существительные на -ия (женский род: лекция, армия, Мария; и мужской — например, судья) в дательном и предложном падежах имеют окончание -ИИ.')}
      ${cards([['лекция','к лекции, о лекции'],['армия','к армии, об армии'],['Мария','к Марии, о Марии'],['станция','к станции, о станции']])}
      ${пр('Сравни: страна — к стране, о стране; лекция — к лекции, о лекции.')}
      ${cap('Разница только в окончании: -Е у обычных, -ИИ у слов на -ия.')}`,

    ()=>`${kick('05 · Слова на -ий и -ие')}
      ${h2('Предложный падеж — ИИ')}
      ${lead('Существительные на -ий (санаторий, лекторий, гений) и на -ие (здание, сочинение, внимание) в предложном падеже имеют окончание -ИИ.')}
      ${cards([['санаторий','о санатории'],['лекторий','в лектории'],['гений','о гении'],['здание','о здании'],['сочинение','о сочинении'],['внимание','о внимании']])}
      ${пр('Сравни: конь — о коне, окно — в окне; санаторий — о санатории, здание — о здании.')}
      ${cap('Проверь себя: если слово оканчивается на -ий или -ие, в предложном падеже будет -ИИ.')}`,

    (st)=>{
      const к = (st.о6||0);
      const п = ПАДЕЖИ[к];
      const кн = ПАДЕЖИ.map((х,i)=>кноп(х[0],`ru1048Падеж(${i})`)).join('');
      return `${kick('06 · Интерактив: сравни окончания')}
        ${h2('Нажми слово — увидишь падеж и окончание')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">${esc(п[1])}</p>
        ${двa(card('-ия, -ий, -ие','ИИ'),card('обычные слова','Е'))}
        ${cap('Одна буква отличает особый случай от общего правила.')}`;
    },

    ()=>`${kick('07 · Как рассуждать')}
      ${h2('Порядок действий')}
      ${шаги([['1 определи падеж','дательный или предложный'],['2 посмотри на конец слова','-ия, -ий или -ие'],['3 выбери окончание','особый случай — ИИ; обычное слово — Е'],['4 проверь','подставь вопрос: к лекции? о лекции?']])}
      ${пр('Думаю о сочинении → предложный падеж, слово на -ие → ИИ.')}
      ${cap('Четыре шага — и окончание выбрано верно.')}`,

    ()=>`${kick('08 · Почему так')}
      ${h2('История окончания')}
      ${lead('В древнерусском языке у слов на -ия, -ий, -ие основы оканчивались на мягкий согласный, и окончание -И сохранялось. Отсюда и современное -ИИ.')}
      ${двa(пр('по общему правилу: к странЕ, о странЕ'),пр('особый случай: к лекциИ, о лекциИ'))}
      ${cards([['общее правило','-Е'],['особые слова','-ИИ'],['как запомнить','видишь -ия, -ий, -ие — пиши И']])}
      ${cap('Понимание истории помогает не путать окончания.')}`,

    (st)=>{
      const к = (st.о9||0);
      const п = ЛОВУШКИ[к];
      const кн = ЛОВУШКИ.map((х,i)=>кноп(х[0],`ru1048Ловушка(${i})`)).join('');
      return `${kick('09 · Интерактив: верно или ошибка')}
        ${h2('Нажми вариант — увидишь, есть ли ошибка')}
        ${кнопки(кн)}
        <p class="verdict ok">«${esc(п[0])}» — ${esc(п[1])}. ${esc(п[2])}</p>
        ${cards([['-ия, -ий, -ие','ИИ'],['обычные','Е'],['частая ошибка','писать -Е у особых слов']])}
        ${cap('Сравни похожие варианты: ошибка всего в одной букве.')}`;
    },

    ()=>`${kick('10 · Сравни пары')}
      ${h2('Особое и обычное рядом')}
      ${cards([['о лекции — о стране','ИИ / Е'],['к Марии — к сестре','ИИ / Е'],['о санатории — о коне','ИИ / Е'],['о здании — в окне','ИИ / Е']])}
      ${пр('Видишь пару — сразу понятно, почему буквы разные.')}
      ${cap('Слова на -ия, -ий, -ие всегда с И в этих падежах.')}`,

    ()=>`${kick('11 · Где это встречается')}
      ${h2('Имена, наука, школа')}
      ${lead('Такие слова встречаются постоянно: в названиях уроков, в именах, в текстах учебника.')}
      ${рельс('flex-wrap:wrap',chip('на уроке математики'),chip('на лекции'),chip('об истории'),chip('о Марии'),chip('в сочинении'),chip('в здании школы'))}
      ${пр('Правило касается и имён: Мария — о Марии, Юлия — о Юлии, Наталия — о Наталии.')}
      ${cap('Ошибка в этих словах очень заметна, поэтому правило стоит выучить.')}`,

    ()=>`${kick('12 · Как проверить себя')}
      ${h2('Два вопроса')}
      ${шаги([['1 какой падеж?','дательный или предложный'],['2 какой конец у слова?','-ия, -ий или -ие'],['если да — ИИ','лекции, Марии, санатории, здании'],['если нет — Е','стране, коне, окне']])}
      ${пр('Проверка: «думаю о сочинени…» — предложный падеж, слово на -ие → о сочинении.')}
      ${cap('Два вопроса — и сомнение снято.')}`,

    (st)=>{
      const к = (st.о13||0);
      const п = РАЗБОР[к];
      const кн = РАЗБОР.map((х,i)=>кноп('разбор '+(i+1),`ru1048Разбор(${i})`)).join('');
      return `${kick('13 · Интерактив: разбор по клику')}
        ${h2('Нажми вопрос — увидишь правильный ответ')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">Правильно: ${esc(п[1])}. ${esc(п[2])}</p>
        ${cards([['особые слова','ИИ'],['обычные слова','Е']])}
        ${cap('Сравни разборы: всё решает окончание слова и падеж.')}`;
    },

    ()=>`${kick('14 · Ловушка')}
      ${h2('Три частые ошибки')}
      ${lead('Первая: пишут -Е у слов на -ия, -ий, -ие. Вторая: путают с 3 склонением. Третья: не проверяют падеж.')}
      ${двa(пр('«о лекцие» ✗ — правильно «о лекции»'),пр('«в санаторие» ✗ — правильно «в санатории»'))}
      ${cards([['-ия, -ий, -ие','ИИ в Д. и П. падежах'],['обычные слова','Е: о стране, о коне'],['3 склонение','ночь — о ночи: тоже И, но по другому правилу']])}
      ${cap('Проверь два вопроса — и ошибки не будет.')}`,

    ()=>`${kick('15 · В карман')}
      ${h2('Три фразы, которые стоит унести')}
      ${cards([['слова на -ия','в Д. и П. — ИИ: о лекции, к Марии'],['слова на -ий и -ие','в П. — ИИ: о санатории, о здании'],['обычные слова','Е: о стране, о коне, в окне']])}
      ${рельс('flex-wrap:wrap',chip('о лекции',GOLD),chip('о стране',BLUE),chip('о здании',GOLD),chip('в окне',BLUE))}
      ${cap('Видишь -ия, -ий, -ие — пиши И.')}`,

    (st)=>тренер(st)
  ]});

  const L1048={
    id:ID, title:'Склонение на -ия, -ий, -ие', ico:'✏️',
    src:'Русский язык · 5 класс · Морфология', subj:'rus', group:null,
    explain:[
      'У обычных существительных в дательном и предложном падежах пишется окончание -Е: к стране, о стране, о коне, в окне.',
      'Слова на -ия, -ий, -ие — особый случай: у них в этих падежах пишется -ИИ, а не -Е.',
      'Существительные на -ия (лекция, армия, станция, Мария) в дательном и предложном падежах имеют окончание -ИИ: к лекции, о лекции, к Марии, о Марии.',
      'Существительные на -ий (санаторий, лекторий, гений) в предложном падеже имеют окончание -ИИ: о санатории, в лектории, о гении.',
      'Существительные на -ие (здание, сочинение, внимание) в предложном падеже имеют окончание -ИИ: о здании, о сочинении, о внимании.',
      'Сравниваем: страна — к стране, о стране; лекция — к лекции, о лекции. Разница только в окончании.',
      'Сравниваем: конь — о коне, окно — в окне; санаторий — о санатории, здание — о здании.',
      'Порядок рассуждения: определить падеж (дательный или предложный), посмотреть на конец слова (-ия, -ий, -ие), выбрать окончание, проверить вопросом.',
      'Правило появилось исторически: у этих слов основа оканчивалась на мягкий согласный, и окончание -И сохранялось.',
      'Правило касается и имён: Мария — о Марии, Юлия — о Юлии, Наталия — о Наталии.',
      'Частая ошибка — писать -Е у особых слов: «о лекцие», «в санаторие», «о здание».',
      'Не путай с третьим склонением: у слов «ночь», «степь» в предложном падеже тоже И (о ночи), но это другое правило.',
      'Проверка состоит из двух вопросов: какой падеж? на что оканчивается слово? Если -ия, -ий, -ие — пишем -ИИ.',
      'В уроке есть интерактив: показать окончание, просклонять семь слов, сравнить окончания, найти ошибку среди похожих вариантов и разобрать вопрос по клику.',
      'Держи в голове пары: о лекции — о стране; о здании — в окне. Пара сразу показывает разницу.',
      'Тренажёр: десять слов по кругу. Вставь букву И или Е и проверь себя по объяснению.'
    ],
    tasks:[
      { q:'«о лекц_и» — какая буква?', kind:'choice',
        choices:['и','е'], ans:0,
        hints:['На что оканчивается слово?','-ия.'],
        sol:'И: о лекции.' },
      { q:'«о стран_» — какая буква?', kind:'choice',
        choices:['е','и'], ans:0,
        hints:['Это обычное слово 1 склонения.','Общее правило.'],
        sol:'Е: о стране.' },
      { q:'«о здани_» — какая буква?', kind:'choice',
        choices:['и','е'], ans:0,
        hints:['На что оканчивается слово?','-ие.'],
        sol:'И: о здании.' },
      { q:'«к Мари_» — какая буква?', kind:'choice',
        choices:['и','е'], ans:0,
        hints:['Какой падеж?','Дательный.'],
        sol:'И: к Марии.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L1048; else arr.push(L1048); }
  }catch(e){}

  const С = ()=>{
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const перерисовать = ()=>{ if(typeof chRender==='function') chRender(0); };

  window.ru1048Окончание=function(){ const s=С(); s.о1=1; перерисовать(); };
  window.ru1048Слово=function(i){ const s=С(); s.о3=i; перерисовать(); };
  window.ru1048Падеж=function(i){ const s=С(); s.о6=i; перерисовать(); };
  window.ru1048Ловушка=function(i){ const s=С(); s.о9=i; перерисовать(); };
  window.ru1048Разбор=function(i){ const s=С(); s.о13=i; перерисовать(); };
  window.ru1048Game=function(ответ){
    const st=С();
    if(st.gRes!=null){ st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null; }
    else{ const it=GAME[(st.gIdx||0)%GAME.length]; st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1; }
    перерисовать();
  };
})();
