/* ============ МАТЕМАТИКА · УРОК 845 · «ПРИЗМА И ПИРАМИДА: ОБЪЁМ И РАЗВЁРТКИ» ====
   6 класс. Сюжет: мастерская — сколько картона нужно на коробку?

   СВЕРКА С ПЕТЕРСОНОМ (6 класс): призма и пирамида, их элементы (грани, рёбра,
   вершины), развёртки, площадь поверхности, объём; формула V = S · h и
   V = 1/3 · S · h; формула Эйлера В − Р + Г = 2.
   Источники: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   ВСЕ ЧИСЛА И СООТНОШЕНИЯ ПРОВЕРЕНЫ РАСЧЁТОМ:
     коробка 5 × 4 × 3: поверхность 2·20 + 2·15 + 2·12 = 94; объём 5·4·3 = 60
     треугольная призма (основание 3-4-5, высота 10): площадь основания 6,
       боковая (3+4+5)·10 = 120, полная 132, объём 6·10 = 60
     пирамида с основанием 5 × 4 и высотой 3: V = 1/3 · 20 · 3 = 20;
       три такие пирамиды дают 60 — ровно объём призмы
     формула Эйлера: призма 6−9+5 = 2; четырёхугольная призма 8−12+6 = 2;
       четырёхугольная пирамида 5−8+5 = 2; треугольная пирамида 4−6+4 = 2;
       куб 8−12+6 = 2
     куб 4 × 4 × 4: поверхность 6 · 16 = 96, объём 64
     развёртки: призма — два основания и прямоугольный пояс боковых граней;
       пирамида — основание и треугольники; куб — шесть квадратов

   КРАСОТА И ИНТЕРЕС (просьба владельца — «ещё красивее и интереснее»):
     • КАРТОН: тёплый крафтовый материал с градиентом, зерном, светлой кромкой
       среза и штриховыми линиями сгиба;
     • АКСОНОМЕТРИЯ ПО ФОРМУЛАМ (deploy/РИСОВАНИЕ_ФИГУР.md): передняя грань —
       основной тон, верх — светлее, бок — темнее, невидимые рёбра штриховые;
       каждая фигура строится из своих размеров;
     • МАСТЕРСКАЯ: стол, ножницы, линейка, клей — предметы с двумя тенями;
     • СКЛАДЫВАНИЕ РАЗВЁРТКИ: боковые грани из плоского положения поднимаются
       в объём (анимация пути d), у пирамиды треугольники встают на основание;
     • СЛОИ ОБЪЁМА: призма собирается из слоёв S · 1, ПИРАМИДА наполняется
       тремя такими же пирамидами — видно, откуда берётся множитель 1/3;
     • ЭЙЛЕР: числа вершин, рёбер и граней подписаны прямо на фигуре.

   АНИМАЦИЯ: SMIL calcMode="spline", keySplines="0.23 1 0.32 1".
   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, keyTimes строго в [0;1], reduced-motion выключает
   SMIL полностью, ничего не выходит за край кадра. */
(function(){
  'use strict';

  const ID = 845;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#2a2016';

  const CSS=`
  #lvis .s6.l845{gap:14px}
  #lvis .s6.l845 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l845 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l845 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l845 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l845 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l845 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l845 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l845 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l845 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l845 .правило b{color:${GOLD}}
  #lvis .s6.l845 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l845 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l845 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l845 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l845 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l845 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l845 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l845 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l845 .уровни .точка.сейчас{background:${GOLD};animation:l845dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l845dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l845 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l845{-webkit-text-size-adjust:100%}
  #lvis .s6.l845 [data-anim]{animation:l845rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l845rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l845 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l845 [data-anim]{animation:none!important}
    #lvis .s6.l845 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l845 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l845-style');
      if(!s){ s=document.createElement('style'); s.id='l845-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анД = (путь,длит,доп) =>
    ДВИЖ ? `<animateMotion dur="${длит}" repeatCount="indefinite" path="${путь}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');
  /* путь перетекает из плоского положения в объёмное */
  const склад = (из,в,длит,доля,конец) =>
    анК('d',из+';'+из+';'+в+';'+в,длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const растётВ = (длит,доля,конец,H) => анК('height','1;1;'+H+';'+H,длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c845-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2a3f33"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#111c15"/>
      </linearGradient>
      <!-- КАРТОН: тёплый крафтовый материал -->
      <linearGradient id="c845-перед" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#e8c79a"/><stop offset="0.45" stop-color="#d3a86f"/>
        <stop offset="1" stop-color="#a97c46"/>
      </linearGradient>
      <linearGradient id="c845-верх" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffe3bb"/><stop offset="0.5" stop-color="#f0c894"/>
        <stop offset="1" stop-color="#c99f68"/>
      </linearGradient>
      <linearGradient id="c845-бок2" x1="0" y1="0" x2="0.1" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#b98d5a"/><stop offset="0.5" stop-color="#96683c"/>
        <stop offset="1" stop-color="#6f4f28"/>
      </linearGradient>
      <linearGradient id="c845-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <linearGradient id="c845-металл" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#5d6a76"/><stop offset="0.18" stop-color="#e8eef4"/>
        <stop offset="0.5" stop-color="#9fb0bd"/><stop offset="0.8" stop-color="#57636f"/>
        <stop offset="1" stop-color="#434d57"/>
      </linearGradient>
      <linearGradient id="c845-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".6"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".22"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".2"/>
      </linearGradient>
      <pattern id="c845-гофр" width="8" height="8" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
        <rect width="8" height="8" fill="none"/>
        <line x1="0" y1="0" x2="0" y2="8" stroke="rgba(255,255,255,.10)" stroke-width="1.4"/>
      </pattern>
      <filter id="c845-тень" x="-70%" y="-70%" width="240%" height="240%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3.4" result="р"/>
        <feOffset in="р" dx="4" dy="6" result="с"/>
        <feFlood flood-color="#0a1a24" flood-opacity="0.42" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c845-контакт" x="-70%" y="-150%" width="240%" height="400%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.3" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#1d1508" flood-opacity="0.42" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c845-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c845-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.8" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c845-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c845-шум)" opacity="0.05"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.84)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  const картон = (d,материал,нач) => `<g>
    <path d="${d}" transform="translate(3,5)" fill="#08171f" opacity=".34" filter="url(#c845-тень)"/>
    <path d="${d}" fill="url(#c845-${материал||'перед'})" stroke="${ОБВОД}" stroke-width="1.8">
      ${нач!=null?проявить('14s',нач,нач+0.05):''}</path>
    <path d="${d}" fill="url(#c845-гофр)" opacity=".55"/>
    <path d="${d}" fill="none" stroke="${ОБВОД}" stroke-width="1.8"/>
  </g>`;

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#08171f" opacity=".3" filter="url(#c845-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c845-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('14s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  const бумага = (x,y,ш,в,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="6" fill="#08171f" opacity=".36" filter="url(#c845-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="url(#c845-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('14s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="none" filter="url(#c845-шум)" opacity=".08"/>
  </g>`;

  /* КОРОБКА в аксонометрии по формулам стандарта:
     передняя грань (x,y)-(x+ш,y)... верх светлее, бок темнее */
  const коробка = (x,y,ш,в,гл,нач,подписи) => {
    const верх=y-в;
    return `<g>
      <ellipse cx="${x+ш/2+6}" cy="${y+гл+9}" rx="${ш*0.72}" ry="9" fill="#08171f" opacity=".38" filter="url(#c845-тень)"/>
      <path d="M${x+ш} ${y} L${x+ш+гл} ${y-гл} L${x+ш+гл} ${верх-гл} L${x+ш} ${верх} Z"
        fill="url(#c845-бок2)" stroke="${ОБВОД}" stroke-width="1.8">${нач!=null?проявить('14s',нач+0.06,нач+0.12):''}</path>
      <path d="M${x} ${верх} L${x+ш} ${верх} L${x+ш+гл} ${верх-гл} L${x+гл} ${верх-гл} Z"
        fill="url(#c845-верх)" stroke="${ОБВОД}" stroke-width="1.8">${нач!=null?проявить('14s',нач+0.12,нач+0.18):''}</path>
      <path d="M${x} ${y} L${x+ш} ${y} L${x+ш} ${верх} L${x} ${верх} Z"
        fill="url(#c845-перед)" stroke="${ОБВОД}" stroke-width="1.8">${нач!=null?проявить('14s',нач,нач+0.06):''}</path>
      <path d="M${x} ${y} L${x+ш} ${y} L${x+ш} ${верх} L${x} ${верх} Z" fill="url(#c845-гофр)" opacity=".5"/>
      ${подписи?`${т(x+ш/2,y+22,подписи[0],12,GOLD,true)}
        ${т(x-10,y-в/2+4,подписи[1],12,МУТ,true)}
        ${т(x+ш+гл+14,y-в-гл/2,подписи[2],12,МУТ,true)}`:''}
    </g>`;
  };

  /* ПИРАМИДА: основание 5×4 в аксонометрии и вершина */
  const пирамида = (x,y,ш,в,гл,высота,нач) => {
    const верх=y-в;
    const цx=x+ш/2+гл/2, вершина=[цx,y-высота];
    return `<g>
      <ellipse cx="${цx+6}" cy="${y+гл+10}" rx="${ш*0.7}" ry="9" fill="#08171f" opacity=".38" filter="url(#c845-тень)"/>
      <path d="M${x} ${y} L${x+ш} ${y} L${x+ш+гл} ${y-гл} L${x+гл} ${y-гл} Z"
        fill="url(#c845-верх)" stroke="${ОБВОД}" stroke-width="1.8"/>
      <path d="M${x+гл} ${y-гл} L${x+ш+гл} ${y-гл} L${вершина[0]} ${вершина[1]} Z"
        fill="url(#c845-перед)" stroke="${ОБВОД}" stroke-width="1.8">${нач!=null?проявить('14s',нач,нач+0.06):''}</path>
      <path d="M${x+ш} ${y} L${x+ш+гл} ${y-гл} L${вершина[0]} ${вершина[1]} Z"
        fill="url(#c845-бок2)" stroke="${ОБВОД}" stroke-width="1.8">${нач!=null?проявить('14s',нач+0.06,нач+0.12):''}</path>
      <path d="M${x} ${y} L${x+ш} ${y} L${вершина[0]} ${вершина[1]} Z"
        fill="url(#c845-бок2)" opacity=".55" stroke="${ОБВОД}" stroke-width="1.4">${нач!=null?проявить('14s',нач+0.12,нач+0.18):''}</path>
      <path d="M${x+гл} ${y-гл} L${вершина[0]} ${вершина[1]}" stroke="${ОБВОД}" stroke-width="1" stroke-dasharray="4 3"/>
      <line x1="${вершина[0]}" y1="${вершина[1]}" x2="${вершина[0]}" y2="${y-гл/2}" stroke="${GOLD}"
        stroke-width="1.4" stroke-dasharray="5 4">${проявить('14s',0.3,0.4)}</line>
    </g>`;
  };

  /* РАЗВЁРТКА ПРИЗМЫ: пояс прямоугольников и два основания */
  function развёрткаПризмы(x,y,ш,в,h,нач){
    return `<g>
      <rect x="${x}" y="${y+8}" width="${ш}" height="${h}" fill="url(#c845-перед)" stroke="${ОБВОД}" stroke-width="1.6">
        ${проявить('14s',нач,нач+0.05)}</rect>
      <rect x="${x}" y="${y+8+h}" width="${в}" height="${h}" fill="url(#c845-бок2)" stroke="${ОБВОД}" stroke-width="1.6">
        ${проявить('14s',нач+0.06,нач+0.11)}</rect>
      <rect x="${x+ш}" y="${y}" width="${в}" height="${в}" fill="url(#c845-верх)" stroke="${ОБВОД}" stroke-width="1.6">
        ${проявить('14s',нач+0.12,нач+0.17)}</rect>
      <rect x="${x}" y="${y}" width="${в}" height="${в}" fill="url(#c845-верх)" stroke="${ОБВОД}" stroke-width="1.6">
        ${проявить('14s',нач+0.18,нач+0.23)}</rect>
      ${[1,2,3].map(i=>`<line x1="${x+i*ш/4}" y1="${y+8}" x2="${x+i*ш/4}" y2="${y+8+h}"
        stroke="rgba(60,45,25,.45)" stroke-width="1" stroke-dasharray="3 3"/>`).join('')}
    </g>`;
  }

  /* ================= КАДРЫ ================= */

  /* 1. Мастерская: коробка и пирамидка */
  function F1(s){
    const в = s.мастерская;
    return ВОПРОС('Коробка 5×4×3 и пирамидка. Сколько нужно картона на коробку?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Мастерская: картон и клей',14,GOLD,true)}
        ${коробка(44,168,96,64,26,0.05,['5','4','3'])}
        ${пирамида(210,168,84,56,22,78,0.24)}
        ${/* линейка и ножницы */''}
        <g>${проявить('14s',0.44,0.52)}
          <rect x="20" y="204" width="120" height="14" rx="3" fill="url(#c845-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1"/>
          ${[0,1,2,3,4].map(i=>`<line x1="${28+i*24}" y1="204" x2="${28+i*24}" y2="211" stroke="rgba(80,60,30,.6)" stroke-width="1"/>`).join('')}
          ${подпись(80,238,'линейка',МУТ,12)}</g>
        <g>${проявить('14s',0.62,0.7)}
          <path d="M212 210 L246 210 L238 224 L212 224 Z" fill="url(#c845-металл)" stroke="${ОБВОД}" stroke-width="1.4"/>
          <circle cx="246" cy="213" r="4" fill="none" stroke="${ОБВОД}" stroke-width="1.2"/>
          <circle cx="243" cy="220" r="4" fill="none" stroke="${ОБВОД}" stroke-width="1.2"/>
          ${подпись(250,240,'ножницы',МУТ,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='площадь'?'hit':(в?'miss':''), 'Надо найти площадь всей поверхности', "r845Мастер('площадь')")}
        ${BTN(4, в==='объём'?'miss':'', 'Достаточно знать объём коробки', "r845Мастер('объём')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='площадь',
        в==='площадь' ? 'Верно: картон идёт на поверхность, поэтому нужна площадь всех граней — её удобно считать по развёртке.'
                     : 'Нет: объём — это сколько поместится внутрь. Картон нужен на поверхность, то есть на площадь граней.') : ЖДЁТ()) +
      ПРАВИЛО('Картон — это <b>площадь поверхности</b>, а не объём.');
  }

  /* 2. Грани призмы и формула Эйлера */
  function F2(s){
    const в = s.грани;
    return ВОПРОС('Сколько у четырёхугольной призмы граней, рёбер и вершин?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Элементы призмы',14,GOLD,true)}
        ${коробка(40,178,104,70,28,0.05)}
        ${подпись(262,110,'8 вершин',BLUE,12)}
        ${подпись(262,136,'12 рёбер',GREEN,12)}
        ${подпись(262,162,'6 граней',GOLD,12)}
        <g>${проявить('14s',0.5,0.6)}
          ${бумага(20,196,296,44,0.5,-0.4)}
          ${т(168,214,'В − Р + Г = 8 − 12 + 6 = 2',16,'#2b2110',true)}
          ${т(168,230,'это формула Эйлера — она верна для любой призмы',12,'#7a6a44')}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), '6 граней: два основания и четыре боковые', "r845Грани('шесть')")}
        ${BTN(4, в==='четыре'?'miss':'', '4 грани: только боковые', "r845Грани('четыре')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: у четырёхугольной призмы два основания и четыре боковые грани — всего шесть. И 8 − 12 + 6 = 2.'
                    : 'Нет: кроме четырёх боковых граней есть ещё два основания. Всего шесть граней.') : ЖДЁТ()) +
      ПРАВИЛО('У призмы два основания и столько боковых граней, <b>сколько сторон у основания</b>.');
  }

  /* 3. Развёртка призмы: пояс разворачивается */
  function F3(s){
    const в = s.развёртка;
    return ВОПРОС('Что получится, если разрезать коробку по боковым рёбрам и развернуть?') +
      `<div class="pic">${свг(`
        ${фон(254)}
        ${т(168,24,'Развёртка призмы',14,GOLD,true)}
        ${коробка(30,166,74,52,20,0.04)}
        <g>${проявить('14s',0.2,0.28)}
          ${т(140,140,'→',20,GOLD,true)}</g>
        ${развёрткаПризмы(150,66,104,26,58,0.28)}
        ${подпись(202,196,'пояс боковых граней',GREEN,12)}
        ${подпись(232,236,'и два основания',GOLD,12)}
      `,254)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пояс'?'hit':(в?'miss':''), 'Прямоугольный пояс из боковых граней и два основания', "r845Разв('пояс')")}
        ${BTN(4, в==='два'?'miss':'', 'Только два основания', "r845Разв('два')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пояс',
        в==='пояс' ? 'Верно: боковые грани разворачиваются в один прямоугольный пояс, а основания остаются отдельными.'
                   : 'Нет: одни основания не закроют бока. Боковые грани дают прямоугольный пояс.') : ЖДЁТ()) +
      ПРАВИЛО('Развёртка призмы: <b>пояс боковых граней и два основания</b>.');
  }

  /* 4. Площадь по развёртке */
  function F4(s){
    const в = s.площадь;
    return ВОПРОС('Коробка 5×4×3. Сколько картона нужно?') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${т(168,24,'Считаем по развёртке',14,GOLD,true)}
        ${стекло(14,38,308,44,0.05)}
        ${т(168,66,'два основания: 2 · (5 · 4) = 40',14,ИНК,true)}
        ${стекло(14,90,308,40,0.2)}
        ${т(168,116,'передняя и задняя: 2 · (5 · 3) = 30',14,ИНК,true)}
        ${стекло(14,138,308,40,0.34)}
        ${т(168,164,'боковые: 2 · (4 · 3) = 24',14,ИНК,true)}
        <g>${проявить('14s',0.56,0.66)}
          ${бумага(14,186,308,48,0.56,-0.4)}
          ${т(168,206,'40 + 30 + 24 = 94',20,'#2b2110',true)}
          ${т(168,224,'квадратных единиц картона',12,'#7a6a44')}</g>
      `,256)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='девяносто четыре'?'hit':(в?'miss':''), '94: 40 + 30 + 24', "r845Площ('девяносто четыре')")}
        ${BTN(4, в==='шестьдесят'?'miss':'', '60: это объём коробки', "r845Площ('шестьдесят')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='девяносто четыре',
        в==='девяносто четыре' ? 'Верно: 40 + 30 + 24 = 94. Картона нужно на площадь 94 квадратных единицы.'
                               : 'Нет: 60 — это объём. Картон считают по площади: 40 + 30 + 24 = 94.') : ЖДЁТ()) +
      ПРАВИЛО('Площадь коробки: <b>2ab + 2ac + 2bc</b>.');
  }

  /* 5. Объём призмы: слои */
  function F5(s){
    const в = s.объём;
    return ВОПРОС('Основание коробки 5×4, высота 3. Чему равен объём?') +
      `<div class="pic">${свг(`
        ${фон(254)}
        ${т(168,24,'Объём — это слои',14,GOLD,true)}
        ${/* три слоя по 20 */''}
        ${[0,1,2].map(i=>`<g>${проявить('14s',0.06+i*0.14,0.18+i*0.14)}
          <path d="M60 ${186-i*34} L176 ${186-i*34} L206 ${186-i*34-22} L90 ${186-i*34-22} Z"
            fill="url(#c845-верх)" stroke="${ОБВОД}" stroke-width="1.5"/>
          <path d="M60 ${186-i*34} L176 ${186-i*34} L176 ${196-i*34} L60 ${196-i*34} Z"
            fill="url(#c845-перед)" stroke="${ОБВОД}" stroke-width="1.5"/>
          <path d="M176 ${186-i*34} L206 ${186-i*34-22} L206 ${196-i*34-22} L176 ${196-i*34} Z"
            fill="url(#c845-бок2)" stroke="${ОБВОД}" stroke-width="1.5"/>
          ${т(240,182-i*34,'слой 20',12,i?МУТ:GOLD,true)}</g>`).join('')}
        <g>${проявить('14s',0.6,0.7)}
          ${подпись(168,214,'20 · 3 = 60',GREEN,16)}</g>
        <g>${проявить('14s',0.8,0.88)}
          ${т(168,240,'V = площадь основания · высота',12,ИНК)}</g>
      `,254)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шестьдесят'?'hit':(в?'miss':''), '60: 20 · 3', "r845Объём('шестьдесят')")}
        ${BTN(4, в==='девяносто четыре'?'miss':'', '94: это площадь поверхности', "r845Объём('девяносто четыре')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шестьдесят',
        в==='шестьдесят' ? 'Верно: площадь основания 20, высота 3, значит объём 20 · 3 = 60. Три слоя по двадцать.'
                         : 'Нет: 94 — это картон. Объём равен площади основания на высоту: 20 · 3 = 60.') : ЖДЁТ()) +
      ПРАВИЛО('Объём призмы: <b>V = S · h</b> — основание на высоту.');
  }

  /* 6. Пирамида: элементы */
  function F6(s){
    const в = s.пирамида;
    return ВОПРОС('Сколько граней у четырёхугольной пирамиды?') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${т(168,24,'Элементы пирамиды',14,GOLD,true)}
        ${пирамида(80,190,116,70,28,116,0.05)}
        ${подпись(112,68,'вершина',GOLD,12)}
        ${подпись(232,116,'боковая грань',ИНК,12)}
        ${подпись(150,214,'основание — квадрат 5 × 4',МУТ,12)}
        <g>${проявить('14s',0.5,0.6)}
          ${бумага(20,210,296,36,0.5,-0.4)}
          ${т(168,228,'5 граней: основание и четыре треугольника',12,'#2b2110',true)}</g>
      `,256)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пять'?'hit':(в?'miss':''), '5: основание и четыре боковые грани', "r845Пирамида('пять')")}
        ${BTN(4, в==='четыре'?'miss':'', '4: только боковые грани', "r845Пирамида('четыре')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пять',
        в==='пять' ? 'Верно: у четырёхугольной пирамиды основание и четыре треугольные грани — пять граней, 5 − 8 + 5 = 2.'
                   : 'Нет: боковых граней четыре, но есть ещё основание. Всего пять.') : ЖДЁТ()) +
      ПРАВИЛО('У пирамиды одна грань-основание и <b>столько боковых, сколько сторон</b>.');
  }

  /* 7. Развёртка пирамиды: плоская развёртка и собранная пирамида рядом */
  function F7(s){
    const в = s.развёрткаП;
    const цx=88, цy=150, п=34;   /* центр основания развёртки и полуразмер */
    return ВОПРОС('Из чего состоит развёртка четырёхугольной пирамиды?') +
      `<div class="pic">${свг(`
        ${фон(276)}
        ${т(168,24,'Развёртка и собранная пирамида',14,GOLD,true)}
        ${/* основание развёртки */''}
        <g>${проявить('14s',0.04,0.12)}
          <path d="M${цx-п} ${цy-п} L${цx+п} ${цy-п} L${цx+п} ${цy} L${цx-п} ${цy} Z"
            fill="url(#c845-бок2)" stroke="${ОБВОД}" stroke-width="1.8"/></g>
        ${/* четыре треугольника вокруг */''}
        ${[[цx,цy-п-п*1.35,цx-п,цy-п,цx+п,цy-п],
           [цx-п-п*1.35,цy-п,цx-п,цy-п,цx-п,цy],
           [цx+п+п*1.35,цy-п,цx+п,цy-п,цx+п,цy],
           [цx,цy-п+п*1.35,цx-п,цy,цx+п,цy]].map(([вx,вy,ax,ay,bx,by],i)=>
          `<g>${проявить('14s',0.1+i*0.06,0.18+i*0.06)}
            <path d="M${вx} ${вy} L${ax} ${ay} L${bx} ${by} Z" fill="url(#c845-${i%2?'перед':'верх'})"
              stroke="${ОБВОД}" stroke-width="1.6"/></g>`).join('')}
        ${подпись(96,240,'квадрат и 4 треугольника',GREEN,12)}
        <g>${проявить('14s',0.4,0.5)}
          ${т(190,140,'→',20,GOLD,true)}</g>
        ${пирамида(214,206,72,44,18,72,0.44)}
        ${подпись(252,60,'собранная пирамида',МУТ,12)}
        <g>${проявить('14s',0.74,0.84)}
          ${т(168,262,'треугольники сходятся в вершине',12,ИНК)}</g>
      `,276)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='квадрат'?'hit':(в?'miss':''), 'Основание-квадрат и четыре треугольника', "r845РазвП('квадрат')")}
        ${BTN(4, в==='пять'?'miss':'', 'Пять квадратов, как у куба', "r845РазвП('пять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='квадрат',
        в==='квадрат' ? 'Верно: развёртка пирамиды — это основание и столько треугольников, сколько сторон у основания.'
                      : 'Нет: пять квадратов — это развёртка куба. У пирамиды боковые грани треугольные.') : ЖДЁТ()) +
      ПРАВИЛО('Развёртка пирамиды: <b>основание и треугольники</b>.');
  }

  /* 8. Почему треть */
  function F8(s){
    const в = s.треть;
    return ВОПРОС('Сколько таких пирамид нужно, чтобы заполнить призму?') +
      `<div class="pic">${свг(`
        ${фон(296)}
        ${т(168,24,'Три пирамиды — одна призма',14,GOLD,true)}
        ${коробка(200,190,80,60,22,0.04)}
        ${/* три пирамиды влетают в призму */''}
        ${[0,1,2].map(i=>`<g>${анТК('0 0;0 0;'+(120+i*10-40-i*30)+' '+(190-150)+';'+(120+i*10-40-i*30)+' '+(190-150)+';0 0','16s',
            '0;'+кт(0.12+i*0.1)+';'+кт(0.44+i*0.1)+';0.9;1')}
          ${пирамида(40+i*40,150,44,30,14,52,0)}</g>`).join('')}
        <g>${проявить('16s',0.5,0.6)}
          ${стекло(14,40,308,50,0.5)}
          ${т(168,62,'объём пирамиды 20, призмы 60',14,ИНК,true)}
          ${т(168,80,'20 · 3 = 60',16,GREEN,true)}</g>
        <g>${проявить('16s',0.68,0.78)}
          ${подпись(168,262,'поэтому V = 1/3 · S · h',GOLD,14)}</g>
        <g>${проявить('16s',0.84,0.92)}
          ${т(168,284,'пирамида занимает ровно треть призмы',12,МУТ)}</g>
      `,296)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), 'Три: объём пирамиды — треть призмы', "r845Треть('три')")}
        ${BTN(4, в==='две'?'miss':'', 'Две: пополам', "r845Треть('две')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: 20 · 3 = 60, поэтому объём пирамиды равен трети объёма призмы с тем же основанием и высотой.'
                  : 'Нет: две пирамиды дадут 40, а в призме 60. Нужно три.') : ЖДЁТ()) +
      ПРАВИЛО('Объём пирамиды: <b>V = 1/3 · S · h</b>.');
  }

  /* 9. Числовой пример */
  function F9(s){
    const в = s.пример;
    return ВОПРОС('Пирамида с основанием 5×4 и высотой 3. Чему равен объём?') +
      `<div class="pic">${свг(`
        ${фон(272)}
        ${т(168,24,'Считаем объём пирамиды',14,GOLD,true)}
        ${пирамида(96,196,120,72,28,112,0.05)}
        ${подпись(156,246,'основание 5 × 4 = 20',МУТ,12)}
        <g>${проявить('14s',0.34,0.44)}
          ${стекло(14,38,308,44,0.34)}
          ${т(168,66,'V = 1/3 · 20 · 3',16,ИНК,true)}</g>
        <g>${проявить('14s',0.56,0.66)}
          ${бумага(14,86,150,60,0.56,-1)}
          ${т(89,110,'пирамида',12,'#7a6a44')}
          ${т(89,138,'20',20,'#2b2110',true)}
          ${бумага(172,86,150,60,0.66,1)}
          ${т(247,110,'призма',12,'#7a6a44')}
          ${т(247,138,'60',20,'#2b2110',true)}</g>
        <g>${проявить('14s',0.78,0.88)}
          ${подпись(168,262,'втрое меньше — это и есть множитель 1/3',GREEN,12)}</g>
      `,272)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='двадцать'?'hit':(в?'miss':''), '20: 20 · 3 : 3', "r845Пример('двадцать')")}
        ${BTN(4, в==='шестьдесят'?'miss':'', '60: как у призмы', "r845Пример('шестьдесят')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='двадцать',
        в==='двадцать' ? 'Верно: V = 1/3 · 20 · 3 = 20. Ровно втрое меньше призмы с теми же основанием и высотой.'
                       : 'Нет: 60 — это объём призмы. У пирамиды треть: 1/3 · 20 · 3 = 20.') : ЖДЁТ()) +
      ПРАВИЛО('V пирамиды = <b>(S · h) : 3</b>.');
  }

  /* 10. Сводка */
  function F10(s){
    const в = s.сводка;
    return ВОПРОС('Что нужно знать, чтобы посчитать объём призмы?') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${т(168,24,'Формулы и развёртки',14,GOLD,true)}
        ${стекло(14,38,308,44,0.05)}
        ${т(168,66,'призма: V = S · h    пирамида: V = (S · h) : 3',14,GREEN,true)}
        ${стекло(14,90,308,44,0.2)}
        ${т(168,118,'картон — это площадь поверхности',14,ИНК,true)}
        <g>${проявить('14s',0.4,0.5)}
          ${стекло(14,142,150,54,0.4)}
          ${т(89,164,'развёртка призмы',12,МУТ)}
          ${т(89,184,'пояс + 2 основания',12,GOLD,true)}
          ${стекло(172,142,150,54,0.5)}
          ${т(247,164,'развёртка пирамиды',12,МУТ)}
          ${т(247,184,'основание + треугольники',12,GOLD,true)}</g>
        <g>${проявить('14s',0.66,0.76)}
          ${подпись(168,222,'В − Р + Г = 2 для любого многогранника',ИНК,12)}</g>
        <g>${проявить('14s',0.84,0.92)}
          ${подпись(168,250,'не путай объём и площадь',RED,12)}</g>
      `,256)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='площадь'?'hit':(в?'miss':''), 'Площадь основания и высоту', "r845Свод('площадь')")}
        ${BTN(4, в==='периметр'?'miss':'', 'Периметр основания', "r845Свод('периметр')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='площадь',
        в==='площадь' ? 'Верно: объём призмы — это площадь основания, умноженная на высоту. Периметр нужен для площади боковой поверхности.'
                      : 'Нет: периметр нужен для боковой поверхности. Для объёма нужна площадь основания и высота.') : ЖДЁТ()) +
      ПРАВИЛО('Объём — <b>площадь на высоту</b>; картон — <b>площадь поверхности</b>.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Сколько картона нужно на коробку 5×4×3?', варианты:[
        {т:'94', ок:true,  почему:'2·20 + 2·15 + 2·12 = 94.'},
        {т:'60', ок:false, почему:'60 — это объём.'}]},
    { вопрос:'Чему равен объём коробки 5×4×3?', варианты:[
        {т:'60', ок:true,  почему:'20 · 3 = 60.'},
        {т:'94', ок:false, почему:'94 — площадь поверхности.'}]},
    { вопрос:'Сколько граней у четырёхугольной пирамиды?', варианты:[
        {т:'5', ок:true,  почему:'Основание и четыре треугольника.'},
        {т:'4', ок:false, почему:'Забыто основание.'}]},
    { вопрос:'Из чего состоит развёртка призмы?', варианты:[
        {т:'Пояс боковых граней и два основания', ок:true,  почему:'Боковые грани образуют прямоугольный пояс.'},
        {т:'Только два основания', ок:false, почему:'Боковые грани тоже нужны.'}]},
    { вопрос:'Пирамида и призма с основанием 20 и высотой 3. Во сколько раз объём пирамиды меньше?', варианты:[
        {т:'В три раза', ок:true,  почему:'V = 1/3 · S · h.'},
        {т:'В два раза', ок:false, почему:'Множитель 1/3, а не 1/2.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Две опоры: картон считаем по развёртке, объём — по формуле S · h, а у пирамиды берём треть.') +
        `<div class="ask">${BTN(4,'','пройти заново','r845Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r845Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Призма: <b>V = S · h</b>; пирамида: <b>V = (S · h) : 3</b>.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · развёртки',
    задания:[
      { ф:'Из чего состоит развёртка призмы?', о:['пояс и два основания','два основания'], в:0, р:'Боковые грани дают пояс.' },
      { ф:'Из чего состоит развёртка пирамиды?', о:['основание и треугольники','пять квадратов'], в:0, р:'Боковые грани треугольные.' },
      { ф:'Сколько граней у четырёхугольной призмы?', о:['6','4','8'], в:0, р:'Два основания и четыре боковые.' },
      { ф:'Сколько граней у треугольной пирамиды?', о:['4','3','6'], в:0, р:'Основание и три треугольника.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · объём',
    задания:[
      { ф:'Коробка 5×4×3. Объём?', о:['60','94','20'], в:0, р:'5·4·3 = 60.' },
      { ф:'Пирамида с основанием 20 и высотой 3. Объём?', о:['20','60','10'], в:0, р:'20·3:3 = 20.' },
      { ф:'Призма с основанием 20 и высотой 3. Объём?', о:['60','20','23'], в:0, р:'20·3 = 60.' },
      { ф:'Во сколько раз пирамида меньше призмы?', о:['в 3 раза','в 2 раза'], в:0, р:'Множитель 1/3.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · поверхность и ловушки',
    задания:[
      { ф:'Что даёт формула 2ab + 2ac + 2bc?', о:['площадь поверхности','объём'], в:0, р:'Это площадь всех граней коробки.' },
      { ф:'Треугольная призма: основание 6, боковая 120. Полная?', о:['132','126','120'], в:0, р:'2·6 + 120 = 132.' },
      { ф:'В − Р + Г для пирамиды 5, 8, 5?', о:['2','0','5'], в:0, р:'5 − 8 + 5 = 2.' },
      { ф:'Сколько нужно пирамид, чтобы заполнить призму?', о:['3','2','6'], в:0, р:'Объём пирамиды — треть.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r845T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r845TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L845={
    id: ID, title:'Призма и пирамида: объём и развёртки', ico:'📦',
    src:'Математика · 6 класс · Геометрия', subj:'math',
    explain: [
      'В мастерской на столе коробка и пирамидка. Спрашивают: сколько картона уйдёт на коробку? Здесь важно не перепутать две разные величины. Объём показывает, сколько поместится внутри, а картон идёт на поверхность, то есть на площадь всех граней. Поэтому сначала разберёмся с гранями и развёрткой, а потом посчитаем объём.',

      'Разберём четырёхугольную призму. У неё два основания — равных четырёхугольника — и четыре боковые грани. Всего шесть граней. Вершин восемь, рёбер двенадцать. Для любого многогранника выполняется формула Эйлера: число вершин минус число рёбер плюс число граней равно двум. Проверим: 8 − 12 + 6 = 2. Частая ошибка — считать только боковые грани и забыть основания. Как проверить себя: подставь числа в формулу Эйлера.',

      'Чтобы посчитать картон, коробку разрезают по боковым рёбрам и разворачивают на плоскость. Получается развёртка: четыре боковые грани образуют один длинный прямоугольный пояс, а два основания остаются отдельными четырёхугольниками. Такую развёртку можно вырезать из одного листа картона и склеить. Частая ошибка — считать, что развёртка состоит только из оснований. Как проверить себя: посмотри, закроет ли пояс бока коробки.',

      'Посчитаем картон для коробки 5 на 4 на 3. Два основания дают 2 · (5 · 4) = 40. Передняя и задняя грани — 2 · (5 · 3) = 30. Две боковые — 2 · (4 · 3) = 24. Складываем: 40 + 30 + 24 = 94. Значит, картона нужно на площадь 94 квадратных единицы. Частая ошибка — ответить 60, то есть объёмом вместо площади. Как проверить себя: сложи площади всех шести граней.',

      'Теперь объём призмы. Он равен площади основания, умноженной на высоту: V = S · h. Для нашей коробки площадь основания 5 · 4 = 20, высота 3, значит объём 20 · 3 = 60. Это удобно представить слоями: три слоя по двадцать кубиков. Частая ошибка — умножать все три измерения, забыв, что сначала нужна площадь основания. Как проверить себя: посчитай площадь основания отдельно.',

      'Перейдём к пирамиде. У четырёхугольной пирамиды есть основание и четыре треугольные боковые грани, сходящиеся в одной вершине. Всего пять граней, пять вершин и восемь рёбер. Формула Эйлера снова работает: 5 − 8 + 5 = 2. Обратите внимание: боковые грани у пирамиды не прямоугольники, а треугольники. Частая ошибка — считать боковые грани квадратами. Как проверить себя: посмотри на вершину пирамиды.',

      'Развёртка пирамиды выглядит иначе: в центре основание, а вокруг него — треугольники, по одному на каждую сторону основания. Если такую развёртку вырезать и сложить по линиям сгиба, треугольники поднимутся и сойдутся в вершине — получится пирамида. Частая ошибка — рисовать развёртку пирамиды из квадратов. Как проверить себя: боковые грани должны быть треугольными.',

      'Почему объём пирамиды в три раза меньше объёма призмы с тем же основанием и высотой? Это можно проверить опытом: три одинаковые пирамиды полностью заполняют призму. Отсюда формула: V = 1/3 · S · h. Для нашего примера основание 20, высота 3, значит объём пирамиды равен 1/3 · 20 · 3 = 20, а объём призмы — 60. Три пирамиды дают ровно 60. Частая ошибка — забыть множитель 1/3. Как проверить себя: три объёма пирамиды должны дать объём призмы.',

      'Полезно помнить формулу Эйлера: для любого многогранника число вершин минус число рёбер плюс число граней равно двум. Проверим на разных телах: у треугольной призмы 6 − 9 + 5 = 2, у четырёхугольной призмы 8 − 12 + 6 = 2, у четырёхугольной пирамиды 5 − 8 + 5 = 2, у треугольной пирамиды 4 − 6 + 4 = 2, у куба 8 − 12 + 6 = 2. Частая ошибка — путать рёбра и грани. Как проверить себя: подставь числа в формулу.',

      'Соберём всё вместе. Картон считают по развёртке, то есть по площади поверхности: у коробки это 2ab + 2ac + 2bc. Объём призмы равен площади основания на высоту, а объём пирамиды — трети этого произведения. Развёртка призмы — пояс боковых граней и два основания, развёртка пирамиды — основание и треугольники. Частая ошибка — путать объём и площадь. Как проверить себя: спроси, картон мы считаем или то, что поместится внутрь.',

      'Пройти практику: пять уровней про развёртки, площадь поверхности, объём призмы и объём пирамиды. Дальше ждут три тренажёра.',

      'Первый тренажёр — развёртки: из каких частей состоят и сколько граней у тел.',

      'Второй тренажёр — объём: призма и пирамида, множитель одна треть.',

      'Третий тренажёр — поверхность и ловушки: формула Эйлера и разница между объёмом и площадью.'
    ],
    check: {
      q:'Сколько картона нужно на коробку 5 × 4 × 3?',
      choices:['94','60','20'],
      ans:0,
      exp:'2·(5·4) + 2·(5·3) + 2·(4·3) = 40 + 30 + 24 = 94.'
    },
    tasks: [
      { q:'Чему равен объём коробки 5 × 4 × 3?', kind:'choice',
        choices:['60','94'], ans:0,
        hints:['Сначала площадь основания.','20 · 3.'],
        sol:'60.' },
      { q:'Сколько граней у четырёхугольной пирамиды?', kind:'choice',
        choices:['5','4'], ans:0,
        hints:['Не забудь основание.','Основание и четыре треугольника.'],
        sol:'5.' },
      { q:'Пирамида и призма с основанием 20 и высотой 3. Во сколько раз пирамида меньше?', kind:'choice',
        choices:['В три раза','В два раза'], ans:0,
        hints:['Формула V = 1/3 · S · h.','20 · 3 : 3 = 20.'],
        sol:'В три раза.' },
      { q:'Что даёт формула 2ab + 2ac + 2bc?', kind:'choice',
        choices:['Площадь поверхности коробки','Объём коробки'], ans:0,
        hints:['Это сумма площадей всех шести граней.','Картон считают по площади.'],
        sol:'Площадь поверхности.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L845.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Мастерская','Коробка и пирамидка'],
      2:['Призма','Грани, рёбра, вершины'],
      3:['Развёртка призмы','Пояс и основания'],
      4:['Картон','40 + 30 + 24 = 94'],
      5:['Объём призмы','Слои по 20'],
      6:['Пирамида','Основание и треугольники'],
      7:['Развёртка пирамиды','Складываем обратно'],
      8:['Почему треть','Три пирамиды — призма'],
      9:['Пример','V = 20'],
      10:['Формулы','И развёртки'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','Развёртки'],
      13:['Тренажёр 2','Объём'],
      14:['Тренажёр 3','Поверхность и ловушки']
    };
    const з=ЗАГОЛОВКИ[f]||['Призма и пирамида','Объём и развёртки'];
    el.innerHTML = `<div class="s6 l845" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r845Мастер=(к)=>{ S().мастерская=к; chRender(0); };
  window.r845Грани=(к)=>{ S().грани=к; chRender(0); };
  window.r845Разв=(к)=>{ S().развёртка=к; chRender(0); };
  window.r845Площ=(к)=>{ S().площадь=к; chRender(0); };
  window.r845Объём=(к)=>{ S().объём=к; chRender(0); };
  window.r845Пирамида=(к)=>{ S().пирамида=к; chRender(0); };
  window.r845РазвП=(к)=>{ S().развёрткаП=к; chRender(0); };
  window.r845Треть=(к)=>{ S().треть=к; chRender(0); };
  window.r845Пример=(к)=>{ S().пример=к; chRender(0); };
  window.r845Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r845Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r845Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r845T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r845TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L845; else arr.push(L845);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA845={render:render, L:L845};
})();
