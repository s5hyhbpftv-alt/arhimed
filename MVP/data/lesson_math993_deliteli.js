/* ============ МАТЕМАТИКА · УРОК 993 · «ДЕЛИТЕЛИ И КРАТНЫЕ» ============
   5 класс. Сюжет: плиточная мастерская — из плиток выкладывают прямоугольники.

   ПЕРЕДЕЛАН ПО НОВОМУ СТАНДАРТУ (было 10 кадров и один тренажёр):
   14 кадров = 10 объясняющих + практика из пяти уровней + ТРИ тренажёра,
   реалистичные материалы (керамика, дерево, стекло, бумага), пара теней
   у каждого предмета, зерно и виньетка, движение на кривых проекта
   (SMIL calcMode="spline", keySplines="0.23 1 0.32 1").

   СВЕРКА С ПЕТЕРСОНОМ (5 класс): делители и кратные, деление с остатком,
   простые и составные числа; НОД и НОК — углублённая линия.
   Источники: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     делители 12: 1, 2, 3, 4, 6, 12 (6 штук);  пары: 1·12, 2·6, 3·4
     делители 24: 1, 2, 3, 4, 6, 8, 12, 24 (8 штук); пары: 1·24, 2·12, 3·8, 4·6
     делители 18: 1, 2, 3, 6, 9, 18;  пары: 1·18, 2·9, 3·6
     общие делители 18 и 24: 1, 2, 3, 6
     кратные 12: 12, 24, 36, 48, 60;  кратные 18: 18, 36, 54, 72
     общие кратные 12 и 18: 36, 72, 108 → наименьшее 36
     делители 7: 1 и 7 (простое);  делители 1: только 1

   ЗАЧЕМ ПЛИТКИ: делитель — это сторона прямоугольника, который ровно
   выкладывается плитками. Поэтому все делители числа видны как пары сторон,
   и рисунок не спорит с арифметикой: 12 плиток дают ровно 3 пары сторон.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, keyTimes строго в [0;1], reduced-motion выключает
   SMIL полностью, ничего не выходит за край кадра. Подписи короткие. */
(function(){
  'use strict';

  const ID = 993;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f', ОБВОД='#33291e';

  const CSS=`
  #lvis .s6.l993{gap:14px}
  #lvis .s6.l993 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l993 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l993 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l993 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l993 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l993 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l993 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l993 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l993 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l993 .правило b{color:${GOLD}}
  #lvis .s6.l993 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l993 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l993 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l993 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l993 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l993 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l993 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l993 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l993 .уровни .точка.сейчас{background:${GOLD};animation:l993dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l993dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l993 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l993{-webkit-text-size-adjust:100%}
  #lvis .s6.l993 [data-anim]{animation:l993rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l993rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l993 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l993 [data-anim]{animation:none!important}
    #lvis .s6.l993 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l993 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l993-style');
      if(!s){ s=document.createElement('style'); s.id='l993-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ: КРИВЫЕ ПРОЕКТА ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анД = (путь,длит,доп) =>
    ДВИЖ ? `<animateMotion dur="${длит}" repeatCount="indefinite" path="${путь}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');
  const светит = (длит,нач) => ан('opacity','1;0.5;1',длит, нач!=null?'begin="'+нач+'s"':'');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c993-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2a3f33"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#111c15"/>
      </linearGradient>
      <linearGradient id="c993-плитка" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdf6e3"/><stop offset="0.32" stop-color="#f0e0bd"/>
        <stop offset="0.72" stop-color="#d8c39a"/><stop offset="1" stop-color="#b8a077"/>
      </linearGradient>
      <linearGradient id="c993-плитка2" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#dff0fb"/><stop offset="0.32" stop-color="#b8d9ef"/>
        <stop offset="0.72" stop-color="#7fb0cd"/><stop offset="1" stop-color="#568aa8"/>
      </linearGradient>
      <linearGradient id="c993-плитка3" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#e2f5e9"/><stop offset="0.32" stop-color="#b9e4c9"/>
        <stop offset="0.72" stop-color="#83c1a0"/><stop offset="1" stop-color="#5d9a78"/>
      </linearGradient>
      <linearGradient id="c993-дерево" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#a9793f"/><stop offset="0.35" stop-color="#8a5f2f"/>
        <stop offset="1" stop-color="#4f3418"/>
      </linearGradient>
      <linearGradient id="c993-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.55" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <linearGradient id="c993-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".7"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".3"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".25"/>
      </linearGradient>
      <linearGradient id="c993-металл" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#5d6a76"/><stop offset="0.18" stop-color="#e8eef4"/>
        <stop offset="0.5" stop-color="#9fb0bd"/><stop offset="0.8" stop-color="#57636f"/>
        <stop offset="1" stop-color="#434d57"/>
      </linearGradient>
      <filter id="c993-тень" x="-70%" y="-70%" width="240%" height="240%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3.6" result="р"/>
        <feOffset in="р" dx="3" dy="5" result="с"/>
        <feFlood flood-color="#0a1a24" flood-opacity="0.4" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c993-контакт" x="-70%" y="-150%" width="240%" height="400%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.4" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#1d1508" flood-opacity="0.42" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c993-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c993-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c993-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c993-шум)" opacity="0.05"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  /* ПЛИТКА: керамика с фаской, глазурью и блеском */
  const плитка = (x,y,ш,в,материал,нач,номер) => `<g>
    <rect x="${x+1.5}" y="${y+2.5}" width="${ш}" height="${в}" rx="3" fill="#08171f" opacity=".34" filter="url(#c993-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="3" fill="url(#c993-${материал||'плитка'})"
      stroke="rgba(70,55,30,.6)" stroke-width="1">
      ${нач!=null?проявить('12s',нач,нач+0.04):''}</rect>
    <path d="M${x+1.5} ${y+ш*0.22} L${x+1.5} ${y+1.5} L${x+ш*0.3} ${y+1.5}" fill="none" stroke="rgba(255,255,255,.55)" stroke-width="1.2"/>
    <path d="M${x+ш-1.5} ${y+в-ш*0.2} L${x+ш-1.5} ${y+в-1.5} L${x+ш-ш*0.3} ${y+в-1.5}" fill="none" stroke="rgba(0,0,0,.18)" stroke-width="1.2"/>
    ${номер!=null?т(x+ш/2,y+в/2+5,String(номер),14,'#4a3a1c',true):''}
  </g>`;

  const стол = (x,y,ш) => `<g>
    <rect x="${x}" y="${y}" width="${ш}" height="10" rx="3" fill="url(#c993-дерево)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="3" rx="1.5" fill="rgba(255,255,255,.22)"/>
    <rect x="${x}" y="${y+10}" width="${ш}" height="4" fill="rgba(0,0,0,.25)"/>
  </g>`;

  const бумага = (x,y,ш,в,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="6" fill="#08171f" opacity=".36" filter="url(#c993-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="url(#c993-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('12s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="6" fill="none" filter="url(#c993-шум)" opacity=".08"/>
    <line x1="${x+5}" y1="${y+5}" x2="${x+ш-5}" y2="${y+5}" stroke="rgba(255,255,255,.5)" stroke-width="1.2"/>
  </g>`;

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#08171f" opacity=".32" filter="url(#c993-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c993-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('12s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  const фишка = (x,y,текст,цвет,кегль,нач) => {
    const ш=Math.max(26,String(текст).length*11+12);
    return `<g>
      <rect x="${x}" y="${y}" width="${ш}" height="26" rx="13"
        fill="rgba(255,255,255,.06)" stroke="${цвет||ЛИНИЯ}" stroke-width="1.5">
        ${нач!=null?проявить('12s',нач,нач+0.05):''}</rect>
      ${т(x+ш/2,y+18,текст,кегль||14,цвет||ИНК,true)}
    </g>`;
  };

  /* ПРЯМОУГОЛЬНИК ИЗ ПЛИТОК: сторона × сторона = число */
  function выкладка(x,y,число,сторона,материал,нач,подпись){
    const кол=сторона, стр=число/сторона, размер=Math.min(15,86/Math.max(кол,стр));
    return `<g>
      ${Array.from({length:число},(_,i)=>{
        const к=i%кол, р=Math.floor(i/кол);
        return плитка(x+к*размер,y+р*размер,размер-1.4,размер-1.4,материал,нач!=null?нач+i*0.012:null);
      }).join('')}
      ${подпись?т(x+кол*размер/2,y+стр*размер+18,подпись,12,МУТ):''}
    </g>`;
  }

  /* ================= КАДРЫ ================= */

  /* 1. Мастерская: 12 плиток */
  function F1(s){
    const в = s.мастерская;
    return ВОПРОС('Сколько разных прямоугольников можно выложить из 12 одинаковых плиток?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Плиточная мастерская',14,GOLD,true)}
        ${стол(18,196,300)}
        ${Array.from({length:12},(_,i)=>плитка(40+i*21,160,20,20,'плитка',i*0.03,i+1)).join('')}
        <g>${проявить('12s',0.42,0.52)}
          ${т(168,60,'12 плиток',16,ИНК,true)}
          ${т(168,90,'уложим их в прямоугольник',12,МУТ)}
          ${т(168,120,'без пропусков и лишних плиток',12,МУТ)}</g>
        <g>${проявить('12s',0.62,0.72)}
          ${бумага(78,214,180,30,0.62,0)}
          ${т(168,235,'сколько способов?',14,'#2b2110',true)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), '3 способа: 1×12, 2×6, 3×4', "r993Shop('три')")}
        ${BTN(4, в==='шесть'?'miss':'', '6 способов: по числу делителей', "r993Shop('шесть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: прямоугольники 1×12, 2×6 и 3×4 — три способа. Четвёртый, 4×3, — тот же самый, только повёрнутый.'
                  : 'Нет: делителей у 12 шесть, но стороны считают парами, поэтому способов три.') : ЖДЁТ()) +
      ПРАВИЛО('Стороны прямоугольника — это <b>делители</b> числа плиток.');
  }

  /* 2. Делители 12 */
  function F2(s){
    const в = s.делитель;
    const делители=[1,2,3,4,6,12];
    return ВОПРОС('Какое число называют делителем числа 12?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Делители числа 12',14,GOLD,true)}
        ${делители.map((д,i)=>фишка(18+i*52,42,String(д),GOLD,14,i*0.05)).join('')}
        <g>${проявить('12s',0.3,0.4)}
          ${т(168,92,'12 делится на каждое без остатка',12,МУТ)}</g>
        <g>${проявить('12s',0.4,0.5)}
          ${стол(18,206,300)}
          ${выкладка(30,116,12,2,'плитка2',0.44,'2 · 6')}
          ${выкладка(190,116,12,3,'плитка3',0.5,'3 · 4')}</g>
        <g>${проявить('12s',0.7,0.8)}
          ${т(168,236,'делителей у числа 12 ровно шесть',14,GREEN,true)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нацело'?'hit':(в?'miss':''), 'То, на которое 12 делится без остатка', "r993Div('нацело')")}
        ${BTN(4, в==='больше'?'miss':'', 'Любое число, которое больше 12', "r993Div('больше')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нацело',
        в==='нацело' ? 'Верно: делитель — то число, на которое делят без остатка. У 12 их шесть: 1, 2, 3, 4, 6, 12.'
                     : 'Нет: делитель не обязан быть больше. Например, 2 — делитель 12, потому что 12 : 2 = 6 без остатка.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Делитель</b> — число, на которое данное делится без остатка.');
  }

  /* 3. Пары делителей */
  function F3(s){
    const в = s.пары;
    const пары=[[1,12],[2,6],[3,4]];
    return ВОПРОС('Как найти все делители, не перебирая всё подряд?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Ищем делители парами',14,GOLD,true)}
        ${пары.map(([а,б],i)=>{
          const y=44+i*54;
          return `<g>
            ${стекло(16,y,96,44,i*0.06)}
            ${т(64,y+28,а+' · '+б,16,GOLD,true)}
            ${проявить('12s',0.2+i*0.1,0.28+i*0.1)}
            ${т(130,y+28,'= 12',14,ИНК,true)}
          </g>`;
        }).join('')}
        <g>${проявить('12s',0.5,0.6)}
          ${бумага(16,208,304,34,0.5,-0.5)}
          ${т(168,230,'12 = 1·12 = 2·6 = 3·4',14,'#2b2110',true)}
          ${т(168,246,'делители: 1, 2, 3, 4, 6, 12',12,'#7a6a44')}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='парами'?'hit':(в?'miss':''), 'Перебирать парами: 1·12, 2·6, 3·4', "r993Pairs('парами')")}
        ${BTN(4, в==='подряд'?'miss':'', 'Проверять все числа от 1 до 12 подряд', "r993Pairs('подряд')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='парами',
        в==='парами' ? 'Верно: делители идут парами, и перебор можно остановить, когда пара встретилась: 1·12, 2·6, 3·4.'
                     : 'Нет: перебор подряд тоже работает, но дольше. Пары позволяют остановиться на середине.') : ЖДЁТ()) +
      ПРАВИЛО('Делители удобно искать <b>парами</b>: произведение пары даёт само число.');
  }

  /* 4. Кратные: шаги по лучу */
  function F4(s){
    const в = s.кратное;
    const шаг=44, x0=30;
    return ВОПРОС('Какие числа называют кратными числу 12?') +
      `<div class="pic">${свг(`
        ${фон(240)}
        ${т(168,24,'Кратные 12 — шаги по лучу',14,GOLD,true)}
        <line x1="${x0}" y1="130" x2="${x0+5*шаг}" y2="130" stroke="${ЛИНИЯ}" stroke-width="2.4"/>
        ${Array.from({length:6},(_,i)=>`<g>
          <line x1="${x0+i*шаг}" y1="123" x2="${x0+i*шаг}" y2="137" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
          ${т(x0+i*шаг,156,String(i*12),14,i?GOLD:МУТ,true)}</g>`).join('')}
        ${Array.from({length:6},(_,i)=>`<g>${проявить('12s',0.1+i*0.06,0.18+i*0.06)}
          ${плитка(x0+i*шаг-14,176,28,24,'плитка3',null)}
          ${т(x0+i*шаг,193,String(i*12),12,'#2a4436',true)}</g>`).join('')}
        <g>${анД('M0 0 L'+(5*шаг)+' 0','9s')}
          <circle cx="${x0}" cy="130" r="8" fill="url(#c993-металл)" stroke="rgba(20,35,28,.6)" stroke-width="1.2"/>
          <circle cx="${x0-2.4}" cy="127.6" r="2.2" fill="#fff" opacity=".85"/>
        </g>
        <g>${проявить('12s',0.6,0.7)}
          ${т(168,222,'12, 24, 36, 48, 60 … — без конца',14,ИНК,true)}</g>
      `,240)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='умножением'?'hit':(в?'miss':''), 'Те, что получаются умножением 12 на 1, 2, 3, 4…', "r993Mult('умножением')")}
        ${BTN(4, в==='меньше'?'miss':'', 'Те, которые меньше 12 и делятся на 12', "r993Mult('меньше')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='умножением',
        в==='умножением' ? 'Верно: кратные получают умножением на 1, 2, 3, 4 и так далее — 12, 24, 36, 48…'
                         : 'Нет: кратных бесконечно много, и самое маленькое из них — само число 12.') : ЖДЁТ()) +
      ПРАВИЛО('Кратные — это <b>12 · 1, 12 · 2, 12 · 3 …</b>; их бесконечно много.');
  }

  /* 5. Связь слов */
  function F5(s){
    const в = s.связь;
    return ВОПРОС('Как связаны слова «делитель» и «кратное» для чисел 4 и 12?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        ${т(168,24,'Два слова об одном и том же',14,GOLD,true)}
        ${стекло(24,50,120,52,0.06)}
        ${т(84,82,'4 — делитель 12',14,GREEN,true)}
        ${стекло(192,50,120,52,0.2)}
        ${т(252,82,'12 — кратное 4',14,GOLD,true)}
        <g>${проявить('12s',0.3,0.4)}
          <path d="M144 76 L192 76" stroke="${ЛИНИЯ}" stroke-width="2"/>
          <path d="M192 88 L144 88" stroke="${ЛИНИЯ}" stroke-width="2"/></g>
        <g>${проявить('12s',0.5,0.6)}
          ${т(168,124,'12 : 4 = 3 — делится, значит 4 делитель',12,МУТ)}
          ${т(168,150,'4 · 3 = 12 — значит 12 кратно 4',12,МУТ)}
          ${т(168,182,'одно и то же — с двух сторон',14,ИНК,true)}</g>
        <g>${проявить('12s',0.72,0.82)}
          ${т(168,214,'4 делитель 12 ⇔ 12 кратно 4',16,GOLD,true)}</g>
      `,238)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='оба'?'hit':(в?'miss':''), 'Это одно и то же утверждение', "r993Link('оба')")}
        ${BTN(4, в==='разное'?'miss':'', 'Это разные независимые понятия', "r993Link('разное')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='оба',
        в==='оба' ? 'Верно: «4 — делитель 12» и «12 кратно 4» говорят одно и то же, только с разных сторон.'
                  : 'Нет: если 12 делится на 4 без остатка, то оба утверждения верны одновременно.') : ЖДЁТ()) +
      ПРАВИЛО('<b>a — делитель b</b> означает то же, что <b>b кратно a</b>.');
  }

  /* 6. Делители 24: восемь штук */
  function F6(s){
    const в = s.делители24;
    const делители=[1,2,3,4,6,8,12,24];
    return ВОПРОС('Сколько делителей у числа 24?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Делители 24',14,GOLD,true)}
        ${делители.map((д,i)=>`<g>${светит('10s',i*0.4)}
          ${фишка(12+i*39,40,String(д),GOLD,14,0)}</g>`).join('')}
        <g>${проявить('12s',0.3,0.4)}
          ${т(168,90,'пары: 1·24, 2·12, 3·8, 4·6',14,ИНК,true)}
          ${стол(18,214,300)}
          ${выкладка(24,108,24,2,'плитка2',0.34,'2 · 12')}
          ${выкладка(118,108,24,3,'плитка3',0.42,'3 · 8')}
          ${выкладка(212,108,24,4,'плитка',0.5,'4 · 6')}</g>
        <g>${проявить('12s',0.72,0.82)}
          ${т(168,238,'четыре пары дают восемь делителей',14,GREEN,true)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='восемь'?'hit':(в?'miss':''), '8 делителей: 1, 2, 3, 4, 6, 8, 12, 24', "r993Div24('восемь')")}
        ${BTN(4, в==='четыре'?'miss':'', '4 делителя: по числу пар', "r993Div24('четыре')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='восемь',
        в==='восемь' ? 'Верно: пары 1·24, 2·12, 3·8, 4·6 дают восемь делителей.'
                     : 'Нет: пар четыре, но в каждой паре два числа, поэтому делителей восемь.') : ЖДЁТ()) +
      ПРАВИЛО('Пар четыре, а делителей <b>восемь</b>: в паре два числа.');
  }

  /* 7. Общие делители 18 и 24 */
  function F7(s){
    const в = s.общие;
    const д18=[1,2,3,6,9,18], д24=[1,2,3,4,6,8,12,24], общие=[1,2,3,6];
    return ВОПРОС('Какие делители общие у чисел 18 и 24?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Общие делители двух чисел',14,GOLD,true)}
        ${т(14,56,'18:',12,МУТ,true,'start')}
        ${д18.map((д,i)=>`<g>${общие.includes(д)?светит('10s',i*0.35):проявить('12s',0.04*i,0.04*i+0.05)}
          <rect x="${34+i*50}" y="38" width="42" height="26" rx="13" fill="rgba(255,255,255,.06)"
            stroke="${общие.includes(д)?GREEN:ЛИНИЯ}" stroke-width="1.5"/>
          ${т(55+i*50,56,String(д),14,общие.includes(д)?GREEN:ИНК,true)}</g>`).join('')}
        ${т(14,106,'24:',12,МУТ,true,'start')}
        ${д24.map((д,i)=>`<g>${общие.includes(д)?светит('10s',i*0.35+0.15):проявить('12s',0.04*i,0.04*i+0.05)}
          <rect x="${34+i*36}" y="88" width="32" height="26" rx="13" fill="rgba(255,255,255,.06)"
            stroke="${общие.includes(д)?GREEN:ЛИНИЯ}" stroke-width="1.5"/>
          ${т(50+i*36,106,String(д),12,общие.includes(д)?GREEN:ИНК,true)}</g>`).join('')}
        <g>${проявить('12s',0.5,0.6)}
          ${бумага(20,144,296,44,0.5,-0.4)}
          ${т(168,166,'общие: 1, 2, 3, 6',16,'#2b2110',true)}
          ${т(168,184,'самый большой из них — 6',12,'#7a6a44')}</g>
        <g>${проявить('12s',0.74,0.84)}
          ${т(168,220,'общий делитель делит оба числа сразу',12,ИНК)}
          ${т(168,242,'единица есть у всех чисел',12,МУТ)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), '6 — наибольший общий делитель', "r993Common('шесть')")}
        ${BTN(4, в==='девять'?'miss':'', '9 — делитель 18', "r993Common('девять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: общие делители 1, 2, 3 и 6. Число 9 делит 18, но не делит 24, поэтому общим не является.'
                    : 'Нет: 9 делит 18, но 24 на 9 не делится. Общие делители тут 1, 2, 3, 6.') : ЖДЁТ()) +
      ПРАВИЛО('Общий делитель делит <b>оба числа сразу</b>: 1, 2, 3, 6.');
  }

  /* 8. Общие кратные 12 и 18 */
  function F8(s){
    const в = s.общиеКратные;
    const шаг=36, x0=42;
    return ВОПРОС('Где впервые встретятся кратные чисел 12 и 18?') +
      `<div class="pic">${свг(`
        ${фон(254)}
        ${т(168,24,'Общие кратные 12 и 18',14,GOLD,true)}
        ${[12,18].map((число,i)=>{
          const y=90+i*54;
          return `<g>
            <line x1="${x0}" y1="${y}" x2="${x0+6*шаг}" y2="${y}" stroke="${ЛИНИЯ}" stroke-width="2"/>
            ${Array.from({length:7},(_,k)=>`<g>
              <line x1="${x0+k*шаг}" y1="${y-6}" x2="${x0+k*шаг}" y2="${y+6}" stroke="${ЛИНИЯ}" stroke-width="1.5"/>
              ${т(x0+k*шаг,y-14,String(число*k),12,i?BLUE:GOLD,true)}</g>`).join('')}
            <g>${анД('M0 0 L'+(5*шаг)+' 0','9s')}
              <circle cx="${x0}" cy="${y}" r="6" fill="${i?BLUE:GOLD}" stroke="${ОБВОД}" stroke-width="1.2"/></g>
          </g>`;
        }).join('')}
        <g>${проявить('12s',0.5,0.6)}
          ${т(168,192,'кратные 12: 12, 24, 36, 48, 60',12,МУТ)}
          ${т(168,212,'кратные 18: 18, 36, 54, 72',12,МУТ)}
          ${т(168,240,'первое общее — 36',16,GREEN,true)}</g>
      `,254)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тридцать шесть'?'hit':(в?'miss':''), '36: первое общее кратное', "r993Both('тридцать шесть')")}
        ${BTN(4, в==='двести шестнадцать'?'miss':'', '216: произведение 12 · 18', "r993Both('двести шестнадцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тридцать шесть',
        в==='тридцать шесть' ? 'Верно: 36 делится и на 12, и на 18. Это наименьшее общее кратное.'
                             : 'Нет: 216 тоже общее кратное, но не первое. Раньше всего встречается 36.') : ЖДЁТ()) +
      ПРАВИЛО('Наименьшее общее кратное 12 и 18 — <b>36</b>.');
  }

  /* 9. Ловушка: конечно и бесконечно */
  function F9(s){
    const в = s.ловушка;
    return ВОПРОС('Чего у числа больше: делителей или кратных?') +
      `<div class="pic">${свг(`
        ${фон(242)}
        ${т(168,24,'Делители кончаются, кратные — нет',14,GOLD,true)}
        ${стекло(20,44,140,74,0.06)}
        ${т(90,74,'делители 12',12,МУТ)}
        ${т(90,102,'1, 2, 3, 4, 6, 12',12,ИНК,true)}
        ${стекло(176,44,140,74,0.2)}
        ${т(246,74,'кратные 12',12,МУТ)}
        ${т(246,102,'12, 24, 36, 48 …',12,GOLD,true)}
        <g>${проявить('12s',0.34,0.44)}
          ${т(168,146,'делителей у числа конечное число',14,ИНК,true)}
          ${т(168,174,'а кратных — бесконечно много',14,GOLD,true)}</g>
        <g>${проявить('12s',0.6,0.7)}
          ${бумага(24,196,288,36,0.6,-0.4)}
          ${т(168,212,'любое число делится на 1 и на себя',12,'#2b2110',true)}
          ${т(168,228,'делитель не больше самого числа',12,'#7a6a44')}</g>
      `,242)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='кратных'?'hit':(в?'miss':''), 'Кратных: их бесконечно много', "r993Rest('кратных')")}
        ${BTN(4, в==='делителей'?'miss':'', 'Делителей: их тоже бесконечно', "r993Rest('делителей')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='кратных',
        в==='кратных' ? 'Верно: делители ограничены самим числом (у 12 их шесть), а кратные не кончаются никогда.'
                      : 'Нет: делителей конечное число — все они не больше самого числа. А кратные бесконечны.') : ЖДЁТ()) +
      ПРАВИЛО('Делителей — <b>конечно</b>, кратных — <b>бесконечно</b>.');
  }

  /* 10. Порядок действий */
  function F10(s){
    const в = s.сводка;
    return ВОПРОС('Как проверить, что число 8 — делитель 24?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Как искать делители',14,GOLD,true)}
        ${бумага(14,36,308,192,0.05,0)}
        ${[['1','перебираем пары','1·24, 2·12, 3·8, 4·6'],
           ['2','останавливаемся','дальше пары повторяются'],
           ['3','проверяем делением','24 : 8 = 3 без остатка']].map(([н,что,как],i)=>{
          const y=50+i*58;
          return `<g>
            <circle cx="40" cy="${y+22}" r="13" fill="rgba(255,215,106,.18)" stroke="${GOLD}" stroke-width="1.6">
              ${проявить('12s',0.06+i*0.1,0.14+i*0.1)}</circle>
            ${т(40,y+27,н,14,GOLD,true)}
            <g>${проявить('12s',0.08+i*0.1,0.16+i*0.1)}
              ${т(190,y+18,что,14,'#2b2110',true)}
              ${т(190,y+40,как,12,'#7a6a44')}</g>
          </g>`;
        }).join('')}
        <g>${проявить('12s',0.68,0.78)}
          ${т(168,246,'делитель · частное = само число',12,GREEN,true)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='делением'?'hit':(в?'miss':''), 'Разделить 24 на 8 и посмотреть на остаток', "r993Свод('делением')")}
        ${BTN(4, в==='сравнить'?'miss':'', 'Сравнить числа: 8 меньше 24', "r993Свод('сравнить')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='делением',
        в==='делением' ? 'Верно: 24 : 8 = 3, остатка нет — значит 8 делитель 24. Проверка всегда одна: деление.'
                       : 'Нет: то, что 8 меньше 24, ничего не доказывает. Нужно разделить и проверить остаток.') : ЖДЁТ()) +
      ПРАВИЛО('Проверка одна: <b>делим и смотрим остаток</b>.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Сколько делителей у числа 12?', варианты:[
        {т:'6: 1, 2, 3, 4, 6, 12', ок:true,  почему:'Три пары по два числа.'},
        {т:'3: по числу пар', ок:false, почему:'Пар три, но в каждой паре два делителя.'}]},
    { вопрос:'Какое из чисел кратно 12?', варианты:[
        {т:'36', ок:true,  почему:'36 = 12 · 3.'},
        {т:'18', ок:false, почему:'18 на 12 не делится: 18 = 12 + 6.'}]},
    { вопрос:'Сколько делителей у числа 24?', варианты:[
        {т:'8', ок:true,  почему:'1, 2, 3, 4, 6, 8, 12, 24.'},
        {т:'4', ок:false, почему:'Четыре пары, а делителей восемь.'}]},
    { вопрос:'Наибольший общий делитель 18 и 24?', варианты:[
        {т:'6', ок:true,  почему:'Общие: 1, 2, 3, 6.'},
        {т:'9', ок:false, почему:'9 не делит 24.'}]},
    { вопрос:'Наименьшее общее кратное 12 и 18?', варианты:[
        {т:'36', ок:true,  почему:'36 делится и на 12, и на 18.'},
        {т:'216', ок:false, почему:'Это произведение, но общее кратное есть меньше.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Две опоры: делители ищем парами, а кратное проверяем делением без остатка.') +
        `<div class="ask">${BTN(4,'','пройти заново','r993Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r993Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Делители — <b>парами</b>, кратное — <b>умножением</b>.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · делители',
    задания:[
      { ф:'Сколько делителей у 12?', о:['6','3','12'], в:0, р:'1, 2, 3, 4, 6, 12.' },
      { ф:'Сколько делителей у 24?', о:['8','4','6'], в:0, р:'Четыре пары по два числа.' },
      { ф:'Является ли 8 делителем 24?', о:['да','нет'], в:0, р:'24 : 8 = 3 без остатка.' },
      { ф:'Является ли 9 делителем 24?', о:['нет','да'], в:0, р:'24 : 9 = 2 и остаток 6.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · кратные',
    задания:[
      { ф:'Кратно ли 36 числу 12?', о:['да','нет'], в:0, р:'36 = 12 · 3.' },
      { ф:'Кратно ли 18 числу 12?', о:['нет','да'], в:0, р:'18 = 12 + 6, остаток есть.' },
      { ф:'Первое общее кратное 12 и 18?', о:['36','216','72'], в:0, р:'36 делится на оба.' },
      { ф:'Сколько кратных у числа 12?', о:['бесконечно много','шесть'], в:0, р:'12 · 1, 12 · 2, 12 · 3 …' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · общие и ловушки',
    задания:[
      { ф:'Наибольший общий делитель 18 и 24?', о:['6','9','12'], в:0, р:'Общие: 1, 2, 3, 6.' },
      { ф:'Чего у числа больше?', о:['кратных','делителей'], в:0, р:'Кратных бесконечно много.' },
      { ф:'Верно ли: «4 — делитель 12»?', о:['да, и 12 кратно 4','нет'], в:0, р:'Это одно и то же утверждение.' },
      { ф:'Сколько делителей у числа 7?', о:['2: 1 и 7','7','1'], в:0, р:'Простое число: только 1 и само число.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r993T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r993TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L993={
    id: ID, title:'Делители и кратные', ico:'🧱',
    src:'Математика · 5 класс · Делимость', subj:'math',
    explain: [
      'В плиточной мастерской есть 12 одинаковых плиток. Из них выкладывают прямоугольник без пропусков. Оказалось, что способов ровно три: в один ряд, в два ряда по шесть и в три ряда по четыре. Стороны этих прямоугольников — делители числа 12. Так делимость видно глазами: если число раскладывается на равные ряды, то число рядов и есть делитель.',

      'Делителем числа 12 называют такое число, на которое 12 делится без остатка. Проверим по порядку: 12 : 1 = 12, 12 : 2 = 6, 12 : 3 = 4, 12 : 4 = 3, 12 : 6 = 2, 12 : 12 = 1. Все шесть делятся без остатка, значит у числа 12 ровно шесть делителей: 1, 2, 3, 4, 6 и 12. Частая ошибка — забыть единицу или само число. Как проверить себя: любое число делится на 1 и на себя.',

      'Чтобы не проверять все числа подряд, делители ищут парами. Если 1 · 12 = 12, то 1 и 12 — делители. Дальше 2 · 6 = 12, потом 3 · 4 = 12. Как только пары начали повторяться, перебор можно прекратить: дальше пойдут те же числа в другом порядке. Частая ошибка — продолжать перебор до самого числа и терять время. Как проверить себя: перемножь пару — должно получиться само число.',

      'Теперь второе слово. Кратными числа 12 называют числа, которые получаются умножением 12 на 1, 2, 3 и так далее: 12, 24, 36, 48, 60 и дальше без конца. Кратные удобно отмечать шагами по числовому лучу: каждый следующий шаг длиной 12. Частая ошибка — считать кратным число, которое меньше 12: самое маленькое кратное — это само число. Как проверить себя: раздели число на 12 и посмотри, есть ли остаток.',

      'Слова «делитель» и «кратное» описывают одно и то же отношение, только с разных сторон. Если 12 делится на 4 без остатка, то 4 — делитель числа 12, а 12 — кратное числа 4. Оба утверждения верны одновременно. Частая ошибка — считать это двумя разными правилами и учить их отдельно. Как проверить себя: скажи фразу в обе стороны.',

      'Разберём число 24. Ищем делители парами: 1 · 24, 2 · 12, 3 · 8, 4 · 6. Пары кончились, значит делители такие: 1, 2, 3, 4, 6, 8, 12, 24 — всего восемь. Обратите внимание: пар четыре, а делителей восемь, потому что в каждой паре два числа. Частая ошибка — назвать четыре делителя вместо восьми. Как проверить себя: пересчитай числа в паре.',

      'Бывает нужно найти делители сразу двух чисел. Возьмём 18 и 24. Делители 18: 1, 2, 3, 6, 9, 18. Делители 24: 1, 2, 3, 4, 6, 8, 12, 24. Общие у них 1, 2, 3 и 6. Заметьте: 9 делит 18, но не делит 24, поэтому общим делителем не является. Самый большой общий делитель — 6. Частая ошибка — брать делитель только одного числа. Как проверить себя: каждое общее число должно делить оба.',

      'Похожая задача про кратные. Возьмём кратные 12: 12, 24, 36, 48, 60. И кратные 18: 18, 36, 54, 72. Общие у них 36, 72 и дальше. Первое общее кратное — 36, оно делится и на 12, и на 18. Частая ошибка — считать общим кратным произведение 12 · 18 = 216: оно тоже подходит, но не первое. Как проверить себя: ищи самое маленькое.',

      'Важно помнить разницу в количестве. Делителей у числа конечное число: все они не больше самого числа, у 12 их шесть. А кратных бесконечно много: 12, 24, 36 и так далее без конца. Ещё два факта: единица — делитель любого числа, а само число — и свой делитель, и своё кратное. Частая ошибка — думать, что делителей тоже бесконечно много. Как проверить себя: вспомни, что делитель не больше числа.',

      'Соберём порядок действий. Первое: чтобы найти делители, перебираем пары, начиная с единицы, и останавливаемся, когда пары начали повторяться. Второе: чтобы проверить делитель, делим и смотрим остаток. Третье: чтобы получить кратные, умножаем число на 1, 2, 3 и далее. Проверка: делитель умножить на частное должно дать само число. Частая ошибка — путать, что искать: делители или кратные. Как проверить себя: спроси, делим мы или умножаем.',

      'Пройти практику: пять уровней про делители, кратные, общие делители и общие кратные. Дальше ждут три тренажёра.',

      'Первый тренажёр — делители: перебираем пары и проверяем делением.',

      'Второй тренажёр — кратные: умножаем число на 1, 2, 3 и ищем общие кратные.',

      'Третий тренажёр — общие делители и ловушки: помним, что делителей конечное число, а кратных бесконечно.'
    ],
    check: {
      q:'Сколько делителей у числа 12?',
      choices:['6','3','12'],
      ans:0,
      exp:'1, 2, 3, 4, 6, 12 — три пары, всего шесть делителей.'
    },
    tasks: [
      { q:'Какое из чисел кратно 12?', kind:'choice',
        choices:['36','18'], ans:0,
        hints:['36 = 12 · 3.','18 на 12 не делится.'],
        sol:'36.' },
      { q:'Сколько делителей у числа 24?', kind:'choice',
        choices:['8','4'], ans:0,
        hints:['Пары 1·24, 2·12, 3·8, 4·6.','В паре два числа.'],
        sol:'8.' },
      { q:'Наибольший общий делитель 18 и 24?', kind:'choice',
        choices:['6','9'], ans:0,
        hints:['Общие: 1, 2, 3, 6.','9 не делит 24.'],
        sol:'6.' },
      { q:'Наименьшее общее кратное 12 и 18?', kind:'choice',
        choices:['36','216'], ans:0,
        hints:['36 делится на оба числа.','216 — произведение, но не первое.'],
        sol:'36.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L993.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Мастерская','12 плиток и прямоугольники'],
      2:['Делители','Шесть чисел для 12'],
      3:['Пары','Перебор до середины'],
      4:['Кратные','Шаги по 12'],
      5:['Два слова','Делитель и кратное'],
      6:['Число 24','Восемь делителей'],
      7:['Общие делители','18 и 24'],
      8:['Общие кратные','Первое — 36'],
      9:['Сколько их','Конечно и бесконечно'],
      10:['Порядок действий','Три шага'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','Делители'],
      13:['Тренажёр 2','Кратные'],
      14:['Тренажёр 3','Общие и ловушки']
    };
    const з=ЗАГОЛОВКИ[f]||['Делители и кратные','Делимость'];
    el.innerHTML = `<div class="s6 l993" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r993Shop=(к)=>{ S().мастерская=к; chRender(0); };
  window.r993Div=(к)=>{ S().делитель=к; chRender(0); };
  window.r993Pairs=(к)=>{ S().пары=к; chRender(0); };
  window.r993Mult=(к)=>{ S().кратное=к; chRender(0); };
  window.r993Link=(к)=>{ S().связь=к; chRender(0); };
  window.r993Div24=(к)=>{ S().делители24=к; chRender(0); };
  window.r993Common=(к)=>{ S().общие=к; chRender(0); };
  window.r993Both=(к)=>{ S().общиеКратные=к; chRender(0); };
  window.r993Rest=(к)=>{ S().ловушка=к; chRender(0); };
  window.r993Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r993Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r993Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r993T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r993TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L993; else arr.push(L993);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA993={render:render, L:L993};
})();
