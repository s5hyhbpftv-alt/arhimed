/* ============ РУССКИЙ ЯЗЫК · УРОК 894 · «ВИД ГЛАГОЛА» ============
   5 класс, «Морфология. Глагол». Продолжение линии 877–893.

   ФОРМАТ — КАК У РУССКИХ УРОКОВ: window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT (.kicker, h2, .lead, .cap, .steps/.st,
   .ex-line, .cases, .rail, .pair/.card/.tag/.row/.split, .verdict, .score).

   ИНТЕРАКТИВ: кадр 3 выбор вида по вопросу; кадр 6 видовые пары; кадр 9 времена
   глаголов разного вида; кадр 13 разбор по клику. Плюс тренажёр.

   ПРОВЕРЕННЫЕ СВЕДЕНИЯ (5 класс):
     несовершенный вид — что делать? что делает? что делал? что будет делать?
       (читать, писать, рисовать): действие длится, повторяется, без указания
       на завершение; имеет настоящее, прошедшее и будущее сложное время
       (читаю, читал, буду читать);
     совершенный вид — что сделать? что сделал? что сделает? (прочитать,
       написать, нарисовать): действие завершено, есть результат; имеет только
       прошедшее и будущее простое время (прочитал, прочитаю), НАСТОЯЩЕГО
       времени у него нет;
     вид — постоянный признак глагола;
     видовые пары: читать — прочитать, писать — написать, решать — решить;
     вид меняют приставки (писать — написать) и суффиксы (решать — решить). */
(function(){
  'use strict';

  const ID = 894;
  const R = window.RUKIT;
  if(!R){ return; }
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;
  const пр    = (t)=>`<p class="ex-line">${esc(t)}</p>`;
  const рельс = (стиль,...xs)=>`<div class="rail" style="${стиль||''}">${xs.join('')}</div>`;
  const двa    = (a,b)=>`<div class="cases">${a}${b}</div>`;
  const шаги  = (items)=>`<div class="steps">${items.map(([t,s],i)=>
    `<div class="st"><span class="n">${i+1}</span><span class="t">${esc(t)}<small>${esc(s)}</small></span></div>`).join('')}</div>`;
  const кноп  = (t,on)=>`<button type="button" class="chip" style="flex:1 1 40%;font-size:17px" onclick="${on}">${esc(t)}</button>`;
  const кнопки= (...xs)=>`<div class="row">${xs.join('')}</div>`;

  const ВИДЫ=[
    ['читать','несовершенный','Что делать? Действие длится или повторяется.'],
    ['прочитать','совершенный','Что сделать? Действие завершено, есть результат.'],
    ['писать','несовершенный','Что делать? Процесс.'],
    ['написать','совершенный','Что сделать? Результат.'],
    ['решать','несовершенный','Что делать?'],
    ['решить','совершенный','Что сделать?'],
    ['рисовать','несовершенный','Что делать?'],
    ['нарисовать','совершенный','Что сделать?']
  ];
  const ПАРЫ=[
    ['читать — прочитать','приставка про- меняет вид'],
    ['писать — написать','приставка на- меняет вид'],
    ['решать — решить','суффикс меняет вид'],
    ['строить — построить','приставка по- меняет вид'],
    ['мыть — вымыть','приставка вы- меняет вид']
  ];
  const ВРЕМЕНА=[
    ['читать (несов.)','настоящее: читаю · прошедшее: читал · будущее: буду читать'],
    ['прочитать (сов.)','прошедшее: прочитал · будущее: прочитаю · настоящего НЕТ'],
    ['рисовать (несов.)','рисую · рисовал · буду рисовать'],
    ['нарисовать (сов.)','нарисовал · нарисую · настоящего НЕТ'],
    ['решать (несов.)','решаю · решал · буду решать'],
    ['решить (сов.)','решил · решу · настоящего НЕТ']
  ];
  const РАЗБОР=[
    ['Мальчик читал книгу весь вечер.','читал — несовершенный вид','Действие длилось, результата завершения нет.'],
    ['Мальчик прочитал книгу.','прочитал — совершенный вид','Действие завершено, есть результат.'],
    ['Он будет рисовать портрет.','будет рисовать — несовершенный вид, будущее сложное','Несовершенный вид имеет будущее сложное время.'],
    ['Он нарисует портрет.','нарисует — совершенный вид, будущее простое','У совершенного вида будущее простое.'],
    ['Дети решают задачу.','решают — несовершенный вид, настоящее время','Настоящее время бывает только у несовершенного вида.'],
    ['Дети решили задачу.','решили — совершенный вид, прошедшее время','Результат достигнут.']
  ];

  const GAME=[
    ['«читать» — какой вид?','несовершенный','Что делать? Процесс.','совершенный'],
    ['«прочитать» — какой вид?','совершенный','Что сделать? Результат.','несовершенный'],
    ['Какой вопрос у совершенного вида?','что сделать?','Это вопрос совершенного вида.','что делать?'],
    ['Есть ли настоящее время у совершенного вида?','нет','У совершенного вида только прошедшее и будущее.','да'],
    ['«написать» — какой вид?','совершенный','Что сделать?','несовершенный'],
    ['«рисовать» — какой вид?','несовершенный','Что делать?','совершенный'],
    ['Какое время у глагола «буду читать»?','будущее сложное','Несовершенный вид, будущее сложное.','настоящее'],
    ['Какое время у глагола «прочитаю»?','будущее простое','Совершенный вид, будущее простое.','настоящее'],
    ['«решить» — какой вид?','совершенный','Что сделать?','несовершенный'],
    ['Вид глагола — признак постоянный или нет?','постоянный','Вид не меняется при изменении формы.','непостоянный']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    const кн = варианты.map(о=>кноп(о,`ru894Game('${esc(о)}')`)).join('');
    const ответ = готово ? (верно ? window.RUFEED.note('ok','верно',it[2]) : window.RUFEED.note('no','исправить',it[2]))
                         : `<p class="verdict">Сначала задай вопрос: что делать? или что сделать?</p>`;
    return `${kick('16 · Тренажёр')}
      ${word(it[0],'выбери ответ')}
      ${ответ}
      ${кнопки(кн)}
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее задание.':'Сначала вопрос, потом вид.')}`;
  }

  const подсказка_1 = cap('Вопрос решает всё: что делать? — несовершенный, что сделать? — совершенный.');

  window.RULESSON({id:ID, game:GAME, frames:[
    (st)=>{
      const ок = st.о1!=null;
      const отв = ок ? `<p class="verdict ok">«Читать» отвечает на вопрос что делать? — это несовершенный вид. «Прочитать» — что сделать? — совершенный.</p>`
                     : `<p class="verdict">Нажми кнопку — сравним два глагола.</p>`;
      return `${kick('01 · Разминка')}
        ${h2('Два глагола об одном, но по-разному')}
        ${lead('Мы разобрали прилагательное. Теперь возьмёмся за глагол и его постоянный признак — вид.')}
        ${двa(пр('Я читал книгу.'),пр('Я прочитал книгу.'))}
        ${отв}
        ${кнопки(кноп('показать разницу','ru894Разница()'))}
        ${pair(card('несовершенный','что делать? — читать'),card('совершенный','что сделать? — прочитать'))}
        ${подсказка_1}`;
    },

    ()=>`${kick('02 · Несовершенный вид')}
      ${h2('Что делать? Что делает? Что делал?')}
      ${lead('Глаголы несовершенного вида называют действие, которое длится, повторяется или просто происходит — без указания на завершение.')}
      ${cards([['читать','читаю, читал, буду читать'],['писать','пишу, писал, буду писать'],['рисовать','рисую, рисовал, буду рисовать'],['решать','решаю, решал, буду решать']])}
      ${пр('Такой глагол имеет три времени: настоящее, прошедшее и будущее сложное.')}
      ${cap('Будущее сложное — это «буду + неопределённая форма»: буду читать.')}`,

    (st)=>{
      const к = (st.о3||0);
      const п = ВИДЫ[к];
      const кн = ВИДЫ.map((х,i)=>кноп(х[0],`ru894Вид(${i})`)).join('');
      return `${kick('03 · Интерактив: определи вид')}
        ${h2('Нажми глагол — увидишь вид')}
        ${кнопки(кн)}
        ${пр('Выбрано: ' + п[0])}
        <p class="verdict ok">Это ${esc(п[1])} вид. ${esc(п[2])}</p>
        ${cards([['несовершенный','что делать?'],['совершенный','что сделать?']])}
        ${cap('Вопрос — самый быстрый способ определить вид.')}`;
    },

    ()=>`${kick('04 · Совершенный вид')}
      ${h2('Что сделать? Что сделал? Что сделает?')}
      ${lead('Глаголы совершенного вида называют действие завершённое, с результатом.')}
      ${cards([['прочитать','прочитал, прочитаю'],['написать','написал, напишу'],['нарисовать','нарисовал, нарисую'],['решить','решил, решу']])}
      ${пр('У совершенного вида только два времени: прошедшее и будущее простое.')}
      ${cap('Настоящего времени у совершенного вида нет — это главная примета.')}`,

    ()=>`${kick('05 · Времена и вид')}
      ${h2('Почему у совершенного вида нет настоящего')}
      ${lead('Настоящее время — это действие, которое происходит сейчас, то есть ещё не завершено. А совершенный вид как раз означает завершённость.')}
      ${двa(пр('читаю (сейчас, несов.)'),пр('прочитаю (потом, сов.) — не «сейчас»'))}
      ${cards([['несовершенный','настоящее, прошедшее, будущее сложное'],['совершенный','прошедшее, будущее простое'],['примета','нет настоящего времени']])}
      ${cap('Если у глагола есть настоящее время, вид точно несовершенный.')}`,

    (st)=>{
      const к = (st.о6||0);
      const п = ПАРЫ[к];
      const кн = ПАРЫ.map((х,i)=>кноп('пара '+(i+1),`ru894Пара(${i})`)).join('');
      return `${kick('06 · Интерактив: видовые пары')}
        ${h2('Нажми пару — увидишь, что меняет вид')}
        ${кнопки(кн)}
        ${word(п[0],'два вида одного действия')}
        <p class="verdict ok">${esc(п[1])}</p>
        ${cards([['приставка','писать — написать'],['суффикс','решать — решить'],['вид','постоянный признак']])}
        ${cap('Приставка и суффикс — главные «переключатели» вида.')}`;
    },

    ()=>`${kick('07 · Вид — постоянный признак')}
      ${h2('Он не меняется при изменении формы')}
      ${lead('Как бы мы ни меняли глагол — по временам, лицам, числам, — вид остаётся тем же.')}
      ${cards([['читать','читаю, читаешь, читал, буду читать — несов.'],['прочитать','прочитал, прочитаешь, прочитаю — сов.'],['вывод','вид не зависит от формы']])}
      ${пр('В разборе вид указывают сразу после части речи и начальной формы.')}
      ${cap('Постоянные признаки глагола: вид, спряжение. Непостоянные: время, лицо, число, род.')}`,

    ()=>`${kick('08 · Как определить вид')}
      ${h2('Порядок действий')}
      ${шаги([['1 поставь вопрос','что делать? или что сделать?'],['2 посмотри на приставку','приставка часто даёт совершенный вид'],['3 проверь время','есть ли настоящее время'],['4 назови вид','несовершенный или совершенный']])}
      ${пр('«Нарисует» → что сделать? → совершенный; настоящего времени нет.')}
      ${cap('Четыре шага — и вид определён.')}`,

    (st)=>{
      const к = (st.о9||0);
      const п = ВРЕМЕНА[к];
      const кн = ВРЕМЕНА.map((х,i)=>кноп('глагол '+(i+1),`ru894Время(${i})`)).join('');
      return `${kick('09 · Интерактив: времена глаголов')}
        ${h2('Нажми глагол — увидишь его времена')}
        ${кнопки(кн)}
        ${word(п[0],'времена')}
        <p class="verdict ok">${esc(п[1])}</p>
        ${cards([['несовершенный','три времени'],['совершенный','два времени'],['настоящее','только у несовершенного']])}
        ${cap('Сравни примеры: у совершенного вида настоящего нет.')}`;
    },

    ()=>`${kick('10 · Вид и смысл')}
      ${h2('Что меняется в тексте')}
      ${lead('Несовершенный вид рисует процесс, совершенный — результат. В рассказе они работают вместе.')}
      ${двa(пр('Мальчик решал задачу долго.'),пр('Мальчик решил задачу.'))}
      ${cards([['процесс','решать, читать, рисовать'],['результат','решить, прочитать, нарисовать'],['в тексте','процесс создаёт фон, результат двигает сюжет']])}
      ${cap('Понимание вида помогает точнее выражать мысль.')}`,

    ()=>`${kick('11 · Где ошибаются')}
      ${h2('Три частые ошибки')}
      ${cards([['«я прочитаю сейчас» ✗','совершенный вид не бывает в настоящем: «я читаю сейчас»'],['путают пары','читать — прочитать, а не «читать — читать»'],['считают вид непостоянным','вид — постоянный признак']])}
      ${пр('Проверь: есть ли у глагола настоящее время? Если есть — вид несовершенный.')}
      ${cap('Одна проверка снимает большинство сомнений.')}`,

    ()=>`${kick('12 · Вид и другие признаки')}
      ${h2('Как вид связан со временем и спряжением')}
      ${lead('Вид влияет на время: у совершенного вида нет настоящего. Спряжение же зависит от неопределённой формы и одинаково у обоих видов.')}
      ${cards([['вид','постоянный признак'],['время','зависит от вида'],['спряжение','зависит от -ить и исключений'],['лицо и число','зависят от формы']])}
      ${пр('О спряжении и личных окончаниях — в следующем уроке.')}
      ${cap('Признаки глагола связаны между собой.')}`,

    (st)=>{
      const к = (st.о13||0);
      const п = РАЗБОР[к];
      const кн = РАЗБОР.map((х,i)=>кноп('разбор '+(i+1),`ru894Разбор(${i})`)).join('');
      return `${kick('13 · Интерактив: разбор по клику')}
        ${h2('Нажми предложение — увидишь вид и время')}
        ${кнопки(кн)}
        ${пр(п[0])}
        ${cards([['разбор',п[1]],['почему',п[2]]])}
        ${cap('Сравни разборы: одна и та же мысль — разные виды.')}`;
    },

    ()=>`${kick('14 · Ловушка')}
      ${h2('Настоящее время у совершенного вида')}
      ${lead('Самая частая ошибка — поставить глагол совершенного вида в настоящее время. Такого времени у него нет.')}
      ${двa(пр('«Я прочитаю книгу сейчас» ✗ — сейчас: «я читаю»'),пр('«Я прочитаю книгу завтра» ✓ — будущее простое'))}
      ${cards([['несовершенный','читаю сейчас'],['совершенный','прочитаю потом'],['проверка','есть настоящее время?']])}
      ${cap('Форма «прочитаю» — будущее, а не настоящее.')}`,

    ()=>`${kick('15 · В карман')}
      ${h2('Три фразы, которые стоит унести')}
      ${cards([['несовершенный','что делать? — процесс: читать, решать'],['совершенный','что сделать? — результат: прочитать, решить'],['примета','у совершенного вида нет настоящего времени']])}
      ${рельс('flex-wrap:wrap',chip('читать',BLUE),chip('прочитать',GOLD),chip('решать',BLUE),chip('решить',GOLD))}
      ${cap('Вид — постоянный признак глагола.')}`,

    (st)=>тренер(st)
  ]});

  const L894={
    id:ID, title:'Вид глагола: совершенный и несовершенный', ico:'⏳',
    src:'Русский язык · 5 класс · Морфология', subj:'rus', group:null,
    explain:[
      'Вид — постоянный признак глагола. Видов два: несовершенный и совершенный.',
      'Несовершенный вид отвечает на вопросы что делать? что делает? что делал? что будет делать?: читать, писать, рисовать, решать. Действие длится, повторяется или просто происходит — без указания на завершение.',
      'Совершенный вид отвечает на вопросы что сделать? что сделал? что сделает?: прочитать, написать, нарисовать, решить. Действие завершено, есть результат.',
      'У несовершенного вида три времени: настоящее, прошедшее и будущее сложное (читаю, читал, буду читать).',
      'У совершенного вида только два времени: прошедшее и будущее простое (прочитал, прочитаю). Настоящего времени у него нет — это главная примета.',
      'Почему нет настоящего времени: настоящее — это действие, которое происходит сейчас, то есть не завершено, а совершенный вид означает завершённость.',
      'Видовые пары: читать — прочитать, писать — написать, решать — решить, строить — построить, мыть — вымыть.',
      'Вид меняют приставки (писать — написать) и суффиксы (решать — решить).',
      'Вид — постоянный признак: при изменении глагола по временам, лицам и числам он не меняется.',
      'Порядок определения вида: задать вопрос что делать? или что сделать?, посмотреть на приставку, проверить, есть ли настоящее время, назвать вид.',
      'Быстрая проверка: если у глагола есть настоящее время, вид несовершенный.',
      'В тексте несовершенный вид рисует процесс («Мальчик решал задачу долго»), совершенный — результат («Мальчик решил задачу»).',
      'Частая ошибка — поставить глагол совершенного вида в настоящее время: «я прочитаю сейчас» неверно, правильно «я читаю сейчас», а «прочитаю» — будущее.',
      'Вид влияет на время, но не на спряжение: спряжение определяют по неопределённой форме, и оно одинаково у обоих видов.',
      'В уроке есть интерактив: показать разницу, определить вид восьми глаголов, разобрать видовые пары, увидеть времена глаголов и разобрать предложение по клику.',
      'Тренажёр: десять заданий по кругу. Определи вид, время или ответь на вопрос о признаке.'
    ],
    tasks:[
      { q:'«читать» — какой вид?', kind:'choice',
        choices:['несовершенный','совершенный'], ans:0,
        hints:['Какой вопрос?','Что делать?'],
        sol:'Несовершенный.' },
      { q:'«прочитать» — какой вид?', kind:'choice',
        choices:['совершенный','несовершенный'], ans:0,
        hints:['Какой вопрос?','Что сделать?'],
        sol:'Совершенный.' },
      { q:'Есть ли настоящее время у глаголов совершенного вида?', kind:'choice',
        choices:['нет','да'], ans:0,
        hints:['Завершённость и «сейчас» несовместимы.','У него только прошедшее и будущее.'],
        sol:'Нет, настоящего времени нет.' },
      { q:'Вид глагола — признак постоянный или непостоянный?', kind:'choice',
        choices:['постоянный','непостоянный'], ans:0,
        hints:['Меняется ли он при изменении формы?','Читаю, читал — вид тот же.'],
        sol:'Постоянный.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L894; else arr.push(L894); }
  }catch(e){}

  const С = ()=>{
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const перерисовать = ()=>{ if(typeof chRender==='function') chRender(0); };

  window.ru894Разница=function(){ const s=С(); s.о1=1; перерисовать(); };
  window.ru894Вид=function(i){ const s=С(); s.о3=i; перерисовать(); };
  window.ru894Пара=function(i){ const s=С(); s.о6=i; перерисовать(); };
  window.ru894Время=function(i){ const s=С(); s.о9=i; перерисовать(); };
  window.ru894Разбор=function(i){ const s=С(); s.о13=i; перерисовать(); };
  window.ru894Game=function(ответ){
    const st=С();
    if(st.gRes!=null){ st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null; }
    else{ const it=GAME[(st.gIdx||0)%GAME.length]; st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1; }
    перерисовать();
  };
})();
