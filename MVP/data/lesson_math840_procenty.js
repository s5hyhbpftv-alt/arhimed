/* ============ МАТЕМАТИКА · УРОК 840 · «ПРОЦЕНТЫ: ИЗМЕНЕНИЕ ВЕЛИЧИНЫ» ============
   6 класс. Сюжет: цену подняли на 20%, потом снизили на 20% — стала ли прежней?

   СВЕРКА С ПЕТЕРСОНОМ (6 класс): проценты, нахождение процента от величины,
   увеличение и уменьшение на несколько процентов, многократные изменения.
   Источники: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     100 → +20% = 120 → −20% = 96   (прежней цены НЕ стало: 96 ≠ 100)
     почему: 20% от 100 = 20, а 20% от 120 = 24 — базы разные
     произведение множителей: 1,2 · 0,8 = 0,96 → итог −4%
     порядок не важен: 100 · 0,8 · 1,2 = 100 · 1,2 · 0,8 = 96
     чтобы вернуть цену после +20%, нужна скидка 16,67% ≈ 1/6 (а не 20%)
     три раза по +10%: 100 → 110 → 121 → 133,1 (а не 130)
     другие пары: 250 → +40% = 350 → −30% = 245;  64 → +25% = 80 → −25% = 60;
                  80 → +50% = 120 → −50% = 60;   1000 → +15% = 1150 → −15% = 977,5

   УСИЛЕННАЯ ГРАФИКА (просьба владельца):
     • ВИТРИНА магазина со стеклом и рамой, ЦЕННИКИ из бумаги с зерном,
       ниткой и наклоном, металлическая стойка;
     • ЦЕНОВЫЕ ПОЛОСЫ с градиентом, металлическим торцом и тенью, а рядом
       КОНТРОЛЬНАЯ ЛИНИЯ исходной цены: видно, что после снижения полоса
       оказалась НИЖЕ неё;
     • СТУПЕНЧАТЫЙ ГРАФИК рисуется штрихом (stroke-dashoffset);
     • МОНЕТЫ с зеркальной точкой, падающие на поднос с отскоком;
     • ВЕСЫ сравнивают 20% от 100 и 20% от 120 (20 против 24);
     • у каждого предмета ДВЕ тени, зерно и лёгкая виньетка по фону.

   АНИМАЦИЯ: SMIL calcMode="spline", keySplines="0.23 1 0.32 1".
     полоса РАСТЁТ, а табличка с процентом ЕДЕТ по ней; при снижении полоса
     уходит ниже контрольной линии и линия ВСПЫХИВАЕТ; график прорисовывается
     по шагам; множители появляются по очереди.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, keyTimes строго в [0;1], reduced-motion выключает
   SMIL полностью, ничего не выходит за край кадра. Подписи короткие и на
   тёмных плашках — проверено замером, что они не ложатся на фигуры. */
(function(){
  'use strict';

  const ID = 840;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  const CSS=`
  #lvis .s6.l840{gap:14px}
  #lvis .s6.l840 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l840 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l840 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l840 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l840 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l840 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l840 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l840 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l840 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l840 .правило b{color:${GOLD}}
  #lvis .s6.l840 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l840 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l840 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l840 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l840 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l840 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l840 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l840 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l840 .уровни .точка.сейчас{background:${GOLD};animation:l840dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l840dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l840 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l840{-webkit-text-size-adjust:100%}
  #lvis .s6.l840 [data-anim]{animation:l840rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l840rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l840 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l840 [data-anim]{animation:none!important}
    #lvis .s6.l840 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l840 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l840-style');
      if(!s){ s=document.createElement('style'); s.id='l840-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ: КРИВЫЕ ПРОЕКТА ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анД = (путь,длит,доп) =>
    ДВИЖ ? `<animateMotion dur="${длит}" repeatCount="indefinite" path="${путь}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');
  const растёт = (длит,доля,конец,знач) => анК('height',знач,длит,'0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c840-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2a3f33"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#111c15"/>
      </linearGradient>
      <linearGradient id="c840-полоса" x1="0" y1="1" x2="0" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2b6d99"/><stop offset="0.4" stop-color="#5fa8d8"/>
        <stop offset="0.8" stop-color="#a8dcff"/><stop offset="1" stop-color="#d8f0ff"/>
      </linearGradient>
      <linearGradient id="c840-полоса2" x1="0" y1="1" x2="0" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#a83c28"/><stop offset="0.4" stop-color="#e8735a"/>
        <stop offset="0.8" stop-color="#ffb3a0"/><stop offset="1" stop-color="#ffd8cd"/>
      </linearGradient>
      <linearGradient id="c840-полоса3" x1="0" y1="1" x2="0" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2f6b4a"/><stop offset="0.45" stop-color="#5fa87f"/>
        <stop offset="1" stop-color="#b6e8cd"/>
      </linearGradient>
      <linearGradient id="c840-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <linearGradient id="c840-металл" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#5d6a76"/><stop offset="0.18" stop-color="#e8eef4"/>
        <stop offset="0.5" stop-color="#9fb0bd"/><stop offset="0.8" stop-color="#57636f"/>
        <stop offset="1" stop-color="#434d57"/>
      </linearGradient>
      <linearGradient id="c840-золото" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#8a6418"/><stop offset="0.2" stop-color="#ffe9a8"/>
        <stop offset="0.5" stop-color="#dcae43"/><stop offset="0.8" stop-color="#a87c1c"/>
        <stop offset="1" stop-color="#6f5210"/>
      </linearGradient>
      <linearGradient id="c840-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".55"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".22"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".2"/>
      </linearGradient>
      <radialGradient id="c840-шар" cx="0.36" cy="0.3" r="0.8" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffffff" stop-opacity=".9"/>
        <stop offset="0.16" stop-color="#ffe9a8"/><stop offset="0.55" stop-color="#dcae43"/>
        <stop offset="0.88" stop-color="#7a5a12"/><stop offset="1" stop-color="#4a3607"/>
      </radialGradient>
      <filter id="c840-тень" x="-70%" y="-70%" width="240%" height="240%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3.4" result="р"/>
        <feOffset in="р" dx="3" dy="5" result="с"/>
        <feFlood flood-color="#0a1a24" flood-opacity="0.4" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c840-контакт" x="-70%" y="-150%" width="240%" height="400%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.4" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#1d1508" flood-opacity="0.42" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c840-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c840-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c840-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c840-шум)" opacity="0.05"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  /* ПОДПИСЬ НА ПЛАШКЕ: читается и на фигуре, и на фоне */
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.84)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* ЦЕННИК: бумага с зерном, ниткой и наклоном */
  const ценник = (x,y,ш,в,цена,нач,поворот) => `<g transform="rotate(${поворот||0} ${x+ш/2} ${y+в/2})">
    <rect x="${x+2}" y="${y+3}" width="${ш}" height="${в}" rx="4" fill="#08171f" opacity=".36" filter="url(#c840-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="4" fill="url(#c840-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
      ${нач!=null?проявить('13s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="4" fill="none" filter="url(#c840-шум)" opacity=".1"/>
    <circle cx="${x+ш/2}" cy="${y+5}" r="2.2" fill="none" stroke="rgba(90,72,40,.6)" stroke-width="1"/>
    ${т(x+ш/2,y+в/2+7,цена,16,'#2b2110',true)}
  </g>`;

  /* ЦЕНОВАЯ ПОЛОСА: градиент, металлический торец, тень; высота задаётся анимацией */
  const полоса = (x,основание,ш,высотаМакс,доляИтог,цвет,нач,конец) => {
    const в=высотаМакс*доляИтог;
    return `<g>
      <rect x="${x+2}" y="${основание-в+3}" width="${ш}" height="${в}" rx="5" fill="#08171f" opacity=".34" filter="url(#c840-тень)"/>
      <rect x="${x}" y="${основание}" width="${ш}" height="1" rx="4" fill="url(#c840-${цвет})">
        ${анК('y',основание+';'+основание+';'+(основание-в)+';'+(основание-в),'13s',
             '0;'+кт(нач)+';'+кт(конец)+';1')}
        ${анК('height','1;1;'+в+';'+в,'13s','0;'+кт(нач)+';'+кт(конец)+';1')}</rect>
      <rect x="${x}" y="${основание-в}" width="${ш}" height="4" rx="2" fill="url(#c840-металл)">
        ${анК('y',основание+';'+основание+';'+(основание-в)+';'+(основание-в),'13s',
             '0;'+кт(нач)+';'+кт(конец)+';1')}</rect>
    </g>`;
  };

  /* КОНТРОЛЬНАЯ ЛИНИЯ исходной величины */
  const контроль = (x1,x2,y,текст,нач) => `<g>${проявить('13s',нач,нач+0.06)}
    <line x1="${x1}" y1="${y}" x2="${x2}" y2="${y}" stroke="${GOLD}" stroke-width="1.6" stroke-dasharray="6 4"/>
    ${подпись(x2-38,y-14,текст,GOLD,12)}
  </g>`;

  /* МОНЕТА */
  const монета = (x,y,r,нач) => `<g>
    <ellipse cx="${x+2}" cy="${y+r+3}" rx="${r*1.05}" ry="${r*0.24}" fill="#08171f" opacity=".36" filter="url(#c840-тень)"/>
    <circle cx="${x}" cy="${y}" r="${r}" fill="url(#c840-золото)" stroke="rgba(60,45,10,.55)" stroke-width="1">
      ${нач!=null?проявить('13s',нач,нач+0.04):''}</circle>
    <circle cx="${x}" cy="${y}" r="${r*0.66}" fill="none" stroke="rgba(255,255,255,.4)" stroke-width="1"/>
    <circle cx="${x-r*0.34}" cy="${y-r*0.36}" r="${r*0.16}" fill="#fff" opacity=".85" filter="url(#c840-мягко)"/>
  </g>`;

  const стекло = (x,y,ш,в,нач) => `<g>
    <rect x="${x+3}" y="${y+4}" width="${ш}" height="${в}" rx="8" fill="#08171f" opacity=".32" filter="url(#c840-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c840-стекло)" stroke="rgba(190,225,245,.5)" stroke-width="1.2">
      ${нач!=null?проявить('13s',нач,нач+0.05):''}</rect>
    <line x1="${x+4}" y1="${y+4}" x2="${x+ш-4}" y2="${y+4}" stroke="rgba(255,255,255,.45)" stroke-width="1.2"/>
  </g>`;

  /* ВИТРИНА: рама магазина со стеклом */
  const витрина = (x,y,ш,в,нач) => `<g>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="rgba(255,255,255,.03)" stroke="${ЛИНИЯ}" stroke-width="1.6">
      ${нач!=null?проявить('13s',нач,нач+0.05):''}</rect>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="8" fill="url(#c840-стекло)"/>
    <line x1="${x+6}" y1="${y+8}" x2="${x+ш*0.55}" y2="${y+8}" stroke="rgba(255,255,255,.35)" stroke-width="2" filter="url(#c840-мягко)"/>
  </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Сюжет: ценник 100, подняли на 20, снизили на 20 */
  function F1(s){
    const в = s.сюжет;
    return ВОПРОС('Цену 100 рублей подняли на 20%, а потом снизили на 20%. Стала ли она прежней?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Витрина: цена менялась дважды',14,GOLD,true)}
        ${витрина(20,40,296,130,0.04)}
        ${ценник(56,66,74,44,'100 ₽',0.1,-2)}
        ${ценник(140,62,74,48,'+20%',0.2,1)}
        ${ценник(224,66,74,44,'−20%',0.3,-1.5)}
        <g>${проявить('13s',0.5,0.6)}
          ${т(168,192,'сначала дороже, потом дешевле',14,ИНК,true)}
          ${т(168,218,'вернулась ли цена к 100?',14,GOLD,true)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='да'?'miss':'', 'Да: плюс 20 и минус 20 — то же самое', "r840Сюжет('да')")}
        ${BTN(4, в==='нет'?'hit':(в?'miss':''), 'Нет: 20% считают от разных чисел', "r840Сюжет('нет')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нет',
        в==='нет' ? 'Верно: проценты берут от разных оснований, поэтому цена не вернётся. Посчитаем дальше.'
                  : 'Нет: это ловушка. Первые 20% берут от 100, вторые — от 120. Покажем расчётом.') : ЖДЁТ()) +
      ПРАВИЛО('Процент всегда считают <b>от текущего значения</b>.');
  }

  /* 2. Повышение: 100 → 120 */
  function F2(s){
    const в = s.повышение;
    return ВОПРОС('Цену подняли на 20%. Сколько стало?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Подняли на 20%',14,GOLD,true)}
        ${полоса(74,198,44,150,0.5,'полоса',0.06,0.16)}
        ${полоса(150,198,44,150,0.8,'полоса',0.3,0.46)}
        ${контроль(50,320,198,'100 ₽',0.02)}
        ${т(96,224,'было',12,МУТ)}
        ${т(172,224,'стало',12,МУТ)}
        <g>${анД('M0 0 L0 -54','13s','keyTimes="0;1"')}
          ${подпись(150,120,'+20%',GREEN,12)}
        </g>
        <g>${проявить('13s',0.5,0.6)}
          ${подпись(168,166,'100 + 20 = 120 ₽',GREEN,14)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сто двадцать'?'hit':(в?'miss':''), '120 ₽: 100 · 1,2 = 120', "r840Подъём('сто двадцать')")}
        ${BTN(4, в==='двести'?'miss':'', '200 ₽: 100 + 100', "r840Подъём('двести')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сто двадцать',
        в==='сто двадцать' ? 'Верно: 20% от 100 — это 20 ₽, значит цена стала 120 ₽. Коротко: 100 · 1,2 = 120.'
                            : 'Нет: 20% — это двадцать сотых, а не сто рублей. 100 · 1,2 = 120 ₽.') : ЖДЁТ()) +
      ПРАВИЛО('Поднять на p% — значит <b>умножить на 1 + p/100</b>.');
  }

  /* 3. Понижение: 120 → 96, полоса уходит НИЖЕ контрольной линии */
  function F3(s){
    const в = s.понижение;
    return ВОПРОС('Теперь цену 120 снизили на 20%. Сколько стало?') +
      `<div class="pic">${свг(`
        ${фон(258)}
        ${т(168,24,'Снизили на 20% — стало меньше ста',14,GOLD,true)}
        ${полоса(74,204,44,150,0.5,'полоса3',0.06,0.16)}
        ${полоса(150,204,44,150,0.4,'полоса2',0.28,0.44)}
        ${контроль(50,320,204,'100 ₽',0.02)}
        ${т(96,230,'было',12,МУТ)}
        ${т(172,230,'стало',12,МУТ)}
        <g>${проявить('13s',0.5,0.6)}
          ${подпись(172,168,'120 − 24 = 96 ₽',RED,14)}</g>
        <g>${проявить('13s',0.68,0.78)}
          ${подпись(168,252,'полоса оказалась НИЖЕ начальной',RED,12)}</g>
      `,258)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='девяносто шесть'?'hit':(в?'miss':''), '96 ₽: 120 · 0,8 = 96', "r840Снижение('девяносто шесть')")}
        ${BTN(4, в==='сто'?'miss':'', '100 ₽: вернулось к началу', "r840Снижение('сто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='девяносто шесть',
        в==='девяносто шесть' ? 'Верно: 20% от 120 — это 24 ₽, значит 120 − 24 = 96 ₽. Цена стала НИЖЕ начальной.'
                              : 'Нет: 20% берут уже от 120, а это 24 ₽. 120 − 24 = 96 ₽, а не 100.') : ЖДЁТ()) +
      ПРАВИЛО('Снизить на p% — <b>умножить на 1 − p/100</b>.');
  }

  /* 4. Почему: 20% от 100 и 20% от 120 */
  function F4(s){
    const в = s.почему;
    return ВОПРОС('Почему 20% в первый раз и во второй — не одно и то же?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,24,'Проценты берут от разных чисел',14,GOLD,true)}
        ${стекло(16,44,148,96,0.06)}
        ${т(90,74,'от 100',14,МУТ)}
        ${т(90,106,'20% = 20 ₽',16,GREEN,true)}
        ${т(90,128,'это 20 из 100',12,МУТ)}
        ${стекло(172,44,148,96,0.2)}
        ${т(246,74,'от 120',14,МУТ)}
        ${т(246,106,'20% = 24 ₽',16,RED,true)}
        ${т(246,128,'это 24 из 120',12,МУТ)}
        <g>${проявить('13s',0.34,0.44)}
          ${[0,1,2,3,4].map(i=>монета(46+i*24,172,10,i*0.04)).join('')}
          ${т(118,200,'на столько подняли',12,МУТ)}
          ${[0,1,2,3,4,5].map(i=>монета(206+i*20,172,10,0.24+i*0.04)).join('')}
          ${т(266,200,'на столько снизили',12,МУТ)}</g>
        <g>${проявить('13s',0.66,0.76)}
          ${подпись(168,232,'24 больше 20 — поэтому не вернулось',GREEN,12)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='база'?'hit':(в?'miss':''), 'Потому что 20% считают от 100, а потом от 120', "r840Почему('база')")}
        ${BTN(4, в==='ошибка'?'miss':'', 'Потому что в расчёт вкралась ошибка', "r840Почему('ошибка')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='база',
        в==='база' ? 'Верно: основание изменилось. 20% от 100 — это 20, а 20% от 120 — уже 24.'
                   : 'Нет: ошибки нет. Просто 20% считают от нового числа, а оно больше.') : ЖДЁТ()) +
      ПРАВИЛО('Одинаковые проценты от <b>разных чисел</b> дают разные величины.');
  }

  /* 5. Итог: произведение множителей */
  function F5(s){
    const в = s.итог;
    return ВОПРОС('Как одним действием узнать итоговую цену?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Перемножаем множители',14,GOLD,true)}
        ${стекло(16,42,304,52,0.05)}
        ${т(168,74,'100 · 1,2 · 0,8 = 96',20,GOLD,true)}
        <g>${проявить('13s',0.26,0.36)}
          ${стекло(16,108,148,54,0.26)}
          ${т(90,132,'1,2 · 0,8',16,ИНК,true)}
          ${т(90,152,'= 0,96',16,GREEN,true)}
          ${стекло(172,108,148,54,0.38)}
          ${т(246,132,'100 · 0,96',16,ИНК,true)}
          ${т(246,152,'= 96',16,RED,true)}</g>
        ${/* ступенчатый график: 100 → 120 → 96 */''}
        <g>${проявить('13s',0.5,0.6)}
          <line x1="30" y1="222" x2="306" y2="222" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
          <line x1="30" y1="196" x2="306" y2="196" stroke="${GOLD}" stroke-width="1.4" stroke-dasharray="6 4"/>
          <polyline points="60,196 168,168 276,206" fill="none" stroke="${GREEN}" stroke-width="2.4"
            stroke-dasharray="300" stroke-dashoffset="300">
            ${ан('stroke-dashoffset','300;300;0;0','13s','keyTimes="0;0.5;0.78;1"')}</polyline>
          ${т(60,214,'100',12,GOLD,true)}
          ${т(168,186,'120',12,GREEN,true)}
          ${т(276,224,'96',12,RED,true)}</g>
        <g>${проявить('13s',0.78,0.88)}
          ${подпись(168,244,'итог ниже начала на 4%',RED,12)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='умножить'?'hit':(в?'miss':''), 'Перемножить множители: 100 · 1,2 · 0,8 = 96', "r840Итог('умножить')")}
        ${BTN(4, в==='сложить'?'miss':'', 'Сложить проценты: +20 − 20 = 0%', "r840Итог('сложить')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='умножить',
        в==='умножить' ? 'Верно: изменения перемножаются: 1,2 · 0,8 = 0,96, значит цена стала 96 ₽ — на 4% меньше.'
                      : 'Нет: складывать проценты нельзя. Множители перемножаются: 1,2 · 0,8 = 0,96.') : ЖДЁТ()) +
      ПРАВИЛО('Несколько изменений: <b>перемножь множители</b>.');
  }

  /* 6. Как вернуть цену */
  function F6(s){
    const в = s.вернуть;
    return ВОПРОС('На сколько процентов снизить цену 120, чтобы снова получить 100?') +
      `<div class="pic">${свг(`
        ${фон(246)}
        ${т(168,24,'Скидка должна быть больше 20%',14,GOLD,true)}
        ${полоса(70,200,44,140,0.62,'полоса',0.06,0.2)}
        ${полоса(146,200,44,140,0.52,'полоса3',0.36,0.52)}
        ${контроль(46,320,200,'100 ₽',0.02)}
        ${т(92,224,'120 ₽',12,МУТ)}
        ${т(168,224,'100 ₽',12,GREEN,true)}
        <g>${проявить('13s',0.44,0.54)}
          ${стекло(16,64,304,60,0.44)}
          ${т(168,92,'20 : 120 · 100% = 16,7%',20,GREEN,true)}
          ${т(168,112,'это примерно одна шестая',12,МУТ)}</g>
        <g>${проявить('13s',0.68,0.78)}
          ${подпись(168,240,'скидка 20% дала бы 96, а не 100',RED,12)}</g>
      `,246)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='семнадцать'?'hit':(в?'miss':''), 'Примерно 16,7%: 20 : 120 · 100%', "r840Вернуть('семнадцать')")}
        ${BTN(4, в==='двадцать'?'miss':'', 'Ровно 20%: столько же, сколько подняли', "r840Вернуть('двадцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='семнадцать',
        в==='семнадцать' ? 'Верно: 20 ₽ составляют от 120 уже 16,7%, а не 20%. Поэтому скидка нужна меньше.'
                         : 'Нет: 20% от 120 — это 24 ₽, и цена упадёт до 96. Нужна скидка 16,7%.') : ЖДЁТ()) +
      ПРАВИЛО('Путь вверх и путь вниз <b>не совпадают</b>, если считать проценты от разных чисел.');
  }

  /* 7. Формулы */
  function F7(s){
    const в = s.формулы;
    return ВОПРОС('На что умножают цену при снижении на 30%?') +
      `<div class="pic">${свг(`
        ${фон(240)}
        ${т(168,24,'Два множителя',14,GOLD,true)}
        ${стекло(16,42,304,52,0.05)}
        ${т(168,74,'поднять на p% → умножить на 1 + p/100',14,GREEN,true)}
        ${стекло(16,108,304,52,0.24)}
        ${т(168,140,'снизить на p% → умножить на 1 − p/100',14,RED,true)}
        <g>${проявить('13s',0.46,0.56)}
          ${стекло(16,172,148,44,0.46)}
          ${т(90,200,'+20% → 1,2',16,GOLD,true)}
          ${стекло(172,172,148,44,0.58)}
          ${т(246,200,'−30% → 0,7',16,GOLD,true)}</g>
      `,240)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='семь'?'hit':(в?'miss':''), 'На 0,7: 1 − 30/100 = 0,7', "r840Форм('семь')")}
        ${BTN(4, в==='три'?'miss':'', 'На 0,3: тридцать сотых', "r840Форм('три')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='семь',
        в==='семь' ? 'Верно: снижение на 30% — это 70% от прежней цены, то есть умножение на 0,7.'
                   : 'Нет: 0,3 — это сама скидка. От цены остаётся 70%, значит множитель 0,7.') : ЖДЁТ()) +
      ПРАВИЛО('Множитель показывает, <b>сколько осталось</b> от прежней величины.');
  }

  /* 8. Три раза по +10% */
  function F8(s){
    const в = s.цепочка;
    const столб=[0,1,2,3];
    return ВОПРОС('Цену 100 поднимали на 10% три раза. Сколько стало?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Три раза по 10% — это не 30%',14,GOLD,true)}
        ${столб.map(i=>{
          const x=54+i*64;
          const высота=[16,32,50,69][i];
          return `<g>${проявить('13s',0.06+i*0.08,0.16+i*0.08)}
            <rect x="${x+2}" y="${196-высота+3}" width="44" height="${высота}" rx="5" fill="#08171f" opacity=".34" filter="url(#c840-тень)"/>
            <rect x="${x}" y="${196-высота}" width="44" height="${высота}" rx="5" fill="url(#c840-полоса3)" stroke="rgba(20,40,30,.4)" stroke-width="1"/>
            ${т(x+22,216,['100','110','121','133,1'][i],12,ИНК,true)}
            ${т(x+22,236,['начало','+10%','+10%','+10%'][i],12,МУТ)}</g>`;
        }).join('')}
        <line x1="30" y1="196" x2="306" y2="196" stroke="${GOLD}" stroke-width="1.6" stroke-dasharray="6 4"/>
        <g>${проявить('13s',0.62,0.72)}
          ${подпись(168,52,'+30% дало бы 130, а вышло 133,1',GREEN,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сто тридцать три'?'hit':(в?'miss':''), '133,1: 100 · 1,1³', "r840Цепь('сто тридцать три')")}
        ${BTN(4, в==='сто тридцать'?'miss':'', '130: 100 + 30', "r840Цепь('сто тридцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сто тридцать три',
        в==='сто тридцать три' ? 'Верно: каждый раз 10% берут от новой цены: 110 → 121 → 133,1. Это больше 130.'
                               : 'Нет: складывать проценты нельзя. 100 → 110 → 121 → 133,1.') : ЖДЁТ()) +
      ПРАВИЛО('Проценты <b>накапливаются умножением</b>: 1,1 · 1,1 · 1,1 = 1,331.');
  }

  /* 9. Порядок не важен, база важна */
  function F9(s){
    const в = s.порядок;
    return ВОПРОС('Что изменится, если сначала снизить на 20%, а потом поднять на 20%?') +
      `<div class="pic">${свг(`
        ${фон(246)}
        ${т(168,24,'Порядок не важен — база важна',14,GOLD,true)}
        ${стекло(16,42,304,50,0.05)}
        ${т(168,72,'1,2 · 0,8 = 0,8 · 1,2 = 0,96',16,ИНК,true)}
        <g>${проявить('13s',0.24,0.34)}
          ${стекло(16,104,148,52,0.24)}
          ${т(90,128,'сначала +20%',12,МУТ)}
          ${т(90,148,'100 → 96',16,RED,true)}
          ${стекло(172,104,148,52,0.36)}
          ${т(246,128,'сначала −20%',12,МУТ)}
          ${т(246,148,'100 → 96',16,RED,true)}</g>
        <g>${проявить('13s',0.56,0.66)}
          ${подпись(168,186,'результат одинаковый: минус 4%',GREEN,12)}</g>
        <g>${проявить('13s',0.72,0.82)}
          ${подпись(168,222,'разные проценты считают от разных баз',ИНК,12)}</g>
      `,246)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='одинаково'?'hit':(в?'miss':''), 'Ничего: произведение множителей то же', "r840Порядок('одинаково')")}
        ${BTN(4, в==='дороже'?'miss':'', 'Станет дороже: подняли последним', "r840Порядок('дороже')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='одинаково',
        в==='одинаково' ? 'Верно: множители перемножаются, а от порядка множителей произведение не меняется.'
                        : 'Нет: умножение перестановочно. И там и там выйдет 96.') : ЖДЁТ()) +
      ПРАВИЛО('Смотри не на порядок, а на то, <b>от чего считают процент</b>.');
  }

  /* 10. Сводка */
  function F10(s){
    const в = s.сводка;
    return ВОПРОС('Что делать первым шагом при изменении на проценты?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Три шага',14,GOLD,true)}
        ${стекло(14,38,308,192,0.05)}
        ${[['1','заменить проценты множителем','+20% → 1,2;  −20% → 0,8'],
           ['2','перемножить множители','1,2 · 0,8 = 0,96'],
           ['3','умножить величину','100 · 0,96 = 96 ₽']].map(([н,что,как],i)=>{
          const y=54+i*58;
          return `<g>
            <circle cx="40" cy="${y+22}" r="13" fill="rgba(255,215,106,.18)" stroke="${GOLD}" stroke-width="1.6">
              ${проявить('13s',0.06+i*0.1,0.14+i*0.1)}</circle>
            ${т(40,y+27,н,14,GOLD,true)}
            <g>${проявить('13s',0.08+i*0.1,0.16+i*0.1)}
              ${т(190,y+18,что,14,ИНК,true)}
              ${т(190,y+40,как,12,МУТ)}</g>
          </g>`;
        }).join('')}
        <g>${проявить('13s',0.7,0.8)}
          ${подпись(168,244,'проценты складывать нельзя',RED,12)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='множитель'?'hit':(в?'miss':''), 'Заменить процент множителем', "r840Свод('множитель')")}
        ${BTN(4, в==='сложить'?'miss':'', 'Сложить проценты между собой', "r840Свод('сложить')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='множитель',
        в==='множитель' ? 'Верно: сначала процент превращают в множитель, потом перемножают и только затем умножают величину.'
                       : 'Нет: складывать проценты нельзя. Сначала множители, потом перемножение.') : ЖДЁТ()) +
      ПРАВИЛО('Порядок: <b>множитель → произведение → умножение величины</b>.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Цену 100 подняли на 20%. Сколько стало?', варианты:[
        {т:'120 ₽', ок:true,  почему:'100 · 1,2 = 120.'},
        {т:'200 ₽', ок:false, почему:'20% — это двадцать сотых, а не 100 ₽.'}]},
    { вопрос:'120 подняли ещё раз, теперь на 20%. Сколько?', варианты:[
        {т:'144 ₽', ок:true,  почему:'120 · 1,2 = 144.'},
        {т:'140 ₽', ок:false, почему:'20% от 120 — это 24, а не 20.'}]},
    { вопрос:'100 подняли на 20%, потом снизили на 20%. Сколько стало?', варианты:[
        {т:'96 ₽', ок:true,  почему:'1,2 · 0,8 = 0,96.'},
        {т:'100 ₽', ок:false, почему:'Проценты считают от разных чисел.'}]},
    { вопрос:'На что умножить цену при снижении на 30%?', варианты:[
        {т:'на 0,7', ок:true,  почему:'Остаётся 70% цены.'},
        {т:'на 0,3', ок:false, почему:'0,3 — это сама скидка.'}]},
    { вопрос:'Какова цена после трёх повышений по 10% от 100?', варианты:[
        {т:'133,1 ₽', ок:true,  почему:'100 · 1,1³ = 133,1.'},
        {т:'130 ₽', ок:false, почему:'Проценты складывать нельзя.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Одна привычка: процент превращаем в множитель, множители перемножаем, и только потом умножаем величину.') +
        `<div class="ask">${BTN(4,'','пройти заново','r840Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r840Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Процент — это <b>множитель</b>, а не слагаемое.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · множители',
    задания:[
      { ф:'Поднять на 20% — умножить на…', о:['1,2','0,2','2'], в:0, р:'1 + 20/100 = 1,2.' },
      { ф:'Снизить на 25% — умножить на…', о:['0,75','0,25','1,25'], в:0, р:'1 − 25/100 = 0,75.' },
      { ф:'Снизить на 30% — умножить на…', о:['0,7','0,3','1,3'], в:0, р:'Остаётся 70%.' },
      { ф:'Поднять на 15% — умножить на…', о:['1,15','0,15','1,5'], в:0, р:'1 + 15/100 = 1,15.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · цепочки',
    задания:[
      { ф:'100 → +20% → −20% = ?', о:['96','100','104'], в:0, р:'1,2 · 0,8 = 0,96.' },
      { ф:'100 → три раза +10% = ?', о:['133,1','130','121'], в:0, р:'100 · 1,331.' },
      { ф:'120 → −20% = ?', о:['96','100','144'], в:0, р:'120 · 0,8 = 96.' },
      { ф:'200 → +50% → −50% = ?', о:['150','200','100'], в:0, р:'1,5 · 0,5 = 0,75 → 150.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · ловушки',
    задания:[
      { ф:'Вернулась ли цена после +20% и −20%?', о:['нет, стала 96','да, стала 100'], в:0, р:'Базы разные.' },
      { ф:'На сколько снизить 120, чтобы получить 100?', о:['16,7%','20%','24%'], в:0, р:'20 : 120 · 100%.' },
      { ф:'20% от 100 и 20% от 120 — одинаково?', о:['нет: 20 и 24','да, оба 20'], в:0, р:'Проценты от разных чисел.' },
      { ф:'Можно ли складывать проценты подряд?', о:['нет, их перемножают','да, всегда'], в:0, р:'1,1 · 1,1 · 1,1 = 1,331.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r840T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r840TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L840={
    id: ID, title:'Проценты: изменение величины', ico:'🏷️',
    src:'Математика · 6 класс · Проценты', subj:'math',
    explain: [
      'В витрине ценник 100 рублей. Цену подняли на 20%, а потом снизили на 20%. Кажется, что она вернулась к прежней. Проверим. Первое повышение: 20% от 100 — это 20 рублей, цена стала 120. Второе изменение: 20% считают уже от 120, а это 24 рубля, значит цена стала 120 − 24 = 96. Прежней цены не получилось.',

      'Разберём повышение отдельно. Поднять на 20% — значит прибавить двадцать сотых от текущего значения. От 100 рублей это 20 рублей, поэтому получится 120. Коротко такие задачи решают умножением: 100 · 1,2 = 120. Множитель 1,2 означает «вся прежняя цена плюс её пятая часть». Частая ошибка — прибавлять к цене число процентов, то есть 20 рублей вместо правильных 20. Как проверить себя: двадцать процентов — это пятая часть.',

      'Теперь снижение. Цену 120 снизили на 20%: двадцать процентов от 120 — это 24 рубля, значит осталось 96. Обратите внимание: 96 меньше начальной сотни. На рисунке это видно сразу — столбик опустился ниже контрольной линии. Множитель снижения равен 0,8: 120 · 0,8 = 96. Частая ошибка — думать, что после снижения цена вернулась. Как проверить себя: сравни результат с начальным числом.',

      'Почему же так вышло? Потому что проценты считали от разных чисел. В первый раз основание было 100, и 20% от него — это 20. Во второй раз основание стало 120, и те же 20% дали уже 24. Одинаковые проценты от разных величин дают разные числа. Частая ошибка — считать, что «двадцать процентов» — это всегда одна и та же величина. Как проверить себя: спроси, от какого числа считают.',

      'Удобно заменять изменение множителем. Повышение на 20% — умножение на 1,2, понижение на 20% — умножение на 0,8. Тогда оба изменения выполняются одним выражением: 100 · 1,2 · 0,8. Перемножим множители: 1,2 · 0,8 = 0,96. Значит цена стала 96 — это на 4% меньше начальной. Частая ошибка — складывать проценты: +20 − 20 = 0. Как проверить себя: перемножь множители, а не складывай проценты.',

      'Интересный вопрос: на сколько процентов надо снизить цену 120, чтобы вернуться к 100? Казалось бы, на те же 20%, но нет: 20% от 120 — это 24, и получилось бы 96. Нужно понять, сколько составляют 20 рублей от 120: 20 : 120 · 100% ≈ 16,7%, то есть примерно одна шестая. Скидка должна быть меньше, чем было повышение. Частая ошибка — считать путь вверх и путь вниз одинаковым. Как проверить себя: раздели разницу на новое основание.',

      'Соберём формулы. Чтобы повысить величину на p процентов, умножают на 1 + p/100. Чтобы понизить на p процентов, умножают на 1 − p/100. Например, снижение на 30% — это умножение на 0,7, потому что от цены остаётся 70%. Множитель показывает, сколько осталось от прежней величины. Частая ошибка — путать множитель 0,7 с самой скидкой 0,3. Как проверить себя: сложи остаток и скидку — должно выйти единица.',

      'Проверим ещё одну ловушку: цену 100 поднимали на 10% три раза. Кажется, что всего на 30% и получится 130. На самом деле каждый раз 10% берут от новой цены: 100 → 110 → 121 → 133,1. Итог больше 130. Это видно на столбиках: они растут всё медленнее, но суммарно дают больше. Частая ошибка — складывать проценты. Как проверить себя: перемножь множители 1,1 · 1,1 · 1,1 = 1,331.',

      'А что если изменить порядок: сначала снизить на 20%, потом поднять на 20%? Результат тот же — 96. Множители перемножаются, а от порядка множителей произведение не меняется: 1,2 · 0,8 = 0,8 · 1,2. Важно не то, в каком порядке мы действуем, а то, от какого числа считаем каждый процент. Частая ошибка — думать, что порядок меняет ответ. Как проверить себя: посчитай оба порядка и сравни.',

      'Соберём порядок действий. Первое: заменить каждый процент множителем. Второе: перемножить множители — получить общий. Третье: умножить начальную величину на этот общий множитель. Проверка: если общий множитель больше единицы, величина выросла, если меньше — уменьшилась. Частая ошибка — складывать проценты и получать неверный результат. Как проверить себя: посмотри, больше или меньше единицы вышел общий множитель.',

      'Пройти практику: пять уровней про повышение, снижение, цепочки изменений и ловушку «вернулось ли». Дальше ждут три тренажёра.',

      'Первый тренажёр — множители: превращаем проценты в числа, на которые умножают.',

      'Второй тренажёр — цепочки: перемножаем несколько изменений подряд.',

      'Третий тренажёр — ловушки: проверяем, вернулась ли величина и можно ли складывать проценты.'
    ],
    check: {
      q:'Цену 100 подняли на 20%, потом снизили на 20%. Сколько стало?',
      choices:['96 ₽','100 ₽','104 ₽'],
      ans:0,
      exp:'100 · 1,2 = 120; 120 · 0,8 = 96. Итог ниже начальной цены на 4%.'
    },
    tasks: [
      { q:'Снизить на 30% — это умножить на…', kind:'choice',
        choices:['0,7','0,3'], ans:0,
        hints:['От цены остаётся 70%.','1 − 30/100 = 0,7.'],
        sol:'На 0,7.' },
      { q:'Цену 100 поднимали на 10% три раза. Сколько стало?', kind:'choice',
        choices:['133,1 ₽','130 ₽'], ans:0,
        hints:['100 → 110 → 121 → …','100 · 1,331.'],
        sol:'133,1 ₽.' },
      { q:'На сколько снизить 120, чтобы получить 100?', kind:'choice',
        choices:['примерно на 16,7%','на 20%'], ans:0,
        hints:['20 : 120 · 100%.','20% от 120 — это 24.'],
        sol:'На 16,7%.' },
      { q:'200 подняли на 50%, потом снизили на 50%. Сколько стало?', kind:'choice',
        choices:['150','200'], ans:0,
        hints:['1,5 · 0,5 = 0,75.','200 · 0,75.'],
        sol:'150.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L840.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Витрина','Подняли и снизили'],
      2:['Повышение','100 → 120'],
      3:['Понижение','120 → 96'],
      4:['Почему','Разные основания'],
      5:['Множители','1,2 · 0,8 = 0,96'],
      6:['Обратный путь','Скидка 16,7%'],
      7:['Формулы','Множители изменения'],
      8:['Три раза по 10%','133,1, а не 130'],
      9:['Порядок','Не важен'],
      10:['Три шага','Порядок решения'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','Множители'],
      13:['Тренажёр 2','Цепочки'],
      14:['Тренажёр 3','Ловушки']
    };
    const з=ЗАГОЛОВКИ[f]||['Изменение величины','Проценты'];
    el.innerHTML = `<div class="s6 l840" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r840Сюжет=(к)=>{ S().сюжет=к; chRender(0); };
  window.r840Подъём=(к)=>{ S().повышение=к; chRender(0); };
  window.r840Снижение=(к)=>{ S().понижение=к; chRender(0); };
  window.r840Почему=(к)=>{ S().почему=к; chRender(0); };
  window.r840Итог=(к)=>{ S().итог=к; chRender(0); };
  window.r840Вернуть=(к)=>{ S().вернуть=к; chRender(0); };
  window.r840Форм=(к)=>{ S().формулы=к; chRender(0); };
  window.r840Цепь=(к)=>{ S().цепочка=к; chRender(0); };
  window.r840Порядок=(к)=>{ S().порядок=к; chRender(0); };
  window.r840Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r840Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r840Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r840T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r840TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L840; else arr.push(L840);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA840={render:render, L:L840};
})();
