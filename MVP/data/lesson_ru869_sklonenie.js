/* ============ РУССКИЙ ЯЗЫК · УРОК 869 · «СКЛОНЕНИЕ СУЩЕСТВИТЕЛЬНЫХ» ============
   4 класс, «Имя существительное». Была заглушкой в soon_lessons.js — теперь урок.
   Первый урок 4 класса: путь «Открытое море». «Чайка» выросла во фрегат и выходит
   из прибрежных вод. Рисунки — общая библиотека моря MVP/data/ris_more.js.

   СЮЖЕТ. «Роза падежей». В штурманской рубке у штурмана — компас с шестью
   лучами. «На этом корабле, — говорит он, — слово поворачивается, как стрелка:
   спросишь по-другому — у слова другой конец». Его какаду выкрикивает вопросы
   падежей, а сам не склоняется ни в одном.

   РУКАМИ: роза падежей — касаешься луча (или кнопки), стрелка поворачивается, и
   строка в журнале меняется: есть корабль — нет корабля — рад кораблю — вижу
   корабль — любуюсь кораблём — думаю о корабле, окончание подсвечено; три
   предложения — для выделенного слова выбираешь падеж из шести.

   ПРОВЕРЕНО ПО ПРОГРАММЕ 4 КЛАССА (Канакина, Горецкий):
     изменение существительных по падежам — склонение;
     И. кто? что? (есть) · Р. кого? чего? (нет) · Д. кому? чему? (дать, рад) ·
     В. кого? что? (вижу) · Т. кем? чем? (любуюсь, доволен) ·
     П. о ком? о чём? (думаю о);
     при склонении меняется окончание, корень остаётся;
     И. и В. у неодушевлённых совпадают — различают вспомогательным словом
     (есть парус / вижу парус); Р. и В. у одушевлённых совпадают (нет
     капитана / вижу капитана) — различают вопросом от глагола;
     предложный падеж употребляется только с предлогом;
     несклоняемые существительные (какаду, кенгуру, пальто, кино);
     к острову — Д., закатом — Т., о звёздах — П., из гавани — Р. */
(function(){
  'use strict';

  const ID = 869;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f', ОБВОД='#33291e';
  const ЧЕРНИЛА='#2e2416';

  const ДЕЛА = [
    {ключ:'роза',    имя:'Повернуть розу падежей', итог:'6 падежей'},
    {ключ:'падежи',  имя:'Определить падеж',       итог:'Д · Т · П'},
    {ключ:'журнал',  имя:'Проверить запись в журнале', итог:'из гавани — Р.'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const ПАДЕЖИ = [
    {б:'И', имя:'именительный', вопр:'кто? что?',     пом:'есть',     осн:'корабл', ок:'ь'},
    {б:'Р', имя:'родительный',  вопр:'кого? чего?',   пом:'нет',      осн:'корабл', ок:'я'},
    {б:'Д', имя:'дательный',    вопр:'кому? чему?',   пом:'рад',      осн:'корабл', ок:'ю'},
    {б:'В', имя:'винительный',  вопр:'кого? что?',    пом:'вижу',     осн:'корабл', ок:'ь'},
    {б:'Т', имя:'творительный', вопр:'кем? чем?',     пом:'любуюсь',  осн:'корабл', ок:'ём'},
    {б:'П', имя:'предложный',   вопр:'о ком? о чём?', пом:'думаю о',  осн:'корабл', ок:'е'}
  ];
  const УГОЛ = (k) => -90 + k*60;
  const ТРИ = [
    {до:'Фрегат идёт к ', сл:'острову', после:'.', вопр:'идёт к чему?', верно:2},
    {до:'Матросы любуются ', сл:'закатом', после:'.', вопр:'любуются чем?', верно:4},
    {до:'Штурман рассказал о ', сл:'звёздах', после:'.', вопр:'рассказал о чём?', верно:5}
  ];

  const CSS=`
  #lvis .s6.l869{gap:14px}
  #lvis .s6.l869 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l869 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l869 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l869 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l869 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l869 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l869 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l869 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l869 .карт .текст b{color:${GOLD}}
  #lvis .s6.l869 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l869 .правило b{color:${GOLD}}
  #lvis .s6.l869 .журнал{width:100%;padding:12px 14px;border-radius:6px 16px 16px 6px;
    background:linear-gradient(90deg,#efe2c0,#dcc79a);border-left:7px solid #7a4a24;display:flex;flex-direction:column;gap:7px;color:${ЧЕРНИЛА}}
  #lvis .s6.l869 .журнал .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:#6a4a24}
  #lvis .s6.l869 .журнал .шапка b{font-size:18px;letter-spacing:0;text-transform:none;color:#7a2a10;font-family:Georgia,serif}
  #lvis .s6.l869 .журнал .шапка b.готово{color:#1a6a3a}
  #lvis .s6.l869 .журнал ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l869 .журнал li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:#6a5236}
  #lvis .s6.l869 .журнал li i{flex:0 0 22px;width:22px;height:22px;border-radius:50%;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;border:1.5px solid #a88a5a;color:#8a6a3a}
  #lvis .s6.l869 .журнал li.есть{color:${ЧЕРНИЛА}}
  #lvis .s6.l869 .журнал li.есть i{border-color:#1a4a6a;background:#2a6a9a;color:#fff}
  #lvis .s6.l869 .журнал li span{margin-left:auto;white-space:nowrap;font-size:14px;color:#8a6a3a;font-family:Georgia,serif}
  #lvis .s6.l869 .журнал li.есть span{color:#1a6a3a;font-weight:700}
  #lvis .s6.l869 .буйки{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;width:100%}
  #lvis .s6.l869 .буйки button{min-height:56px;border-radius:28px;cursor:pointer;font:inherit;font-size:19px;font-weight:700;
    font-family:Georgia,serif;border:2px solid #e86a5a;background:linear-gradient(180deg,#fff6ea,#f0dcc0);color:${ЧЕРНИЛА};
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),opacity 200ms}
  #lvis .s6.l869 .буйки button:active{transform:scale(.96)}
  #lvis .s6.l869 .буйки button.пойман{opacity:.35;border-color:${GREEN};text-decoration:line-through}
  #lvis .s6.l869 .буйки button.мимо{border-color:${RED};animation:l869нет 360ms cubic-bezier(.23,1,.32,1)}
  @keyframes l869нет{0%,100%{transform:none}25%{transform:translateX(-6px)}75%{transform:translateX(6px)}}
  #lvis .s6.l869 .ряд{display:grid;gap:8px;width:100%}
  #lvis .s6.l869 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:18px;font-weight:700;
    border:1.5px solid rgba(127,209,255,.5);background:rgba(127,209,255,.1);color:${ИНК};padding:6px 4px;font-family:Georgia,serif;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l869 .ряд button:active{transform:translateY(2px)}
  #lvis .s6.l869 .ряд button.вкл{border-color:${GOLD};background:rgba(255,215,106,.22);color:${GOLD}}
  #lvis .s6.l869 .случай{width:100%;display:flex;flex-direction:column;gap:6px}
  #lvis .s6.l869 .случай .что{font-size:19px;color:var(--ink);font-family:Georgia,serif}
  #lvis .s6.l869 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l869 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:17px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l869 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l869 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l869 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l869 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-size:18px}
  #lvis .s6.l869 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-size:18px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l869 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l869 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l869 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l869 .уровни .точка.сейчас{background:${GOLD};animation:l869dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l869dot{0%,100%{opacity:1}50%{opacity:.55}}
  #lvis .s6.l869 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l869{-webkit-text-size-adjust:100%}
  #lvis .s6.l869 [data-anim]{animation:l869rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l869rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  #lvis .s6.l869 .падежи{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;width:100%}
  #lvis .s6.l869 .падежи button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:16px;font-family:Georgia,serif;
    border:1.5px solid rgba(255,215,106,.45);background:rgba(255,215,106,.08);color:${ИНК};padding:6px 4px;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1),background 160ms}
  #lvis .s6.l869 .падежи button b{color:${GOLD};font-size:18px;margin-right:4px}
  #lvis .s6.l869 .падежи button.был{border-color:rgba(143,209,168,.6)}
  #lvis .s6.l869 .падежи button.вкл{border-color:${GOLD};background:rgba(255,215,106,.24)}
  #lvis .s6.l869 .падежи button:active{transform:translateY(2px)}
  #lvis .s6.l869 .ask.шесть button{flex:1 1 calc(16% - 8px);text-align:center;font-size:17px;padding:13px 2px;min-width:44px;font-family:Georgia,serif}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l869 [data-anim]{animation:none!important}
    #lvis .s6.l869 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l869 button{transition:none!important;animation:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l869-style');
      if(!s){ s=document.createElement('style'); s.id='l869-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Судовой журнал</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,f) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, 'r869Отв('+f+','+к+')')).join('')}</div>`;
  const ЖУРНАЛ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'журнал',
      `<div class="шапка"><span>Штурманская рубка</span><b class="${всё?'готово':''}">${
        всё?'курс проложен':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}<span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.45;0.45;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const сдвигРаз = (из,в,длит,задержка) => ДВИЖ ?
    `<animateTransform attributeName="transform" type="translate" from="${из}" to="${в}" dur="${длит}" begin="${(задержка||0).toFixed(2)}s"
       fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  /* слово с выделенным окончанием */
  const тОк = (x,y,осн,ок,кегль,цвет,цветОк,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль}" font-weight="bold" fill="${цвет}" font-family="Georgia,'Times New Roman',serif">${esc(осн)}<tspan fill="${цветОк}">${esc(ок)}</tspan></text>`;
  const Р = () => window.РМ;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <filter id="c869-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="1.5" dy="2" stdDeviation="1.6" flood-color="#0b1c2a" flood-opacity=".45"/>
      </filter>
      <radialGradient id="c869-лампа" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#ffd890" stop-opacity=".5"/><stop offset="1" stop-color="#ff9a40" stop-opacity="0"/>
      </radialGradient>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${Р().defs()}${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c869-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(14,26,20,.92)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  /* облачко-реплика */
  const реплика = (x,y,ш,текст,хвост) => `<g filter="url(#c869-тень)">
      <rect x="${x}" y="${y}" width="${ш}" height="30" rx="14" fill="#fffaf0" stroke="${ОБВОД}" stroke-width="1"/>
      <path d="M${хвост[0]} ${y+29} l${хвост[1]} 12 l6 -12z" fill="#fffaf0" stroke="${ОБВОД}" stroke-width="1"/>
      <rect x="${хвост[0]-1}" y="${y+26}" width="10" height="5" fill="#fffaf0"/></g>
      ${т(x+ш/2,y+20,текст,14,ЧЕРНИЛА,true)}`;
  /* штурманская рубка: тёмное дерево, лампа, иллюминатор с морем */
  const рубка = (Н) => { const М=Р();
    return `${М.доски(0,0,336,Н,true)}
      <circle cx="300" cy="40" r="70" fill="url(#c869-лампа)" data-декор="1">${анЛин('r','66;74;68;72;66','2.4s')}</circle>
      <g><circle cx="44" cy="44" r="26" fill="url(#рм-море)" stroke="url(#рм-латунь)" stroke-width="5"/>
        <path d="M22 44 q10 -4 22 0 t22 0" stroke="#e8f6ff" stroke-width="1.2" fill="none" opacity=".7">${анЛин('transform','0','1s')}</path>
        <ellipse cx="36" cy="34" rx="8" ry="3" fill="#fff" opacity=".35" transform="rotate(-30 36 34)"/></g>`; };

  /* ================= КАДРЫ ================= */

  /* 1. Роза падежей */
  function F1(s){
    const Н=340, М=Р(), к=s.пад1==null?0:s.пад1, вид=Array.isArray(s.вид1)?s.вид1:[0], все=вид.length>=6, в=s.ответ1, ок=в===0;
    const п=ПАДЕЖИ[к];
    const лучи = ПАДЕЖИ.map((_,i)=>{ const a=(-90+i*60)*Math.PI/180, R=78*1.18;
      return `<g onclick="r869Пад(${i})" style="cursor:pointer"><circle cx="${(168+R*Math.cos(a)).toFixed(1)}" cy="${(150+R*Math.sin(a)).toFixed(1)}" r="22" fill="transparent"/></g>`; }).join('');
    return ЖУРНАЛ(s) +
      ЗАДАЧА('«Чайка» выходит в открытое море. В штурманской рубке штурман показывает компас с шестью лучами: «Это <b>роза падежей</b>. Поверни стрелку — и слово в журнале ответит на другой вопрос и сменит окончание». Касайся лучей — побывай во всех шести.') +
      `<div class="pic">${свг(`
        ${рубка(Н)}
        ${М.роза(168,150,78,{лучей:6,подписи:ПАДЕЖИ.map(x=>x.б),угол:УГОЛ(к),было:s.былоУгол1!=null?s.былоУгол1:null,свет:к})}
        ${лучи}
        ${М.лист(14,260,308,68,{})}
        ${т(30,284,п.имя+' · '+п.вопр,12,'#7a4a24',true,'start')}
        <g>${ДВИЖ?`<animate attributeName="opacity" from="0" to="1" dur="0.5s" fill="freeze"/>`:''}
          <text x="168" y="316" text-anchor="middle" font-size="22" font-weight="bold" fill="${ЧЕРНИЛА}" font-family="Georgia,serif">${esc(п.пом)} корабл<tspan fill="#b85a10">${esc(п.ок)}</tspan></text></g>
        ${т(318,284,'побывал: '+вид.length+' из 6',11,вид.length>=6?'#1a6a3a':'#7a5a3a',true,'end')}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="падежи">${ПАДЕЖИ.map((x,i)=>BTN(3+(i%5),(i===к?'вкл':'')+(вид.includes(i)?' был':''),'<b>'+x.б+'</b> '+x.пом,'r869Пад('+i+')')).join('')}</div>` +
      (!все ? СКАЗ('Попробуй','Коснись луча или кнопки. Смотри на конец слова «корабль»: он меняется.') :
        ОТВЕТЫ('пара',['склонение','спряжение'],0,в,1) +
        (в==null ? СКАЗ('Вопрос','Как называется изменение существительного по падежам?') :
          РАЗБОР(ок,['Изменение по падежам — <b>склонение</b>: корабль, корабля, кораблю, корабль, кораблём, о корабле.',
            '«Спряжение» — это про глаголы: пишу, пишешь. У существительных — <b>склонение</b>.'][в]))) +
      (ок ? ПРАВИЛО('Изменение существительного по <b>падежам</b> называется <b>склонением</b>. Падежей шесть: И, Р, Д, В, Т, П.') : '');
  }

  /* 2. Вопросы падежей */
  function F2(s){
    const Н=280, в=s.ответ2, ок=в===1, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Какаду штурмана знает все шесть вопросов и выкрикивает их по очереди. Сейчас он кричит вопрос <b>родительного</b> падежа — того, что после слова «нет». Какой?') +
      `<div class="pic">${свг(`
        ${рубка(Н)}
        ${М.карта(170,70,154,130,{})}
        ${М.штурман(92,Н-20,1,{смотрит:true})}
        ${М.какаду(120,Н-152,0.9,{})}
        ${реплика(150,24,150,ок?'Кого? Чего?':'Кр-р-р… ?',[166,-12])}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['кому? чему?','кого? чего?','кем? чем?'],1,в,2) +
      (в==null ? СКАЗ('Вопрос','На какой вопрос отвечает родительный падеж?') :
        РАЗБОР(ок,['«Кому? чему?» — вопросы <b>дательного</b>: рад кому? — кораблю. Родительный — нет <b>кого? чего?</b>',
          'Нет <b>кого? чего?</b> — нет корабля, нет штурмана. Это родительный падеж.',
          '«Кем? чем?» — вопросы <b>творительного</b>: любуюсь чем? — кораблём. Родительный — <b>кого? чего?</b>'][в])) +
      (ок ? ПРАВИЛО('Вопросы падежей: И. кто? что? · Р. кого? чего? · Д. кому? чему? · В. кого? что? · Т. кем? чем? · П. о ком? о чём?') : '');
  }

  /* 3. Определи падеж */
  function F3(s){
    const Н=240, М=Р(), реш=s.реш3||{}, все=ТРИ.every((_,i)=>реш[i]!=null), верно=все&&ТРИ.every((x,i)=>реш[i]===x.верно);
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Штурман диктует три строки в судовой журнал. В каждой выделено одно слово. Задай к нему вопрос от глагола и определи <b>падеж</b>.') +
      `<div class="pic">${свг(`
        ${М.небо(336,90,{закат:true,солнце:[70,82,12]})}
        ${М.море(90,Н-90,336,{дорожка:70})}
        ${М.фрегат(276,176,0.4,{})}
        ${М.лист(12,96,196,Н-110,{})}
        ${ТРИ.map((x,i)=>{ const y=128+i*34, р=реш[i];
          return т(24,y,x.сл,16,ЧЕРНИЛА,true,'start')+т(24,y+14,x.вопр,10.5,'#7a5a3a',false,'start')+
            (р!=null?`<g filter="url(#c869-тень)"><circle cx="186" cy="${y-5}" r="12" fill="${р===x.верно?'#2e7a4a':'#a8321e'}" stroke="${ОБВОД}"/></g>${т(186,y,ПАДЕЖИ[р].б,13,'#fff',true)}`:''); }).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      ТРИ.map((x,i)=>`<div class="случай">${A(3+i,'что',x.до+'<b>'+x.сл+'</b>'+x.после)}<div class="ask шесть">${ПАДЕЖИ.map((п,j)=>
        BTN(4+i,реш[i]===j?(j===x.верно?'hit':'miss'):'',п.б,'r869Три('+i+','+j+')')).join('')}</div></div>`).join('') +
      (!все ? СКАЗ('Подсказка','Задай вопрос от глагола: идёт к <b>чему?</b> любуются <b>чем?</b> рассказал <b>о чём?</b>') :
        РАЗБОР(верно, верно ? 'К острову — <b>дательный</b> (к чему?), закатом — <b>творительный</b> (чем?), о звёздах — <b>предложный</b> (о чём?).' :
          'Проверь вопросом: к чему? — Д., чем? — Т., о чём? — П.')) +
      (верно ? ПРАВИЛО('Чтобы определить падеж, найди слово, от которого зависит существительное, и <b>задай вопрос</b>.') : '');
  }

  /* 4. Именительный или винительный */
  function F4(s){
    const Н=260, в=s.ответ4, ок=в===1, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Два предложения: «<b>Парус</b> надулся ветром» и «Матрос чинит <b>парус</b>». Слово одинаковое, вопрос тоже — «что?». А падежи разные! Штурман советует: подставь «<b>есть</b>» или «<b>вижу</b>». В каком предложении «парус» — в винительном?') +
      `<div class="pic">${свг(`
        ${М.небо(336,110,{солнце:[296,30,10],облака:[[80,30,0.5,10]]})}
        ${М.море(110,Н-110,336,{})}
        ${М.фрегат(100,210,0.55,{})}
        ${М.доски(190,Н-60,146,60,false)}
        ${М.юнга(262,Н-30,0.62,{поза:'пишет',шапка:false})}
        <path d="M200 ${Н-58} q30 -30 70 -22 q-10 20 -30 24z" fill="url(#рм-парус)" stroke="${ОБВОД}" stroke-width="1"/>
        ${подпись(100,40,'есть парус — И.',ок?GREEN:ИНК,12)}${подпись(262,160,'вижу парус — В.',ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Парус надулся ветром','Матрос чинит парус'],1,в,4) +
      (в==null ? СКАЗ('Вопрос','Где «парус» в винительном падеже?') :
        РАЗБОР(ок,['«Парус надулся» — парус сам что-то делает, он подлежащее: <b>есть</b> парус — именительный. Винительный — «чинит (вижу) парус».',
          'Чинит <b>что?</b> — парус. Подставь «вижу парус» — подходит: это <b>винительный</b>. А «парус надулся» — именительный.'][в])) +
      (ок ? ПРАВИЛО('И. и В. часто совпадают. Помогают слова-помощники: <b>есть</b> (И.) и <b>вижу</b> (В.). Подлежащее всегда в именительном.') : '');
  }

  /* 5. Родительный или винительный */
  function F5(s){
    const Н=260, в=s.ответ5, ок=в===1, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга стоит у борта и <b>ждёт капитана</b> с берега. У одушевлённых слов родительный и винительный совпадают: нет капитана, вижу капитана. В каком падеже «капитана» в «ждёт капитана»?') +
      `<div class="pic">${свг(`
        ${М.небо(336,110,{солнце:[40,30,10]})}
        ${М.берег(110,{x0:170,x1:336})}
        ${М.море(110,Н-110,336,{})}
        ${М.доски(0,Н-70,170,70,false)}
        ${М.юнга(80,Н-40,0.7,{поза:'машет',взгляд:1})}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 0;-30 6;0 0" dur="8s" repeatCount="indefinite"/>`:''}${М.лодка(252,Н-70,0.6,{внутри:М.капитан(0,18,0.5,{трубка:false,взгляд:-1})})}</g>
        ${подпись(80,60,ок?'ждёт кого? — вижу кого? — В.':'ждёт кого?',ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['родительный','винительный'],1,в,5) +
      (в==null ? СКАЗ('Вопрос','«Юнга ждёт капитана» — какой падеж?') :
        РАЗБОР(ок,['Подставь «нет» и «вижу». «Ждёт» ведёт себя как «вижу»: ждёт кого? — вижу кого? Это <b>винительный</b>.',
          'Верно: ждёт кого? — как «вижу кого?». Винительный.'][в])) +
      (ок ? ПРАВИЛО('У одушевлённых Р. и В. совпадают (капитана). Различают по слову, от которого ставят вопрос: <b>нет</b> — Р., <b>вижу</b> — В.') : '');
  }

  /* 6. Предлоги */
  function F6(s){
    const Н=250, в=s.ответ6, ок=в===0, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Какаду выкрикивает: «Думаю — о чём? О море! О ветре! О звёздах!» Юнга замечает: без маленького слова «о» этот вопрос не задать. Какой падеж <b>всегда</b> живёт с предлогом?') +
      `<div class="pic">${свг(`
        ${М.небо(336,130,{ночь:true,луна:[296,34]})}
        ${М.море(130,Н-130,336,{ночь:true})}
        ${М.фрегат(110,210,0.5,{})}
        ${М.доски(186,196,150,Н-196,true)}
        ${М.бочка(292,196,0.8,{})}
        ${М.какаду(250,163,1.2,{влево:true})}
        ${реплика(170,40,150,'о чём? о ком?',[260,-8])}
        ${ок?подпись(96,Н-12,'П. — только с предлогом',GREEN,12):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['предложный','творительный','дательный'],0,в,6) +
      (в==null ? СКАЗ('Вопрос','Какой падеж никогда не бывает без предлога?') :
        РАЗБОР(ок,['<b>Предложный</b> — даже в названии «предлог»: о море, в гавани, на палубе.',
          'Творительный бывает без предлога: любуюсь закатом. А <b>предложный</b> — только с предлогом.',
          'Дательный бывает без предлога: рад кораблю. Всегда с предлогом — <b>предложный</b>.'][в])) +
      (ок ? ПРАВИЛО('<b>Предложный падеж</b> всегда с предлогом: о, об, в, на, при. Остальные падежи бывают и с предлогом, и без.') : '');
  }

  /* 7. Меняется окончание */
  function F7(s){
    const Н=260, в=s.ответ7, ок=в===0, М=Р();
    const формы=[['есть','волн','а'],['нет','волн','ы'],['рад','волн','е'],['вижу','волн','у'],['любуюсь','волн','ой'],['думаю о','волн','е']];
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Штурман пишет слово «<b>волна</b>» во всех шести падежах одно под другим. Юнга смотрит на столбик: одна часть слова стоит на месте, другая всё время меняется. Какая меняется?') +
      `<div class="pic">${свг(`
        ${М.небо(336,60,{облака:[[250,24,0.4,-6]]})}
        ${М.море(60,Н-60,336,{})}
        ${М.лист(18,70,300,Н-86,{})}
        ${формы.map(([п,о,к],i)=>`<g>${ДВИЖ?`<animate attributeName="opacity" values="0.4;1;1;0.4" keyTimes="0;${(i/6).toFixed(2)};${((i+1)/6).toFixed(2)};1" dur="6s" repeatCount="indefinite"/>`:''}
          ${т(128,96+i*27,п,13,'#7a5a3a',false,'end')}${тОк(136,96+i*27,о,к,19,ЧЕРНИЛА,'#b85a10','start')}</g>`).join('')}
        ${ок?`<rect data-декор="1" x="132" y="76" width="52" height="${6*27}" rx="6" fill="none" stroke="${GREEN}" stroke-width="2" stroke-dasharray="5 4"/>`+т(250,160,'корень на месте',12,'#1a6a3a',true):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['окончание','корень','приставка'],0,в,7) +
      (в==null ? СКАЗ('Вопрос','Что меняется, когда слово склоняется?') :
        РАЗБОР(ок,['Волн-<b>а</b>, волн-<b>ы</b>, волн-<b>е</b>… меняется <b>окончание</b>, а корень «волн» стоит на месте.',
          'Корень «волн» во всех шести строках одинаковый. Меняется <b>окончание</b>.',
          'Приставки в слове «волна» нет вовсе. Меняется <b>окончание</b>.'][в])) +
      (ок ? ПРАВИЛО('При склонении у существительного меняется <b>окончание</b>. По окончанию и вопросу узнают падеж.') : '');
  }

  /* 8. Несклоняемое */
  function F8(s){
    const Н=260, в=s.ответ8, ок=в===0, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга пробует склонять имя попугая: «Вот <b>какаду</b>. Нет <b>какаду</b>. Рад <b>какаду</b>. Любуюсь <b>какаду</b>…» Слово не меняется ни на одном луче! Какое из слов не склоняется?') +
      `<div class="pic">${свг(`
        ${рубка(Н)}
        ${М.роза(90,130,54,{лучей:6,подписи:ПАДЕЖИ.map(x=>x.б),угол:ДВИЖ?-90:-30})}
        <g><rect x="248" y="214" width="6" height="42" fill="url(#рм-латунь)"/>
          <rect x="212" y="209" width="78" height="7" rx="3.5" fill="#8a5a2e" stroke="${ОБВОД}" stroke-width="1"/>
          <ellipse cx="251" cy="256" rx="24" ry="4" fill="url(#рм-латунь)"/></g>
        ${М.какаду(250,170,1.6,{влево:true})}
        ${подпись(226,44,'какаду · какаду · какаду',ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['какаду','штурман','компас'],0,в,8) +
      (в==null ? СКАЗ('Вопрос','Какое слово не склоняется?') :
        РАЗБОР(ок,['<b>Какаду</b> во всех падежах одинаковый — несклоняемое слово, как кенгуру, пальто, кино, метро.',
          'Штурман склоняется: нет штурмана, рад штурману. Не склоняется — <b>какаду</b>.',
          'Компас склоняется: нет компаса, любуюсь компасом. Не склоняется — <b>какаду</b>.'][в])) +
      (ок ? ПРАВИЛО('Есть <b>несклоняемые</b> существительные: какаду, кенгуру, пальто, кино, метро. Падеж у них узнают только по вопросу.') : '');
  }

  /* 9. Запись в журнале */
  function F9(s){
    const Н=270, в=s.ответ9, ок=в===0, М=Р();
    const с1=['Утром','фрегат','вышел'], с2=['из гавани','и взял'], с3=['курс','к острову.'];
    const ряд=(слова,y)=>{ let x=26; return слова.map(w=>{ const ш=w.length*11.2+10, выд=w==='из гавани', g=`<g>${выд?`<rect x="${x-4}" y="${y-22}" width="${ш}" height="32" rx="6" fill="${ок?'rgba(143,209,168,.35)':'rgba(255,215,106,.3)'}" stroke="${ок?GREEN:'#c8901a'}"/>`:''}${т(x,y,w,20,ЧЕРНИЛА,false,'start')}</g>`; x+=ш+4; return g; }).join(''); };
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга записывает в журнал: «<b>Утром фрегат вышел из гавани и взял курс к острову.</b>» Определи падеж слова «гавани».') +
      `<div class="pic">${свг(`
        ${М.небо(336,96,{солнце:[40,40,11],облака:[[240,34,0.5,-8]]})}
        ${М.море(96,30,336,{дорожка:40})}
        ${М.лист(12,116,312,Н-128,{})}
        ${ряд(с1,158)}${ряд(с2,198)}${ряд(с3,238)}
        ${ок?т(300,198,'из чего? — Р.',12,'#1a6a3a',true,'end'):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['родительный','дательный','предложный'],0,в,9) +
      (в==null ? СКАЗ('Вопрос','В каком падеже «гавани»?') :
        РАЗБОР(ок,['Вышел <b>из чего?</b> — из гавани. Вопрос «чего?» — <b>родительный</b> падеж.',
          'Дательный — к чему?, кому? А здесь вышел <b>из чего?</b> — родительный.',
          'Предложный — о чём? в чём? А здесь вышел <b>из чего?</b> — родительный.'][в])) +
      (ок ? ПРАВИЛО('Предлог подсказывает, но решает <b>вопрос</b>: из чего? — Р., к чему? — Д., о чём? — П.') : '');
  }

  /* 10. Открытое море */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=320, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА(всё
        ? 'Берег скрылся за кормой. Штурман убирает розу падежей в футляр: «Теперь ты умеешь повернуть любое слово. В открытом море это пригодится — журнал надо вести без ошибок». Какаду кричит: «Кор-р-рабль! Кор-р-рабля! Кор-р-раблю!»'
        : 'Роза падежей ещё не освоена — вернись к делам в списке. Вот что получится в конце.') +
      `<div class="pic">${свг(`
        ${М.небо(336,130,{закат:true,солнце:[270,118,16],облака:[[80,40,0.6,10]]})}
        ${М.море(130,Н-130,336,{дорожка:270})}
        ${М.фрегат(160,196,0.7,{имя:'Чайка'})}
        ${М.чайка(60,50,0.7,{дрейф:'8 -4'})}
        ${[['И Р Д В Т П — шесть падежей',GOLD],['меняется окончание',BLUE],['падеж — по вопросу',GREEN]].map(([t0,ц],i)=>`<g>${проявить('9s',0.1+i*0.18,0.16+i*0.18)}${подпись(168,Н-70+i*28,t0,ц,13)}</g>`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','Изменение существительного по падежам — <b>склонение</b>. Падежей шесть: И. (кто? что?), Р. (кого? чего?), Д. (кому? чему?), В. (кого? что?), Т. (кем? чем?), П. (о ком? о чём?). При склонении меняется <b>окончание</b>. Падеж определяют по вопросу от главного слова; И. и В., Р. и В. различают словами-помощниками. Предложный — всегда с предлогом. Есть несклоняемые слова: какаду, пальто, кино.') +
      ПРАВИЛО('<b>Задай вопрос — узнаешь падеж.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'На какой вопрос отвечает дательный падеж?', варианты:[{т:'кому? чему?',ок:true},{т:'кем? чем?',ок:false}], разбор:'Рад кому? чему?' },
    { вопрос:'«Нет компаса» — какой падеж?', варианты:[{т:'винительный',ок:false},{т:'родительный',ок:true}], разбор:'Нет чего? — Р.' },
    { вопрос:'«Любуюсь морем» — какой падеж?', варианты:[{т:'творительный',ок:true},{т:'предложный',ок:false}], разбор:'Любуюсь чем? — Т.' },
    { вопрос:'Что меняется при склонении?', варианты:[{т:'корень',ок:false},{т:'окончание',ок:true}], разбор:'Корень стоит на месте.' },
    { вопрос:'Какое слово не склоняется?', варианты:[{т:'пальто',ок:true},{т:'палуба',ок:false}], разбор:'Пальто, кино, какаду.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r869Reset()")}</div>` +
        ПРАВИЛО('<b>Задай вопрос — узнаешь падеж.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r869Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Вопросы падежей', задания:[
    {q:'Творительный падеж отвечает на вопрос…', в:1, варианты:['кому? чему?','кем? чем?'], раз:'Любуюсь кем? чем?'},
    {q:'Предложный падеж отвечает на вопрос…', в:0, варианты:['о ком? о чём?','кого? что?'], раз:'Думаю о ком? о чём?'},
    {q:'Какой падеж отвечает на вопрос «кого? что?»', в:1, варианты:['родительный','винительный'], раз:'Вижу кого? что?'},
    {q:'Слово-помощник родительного падежа:', в:0, варианты:['нет','вижу'], раз:'Нет кого? чего?'}
  ]};
  const Т2 = { имя:'Определи падеж', задания:[
    {q:'«Юнга подошёл к мачте» — мачте?', в:1, варианты:['предложный','дательный'], раз:'К чему? — Д.'},
    {q:'«Штурман доволен ветром» — ветром?', в:0, варианты:['творительный','дательный'], раз:'Доволен чем? — Т.'},
    {q:'«На палубе тихо» — на палубе?', в:1, варианты:['дательный','предложный'], раз:'На чём? — П.'},
    {q:'«У капитана трубка» — у капитана?', в:0, варианты:['родительный','винительный'], раз:'У кого? — Р.'}
  ]};
  const Т3 = { имя:'Ловушки', задания:[
    {q:'«Ветер наполнил парус» — парус?', в:1, варианты:['именительный','винительный'], раз:'Наполнил что? (вижу) — В.'},
    {q:'«Парус наполнился ветром» — парус?', в:0, варианты:['именительный','винительный'], раз:'Парус — подлежащее, И.'},
    {q:'«Нет кенгуру» — склоняется ли «кенгуру»?', в:1, варианты:['да','нет'], раз:'Несклоняемое слово.'},
    {q:'«Думаю о море» — может ли П. быть без предлога?', в:1, варианты:['да','нет'], раз:'П. — всегда с предлогом.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r869T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r869TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L869 = {
    id: ID,
    title: 'Склонение существительных',
    ico: '🧭',
    src: 'Русский язык · 4 класс · Имя существительное', subj: 'rus',
    explain: [
      'Роза падежей: слово «корабль» отвечает на шесть вопросов и меняет окончание. Изменение по падежам — склонение.',
      'Вопросы падежей: И. кто? что?, Р. кого? чего?, Д. кому? чему?, В. кого? что?, Т. кем? чем?, П. о ком? о чём?',
      'Падеж определяют вопросом от главного слова: к острову — Д., закатом — Т., о звёздах — П.',
      'И. и В. различают словами «есть» и «вижу»: парус надулся — И., чинит парус — В.',
      'У одушевлённых Р. и В. совпадают: ждёт капитана — вижу кого? — В.',
      'Предложный падеж всегда с предлогом: о море, в гавани.',
      'При склонении меняется окончание: волна, волны, волне, волну, волной, о волне.',
      'Несклоняемые слова: какаду, пальто, кино — во всех падежах одинаковые.',
      '«Вышел из гавани» — из чего? — родительный.',
      'Итог: задай вопрос — узнаешь падеж.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: вопросы падежей.',
      'Тренажёр 2: определи падеж.',
      'Тренажёр 3: ловушки.'
    ],
    check: {
      q: 'На какой вопрос отвечает родительный падеж?',
      choices: ['кому? чему?','кого? чего?','кем? чем?'],
      ans: 1,
      exp: 'Нет кого? чего? — родительный.'
    },
    tasks: [
      { q:'Сколько падежей в русском языке?', kind:'unit', ans:6, tol:0,
        hints:['И, Р, Д, В, Т, П.'], sol:'6.' },
      { q:'«Матросы любуются закатом» — в каком падеже «закатом»?', kind:'choice', choices:['дательный','творительный','предложный'], ans:1, tol:0,
        hints:['Любуются чем?'], sol:'Творительный.' },
      { q:'Какой падеж всегда с предлогом?', kind:'choice', choices:['родительный','предложный','винительный'], ans:1, tol:0,
        hints:['Название подсказывает.'], sol:'Предложный.' }
    ]
  };

  function render(el){
    if(!window.РМ){ el.innerHTML=''; return; }
    css();
    const s = S();
    const step = Math.max(0, Math.min(L869.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    s.былоУгол1=null;
    const ЗАГОЛОВКИ={1:'Роза падежей',2:'Вопросы какаду',3:'Три строки',4:'Есть или вижу',5:'Нет или вижу',
      6:'Падеж с предлогом',7:'Что меняется',8:'Несклоняемый какаду',9:'Запись в журнале',10:'Открытое море',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l869" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Склонение'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r869Пад=(i)=>{ const s=S(); const был=s.пад1==null?0:s.пад1; s.былоУгол1=УГОЛ(был); s.пад1=i;
    const в=Array.isArray(s.вид1)?s.вид1.slice():[0]; if(!в.includes(i)) в.push(i); s.вид1=в; chRender(0); };
  window.r869Три=(i,j)=>{ const s=S(); const р=Object.assign({},s.реш3||{}); р[i]=j; s.реш3=р;
    if(ТРИ.every((x,k)=>р[k]===x.верно)) s.дело_падежи=true; chRender(0); };
  window.r869Отв=(f,к)=>{ const s=S(); s['ответ'+f]=к; if(f===1&&к===0) s.дело_роза=true; if(f===9&&к===0) s.дело_журнал=true; chRender(0); };
  window.r869Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r869Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r869T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r869TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L869; else arr.push(L869); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.RU869={render:render, L:L869};
})();
