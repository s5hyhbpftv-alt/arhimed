/* ============ РУССКИЙ ЯЗЫК · УРОК 868 · «ТЕКСТ: НАЧАЛО, СЕРЕДИНА, КОНЕЦ» ============
   3 класс, «Текст». Была заглушкой в soon_lessons.js — теперь урок.
   Продолжение 861–864: «Чайка» идёт вдоль берега дальше.
   Рисунки — общая библиотека моря MVP/data/ris_more.js (window.РМ).

   СЮЖЕТ. «Письмо в бутылке». Волна выносит на песок бутылку. Внутри письмо,
   но в воде бумага порвалась на полоски — рассказ о том, как дельфин спас
   лодку, рассыпался. Юнга собирает его по порядку, подбирает заголовок,
   находит главную мысль, части текста и план — и пишет ответ.

   РУКАМИ: полоски письма — касаешься по порядку; верная ложится в письмо на
   своё место, неверная вздрагивает, и подсказка объясняет, что должно идти
   раньше; части текста — каждой полоске назначаешь «начало», «основная
   часть» или «концовка».

   ПРОВЕРЕНО ПО ПРОГРАММЕ 3 КЛАССА (Канакина, Горецкий):
     текст — несколько предложений, связанных по смыслу и расположенных по
     порядку; у текста есть тема (о чём) и главная мысль (что хотел сказать
     автор); заголовок отражает тему или главную мысль; части текста —
     начало (вступление), основная часть, концовка (заключение); каждая
     часть пишется с красной строки — это абзац; типы текстов:
     повествование (что было), описание (какой), рассуждение (почему);
     лишнее предложение не связано с темой; план — порядок частей.
     Рассказ: 1 утро, выход в море; 2 ветер и скалы; 3 дельфин подталкивает;
     4 вывел в бухту; 5 вывод «в море есть верный друг». */
(function(){
  'use strict';

  const ID = 868;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f', ОБВОД='#33291e';
  const ЧЕРНИЛА='#2e2416';

  const ДЕЛА = [
    {ключ:'письмо', имя:'Собрать письмо по порядку', итог:'5 полосок'},
    {ключ:'части',  имя:'Найти части текста',        итог:'начало · конец'},
    {ключ:'план',   имя:'Выбрать план',               итог:'4 пункта'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const РАССКАЗ = [
    'Однажды утром мы с дедушкой вышли в море на старой лодке.',
    'Вдруг налетел ветер, и лодку понесло к острым скалам.',
    'Тут у борта показался дельфин и стал подталкивать лодку носом.',
    'Он вывел нас в тихую бухту, где волны были совсем маленькие.',
    'С тех пор дедушка говорит, что в море у нас есть верный друг.'
  ];
  const КОРОТКО = ['Однажды утром…','Вдруг налетел ветер…','Тут показался дельфин…','Он вывел нас в бухту…','С тех пор дедушка говорит…'];
  const ПОРЯДОК_КНОПОК = [3,0,4,2,1];
  const ПОДСКАЗКИ = [
    'Рассказ начинается с того, <b>кто, где и когда</b>: «Однажды утром мы с дедушкой вышли в море…» — это начало.',
    'Что случилось сначала? Сперва приходит <b>беда</b> — ветер и скалы, а спасение потом.',
    'Кто пришёл на помощь? Сначала <b>появляется дельфин</b>, а уж потом ведёт лодку в бухту.',
    'Спасение должно <b>закончиться</b> — лодка в тихой бухте, — прежде чем делать вывод.'
  ];
  const ЧАСТИ = [
    {т:'Однажды утром мы с дедушкой вышли в море…', верно:0},
    {т:'Вдруг налетел ветер… показался дельфин… вывел нас в бухту…', верно:1},
    {т:'С тех пор дедушка говорит, что в море у нас есть верный друг.', верно:2}
  ];
  const ИМЕНА_ЧАСТЕЙ = ['начало','основная часть','концовка'];

  const CSS=`
  #lvis .s6.l868{gap:14px}
  #lvis .s6.l868 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l868 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l868 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l868 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l868 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l868 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l868 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l868 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l868 .карт .текст b{color:${GOLD}}
  #lvis .s6.l868 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l868 .правило b{color:${GOLD}}
  #lvis .s6.l868 .журнал{width:100%;padding:12px 14px;border-radius:6px 16px 16px 6px;
    background:linear-gradient(90deg,#efe2c0,#dcc79a);border-left:7px solid #7a4a24;display:flex;flex-direction:column;gap:7px;color:${ЧЕРНИЛА}}
  #lvis .s6.l868 .журнал .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:#6a4a24}
  #lvis .s6.l868 .журнал .шапка b{font-size:18px;letter-spacing:0;text-transform:none;color:#7a2a10;font-family:Georgia,serif}
  #lvis .s6.l868 .журнал .шапка b.готово{color:#1a6a3a}
  #lvis .s6.l868 .журнал ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l868 .журнал li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:#6a5236}
  #lvis .s6.l868 .журнал li i{flex:0 0 22px;width:22px;height:22px;border-radius:50%;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;border:1.5px solid #a88a5a;color:#8a6a3a}
  #lvis .s6.l868 .журнал li.есть{color:${ЧЕРНИЛА}}
  #lvis .s6.l868 .журнал li.есть i{border-color:#1a4a6a;background:#2a6a9a;color:#fff}
  #lvis .s6.l868 .журнал li span{margin-left:auto;white-space:nowrap;font-size:14px;color:#8a6a3a;font-family:Georgia,serif}
  #lvis .s6.l868 .журнал li.есть span{color:#1a6a3a;font-weight:700}
  #lvis .s6.l868 .буйки{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;width:100%}
  #lvis .s6.l868 .буйки button{min-height:56px;border-radius:28px;cursor:pointer;font:inherit;font-size:19px;font-weight:700;
    font-family:Georgia,serif;border:2px solid #e86a5a;background:linear-gradient(180deg,#fff6ea,#f0dcc0);color:${ЧЕРНИЛА};
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),opacity 200ms}
  #lvis .s6.l868 .буйки button:active{transform:scale(.96)}
  #lvis .s6.l868 .буйки button.пойман{opacity:.35;border-color:${GREEN};text-decoration:line-through}
  #lvis .s6.l868 .буйки button.мимо{border-color:${RED};animation:l868нет 360ms cubic-bezier(.23,1,.32,1)}
  @keyframes l868нет{0%,100%{transform:none}25%{transform:translateX(-6px)}75%{transform:translateX(6px)}}
  #lvis .s6.l868 .ряд{display:grid;gap:8px;width:100%}
  #lvis .s6.l868 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:18px;font-weight:700;
    border:1.5px solid rgba(127,209,255,.5);background:rgba(127,209,255,.1);color:${ИНК};padding:6px 4px;font-family:Georgia,serif;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l868 .ряд button:active{transform:translateY(2px)}
  #lvis .s6.l868 .ряд button.вкл{border-color:${GOLD};background:rgba(255,215,106,.22);color:${GOLD}}
  #lvis .s6.l868 .случай{width:100%;display:flex;flex-direction:column;gap:6px}
  #lvis .s6.l868 .случай .что{font-size:19px;color:var(--ink);font-family:Georgia,serif}
  #lvis .s6.l868 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l868 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:17px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l868 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l868 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l868 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l868 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-size:18px}
  #lvis .s6.l868 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-size:18px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l868 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l868 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l868 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l868 .уровни .точка.сейчас{background:${GOLD};animation:l868dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l868dot{0%,100%{opacity:1}50%{opacity:.55}}
  #lvis .s6.l868 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l868{-webkit-text-size-adjust:100%}
  #lvis .s6.l868 [data-anim]{animation:l868rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l868rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  #lvis .s6.l868 .полоски{display:flex;flex-direction:column;gap:8px;width:100%}
  #lvis .s6.l868 .полоски button{min-height:56px;padding:10px 16px;border-radius:6px 14px 8px 12px;cursor:pointer;font:inherit;font-size:16px;line-height:1.35;text-align:left;
    font-family:Georgia,serif;color:#2e2416;border:1.5px solid #a88a5a;background:linear-gradient(180deg,#fbf5e3,#e8d6aa);
    clip-path:polygon(0 6%,3% 0,8% 5%,14% 0,100% 0,100% 90%,97% 100%,92% 94%,86% 100%,0 100%);
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;transition:transform 140ms cubic-bezier(.23,1,.32,1),opacity 200ms}
  #lvis .s6.l868 .полоски button:active{transform:scale(.98)}
  #lvis .s6.l868 .полоски button.пойман{opacity:.35;text-decoration:line-through}
  #lvis .s6.l868 .полоски button.мимо{background:linear-gradient(180deg,#ffe0d6,#f0c0b0);animation:l868нет 360ms cubic-bezier(.23,1,.32,1)}
  @keyframes l868нет{0%,100%{transform:none}25%{transform:translateX(-6px)}75%{transform:translateX(6px)}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l868 [data-anim]{animation:none!important}
    #lvis .s6.l868 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l868 button{transition:none!important;animation:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l868-style');
      if(!s){ s=document.createElement('style'); s.id='l868-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Судовой журнал</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,f) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, 'r868Отв('+f+','+к+')')).join('')}</div>`;
  const ЖУРНАЛ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'журнал',
      `<div class="шапка"><span>Письмо в бутылке</span><b class="${всё?'готово':''}">${
        всё?'ответ отправлен':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}<span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.45;0.45;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const сдвигРаз = (из,в,длит,задержка) => ДВИЖ ?
    `<animateTransform attributeName="transform" type="translate" from="${из}" to="${в}" dur="${длит}" begin="${(задержка||0).toFixed(2)}s"
       fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  const Р = () => window.РМ;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <filter id="c868-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="1.5" dy="2" stdDeviation="1.6" flood-color="#0b1c2a" flood-opacity=".45"/>
      </filter>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${Р().defs()}${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c868-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(14,26,20,.92)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  /* пляж: небо, море, песок с пеной */
  const пляж = (Н,линия,песок) => { const М=Р();
    return `${М.небо(336,линия,{солнце:[296,30,10],облака:[[70,26,0.55,10],[210,40,0.42,-6]]})}
      ${М.море(линия,Н-линия,336,{дорожка:296})}
      <path d="M0 ${песок} Q120 ${песок-14} 220 ${песок-4} T336 ${песок-10} V${Н} H0 Z" fill="url(#рм-песок)"/>
      <path d="M0 ${песок+2} Q120 ${песок-12} 220 ${песок-2} T336 ${песок-8}" stroke="#fff" stroke-width="2" fill="none" stroke-dasharray="7 5" opacity=".85">${анЛин('stroke-dashoffset','0;12','2.4s')}</path>
      ${[[40,песок+24],[260,песок+30],[180,песок+40]].map(([x,y])=>`<path d="M${x} ${y} q3 -5 6 0 q3 -5 6 0" stroke="#e8c898" stroke-width="1.4" fill="none" data-декор="1"/>`).join('')}`; };
  /* разорванная полоска бумаги */
  const полоска = (x,y,ш,текст,цвет) => `<g filter="url(#c868-тень)">
      <path d="M${x} ${y} l6 -3 l8 3 l7 -2 H${x+ш-8} l6 3 l2 12 l-5 3 l-6 -2 H${x+14} l-7 3 l-7 -3 Z" fill="url(#рм-бумага)" stroke="#a88a5a" stroke-width=".8"/></g>
      ${т(x+10,y+11,текст,11,цвет||ЧЕРНИЛА,true,'start')}`;

  /* ================= КАДРЫ ================= */

  /* 1. Письмо в бутылке */
  function F1(s){
    const Н=330, М=Р(), лежит=Array.isArray(s.пол1)?s.пол1:[], ош=s.ош1, свеж=s.свеж1, всё=лежит.length===5;
    const ждём=лежит.length;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Утром волна вынесла на песок <b>бутылку с письмом</b>. Юнга вытащил пробку — а бумага в воде порвалась на полоски, и рассказ рассыпался. Собери его: касайся полосок <b>по порядку</b>, от начала до конца.') +
      `<div class="pic">${свг(`
        ${пляж(Н,96,150)}
        ${М.дельфин(220,120,0.5,{прыжок:true})}
        ${М.бутылка(58,188,0.9,{открыта:true,угол:-14})}
        ${М.юнга(30,Н-150,0.52,{поза:'стоит',взгляд:1})}
        ${М.лист(84,166,244,Н-180,{})}
        ${т(206,190,всё?'Как дельфин спас лодку':'письмо',13,'#7a4a24',true)}
        ${лежит.map((i,k)=>`<g>${свеж===i&&ДВИЖ?сдвигРаз('0 30','0 0','0.5s',0):''}${т(96,216+k*24,(k+1)+'. '+КОРОТКО[i],13,ЧЕРНИЛА,false,'start')}</g>`).join('')}
        ${Array.from({length:5-лежит.length},(_,k)=>`<line x1="96" y1="${220+(лежит.length+k)*24}" x2="316" y2="${220+(лежит.length+k)*24}" stroke="#c8b08a" stroke-width="1" stroke-dasharray="4 4" data-декор="1"/>`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="полоски">${ПОРЯДОК_КНОПОК.map((i,k)=>BTN(3+k,лежит.includes(i)?'пойман':(ош===i?'мимо':''),РАССКАЗ[i],'r868Полоска('+i+')')).join('')}</div>` +
      (всё ? РАЗБОР(true,'Письмо собрано: <b>начало</b> (кто, где, когда), <b>события</b> по порядку (беда — помощь — спасение) и <b>концовка</b> (вывод). Поменяй полоски местами — и рассказ потеряет смысл.') :
        ош!=null ? РАЗБОР(false,ПОДСКАЗКИ[Math.min(ждём,3)]) :
        СКАЗ('Подсказка','Сначала найди, с чего всё началось: кто, где и когда.')) +
      (всё ? ПРАВИЛО('<b>Текст</b> — это несколько предложений, связанных по смыслу и расставленных <b>по порядку</b>. Переставишь — смысл рассыплется.') : '');
  }

  /* 2. Текст или набор предложений */
  function F2(s){
    const Н=260, в=s.ответ2, ок=в===1, М=Р();
    const свиток=(x,строки,свет,цв)=>`${М.лист(x,40,150,170,{})}${свет?`<rect x="${x-3}" y="38" width="156" height="176" rx="6" fill="none" stroke="${цв}" stroke-width="2.4"/>`:''}
      ${строки.map((с,k)=>т(x+12,70+k*22,с,12,ЧЕРНИЛА,false,'start')).join('')}`;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('В бутылке было ещё два листка. Юнга читает оба. Капитан спрашивает: «Какой из них — <b>текст</b>, а какой — просто предложения вперемешку?»') +
      `<div class="pic">${свг(`
        ${Р().доски(0,0,336,Н,true)}
        ${свиток(12,['Чайка кричит.','Я люблю кашу.','Завтра среда.','Якорь тяжёлый.','Кот спит.'],ок,RED)}
        ${свиток(174,['Чайка кружит','над лодкой.','Она ждёт,','когда рыбак','бросит ей рыбку.'],ок,GREEN)}
        ${ок?подпись(87,Н-16,'набор предложений',RED,12)+подпись(249,Н-16,'текст',GREEN,12):т(87,Н-16,'листок 1',13,МУТ,true)+т(249,Н-16,'листок 2',13,МУТ,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['листок 1','листок 2'],1,в,2) +
      (в==null ? СКАЗ('Вопрос','Какой листок — текст?') :
        РАЗБОР(ок,['В первом листке предложения ни о чём общем: чайка, каша, среда, кот. Это <b>не текст</b>. Во втором все предложения — о чайке и связаны.',
          'Во втором листке все предложения — <b>об одном</b> (о чайке) и связаны по смыслу. Это текст.'][в])) +
      (ок ? ПРАВИЛО('В тексте все предложения говорят <b>об одном</b> — у текста есть <b>тема</b>. Набор несвязанных предложений — не текст.') : '');
  }

  /* 3. Заголовок */
  function F3(s){
    const Н=260, в=s.ответ3, ок=в===0, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('У письма нет названия. Юнга хочет подписать его, чтобы по заголовку сразу было ясно, <b>о чём</b> рассказ. Какой заголовок подходит лучше всего?') +
      `<div class="pic">${свг(`
        ${М.небо(336,110,{солнце:[40,30,10],облака:[[240,34,0.5,-8]]})}
        ${М.море(110,Н-110,336,{})}
        ${М.утёс(300,112,0.8)}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 0;26 0;0 0" dur="6s" repeatCount="indefinite"/>`:''}${М.лодка(120,196,0.7,{})}</g>
        ${М.дельфин(200,206,0.6,{прыжок:true})}
        ${подпись(168,64,ок?'Как дельфин спас лодку':'заголовок: ?',ок?GREEN:ИНК,14)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Как дельфин спас лодку','Дедушка','Море бывает синим'],0,в,3) +
      (в==null ? СКАЗ('Вопрос','Какой заголовок лучше?') :
        РАЗБОР(ок,['Заголовок называет <b>тему</b> — о чём весь рассказ: дельфин спас лодку.',
          '«Дедушка» — лишь один из героев. Рассказ о другом — о том, <b>как дельфин спас лодку</b>.',
          'Про синее море в письме ничего нет. Заголовок должен говорить, <b>о чём текст</b>.'][в])) +
      (ок ? ПРАВИЛО('<b>Заголовок</b> коротко говорит, о чём текст — его <b>тему</b> — или что в нём главное.') : '');
  }

  /* 4. Главная мысль */
  function F4(s){
    const Н=250, в=s.ответ4, ок=в===1, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Тема письма — как дельфин спас лодку. А что автор хотел этим сказать? Это называют <b>главной мыслью</b>. Её часто прячут в последнем предложении.') +
      `<div class="pic">${свг(`
        ${М.небо(336,110,{закат:true,солнце:[80,96,14]})}
        ${М.море(110,Н-110,336,{дорожка:80})}
        ${М.лодка(170,180,0.72,{})}
        ${М.дельфин(260,190,0.5,{прыжок:true})}
        ${ок?подпись(168,Н-14,'в море у нас есть верный друг',GREEN,13):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Ветер бывает очень сильным','В море у нас есть верный друг','Лодка у дедушки старая'],1,в,4) +
      (в==null ? СКАЗ('Вопрос','Какая главная мысль письма?') :
        РАЗБОР(ок,['Про ветер в письме сказано, но это лишь одно событие. Главная мысль — вывод всего рассказа: <b>в море у нас есть верный друг</b>.',
          'Верно: к этому выводу ведёт весь рассказ, и он стоит в <b>концовке</b>.',
          'Старая лодка — деталь. Главное, ради чего написано письмо: <b>в море у нас есть верный друг</b>.'][в])) +
      (ок ? ПРАВИЛО('<b>Тема</b> — о чём текст. <b>Главная мысль</b> — что автор хотел сказать; её часто находят в конце.') : '');
  }

  /* 5. Абзацы */
  function F5(s){
    const Н=270, в=s.ответ5, ок=в===2, М=Р();
    const абз=[[4,true],[3,false],[2,true]];
    let y=56, строки='';
    абз.forEach(([n],a)=>{ for(let k=0;k<n;k++){ const отступ=k===0, x0=отступ?60:36, дл=k===n-1?150:254-(отступ?24:0);
        строки+=`<rect x="${x0}" y="${y}" width="${дл}" height="7" rx="3.5" fill="#8a6a3a" opacity=".55"/>`;
        if(отступ&&ок) строки+=`<g>${ДВИЖ?`<animate attributeName="opacity" from="0" to="1" dur="0.5s" begin="${(a*0.3).toFixed(1)}s" fill="freeze"/>`:''}<path d="M24 ${y+4} h26 l-6 -5 M50 ${y+4} l-6 5" stroke="#b85a10" stroke-width="2" fill="none"/></g>`;
        y+=16; } y+=10; });
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Дедушка из письма пишет аккуратно: каждую новую часть он начинает <b>с красной строки</b> — с отступа от края. Такой кусок текста называют <b>абзацем</b>. Сколько абзацев на листке?') +
      `<div class="pic">${свг(`
        ${Р().доски(0,0,336,Н,true)}
        ${М.лист(14,26,308,Н-44,{})}
        ${строки}
        ${ок?подпись(250,Н-30,'три красные строки',GREEN,12):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['9','1','3'],2,в,5) +
      (в==null ? СКАЗ('Вопрос','Сколько абзацев на листке?') :
        РАЗБОР(ок,['9 — это число строк. А абзацев столько, сколько <b>красных строк</b> — отступов: <b>три</b>.',
          'Посмотри на левый край: отступ встречается <b>трижды</b>. Абзацев три.',
          'Три отступа — три абзаца: начало, основная часть, концовка.'][в])) +
      (ок ? ПРАВИЛО('<b>Абзац</b> — часть текста, которая начинается с <b>красной строки</b>. Обычно каждая часть текста — отдельный абзац.') : '');
  }

  /* 6. Части текста */
  function F6(s){
    const Н=240, М=Р(), реш=s.реш6||{}, все=ЧАСТИ.every((_,i)=>реш[i]!=null), верно=все&&ЧАСТИ.every((ч,i)=>реш[i]===ч.верно);
    const вагоны=[['начало','#e8b878'],['основная часть','#8fd2f0'],['концовка','#8fd1a8']];
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Капитан сравнивает рассказ с поездом из трёх вагонов: <b>начало</b> — кто, где, когда; <b>основная часть</b> — что случилось; <b>концовка</b> — чем кончилось и какой вывод. Назови каждую часть письма.') +
      `<div class="pic">${свг(`
        ${М.небо(336,90,{облака:[[80,30,0.5,8]]})}
        ${М.море(90,Н-90,336,{})}
        ${вагоны.map(([имя,цв],k)=>{ const x=10+k*108, есть=верно; return `<g filter="url(#c868-тень)"><rect x="${x}" y="130" width="100" height="56" rx="8" fill="${есть?цв:'url(#рм-бумага)'}" stroke="${ОБВОД}" stroke-width="1.2"/></g>
          ${т(x+50,158,имя,12,ЧЕРНИЛА,true)}${т(x+50,176,['кто, где','события','вывод'][k],11,'#5a4a34',false)}
          ${k<2?`<rect x="${x+100}" y="154" width="8" height="4" fill="${ОБВОД}"/>`:''}`; }).join('')}
        ${[0,1,2].map(k=>`<circle cx="${40+k*108}" cy="192" r="7" fill="#3a3e46" stroke="${ОБВОД}"/><circle cx="${88+k*108}" cy="192" r="7" fill="#3a3e46" stroke="${ОБВОД}"/>`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      ЧАСТИ.map((ч,i)=>`<div class="случай">${A(3+i,'что','«'+ч.т+'»')}<div class="ask три">${ИМЕНА_ЧАСТЕЙ.map((v,j)=>
        BTN(4+i,реш[i]===j?(j===ч.верно?'hit':'miss'):'',v,'r868Часть('+i+','+j+')')).join('')}</div></div>`).join('') +
      (!все ? СКАЗ('Подсказка','Начало знакомит, основная часть рассказывает события, концовка подводит итог.') :
        РАЗБОР(верно, верно ? 'Утро и лодка — <b>начало</b>; ветер, дельфин, бухта — <b>основная часть</b>; «в море у нас есть верный друг» — <b>концовка</b>.' :
          'Проверь: кто, где, когда — начало; события — основная часть; вывод — концовка.')) +
      (верно ? ПРАВИЛО('У текста три части: <b>начало</b> (вступление), <b>основная часть</b> и <b>концовка</b> (заключение).') : '');
  }

  /* 7. Лишнее предложение */
  function F7(s){
    const Н=240, в=s.ответ7, ок=в===2, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга пишет заметку о дельфинах для журнала, но отвлёкся — и одно предложение в заметке <b>лишнее</b>, не по теме. Найди его.') +
      `<div class="pic">${свг(`
        <g><rect x="0" y="0" width="336" height="${Н}" fill="url(#рм-море)"/></g>
        ${[0,1,2,3].map(k=>`<path d="M${k*90-30} 0 l40 ${Н}" stroke="#e8f6ff" stroke-width="18" opacity=".05" data-декор="1"/>`).join('')}
        <g>${М.качать('0 0;30 -6;0 0','6s')}${М.дельфин(110,90,0.9,{})}</g>
        <g>${М.качать('0 0;-24 6;0 0','7s')}${М.дельфин(240,150,0.6,{влево:true})}</g>
        ${[0,1,2].map(k=>`<circle cx="${60+k*8}" cy="170" r="${2+k}" fill="none" stroke="#e8f6ff" stroke-width="1" opacity=".7">${анЛин('cy','170;40',(2.4+k*0.5).toFixed(1)+'s')}${анЛин('opacity','0.8;0','2.6s')}</circle>`).join('')}
        ${ок?подпись(168,Н-14,'мороженое — не про дельфинов',RED,12):подпись(168,Н-14,'тема: дельфины',ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Дельфины живут в тёплых морях.','Они дышат воздухом и поднимаются к поверхности.','Я люблю мороженое.','Дельфины помогают друг другу.'],2,в,7) +
      (в==null ? СКАЗ('Вопрос','Какое предложение лишнее?') :
        РАЗБОР(ок,['Это о дельфинах — оно по теме. Лишнее — «<b>Я люблю мороженое</b>».','Дышат воздухом — тоже о дельфинах. Лишнее — «<b>Я люблю мороженое</b>».',
          'Верно: мороженое к дельфинам отношения не имеет. Остальные предложения — об одном.','Помогают друг другу — о дельфинах. Лишнее — «<b>Я люблю мороженое</b>».'][в])) +
      (ок ? ПРАВИЛО('В тексте каждое предложение — <b>по теме</b>. Предложение о другом нарушает связь, его убирают.') : '');
  }

  /* 8. Типы текста */
  function F8(s){
    const Н=240, в=s.ответ8, ок=в===0, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга написал о дельфине три коротких текста. Один <b>рассказывает</b>, что было. Другой <b>описывает</b>, какой дельфин. Третий <b>рассуждает</b>, почему. Какой из них — описание?') +
      `<div class="pic">${свг(`
        ${М.небо(336,100,{солнце:[296,30,10]})}
        ${М.море(100,Н-100,336,{})}
        ${М.дельфин(168,150,1.1,{прыжок:true})}
        ${[['какой?',60],['что было?',168],['почему?',276]].map(([t0,x],k)=>подпись(x,Н-14,t0,ок&&k===0?GREEN:ИНК,12)).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Дельфин серый, гладкий, с высоким плавником и умными глазами.','Дельфин подплыл к лодке, нырнул и выпрыгнул из воды.','Дельфина называют другом моряков, потому что он помогает людям.'],0,в,8) +
      (в==null ? СКАЗ('Вопрос','Какой текст — описание?') :
        РАЗБОР(ок,['<b>Описание</b> отвечает на вопрос <b>какой?</b>: серый, гладкий, с плавником.',
          'Это <b>повествование</b>: что было — подплыл, нырнул, выпрыгнул. Описание — про то, <b>какой</b> дельфин.',
          'Это <b>рассуждение</b>: почему — потому что помогает. Описание — про то, <b>какой</b> дельфин.'][в])) +
      (ок ? ПРАВИЛО('Типы текстов: <b>повествование</b> (что было?), <b>описание</b> (какой?), <b>рассуждение</b> (почему?).') : '');
  }

  /* 9. План */
  function F9(s){
    const Н=240, в=s.ответ9, ок=в===0, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Юнга хочет пересказать письмо капитану и составляет <b>план</b> — короткие пункты по порядку. Какой план подходит к рассказу?') +
      `<div class="pic">${свг(`
        ${Р().доски(0,0,336,Н,true)}
        ${М.лист(14,26,200,Н-44,{линии:4})}
        ${(ок?['1. В море.','2. Шторм.','3. Дельфин-спаситель.','4. Верный друг.']:['1. …','2. …','3. …','4. …']).map((p,k)=>т(30,70+k*36,p,14,ЧЕРНИЛА,true,'start')).join('')}
        ${М.юнга(276,Н-26,0.72,{поза:'пишет',шапка:false})}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['1. В море. 2. Шторм. 3. Дельфин-спаситель. 4. Верный друг.','1. Верный друг. 2. Шторм. 3. В море. 4. Дельфин.','1. Дельфин. 2. Дедушка. 3. Мороженое.'],0,в,9) +
      (в==null ? СКАЗ('Вопрос','Какой план подходит?') :
        РАЗБОР(ок,['Пункты идут в том же <b>порядке</b>, что и события рассказа, и называют главное в каждой части.',
          'Пункты перепутаны: вывод «верный друг» оказался в начале. План идёт <b>по порядку</b> событий.',
          'Мороженого в рассказе нет, и о шторме ни слова. План должен пересказывать <b>этот</b> текст по порядку.'][в])) +
      (ок ? ПРАВИЛО('<b>План</b> — короткие пункты, которые называют части текста <b>по порядку</b>. По плану легко пересказать текст.') : '');
  }

  /* 10. Ответное письмо */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=300, М=Р();
    return ЖУРНАЛ(s) +
      ЗАДАЧА(всё
        ? 'Юнга пишет ответ: с началом, основной частью и концовкой, каждую часть — с красной строки. Письмо в бутылку, пробку покрепче — и в море. Вдали из воды выпрыгивает дельфин, будто обещает доставить.'
        : 'Письмо ещё не собрано — вернись к делам в списке. Вот что получится в конце.') +
      `<div class="pic">${свг(`
        ${М.небо(336,120,{закат:true,солнце:[80,106,15],облака:[[240,40,0.6,10]]})}
        ${М.море(120,Н-120,336,{дорожка:80})}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 0;60 -20;120 0" dur="5s" repeatCount="indefinite"/>`:''}${М.бутылка(110,176,0.8,{угол:10})}</g>
        ${М.дельфин(250,170,0.6,{прыжок:true})}
        ${М.парусник(60,198,0.5,{имя:'Чайка'})}
        ${[['начало → основная часть → концовка',GOLD],['тема · главная мысль · заголовок',BLUE],['абзац — с красной строки',GREEN]].map(([t0,ц],i)=>`<g>${проявить('9s',0.1+i*0.18,0.16+i*0.18)}${подпись(168,Н-70+i*28,t0,ц,12)}</g>`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','<b>Текст</b> — предложения, связанные по смыслу и стоящие по порядку. У текста есть <b>тема</b> и <b>главная мысль</b>, заголовок называет тему. Части текста: <b>начало</b>, <b>основная часть</b>, <b>концовка</b>; каждая пишется с красной строки — это <b>абзац</b>. Тексты бывают повествованием, описанием и рассуждением. План — пункты по порядку частей.') +
      ПРАВИЛО('<b>Начало — середина — конец, и всё об одном.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Что такое текст?', варианты:[{т:'любые предложения подряд',ок:false},{т:'предложения, связанные по смыслу',ок:true}], разбор:'Все об одном и по порядку.' },
    { вопрос:'С чего начинается абзац?', варианты:[{т:'с красной строки',ок:true},{т:'с заголовка',ок:false}], разбор:'Отступ от края.' },
    { вопрос:'Где обычно главная мысль?', варианты:[{т:'в концовке',ок:true},{т:'в заголовке всегда',ок:false}], разбор:'Часто её дают в конце.' },
    { вопрос:'«Кит огромный, серый, с фонтаном» — это…', варианты:[{т:'повествование',ок:false},{т:'описание',ок:true}], разбор:'Какой? — описание.' },
    { вопрос:'Сколько частей у текста?', варианты:[{т:'три',ок:true},{т:'одна',ок:false}], разбор:'Начало, основная часть, концовка.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r868Reset()")}</div>` +
        ПРАВИЛО('<b>Начало — середина — конец, и всё об одном.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r868Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Текст или нет', задания:[
    {q:'«Идёт дождь. Лодка синяя. Я пью чай.» — это текст?', в:1, варианты:['да','нет'], раз:'Предложения ни о чём общем.'},
    {q:'«Пошёл дождь. Рыбаки спрятались под навес. Скоро дождь кончился.» — текст?', в:0, варианты:['да','нет'], раз:'Все об одном, по порядку.'},
    {q:'Что называет заголовок?', в:0, варианты:['тему текста','первое слово'], раз:'О чём текст.'},
    {q:'Главная мысль — это…', в:1, варианты:['о чём текст','что хотел сказать автор'], раз:'Тема — о чём, мысль — что сказать.'}
  ]};
  const Т2 = { имя:'Части и абзацы', задания:[
    {q:'«Жил-был старый рыбак» — какая часть?', в:0, варианты:['начало','концовка'], раз:'Знакомит с героем.'},
    {q:'«Так рыбак стал самым счастливым на свете» — какая часть?', в:1, варианты:['начало','концовка'], раз:'Итог.'},
    {q:'Новую часть текста пишут…', в:0, варианты:['с красной строки','с большой буквы в середине'], раз:'Абзац.'},
    {q:'Что идёт после начала?', в:1, варианты:['концовка','основная часть'], раз:'Начало — середина — конец.'}
  ]};
  const Т3 = { имя:'Тип текста', задания:[
    {q:'«Маяк высокий, белый, с красной крышей» — это…', в:1, варианты:['повествование','описание'], раз:'Какой?'},
    {q:'«Лодка отчалила, поплыла и скрылась» — это…', в:0, варианты:['повествование','рассуждение'], раз:'Что было?'},
    {q:'«Море солёное, потому что в нём растворены соли» — это…', в:1, варианты:['описание','рассуждение'], раз:'Почему?'},
    {q:'План — это…', в:0, варианты:['пункты по порядку частей','заголовок'], раз:'По нему пересказывают.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r868T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r868TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L868 = {
    id: ID,
    title: 'Текст: начало, середина, конец',
    ico: '📜',
    src: 'Русский язык · 3 класс · Текст', subj: 'rus',
    explain: [
      'Письмо в бутылке порвалось на полоски. Собираем рассказ по порядку: текст — предложения, связанные по смыслу и расставленные по порядку.',
      'Набор несвязанных предложений — не текст: в тексте все предложения об одном.',
      'Заголовок называет тему текста: «Как дельфин спас лодку».',
      'Главная мысль — что хотел сказать автор: в море у нас есть верный друг.',
      'Абзац начинается с красной строки: на листке три абзаца.',
      'Части текста: начало, основная часть, концовка.',
      'Лишнее предложение не по теме: «Я люблю мороженое» в заметке о дельфинах.',
      'Типы текстов: повествование, описание, рассуждение.',
      'План — пункты по порядку частей текста.',
      'Итог: начало — середина — конец, и всё об одном.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: текст или нет.',
      'Тренажёр 2: части и абзацы.',
      'Тренажёр 3: тип текста.'
    ],
    check: {
      q: 'Из каких частей состоит текст?',
      choices: ['начало, основная часть, концовка','заголовок и точка','слова и буквы'],
      ans: 0,
      exp: 'Начало, основная часть и концовка.'
    },
    tasks: [
      { q:'Что такое текст?', kind:'choice', choices:['любые предложения','предложения, связанные по смыслу и по порядку','одно длинное слово'], ans:1, tol:0,
        hints:['Все предложения — об одном.'], sol:'Предложения, связанные по смыслу.' },
      { q:'С чего начинается новый абзац?', kind:'choice', choices:['с красной строки','с восклицательного знака','с заголовка'], ans:0, tol:0,
        hints:['Отступ от края.'], sol:'С красной строки.' },
      { q:'Какой тип текста отвечает на вопрос «какой?»', kind:'choice', choices:['повествование','описание','рассуждение'], ans:1, tol:0,
        hints:['Признаки предмета.'], sol:'Описание.' }
    ]
  };

  function render(el){
    if(!window.РМ){ el.innerHTML=''; return; }
    css();
    const s = S();
    const step = Math.max(0, Math.min(L868.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    s.свеж1=null;
    const ЗАГОЛОВКИ={1:'Письмо в бутылке',2:'Текст или нет',3:'Заголовок',4:'Главная мысль',5:'Красная строка',
      6:'Три вагона',7:'Лишнее предложение',8:'Три текста о дельфине',9:'План пересказа',10:'Ответное письмо',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l868" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Текст'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r868Полоска=(i)=>{ const s=S(); const л=Array.isArray(s.пол1)?s.пол1.slice():[];
    if(л.includes(i)) return;
    if(i===л.length){ л.push(i); s.пол1=л; s.ош1=null; s.свеж1=i; if(л.length===5) s.дело_письмо=true; }
    else s.ош1=i;
    chRender(0); };
  window.r868Часть=(i,j)=>{ const s=S(); const р=Object.assign({},s.реш6||{}); р[i]=j; s.реш6=р;
    if(ЧАСТИ.every((ч,k)=>р[k]===ч.верно)) s.дело_части=true; chRender(0); };
  window.r868Отв=(f,к)=>{ const s=S(); s['ответ'+f]=к; if(f===9&&к===0) s.дело_план=true; chRender(0); };
  window.r868Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r868Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r868T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r868TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L868; else arr.push(L868); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.RU868={render:render, L:L868};
})();
