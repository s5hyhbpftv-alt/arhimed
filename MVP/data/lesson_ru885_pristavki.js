/* ============ РУССКИЙ ЯЗЫК · УРОК 885 · «ПРИСТАВКИ НА З И С» ============
   5 класс, «Орфография». Продолжение линии 877–884 (речь, текст, фонетика,
   орфоэпия, О и ё после шипящих, Ы и и после ц).

   ФОРМАТ — КАК У РУССКИХ УРОКОВ (vis_ru.js): window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT: .kicker, <h2>, .lead, .cap, .row,
   .card/.nm/.ex, .tag, .pair, .split/.word, .score. Без SVG-холста и эмодзи.
   Последний кадр — тренажёр на массиве game, кадров двенадцать.

   ПРАВИЛО (школьный курс, 5 класс):
     приставки БЕЗ-, ВОЗ- (ВЗ-), ИЗ-, НИЗ-, РАЗ- (РОЗ-), ЧЕРЕЗ- (ЧРЕЗ-)
     пишутся с З перед гласными и звонкими согласными и с С перед глухими:
       З: безоблачный (гласная), разбить (б), взлететь (л), измерить (м),
          воздать (д), низвергнуть (в), чрезмерный (м);
       С: бесконечный (к), бесполезный (п), вспомнить (п), истратить (т),
          раскрыть (к), восход (х), исчезнуть (ч), расчёт (ч), бесценный (ц);
     звонкие согласные: б, в, г, д, ж, з, л, м, н, р; глухие: п, ф, к, т, ш, с,
       х, ц, ч, щ;
     приставка С- (не СЗ-) ВСЕГДА пишется с С: сделать, сшить, сдать, сбегать;
     З и С в этих приставках не проверяют произношением корня — смотрят на
       первую букву корня.
   ЛОВУШКА: «здесь, здание, здоровье, зги» — здесь З входит в КОРЕНЬ, это не
     приставка, правило к ним не применяется. */
(function(){
  'use strict';

  const ID = 885;
  const R = window.RUKIT;
  if(!R){ return; }
  /* палитра набора задаётся локально в каждом уроке каркаса */
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const row   = (...xs)=>`<div class="row">${xs.join('')}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;

  /* ── тренажёр: десять слов по кругу ── */
  const GAME=[
    ['бе_облачный — какая буква?','з','Перед гласной пишем З: безоблачный.','с'],
    ['бе_конечный — какая буква?','с','Перед глухой К пишем С: бесконечный.','з'],
    ['ра_бить — какая буква?','з','Перед звонкой Б пишем З: разбить.','с'],
    ['ра_крыть — какая буква?','с','Перед глухой К пишем С: раскрыть.','з'],
    ['в_лететь — какая буква?','з','Перед звонкой Л пишем З: взлететь.','с'],
    ['в_помнить — какая буква?','с','Перед глухой П пишем С: вспомнить.','з'],
    ['и_мерить — какая буква?','з','Перед звонкой М пишем З: измерить.','с'],
    ['во_ход — какая буква?','с','Перед глухой Х пишем С: восход.','з'],
    ['_делать — какая буква?','с','Приставка С- всегда пишется с С: сделать.','з'],
    ['_дание — какая буква?','з','Здесь З в корне, приставки нет: здание.','с']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    return `${kick('12 · Тренажёр')}
      ${word(it[0],'вставь з или с')}
      ${готово ? (верно ? window.RUFEED.note('ok','верно',it[2]) : window.RUFEED.note('no','исправить',it[2]))
               : `<p class="verdict">Сначала найди приставку и посмотри на первую букву корня.</p>`}
      <div class="row">${варианты.map(о=>
        `<button type="button" class="chip" style="flex:1 1 40%;font-size:20px"
           onclick="ru885Game('${esc(о)}')">${esc(о)}</button>`).join('')}</div>
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее слово.':'Сначала приставка, потом буква.')}`;
  }

  window.RULESSON({id:ID, game:GAME, frames:[
    ()=>`${kick('01 · Правило')}
      ${h2('Приставка смотрит на соседний звук')}
      ${lead('В приставках без-, воз-, из-, низ-, раз-, через- пишется З, если корень начинается со звонкого звука или гласной, и С — если корень начинается с глухого.')}
      ${pair(card('перед звонким и гласным','З: безоблачный, разбить, взлететь'),card('перед глухим','С: бесконечный, раскрыть, вспомнить'))}
      ${cap('Слышим мы одно, а пишем по правилу: [бесконечный] — слышится [с], так и пишем.')}`,

    ()=>`${kick('02 · Наши приставки')}
      ${h2('Шесть приставок, которые меняют букву')}
      ${row(chip('без- / бес-'),chip('воз- / вос-'),chip('из- / ис-'),chip('низ- / нис-'),chip('раз- / рас-'),chip('через- / черес-'))}
      ${lead('Все они оканчиваются на З или С — и выбирают букву по первой букве корня.')}
      ${cap('Приставка С- в этот список не входит: она всегда пишется с С, об этом дальше.')}`,

    ()=>`${kick('03 · Звонкие и глухие')}
      ${h2('Как различить звуки')}
      ${cards([['звонкие','б, в, г, д, ж, з, л, м, н, р'],['глухие','п, ф, к, т, ш, с, х, ц, ч, щ'],['гласные','а, о, у, ы, э, и, е, ё, ю, я — тоже «звонкие» для правила']])}
      ${cap('Проверка простая: положи ладонь на горло. Звонкий звук «звенит», глухой — нет.')}`,

    ()=>`${kick('04 · Пишем З')}
      ${h2('Перед звонким и гласным')}
      ${lead('Безоблачный, разбить, взлететь, измерить, воздать, низвергнуть, чрезмерный.')}
      ${row(chip('без-облачный',GREEN),chip('ра-збить',GREEN),chip('в-злететь',GREEN),chip('и-змерить',GREEN))}
      ${pair(card('почему З','корень начинается с гласной или звонкого'),card('примеры','облако, бить, лететь, мерить'))}`,

    ()=>`${kick('05 · Пишем С')}
      ${h2('Перед глухим')}
      ${lead('Бесконечный, бесполезный, вспомнить, истратить, раскрыть, восход, исчезнуть, расчёт, бесценный.')}
      ${row(chip('бес-конечный',BLUE),chip('рас-крыть',BLUE),chip('вос-ход',BLUE),chip('ис-чезнуть',BLUE))}
      ${pair(card('почему С','корень начинается с глухого'),card('примеры','конец, крыть, ход, чезнуть'))}`,

    ()=>`${kick('06 · Особая приставка С-')}
      ${h2('С- всегда пишется с С')}
      ${lead('Есть приставка С-, и она никогда не превращается в З: сделать, сшить, сдать, сбегать, сжать.')}
      ${pair(card('верно','сделать, сшить, сдать, сбегать'),card('неверно','зделать, зшить, здать, збегать'))}
      ${cap('Слова «зделать» в русском языке нет — это самая частая ошибка в этой теме.')}`,

    ()=>`${kick('07 · Как решать')}
      ${h2('Порядок действий')}
      ${cards([['1. найди приставку','без-, воз-, из-, низ-, раз-, через- или С-'],['2. посмотри на корень','какая первая буква — гласная, звонкая или глухая'],['3. выбери букву','звонкая или гласная — З, глухая — С'],['4. проверь себя','приставка С- всегда с С']])}
      ${cap('Четыре шага — и буква найдена без словаря.')}`,

    ()=>`${kick('08 · Ловушка')}
      ${h2('З бывает и в корне')}
      ${lead('В словах здесь, здание, здоровье, зги никакой приставки нет: З входит в корень. Правило к ним не применяется.')}
      ${pair(card('приставка есть','бес-конечный, рас-крыть'),card('приставки нет','здесь, здание, здоровье, зги'))}
      ${cap('Прежде чем выбирать букву, убедись, что это действительно приставка.')}`,

    ()=>`${kick('09 · Проверь на словах')}
      ${h2('Расчёт и рассчитать')}
      ${lead('В словах расчёт и рассчитать корень начинается с глухого Ч и С, поэтому пишем С.')}
      ${pair(card('пишем С','расчёт, рассчитать, бессчётный'),card('подсказка','корень «чёт» начинается с глухого Ч'))}
      ${cap('Здесь важно не «услышать», а посмотреть на первую букву корня.')}`,

    ()=>`${kick('10 · Сравни пары')}
      ${h2('Одна приставка — две буквы')}
      ${cards([['безоблачный — бесконечный','гласная / глухая К'],['разбить — раскрыть','звонкая Б / глухая К'],['взлететь — вспомнить','звонкая Л / глухая П'],['измерить — истратить','звонкая М / глухая Т']])}
      ${cap('Одна и та же приставка пишется по-разному — и это не ошибка, а правило.')}`,

    ()=>`${kick('11 · В карман')}
      ${h2('Три опоры')}
      ${cards([['перед звонким — З','безоблачный, разбить, взлететь'],['перед глухим — С','бесконечный, раскрыть, вспомнить'],['приставка С- всегда С','сделать, сшить, сдать']])}
      ${cap('И помни: «здесь», «здание», «здоровье», «зги» — З в корне, правило не работает.')}`,

    (st)=>тренер(st)
  ]});

  /* ── карточка каталога ── */
  const L885={
    id:ID, title:'Приставки на з и с', ico:'🔤',
    src:'Русский язык · 5 класс · Орфография', subj:'rus', group:null,
    explain:[
      'Приставки без-, воз-, из-, низ-, раз-, через- оканчиваются то на З, то на С. Выбор зависит от первой буквы корня: перед гласной и звонкой согласной пишем З, перед глухой — С.',
      'Приставок, которые меняют букву, шесть: без- / бес-, воз- / вос-, из- / ис-, низ- / нис-, раз- / рас-, через- / черес-.',
      'Звонкие согласные: б, в, г, д, ж, з, л, м, н, р. Глухие: п, ф, к, т, ш, с, х, ц, ч, щ. Гласные для правила тоже «звонкие»: безоблачный.',
      'Перед звонким и гласным пишем З: безоблачный, разбить, взлететь, измерить, воздать, низвергнуть, чрезмерный.',
      'Перед глухим пишем С: бесконечный, бесполезный, вспомнить, истратить, раскрыть, восход, исчезнуть, расчёт, бесценный.',
      'Отдельно стоит приставка С-: она всегда пишется с С — сделать, сшить, сдать, сбегать, сжать. Слова «зделать» не существует.',
      'Порядок действий: найти приставку, посмотреть на первую букву корня, выбрать З или С, а потом проверить, не приставка ли это С-.',
      'Ловушка: в словах здесь, здание, здоровье, зги З входит в корень, приставки там нет, и правило к ним не применяется.',
      'Слова расчёт, рассчитать, бессчётный пишутся с С, потому что корень начинается с глухих Ч и С.',
      'Полезно сравнивать пары: безоблачный — бесконечный, разбить — раскрыть, взлететь — вспомнить, измерить — истратить. Одна приставка, а буквы разные.',
      'Три опоры: перед звонким — З, перед глухим — С, приставка С- всегда с С.',
      'Тренажёр: десять слов по кругу. Вставь пропущенную букву — и проверь себя по объяснению.'
    ],
    tasks:[
      { q:'Как пишется: бе_облачный?', kind:'choice',
        choices:['безоблачный','бесоблачный'], ans:0,
        hints:['Какая первая буква корня?','Это гласная О.'],
        sol:'З: безоблачный.' },
      { q:'Как пишется: бе_конечный?', kind:'choice',
        choices:['бесконечный','безконечный'], ans:0,
        hints:['Какая первая буква корня?','Это глухая К.'],
        sol:'С: бесконечный.' },
      { q:'Как пишется: _делать?', kind:'choice',
        choices:['сделать','зделать'], ans:0,
        hints:['Какая это приставка?','Приставка С-.'],
        sol:'Сделать: приставка С- всегда с С.' },
      { q:'«Здание» — какая буква и почему?', kind:'choice',
        choices:['З, это корень','С, это приставка'], ans:0,
        hints:['Есть ли здесь приставка?','Приставки нет.'],
        sol:'З в корне, правило не применяется.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){
      const м=arr.findIndex(L=>L && L.id===ID);
      if(м>=0) arr[м]=L885; else arr.push(L885);
    }
  }catch(e){}

  function игра(ответ){
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    const st=CHS[lk];
    if(st.gRes!=null){
      st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null;
    }else{
      const it=GAME[(st.gIdx||0)%GAME.length];
      st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1;
    }
    if(typeof chRender==='function') chRender(0);
  }
  window.ru885Game=function(о){ try{ игра(о); }catch(e){} };
})();
