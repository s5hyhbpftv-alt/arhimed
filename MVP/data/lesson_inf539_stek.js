/* ====== ИНФОРМАТИКА · УРОК 539 · «СТЕК И ОЧЕРЕДЬ: КАК МАШИНА СКЛАДЫВАЕТ ДЕЛА» ================
   5–6 класс, курс «С нуля». После списков (515) — два способа хранить дела,
   которые приходят по одному: стек и очередь.

   ГЛАВНАЯ МЫСЛЬ. Стек — последним пришёл, первым ушёл (тупиковый канал,
   стопка тарелок, «Отменить», «Назад»). Очередь — первым пришёл, первым ушёл
   (проходной канал, касса, принтер). Стеком проверяют скобки. Не всякий
   порядок выхода можно получить из стека.

   СЮЖЕТ. «Каналы Сиракуз». В гавани два канала: узкий тупиковый — корабли
   входят и выходят с одного конца — и проходной — входят с запада, выходят на
   восток. Смотритель каналов (ученик) пропускает корабли так, как требуют
   купцы, и узнаёт те же «каналы» внутри компьютера.

   РУКАМИ: кнопки «Впустить» и «Выпустить» — корабли вплывают и уходят с
   анимацией; задача собрать порядок выхода B, A, C; разложить четыре случая
   жизни по «стек» и «очередь».

   ВСЕ ОТВЕТЫ ПРОВЕРЕНЫ:
     тупик A, B, C → первым выйдет C; проходной A, B, C → первым A;
     B, A, C: впустить A, впустить B, выпустить B, выпустить A, впустить C, выпустить C;
     C, A, B из тупика нельзя: после C сверху B;
     +Д +О +М −М +Л, две отмены → ДОМ; очередь Дион, Клеон, Мирон, +Ника,
     обслужили двоих → первый Мирон; ( [ ] ) верно, ( [ ) ] нет;
     Главная → Карта → Корабли → Погода, «Назад» ×2 → Карта. */
(function(){
  'use strict';

  const ID = 539;
  const GOLD='#ffd76a', GREEN='#8fe0b0', RED='#ff8a78', ЯНТАРЬ='#f0b050', ЛАЗУРЬ='#8fd0f0';
  const ИНК='#f3f6f8', МУТ='#b0bccc', ЛИНИЯ='#4a5a70';

  const ДЕЛА = [
    {ключ:'тупик',   имя:'Понять тупиковый канал', итог:'первым — C'},
    {ключ:'порядок', имя:'Выпустить B, A, C',       итог:'готово'},
    {ключ:'разбор',  имя:'Разложить случаи',        итог:'4 из 4'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];
  const ПРИХОД = ['A','B','C'];
  const ЦВЕТ = {A:'#e86a50', B:'#e8c050', C:'#5ab0e8'};
  const ЦЕЛЬ3 = ['B','A','C'];
  const СЛУЧАИ = [
    {т:'Стопка тарелок в таверне', о:0},
    {т:'Очередь к пекарю', о:1},
    {т:'Кнопка «Отменить»', о:0},
    {т:'Листы, ждущие принтера', о:1}
  ];

  const CSS=`
  #lvis .s6.l539{gap:14px}
  #lvis .s6.l539 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l539 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l539 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#1a2a36,#101a24);border:1.5px solid var(--line)}
  #lvis .s6.l539 .карт.задача{border-color:${ЛАЗУРЬ}}
  #lvis .s6.l539 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l539 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l539 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l539 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l539 .карт .текст b{color:${GOLD}}
  #lvis .s6.l539 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l539 .правило b{color:${GOLD}}
  #lvis .s6.l539 .лист{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(143,208,240,.14),rgba(143,208,240,.04));
    border:1.5px solid rgba(143,208,240,.5);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l539 .лист .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l539 .лист .шапка b{font-size:18px;letter-spacing:0;text-transform:none;color:${ЛАЗУРЬ}}
  #lvis .s6.l539 .лист .шапка b.готово{color:${GREEN}}
  #lvis .s6.l539 .лист ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l539 .лист li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l539 .лист li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l539 .лист li.есть{color:${ИНК}}
  #lvis .s6.l539 .лист li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,224,176,.14)}
  #lvis .s6.l539 .лист li span{margin-left:auto;white-space:nowrap;font-size:14px;color:var(--mut)}
  #lvis .s6.l539 .лист li.есть span{color:${GREEN}}
  #lvis .s6.l539 .ряд{display:grid;gap:8px;width:100%}
  #lvis .s6.l539 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:16px;font-weight:700;
    border:1.5px solid rgba(143,208,240,.55);background:rgba(143,208,240,.12);color:${ИНК};padding:6px 4px;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l539 .ряд button:active{transform:translateY(2px);background:rgba(143,208,240,.26)}
  #lvis .s6.l539 .ряд button:disabled{opacity:.4;cursor:default}
  #lvis .s6.l539 .ряд button.вкл{border-color:${GOLD};background:rgba(255,215,106,.22)}
  #lvis .s6.l539 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l539 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l539 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l539 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l539 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l539 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-size:17px}
  #lvis .s6.l539 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-size:18px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l539 .случай{width:100%;display:flex;flex-direction:column;gap:6px}
  #lvis .s6.l539 .случай .что{font-size:17px;color:var(--ink)}
  #lvis .s6.l539 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l539 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l539 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l539 .уровни .точка.сейчас{background:${ЛАЗУРЬ};animation:l539dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l539dot{0%,100%{opacity:1}50%{opacity:.55}}
  #lvis .s6.l539 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l539{-webkit-text-size-adjust:100%}
  #lvis .s6.l539 [data-anim]{animation:l539rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l539rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l539 [data-anim]{animation:none!important}
    #lvis .s6.l539 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l539 button{transition:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l539-style');
      if(!s){ s=document.createElement('style'); s.id='l539-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,выкл) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}"${выкл?' disabled':''}>${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Каналы Сиракуз</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,f) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, 'r539Отв('+f+','+к+')')).join('')}</div>`;
  const ЛИСТ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'лист',
      `<div class="шапка"><span>Смотритель каналов</span><b class="${всё?'готово':''}">${
        всё?'гавань в порядке':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}<span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.45;0.45;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const вырасти = (длит,доля) => анК('opacity','0.15;0.15;1;1',длит,'0;'+кт(доля)+';'+кт(доля+0.06)+';1');
  const сдвигРаз = (из,в,длит,задержка) => ДВИЖ ?
    `<animateTransform attributeName="transform" type="translate" from="${из}" to="${в}" dur="${длит}" begin="${(задержка||0).toFixed(2)}s"
       fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';
  const анРаз = (имя,из,в,длит,задержка) => ДВИЖ ?
    `<animate attributeName="${имя}" from="${из}" to="${в}" dur="${длит}" begin="${(задержка||0).toFixed(2)}s" fill="freeze"/>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c539-земля" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c8b08a"/><stop offset="1" stop-color="#9a8260"/>
      </linearGradient>
      <linearGradient id="c539-вода" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#3a8ac0"/><stop offset="0.5" stop-color="#2a6aa0"/><stop offset="1" stop-color="#1c5080"/>
      </linearGradient>
      <linearGradient id="c539-камень" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#d8d0c0"/><stop offset="1" stop-color="#9a927e"/>
      </linearGradient>
      <linearGradient id="c539-дерево" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#a86a34"/><stop offset="1" stop-color="#5a3414"/>
      </linearGradient>
      <linearGradient id="c539-воск" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#3a3024"/><stop offset="1" stop-color="#241c14"/>
      </linearGradient>
      <linearGradient id="c539-экран" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#20304a"/><stop offset="1" stop-color="#101a2c"/>
      </linearGradient>
      <filter id="c539-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="0" dy="2" stdDeviation="1.8" flood-color="#000" flood-opacity=".5"/>
      </filter>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c539-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,16,24,.9)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  /* корабль сверху: корпус, палуба, парус-буква */
  const корабль = (x,y,б,м,вправо) => `<g transform="translate(${x} ${y}) scale(${м||1})" filter="url(#c539-тень)">
      <g transform="scale(${вправо?-1:1} 1)"><path d="M-28 0 q6 -13 26 -13 h14 q16 0 16 13 q0 13 -16 13 h-14 q-20 0 -26 -13z" fill="url(#c539-дерево)" stroke="#3a1c08" stroke-width="1"/>
      <path d="M-20 0 q5 -8 18 -8 h12 q10 0 10 8 q0 8 -10 8 h-12 q-13 0 -18 -8z" fill="#c89458"/></g>
      <circle cx="0" cy="0" r="9" fill="${ЦВЕТ[б]||'#ddd'}" stroke="#2a1a0a" stroke-width="1"/>
      ${т(0,4.5,б,12,'#1a1008',true)}</g>`;
  /* канал: тупик (закрыт справа) или проходной */
  const КАН = {x0:20, x1:316, y:118, в:30};
  function канал(Н,тупик){
    const {x0,x1,y,в}=КАН;
    const волны = [0,1,2,3].map(k=>`<path d="M${x0+10+k*72} ${y+(k%2?8:-8)} q10 -4 20 0 t20 0" stroke="#bfe4ff" stroke-width="1" fill="none" opacity=".45">${анЛин('opacity','0.45;0.1;0.45',(2+k*0.4).toFixed(1)+'s')}</path>`).join('');
    return `<rect x="0" y="0" width="336" height="${Н}" fill="url(#c539-земля)"/>
      ${[[40,30],[110,48],[190,26],[270,44]].map(([x,yy])=>`<circle cx="${x}" cy="${yy}" r="10" fill="#7a9a4a" opacity=".7" data-декор="1"/>`).join('')}
      <rect x="0" y="${y-в-12}" width="336" height="${2*в+24}" fill="url(#c539-камень)"/>
      <rect x="${тупик?0:0}" y="${y-в}" width="${тупик?x1:336}" height="${2*в}" fill="url(#c539-вода)"/>
      ${волны}
      ${тупик?`<rect x="${x1}" y="${y-в-12}" width="${336-x1}" height="${2*в+24}" fill="url(#c539-камень)"/><line x1="${x1}" y1="${y-в}" x2="${x1}" y2="${y+в}" stroke="#6a624e" stroke-width="3"/>`:''}
      <line x1="0" y1="${y-в}" x2="${тупик?x1:336}" y2="${y-в}" stroke="#6a624e" stroke-width="2"/>
      <line x1="0" y1="${y+в}" x2="${тупик?x1:336}" y2="${y+в}" stroke="#6a624e" stroke-width="2"/>`;
  }
  /* корабли в канале: тупик — последний у входа слева; очередь — первый у выхода справа */
  const место = (k) => КАН.x1-40-k*64;
  function флот(s,f,тупик,спис){
    const {y}=КАН, свежий=s.вплыл===f, ушёл=s.уплыл && s.уплыл.f===f ? s.уплыл.б : null;
    let g='';
    спис.forEach((б,k)=>{
      /* в тупике k = снизу (у стены); в очереди k = место от выхода */
      const x = место(k), новый = свежий && (тупик ? k===спис.length-1 : k===спис.length-1);
      const сдвиг = !тупик && ушёл && ДВИЖ;
      g+=`<g>${новый?сдвигРаз('-'+(x+40)+' 0','0 0','0.7s',0):(сдвиг?сдвигРаз('-64 0','0 0','0.6s',0):'')}${корабль(x,y,б,1,!тупик)}</g>`;
    });
    if(ушёл && ДВИЖ){
      const x = тупик ? место(спис.length) : место(0);
      g+=`<g>${сдвигРаз('0 0',(тупик?'-'+(x+60):'140')+' 0','0.8s',0)}${анРаз('opacity','1','0','0.8s',0)}${корабль(x,y,ушёл,1,!тупик)}</g>`;
    }
    return g;
  }
  const строка = (x,y,метка,спис,цвет) => т(x,y,метка+(спис.length?спис.join(', '):'—'),13,цвет||ИНК,true,'start');

  /* песочница каналов для кадров 1–3 */
  function песочница(s,f,тупик){
    const к='к'+f; if(!s[к]) s[к]={в:[],ушли:[],след:0};
    return s[к];
  }
  function кнопки(f,п,тупик){
    const есть = п.след<ПРИХОД.length;
    return `<div class="ряд" style="grid-template-columns:1fr 1fr 1fr">
      ${BTN(3,'',есть?'Впустить '+ПРИХОД[п.след]:'Все вошли','r539Впустить('+f+')',!есть)}
      ${BTN(4,'','Выпустить','r539Выпустить('+f+')',!п.в.length)}
      ${BTN(5,'','Сначала','r539Заново('+f+')')}</div>`;
  }
  const выход = (п,тупик) => тупик ? п.в[п.в.length-1] : п.в[0];

  /* ================= КАДРЫ ================= */

  /* 1. Тупиковый канал — стек */
  function F1(s){
    const Н=240, п=песочница(s,1,true), в=s.ответ1, ок=в===2, готов = п.ушли.length>=1 || (п.след>=3);
    return ЛИСТ(s) +
      ЗАДАЧА('Старый канал у складов — <b>тупик</b>: корабли входят и выходят с одного конца, западного. Сегодня придут три корабля: <b>A</b>, потом <b>B</b>, потом <b>C</b>. Впускай их и выпускай — посмотри, кто сможет выйти.') +
      `<div class="pic">${свг(`
        ${канал(Н,true)}
        ${т(24,КАН.y-КАН.в-18,'вход и выход ⇄',11,'#3a2a14',true,'start')}${т(312,КАН.y-КАН.в-18,'стена',11,'#3a2a14',true,'end')}
        ${флот(s,1,true,п.в)}
        ${строка(16,Н-44,'вышли: ',п.ушли,'#2a1a08')}
        ${строка(16,Н-22,'ждут у входа: ',ПРИХОД.slice(п.след),'#4a3a24')}
        ${п.в.length?т(место(п.в.length-1),КАН.y+КАН.в+14,'выйдет первым',10,'#7a1a08',true):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      кнопки(1,п,true) +
      (!готов ? СКАЗ('Попробуй','Впусти все три корабля, потом выпускай по одному.') :
        ОТВЕТЫ('три',['A','B','C'],2,в,1) +
        (в==null ? СКАЗ('Вопрос','Вошли A, B, C. Кто может выйти из тупика первым?') :
          РАЗБОР(ок,['A зашёл первым и стоит у самой стены: B и C загородили ему выход. Первым выйдет тот, кто у входа, — <b>C</b>.',
            'B стоит посередине, его запирает C. Первым выходит <b>C</b> — последний вошедший.',
            'C вошёл <b>последним</b> и стоит у выхода — ему и выходить первым.'][в]))) +
      (ок ? ПРАВИЛО('<b>Стек</b>: последним пришёл — первым ушёл. Класть и брать можно только «сверху» — с одного конца.') : '');
  }

  /* 2. Проходной канал — очередь */
  function F2(s){
    const Н=240, п=песочница(s,2,false), в=s.ответ2, ок=в===0, готов = п.ушли.length>=1 || п.след>=3;
    return ЛИСТ(s) +
      ЗАДАЧА('Новый канал Архимед прорыл <b>насквозь</b>: корабли входят с запада, а выходят на восток. Те же A, B, C. Впускай и выпускай.') +
      `<div class="pic">${свг(`
        ${канал(Н,false)}
        ${т(24,КАН.y-КАН.в-18,'вход →',11,'#3a2a14',true,'start')}${т(312,КАН.y-КАН.в-18,'→ выход',11,'#3a2a14',true,'end')}
        ${флот(s,2,false,п.в)}
        ${строка(16,Н-44,'вышли: ',п.ушли,'#2a1a08')}
        ${строка(16,Н-22,'ждут у входа: ',ПРИХОД.slice(п.след),'#4a3a24')}
        ${п.в.length?т(место(0),КАН.y+КАН.в+14,'выйдет первым',10,'#7a1a08',true):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      кнопки(2,п,false) +
      (!готов ? СКАЗ('Попробуй','Впусти все три корабля, потом выпускай по одному.') :
        ОТВЕТЫ('три',['A','B','C'],0,в,2) +
        (в==null ? СКАЗ('Вопрос','Вошли A, B, C. Кто выйдет из проходного канала первым?') :
          РАЗБОР(ок,['Верно: A вошёл первым и первым доплыл до восточного выхода.',
            'B стоит за A: пока A не вышел, B не пройдёт. Первым — <b>A</b>.',
            'C вошёл последним и стоит в хвосте. Первым выходит <b>A</b>.'][в]))) +
      (ок ? ПРАВИЛО('<b>Очередь</b>: первым пришёл — первым ушёл. Входят с одного конца, выходят с другого.') : '');
  }

  /* 3. Собрать порядок B, A, C */
  function F3(s){
    const Н=250, п=песочница(s,3,true), ok=п.ушли.join('')===ЦЕЛЬ3.join('');
    const сбой = п.ушли.some((б,i)=>б!==ЦЕЛЬ3[i]);
    return ЛИСТ(s) +
      ЗАДАЧА('Купцы спорят за очередь на разгрузку. Приплывают, как всегда, <b>A, B, C</b> — в тупиковый канал. А выйти к складу они должны в порядке <b>B, A, C</b>. Впускать и выпускать можно вперемешку.') +
      `<div class="pic">${свг(`
        ${канал(Н,true)}
        ${т(24,КАН.y-КАН.в-18,'вход и выход ⇄',11,'#3a2a14',true,'start')}
        ${флот(s,3,true,п.в)}
        ${ЦЕЛЬ3.map((б,i)=>{ const есть=п.ушли[i]; const x=222+i*40;
          return `<g><rect x="${x-16}" y="${Н-60}" width="32" height="32" rx="7" fill="${есть?(есть===б?'rgba(26,106,58,.8)':'rgba(160,40,20,.8)'):'rgba(0,0,0,.12)'}" stroke="#3a2a14" stroke-dasharray="${есть?'':'4 3'}"/>
            ${т(x,Н-38,есть||б,15,есть?'#fff':'#5a4a34',true)}</g>`; }).join('')}
        ${т(190,Н-40,'к складу:',12,'#2a1a08',true,'end')}
        ${строка(16,Н-12,'ждут: ',ПРИХОД.slice(п.след),'#4a3a24')}
        ${рамка(Н)}
      `,Н)}</div>` +
      кнопки(3,п,true) +
      (ok ? РАЗБОР(true,'Впустил A и B, выпустил B, потом A, затем впустил и выпустил C. Порядок <b>B, A, C</b> собран.') :
        сбой ? РАЗБОР(false,'К складу вышел не тот корабль. Нажми «Сначала» и подумай: кто должен стоять у входа, когда пора выпускать?') :
        СКАЗ('Подсказка','Первым к складу нужен B. Значит, B должен оказаться у входа — а за ним уже стоит A.')) +
      (ok ? ПРАВИЛО('Чередуя «положить» и «снять», из стека получают разные порядки — но только те, где выходит тот, кто <b>сверху</b>.') : '');
  }

  /* 4. Невозможный порядок */
  function F4(s){
    const Н=230, в=s.ответ4, ок=в===1;
    return ЛИСТ(s) +
      ЗАДАЧА('Самый нетерпеливый купец требует: «Пусть выйдут <b>C, потом A, потом B</b>!» Корабли всё так же приходят A, B, C в тупик. Можно ли так?') +
      `<div class="pic">${свг(`
        ${канал(Н,true)}
        ${корабль(место(0),КАН.y,'A',1)}${корабль(место(1),КАН.y,'B',1)}
        <g>${ДВИЖ?сдвигРаз('0 0','-120 0','1.2s',0.3):''}${корабль(место(2),КАН.y,'C',1)}</g>
        ${ок?`<circle cx="${место(1)}" cy="${КАН.y}" r="22" fill="none" stroke="${RED}" stroke-width="2.4">${анЛин('opacity','1;0.3;1','1.2s')}</circle>`:''}
        ${подпись(168,Н-14, ок?'после C у выхода B, а не A':'C вышел… кто теперь у выхода?', ок?RED:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Можно: сначала C, потом A, потом B','Нельзя: после C у выхода стоит B, а A заперт'],1,в,4) +
      (в==null ? СКАЗ('Вопрос','Можно ли получить из тупика порядок C, A, B?') :
        РАЗБОР(ок,['Чтобы C вышел первым, в канал должны войти все трое: A у стены, B посередине. После C у выхода — <b>B</b>. A пройти через B не может.',
          'Верно: A заперт за B. Из стека можно брать только верхний — порядок C, A, B <b>невозможен</b>.'][в])) +
      (ок ? ПРАВИЛО('Из стека выходит только <b>верхний</b>. Через соседа не перепрыгнешь — поэтому некоторые порядки получить нельзя.') : '');
  }

  /* 5. «Отменить» — стек */
  function F5(s){
    const Н=260, в=s.ответ5, ок=в===1;
    const дела=['+ Д','+ О','+ М','− М','+ Л'];
    return ЛИСТ(s) +
      ЗАДАЧА('Писарь пишет на восковой табличке и запоминает каждое действие в стопку: написал Д, О, М, стёр М, написал Л — вышло «ДОЛ». Теперь он дважды жмёт «Отменить». Что будет на табличке?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="#2a2018"/>
        <g filter="url(#c539-тень)"><rect x="16" y="30" width="160" height="110" rx="8" fill="url(#c539-дерево)"/><rect x="26" y="40" width="140" height="90" rx="4" fill="url(#c539-воск)"/></g>
        ${т(96,100,ок?'ДОМ':'ДОЛ',34,'#e8d0a0',true)}
        ${т(96,160,'табличка',11,МУТ,true)}
        ${дела.map((д,i)=>{ const y=Н-40-i*38, снято = ок && i>=3;
          return `<g>${снято?сдвигРаз('0 0','60 0','0.6s',0.2+(4-i)*0.3):''}<g opacity="${снято&&!ДВИЖ?0.3:1}">${снято?анРаз('opacity','1','0.25','0.6s',0.2+(4-i)*0.3):''}
            <g filter="url(#c539-тень)"><rect x="206" y="${y-16}" width="96" height="30" rx="6" fill="${д[0]==='+'?'#2a4a3a':'#4a2a2a'}" stroke="${д[0]==='+'?GREEN:RED}" stroke-width="1.4"/></g>
            ${т(254,y+4,д,15,ИНК,true)}</g></g>`; }).join('')}
        ${т(254,22,'стопка действий ↑ верх',10,МУТ,true)}
        ${подпись(96,Н-20, ок?'сняли «+Л» и «−М»':'отмена берёт верхнее', ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['ДО','ДОМ','Д'],1,в,5) +
      (в==null ? СКАЗ('Вопрос','Что будет на табличке после двух отмен?') :
        РАЗБОР(ок,['«ДО» — это если бы отмена стирала буквы. Но она отменяет <b>действия</b>: первая снимает «+Л» (стало «ДО»), вторая — «−М», и М <b>возвращается</b>: ДОМ.',
          'Отмена снимает верхнее действие: «+Л» → ДО, потом «−М» → М вернулась: <b>ДОМ</b>.',
          'Отмен было две, а не четыре: снимаются только «+Л» и «−М». Получится <b>ДОМ</b>.'][в])) +
      (ок ? ПРАВИЛО('Кнопка «Отменить» — это <b>стек</b> действий: отменяется последнее сделанное.') : '');
  }

  /* 6. Очередь к пекарю */
  function F6(s){
    const Н=230, в=s.ответ6, ок=в===0;
    const люди=['Дион','Клеон','Мирон','Ника'];
    const человек=(x,y,имя,уйдёт,i)=>`<g>${уйдёт&&ок?сдвигРаз('0 0','60 -40','0.7s',0.2+i*0.3):''}<g opacity="1">${уйдёт&&ок?анРаз('opacity','1','0','0.7s',0.2+i*0.3):''}
      <g filter="url(#c539-тень)"><circle cx="${x}" cy="${y-30}" r="10" fill="#d8a878"/><path d="M${x-14} ${y} q0 -18 14 -18 q14 0 14 18z" fill="${['#b04a3a','#3a6ab0','#5a8a3a','#a05ab0'][i]}"/></g>
      ${т(x,y+16,имя,11,'#2a1a08',true)}</g></g>`;
    return ЛИСТ(s) +
      ЗАДАЧА('Утром у пекаря очередь: <b>Дион, Клеон, Мирон</b>. Подошла <b>Ника</b> и встала в конец. Пекарь обслужил двоих. Кто теперь первый?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c539-земля)"/>
        <g filter="url(#c539-тень)"><rect x="248" y="40" width="80" height="110" rx="6" fill="#8a5a34"/><path d="M244 44 h88 l-8 -22 h-72 z" fill="#b04a2a"/></g>
        ${т(288,100,'пекарня',12,'#fff0d8',true)}
        <circle cx="288" cy="72" r="12" fill="#e8b060"/>
        ${люди.map((и,i)=>человек(210-i*52,150,и,i<2,i)).join('')}
        ${т(60,190,'← конец',11,'#4a3a24',true)}${т(210,190,'начало →',11,'#4a3a24',true)}
        ${подпись(168,Н-10, ок?'первым теперь стоит Мирон':'кого обслужат первыми?', ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['Мирон','Ника','Клеон'],0,в,6) +
      (в==null ? СКАЗ('Вопрос','Кто стоит первым после того, как обслужили двоих?') :
        РАЗБОР(ок,['Обслужили Диона и Клеона — тех, кто пришёл раньше. Теперь первый <b>Мирон</b>, за ним Ника.',
          'Ника пришла последней и стоит в конце — в очереди новенькие ждут дольше всех. Первый — <b>Мирон</b>.',
          'Клеона уже обслужили: он был вторым. Первый теперь <b>Мирон</b>.'][в])) +
      (ок ? ПРАВИЛО('Люди у пекаря, листы у принтера, сообщения в чате — <b>очередь</b>: первым пришёл, первым обслужен.') : '');
  }

  /* 7. Скобки */
  function F7(s){
    const Н=250, в=s.ответ7, ок=в===0;
    const банка=(x,y,зн,закрыта)=>`<g filter="url(#c539-тень)"><path d="M${x-14} ${y} q-4 -26 6 -34 h16 q10 8 6 34 z" fill="${зн==='('?'#c07a4a':'#6a8ac0'}"/>
      ${закрыта?`<rect x="${x-10}" y="${y-40}" width="20" height="7" rx="2" fill="#3a2a14"/>`:''}</g>${т(x,y-12,зн==='('?'( )':'[ ]',11,'#fff',true)}`;
    return ЛИСТ(s) +
      ЗАДАЧА('Кладовщик ставит амфоры: открывающая скобка — <b>ставим</b> амфору, закрывающая — накрываем крышкой <b>верхнюю</b>. Крышка должна подойти: круглая к круглой, квадратная к квадратной. Какая запись правильная?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="#2e2418"/>
        ${т(84,34,'( [ ] )',22,GREEN,true)}${т(252,34,'( [ ) ]',22,RED,true)}
        <line x1="168" y1="14" x2="168" y2="${Н-30}" stroke="#5a4a34" stroke-width="1.4" stroke-dasharray="4 4"/>
        ${банка(84,150,'(',ок)}${банка(84,110,'[',ок)}
        ${банка(252,150,'(',false)}${банка(252,110,'[',false)}
        ${ок?т(252,76,') не подходит к [',11,RED,true):''}
        ${ок?т(84,76,'] к [, ) к (',11,GREEN,true):''}
        ${т(84,180,'стопка',10,МУТ,true)}${т(252,180,'стопка',10,МУТ,true)}
        ${подпись(168,Н-10, ок?'закрываем только верхнюю':'какая крышка на верхнюю?', ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('пара',['( [ ] )','( [ ) ]'],0,в,7) +
      (в==null ? СКАЗ('Вопрос','Какая запись скобок правильная?') :
        РАЗБОР(ок,['В «( [ ] )» сверху квадратная амфора — её закрывает «]», потом круглую — «)». Все крышки подошли.',
          'В «( [ ) ]» сверху стоит квадратная амфора, а приходит круглая крышка «)» — не подходит. Скобки перепутаны.'][в])) +
      (ок ? ПРАВИЛО('Скобки проверяют <b>стеком</b>: открывающую кладём, закрывающая должна подойти к <b>верхней</b>. Так компьютер проверяет формулы и программы.') : '');
  }

  /* 8. Кнопка «Назад» */
  function F8(s){
    const Н=240, в=s.ответ8, ок=в===1;
    const стр=['Главная','Карта','Корабли','Погода'];
    return ЛИСТ(s) +
      ЗАДАЧА('Прошло две тысячи лет. Ученица открывает в браузере: <b>Главная → Карта → Корабли → Погода</b>. Потом дважды жмёт «Назад». Где она окажется?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="#141c2a"/>
        ${стр.map((и,i)=>{ const y=Н-54-i*40, снята=ок&&i>=2;
          return `<g>${снята?сдвигРаз('0 0','90 0','0.6s',0.2+(3-i)*0.35):''}<g>${снята?анРаз('opacity','1','0.2','0.6s',0.2+(3-i)*0.35):''}
            <g filter="url(#c539-тень)"><rect x="40" y="${y-16}" width="150" height="32" rx="6" fill="url(#c539-экран)" stroke="${ок&&i===1?GREEN:'#4a6a9a'}" stroke-width="${ок&&i===1?2.4:1.2}"/></g>
            <circle cx="56" cy="${y}" r="4" fill="#6a8ac0"/>${т(116,y+5,и,14,ИНК,true)}</g></g>`; }).join('')}
        ${т(115,22,'история: верх — последняя',11,МУТ,true)}
        <g filter="url(#c539-тень)"><rect x="224" y="70" width="90" height="44" rx="10" fill="#22324a" stroke="${ЛАЗУРЬ}"/></g>${т(269,98,'← Назад ×2',13,ЛАЗУРЬ,true)}
        ${подпись(168,Н-10, ок?'вернулись на «Карту»':'«Назад» снимает верхнюю страницу', ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['Корабли','Карта','Главная'],1,в,8) +
      (в==null ? СКАЗ('Вопрос','Какая страница откроется после двух «Назад»?') :
        РАЗБОР(ок,['Первое «Назад» снимает «Погоду» — откроются «Корабли». Второе снимает и их: <b>Карта</b>.',
          'История — стек: снимаем «Погоду», потом «Корабли» — и видим <b>Карту</b>.',
          'До «Главной» нужно три «Назад». После двух — <b>Карта</b>.'][в])) +
      (ок ? ПРАВИЛО('История браузера — <b>стек</b> страниц: «Назад» возвращает к последней открытой до текущей.') : '');
  }

  /* 9. Разложить случаи */
  function F9(s){
    const Н=200, реш=s.реш9||{}, все=СЛУЧАИ.every((_,i)=>реш[i]!=null), верно=все&&СЛУЧАИ.every((c,i)=>реш[i]===c.о);
    return ЛИСТ(s) +
      ЗАДАЧА('Архимед показывает ученикам, что каналы бывают не только с водой. Разложи каждый случай: это <b>стек</b> (последним пришёл — первым ушёл) или <b>очередь</b> (первым пришёл — первым ушёл)?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="#1a2430"/>
        <g filter="url(#c539-тень)">${[0,1,2,3].map(k=>`<ellipse cx="84" cy="${150-k*16}" rx="46" ry="10" fill="#e8e0d0" stroke="#8a826e"/>`).join('')}</g>
        ${т(84,178,'стек: берут сверху',12,GOLD,true)}
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 0;6 0;0 0" dur="1.6s" repeatCount="indefinite"/>${ЗАВОД}`:''}
          ${[0,1,2,3].map(k=>`<circle cx="${204+k*30}" cy="110" r="10" fill="${['#b04a3a','#3a6ab0','#5a8a3a','#a05ab0'][k]}"/>`).join('')}
          <path d="M306 110 l10 0 m-5 -5 l5 5 l-5 5" stroke="${ЛАЗУРЬ}" stroke-width="2" fill="none"/></g>
        ${т(252,178,'очередь: берут спереди',12,ЛАЗУРЬ,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      СЛУЧАИ.map((c,i)=>`<div class="случай">${A(3+i,'что',c.т)}<div class="ask пара">${['стек','очередь'].map((v,j)=>
        BTN(4+i,реш[i]===j?(j===c.о?'hit':'miss'):'',v,'r539Реш('+i+','+j+')')).join('')}</div></div>`).join('') +
      (!все ? СКАЗ('Подсказка','Спроси себя: кого возьмут следующим — того, кто пришёл последним, или того, кто первым?') :
        РАЗБОР(верно, верно ? 'Тарелки и «Отменить» — <b>стек</b>: берут последнее. Очередь к пекарю и листы принтера — <b>очередь</b>: первым пришёл, первым обслужен.' :
          'Проверь ещё раз: тарелку берут верхнюю — ту, что положили последней (стек). К пекарю первым идёт тот, кто первым пришёл (очередь).')) +
      (верно ? ПРАВИЛО('Стек и очередь — два самых частых «канала» для дел в компьютере. Выбирают по тому, кого нужно обслужить <b>следующим</b>.') : '');
  }

  /* 10. Итог */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=260;
    return ЛИСТ(s) +
      ЗАДАЧА(всё
        ? 'Вечер. Ни один купец не простоял у складов лишнего: в тупик пускали тех, кого надо выпустить скоро, в проходной канал — всех по очереди. Архимед: «Внутри машин будущего тоже будут такие каналы — только вместо кораблей там поплывут дела».'
        : 'Каналы ещё не в порядке — вернись к делам в списке. Вот что получится в конце.') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="${Н}" fill="url(#c539-земля)"/>
        <rect x="0" y="40" width="200" height="48" fill="url(#c539-камень)"/><rect x="0" y="48" width="190" height="32" fill="url(#c539-вода)"/><rect x="190" y="40" width="10" height="48" fill="#8a826e"/>
        ${корабль(160,64,'A',0.8)}${корабль(110,64,'B',0.8)}${корабль(60,64,'C',0.8)}
        ${т(268,70,'стек',14,'#2a1a08',true)}
        <rect x="0" y="120" width="336" height="48" fill="url(#c539-камень)"/><rect x="0" y="128" width="336" height="32" fill="url(#c539-вода)"/>
        <g>${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="0 0;40 0" dur="4s" repeatCount="indefinite"/>${ЗАВОД}`:''}${корабль(80,144,'C',0.8,true)}${корабль(140,144,'B',0.8,true)}${корабль(200,144,'A',0.8,true)}</g>
        ${т(300,112,'очередь',13,'#2a1a08',true)}
        ${[['последним пришёл — первым ушёл',GOLD],['первым пришёл — первым ушёл',ЛАЗУРЬ]].map(([t,ц],i)=>`<g>${проявить('8s',0.1+i*0.2,0.16+i*0.2)}${подпись(168,206+i*30,t,ц,12)}</g>`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','<b>Стек</b> — тупиковый канал: последним пришёл, первым ушёл. Так устроены стопка тарелок, «Отменить», «Назад» и проверка скобок. Из стека выходит только верхний, поэтому не всякий порядок можно получить. <b>Очередь</b> — проходной канал: первым пришёл, первым ушёл, как у пекаря и у принтера.') +
      ПРАВИЛО('<b>Стек — сверху, очередь — спереди.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'В стек положили 1, 2, 3. Что сверху?', варианты:[{т:'1',ок:false},{т:'3',ок:true}], разбор:'Последнее положенное — сверху.' },
    { вопрос:'В очередь встали 1, 2, 3. Кто уйдёт первым?', варианты:[{т:'1',ок:true},{т:'3',ок:false}], разбор:'Первым пришёл — первым ушёл.' },
    { вопрос:'В стек положили A, B, сняли одно, положили C. Что сверху?', варианты:[{т:'B',ок:false},{т:'C',ок:true}], разбор:'Сняли B, сверху A, положили C.' },
    { вопрос:'Правильна ли запись ( ( ) ?', варианты:[{т:'нет',ок:true},{т:'да',ок:false}], разбор:'Одна амфора осталась без крышки.' },
    { вопрос:'Кнопка «Назад» в браузере — это…', варианты:[{т:'очередь',ок:false},{т:'стек',ок:true}], разбор:'Возвращает к последней открытой.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r539Reset()")}</div>` +
        ПРАВИЛО('<b>Стек — сверху, очередь — спереди.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r539Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Стек', задания:[
    {q:'В стек положили X, Y, Z и сняли два. Что осталось?', в:0, варианты:['X','Z'], раз:'Сняли Z и Y.'},
    {q:'Тупик: вошли 1, 2, 3. Можно выпустить в порядке 3, 1, 2?', в:1, варианты:['можно','нельзя'], раз:'После 3 сверху 2.'},
    {q:'Тупик: вошли 1, 2, выпустили 2, вошёл 3. Кто выйдет следующим?', в:1, варианты:['1','3'], раз:'3 сверху.'},
    {q:'Правильна ли запись [ ( ) ] ?', в:0, варианты:['да','нет'], раз:'( закрыта ), потом [ закрыта ].'}
  ]};
  const Т2 = { имя:'Очередь', задания:[
    {q:'В очередь встали 5, 7, 9. Обслужили одного. Кто первый?', в:1, варианты:['9','7'], раз:'Ушёл 5.'},
    {q:'Листы на печать: 1, 2, 3. Какой напечатается последним?', в:0, варианты:['3','1'], раз:'Первым пришёл — первым напечатан.'},
    {q:'Можно ли в очереди пропустить вперёд последнего?', в:1, варианты:['да, это обычная очередь','нет, это уже не очередь'], раз:'Очередь обслуживает по порядку прихода.'},
    {q:'Проходной канал: вошли A, B. Потом вошёл C, вышел один. Кто остался?', в:0, варианты:['B и C','A и B'], раз:'Вышел A.'}
  ]};
  const Т3 = { имя:'Стек или очередь', задания:[
    {q:'Стопка книг на столе.', в:0, варианты:['стек','очередь'], раз:'Берут верхнюю.'},
    {q:'Очередь в билетную кассу.', в:1, варианты:['стек','очередь'], раз:'Первым пришёл — первым купил.'},
    {q:'Кнопка «Отменить» в редакторе.', в:0, варианты:['стек','очередь'], раз:'Отменяется последнее действие.'},
    {q:'Сообщения чата приходят и показываются по порядку.', в:1, варианты:['стек','очередь'], раз:'Первое пришло — первым показано.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r539T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r539TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L539 = {
    id: ID,
    title: 'Стек и очередь: как машина складывает дела',
    ico: '⛵',
    src: 'Информатика · 5–6 класс · С нуля: стек и очередь',
    subj: 'inf',
    explain: [
      'Тупиковый канал: корабли входят и выходят с одного конца. Вошли A, B, C — первым выйдет C. Это стек.',
      'Проходной канал: входят с запада, выходят на восток. Первым выйдет A. Это очередь.',
      'Из стека можно получить порядок B, A, C, если чередовать «впустить» и «выпустить».',
      'Порядок C, A, B из стека невозможен: после C сверху B, а A заперт.',
      '«Отменить» — стек действий: две отмены снимают «+Л» и «−М» — на табличке ДОМ.',
      'У пекаря — очередь: обслужили двоих, первым стал Мирон.',
      'Скобки проверяют стеком: ( [ ] ) правильно, ( [ ) ] — нет.',
      'История браузера — стек: два «Назад» с «Погоды» ведут на «Карту».',
      'Тарелки и «Отменить» — стек, пекарь и принтер — очередь.',
      'Итог: стек — сверху, очередь — спереди.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: стек.',
      'Тренажёр 2: очередь.',
      'Тренажёр 3: стек или очередь.'
    ],
    check: {
      q: 'В тупиковый канал вошли корабли A, B, C. Кто выйдет первым?',
      choices: ['A','B','C'],
      ans: 2,
      exp: 'Тупик — стек: первым выходит последний вошедший, C.'
    },
    tasks: [
      { q:'В очередь встали A, B, C. Кто уйдёт первым?', kind:'choice', choices:['C','A','B'], ans:1, tol:0,
        hints:['Первым пришёл — первым ушёл.'], sol:'A.' },
      { q:'В стек положили 1, 2, 3, 4 и сняли два. Сколько осталось и что сверху?', kind:'choice', choices:['2 элемента, сверху 2','2 элемента, сверху 4'], ans:0, tol:0,
        hints:['Сняли 4 и 3.'], sol:'Остались 1 и 2, сверху 2.' },
      { q:'Правильна ли запись скобок ( [ ) ] ?', kind:'choice', choices:['да','нет'], ans:1, tol:0,
        hints:['Закрывающая должна подойти к верхней открытой.'], sol:'Нет: сверху [, а пришла ).' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L539.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    s.вплыл=null; s.уплыл=null;
    const ЗАГОЛОВКИ={1:'Тупиковый канал',2:'Проходной канал',3:'Порядок B, A, C',4:'Невозможный порядок',5:'Кнопка «Отменить»',
      6:'Очередь к пекарю',7:'Скобки',8:'Кнопка «Назад»',9:'Стек или очередь',10:'Каналы в порядке',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l539" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Стек и очередь'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  const ТУПИК = {1:true, 2:false, 3:true};
  const п = (s,f) => { const к='к'+f; if(!s[к]) s[к]={в:[],ушли:[],след:0}; return s[к]; };
  window.r539Впустить=(f)=>{ const s=S(), к=п(s,f); if(к.след>=ПРИХОД.length) return;
    к.в=к.в.concat([ПРИХОД[к.след]]); к.след++; s.вплыл=f; chRender(0); };
  window.r539Выпустить=(f)=>{ const s=S(), к=п(s,f); if(!к.в.length) return;
    const б = ТУПИК[f] ? к.в[к.в.length-1] : к.в[0];
    к.в = ТУПИК[f] ? к.в.slice(0,-1) : к.в.slice(1); к.ушли=к.ушли.concat([б]); s.уплыл={f:f,б:б};
    if(f===3 && к.ушли.join('')===ЦЕЛЬ3.join('')) s.дело_порядок=true;
    chRender(0); };
  window.r539Заново=(f)=>{ const s=S(); s['к'+f]={в:[],ушли:[],след:0}; chRender(0); };
  window.r539Отв=(f,к)=>{ const s=S(); s['ответ'+f]=к; if(f===1 && к===2) s.дело_тупик=true; chRender(0); };
  window.r539Реш=(i,j)=>{ const s=S(); const р=Object.assign({},s.реш9||{}); р[i]=j; s.реш9=р;
    if(СЛУЧАИ.every((c,k)=>р[k]===c.о)) s.дело_разбор=true; chRender(0); };
  window.r539Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r539Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r539T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r539TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L539; else arr.push(L539); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.IN539={render:render, L:L539};
})();
