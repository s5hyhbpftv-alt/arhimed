/* ====== ИНФОРМАТИКА · УРОК 542 · «КАК ПОИСКОВИК НАХОДИТ СТРАНИЦЫ» ============================
   5–6 класс, курс «С нуля». После поиска в списке (517), таблиц (518), графов
   (519), интернета (523) и логики И/ИЛИ (536): как искать среди миллионов.

   ГЛАВНАЯ МЫСЛЬ. Читать всё подряд в момент вопроса — слишком долго. Поэтому
   заранее составляют указатель (индекс): слово → номера свитков. Запрос из
   двух слов — пересечение (И), «или» — объединение. Порядок выдачи решают
   по тому, как часто и где встречается слово, и по ссылкам: на важный
   свиток ссылаются многие. Повтор слова сто раз — обман. Новые страницы
   находит «паук», шагая по ссылкам; куда ссылок нет — туда он не попадёт.

   СЮЖЕТ. «Библиотека Александрии». Эратосфен просит найти всё о Сиракузах
   среди полумиллиона свитков. Ученик вместе с библиотекарем Каллимахом
   (он и правда составил первый каталог — «Пинакес») строит указатель.

   РУКАМИ: самому составить строку указателя для слова МОРЕ; выбрать слова
   для поиска И/ИЛИ; найти самый цитируемый свиток касанием; пустить паука
   шаг за шагом по ссылкам.

   ВСЕ ОТВЕТЫ ПРОВЕРЕНЫ:
     500 000 свитков по 100 в день — 5000 дней (почти 14 лет);
     свитки: 1 ЛУНА МОРЕ; 2 ЗВЁЗДЫ ЛУНА; 3 МОРЕ КОРАБЛЬ; 4 ЛУНА МОРЕ КОРАБЛЬ;
     5 ЗВЁЗДЫ; 6 МОРЕ. МОРЕ → 1, 3, 4, 6; ЛУНА И МОРЕ → 1, 4;
     ЗВЁЗДЫ ИЛИ КОРАБЛЬ → 2, 3, 4, 5 (четыре);
     ссылки A→C, B→C, D→C, E→C, C→B, A→B, D→E: больше всех у C (4);
     паук из A: A→B, B→D, D→A; C→A и E — не найдены: находит A, B, D. */
(function(){
  'use strict';

  const ID = 542;
  const GOLD='#ffd76a', GREEN='#8fe0b0', RED='#ff8a78', ЯНТАРЬ='#f0b050', ЛАЗУРЬ='#8fd0f0';
  const ИНК='#f7f0e2', МУТ='#c0b4a0', ЛИНИЯ='#6a5440', ЧЕРНИЛА='#3a2410';

  const ДЕЛА = [
    {ключ:'указатель', имя:'Составить указатель', итог:'МОРЕ → 1, 3, 4, 6'},
    {ключ:'ссылки',    имя:'Найти главный свиток', итог:'C — 4 ссылки'},
    {ключ:'паук',      имя:'Пустить паука',        итог:'A, B, D'}
  ];
  const сделано = (s,к) => !!s['дело_'+к];

  const СВИТКИ = [
    ['ЛУНА','МОРЕ'], ['ЗВЁЗДЫ','ЛУНА'], ['МОРЕ','КОРАБЛЬ'], ['ЛУНА','МОРЕ','КОРАБЛЬ'], ['ЗВЁЗДЫ'], ['МОРЕ']
  ];
  const СЛОВА = ['ЛУНА','МОРЕ','ЗВЁЗДЫ','КОРАБЛЬ'];
  const где = (сл) => СВИТКИ.map((с,i)=>с.includes(сл)?i+1:0).filter(Boolean);
  const МОРЕ = где('МОРЕ');

  const УЗЛЫ = {A:[60,70], B:[170,50], C:[280,90], D:[110,180], E:[240,190]};
  const ССЫЛКИ = [['A','C'],['B','C'],['D','C'],['E','C'],['C','B'],['A','B'],['D','E']];
  const входящих = (у) => ССЫЛКИ.filter(([a,b])=>b===у).length;
  const ПАУК = {A:[210,50], B:[90,100], C:[280,150], D:[150,190], E:[60,200]};
  const ПАУТИНА = [['A','B'],['B','D'],['D','A'],['C','A']];
  const волна = (n) => { let найд=['A'], фронт=['A'];
    for(let k=0;k<n;k++){ const нов=[]; фронт.forEach(u=>ПАУТИНА.forEach(([a,b])=>{ if(a===u && !найд.includes(b) && !нов.includes(b)) нов.push(b); })); найд=найд.concat(нов); фронт=нов; }
    return найд; };

  const CSS=`
  #lvis .s6.l542{gap:14px}
  #lvis .s6.l542 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l542 .pic svg{display:block;width:100%;height:auto;border-radius:14px}
  #lvis .s6.l542 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#2a2218,#18120c);border:1.5px solid var(--line)}
  #lvis .s6.l542 .карт.задача{border-color:${ЯНТАРЬ}}
  #lvis .s6.l542 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l542 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l542 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l542 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l542 .карт .текст b{color:${GOLD}}
  #lvis .s6.l542 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l542 .правило b{color:${GOLD}}
  #lvis .s6.l542 .лист{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(240,176,80,.16),rgba(240,176,80,.04));
    border:1.5px solid rgba(240,176,80,.55);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l542 .лист .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l542 .лист .шапка b{font-size:18px;letter-spacing:0;text-transform:none;color:${ЯНТАРЬ}}
  #lvis .s6.l542 .лист .шапка b.готово{color:${GREEN}}
  #lvis .s6.l542 .лист ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l542 .лист li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l542 .лист li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l542 .лист li.есть{color:${ИНК}}
  #lvis .s6.l542 .лист li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,224,176,.14)}
  #lvis .s6.l542 .лист li span{margin-left:auto;white-space:nowrap;font-size:14px;color:var(--mut)}
  #lvis .s6.l542 .лист li.есть span{color:${GREEN}}
  #lvis .s6.l542 .ряд{display:grid;gap:8px;width:100%}
  #lvis .s6.l542 .ряд button{min-height:56px;border-radius:14px;cursor:pointer;font:inherit;font-size:16px;font-weight:700;
    border:1.5px solid rgba(240,176,80,.55);background:rgba(240,176,80,.13);color:${ИНК};padding:6px 4px;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l542 .ряд button:active{transform:translateY(2px);background:rgba(240,176,80,.28)}
  #lvis .s6.l542 .ряд button:disabled{opacity:.4;cursor:default}
  #lvis .s6.l542 .ряд button.вкл{border-color:${GOLD};background:rgba(255,215,106,.26)}
  #lvis .s6.l542 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l542 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l542 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l542 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l542 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l542 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center;font-size:17px}
  #lvis .s6.l542 .ask.три button{flex:1 1 calc(33% - 8px);text-align:center;font-variant-numeric:tabular-nums;font-size:17px;padding:13px 4px;overflow-wrap:normal;word-break:keep-all}
  #lvis .s6.l542 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l542 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09)}
  #lvis .s6.l542 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l542 .уровни .точка.сейчас{background:${GOLD};animation:l542dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l542dot{0%,100%{opacity:1}50%{opacity:.55}}
  #lvis .s6.l542 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l542{-webkit-text-size-adjust:100%}
  #lvis .s6.l542 [data-anim]{animation:l542rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l542rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l542 [data-anim]{animation:none!important}
    #lvis .s6.l542 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l542 button{transition:none!important}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l542-style');
      if(!s){ s=document.createElement('style'); s.id='l542-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }
  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,выкл) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}"${выкл?' disabled':''}>${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Библиотека Александрии</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ОТВЕТЫ = (кл,варианты,верный,в,f) => `<div class="ask ${кл}">${варианты.map((v,к)=>
      BTN(5+к, в===к?(к===верный?'hit':'miss'):'', v, 'r542Отв('+f+','+к+')')).join('')}</div>`;
  const ЛИСТ = (s) => {
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ));
    return A(0,'лист',
      `<div class="шапка"><span>Каталог Каллимаха</span><b class="${всё?'готово':''}">${
        всё?'поиск работает':ДЕЛА.filter(д=>сделано(s,д.ключ)).length+' из 3'}</b></div>
       <ul>${ДЕЛА.map(д=>{
         const есть = сделано(s,д.ключ);
         return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${д.имя}<span>${есть?д.итог:'—'}</span></li>`;
       }).join('')}</ul>`);
  };

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анЛин = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const ЗАВОД = `<rect width="0" height="0" fill="none"><animate attributeName="x" values="0;0" dur="1s" repeatCount="indefinite"/></rect>`;
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.45;0.45;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const вырасти = (длит,доля) => анК('opacity','0.15;0.15;1;1',длит,'0;'+кт(доля)+';'+кт(доля+0.06)+';1');
  const анРаз = (имя,из,в,длит,задержка) => ДВИЖ ?
    `<animate attributeName="${имя}" from="${из}" to="${в}" dur="${длит}" begin="${(задержка||0).toFixed(2)}s" fill="freeze"/>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь,ореол) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       ${ореол?`stroke="${ореол}" stroke-width="3" paint-order="stroke" stroke-linejoin="round"`:''}
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;
  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c542-зал" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2e2418"/><stop offset="1" stop-color="#16100a"/>
      </linearGradient>
      <linearGradient id="c542-полка" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#8a5a2a"/><stop offset="1" stop-color="#4a2c10"/>
      </linearGradient>
      <linearGradient id="c542-папирус" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f4e6c0"/><stop offset="1" stop-color="#d0b27a"/>
      </linearGradient>
      <radialGradient id="c542-торец" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#8a6a3a"/><stop offset="0.4" stop-color="#e8d4a4"/><stop offset="1" stop-color="#b89a60"/>
      </radialGradient>
      <radialGradient id="c542-лампа" cx="0.5" cy="0.5" r="0.5">
        <stop offset="0" stop-color="#ffd890" stop-opacity=".5"/><stop offset="1" stop-color="#ff9a40" stop-opacity="0"/>
      </radialGradient>
      <filter id="c542-тень" x="-30%" y="-30%" width="160%" height="170%">
        <feDropShadow dx="0" dy="2" stdDeviation="1.8" flood-color="#000" flood-opacity=".55"/>
      </filter>
      <marker id="c542-стр" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
        <path d="M0 0 L10 5 L0 10 z" fill="#c8a870"/>
      </marker>
      <marker id="c542-стрз" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
        <path d="M0 0 L10 5 L0 10 z" fill="${GREEN}"/>
      </marker>
    </defs>`;
  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;
  const рамка = (в) => `<rect x="0.8" y="0.8" width="334.4" height="${в-1.6}" rx="14" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
  const зал = (Н) => `<rect x="0" y="0" width="336" height="${Н}" fill="url(#c542-зал)"/>
    <circle cx="300" cy="30" r="80" fill="url(#c542-лампа)" data-декор="1">${анЛин('r','76;84;78;82;76','2.4s')}</circle>`;
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+20, в=к+11;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g filter="url(#c542-тень)">
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+4).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(20,12,6,.92)" stroke="${цвет||GOLD}" stroke-width="1.3"/>
      ${т(cx,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };
  /* свиток с номером и словами */
  const свиток = (x,y,н,слова,свет,тускл) => `<g opacity="${тускл?0.35:1}">
      ${свет?`<rect x="${x-44}" y="${y-8}" width="88" height="${24+слова.length*14}" rx="8" fill="none" stroke="${GOLD}" stroke-width="2.4">${анЛин('opacity','1;0.4;1','1.4s')}</rect>`:''}
      <g filter="url(#c542-тень)"><rect x="${x-38}" y="${y}" width="76" height="${10+слова.length*14}" rx="3" fill="url(#c542-папирус)"/>
      <rect x="${x-42}" y="${y-4}" width="84" height="7" rx="3.5" fill="url(#c542-полка)"/><rect x="${x-42}" y="${y+8+слова.length*14}" width="84" height="7" rx="3.5" fill="url(#c542-полка)"/></g>
      ${слова.map((w,k)=>т(x,y+17+k*14,w,10.5,ЧЕРНИЛА,true)).join('')}
      <g filter="url(#c542-тень)"><circle cx="${x-38}" cy="${y-2}" r="10" fill="#a8321e" stroke="#5a1a0a"/></g>${т(x-38,y+2,String(н),11,'#ffe8c8',true)}</g>`;
  const стеллаж = (Н) => { let g=''; for(let r=0;r<3;r++){ const y=40+r*56;
      g+=`<rect x="16" y="${y+34}" width="304" height="8" fill="url(#c542-полка)" data-декор="1"/>`;
      for(let i=0;i<13;i++) g+=`<circle cx="${30+i*23}" cy="${y+20}" r="10" fill="url(#c542-торец)" stroke="#6a4a24" stroke-width=".8" data-декор="1"/>`; }
    return g; };

  /* ================= КАДРЫ ================= */

  /* 1. Читать всё подряд */
  function F1(s){
    const Н=260, в=s.ответ1, ок=в===1;
    return ЛИСТ(s) +
      ЗАДАЧА('Эратосфен, хранитель Александрийской библиотеки, получил письмо: «Найдите всё, что написано о <b>Сиракузах</b>». В библиотеке <b>500 000 свитков</b>. Самый быстрый чтец просматривает <b>100 свитков в день</b>. Сколько дней уйдёт, если читать всё подряд?') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${стеллаж(Н)}
        <g transform="translate(250 ${Н-40})">${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="250 ${Н-40};246 ${Н-42};250 ${Н-40}" dur="1.2s" repeatCount="indefinite"/>`:''}
          <g filter="url(#c542-тень)"><circle cx="0" cy="-26" r="9" fill="#d8a878"/><path d="M-14 0 q0 -18 14 -18 q14 0 14 18z" fill="#4a6aa0"/>
          <rect x="-22" y="-16" width="44" height="12" rx="3" fill="url(#c542-папирус)"/></g></g>
        ${т(24,Н-26,ок?'5000 дней — почти 14 лет':'500 000 свитков : 100 в день = ?',13,ок?RED:GOLD,true,'start')}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('три',['500','5000','50 000'],1,в,1) +
      (в==null ? СКАЗ('Вопрос','Сколько дней уйдёт на чтение всей библиотеки?') :
        РАЗБОР(ок,['500 — это если бы свитков было 50 000. 500 000 : 100 = <b>5000</b> дней.',
          '500 000 : 100 = <b>5000 дней</b> — почти 14 лет. Письмо о Сиракузах за это время устареет.',
          '50 000 — лишний ноль. 500 000 : 100 = <b>5000</b>.'][в])) +
      (ок ? ПРАВИЛО('Читать всё <b>в момент вопроса</b> — слишком долго. Нужно подготовиться <b>заранее</b>.') : '');
  }

  /* 2. Составить указатель */
  function F2(s){
    const Н=300, выбор=Array.isArray(s.указ2)?s.указ2:[], пров=s.пров2;
    const верно = пров && выбор.slice().sort().join(',')===МОРЕ.join(',');
    return ЛИСТ(s) +
      ЗАДАЧА('Библиотекарь <b>Каллимах</b> придумал выход: один раз прочесть всё и составить <b>указатель</b> — для каждого слова список номеров свитков, где оно есть. Помоги ему: отметь все свитки, где встречается слово <b>МОРЕ</b>.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${СВИТКИ.map((сл,i)=>свиток(60+(i%3)*108,36+Math.floor(i/3)*96,i+1,сл,выбор.includes(i+1),false)).join('')}
        <g filter="url(#c542-тень)"><rect x="16" y="${Н-58}" width="304" height="40" rx="6" fill="url(#c542-папирус)"/></g>
        ${т(28,Н-32,'МОРЕ →',15,'#7a1a08',true,'start')}
        ${т(100,Н-32,выбор.slice().sort((a,b)=>a-b).join(', ')||'…',16,верно?'#1a6a3a':ЧЕРНИЛА,true,'start')}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="ряд" style="grid-template-columns:repeat(6,1fr)">${СВИТКИ.map((_,i)=>BTN(3,выбор.includes(i+1)?'вкл':'',String(i+1),'r542Указ('+(i+1)+')')).join('')}</div>` +
      `<div class="ask">${BTN(4,'','Записать в указатель','r542Записать()')}</div>` +
      (!пров ? СКАЗ('Задание','Касайся номеров свитков, где есть МОРЕ, и нажми «Записать».') :
        РАЗБОР(верно, верно ? 'МОРЕ → <b>1, 3, 4, 6</b>. Теперь на вопрос «где МОРЕ?» ответ готов мгновенно — без чтения свитков.' :
          'Проверь каждый свиток: МОРЕ есть в 1, 3, 4 и 6. В 2 и 5 его нет.')) +
      (верно ? ПРАВИЛО('<b>Указатель (индекс)</b>: слово → номера свитков. Его составляют <b>заранее</b>, а на вопрос отвечают по нему за мгновение.') : '');
  }

  /* 3. Два слова: И */
  function F3(s){
    const Н=240, в=s.ответ3, ок=в===0;
    const строка=(y,сл,спис,общие)=>`${т(24,y,сл+' →',14,ИНК,true,'start')}${спис.map((n,k)=>{ const x=130+k*40, об=общие.includes(n)&&ок;
      return `<g><circle cx="${x}" cy="${y-5}" r="15" fill="${об?'rgba(143,224,176,.3)':'rgba(255,255,255,.06)'}" stroke="${об?GREEN:'#8a7a5a'}" stroke-width="${об?2.4:1.2}"/>${т(x,y,String(n),14,об?GREEN:ИНК,true)}</g>`; }).join('')}`;
    const л=где('ЛУНА'), м=где('МОРЕ'), общ=л.filter(x=>м.includes(x));
    return ЛИСТ(s) +
      ЗАДАЧА('Астроном просит свитки, где есть <b>и ЛУНА, и МОРЕ</b> — он изучает приливы. В указателе две строки. Какие свитки ему выдать?') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        <g filter="url(#c542-тень)"><rect x="12" y="30" width="312" height="120" rx="6" fill="rgba(244,230,192,.08)" stroke="#8a6a3a"/></g>
        ${строка(76,'ЛУНА',л,общ)}${строка(126,'МОРЕ',м,общ)}
        ${подпись(168,Н-16, ок?'ЛУНА И МОРЕ → 1, 4':'номера, которые есть в обеих строках', ок?GREEN:ИНК,12)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Свитки 1 и 4','Свитки 1, 2, 3, 4, 6','Только свиток 4'],0,в,3) +
      (в==null ? СКАЗ('Вопрос','Какие свитки содержат и ЛУНУ, и МОРЕ?') :
        РАЗБОР(ок,['Ищем номера, которые стоят <b>в обеих</b> строках: 1 и 4. Это то же И из урока 536: нужны оба условия.',
          'Это все свитки, где есть <b>хотя бы одно</b> слово — так работает ИЛИ. Для «и ЛУНА, и МОРЕ» нужны общие номера: 1 и 4.',
          'Свиток 1 тоже подходит: в нём есть и ЛУНА, и МОРЕ. Ответ — <b>1 и 4</b>.'][в])) +
      (ок ? ПРАВИЛО('Запрос из нескольких слов — <b>пересечение</b> строк указателя: номера, которые есть везде. Это логическое <b>И</b>.') : '');
  }

  /* 4. ИЛИ */
  function F4(s){
    const Н=230, выбор=Array.isArray(s.слова4)?s.слова4:[], режим=s.режим4||'И';
    let итог=null;
    if(выбор.length){ итог = выбор.map(где).reduce((a,b)=> режим==='И' ? a.filter(x=>b.includes(x)) : Array.from(new Set(a.concat(b))).sort((x,y)=>x-y)); }
    const в=s.ответ4, ок=в===0;
    return ЛИСТ(s) +
      ЗАДАЧА('Корабельщик ищет свитки, где есть <b>ЗВЁЗДЫ или КОРАБЛЬ</b> — ему подойдёт любой. Попробуй сам: выбери слова и режим поиска — И или ИЛИ — и посмотри, сколько свитков найдётся.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${СВИТКИ.map((сл,i)=>{ const x=32+i*54, найден = итог && итог.includes(i+1);
          return `<g opacity="${итог&&!найден?0.3:1}"><g filter="url(#c542-тень)"><rect x="${x-20}" y="60" width="40" height="70" rx="4" fill="url(#c542-папирус)"/></g>
            ${[0,1,2].map(k=>`<line x1="${x-12}" y1="${76+k*14}" x2="${x+12}" y2="${76+k*14}" stroke="#8a6a3a" stroke-width="1.2"/>`).join('')}
            ${найден?`<rect x="${x-24}" y="56" width="48" height="78" rx="6" fill="none" stroke="${GOLD}" stroke-width="2.4"/>`:''}
            ${т(x,154,String(i+1),15,найден?GOLD:ИНК,true)}</g>`; }).join('')}
        ${т(168,34,выбор.length?выбор.join(' '+режим+' ')+' → '+(итог.length?итог.join(', '):'ничего'):'выбери слова',14,GOLD,true)}
        ${т(168,Н-26,итог?'найдено свитков: '+итог.length:'',13,ИНК,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="ряд" style="grid-template-columns:repeat(2,1fr)">${СЛОВА.map((w,i)=>BTN(3,выбор.includes(w)?'вкл':'',w,"r542Слово('"+w+"')")).join('')}</div>` +
      `<div class="ряд" style="grid-template-columns:1fr 1fr">${['И','ИЛИ'].map(р=>BTN(4,режим===р?'вкл':'','режим '+р,"r542Режим('"+р+"')")).join('')}</div>` +
      ОТВЕТЫ('три',['4','2','0'],0,в,4) +
      (в==null ? СКАЗ('Вопрос','Сколько свитков найдётся по запросу ЗВЁЗДЫ ИЛИ КОРАБЛЬ?') :
        РАЗБОР(ок,['ЗВЁЗДЫ → 2, 5; КОРАБЛЬ → 3, 4. Вместе — <b>2, 3, 4, 5</b>: четыре свитка.',
          'Два — это одна строка указателя. ИЛИ <b>объединяет</b> обе: 2, 3, 4, 5 — четыре.',
          'Ноль вышел бы с И: общих номеров у ЗВЁЗД и КОРАБЛЯ нет. А с ИЛИ — <b>четыре</b>.'][в])) +
      (ок ? ПРАВИЛО('<b>И</b> сужает поиск (общие номера), <b>ИЛИ</b> расширяет (все номера из обеих строк).') : '');
  }

  /* 5. Что показать первым */
  function F5(s){
    const Н=250, в=s.ответ5, ок=в===1;
    const свиток5=(x,заг,строки,мор,свет)=>`<g filter="url(#c542-тень)"><rect x="${x-66}" y="40" width="132" height="150" rx="4" fill="url(#c542-папирус)"/></g>
      ${свет?`<rect x="${x-70}" y="36" width="140" height="158" rx="6" fill="none" stroke="${GREEN}" stroke-width="2.4"/>`:''}
      ${т(x,62,заг,12,'#7a1a08',true)}
      ${строки.map((w,k)=>т(x,86+k*18,w,10.5,w.includes('море')?'#1a4a8a':ЧЕРНИЛА,w.includes('море'))).join('')}
      ${т(x,210,'«море»: '+мор,12,ИНК,true)}`;
    return ЛИСТ(s) +
      ЗАДАЧА('Моряк спросил про <b>МОРЕ</b>. Нашлось два свитка. В первом — список зерна, где море упомянуто мимоходом. Второй называется «О море и ветрах», и море в нём на каждой строке. Какой показать <b>первым</b>?') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${свиток5(88,'Счёт за зерно',['пшеница 40 мер','ячмень 20 мер','доставлено морем','оплачено'],1,false)}
        ${свиток5(248,'О море и ветрах',['море у Сицилии','ветер над морем','море и течения','море зимой'],7,ок)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Счёт за зерно — он раньше в указателе','«О море и ветрах» — слово в названии и встречается часто'],1,в,5) +
      (в==null ? СКАЗ('Вопрос','Какой свиток показать моряку первым?') :
        РАЗБОР(ок,['Номер в указателе ничего не говорит о пользе. Моряку нужен свиток <b>про море</b>: слово в названии и на каждой строке.',
          'Слово в <b>названии</b> и много раз в тексте — значит, свиток именно об этом. Его и ставят первым.'][в])) +
      (ок ? ПРАВИЛО('Поисковик <b>ранжирует</b> результаты: где слово в заголовке, где встречается чаще — те выше.') : '');
  }

  /* 6. Самый цитируемый */
  function F6(s){
    const Н=260, выбр=s.выбр6, ок=выбр==='C';
    const стрелки = ССЫЛКИ.map(([a,b])=>{ const [x1,y1]=УЗЛЫ[a], [x2,y2]=УЗЛЫ[b], d=Math.hypot(x2-x1,y2-y1), k1=26/d, k2=(d-30)/d;
      const в=ок&&b==='C', nx=-(y2-y1)/d*5, ny=(x2-x1)/d*5;
      return `<line x1="${(x1+(x2-x1)*k1+nx).toFixed(1)}" y1="${(y1+(y2-y1)*k1+ny).toFixed(1)}" x2="${(x1+(x2-x1)*k2+nx).toFixed(1)}" y2="${(y1+(y2-y1)*k2+ny).toFixed(1)}" stroke="${в?GREEN:'#c8a870'}" stroke-width="${в?2.6:1.8}" marker-end="url(#c542-стр${в?'з':''})"/>`; }).join('');
    const узлы = Object.entries(УЗЛЫ).map(([у,[x,y]])=>{ const этот=выбр===у;
      return `<g onclick="r542Узел('${у}')" style="cursor:pointer"><circle cx="${x}" cy="${y}" r="30" fill="transparent"/>
        <g filter="url(#c542-тень)"><rect x="${x-20}" y="${y-24}" width="40" height="48" rx="4" fill="url(#c542-папирус)" stroke="${этот?(ок?GREEN:RED):'#8a6a3a'}" stroke-width="${этот?3:1}"/></g>
        ${т(x,y+7,у,18,ЧЕРНИЛА,true)}
        ${выбр?`<g filter="url(#c542-тень)"><circle cx="${x+20}" cy="${y-24}" r="11" fill="${входящих(у)===4?'#1a6a3a':'#3a2a18'}" stroke="${входящих(у)===4?GREEN:'#c8a870'}"/></g>${т(x+20,y-20,String(входящих(у)),12,'#fff',true)}`:''}</g>`; }).join('');
    return ЛИСТ(s) +
      ЗАДАЧА('Каллимах заметил: учёные в свитках <b>ссылаются</b> друг на друга — «как писал автор свитка C…». Стрелка — ссылка. Если на свиток ссылаются многие, он важный. Коснись свитка, который важнее всех; в кружке на углу появится, сколько на него ссылок.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${стрелки}${узлы}
        ${выбр?подпись(168,Н-12, ок?'на C ссылаются четверо':'на '+выбр+' ссылаются '+входящих(выбр)+' — ищи больше', ок?GREEN:RED,12):''}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="ряд" style="grid-template-columns:repeat(5,1fr)">${Object.keys(УЗЛЫ).map(у=>BTN(3,выбр===у?'вкл':'',у,"r542Узел('"+у+"')")).join('')}</div>` +
      (выбр==null ? СКАЗ('Подсказка','Считай стрелки, которые <b>входят</b> в свиток, — а не выходят из него.') :
        РАЗБОР(ок, ок ? 'В свиток <b>C</b> входят четыре стрелки: от A, B, D и E. Его цитируют чаще всех — значит, его стоит показать выше.' :
          'В '+выбр+' входит стрелок: '+входящих(выбр)+'. Смотри, куда стрелки <b>приходят</b>, а не откуда уходят.')) +
      (ок ? ПРАВИЛО('<b>Ссылки — голоса</b>: чем больше страниц ссылается на страницу, тем выше её ставит поисковик. Так устроен поиск Google.') : '');
  }

  /* 7. Обман повтором */
  function F7(s){
    const Н=240, в=s.ответ7, ок=в===1;
    return ЛИСТ(s) +
      ЗАДАЧА('Хитрый торговец рыбой написал свиток, где слово «МОРЕ» повторено <b>100 раз</b> — чтобы его рекламу выдавали первой. А на свиток Архимеда «О плавающих телах» ссылаются <b>20 учёных</b>. Что показать первым по запросу «море и плавание»?') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        <g filter="url(#c542-тень)"><rect x="24" y="40" width="130" height="150" rx="4" fill="url(#c542-папирус)"/></g>
        ${Array.from({length:9},(_,k)=>т(89,60+k*14,'море море море',9.5,'#6a4a2a',false)).join('')}
        ${т(89,210,'0 ссылок',12,RED,true)}
        <g filter="url(#c542-тень)"><rect x="182" y="40" width="130" height="150" rx="4" fill="url(#c542-папирус)"/></g>
        ${т(247,66,'Архимед',13,'#7a1a08',true)}${т(247,84,'О плавающих телах',10.5,ЧЕРНИЛА,true)}
        ${[0,1,2,3,4].map(k=>`<line x1="198" y1="${104+k*14}" x2="296" y2="${104+k*14}" stroke="#8a6a3a" stroke-width="1.2"/>`).join('')}
        ${т(247,210,'20 ссылок',12,GREEN,true)}
        ${ок?`<rect x="178" y="36" width="138" height="158" rx="6" fill="none" stroke="${GREEN}" stroke-width="2.4"/>`:''}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Свиток торговца: там «море» 100 раз','Свиток Архимеда: на него ссылаются 20 учёных'],1,в,7) +
      (в==null ? СКАЗ('Вопрос','Что показать первым?') :
        РАЗБОР(ок,['Повторить слово сто раз может любой хитрец. А заставить 20 учёных сослаться на пустой свиток — нельзя. Первым — <b>Архимед</b>.',
          'Ссылки подделать трудно, повторы — легко. Поэтому поисковики доверяют <b>ссылкам</b> и наказывают за набивку слов.'][в])) +
      (ок ? ПРАВИЛО('Поисковик борется с обманом: считает не только повторы слова, но и <b>доверие</b> — ссылки от других.') : '');
  }

  /* 8. Паук */
  function F8(s){
    const Н=260, шаг=s.шаг8||0, найд=волна(шаг), конец = волна(шаг+1).length===найд.length, в=s.ответ8, ок=в===0;
    const стрелки = ПАУТИНА.map(([a,b])=>{ const [x1,y1]=ПАУК[a], [x2,y2]=ПАУК[b], d=Math.hypot(x2-x1,y2-y1), k1=26/d, k2=(d-30)/d;
      const пройд = найд.includes(a) && найд.includes(b) && a!=='C';
      return `<line x1="${(x1+(x2-x1)*k1).toFixed(1)}" y1="${(y1+(y2-y1)*k1).toFixed(1)}" x2="${(x1+(x2-x1)*k2).toFixed(1)}" y2="${(y1+(y2-y1)*k2).toFixed(1)}" stroke="${пройд?GREEN:'#c8a870'}" stroke-width="${пройд?2.6:1.8}" marker-end="url(#c542-стр${пройд?'з':''})"/>`; }).join('');
    const узлы = Object.entries(ПАУК).map(([у,[x,y]])=>{ const есть=найд.includes(у);
      return `<g><g filter="url(#c542-тень)"><rect x="${x-20}" y="${y-24}" width="40" height="48" rx="4" fill="url(#c542-папирус)" opacity="${есть?1:0.45}" stroke="${есть?GREEN:'#8a6a3a'}" stroke-width="${есть?2.4:1}"/></g>
        ${т(x,y+7,у,18,ЧЕРНИЛА,true)}</g>`; }).join('');
    const [пх,пу]=ПАУК[найд[найд.length-1]];
    return ЛИСТ(s) +
      ЗАДАЧА('Новые свитки привозят каждый день, и никто не говорит, куда их положили. Каллимах посылает <b>«паука»</b> — мальчика, который начинает со свитка A и идёт <b>только по ссылкам</b>, отмечая каждый найденный свиток. Жми «Шаг паука».') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${стрелки}${узлы}
        <g transform="translate(${пх+24} ${пу-24})">${ДВИЖ?`<animateTransform attributeName="transform" type="translate" values="${пх+24} ${пу-24};${пх+24} ${пу-28};${пх+24} ${пу-24}" dur="0.8s" repeatCount="indefinite"/>`:''}
          <circle cx="0" cy="0" r="7" fill="#2a1a0a"/>${[-1,1].map(с=>[0,1,2].map(k=>`<path d="M0 0 l${с*10} ${-6+k*6} l${с*4} 4" stroke="#2a1a0a" stroke-width="1.6" fill="none"/>`).join('')).join('')}</g>
        ${т(20,Н-18,'найдено: '+найд.join(', ')+(конец?' — дальше идти некуда':''),12,конец?GOLD:ИНК,true,'start')}
        ${рамка(Н)}
      `,Н)}</div>` +
      `<div class="ряд" style="grid-template-columns:1fr 1fr">${BTN(3,'','Шаг паука','r542Паук(1)',конец)}${BTN(4,'','Сначала','r542Паук(0)')}</div>` +
      (!конец ? СКАЗ('Попробуй','Паук идёт по стрелкам от уже найденных свитков.') :
        ОТВЕТЫ('',['A, B, D — на C и E ссылок от них нет','Все пять свитков','A, B, C, D'],0,в,8) +
        (в==null ? СКАЗ('Вопрос','Какие свитки паук найдёт?') :
          РАЗБОР(ок,['Из A, B и D никто не ссылается ни на C, ни на E. C сам ссылается на A — но это стрелка <b>в</b> A, по ней из A не попасть в C.',
            'E ни с кем не связан, а на C никто из найденных не ссылается. Паук находит только <b>A, B, D</b>.',
            'Стрелка C → A идёт <b>из</b> C. Паук из A по ней не пройдёт. Найдены только <b>A, B, D</b>.'][в]))) +
      (ок ? ПРАВИЛО('Поисковик находит страницы <b>пауком</b>, который ходит по ссылкам (обход графа, урок 519). Страница, на которую никто не ссылается, остаётся невидимой.') : '');
  }

  /* 9. Почему так быстро */
  function F9(s){
    const Н=230, в=s.ответ9, ок=в===0;
    return ЛИСТ(s) +
      ЗАДАЧА('Прошло две тысячи лет. В интернете — миллиарды страниц, а поисковик отвечает за <b>полсекунды</b>. Как ему это удаётся?') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        <g filter="url(#c542-тень)"><rect x="24" y="44" width="170" height="36" rx="18" fill="#f4f0e8"/></g>
        ${т(40,68,'Архимед рычаг',14,'#2a2a2a',false,'start')}<circle cx="176" cy="62" r="8" fill="none" stroke="#4a6aa0" stroke-width="2.4"/>
        ${т(196,70,'→ 0,4 с',16,GREEN,true,'start')}
        ${[0,1,2,3].map(k=>`<g>${вырасти('6s',0.1+k*0.1)}<rect x="${24+k*76}" y="120" width="64" height="70" rx="4" fill="#223048" stroke="#4a6aa0"/>
          ${[0,1,2,3].map(j=>`<rect x="${32+k*76}" y="${130+j*14}" width="48" height="6" rx="2" fill="${(j+k)%3?'#6a8ac0':GREEN}"/>`).join('')}</g>`).join('')}
        ${т(168,210,'серверы с готовым указателем',12,МУТ,true)}
        ${рамка(Н)}
      `,Н)}</div>` +
      ОТВЕТЫ('',['Указатель составлен заранее — на вопрос отвечают по нему','Компьютер за полсекунды читает все сайты'],0,в,9) +
      (в==null ? СКАЗ('Вопрос','Почему поиск такой быстрый?') :
        РАЗБОР(ок,['Пауки обходят интернет днём и ночью и заранее складывают <b>указатель</b>. Вопрос — это просто быстрый взгляд в него, как у Каллимаха.',
          'Прочесть миллиарды страниц за полсекунду не может ни один компьютер. Ответ берут из <b>готового указателя</b>.'][в])) +
      (ок ? ПРАВИЛО('Поиск = <b>паук</b> (собирает страницы) + <b>указатель</b> (слово → страницы) + <b>ранжирование</b> (что показать первым).') : '');
  }

  /* 10. Итог */
  function F10(s){
    const всё = ДЕЛА.every(д=>сделано(s,д.ключ)), Н=250;
    return ЛИСТ(s) +
      ЗАДАЧА(всё
        ? 'Через неделю Эратосфен отправил в Сиракузы полный список свитков о городе — вместо четырнадцати лет чтения. Каллимах назвал свой каталог «Пинакес» — «таблицы». Две тысячи лет спустя поисковики делают то же самое, только с миллиардами страниц.'
        : 'Каталог ещё не готов — вернись к делам в списке. Вот что получится в конце.') +
      `<div class="pic">${свг(`
        ${зал(Н)}
        ${стеллаж(Н)}
        <rect x="0" y="0" width="336" height="${Н}" fill="rgba(22,16,10,.55)"/>
        ${[['паук: ходит по ссылкам',ЛАЗУРЬ],['указатель: слово → страницы',GOLD],['И — сужает, ИЛИ — расширяет',GOLD],['ссылки — голоса',GREEN]].map(([t,ц],i)=>`<g>${проявить('10s',0.08+i*0.14,0.14+i*0.14)}${подпись(168,50+i*44,t,ц,12)}</g>`).join('')}
        ${рамка(Н)}
      `,Н)}</div>` +
      СКАЗ('Итог','Читать всё в момент вопроса слишком долго, поэтому заранее составляют <b>указатель</b>: слово → номера страниц. Запрос из двух слов — пересечение (И), «или» — объединение. Первыми показывают страницы, где слово в заголовке и на которые ссылаются многие. Повтор слова — обман, ссылки — доверие. Новые страницы находит <b>паук</b>, шагая по ссылкам.') +
      ПРАВИЛО('<b>Паук + указатель + ранжирование = поиск.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Зачем поисковику указатель?', варианты:[{т:'Чтобы не читать всё при каждом вопросе',ок:true},{т:'Чтобы красиво показать страницы',ок:false}], разбор:'Указатель готовят заранее.' },
    { вопрос:'РЕКА → 2, 5, 7; ГОРА → 5, 8. Где есть и РЕКА, и ГОРА?', варианты:[{т:'2, 5, 7, 8',ок:false},{т:'5',ок:true}], разбор:'Общий номер — 5.' },
    { вопрос:'РЕКА → 2, 5, 7; ГОРА → 5, 8. Сколько страниц найдёт РЕКА ИЛИ ГОРА?', варианты:[{т:'4',ок:true},{т:'5',ок:false}], разбор:'2, 5, 7, 8.' },
    { вопрос:'На страницу X ссылаются 10 страниц, на Y — 1. Какую поставить выше?', варианты:[{т:'Y',ок:false},{т:'X',ок:true}], разбор:'Ссылки — голоса.' },
    { вопрос:'На страницу никто не ссылается. Найдёт ли её паук?', варианты:[{т:'нет',ок:true},{т:'да',ок:false}], разбор:'Паук ходит только по ссылкам.' }
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r542Reset()")}</div>` +
        ПРАВИЛО('<b>Паук + указатель + ранжирование = поиск.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к, выбран===к ? (в.ок?'hit':'miss') : '', в.т, "r542Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Указатель', задания:[
    {q:'Свитки: 1 — КОТ, СОБАКА; 2 — КОТ; 3 — ПТИЦА. КОТ → ?', в:0, варианты:['1, 2','1, 3'], раз:'КОТ есть в 1 и 2.'},
    {q:'Когда составляют указатель?', в:1, варианты:['в момент вопроса','заранее'], раз:'Иначе он не ускорит поиск.'},
    {q:'В указателе слово, а рядом…', в:0, варианты:['номера страниц','весь текст страниц'], раз:'Только номера.'},
    {q:'1 000 000 страниц по 1000 в секунду читаются за…', в:1, варианты:['1 секунду','1000 секунд'], раз:'1 000 000 : 1000.'}
  ]};
  const Т2 = { имя:'И и ИЛИ', задания:[
    {q:'A → 1, 2, 3; B → 3, 4. A И B → ?', в:0, варианты:['3','1, 2, 3, 4'], раз:'Общий номер.'},
    {q:'A → 1, 2, 3; B → 3, 4. A ИЛИ B — сколько?', в:1, варианты:['5','4'], раз:'1, 2, 3, 4.'},
    {q:'Добавили в запрос ещё одно слово через И. Результатов станет…', в:0, варианты:['не больше','больше'], раз:'И сужает.'},
    {q:'A → 1, 2; B → 5, 6. A И B → ?', в:1, варианты:['1, 2, 5, 6','ничего'], раз:'Общих нет.'}
  ]};
  const Т3 = { имя:'Ранжирование и паук', задания:[
    {q:'Слово в заголовке страницы — это…', в:0, варианты:['знак, что страница об этом','ничего не значит'], раз:'Заголовок говорит о теме.'},
    {q:'Сайт повторил слово 500 раз. Поисковик…', в:1, варианты:['поставит его первым','заподозрит обман'], раз:'Набивка слов — обман.'},
    {q:'Паук начал с A. A → B, B → C. Кого он найдёт?', в:0, варианты:['A, B, C','только A'], раз:'Идёт по ссылкам.'},
    {q:'Паук начал с A. A → B, C → A. Найдёт ли он C?', в:1, варианты:['да','нет'], раз:'Стрелка C → A ведёт в A, а не из A.'}
  ]};
  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r542T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r542TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L542 = {
    id: ID,
    title: 'Как поисковик находит страницы',
    ico: '🔎',
    src: 'Информатика · 5–6 класс · С нуля: поисковик',
    subj: 'inf',
    explain: [
      '500 000 свитков по 100 в день — 5000 дней. Читать всё в момент вопроса слишком долго.',
      'Указатель: слово → номера свитков. МОРЕ → 1, 3, 4, 6. Его составляют заранее.',
      'Два слова — пересечение строк указателя: ЛУНА И МОРЕ → 1, 4.',
      'ИЛИ объединяет строки: ЗВЁЗДЫ ИЛИ КОРАБЛЬ → 2, 3, 4, 5.',
      'Первыми показывают свитки, где слово в названии и встречается часто.',
      'Ссылки — голоса: на свиток C ссылаются четверо, он важнее всех.',
      'Повтор слова сто раз — обман; ссылки подделать трудно.',
      'Паук ходит по ссылкам: из A он найдёт только A, B, D.',
      'Поиск быстрый, потому что указатель готов заранее.',
      'Итог: паук + указатель + ранжирование = поиск.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: указатель.',
      'Тренажёр 2: И и ИЛИ.',
      'Тренажёр 3: ранжирование и паук.'
    ],
    check: {
      q: 'ЛУНА → 1, 2, 4; МОРЕ → 1, 3, 4, 6. Какие свитки содержат и ЛУНУ, и МОРЕ?',
      choices: ['1 и 4','1, 2, 3, 4, 6','только 4'],
      ans: 0,
      exp: 'Общие номера обеих строк — 1 и 4.'
    },
    tasks: [
      { q:'500 000 свитков, 100 свитков в день. Сколько дней на чтение всех?', kind:'unit', ans:5000, tol:0,
        hints:['Раздели 500 000 на 100.'], sol:'5000.' },
      { q:'A → 1, 2, 3; B → 3, 4. Сколько страниц найдёт A ИЛИ B?', kind:'unit', ans:4, tol:0,
        hints:['Объедини без повторов.'], sol:'1, 2, 3, 4 — четыре.' },
      { q:'Как поисковик находит новые страницы?', kind:'choice', choices:['паук ходит по ссылкам','страницы сами присылают себя','угадывает'], ans:0, tol:0,
        hints:['Вспомни мальчика-паука.'], sol:'Паук ходит по ссылкам.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L542.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s); else if(f===2) сцена=F2(s); else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s); else if(f===5) сцена=F5(s); else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s); else if(f===8) сцена=F8(s); else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s); else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1); else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);
    const ЗАГОЛОВКИ={1:'Полмиллиона свитков',2:'Указатель Каллимаха',3:'Два слова: И',4:'И или ИЛИ',5:'Что показать первым',
      6:'Ссылки — голоса',7:'Хитрый торговец',8:'Паук',9:'Полсекунды',10:'Каталог готов',11:'Практика',
      12:'Тренажёр 1',13:'Тренажёр 2',14:'Тренажёр 3'};
    el.innerHTML = `<div class="s6 l542" data-frame="${f}"><h2>${ЗАГОЛОВКИ[f]||'Поиск'}</h2>${сцена}</div>`;
  }

  /* ================= ОБРАБОТЧИКИ ================= */
  window.r542Отв=(f,к)=>{ const s=S(); s['ответ'+f]=к; if(f===8&&к===0) s.дело_паук=true; chRender(0); };
  window.r542Указ=(n)=>{ const s=S(); const в=Array.isArray(s.указ2)?s.указ2.slice():[]; const к=в.indexOf(n);
    if(к>=0) в.splice(к,1); else в.push(n); s.указ2=в; s.пров2=false; chRender(0); };
  window.r542Записать=()=>{ const s=S(); s.пров2=true;
    if((s.указ2||[]).slice().sort((a,b)=>a-b).join(',')===МОРЕ.join(',')) s.дело_указатель=true; chRender(0); };
  window.r542Слово=(w)=>{ const s=S(); const в=Array.isArray(s.слова4)?s.слова4.slice():[]; const к=в.indexOf(w);
    if(к>=0) в.splice(к,1); else { в.push(w); if(в.length>2) в.shift(); } s.слова4=в; chRender(0); };
  window.r542Режим=(р)=>{ S().режим4=р; chRender(0); };
  window.r542Узел=(у)=>{ const s=S(); s.выбр6=у; if(у==='C') s.дело_ссылки=true; chRender(0); };
  window.r542Паук=(n)=>{ const s=S(); s.шаг8 = n ? Math.min(4,(s.шаг8||0)+1) : 0; chRender(0); };
  window.r542Pick=(уровень,вариант)=>{ const s=S(); s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1; chRender(0); };
  window.r542Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r542T=(ключ,вариант)=>{ const s=S(); if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const з=набор.задания[(s[ключ+'Шаг']||0)%набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0); };
  window.r542TNext=(ключ)=>{ const s=S(); const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length; s[ключ+'Ответ']=null; chRender(0); };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L542; else arr.push(L542); }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.IN542={render:render, L:L542};
})();
