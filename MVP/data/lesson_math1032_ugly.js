/* ============ МАТЕМАТИКА · УРОК 1032 · «УГЛЫ И МНОГОУГОЛЬНИКИ» ============
   5 класс. Сюжет: мастерская углов — циркуль, транспортир, угольник.

   ЭТАЛОН (995, 830, 932): движение в кадре — это САМА МАТЕМАТИКА.
     кадр 2  луч ПОВОРАЧИВАЕТСЯ из вершины — угол растёт на глазах;
     кадр 3  четыре угла: острый, прямой (с квадратиком), тупой,
             развёрнутый — лучи поворачиваются по очереди;
     кадр 4  ТРАНСПОРТИР ложится на угол, дуга идёт от 0 до 60°, подпись 60°;
     кадр 6  стороны многоугольников ЧЕРТЯТСЯ по одной: сторона дорисовывается
             от вершины к вершине;
     кадр 7  диагонали пятиугольника ПРОСТУПАЮТ по очереди;
     кадр 8  три угла треугольника СЪЕЗЖАЮТСЯ и складываются в развёрнутый
             угол 180° — это и есть доказательство суммы углов;
     кадр 9  у прямоугольника и квадрата появляются прямые углы, затем
             проступают равные диагонали;
     кадр 10 точка ОБХОДИТ многоугольник по периметру (animateMotion),
             и на табло растёт сумма сторон.

   БОЛЬШЕ ВСЕГО: 12 кадров объяснения + практика + ТРИ ТРЕНАЖЁРА
   (виды углов, многоугольники и их элементы, сумма углов и периметр).

   КАЧЕСТВО ПРОРИСОВКИ: циркуль с двумя ножками и держателем, транспортир
   с градусной шкалой, угольник, градиентные многоугольники с вершинами,
   дуги углов с подписями градусов, мягкие тени, знак прямого угла.

   СВЕРКА С ПЕТЕРСОНОМ (5 класс): «углы, виды углов, измерение углов»,
   «многоугольники, элементы многоугольника», «прямоугольник и квадрат»,
   «периметр». Источники: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, ничего за край кадра, prefers-reduced-motion
   выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 1032;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ЛИЛ='#c9a6f2', ОРА='#f2b06a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';

  const CSS=`
  #lvis .s6.l1032{gap:14px}
  #lvis .s6.l1032 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l1032 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l1032 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l1032 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l1032 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l1032 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l1032 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l1032 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l1032 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l1032 .правило b{color:${GOLD}}
  #lvis .s6.l1032 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l1032 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1032 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l1032 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l1032 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l1032 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l1032 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1032 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l1032 .уровни .точка.сейчас{background:${GOLD};animation:l1032dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l1032dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l1032 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l1032 .тренировка{width:100%;padding:12px 14px;border-radius:14px;border:1.5px dashed var(--line);
    font-size:16px;color:var(--mut);text-align:center}
  #lvis .s6.l1032{-webkit-text-size-adjust:100%}
  #lvis .s6.l1032 [data-anim]{animation:l1032rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l1032rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l1032 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l1032 [data-anim]{animation:none!important}
    #lvis .s6.l1032 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l1032 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l1032-style');
      if(!s){ s=document.createElement('style'); s.id='l1032-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анП = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анМ = (путь,длит) =>
    ДВИЖ ? `<animateMotion dur="${длит}" repeatCount="indefinite" path="${путь}"/>` : '';
  /* keyTimes всегда внутри [0;1] — иначе браузер ругается и анимация не идёт */
  const кт = (v) => Math.min(0.96, Math.max(0.02, v)).toFixed(2);
  const светит = (длит,нач) => ан('opacity','1;0.5;1',длит, нач!=null?'begin="'+нач+'s"':'');

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c1032-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22362c"/><stop offset="1" stop-color="#152219"/>
      </linearGradient>
      <linearGradient id="c1032-металл" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#eef3f8"/><stop offset="1" stop-color="#9fb2c4"/>
      </linearGradient>
      <linearGradient id="c1032-стекло" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#dff0fb" stop-opacity=".5"/><stop offset="1" stop-color="#7fd1ff" stop-opacity=".18"/>
      </linearGradient>
      <linearGradient id="c1032-фигура1" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#ffe6a3"/><stop offset="0.55" stop-color="#ffd76a"/><stop offset="1" stop-color="#c9971f"/>
      </linearGradient>
      <linearGradient id="c1032-фигура2" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#b6e8cd"/><stop offset="0.55" stop-color="#8fd1a8"/><stop offset="1" stop-color="#4f9b76"/>
      </linearGradient>
      <linearGradient id="c1032-фигура3" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#b9e6ff"/><stop offset="0.55" stop-color="#7fd1ff"/><stop offset="1" stop-color="#3f86b8"/>
      </linearGradient>
      <linearGradient id="c1032-фигура4" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#e6d2ff"/><stop offset="0.55" stop-color="#c9a6f2"/><stop offset="1" stop-color="#8a5fc0"/>
      </linearGradient>
      <filter id="c1032-тень" x="-30%" y="-30%" width="170%" height="170%">
        <feGaussianBlur stdDeviation="2.4"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||208}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const ЦВЕТА=['url(#c1032-фигура1)','url(#c1032-фигура2)','url(#c1032-фигура3)','url(#c1032-фигура4)'];

  /* УГОЛ: вершина, два луча, дуга и подпись градусов */
  function угол(vx,vy,луч1,луч2,подпись,цвет,нач){
    const рад=а=>а*Math.PI/180;
    const длина=62;
    const x1=vx+Math.cos(рад(луч1))*длина, y1=vy+Math.sin(рад(луч1))*длина;
    const x2=vx+Math.cos(рад(луч2))*длина, y2=vy+Math.sin(рад(луч2))*длина;
    const r=26;
    const a1=Math.min(луч1,луч2), a2=Math.max(луч1,луч2);
    const дугаX1=vx+Math.cos(рад(a1))*r, дугаY1=vy+Math.sin(рад(a1))*r;
    const дугаX2=vx+Math.cos(рад(a2))*r, дугаY2=vy+Math.sin(рад(a2))*r;
    const большая=(a2-a1)>180?1:0;
    return `<g>
      <line x1="${vx}" y1="${vy}" x2="${x1.toFixed(1)}" y2="${y1.toFixed(1)}" stroke="${ИНК}" stroke-width="2.4" stroke-linecap="round"/>
      <line x1="${vx}" y1="${vy}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}" stroke="${ИНК}" stroke-width="2.4" stroke-linecap="round"/>
      <path d="M${дугаX1.toFixed(1)} ${дугаY1.toFixed(1)} A${r} ${r} 0 ${большая} 1 ${дугаX2.toFixed(1)} ${дугаY2.toFixed(1)}"
        fill="none" stroke="${цвет||GOLD}" stroke-width="2.2">${светит('8s',нач)}</path>
      <circle cx="${vx}" cy="${vy}" r="4.5" fill="${GOLD}"/>
      ${подпись?`<g>${ан('opacity','0.3;0.3;1;1','8s','keyTimes="0;'+кт(нач||0.2)+';'+кт((нач||0.2)+0.07)+';1"')}
        ${т((vx+Math.cos(рад((a1+a2)/2))*(r+16)).toFixed(1),(vy+Math.sin(рад((a1+a2)/2))*(r+16)+5).toFixed(1),подпись,14,цвет||GOLD,true)}</g>`:''}
    </g>`;
  }

  /* ЗНАК ПРЯМОГО УГЛА */
  const прямойУгол = (vx,vy,д,поворот,нач) =>
    `<g transform="rotate(${поворот||0} ${vx} ${vy})">
      <path d="M${vx+д} ${vy} L${vx+д} ${vy-д} L${vx} ${vy-д}" fill="none" stroke="${GOLD}" stroke-width="2">
        ${светит('7s',нач)}</path>
    </g>`;

  /* ТРАНСПОРТИР: полукруг с градусной шкалой */
  function транспортир(цx,цy,r,уголГрадусов,нач){
    let s=`<path d="M${цx-r} ${цy} A${r} ${r} 0 0 1 ${цx+r} ${цy} Z" fill="url(#c1032-стекло)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;
    s+=`<line x1="${цx-r}" y1="${цy}" x2="${цx+r}" y2="${цy}" stroke="${ЛИНИЯ}" stroke-width="1.4"/>`;
    for(let i=0;i<=18;i++){
      const а=(180-i*10)*Math.PI/180;
      const длинный=(i%3===0);
      const x1=цx+Math.cos(а)*(r-8), y1=цy-Math.sin(а)*(r-8);
      const x2=цx+Math.cos(а)*r, y2=цy-Math.sin(а)*r;
      s+=`<line x1="${x1.toFixed(1)}" y1="${y1.toFixed(1)}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}"
        stroke="${ЛИНИЯ}" stroke-width="${длинный?1.5:0.9}"/>`;
      if(длинный) s+=`<g>${светит('9s',(нач||0)+i*0.12)}
        ${т((цx+Math.cos(а)*(r-22)).toFixed(1),(цy-Math.sin(а)*(r-22)+4).toFixed(1),String(180-i*10),12,МУТ)}</g>`;
    }
    s+=`<circle cx="${цx}" cy="${цy}" r="4" fill="${ИНК}"/>
      <g>${анП('0 '+цx+' '+цy+';0 '+цx+' '+цy+';'+уголГрадусов+' '+цx+' '+цy+';'+уголГрадусов+' '+цx+' '+цy,'9s','keyTimes="0;0.12;0.7;1" begin="'+(нач||0)+'s"')}
        <line x1="${цx}" y1="${цy}" x2="${цx}" y2="${(цy-r+10).toFixed(1)}" stroke="${GOLD}" stroke-width="2.6" stroke-linecap="round"/>
      </g>
`;
    return s;
  }

  /* МНОГОУГОЛЬНИК: стороны чертятся по очереди, вершины появляются, диагонали проступают */
  function многоугольник(точки,заливка,нач,диагонали,подписи){
    const центр=[точки.reduce((а,p)=>а+p[0],0)/точки.length, точки.reduce((а,p)=>а+p[1],0)/точки.length];
    let s=`<path d="M${точки.map(p=>p[0].toFixed(1)+' '+p[1].toFixed(1)).join(' L')} Z"
      fill="${заливка}" opacity="0.34" stroke="none"/>`;
    for(let i=0;i<точки.length;i++){
      const [x1,y1]=точки[i], [x2,y2]=точки[(i+1)%точки.length];
      s+=`<line x1="${x1}" y1="${y1}" x2="${x1}" y2="${y1}" stroke="${ИНК}" stroke-width="2.4" stroke-linecap="round">
        ${ан('x2',x1+';'+x1+';'+x2+';'+x2,'9s','keyTimes="0;'+кт((нач||0)+i*0.1)+';'+кт((нач||0)+0.2+i*0.1)+';1"')}
        ${ан('y2',y1+';'+y1+';'+y2+';'+y2,'9s','keyTimes="0;'+кт((нач||0)+i*0.1)+';'+кт((нач||0)+0.2+i*0.1)+';1"')}
      </line>`;
      if(подписи) s+=`<g>${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;'+кт((нач||0)+0.24+i*0.1)+';'+кт((нач||0)+0.32+i*0.1)+';1"')}
        ${т(((x1+x2)/2*1.2-центр[0]*0.2).toFixed(1),((y1+y2)/2*1.2-центр[1]*0.2+4).toFixed(1),подписи[i],12,ИНК)}</g>`;
      s+=`<circle cx="${x1}" cy="${y1}" r="4" fill="${GOLD}">
        ${ан('r','4;4;5.6;4','9s','keyTimes="0;'+кт((нач||0)+0.15+i*0.1)+';'+кт((нач||0)+0.24+i*0.1)+';1"')}</circle>`;
      s+=`<line x1="${(x1-6).toFixed(1)}" y1="${y1}" x2="${(x1+6).toFixed(1)}" y2="${y1}" stroke="${GOLD}" stroke-width="1.2">
        ${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;'+кт((нач||0)+0.18+i*0.1)+';'+кт((нач||0)+0.26+i*0.1)+';1"')}</line>`;
    }
    (диагонали||[]).forEach(([i,j],к)=>{
      s+=`<line x1="${точки[i][0]}" y1="${точки[i][1]}" x2="${точки[i][0]}" y2="${точки[i][1]}"
        stroke="${ЛИЛ}" stroke-width="1.8" stroke-dasharray="6 4">
        ${ан('x2',точки[i][0]+';'+точки[i][0]+';'+точки[j][0]+';'+точки[j][0],'9s','keyTimes="0;'+кт(0.5+к*0.06)+';'+кт(0.66+к*0.06)+';1"')}
        ${ан('y2',точки[i][1]+';'+точки[i][1]+';'+точки[j][1]+';'+точки[j][1],'9s','keyTimes="0;'+кт(0.5+к*0.06)+';'+кт(0.66+к*0.06)+';1"')}
      </line>`;
    });
    return s;
  }

  /* ================= КАДРЫ ================= */

  /* 1. Мастерская углов: инструменты действуют по очереди */
  function F1(s){
    const в = s.инструменты;
    return ВОПРОС('Какие инструменты помогают строить и измерять углы?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="208" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="208" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Мастерская углов',14,GOLD,true)}
        ${транспортир(96,150,62,60,0)}
        ${т(96,196,'транспортир',12,МУТ)}
        <g>${ан('opacity','0.35;0.35;1;1','9s','keyTimes="0;0.42;0.52;1"')}
          <path d="M196 150 L196 92 L268 150 Z" fill="url(#c1032-фигура3)" opacity=".35" stroke="${BLUE}" stroke-width="2.2"/>
          ${прямойУгол(196,150,16,0,0.5)}
        </g>
        ${т(232,196,'угольник',12,МУТ)}
        <g>${ан('opacity','0.35;0.35;1;1','9s','keyTimes="0;0.62;0.72;1"')}
          <circle cx="300" cy="60" r="7" fill="url(#c1032-металл)" stroke="#3a4750" stroke-width="1.6"/>
          <path d="M300 66 L286 148" stroke="url(#c1032-металл)" stroke-width="4" stroke-linecap="round"/>
          <path d="M300 66 L314 148" stroke="url(#c1032-металл)" stroke-width="4" stroke-linecap="round"/>
          <circle cx="286" cy="150" r="3" fill="${ИНК}"/><circle cx="314" cy="150" r="3" fill="${ИНК}"/>
          <g>${анП('-14 300 100;6 300 100;-14 300 100','6s')}
            <path d="M300 66 L286 148" stroke="${GOLD}" stroke-width="1.2" opacity=".45"/></g>
        </g>
        ${т(300,196,'циркуль',12,МУТ)}
      `,208)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='транспортир'?'hit':(в?'miss':''), 'Транспортир и угольник: измерять и строить углы', "r1032Ins('транспортир')")}
        ${BTN(4, в==='циркуль'?'miss':'', 'Только циркуль: он рисует любые углы', "r1032Ins('циркуль')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='транспортир',
        в==='транспортир' ? 'Верно: транспортиром измеряют угол в градусах, угольником проверяют прямой угол, циркулем откладывают равные отрезки.'
                         : 'Нет: циркуль откладывает расстояния. Углы измеряют транспортиром, а прямой угол проверяют угольником.') : ЖДЁТ()) +
      ПРАВИЛО('Угол измеряют <b>транспортиром</b>, прямой угол проверяют <b>угольником</b>.');
  }

  /* 2. Луч поворачивается — угол растёт */
  function F2(s){
    const в = s.поворот;
    const vx=96, vy=150;
    return ВОПРОС('Из одной точки выходят два луча. Как получается угол?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="208" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="208" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Луч поворачивается — угол растёт',14,GOLD,true)}
        <line x1="${vx}" y1="${vy}" x2="${vx+150}" y2="${vy}" stroke="${ИНК}" stroke-width="2.6" stroke-linecap="round"/>
        <g>${анП('0 '+vx+' '+vy+';0 '+vx+' '+vy+';-60 '+vx+' '+vy+';-60 '+vx+' '+vy+';0 '+vx+' '+vy,'9s','keyTimes="0;0.1;0.62;0.88;1"')}
          <line x1="${vx}" y1="${vy}" x2="${vx+150}" y2="${vy}" stroke="${BLUE}" stroke-width="2.6" stroke-linecap="round"/>
          <path d="M${vx+40} ${vy} A40 40 0 0 0 ${(vx+40*Math.cos(-60*Math.PI/180)).toFixed(1)} ${(vy+40*Math.sin(-60*Math.PI/180)).toFixed(1)}"
            fill="none" stroke="${GOLD}" stroke-width="2" opacity=".9"/>
        </g>
        <circle cx="${vx}" cy="${vy}" r="5" fill="${GOLD}"/>
        ${т(vx,vy+24,'вершина',12,МУТ)}
        ${т(vx+96,vy+22,'сторона',12,МУТ)}
        ${Array.from({length:10},(_,i)=>{
          const гр=i*10, а=гр*Math.PI/180;
          const x1=vx+Math.cos(а)*118, y1=vy-Math.sin(а)*118;
          const x2=vx+Math.cos(а)*128, y2=vy-Math.sin(а)*128;
          return `<line x1="${x1.toFixed(1)}" y1="${y1.toFixed(1)}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}"
            stroke="${ЛИНИЯ}" stroke-width="1.4">${ан('opacity','1;0.35;1','9s','begin="'+(i*0.08).toFixed(2)+'s"')}</line>`;
        }).join('')}
        ${Array.from({length:6},(_,i)=>{
          const о1=(i*10)*Math.PI/180, о2=((i+1)*10)*Math.PI/180, r=104;
          return `<path d="M${(vx+Math.cos(о1)*r).toFixed(1)} ${(vy-Math.sin(о1)*r).toFixed(1)}
            A${r} ${r} 0 0 0 ${(vx+Math.cos(о2)*r).toFixed(1)} ${(vy-Math.sin(о2)*r).toFixed(1)}"
            fill="none" stroke="${GOLD}" stroke-width="2.6">${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;'+кт(0.1+i*0.08)+';'+кт(0.18+i*0.08)+';1"')}</path>`;
        }).join('')}
        ${Array.from({length:7},(_,i)=>`<g>${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;'+кт(0.08+i*0.08)+';'+кт(0.16+i*0.08)+';1"')}
          ${т(44,vy-104+i*17,(i*10)+'°',12,i===6?GOLD:МУТ,true)}</g>`).join('')}
        <g>${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;'+кт(0.62)+';'+кт(0.72)+';1"')}
          ${т(vx+70,vy-52,'60°',16,GOLD,true)}</g>
        ${т(240,96,'угол — это',12,ИНК)}
        ${т(240,120,'два луча',12,ИНК)}
        ${т(240,144,'из одной точки',12,ИНК)}
      `,208)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='два луча'?'hit':(в?'miss':''), 'Два луча, выходящие из одной точки', "r1032Dva('два луча')")}
        ${BTN(4, в==='отрезок'?'miss':'', 'Два отрезка, соединённые концами', "r1032Dva('отрезок')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='два луча',
        в==='два луча' ? 'Верно: угол — это два луча с общим началом. Общая точка называется вершиной, лучи — сторонами угла.'
                      : 'Нет: у отрезка два конца. Угол образуют два луча, выходящие из одной точки — вершины.') : ЖДЁТ()) +
      ПРАВИЛО('Угол — <b>два луча из одной точки</b>: общая точка — вершина, лучи — стороны.');
  }

  /* 3. Виды углов: четыре угла в четырёх местах, лучи поворачиваются по очереди */
  function F3(s){
    const в = s.виды;
    return ВОПРОС('Как называется угол больше 90°, но меньше 180°?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="268" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="268" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Четыре вида углов',14,GOLD,true)}
        ${угол(56,64,0,-45,'45°',GREEN,0.1)}
        ${т(56,106,'острый',12,GREEN,true)}${т(56,126,'меньше 90°',12,МУТ)}
        ${угол(200,64,0,-90,null,GOLD,0.5)}${прямойУгол(200,64,18,0,0.6)}
        ${т(200,106,'прямой',12,GOLD,true)}${т(200,126,'ровно 90°',12,МУТ)}
        ${угол(56,180,0,-135,'135°',ОРА,1.2)}
        ${т(56,238,'тупой',12,ОРА,true)}${т(56,258,'от 90° до 180°',12,МУТ)}
        ${угол(200,180,0,-180,'180°',BLUE,1.8)}
        ${т(200,238,'развёрнутый',12,BLUE,true)}${т(200,258,'ровно 180°',12,МУТ)}
      `,268)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='тупой'?'hit':(в?'miss':''), 'Тупой: он больше прямого, но меньше развёрнутого', "r1032Vid('тупой')")}
        ${BTN(4, в==='острый'?'miss':'', 'Острый: он меньше прямого', "r1032Vid('острый')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='тупой',
        в==='тупой' ? 'Верно: больше 90°, но меньше 180° — тупой угол. Ровно 90° — прямой, ровно 180° — развёрнутый.'
                    : 'Нет: острый угол меньше 90°. Угол между 90° и 180° называется тупым.') : ЖДЁТ()) +
      ПРАВИЛО('Виды углов: <b>острый &lt; 90°</b>, <b>прямой = 90°</b>, <b>тупой</b> между 90° и 180°, <b>развёрнутый = 180°</b>.');
  }

  /* 4. Транспортир измеряет угол */
  function F4(s){
    const в = s.измерение;
    return ВОПРОС('Стрелка транспортира прошла от 0° до 60°. Какой это угол?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="208" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="208" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Измеряем угол транспортиром',14,GOLD,true)}
        ${транспортир(128,172,104,60,0)}
        ${т(128,200,'центр транспортира — в вершине угла',12,МУТ)}
        <g>${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;0.7;0.8;1"')}
          ${т(252,72,'60°',20,GREEN,true)}
          ${т(252,100,'острый угол',12,МУТ)}
          ${т(252,128,'меньше 90°',12,МУТ)}
          ${т(252,156,'по шкале',12,ИНК)}
          ${т(252,180,'от 0 до 60',12,GOLD,true)}
        </g>
      `,208)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='острый'?'hit':(в?'miss':''), 'Острый: 60° меньше 90°', "r1032Meas('острый')")}
        ${BTN(4, в==='тупой'?'miss':'', 'Тупой: стрелка прошла далеко', "r1032Meas('тупой')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='острый',
        в==='острый' ? 'Верно: 60° меньше 90°, значит угол острый. Транспортир ставят центром в вершину, а нулевую отметку — на сторону.'
                     : 'Нет: тупой угол больше 90°. Здесь 60°, то есть острый.') : ЖДЁТ()) +
      ПРАВИЛО('Транспортир ставят <b>центром в вершину</b>, ноль — на сторону; читают отметку, куда пришла вторая сторона.');
  }

  /* 5. Обозначение угла: рисунок слева, подписи справа */
  function F5(s){
    const в = s.обозначение;
    const vx=64, vy=150, длина=88;
    const рад=а=>а*Math.PI/180;
    const bx=vx+длина*Math.cos(рад(-50)), by=vy+длина*Math.sin(рад(-50));
    return ВОПРОС('Как обозначают угол с вершиной O и сторонами OA и OB?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="216" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="216" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Имена угла',14,GOLD,true)}
        <line x1="${vx}" y1="${vy}" x2="${(vx+длина).toFixed(1)}" y2="${vy}" stroke="${ИНК}" stroke-width="2.6" stroke-linecap="round"/>
        <line x1="${vx}" y1="${vy}" x2="${bx.toFixed(1)}" y2="${by.toFixed(1)}" stroke="${ИНК}" stroke-width="2.6" stroke-linecap="round"/>
        <path d="M${(vx+30).toFixed(1)} ${vy} A30 30 0 0 0 ${(vx+30*Math.cos(рад(-50))).toFixed(1)} ${(vy+30*Math.sin(рад(-50))).toFixed(1)}"
          fill="none" stroke="${GOLD}" stroke-width="2.2">${светит('8s',0.2)}</path>
        <circle cx="${vx}" cy="${vy}" r="5" fill="${GOLD}"/>
        <g>${ан('opacity','0.3;0.3;1;1','8s','keyTimes="0;'+кт(0.2)+';'+кт(0.28)+';1"')}${т(vx-14,vy+22,'O',16,GOLD,true)}</g>
        <g>${ан('opacity','0.3;0.3;1;1','8s','keyTimes="0;'+кт(0.36)+';'+кт(0.44)+';1"')}${т((vx+длина+12).toFixed(1),vy+6,'A',16,BLUE,true)}</g>
        <g>${ан('opacity','0.3;0.3;1;1','8s','keyTimes="0;'+кт(0.52)+';'+кт(0.6)+';1"')}
          ${т((bx-6).toFixed(1),(by-6).toFixed(1),'B',16,BLUE,true)}</g>
        <g>${ан('opacity','0.3;0.3;1;1','8s','keyTimes="0;'+кт(0.66)+';'+кт(0.76)+';1"')}
          ${т(246,64,'∠AOB',20,GREEN,true)}
          ${т(246,90,'или ∠BOA',14,МУТ)}
          ${т(246,118,'вершина O',12,ИНК)}
          ${т(246,140,'стороны OA и OB',12,ИНК)}
          ${т(246,166,'буква вершины',12,GOLD)}
          ${т(246,186,'в середине',12,GOLD)}
        </g>
      `,216)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='AOB'?'hit':(в?'miss':''), '∠AOB: буква вершины стоит в середине', "r1032Mark('AOB')")}
        ${BTN(4, в==='OAB'?'miss':'', '∠OAB: буквы по порядку лучей', "r1032Mark('OAB')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='AOB',
        в==='AOB' ? 'Верно: в обозначении угла буква вершины стоит в середине: ∠AOB или ∠BOA — это один и тот же угол.'
                  : 'Нет: вершину ставят в середину. ∠AOB — верная запись, ∠OAB так не пишут.') : ЖДЁТ()) +
      ПРАВИЛО('Угол обозначают тремя буквами, и <b>буква вершины — в середине</b>: ∠AOB.');
  }

  /* 6. Стороны многоугольников чертятся по очереди */
  function F6(s){
    const в = s.фигуры;
    return ВОПРОС('Как называется многоугольник с пятью сторонами?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="208" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="208" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Стороны чертятся по одной',14,GOLD,true)}
        ${многоугольник([[24,66],[92,42],[160,66]],ЦВЕТА[0],0,null,['a','b','c'])}
        ${т(92,108,'3 стороны',12,GOLD,true)}
        ${многоугольник([[190,58],[262,58],[262,120],[190,120]],ЦВЕТА[1],0.5,null,['a','b','c','d'])}
        ${т(226,146,'4 стороны',12,GREEN,true)}
        ${многоугольник([[24,150],[74,132],[124,150],[110,196],[38,196]],ЦВЕТА[2],1,null,null)}
        ${т(74,182,'5 сторон',12,BLUE,true)}
        <g>${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;0.8;0.88;1"')}
          ${т(240,178,'треугольник',12,МУТ)}
          ${т(240,196,'четырёхугольник, пятиугольник',12,МУТ)}
        </g>
      `,208)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пятиугольник'?'hit':(в?'miss':''), 'Пятиугольник: у него пять сторон и пять вершин', "r1032Fig('пятиугольник')")}
        ${BTN(4, в==='пятиконечник'?'miss':'', 'Пятиконечная фигура', "r1032Fig('пятиконечник')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пятиугольник',
        в==='пятиугольник' ? 'Верно: многоугольник называют по числу сторон и вершин: три — треугольник, четыре — четырёхугольник, пять — пятиугольник.'
                          : 'Нет: название даёт число сторон. Пять сторон — пятиугольник.') : ЖДЁТ()) +
      ПРАВИЛО('Многоугольник называют <b>по числу сторон</b>: 3 — треугольник, 4 — четырёхугольник, 5 — пятиугольник.');
  }

  /* 7. Элементы и диагонали */
  function F7(s){
    const в = s.элементы;
    return ВОПРОС('Сколько диагоналей у четырёхугольника?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="228" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="228" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Вершины, стороны и диагонали',14,GOLD,true)}
        ${многоугольник([[40,60],[150,50],[168,140],[56,158]],ЦВЕТА[3],0,[[0,2],[1,3]],['a','b','c','d'])}
        ${т(104,192,'4 вершины · 4 стороны · 2 диагонали',12,ИНК)}
        ${многоугольник([[214,52],[292,66],[300,138],[248,168],[196,128]],ЦВЕТА[2],0.4,
           [[0,2],[0,3],[1,3],[1,4],[2,4]],null)}
        ${т(248,214,'5 диагоналей',12,BLUE,true)}
      `,228)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='две'?'hit':(в?'miss':''), 'Две: они соединяют противоположные вершины', "r1032Diag('две')")}
        ${BTN(4, в==='четыре'?'miss':'', 'Четыре: по числу вершин', "r1032Diag('четыре')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='две',
        в==='две' ? 'Верно: у четырёхугольника две диагонали — они соединяют вершины, не стоящие рядом.'
                  : 'Нет: диагональ соединяет несоседние вершины. У четырёхугольника таких пар две.') : ЖДЁТ()) +
      ПРАВИЛО('Диагональ соединяет <b>несоседние вершины</b>: у четырёхугольника их две.');
  }

  /* 8. Сумма углов треугольника: три дуги последовательно заполняют 180° */
  function F8(s){
    const в = s.сумма;
    const vx=70, vy=144;
    const цx=68, цy=176;
    const рад=а=>а*Math.PI/180;
    const дуга=(от,до,r,цвет,нач,имя)=>{
      const x1=цx+r*Math.cos(рад(от)), y1=цy-r*Math.sin(рад(от));
      const x2=цx+r*Math.cos(рад(до)), y2=цy-r*Math.sin(рад(до));
      const сер=(от+до)/2;
      return `<g>
        <path d="M${x1.toFixed(1)} ${y1.toFixed(1)} A${r} ${r} 0 0 ${(до-от)>180?1:0} ${x2.toFixed(1)} ${y2.toFixed(1)}"
          fill="none" stroke="${цвет}" stroke-width="3.4" stroke-linecap="round">${светит('9s',нач)}</path>
        <line x1="${цx}" y1="${цy}" x2="${(цx+r*Math.cos(рад(от))).toFixed(1)}" y2="${(цy-r*Math.sin(рад(от))).toFixed(1)}"
          stroke="${цвет}" stroke-width="1.6" opacity=".55"/>
        <line x1="${цx}" y1="${цy}" x2="${(цx+r*Math.cos(рад(до))).toFixed(1)}" y2="${(цy-r*Math.sin(рад(до))).toFixed(1)}"
          stroke="${цвет}" stroke-width="1.6" opacity=".55"/>
        <g>${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;'+Math.min(0.88,нач+0.06).toFixed(2)+';'+Math.min(0.95,нач+0.12).toFixed(2)+';1"')}
          ${т((цx+(r+32)*Math.cos(рад(сер))).toFixed(1),(цy-(r+32)*Math.sin(рад(сер))+5).toFixed(1),имя,14,цвет,true)}</g>
      </g>`;
    };
    return ВОПРОС('Три угла треугольника сложили вместе. Какой угол получился?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="248" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="248" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Три угла вместе',14,GOLD,true)}
        ${т(150,42,'Треугольник и его углы',12,МУТ)}
        <path d="M${vx} ${vy} L${(vx+120)} ${vy} L${(vx+56)} ${(vy-88)} Z" fill="${GOLD}" opacity=".14" stroke="${ИНК}" stroke-width="2.2"/>
        ${угол(vx,vy,0,-36,'α',GREEN,0.1)}
        ${угол(vx+120,vy,180,144,'β',BLUE,0.3)}
        ${угол(vx+56,vy-88,126,54,'γ',ОРА,0.5)}
        ${т(150,196,'36° + 36° + 108° = 180°',12,ИНК)}
        <line x1="${цx}" y1="${цy}" x2="${цx+150}" y2="${цy}" stroke="${ИНК}" stroke-width="2.2" stroke-linecap="round"/>
        ${дуга(0,36,34,GREEN,0.2,'')}
        ${дуга(36,72,48,BLUE,0.9,'')}
        ${дуга(72,180,62,ОРА,1.6,'')}
        <g>${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;0.82;0.9;1"')}
          ${т(252,66,'180°',20,GREEN,true)}
          ${т(252,90,'развёрнутый',12,МУТ)}
          ${т(252,116,'α + β + γ',14,GOLD,true)}
          ${т(252,138,'= 36+36+108',12,ИНК)}
          ${т(252,160,'= 180°',16,GREEN,true)}
        </g>
        ${т(150,218,'три угла заполнили развёрнутый угол целиком',12,МУТ)}
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сто восемьдесят'?'hit':(в?'miss':''), '180°: вместе они образуют развёрнутый угол', "r1032Sum('сто восемьдесят')")}
        ${BTN(4, в==='сто'?'miss':'', '100°: углы складываются примерно', "r1032Sum('сто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сто восемьдесят',
        в==='сто восемьдесят' ? 'Верно: три угла вместе дают ровно 180° — развёрнутый угол. Это верно для любого треугольника.'
                              : 'Нет: получается ровно развёрнутый угол, то есть 180°. Сумма углов треугольника всегда 180°.') : ЖДЁТ()) +
      ПРАВИЛО('Сумма углов треугольника всегда <b>180°</b> — вместе они дают развёрнутый угол.');
  }

  /* 9. Прямоугольник и квадрат */
  function F9(s){
    const в = s.четырёх;
    return ВОПРОС('Чем квадрат отличается от других прямоугольников?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="208" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="208" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Прямоугольник и квадрат',14,GOLD,true)}
        ${многоугольник([[24,52],[152,52],[152,132],[24,132]],ЦВЕТА[0],0,[[0,2],[1,3]],null)}
        ${прямойУгол(24,52,16,90,0.4)}${прямойУгол(152,52,16,180,0.5)}
        ${прямойУгол(152,132,16,270,0.6)}${прямойУгол(24,132,16,0,0.7)}
        ${т(88,152,'прямоугольник',12,GOLD,true)}
        ${т(88,174,'все углы прямые',12,МУТ)}
        ${т(88,196,'диагонали равны',12,ИНК)}
        ${многоугольник([[210,52],[300,52],[300,142],[210,142]],ЦВЕТА[1],0.5,[[0,2],[1,3]],null)}
        ${прямойУгол(210,52,14,90,0.8)}${прямойУгол(300,52,14,180,0.9)}
        ${прямойУгол(210,142,14,0,1.0)}${прямойУгол(300,142,14,270,1.1)}
        ${т(255,152,'квадрат',12,GREEN,true)}
        ${т(255,174,'все стороны равны',12,МУТ)}
        ${т(255,196,'и углы прямые',12,ИНК)}
      `,208)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='стороны'?'hit':(в?'miss':''), 'У квадрата все четыре стороны равны', "r1032Quad('стороны')")}
        ${BTN(4, в==='углы'?'miss':'', 'У квадрата углы больше, чем у прямоугольника', "r1032Quad('углы')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='стороны',
        в==='стороны' ? 'Верно: квадрат — прямоугольник, у которого все стороны равны. Углы у обоих прямые, диагонали равны.'
                     : 'Нет: углы у квадрата и прямоугольника одинаковые — прямые. Разница в сторонах: у квадрата они все равны.') : ЖДЁТ()) +
      ПРАВИЛО('Квадрат — прямоугольник с <b>равными сторонами</b>; углы у обоих прямые.');
  }

  /* 10. Обход периметра */
  function F10(s){
    const в = s.периметр;
    const точки=[[40,60],[160,44],[268,80],[240,160],[86,168]];
    const путь='M'+точки.map(p=>p[0]+' '+p[1]).join(' L')+' Z';
    return ВОПРОС('Точка обошла весь пятиугольник по сторонам. Что она измерила?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="240" fill="url(#c1032-стена)"/>
        <rect x="0" y="0" width="336" height="240" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Обходим фигуру по сторонам',14,GOLD,true)}
        ${многоугольник(точки,ЦВЕТА[2],0,null,['7','5','4','8','6'])}
        <g>${анМ(путь,'10s')}
          <circle cx="40" cy="60" r="8" fill="${GOLD}" stroke="#8a5f05" stroke-width="1.6"/>
          <circle cx="40" cy="60" r="14" fill="${GOLD}" opacity=".22">
            ${светит('2s')}</circle>
        </g>
        <g>${ан('opacity','1;1;1;1','10s')}
          <rect x="60" y="176" width="216" height="56" rx="12" fill="rgba(255,255,255,.05)" stroke="${GOLD}" stroke-width="1.6"/>
          ${т(104,198,'периметр',12,МУТ)}
          ${т(104,220,'5+7+6+4+8 = 30',16,GREEN,true)}
          ${т(230,214,'сумма сторон',12,ИНК)}
        </g>
      `,240)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='периметр'?'hit':(в?'miss':''), 'Периметр: сумму всех сторон', "r1032Per('периметр')")}
        ${BTN(4, в==='площадь'?'miss':'', 'Площадь: место внутри фигуры', "r1032Per('площадь')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='периметр',
        в==='периметр' ? 'Верно: обход по сторонам даёт сумму длин всех сторон — это периметр: 5 + 7 + 6 + 4 + 8 = 30.'
                      : 'Нет: площадь — это место внутри. Обход по сторонам измеряет длину границы, то есть периметр.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Периметр</b> — сумма длин всех сторон многоугольника: 5+7+6+4+8 = 30.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Угол равен 90°. Как он называется?', варианты:[
        {т:'прямой', ок:true,  почему:'Ровно 90° — прямой угол.'},
        {т:'острый', ок:false, почему:'Острый угол меньше 90°.'}]},
    { вопрос:'Угол 120°. Какой это угол?', варианты:[
        {т:'тупой', ок:true,  почему:'Больше 90°, но меньше 180° — тупой.'},
        {т:'развёрнутый', ок:false, почему:'Развёрнутый — ровно 180°.'}]},
    { вопрос:'Сколько сторон у пятиугольника?', варианты:[
        {т:'пять', ок:true,  почему:'Название даёт число сторон: пять.'},
        {т:'четыре', ок:false, почему:'Четыре стороны у четырёхугольника.'}]},
    { вопрос:'Чему равна сумма углов треугольника?', варианты:[
        {т:'180°', ок:true,  почему:'Вместе углы образуют развёрнутый угол.'},
        {т:'90°', ок:false, почему:'90° — это один прямой угол, а углов три.'}]},
    { вопрос:'Стороны четырёхугольника 6, 4, 6 и 4. Чему равен периметр?', варианты:[
        {т:'20', ок:true,  почему:'6+4+6+4 = 20.'},
        {т:'10', ок:false, почему:'Это половина: сложить нужно все четыре стороны.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Сравниваем угол с 90° и 180°, считаем стороны и вершины, складываем длины сторон для периметра.') +
        `<div class="ask">${BTN(4,'','пройти заново','r1032Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r1032Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Острый &lt; 90°, прямой = 90°, тупой до 180°, развёрнутый = 180°; периметр — <b>сумма сторон</b>.');
  }

  /* 12–14. ТРИ ТРЕНАЖЁРА */
  const Т1={
    имя:'Тренажёр 1 · виды углов',
    задания:[
      { ф:'Угол 45°. Какой он?', о:['острый','прямой','тупой'], в:0, р:'45° меньше 90° — острый.' },
      { ф:'Угол 180°. Какой он?', о:['развёрнутый','прямой','тупой'], в:0, р:'180° — развёрнутый угол.' },
      { ф:'Угол 135°. Какой он?', о:['тупой','острый','развёрнутый'], в:0, р:'135° больше 90°, но меньше 180° — тупой.' },
      { ф:'Какой инструмент измеряет угол в градусах?', о:['транспортир','циркуль','линейка'], в:0, р:'Углы измеряют транспортиром.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · многоугольники',
    задания:[
      { ф:'Сколько вершин у шестиугольника?', о:['6','5','8'], в:0, р:'Сколько сторон, столько и вершин: 6.' },
      { ф:'Какая фигура имеет 4 стороны и все углы прямые?', о:['прямоугольник','треугольник','пятиугольник'], в:0,
        р:'4 стороны и прямые углы — прямоугольник.' },
      { ф:'Сколько диагоналей у пятиугольника?', о:['5','4','10'], в:0, р:'Из каждой вершины две несоседние — всего пять диагоналей.' },
      { ф:'Чем квадрат отличается от прямоугольника?', о:['все стороны равны','углы больше','сторон больше'], в:0,
        р:'У квадрата все стороны равны, углы такие же прямые.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · сумма углов и периметр',
    задания:[
      { ф:'Два угла треугольника 60° и 70°. Чему равен третий?', о:['50°','60°','70°'], в:0,
        р:'180 − 60 − 70 = 50°.' },
      { ф:'Стороны треугольника 5, 4 и 7. Чему равен периметр?', о:['16','12','20'], в:0, р:'5 + 4 + 7 = 16.' },
      { ф:'Сторона квадрата 6. Чему равен периметр?', о:['24','12','36'], в:0, р:'У квадрата четыре равные стороны: 6 · 4 = 24.' },
      { ф:'Может ли в треугольнике быть угол 190°?', о:['нет: сумма углов 180°','да, если он тупой','да, если стороны длинные'], в:0,
        р:'190° больше 180°, а сумма всех углов треугольника ровно 180°.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(1,'кап','') +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r1032T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r1032TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L1032={
    id: ID, title:'Углы и многоугольники', ico:'📐',
    src:'Математика · 5 класс · Геометрические фигуры', subj:'math',
    explain: [
      'В мастерской углов на столе лежат инструменты: транспортир с градусной шкалой, угольник с прямым углом и циркуль с двумя ножками. Транспортиром измеряют углы в градусах, угольником проверяют, прямой ли угол, циркулем откладывают равные отрезки. С этого и начинается разговор об углах и многоугольниках.',

      'Угол образуют два луча, выходящие из одной точки. Общая точка называется вершиной, а сами лучи — сторонами угла. Если поворачивать один луч вокруг вершины, угол растёт: от нуля до полного оборота. Величину угла измеряют в градусах. Частая ошибка — считать, что длина сторон влияет на угол: стороны можно продолжить, угол от этого не изменится. Как проверить себя: поверни луч и посмотри, изменился ли раствор между лучами.',

      'Углы различают по величине. Острый угол меньше 90°, прямой равен ровно 90°, тупой больше 90°, но меньше 180°, развёрнутый равен 180° — его стороны образуют прямую линию. Прямой угол отмечают знаком квадратика. Частая ошибка — называть тупым всякий «широкий» угол, не сравнив его с 90° и 180°. Как проверить себя: приложи угольник: если стороны совпали с его сторонами — угол прямой.',

      'Измеряют угол транспортиром. Центр транспортира ставят в вершину угла, а нулевую отметку — на одну из сторон. Тогда вторая сторона укажет на шкале величину угла. Если сторона пришла к отметке 60, угол равен 60° — он острый. Частая ошибка — ставить транспортир не в вершину, а сдвинуть его. Как проверить себя: центр транспортира и вершина угла должны совпасть.',

      'Угол обозначают тремя буквами: на сторонах ставят по букве и одну букву у вершины. Записывают ∠AOB, причём буква вершины стоит в середине. Запись ∠AOB и ∠BOA означает один и тот же угол: важен только порядок — вершина в середине. Частая ошибка — ставить букву вершины с краю. Как проверить себя: найди середину записи — там должна быть буква вершины.',

      'Многоугольник — это фигура из отрезков, соединённых концами. Концы отрезков называются вершинами, а сами отрезки — сторонами. Название даёт число сторон: три — треугольник, четыре — четырёхугольник, пять — пятиугольник. Частая ошибка — путать сторону и вершину. Как проверить себя: посчитай отрезки — их столько же, сколько вершин у любой замкнутой фигуры.',

      'Кроме сторон у многоугольника есть диагонали — отрезки, соединяющие несоседние вершины. У четырёхугольника две диагонали, у пятиугольника пять. Диагонали помогают разбивать фигуру на треугольники. Частая ошибка — считать диагональю сторону. Как проверить себя: у диагонали концы не соединены стороной.',

      'Углы треугольника связаны замечательным свойством: если сложить все три угла, получится ровно 180°, то есть развёрнутый угол. Это верно для любого треугольника — маленького и большого. Частая ошибка — думать, что у большого треугольника сумма углов больше. Как проверить себя: сложи три угла вместе и посмотри, что получилась прямая линия.',

      'Четырёхугольники бывают разные. У прямоугольника все углы прямые, а противоположные стороны равны; диагонали у него тоже равны. Квадрат — это прямоугольник, у которого все четыре стороны равны. Частая ошибка — считать квадрат «не прямоугольником»: квадрат как раз прямоугольник, только с равными сторонами. Как проверить себя: проверь углы угольником — у обоих они прямые.',

      'Периметр многоугольника — сумма длин всех его сторон. Если обойти фигуру по границе, получится как раз периметр: у пятиугольника со сторонами 5, 7, 6, 4 и 8 периметр равен 5 + 7 + 6 + 4 + 8 = 30. Частая ошибка — складывать не все стороны или путать периметр с площадью. Как проверить себя: пройди по границе и сложи длины всех отрезков.',

      'Пройти практику: пять уровней — виды углов, стороны и вершины, сумма углов, периметр. Соберём правило: угол — два луча из одной точки, вершина стоит в середине обозначения; острый угол меньше 90°, прямой равен 90°, тупой идёт до 180°, развёрнутый равен 180°; многоугольник называют по числу сторон, диагональ соединяет несоседние вершины, сумма углов треугольника 180°, а периметр — сумма длин всех сторон.',

      'Первый тренажёр — про виды углов: сравниваем угол с 90° и 180° и называем его. Здесь важно не спешить и каждый раз задавать себе два вопроса: больше ли угол прямого и меньше ли развёрнутого.',

      'Второй тренажёр — про многоугольники и их элементы: считаем стороны, вершины и диагонали, вспоминаем, чем квадрат отличается от прямоугольника. Диагональ соединяет несоседние вершины — это и есть ключ к счёту.',

      'Третий тренажёр — про сумму углов и периметр: находим третий угол треугольника по двум известным и складываем длины всех сторон. Помни: сумма углов треугольника всегда 180°, а периметр — это сумма сторон.',
    ],
    check: {
      q:'Угол 120°. Какой это угол?',

      choices:['тупой','острый','развёрнутый'],
      ans:0,
      exp:'120° больше 90°, но меньше 180° — тупой угол.'
    },
    tasks: [
      { q:'Угол равен 90°. Как он называется?', kind:'choice',
        choices:['прямой','острый','тупой'], ans:0,
        hints:['Сравни с 90°.','Ровно 90° — прямой.'], sol:'Прямой угол.' },
      { q:'Сколько диагоналей у пятиугольника?', kind:'choice',
        choices:['5','4','3'], ans:0,
        hints:['Диагональ соединяет несоседние вершины.','Из каждой вершины их две.'], sol:'Пять диагоналей.' },
      { q:'Чему равна сумма углов треугольника?', kind:'choice',
        choices:['180°','90°','360°'], ans:0,
        hints:['Вместе углы дают развёрнутый угол.','180°.'], sol:'Сумма углов треугольника равна 180°.' },
      { q:'Стороны четырёхугольника 6, 4, 6 и 4. Чему равен периметр?', kind:'choice',
        choices:['20','10','24'], ans:0,
        hints:['Периметр — сумма всех сторон.','6 + 4 + 6 + 4.'], sol:'Периметр равен 20.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L1032.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Инструменты','Транспортир, угольник, циркуль'],
      2:['Угол','Два луча из вершины'],
      3:['Виды углов','Острый, прямой, тупой, развёрнутый'],
      4:['Измерение','Транспортир в вершине'],
      5:['Обозначение','∠AOB: вершина в середине'],
      6:['Многоугольники','Стороны чертятся по одной'],
      7:['Диагонали','Несоседние вершины'],
      8:['Сумма углов','Вместе 180°'],
      9:['Прямоугольник и квадрат','Прямые углы и равные стороны'],
      10:['Периметр','Обход по границе'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','Виды углов'],
      13:['Тренажёр 2','Многоугольники'],
      14:['Тренажёр 3','Сумма углов и периметр']
    };
    const з=ЗАГОЛОВКИ[f]||['Геометрия','Углы и многоугольники'];
    el.innerHTML = `<div class="s6 l1032" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r1032Ins=(к)=>{ S().инструменты=к; chRender(0); };
  window.r1032Dva=(к)=>{ S().поворот=к; chRender(0); };
  window.r1032Vid=(к)=>{ S().виды=к; chRender(0); };
  window.r1032Meas=(к)=>{ S().измерение=к; chRender(0); };
  window.r1032Mark=(к)=>{ S().обозначение=к; chRender(0); };
  window.r1032Fig=(к)=>{ S().фигуры=к; chRender(0); };
  window.r1032Diag=(к)=>{ S().элементы=к; chRender(0); };
  window.r1032Sum=(к)=>{ S().сумма=к; chRender(0); };
  window.r1032Quad=(к)=>{ S().четырёх=к; chRender(0); };
  window.r1032Per=(к)=>{ S().периметр=к; chRender(0); };
  window.r1032Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r1032Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r1032T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r1032TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L1032; else arr.push(L1032);
  })();
  window.MA1032={render:render, L:L1032};
})();
