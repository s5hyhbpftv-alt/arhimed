/* ============ РУССКИЙ ЯЗЫК · УРОК 881 · «СЛОГ, УДАРЕНИЕ, ФОНЕТИЧЕСКИЙ РАЗБОР» ============
   5 класс, ФРП, II четверть, раздел «Фонетика. Орфоэпия».

   СВЕРКА С УЧЕБНИКОМ (Ладыженская, 5 класс, рабочая программа):
     • «Ударение в слове. Гласные ударные и безударные»;
     • «Фонетический разбор слова»;
     • «Орфоэпия. Произносительные нормы литературного языка.
        Орфоэпические словари»;
     • «Умение соблюдать основные правила литературного произношения».

   ДЕСЯТЬ КАДРОВ, у каждого свой рисунок и свой приём:
     1. слог — сколько в слове гласных, столько и слогов;
     2. ударение — громкость: ударный слог сильнее;
     3. ударение меняет смысл — замок и замок;
     4. орфоэпия — как правильно: звонИт, каталОг, щавЕль, тОрты;
     5. транскрипция — квадратные скобки и знаки;
     6. характеристика звука — конструктор признаков;
     7. фонетический разбор — карточка слова «ёжик» по шагам;
     8. перенос слов — где можно разорвать слово;
     9. своё слово — разбор слова «мороз» по шагам;
    10. тренажёр со счётом.

   СТАНДАРТ: заголовок 24, текст кадра 20, подпись 16, служебное 14; в SVG
   кегли только 12/14/16/20/24/32; цели касания от 48 px; движение только
   вертикальное и прозрачность; есть prefers-reduced-motion. */
(function(){
  'use strict';

  const ID = 881;

  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';

  const CSS=`
  #lvis .s6.l881{gap:14px}
  #lvis .s6.l881 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l881 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l881 .sheet{width:100%;padding:16px 14px;border-radius:16px;background:linear-gradient(180deg,#22362c,#17261e);
    border:1.5px solid var(--line);display:flex;flex-direction:column;gap:8px}
  #lvis .s6.l881 .sheet p{margin:0;font-size:20px;line-height:1.5}
  #lvis .s6.l881 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l881 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left}
  #lvis .s6.l881 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l881 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l881 button[disabled]{opacity:.45}
  #lvis .s6.l881 .verdict{font-size:16px;line-height:1.45}
  #lvis .s6.l881 .verdict.ok{color:var(--ok)}
  #lvis .s6.l881 .verdict.no{color:var(--no)}
  #lvis .s6.l881 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l881 .band{display:flex;flex-direction:column;gap:10px;width:100%}
  #lvis .s6.l881 .band button{min-height:56px;padding:14px;text-align:left;font-size:20px;line-height:1.4;border-radius:14px}
  #lvis .s6.l881 .steps{display:flex;flex-direction:column;gap:8px;width:100%}
  #lvis .s6.l881 .steps .st{display:flex;gap:12px;align-items:center;padding:12px 14px;border-radius:14px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line);min-height:56px;font-size:20px;color:var(--mut)}
  #lvis .s6.l881 .steps .st.on{border-color:var(--gold);color:var(--ink)}
  #lvis .s6.l881 .steps .st .n{flex:none;width:34px;height:34px;border-radius:10px;display:flex;align-items:center;
    justify-content:center;font-size:16px;font-weight:700;color:#17261e;background:linear-gradient(180deg,#ffd76a,#d9a441)}
  #lvis .s6.l881 [data-anim]{animation:l881rise .42s cubic-bezier(.23,1,.32,1) both;animation-delay:calc(var(--i,0)*70ms)}
  #lvis .s6.l881[data-frame="3"] [data-anim],#lvis .s6.l881[data-frame="7"] [data-anim]{animation-name:l881pop}
  #lvis .s6.l881[data-frame="5"] [data-anim],#lvis .s6.l881[data-frame="10"] [data-anim]{animation-name:l881pop}
  @keyframes l881rise{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:none}}
  @keyframes l881pop{0%{opacity:0;transform:scale(.95)}70%{transform:scale(1.03)}100%{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l881 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){ #lvis .s6.l881 [data-anim]{animation:none!important} }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l881-style');
      if(!s){ s=document.createElement('style'); s.id='l881-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c881-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22362c"/><stop offset="1" stop-color="#152219"/>
      </linearGradient>
      <linearGradient id="c881-плита" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2c4536"/><stop offset="1" stop-color="#1b2f22"/>
      </linearGradient>
      <radialGradient id="c881-звук" cx="0.5" cy="0.4" r="0.7">
        <stop offset="0" stop-color="#fff2c8"/><stop offset="0.6" stop-color="#ffd76a"/>
        <stop offset="1" stop-color="#c9971f"/>
      </radialGradient>
      <linearGradient id="c881-бумага" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#fdf6e0"/><stop offset="1" stop-color="#e6d8b4"/>
      </linearGradient>
      <filter id="c881-тень" x="-30%" y="-30%" width="160%" height="160%">
        <feGaussianBlur stdDeviation="2.6"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||176}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* плитка-слог */
  const слог = (x,y,б,акцент) => `
    <g>
      <rect x="${x}" y="${y}" width="44" height="52" rx="10" fill="url(#c881-плита)"
        stroke="${акцент?GOLD:ЛИНИЯ}" stroke-width="${акцент?2.4:1.6}"/>
      ${т(x+22,y+36,б,24,акцент?GOLD:ИНК,true)}
      ${акцент?`<path d="M${x+14} ${y-8} L${x+30} ${y-8}" stroke="${GOLD}" stroke-width="3" stroke-linecap="round"/>`:''}
    </g>`;

  /* динамик с полосками громкости */
  const динамик = (x,y,уровень) => `
    <g transform="translate(${x} ${y})">
      <path d="M-26 -10 L-12 -10 L2 -22 L2 22 L-12 10 L-26 10 Z" fill="url(#c881-плита)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${[0,1,2].map(i=>`<path d="M10 ${-8+i*8} q10 ${4} 0 8" fill="none"
        stroke="${i<уровень?GOLD:МУТ}" stroke-width="2.4" opacity="${i<уровень?1:0.35}" stroke-linecap="round"/>`).join('')}
    </g>`;

  /* замок: крепость и навесной замок */
  const крепость = (x,y) => `
    <g transform="translate(${x} ${y})">
      <rect x="-34" y="-8" width="68" height="42" rx="4" fill="#8a8f96" stroke="#4a5158" stroke-width="1.4"/>
      ${[-30,-16,-2,12].map(дx=>`<rect x="${дx}" y="-16" width="12" height="10" fill="#8a8f96" stroke="#4a5158" stroke-width="1.2"/>`).join('')}
      <rect x="-8" y="10" width="16" height="24" rx="3" fill="#5a3a1c"/>
    </g>`;

  const навесной = (x,y) => `
    <g transform="translate(${x} ${y})">
      <path d="M-14 -18 q14 -20 28 0" fill="none" stroke="#c9b591" stroke-width="4"/>
      <rect x="-20" y="-18" width="40" height="34" rx="6" fill="url(#c881-плита)" stroke="${GOLD}" stroke-width="2"/>
      <circle cx="0" cy="-4" r="4" fill="#17261e"/>
    </g>`;

  /* карточка слова с ударением */
  const слово = (x,y,текст,ударный) => {
    const ч=[]; let cx=x+10;
    for(const б of текст){
      const акц=ударный!=null && текст[ударный]===б && б!=='ё';
      ч.push(`<text x="${cx}" y="${y+26}" font-size="24" font-weight="bold" fill="${акц?GOLD:ИНК}"
        font-family="Georgia,serif">${esc(б)}</text>`);
      if(акц) ч.push(`<path d="M${cx-5} ${y+2} L${cx+9} ${y+2}" stroke="${GOLD}" stroke-width="2.6" stroke-linecap="round"/>`);
      cx += б==='м'||б==='ш' ? 22 : 18;
    }
    return `<g>${ч.join('')}</g>`;
  };

  /* ================= КАДРЫ ================= */

  /* 1. Слог */
  function F1(s){
    const в = s.слоги;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c881-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${слог(26,50,'мо',false)}${слог(76,50,'ло',false)}${слог(126,50,'ко',false)}
      ${т(98,26,'мо-ло-ко',16,GOLD,true)}
      ${т(98,126,'три гласных — три слога',12,ИНК)}
      ${слог(204,50,'с',false)}${слог(254,50,'лон',false)}
      ${т(250,26,'слон',16,BLUE,true)}
      ${т(250,126,'один гласный — один слог',12,ИНК)}
    `,176);
    const ответ = в==null?'':(в==='3'
      ? A(3,'verdict ok','✅ Верно: в слове «молоко» три гласных — значит, три слога: мо-ло-ко.')
      : A(3,'verdict no','❌ Нет: посчитай гласные. Их три — значит, и слогов три.'));
    return `<div class="pic">${рис}</div>
      ${A(1,'cap','Слог — часть слова, которую произносят одним толчком. Сколько гласных, столько слогов.')}
      <div class="ask">
        ${BTN(2, в==='3'?'hit':(в?'miss':''), 'В слове «молоко» три слога', "r881Sl('3')")}
        ${BTN(3, в==='2'?'miss':'', 'В слове «молоко» два слога', "r881Sl('2')")}
      </div>
      ${ответ}`;
  }

  /* 2. Ударение */
  function F2(s){
    const в = s.громкость;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="172" fill="url(#c881-стена)"/>
      <rect x="0" y="0" width="336" height="172" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${динамик(70,74, в==='второй'?3:(в==='первый'?1:0))}
      ${слог(150,48,'звон', в==='первый')}${слог(200,48,'о', в==='второй')}${слог(244,48,'к', false)}
      ${т(168,124,'ударный слог звучит сильнее',12,GOLD)}
      ${т(168,146,'звонОк — ударение на второй слог',12,МУТ)}
    `,172);
    const ответ = в==null?'':(в==='второй'
      ? A(3,'verdict ok','✅ Верно: ударение на «о» во втором слоге — звонОк. Первый гласный безударный.')
      : A(3,'verdict no','❌ Нет: послушай себя — сильнее звучит «о»: звонОк.'));
    return `<div class="pic">${рис}</div>
      <div class="ask">
        ${BTN(1, в==='второй'?'hit':(в?'miss':''), 'Ударение на второй слог', "r881Gr('второй')")}
        ${BTN(2, в==='первый'?'miss':'', 'Ударение на первый слог', "r881Gr('первый')")}
      </div>
      ${ответ}`;
  }

  /* 3. Ударение меняет смысл */
  function F3(s){
    const в = s.смысл;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c881-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${крепость(88,74)}
      ${навесной(248,74)}
      ${т(88,142,'зАмок',16,GOLD,true)}
      ${т(88,162,'крепость',12,МУТ)}
      ${т(248,142,'замОк',16,BLUE,true)}
      ${т(248,162,'запор',12,МУТ)}
      ${т(168,26,'Одни буквы — разные слова',14,'#ffe9a8',true)}
    `,176);
    const ответ = в==null?'':(в==='крепость'
      ? A(2,'verdict ok','✅ Верно: «зАмок» — это крепость, ударение на первый слог. От перемены ударения слово меняется.')
      : A(2,'verdict no','❌ Нет: запор — это «замОк», ударение на второй слог.'));
    return `<div class="pic">${рис}</div>
      <div class="ask">
        ${BTN(1, в==='крепость'?'hit':(в?'miss':''), '«зАмок» — это крепость', "r881Sm('крепость')")}
        ${BTN(2, в==='запор'?'miss':'', '«зАмок» — это запор', "r881Sm('запор')")}
      </div>
      ${ответ}`;
  }

  /* 4. Орфоэпия */
  const ОРФО = [
    { с:'звонит', в:1, подсказка:'звонИт' , ошибка:'звОнит' },
    { с:'каталог', в:1, подсказка:'каталОг', ошибка:'катАлог' },
    { с:'щавель', в:1, подсказка:'щавЕль', ошибка:'щАвель' },
    { с:'торты', в:0, подсказка:'тОрты', ошибка:'тортЫ' }
  ];
  function F4(s){
    const и = (s.орфо||0)%ОРФО.length, з=ОРФО[и];
    const отв = s.орфоОтв, готово = отв!=null;
    const верно = отв===з.в;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="150" fill="url(#c881-стена)"/>
      <rect x="0" y="0" width="336" height="150" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      <g>
        <circle cx="168" cy="52" r="26" fill="url(#c881-звук)" opacity=".9"/>
        <path d="M156 46 q12 8 24 0 M156 58 q12 -8 24 0" fill="none" stroke="#8a5f05" stroke-width="2"/>
      </g>
      ${т(168,104,'орфоэпия — наука о правильном произношении',12,ИНК)}
      ${т(168,128,'ударение в спорных словах проверяют по словарю',12,МУТ)}
    `,150);
    return `<div class="pic">${рис}</div>
      ${A(1,'sheet','<p>Как правильно: <b style="color:'+GOLD+'">'+з.подсказка+'</b> или <b style="color:'+RED+'">'+з.ошибка+'</b>?</p>')}
      <div class="ask">
        ${BTN(2, готово?(з.в===0?(верно?'hit':'miss'):(отв===0?'miss':'')):'', з.подсказка, 'r881Orfo(0)')}
        ${BTN(3, готово?(з.в===1?(верно?'hit':'miss'):(отв===1?'miss':'')):'', з.ошибка, 'r881Orfo(1)')}
      </div>
      ${готово ? A(4,'verdict '+(верно?'ok':'no'),
        (верно?'✅ Верно: ':'❌ Не так: ')+'правильно «'+з.подсказка+'». Так говорят в литературном языке.') : A(4,'verdict','Выбери ответ.')}
      ${готово ? `<div class="ask">${BTN(5,'','другое слово','r881OrfoNext()')}</div>` : ''}`;
  }

  /* 5. Транскрипция */
  function F5(s){
    const в = s.транскр;
    const рис = свг(`
      <rect x="0" y="0" width="336" height="176" fill="url(#c881-стена)"/>
      <rect x="0" y="0" width="336" height="176" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      ${т(168,30,'пишем: молоко',16,ИНК,true)}
      <rect x="70" y="56" width="196" height="52" rx="12" fill="url(#c881-плита)" stroke="${GOLD}" stroke-width="2"/>
      ${т(168,90,'[малако́]',24,GOLD,true)}
      ${т(168,132,'в скобках — то, что слышим',12,ИНК)}
      ${т(168,154,'знак ́ ставится над ударным гласным',12,МУТ)}
    `,176);
    const ответ = в==null?'':(в==='слышим'
      ? A(3,'verdict ok','✅ Верно: в квадратных скобках записывают звуки, то есть то, что мы слышим и произносим.')
      : A(3,'verdict no','❌ Нет: в скобках пишут не буквы, а звуки. Буквы пишут без скобок.'));
    return `<div class="pic">${рис}</div>
      <div class="ask">
        ${BTN(1, в==='слышим'?'hit':(в?'miss':''), 'В скобках пишут то, что слышим', "r881Tr('слышим')")}
        ${BTN(2, в==='буквы'?'miss':'', 'В скобках пишут буквы', "r881Tr('буквы')")}
      </div>
      ${ответ}`;
  }

  /* 6. Конструктор признаков звука */
  const ПРИЗНАКИ = [
    ['гласный','[о] — поётся, преграды нет',GOLD],
    ['согласный','[к] — есть преграда',BLUE],
    ['твёрдый','[к] — твёрдый',GREEN],
    ['мягкий','[к\u2019] — мягкий',GREEN],
    ['звонкий','[г] — с голосом',GOLD],
    ['глухой','[к] — без голоса',BLUE]
  ];
  function F6(s){
    const открыт = s.признак;
    return `${A(0,'cap','Характеристика звука собирается из признаков. Нажимай и читай пример.')}
      <div class="steps">${ПРИЗНАКИ.map(([н,п,ц],и)=>
        BTN(1+и,'st'+(открыт===и?' on':''),`<span class="n">${и+1}</span><span>${открыт===и?н+' — '+п:н}</span>`,`r881Feat(${и})`)).join('')}</div>
      ${открыт!=null ? A(8,'cap','У гласного признак один — ударный он или безударный. У согласного признаков три: твёрдость, звонкость и парность.') : ''}`;
  }

  /* 7. Фонетический разбор: ёжик */
  const РАЗБОР7 = [
    ['Слоги и ударение','ё-жик: два слога, ударение на первый'],
    ['Звуки','[йо́жык]'],
    ['Признаки','[й] согл. мягк. зв.; [о] гласн. ударн.; [ж] согл. тв. зв.; [ы] гласн. безуд.; [к] согл. тв. глух.'],
    ['Буквы и звуки','4 буквы, 5 звуков: буква ё дала два звука']
  ];
  function F7(s){
    const открыто = s.разбор7||0;
    const готово = открыто===РАЗБОР7.length;
    return `${A(0,'cap','Фонетический разбор слова «ёжик» идёт по шагам. Открывай по одному.')}
      <div class="steps">${РАЗБОР7.map(([к,ч],и)=>
        `<div class="st${и<открыто?' on':''}"><span class="n">${и+1}</span><span>${и<открыто?к+' — '+ч:'шаг не открыт'}</span></div>`).join('')}</div>
      <div class="ask">${BTN(1,'', готово?'разобрать заново':'следующий шаг', 'r881Step7()')}</div>
      ${готово ? A(2,'verdict ok','✅ Разбор готов: ёжик — 4 буквы, 5 звуков. Буква ё в начале слова дала два звука.')
               : A(2,'cap','Открыто шагов: '+открыто+' из '+РАЗБОР7.length)}`;
  }

  /* 8. Перенос слов */
  const ПЕРЕНОС = [
    { с:'мо-ло-ко', ок:true, п:'можно: слово делится по слогам' },
    { с:'мол-око', ок:false, п:'нельзя: «око» — не слог этого деления' },
    { с:'маль-чик', ок:true, п:'можно: ь остаётся с буквой' },
    { с:'мальч-ик', ок:false, п:'нельзя: одну букву и не оставляют' }
  ];
  function F8(s){
    const выбран = s.перенос;
    const выб = ПЕРЕНОС[выбран];
    const рис = свг(`
      <rect x="0" y="0" width="336" height="150" fill="url(#c881-стена)"/>
      <rect x="0" y="0" width="336" height="150" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
      <rect x="52" y="40" width="232" height="70" rx="12" fill="url(#c881-бумага)" stroke="#c3b48c" stroke-width="1.4"/>
      ${т(96,82,'мо-ло-',24,'#2a2118',true)}
      ${т(190,82,'ко',24,'#2a2118',true)}
      ${т(168,132,'перенос — по слогам, без одинокой буквы',12,ИНК)}
    `,150);
    return `<div class="pic">${рис}</div>
      <div class="band">${ПЕРЕНОС.map((п,и)=>
        BTN(1+и, выбран===и?(п.ок?'hit':'miss'):'', 'перенести так: '+п.с, `r881Hyp(${и})`)).join('')}</div>
      ${выбран!=null ? A(5,'verdict '+(выб.ок?'ok':'no'), (выб.ок?'✅ ':'❌ ')+выб.п) : A(5,'cap','Выбери, как слово можно перенести.')}`;
  }

  /* 9. Своё слово: мороз */
  const РАЗБОР9 = [
    ['Слоги и ударение','мо-ро́з: два слога, ударение на второй'],
    ['Звуки по порядку','[маро́с]'],
    ['Гласные и согласные','2 гласных, 3 согласных; [с] на конце — глухой'],
    ['Буквы и звуки','5 букв, 5 звуков: расхождения нет, но слышим [с], а пишем з']
  ];
  function F9(s){
    const открыто = s.разбор9||0;
    const готово = открыто===РАЗБОР9.length;
    return `${A(0,'cap','Теперь разбери сам слово «мороз» — по тем же шагам.')}
      <div class="steps">${РАЗБОР9.map(([к,ч],и)=>
        `<div class="st${и<открыто?' on':''}"><span class="n">${и+1}</span><span>${и<открыто?к+' — '+ч:'шаг не открыт'}</span></div>`).join('')}</div>
      <div class="ask">${BTN(1,'', готово?'разобрать заново':'следующий шаг', 'r881Step9()')}</div>
      ${готово ? A(2,'verdict ok','✅ Разбор готов. Заметь: пишем «мороз», а слышим [маро́с] — так работает оглушение на конце слова.')
               : A(2,'cap','Открыто шагов: '+открыто+' из '+РАЗБОР9.length)}`;
  }

  /* 10. Тренажёр */
  const ТРЕНАЖЁР = [
    { ф:'Сколько слогов в слове «молоко»?', о:['три','два','четыре'], в:0,
      р:'сколько гласных, столько слогов: мо-ло-ко.' },
    { ф:'Как правильно сказать?', о:['звонИт','звОнит','звонит с ударением на первый слог'], в:0,
      р:'литературная норма — звонИт.' },
    { ф:'Сколько звуков в слове «ёжик»?', о:['пять','четыре','шесть'], в:0,
      р:'буква ё дала два звука: 4 буквы, 5 звуков.' },
    { ф:'Можно ли перенести «мальчик» как «мальч-ик»?', о:['нельзя: одну букву не оставляют','можно','можно, если не влезает'], в:0,
      р:'одна буква не остаётся на строке: маль-чик.' }
  ];
  function F10(s){
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length, з=ТРЕНАЖЁР[и], отв=s.тОтвет, готово=отв!=null, верно=отв===з.в;
    return `${A(0,'kicker','Тренажёр')}
      ${A(1,'sheet','<p>'+з.ф+'</p>')}
      <div class="ask">${з.о.map((о,к)=>BTN(2+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r881Train(${к})`)).join('')}</div>
      ${готово ? A(6,'verdict '+(верно?'ok':'no'), (верно?'Верно: ':'Не так: ')+з.р) : A(6,'verdict','Выбери ответ.')}
      <p class="score">верно: ${s.тВерно||0} · ошибок: ${s.тОшибки||0} · всего: ${ТРЕНАЖЁР.length}</p>
      ${готово ? `<div class="ask">${BTN(7,'','следующий вопрос','r881NextQ()')}</div>` : ''}`;
  }

  const ВОПРОСЫ = {
    1: ['Сколько слогов в слове?', [
      {к:'a', т:'столько, сколько гласных', ок:1, fb:'верно: молоко — три гласных, три слога'},
      {к:'b', т:'столько, сколько букв', ок:0, fb:'буквы считают отдельно; слог считают по гласным'}]],

    2: ['Как найти ударный слог?', [
      {к:'a', т:'позвать слово и услышать сильный гласный', ок:1, fb:'верно: звонОк'},
      {к:'b', т:'посчитать буквы', ок:0, fb:'счётом букв ударение не находится'}]],

    3: ['Меняет ли ударение смысл слова?', [
      {к:'a', т:'да: зАмок и замОк — разные слова', ок:1, fb:'верно'},
      {к:'b', т:'нет, смысл тот же', ок:0, fb:'зАмок — крепость, замОк — запор'}]],

    4: ['Как проверить ударение в спорном слове?', [
      {к:'a', т:'посмотреть в орфоэпическом словаре', ок:1, fb:'верно: орфоэпия — наука о произношении'},
      {к:'b', т:'как говорят друзья', ок:0, fb:'в речи бывают ошибки; норма — в словаре'}]],

    5: ['Что записывают в квадратных скобках?', [
      {к:'a', т:'звуки, которые слышим', ок:1, fb:'верно: [малако́]'},
      {к:'b', т:'буквы', ок:0, fb:'буквы пишут без скобок'}]],

    6: ['Сколько признаков у согласного звука?', [
      {к:'a', т:'твёрдость, звонкость и парность', ок:1, fb:'верно: твёрдый или мягкий, звонкий или глухой'},
      {к:'b', т:'один — ударность', ок:0, fb:'ударность — признак гласного'}]],

    7: ['Сколько звуков в слове «ёжик»?', [
      {к:'a', т:'пять', ок:1, fb:'верно: [йо́жык]'},
      {к:'b', т:'четыре', ок:0, fb:'четыре буквы, но звуков пять'}]],

    8: ['Как переносят слова?', [
      {к:'a', т:'по слогам, не оставляя одну букву', ок:1, fb:'верно: маль-чик, но не мальч-ик'},
      {к:'b', т:'как удобно', ок:0, fb:'есть правила: одну букву не оставляют, ь не отрывают'}]],

    9: ['Почему в слове «мороз» слышим [с]?', [
      {к:'a', т:'звук оглушается на конце слова', ок:1, fb:'верно: [маро́с], а пишем «мороз»'},
      {к:'b', т:'мы ошибаемся', ок:0, fb:'это закономерность, а не ошибка'}]],

    10: ['Что делают первым в фонетическом разборе?', [
      {к:'a', т:'делят слово на слоги и ставят ударение', ок:1, fb:'верно'},
      {к:'b', т:'считают звуки', ок:0, fb:'счёт — последний шаг'}]]
  };

  function pred(f, st){
    const в = ВОПРОСЫ[f]; if(!в) return '';
    const [вопрос, варианты] = в;
    const cur = st['в'+f];
    const выбран = варианты.find(о=>о.к===cur);
    return `<div class="ask">${варианты.map((о,и)=>
      BTN(10+и, cur===о.к?(о.ок?'hit':'miss'):'', о.т, `r881Ask(${f},'${о.к}')`)).join('')}</div>
      ${cur ? `<div class="verdict ${выбран.ок?'ok':'no'}">${выбран.ок?'✅ ':'❌ '}${выбран.fb}</div>`
            : `<div class="verdict">Выбери ответ.</div>`}
      <div class="cap">${вопрос}</div>`;
  }

  const L881 = {
    id: ID, title:'Слог, ударение, фонетический разбор', ico:'🔊',
    src:'Русский язык · 5 класс · Фонетика и графика', subj:'rus',
    explain: [
      'Слог — часть слова, которую произносят одним толчком воздуха. Слогов в слове столько, сколько в нём гласных звуков: «мо-ло-ко» — три гласных, три слога; «слон» — один гласный, один слог. Частая ошибка — считать слоги по числу букв: в слове «молоко» шесть букв, но слогов три. Как проверить себя: произнеси слово и похлопай — каждый хлопок и есть слог.',

      'Ударение — это выделение одного слога силой голоса. Ударный слог произносится громче и длиннее, остальные — слабее. В слове «звонок» сила голоса падает на второй слог: звонОк. Ударение в русском языке свободное: оно может стоять на любом слоге, и это надо запоминать. Частая ошибка — ставить ударение наугад. Как проверить себя: позови слово, будто зовёшь его издалека, — ударный слог сам станет громче.',

      'Ударение может менять смысл слова. «зАмок» — старинная крепость, «замОк» — запор на двери. Так же: «мУка» и «мукА», «Атлас» и «атлАс», «оргАн» и «Орган». Частая ошибка — считать, что ударение — мелочь: от него зависит, какое слово перед нами. Как проверить себя: поставь ударение по-разному и посмотри, получаются ли разные значения.',

      'Орфоэпия — наука о правильном произношении. В спорных словах ударение проверяют по орфоэпическому словарю, а не «как говорят вокруг». Запомни литературную норму: звонИт, каталОг, щавЕль, тОрты, красИвее, позвонИшь. Частая ошибка — говорить «звОнит» и «катАлог»: так говорят в быту, но это не норма. Как проверить себя: если сомневаешься — открой словарь, там ударение отмечено.',

      'В квадратных скобках записывают звуки, то есть то, что мы слышим и произносим. Такую запись называют транскрипцией. Пишем «молоко», а слышим [малако́]: безударные «о» звучат как [а]. Знак ударения ставят над ударным гласным. Частая ошибка — писать в скобках буквы. Как проверить себя: прочитай вслух и запиши то, что слышишь, а не то, что видишь.',

      'Каждый звук получает характеристику. У гласного она короткая: ударный или безударный. У согласного признаков три: твёрдый или мягкий, звонкий или глухой, парный или непарный. Мягкость в транскрипции показывают знаком: [к\u2019]. Частая ошибка — называть у согласного «ударность». Как проверить себя: произнеси звук отдельно и ответь на три вопроса — твёрдый? звонкий? есть ли пара?',

      'Фонетический разбор делают по шагам. Первый — разделить слово на слоги и поставить ударение. Второй — записать звуки в квадратных скобках. Третий — дать характеристику каждому звуку. Четвёртый — посчитать буквы и звуки и объяснить расхождение. Разберём слово «ёжик»: ё-жик, [йо́жык], четыре буквы, пять звуков. Частая ошибка — перескакивать сразу к счёту звуков. Как проверить себя: иди по шагам по порядку, ничего не пропуская.',

      'Слова переносят по слогам, но не как попало. Одну букву на строке не оставляют и не переносят: «мальч-ик» — неверно, правильно «маль-чик». Мягкий знак не отрывают от предыдущей буквы. Нельзя переносить слово, если после переноса часть не читается как слог. Частая ошибка — рвать слово посередине слога. Как проверить себя: прочитай обе части вслух — каждая должна звучать как слог.',

      'Разберём ещё одно слово — «мороз». Слоги: мо-ро́з, ударение на второй слог. Звуки: [маро́с]. Гласных два, согласных три; последний звук [с] — глухой, хотя буква з. Букв пять и звуков пять, но написание и звучание расходятся: на конце слова звонкий оглушается. Частая ошибка — писать в транскрипции «мороз». Как проверить себя: скажи слово и послушай последний звук — он глухой.',

      'Соберём правило. Слогов столько, сколько гласных. Ударный слог сильнее; ударение может менять смысл слова. В спорных словах смотри орфоэпический словарь: звонИт, каталОг, щавЕль, тОрты. В квадратных скобках записывают звуки, а не буквы. Согласный описывают тремя признаками: твёрдость, звонкость, парность. Разбор идёт по шагам: слоги и ударение → звуки → характеристика → счёт букв и звуков. Слова переносят по слогам, не оставляя одну букву. Дальше — тренажёр со счётом.'
    ],
    check: {
      q:'Сколько слогов в слове «молоко»?',
      choices:['три','два','шесть'],
      ans:0,
      exp:'Слогов столько, сколько гласных звуков: мо-ло-ко — три гласных, три слога.'
    },
    tasks: [
      { q:'Сколько звуков в слове «ёжик»?', kind:'unit', ans:5, tol:0,
        hints:['Буква ё в начале слова даёт два звука.','Запиши: [йо́жык].'],
        sol:'Пять звуков: [й], [о], [ж], [ы], [к]; букв четыре.' },
      { q:'Как правильно сказать?', kind:'choice',
        choices:['звонИт','звОнит','звОнит и звонИт одинаково'], ans:0,
        hints:['Смотри орфоэпический словарь.','Ударение падает на второй слог.'],
        sol:'Литературная норма — звонИт.' },
      { q:'Можно ли перенести «мальчик» как «мальч-ик»?', kind:'choice',
        choices:['нельзя: одну букву не оставляют','можно','можно, если не влезает'], ans:0,
        hints:['Часть после переноса должна читаться как слог.','Правильно: маль-чик.'],
        sol:'Нельзя: одна буква не остаётся на строке.' },
      { q:'Что записывают в квадратных скобках при разборе?', kind:'choice',
        choices:['звуки','буквы','слоги'], ans:0,
        hints:['Скобки показывают то, что мы слышим.','Пишем «молоко», слышим [малако́].'],
        sol:'Звуки: [малако́].' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L881.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else сцена=F10(s);

    const ЗАГОЛОВКИ = {
      1:['Слог','Сколько гласных — столько слогов'],
      2:['Ударение','Сильный слог'],
      3:['Смысл','зАмок и замОк'],
      4:['Орфоэпия','Как правильно'],
      5:['Транскрипция','Что слышим, что пишем'],
      6:['Признаки','Конструктор характеристики'],
      7:['Разбор','Фонетический разбор «ёжик»'],
      8:['Перенос','Где можно разорвать слово'],
      9:['Своё слово','Разбор слова «мороз»'],
      10:['Практика','Тренажёр: слог и ударение']
    };
    const [кикер, заголовок] = ЗАГОЛОВКИ[f] || ['Фонетика','Слог и ударение'];

    el.innerHTML = `<div class="s6 l881" data-frame="${f}">
        ${A(0,'kicker',`Урок 881 · ${кикер}`)}
        <h2>${заголовок}</h2>
        ${сцена}
        ${pred(f, s)}
      </div>`;
  }

  window.r881Ask = (f,k) => { S()['в'+f]=k; chRender(0); };
  window.r881Sl = (к) => { S().слоги=к; chRender(0); };
  window.r881Gr = (к) => { S().громкость=к; chRender(0); };
  window.r881Sm = (к) => { S().смысл=к; chRender(0); };
  window.r881Orfo = (к) => { const s=S(); if(s.орфоОтв==null) s.орфоОтв=к; chRender(0); };
  window.r881OrfoNext = () => { const s=S(); s.орфо=((s.орфо||0)+1)%ОРФО.length; s.орфоОтв=null; chRender(0); };
  window.r881Tr = (к) => { S().транскр=к; chRender(0); };
  window.r881Feat = (и) => { const s=S(); s.признак = (s.признак===и ? null : и); chRender(0); };
  window.r881Step7 = () => { const s=S(); s.разбор7 = (s.разбор7||0)>=РАЗБОР7.length ? 0 : (s.разбор7||0)+1; chRender(0); };
  window.r881Hyp = (и) => { S().перенос=и; chRender(0); };
  window.r881Step9 = () => { const s=S(); s.разбор9 = (s.разбор9||0)>=РАЗБОР9.length ? 0 : (s.разбор9||0)+1; chRender(0); };
  window.r881Train = (к) => {
    const s=S();
    if(s.тОтвет != null) return;
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length;
    s.тОтвет=к;
    if(к===ТРЕНАЖЁР[и].в) s.тВерно=(s.тВерно||0)+1; else s.тОшибки=(s.тОшибки||0)+1;
    chRender(0);
  };
  window.r881NextQ = () => {
    const s=S();
    s.тНомер=((s.тНомер||0)+1)%ТРЕНАЖЁР.length;
    s.тОтвет=null;
    chRender(0);
  };

  if(window.WAVE_B){
    const прежний = window.WAVE_B[ID];
    window.WAVE_B[ID] = function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr = window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место = arr.findIndex(L=>L && L.id===ID);
    if(место >= 0) arr[место]=L881; else arr.push(L881);
  })();
  window.RU881 = {render:render, L:L881};
})();
