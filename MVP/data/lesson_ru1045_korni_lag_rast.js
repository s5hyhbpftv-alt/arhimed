/* ============ РУССКИЙ ЯЗЫК · УРОК 1045 · «КОРНИ -ЛОЖ- И -ЛАГ-, -РОС- И -РАСТ-» ====
   5 класс, «Орфография корня». Продолжение линии 877–1044.

   ФОРМАТ — КАК У РУССКИХ УРОКОВ: window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT (.kicker, h2, .lead, .cap, .steps/.st,
   .ex-line, .cases, .rail, .pair/.card/.tag/.row/.split, .verdict, .score).

   ИНТЕРАКТИВ В КАДРАХ (состояние в CHS, перерисовка через chRender):
     кадр 1  «показать правило» — подсказывает, на что смотреть;
     кадр 3  выбор буквы в паре -лаг-/-лож- с объяснением;
     кадр 6  выбор буквы в паре -раст-/-рос-;
     кадр 9  «проверь исключение» — тренажёр исключений;
     кадр 13 разбор по клику: корень и объяснение;
     кадр 16 тренажёр из десяти заданий.

   ПРАВИЛА (школьный курс, 5 класс):
     корень -ЛАГ-/-ЛОЖ-: перед Г пишем А, перед Ж пишем О:
       предлагать — предложить, излагать — изложить, прилагательное — приложение;
     корень -РАСТ-/-РАЩ-/-РОС-: перед СТ и Щ пишем А, перед С пишем О:
       растение, растущий, наращивать — вырос, росла, водоросли;
     ИСКЛЮЧЕНИЯ: росток, ростовщик, Ростов, Ростислав, отрасль — пишем О;
     эти корни НЕ проверяются ударением: перед нами чередование гласных. */
(function(){
  'use strict';

  const ID = 1045;
  const R = window.RUKIT;
  if(!R){ return; }
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;
  const пр    = (t)=>`<p class="ex-line">${esc(t)}</p>`;
  const рельс = (стиль,...xs)=>`<div class="rail" style="${стиль||''}">${xs.join('')}</div>`;
  const двa    = (a,b)=>`<div class="cases">${a}${b}</div>`;
  const шаги  = (items)=>`<div class="steps">${items.map(([t,s],i)=>
    `<div class="st"><span class="n">${i+1}</span><span class="t">${esc(t)}<small>${esc(s)}</small></span></div>`).join('')}</div>`;
  const кноп  = (t,on)=>`<button type="button" class="chip" style="flex:1 1 40%;font-size:17px" onclick="${on}">${esc(t)}</button>`;
  const кнопки= (...xs)=>`<div class="row">${xs.join('')}</div>`;

  const ПАРЫ_ЛАГ=[
    ['предл_гать','а','Перед Г пишем А: предлагать.'],
    ['предл_жить','о','Перед Ж пишем О: предложить.'],
    ['изл_гать','а','Перед Г пишем А: излагать.'],
    ['прил_гательное','а','Перед Г пишем А: прилагательное.']
  ];
  const ПАРЫ_РАСТ=[
    ['р_стение','а','Перед СТ пишем А: растение.'],
    ['выр_с','о','Перед С пишем О: вырос.'],
    ['нар_щивать','а','Перед Щ пишем А: наращивать.'],
    ['водор_сли','о','Перед С пишем О: водоросли.']
  ];
  const ИСКЛЮЧЕНИЯ=[
    ['росток','Пишем О, хотя перед нами СТ: это исключение.'],
    ['ростовщик','Исключение: пишем О.'],
    ['Ростов','Название города — исключение.'],
    ['Ростислав','Имя — исключение.'],
    ['отрасль','Исключение: пишем О перед СТ.']
  ];
  const РАЗБОР=[
    ['предлагать','корень -лаг-, перед Г — А','Проверка не ударением, а согласной после корня.'],
    ['предложить','корень -лож-, перед Ж — О','Та же приставка, другой корень — и другая буква.'],
    ['растение','корень -раст-, перед СТ — А','СТ требуют А.'],
    ['вырос','корень -рос-, перед С — О','Перед С пишем О.'],
    ['отрасль','исключение','Пишем О, хотя перед нами СТ.']
  ];

  const GAME=[
    ['предл_гать — какая буква?','а','Перед Г пишем А.','о'],
    ['предл_жить — какая буква?','о','Перед Ж пишем О.','а'],
    ['р_стение — какая буква?','а','Перед СТ пишем А.','о'],
    ['выр_с — какая буква?','о','Перед С пишем О.','а'],
    ['нар_щивать — какая буква?','а','Перед Щ пишем А.','о'],
    ['водор_сли — какая буква?','о','Перед С пишем О.','а'],
    ['р_сток — какая буква?','о','Исключение: пишем О перед СТ.','а'],
    ['отр_сль — какая буква?','о','Исключение: пишем О.','а'],
    ['изл_гать — какая буква?','а','Перед Г пишем А.','о'],
    ['прил_гательное — какая буква?','а','Перед Г пишем А.','о']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    const кн = варианты.map(о=>кноп(о,`ru1045Game('${esc(о)}')`)).join('');
    const ответ = готово ? (верно ? R.NOTE('ok','верно',it[2]) : R.NOTE('no','исправить',it[2]))
                         : `<p class="verdict">Сначала посмотри, какая согласная стоит после корня.</p>`;
    return `${kick('16 · Тренажёр')}
      ${word(it[0],'вставь букву: а или о')}
      ${ответ}
      ${кнопки(кн)}
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее слово.':'Сначала согласная, потом буква.')}`;
  }

  const подсказка_1 = cap('Эти корни не проверяют ударением: смотри на согласную после корня.');

  window.RULESSON({id:ID, game:GAME, frames:[
    (st)=>{
      const ок = st.о1!=null;
      const отв = ок ? `<p class="verdict ok">Смотрим на согласную после корня: в «предлагать» стоит Г, значит А. В «предложить» стоит Ж, значит О.</p>`
                     : `<p class="verdict">Нажми кнопку — покажу, на что смотреть в этих словах.</p>`;
      return `${kick('01 · Разминка')}
        ${h2('Слова, где букву выбирает соседняя согласная')}
        ${lead('Мы разбирали приставки и окончания. Сегодня заглянем в корень: есть корни, где гласную выбирает не ударение, а согласная после корня.')}
        ${двa(пр('предлагать'),пр('предложить'))}
        ${отв}
        ${кнопки(кноп('показать правило','ru1045Правило()'))}
        ${pair(card('перед Г','пишем А: предлагать'),card('перед Ж','пишем О: предложить'))}
        ${подсказка_1}`;
    },

    ()=>`${kick('02 · Два правила')}
      ${h2('Корни -лаг-/-лож- и -раст-/-рос-')}
      ${шаги([['-ЛАГ- / -ЛОЖ-','перед Г — А, перед Ж — О'],['-РАСТ- / -РАЩ-','перед СТ и Щ — А'],['-РОС-','перед С — О'],['проверка','смотрим на согласную после корня, а не на ударение']])}
      ${cards([['лаг','предлагать, излагать, прилагательное'],['лож','предложить, изложить, приложение'],['раст / ращ','растение, растущий, наращивать'],['рос','вырос, росла, водоросли']])}
      ${cap('Корни с чередованием ударением не проверяются.')}`,

    (st)=>{
      const к = (st.о3||0);
      const п = ПАРЫ_ЛАГ[к];
      const кн = ПАРЫ_ЛАГ.map((х,i)=>кноп(х[0].replace('_','…'),`ru1045Лаг(${i})`)).join('');
      return `${kick('03 · Интерактив: -лаг- или -лож-')}
        ${h2('Нажми слово — увидишь букву и объяснение')}
        ${кнопки(кн)}
        ${пр(п[0].replace('_', ' (?) '))}
        <p class="verdict ok">Пишем «${esc(п[1])}»: ${esc(п[2])}</p>
        ${двa(card('перед Г','А'),card('перед Ж','О'))}
        ${cap('Ключ один: посмотри на букву сразу после корня.')}`;
    },

    ()=>`${kick('04 · Корень -лаг- / -лож-')}
      ${h2('Г требует А, Ж требует О')}
      ${lead('В этом корне всё решает согласная: перед Г пишем А, перед Ж пишем О.')}
      ${cards([['предлагать','перед Г — А'],['предложить','перед Ж — О'],['излагать — изложить','одна приставка, разные буквы'],['прилагательное','перед Г — А']])}
      ${пр('Слагаемое, сложение, возлагать, возложить — тот же закон.')}
      ${cap('Проверять ударением бесполезно: «предлОжит» и «предлагАть» — разные корни.')}`,

    ()=>`${kick('05 · Корень -раст- / -рос-')}
      ${h2('СТ и Щ требуют А, С требует О')}
      ${lead('В корне с чередованием раст/ращ/рос: перед СТ и Щ пишем А, перед С — О.')}
      ${cards([['растение','перед СТ — А'],['растущий','перед Щ — А'],['вырос','перед С — О'],['водоросли','перед С — О']])}
      ${пр('Нарастание, наращивать, поросль, подросток — тот же закон.')}
      ${cap('Считай буквы: СТ и Щ — сигнал А, одиночная С — сигнал О.')}`,

    (st)=>{
      const к = (st.о6||0);
      const п = ПАРЫ_РАСТ[к];
      const кн = ПАРЫ_РАСТ.map((х,i)=>кноп(х[0].replace('_','…'),`ru1045Рост(${i})`)).join('');
      return `${kick('06 · Интерактив: -раст- или -рос-')}
        ${h2('Нажми слово — увидишь букву и объяснение')}
        ${кнопки(кн)}
        ${пр(п[0].replace('_',' (?) '))}
        <p class="verdict ok">Пишем «${esc(п[1])}»: ${esc(п[2])}</p>
        ${двa(card('СТ или Щ','А'),card('одиночная С','О'))}
        ${cap('Внимание: исключения из этого правила существуют, о них дальше.')}`;
    },

    ()=>`${kick('07 · Исключения')}
      ${h2('Пять слов, которые пишутся не по правилу')}
      ${lead('Их запоминают: росток, ростовщик, Ростов, Ростислав, отрасль. Во всех пишется О, хотя рядом СТ.')}
      ${рельс('flex-wrap:wrap',chip('росток',RED),chip('ростовщик',RED),chip('Ростов',RED),chip('Ростислав',RED),chip('отрасль',RED))}
      ${пр('Запоминалка: «Ростовщик из Ростова по имени Ростислав вырастил росток в отрасли».')}
      ${cap('От слова «росток» образовано «подросток» и «ростковый» — там тоже О.')}`,

    ()=>`${kick('08 · Как рассуждать')}
      ${h2('Порядок действий')}
      ${шаги([['1 найди корень','лаг, лож, раст, ращ или рос'],['2 посмотри на согласную','Г, Ж, СТ, Щ или С'],['3 выбери букву','Г и СТ и Щ — А; Ж и С — О'],['4 проверь исключения','росток, ростовщик, Ростов, Ростислав, отрасль']])}
      ${пр('Наращивать → корень -ращ-, перед Щ → А. Вырос → корень -рос-, перед С → О.')}
      ${cap('Четыре шага — и буква выбрана верно.')}`,

    (st)=>{
      const к = (st.о9||0);
      const п = ИСКЛЮЧЕНИЯ[к];
      const кн = ИСКЛЮЧЕНИЯ.map((х,i)=>кноп(х[0],`ru1045Искл(${i})`)).join('');
      return `${kick('09 · Интерактив: исключения')}
        ${h2('Нажми слово — увидишь объяснение')}
        ${lead('Эти пять слов пишутся не по правилу: в них О, хотя рядом СТ.')}
        ${кнопки(кн)}
        ${пр('Выбрано: ' + п[0])}
        <p class="verdict ok">${esc(п[1])}</p>
        ${cap('Исключения надо помнить наизусть — правила для них не работает.')}`;
    },

    ()=>`${kick('10 · Сравни пары')}
      ${h2('Одна приставка — разные корни')}
      ${cards([['предлагать — предложить','Г → А, Ж → О'],['излагать — изложить','Г → А, Ж → О'],['прилагательное — приложение','Г → А, Ж → О'],['расти — вырос','СТ → А, С → О']])}
      ${пр('Сравнение помогает запомнить: буква меняется вместе с согласной.')}
      ${cap('Так устроены все корни с чередованием.')}`,

    ()=>`${kick('11 · Почему нельзя проверить ударением')}
      ${h2('Чередование — не проверяемая гласная')}
      ${lead('В проверяемых корнях мы ищем родственное слово с ударением. Здесь так нельзя: ударение падает на разные гласные, а правило остаётся про согласную.')}
      ${двa(пр('проверяемая: вОда — вОды'),пр('чередование: растЕние — не «рОст»'))}
      ${cards([['проверяемая гласная','ищем ударение'],['чередование','смотрим на согласную или суффикс -а-']])}
      ${cap('Определи сначала, какая это орфограмма, потом применяй правило.')}`,

    ()=>`${kick('12 · Ошибки, которые встречаются чаще всего')}
      ${h2('Три типичные ошибки')}
      ${cards([['«предлогать» ✗','перед Г нужна А: предлагать'],['«рощение» ✗','перед Щ нужна А: ращение, наращивать'],['«рости» ✗','перед СТ нужна А: расти, но росток — исключение']])}
      ${пр('Проверяй себя вопросом: какая согласная стоит сразу после корня?')}
      ${cap('Одна ошибка тянет за собой другую: проверяй по шагам.')}`,

    (st)=>{
      const к = (st.о13||0);
      const п = РАЗБОР[к];
      const кн = РАЗБОР.map((х,i)=>кноп('разбор '+(i+1),`ru1045Разбор(${i})`)).join('');
      return `${kick('13 · Интерактив: разбор по клику')}
        ${h2('Нажми слово — увидишь корень и объяснение')}
        ${кнопки(кн)}
        ${пр(п[0])}
        ${cards([['корень',п[1]],['почему',п[2]]])}
        ${cap('Сравни разборы: у каждого слова свой сигнал — согласная после корня.')}`;
    },

    ()=>`${kick('14 · Ловушка')}
      ${h2('Исключения и проверка ударением')}
      ${lead('Две ловушки сразу: пытаются проверить корень ударением и забывают пять исключений.')}
      ${двa(пр('«рОстение» ✗ проверяли словом «рост»'),пр('«расток» ✗ забыли исключение «росток»'))}
      ${cards([['проверять ударением','нельзя: это чередование'],['исключения','росток, ростовщик, Ростов, Ростислав, отрасль']])}
      ${cap('Сначала определи орфограмму, потом действуй.')}`,

    ()=>`${kick('15 · В карман')}
      ${h2('Три фразы, которые стоит унести')}
      ${cards([['-лаг-/-лож-','перед Г — А, перед Ж — О'],['-раст-/-ращ-/-рос-','СТ и Щ — А, С — О'],['исключения','росток, ростовщик, Ростов, Ростислав, отрасль']])}
      ${рельс('flex-wrap:wrap',chip('Г → А'),chip('Ж → О'),chip('СТ, Щ → А'),chip('С → О'))}
      ${cap('Ударением такие корни не проверяют.')}`,

    (st)=>тренер(st)
  ]});

  const L1045={
    id:ID, title:'Корни -лож- и -лаг-, -рос- и -раст-', ico:'🌱',
    src:'Русский язык · 5 класс · Орфография корня', subj:'rus', group:null,
    explain:[
      'Есть корни, где гласную выбирает не ударение, а согласная после корня. Сегодня два таких корня: -лаг-/-лож- и -раст-/-ращ-/-рос-.',
      'Корень -лаг-/-лож-: перед Г пишем А (предлагать, излагать, прилагательное), перед Ж пишем О (предложить, изложить, приложение).',
      'Корень -раст-/-ращ-: перед СТ и Щ пишем А — растение, растущий, наращивать, нарастание.',
      'Корень -рос-: перед одиночной С пишем О — вырос, росла, водоросли, поросль, подросток.',
      'Исключения из правила -раст-/-рос-: росток, ростовщик, Ростов, Ростислав, отрасль. В них пишется О, хотя рядом СТ.',
      'Запоминалка для исключений: «Ростовщик из Ростова по имени Ростислав вырастил росток в отрасли».',
      'От слова «росток» образованы «подросток» и «ростковый» — в них тоже О.',
      'Порядок разбора: найти корень, посмотреть на согласную после него, выбрать букву, проверить исключения.',
      'Эти корни нельзя проверять ударением: ударение падает на разные гласные, а правило говорит про согласную. Это чередование, а не проверяемая гласная.',
      'Сравнительная таблица: Г → А, Ж → О, СТ и Щ → А, одиночная С → О.',
      'Частые ошибки: «предлогать» (нужно А), «рощение» (нужно А), «рости» (нужно А, но «росток» — исключение).',
      'Перед разбором определи, какая это орфограмма: проверяемая гласная или чередование. Для проверяемой ищи ударение, для чередования смотри на согласную или суффикс.',
      'В уроке есть интерактив: показать правило, выбрать букву в парах -лаг-/-лож- и -раст-/-рос-, проверить пять исключений, разобрать слово по клику.',
      'Ключевой вопрос к любому слову: какая согласная стоит сразу после корня?',
      'Три опоры: Г — А, Ж — О; СТ и Щ — А, С — О; пять исключений с О.',
      'Тренажёр: десять слов по кругу. Вставь букву А или О и проверь себя по объяснению.'
    ],
    tasks:[
      { q:'«предл_гать» — какая буква?', kind:'choice',
        choices:['а','о'], ans:0,
        hints:['Какая согласная после корня?','Это Г.'],
        sol:'А: предлагать.' },
      { q:'«предл_жить» — какая буква?', kind:'choice',
        choices:['о','а'], ans:0,
        hints:['Какая согласная после корня?','Это Ж.'],
        sol:'О: предложить.' },
      { q:'«р_стение» — какая буква?', kind:'choice',
        choices:['а','о'], ans:0,
        hints:['Что стоит после корня?','СТ.'],
        sol:'А: растение.' },
      { q:'«выр_с» — какая буква?', kind:'choice',
        choices:['о','а'], ans:0,
        hints:['Что стоит после корня?','Одиночная С.'],
        sol:'О: вырос.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){ const м=arr.findIndex(L=>L && L.id===ID); if(м>=0) arr[м]=L1045; else arr.push(L1045); }
  }catch(e){}

  const С = ()=>{
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const перерисовать = ()=>{ if(typeof chRender==='function') chRender(0); };

  window.ru1045Правило=function(){ const s=С(); s.о1=1; перерисовать(); };
  window.ru1045Лаг=function(i){ const s=С(); s.о3=i; перерисовать(); };
  window.ru1045Рост=function(i){ const s=С(); s.о6=i; перерисовать(); };
  window.ru1045Искл=function(i){ const s=С(); s.о9=i; перерисовать(); };
  window.ru1045Разбор=function(i){ const s=С(); s.о13=i; перерисовать(); };
  window.ru1045Game=function(ответ){
    const st=С();
    if(st.gRes!=null){ st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null; }
    else{ const it=GAME[(st.gIdx||0)%GAME.length]; st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1; }
    перерисовать();
  };
})();
