/* ============ РУССКИЙ ЯЗЫК · УРОК 1044 · «ПРЯМАЯ РЕЧЬ. ДИАЛОГ» ============
   5 класс, «Синтаксис и пунктуация». Продолжение линии 877–1043.

   ФОРМАТ — КАК У РУССКИХ УРОКОВ: window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT: .kicker, h2, .lead, .cap, .steps/.st,
   .ex-line, .cases, .rail, .pair/.card/.tag/.row/.split, .verdict, .score.

   ИНТЕРАКТИВ В КАДРАХ (состояние в CHS, перерисовка через chRender):
     кадр 1  «найти прямую речь» — показывает слова автора и чужую речь;
     кадр 4  выбор схемы: слова автора впереди, чужая речь впереди;
     кадр 7  выбор знака в конце реплики: точка, вопросительный, восклицательный;
     кадр 10 переключатель: прямая речь или диалог;
     кадр 13 разбор по клику: где слова автора, где чужая речь и какие знаки.
   Плюс тренажёр из десяти заданий.

   ПРАВИЛА (школьный курс, 5 класс):
     прямая речь — точная передача чужого высказывания; она состоит из слов
       автора и самой речи и заключается в кавычки;
     схемы: А: «П.»   А: «П!»   А: «П?»   «П», — а.   «П!» — а.   «П?» — а.;
     если слова автора стоят после прямой речи, после кавычек ставится тире,
       а слова автора пишутся с маленькой буквы;
     диалог — разговор двух или нескольких лиц; каждая реплика пишется с новой
       строки и начинается с ТИРЕ; кавычки в диалоге не ставятся;
     реплики диалога могут сопровождаться словами автора: — спросил он.
   ЛОВУШКИ: кавычки в диалоге не нужны; после «?» и «!» в прямой речи точка
     не ставится; слова автора после прямой речи пишутся с маленькой буквы. */
(function(){
  'use strict';

  const ID = 1044;
  const R = window.RUKIT;
  if(!R){ return; }
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;
  const пр    = (t)=>`<p class="ex-line">${esc(t)}</p>`;
  const рельс = (стиль,...xs)=>`<div class="rail" style="${стиль||''}">${xs.join('')}</div>`;
  const двa    = (a,b)=>`<div class="cases">${a}${b}</div>`;
  const шаги  = (items)=>`<div class="steps">${items.map(([t,s],i)=>
    `<div class="st"><span class="n">${i+1}</span><span class="t">${esc(t)}<small>${esc(s)}</small></span></div>`).join('')}</div>`;
  const кноп  = (t,on)=>`<button type="button" class="chip" style="flex:1 1 40%;font-size:17px" onclick="${on}">${esc(t)}</button>`;
  const кнопки= (...xs)=>`<div class="row">${xs.join('')}</div>`;

  /* ── данные интерактивных кадров ── */
  const СХЕМЫ=[
    ['Мальчик сказал: «Я вернусь завтра».','слова автора впереди','Схема: А: «П». После слов автора — двоеточие, речь в кавычках.'],
    ['«Я вернусь завтра», — сказал мальчик.','чужая речь впереди','Схема: «П», — а. После кавычек тире, слова автора с маленькой буквы.']
  ];
  const ЗНАКИ=[
    ['Мальчик сказал: «Я вернусь завтра».','точка','Спокойное сообщение — в конце точка внутри кавычек.'],
    ['Мальчик спросил: «Ты придёшь?»','вопросительный знак','Вопрос — знак вопроса внутри кавычек, точка не нужна.'],
    ['Мальчик крикнул: «Берегись!»','восклицательный знак','Восклицание — знак внутри кавычек.']
  ];
  const СРАВНЕНИЕ=[
    ['Мальчик сказал: «Я вернусь».','прямая речь','Слова автора, двоеточие, чужая речь в кавычках.'],
    ['— Я вернусь, — сказал мальчик.','диалог','Реплика с новой строки, тире, кавычек нет.'],
    ['— Ты придёшь? — спросила девочка.','диалог','Реплика с тире и словами автора после неё.'],
    ['«Ты придёшь?» — спросила девочка.','прямая речь','Кавычки и тире — это прямая речь, а не диалог.']
  ];
  const РАЗБОР=[
    ['Мальчик сказал: «Я вернусь завтра».','слова автора: мальчик сказал; чужая речь: я вернусь завтра','Двоеточие после слов автора, речь в кавычках, точка внутри кавычек.'],
    ['«Я вернусь завтра», — сказал мальчик.','чужая речь: я вернусь завтра; слова автора: сказал мальчик','После кавычек тире, слова автора с маленькой буквы.'],
    ['— Ты придёшь? — спросила девочка.','реплика: ты придёшь; слова автора: спросила девочка','Диалог: тире в начале строки, кавычек нет, знак вопроса перед тире.'],
    ['Девочка спросила: «Ты придёшь?»','слова автора: девочка спросила; чужая речь: ты придёшь','Вопросительный знак внутри кавычек, точка не ставится.']
  ];

  /* ── тренажёр ── */
  const GAME=[
    ['Какой знак после слов автора перед прямой речью?','двоеточие','Схема А: «П». — после слов автора двоеточие.','тире'],
    ['Что ставится после прямой речи, если дальше слова автора?','тире','Схема «П», — а.: после кавычек тире.','двоеточие'],
    ['Нужны ли кавычки в диалоге?','нет','В диалоге каждая реплика начинается с тире, кавычки не ставятся.','да'],
    ['Чем начинается каждая реплика диалога?','тире','Реплика пишется с новой строки и начинается с тире.','кавычками'],
    ['«Мальчик сказал: «Я вернусь»». Что такое «мальчик сказал»?','слова автора','Это слова автора, они стоят перед прямой речью.','прямая речь'],
    ['С какой буквы пишутся слова автора после прямой речи?','с маленькой','«Я вернусь», — сказал мальчик.','с большой'],
    ['Ставится ли точка после вопросительного знака в прямой речи?','нет','«Ты придёшь?» — точка не нужна.','да'],
    ['Что такое диалог?','разговор двух или нескольких лиц','Реплики пишутся с новой строки.','речь одного человека'],
    ['«— Привет! — сказал он». Что это?','диалог','Реплика с тире и словами автора.','прямая речь'],
    ['Где стоят кавычки при прямой речи?','вокруг чужой речи','Кавычки выделяют саму речь, а не слова автора.','вокруг слов автора']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    const кн = варианты.map(о=>кноп(о,`ru1044Game('${esc(о)}')`)).join('');
    const ответ = готово ? (верно ? R.NOTE('ok','верно',it[2]) : R.NOTE('no','исправить',it[2]))
                         : `<p class="verdict">Сначала найди слова автора, потом выбери знак.</p>`;
    return `${kick('16 · Тренажёр')}
      ${word(it[0],'выбери ответ')}
      ${ответ}
      ${кнопки(кн)}
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее задание.':'Сначала автор или речь, потом знак.')}`;
  }

  const подсказка_1 = cap('Кавычки выделяют чужую речь, а слова автора стоят без кавычек.');
  const подсказка_10 = cap('В диалоге кавычек нет: работает тире в начале реплики.');

  window.RULESSON({id:ID, game:GAME, frames:[
    (st)=>{
      const ок = st.о1!=null;
      const отв = ок ? `<p class="verdict ok">Слова автора: «Мальчик сказал». Чужая речь: «Я вернусь завтра» — она в кавычках.</p>`
                     : `<p class="verdict">Нажми кнопку — покажу, где слова автора, а где чужая речь.</p>`;
      return `${kick('01 · Разминка')}
        ${h2('Чужая речь в предложении')}
        ${lead('Мы уже умеем выделять обращение и вводные слова. Сегодня научимся передавать чужие слова: у прямой речи свои знаки, а у диалога — свои.')}
        ${пр('Мальчик сказал: «Я вернусь завтра».')}
        ${отв}
        ${кнопки(кноп('найти прямую речь','ru1044Найти()'))}
        ${pair(card('слова автора','мальчик сказал'),card('прямая речь','«Я вернусь завтра»'))}
        ${подсказка_1}`;
    },

    ()=>`${kick('02 · Что такое прямая речь')}
      ${h2('Точная передача чужих слов')}
      ${lead('Прямая речь — это чужие слова, переданные точно, без изменений. Она состоит из слов автора и самой речи и заключается в кавычки.')}
      ${шаги([['1 слова автора','кто говорит: мальчик сказал'],['2 знак','двоеточие после слов автора'],['3 прямая речь','в кавычках, с большой буквы'],['4 знак в конце','точка внутри кавычек']])}
      ${пр('Мальчик сказал: «Я вернусь завтра».')}
      ${cap('Кавычки — главный признак прямой речи.')}`,

    ()=>`${kick('03 · Схемы прямой речи')}
      ${h2('Четыре основные схемы')}
      ${lead('Схема помогает не ошибиться со знаками: буквой А обозначают слова автора, буквой П — прямую речь.')}
      ${cards([['А: «П».','слова автора впереди'],['А: «П!»','восклицание'],['А: «П?»','вопрос'],['«П», — а.','слова автора после речи']])}
      ${рельс('flex-wrap:wrap',chip('А: «П».'),chip('А: «П!»'),chip('А: «П?»'),chip('«П», — а.'))}
      ${cap('В схеме «П» — это сама речь, «а» — слова автора с маленькой буквы.')}`,

    (st)=>{
      const к = (st.о4||0);
      const п = СХЕМЫ[к];
      const кн = ['слова автора впереди','чужая речь впереди'].map((х,i)=>кноп(х,`ru1044Схема(${i})`)).join('');
      return `${kick('04 · Интерактив: выбери схему')}
        ${h2('Нажми — увидишь предложение и схему')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">${esc(п[1])}. ${esc(п[2])}</p>
        ${двa(card('А: «П».','двоеточие после слов автора'),card('«П», — а.','тире после кавычек'))}
        ${cap('Сравни: в первой схеме двоеточие, во второй — тире.')}`;
    },

    ()=>`${kick('05 · Слова автора впереди')}
      ${h2('Двоеточие и кавычки')}
      ${lead('Если слова автора стоят перед прямой речью, после них ставится двоеточие, а речь заключается в кавычки.')}
      ${cards([['Мальчик сказал: «Я вернусь».','спокойное сообщение'],['Мама попросила: «Помоги мне».','просьба'],['Учитель напомнил: «Завтра контрольная».','напоминание']])}
      ${пр('Знак в конце речи ставится ВНУТРИ кавычек.')}
      ${cap('После кавычек ничего не добавляется: «Я вернусь».')}`,

    ()=>`${kick('06 · Слова автора после речи')}
      ${h2('Тире вместо двоеточия')}
      ${lead('Если слова автора стоят после прямой речи, после кавычек ставится тире, а слова автора пишутся с маленькой буквы.')}
      ${cards([['«Я вернусь», — сказал мальчик.','запятая внутри кавычек'],['«Ты придёшь?» — спросила девочка.','знак вопроса, запятой нет'],['«Берегись!» — крикнул он.','восклицательный знак, запятой нет']])}
      ${пр('Схема: «П», — а. Тире обязательно, иначе речь и автор сливаются.')}
      ${cap('Запятая ставится только после точки-сообщения, а после «?» и «!» — нет.')}`,

    (st)=>{
      const к = (st.о7||0);
      const п = ЗНАКИ[к];
      const кн = ['точка','вопросительный знак','восклицательный знак'].map((х,i)=>кноп(х,`ru1044Знак(${i})`)).join('');
      return `${kick('07 · Интерактив: выбери знак')}
        ${h2('Что стоит в конце реплики')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">${esc(п[1])}: ${esc(п[2])}</p>
        ${cards([['сообщение','точка'],['вопрос','знак вопроса'],['восклицание','восклицательный знак']])}
        ${cap('После вопросительного и восклицательного знака точка не ставится.')}`;
    },

    ()=>`${kick('08 · Что такое диалог')}
      ${h2('Разговор двух или нескольких лиц')}
      ${lead('Диалог — это разговор. Каждая реплика пишется с новой строки и начинается с тире, а кавычки не ставятся.')}
      ${пр('— Привет! — сказал Ваня.')}
      ${пр('— Привет, — ответила Маша.')}
      ${рельс('flex-wrap:wrap',chip('новая строка'),chip('тире в начале'),chip('кавычек нет'))}
      ${cap('Диалог удобно читать: сразу видно, кто говорит.')}`,

    ()=>`${kick('09 · Реплики и слова автора')}
      ${h2('Кто говорит — пишем рядом')}
      ${lead('Слова автора могут стоять после реплики: тогда перед ними ставится тире, а сами слова пишутся с маленькой буквы.')}
      ${cards([['— Ты придёшь? — спросила девочка.','слова автора после реплики'],['— Да, конечно, — ответил мальчик.','ответ и автор'],['— Хорошо.','реплика без слов автора']])}
      ${пр('Если реплика с «?» или «!», запятая перед тире не нужна.')}
      ${cap('В диалоге знаки те же, что в прямой речи, только вместо кавычек — тире.')}`,

    (st)=>{
      const к = (st.о10||0);
      const п = СРАВНЕНИЕ[к];
      const кн = ['пример 1','пример 2','пример 3','пример 4'].map((х,i)=>кноп(х,`ru1044Сравни(${i})`)).join('');
      return `${kick('10 · Интерактив: прямая речь или диалог')}
        ${h2('Нажми пример — увидишь разбор')}
        ${lead('Главная ловушка: в диалоге не бывает кавычек, а в прямой речи они обязательны.')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">Это ${esc(п[1])}. ${esc(п[2])}</p>
        ${двa(card('прямая речь','кавычки + двоеточие или тире'),card('диалог','новая строка + тире, кавычек нет'))}
        ${подсказка_10}`;
    },

    ()=>`${kick('11 · Знаки внутри реплики')}
      ${h2('Запятая, вопрос, восклицание')}
      ${lead('Перед тире после реплики ставят запятую, если реплика — спокойное сообщение; после «?» и «!» запятая не нужна.')}
      ${двa(пр('— Я вернусь, — сказал он.'),пр('— Ты вернёшься? — спросил он.'))}
      ${cards([['сообщение','запятая перед тире'],['вопрос','знак вопроса, запятой нет'],['восклицание','восклицательный знак, запятой нет']])}
      ${cap('Так же и в прямой речи: «Я вернусь», — сказал он.')}`,

    ()=>`${kick('12 · Как оформить на письме')}
      ${h2('Порядок действий')}
      ${шаги([['1 найди слова автора','кто говорит'],['2 найди саму речь','что именно сказано'],['3 выбери схему','автор впереди — двоеточие, речь впереди — тире'],['4 поставь знаки','кавычки, знак в конце, тире и маленькая буква'],['5 проверь диалог','если разговор — тире в начале строки, кавычек нет']])}
      ${пр('Мальчик сказал: «Я вернусь завтра». → А: «П».')}
      ${cap('Пять шагов — и знаки расставлены верно.')}`,

    (st)=>{
      const к = (st.о13||0);
      const п = РАЗБОР[к];
      const кн = РАЗБОР.map((х,i)=>кноп('разбор '+(i+1),`ru1044Разбор(${i})`)).join('');
      return `${kick('13 · Интерактив: разбор по клику')}
        ${h2('Нажми — увидишь знаки и объяснение')}
        ${кнопки(кн)}
        ${пр(п[0])}
        ${cards([['что где',п[1]],['почему',п[2]]])}
        ${cap('Сравни разборы: диалог и прямая речь отличаются только кавычками и тире.')}`;
    },

    ()=>`${kick('14 · Ловушка')}
      ${h2('Три ошибки в оформлении')}
      ${lead('Первая: ставят кавычки в диалоге. Вторая: пишут точку после «?» и «!». Третья: пишут слова автора после речи с большой буквы.')}
      ${двa(пр('— «Привет!» — сказал он. ✗ кавычки в диалоге не нужны'),пр('«Ты придёшь?». — спросила она. ✗ точка после вопроса лишняя'))}
      ${cards([['после речи','«Я вернусь», — сказал мальчик (с маленькой)'],['после «?» и «!»','запятая не ставится'],['в диалоге','тире вместо кавычек']])}
      ${cap('Проверь эти три места — и знаки будут верными.')}`,

    ()=>`${kick('15 · В карман')}
      ${h2('Три фразы, которые стоит унести')}
      ${cards([['прямая речь','кавычки и двоеточие: Мальчик сказал: «Я вернусь».'],['слова автора после речи','тире и маленькая буква: «Я вернусь», — сказал мальчик.'],['диалог','новая строка и тире, кавычек нет']])}
      ${рельс('flex-wrap:wrap',chip('А: «П».'),chip('«П», — а.'),chip('диалог: — реплика'))}
      ${cap('Кавычки — только для прямой речи. В диалоге работает тире.')}`,

    (st)=>тренер(st)
  ]});

  /* ── карточка каталога ── */
  const L1044={
    id:ID, title:'Прямая речь. Диалог', ico:'💬',
    src:'Русский язык · 5 класс · Синтаксис', subj:'rus', group:null,
    explain:[
      'Прямая речь — точная передача чужих слов. Она состоит из слов автора и самой речи, а речь заключается в кавычки: «Мальчик сказал: «Я вернусь завтра»».',
      'Порядок разбора прямой речи: слова автора (кто говорит), знак после них, сама речь с большой буквы в кавычках, знак в конце внутри кавычек.',
      'Четыре основные схемы: А: «П». — сообщение, А: «П!» — восклицание, А: «П?» — вопрос, «П», — а. — слова автора после речи.',
      'Если слова автора стоят перед прямой речью, после них ставится двоеточие: «Мама попросила: «Помоги мне»».',
      'Если слова автора стоят после прямой речи, после кавычек ставится тире, а слова автора пишутся с маленькой буквы: ««Я вернусь», — сказал мальчик».',
      'Запятая перед тире ставится после спокойного сообщения, а после вопросительного и восклицательного знаков запятая не нужна: ««Ты придёшь?» — спросила девочка».',
      'Диалог — это разговор двух или нескольких лиц. Каждая реплика пишется с новой строки и начинается с тире, кавычки в диалоге не ставятся.',
      'Реплики диалога могут сопровождаться словами автора: «— Ты придёшь? — спросила девочка».',
      'В диалоге действуют те же знаки, что и в прямой речи, только вместо кавычек стоит тире.',
      'Знак в конце прямой речи ставится внутри кавычек: «Мальчик сказал: «Я вернусь».»',
      'Порядок действий на письме: найти слова автора, найти саму речь, выбрать схему, расставить знаки и проверить, не диалог ли это.',
      'Ловушка первая: кавычки в диалоге. Они там не нужны — реплика выделяется тире.',
      'Ловушка вторая: точка после вопросительного или восклицательного знака. Её не ставят.',
      'Ловушка третья: слова автора после прямой речи пишут с большой буквы. Правильно — с маленькой: ««Я вернусь», — сказал мальчик».',
      'В уроке есть интерактив: найти прямую речь, выбрать схему, выбрать знак в конце реплики, сравнить прямую речь с диалогом и разобрать предложение по клику.',
      'Тренажёр: десять заданий по кругу. Выбери знак, схему или реши, диалог это или прямая речь.'
    ],
    tasks:[
      { q:'Какой знак после слов автора перед прямой речью?', kind:'choice',
        choices:['двоеточие','тире'], ans:0,
        hints:['Схема А: «П».','Слова автора впереди.'],
        sol:'Двоеточие.' },
      { q:'Что ставится после прямой речи, если дальше слова автора?', kind:'choice',
        choices:['тире','двоеточие'], ans:0,
        hints:['Схема «П», — а.','После кавычек.'],
        sol:'Тире.' },
      { q:'Нужны ли кавычки в диалоге?', kind:'choice',
        choices:['нет','да'], ans:0,
        hints:['Чем начинается реплика?','В начале строки тире.'],
        sol:'Нет, кавычки не нужны.' },
      { q:'Ставится ли точка после вопросительного знака в прямой речи?', kind:'choice',
        choices:['нет','да'], ans:0,
        hints:['«Ты придёшь?»','Один знак в конце.'],
        sol:'Нет, не ставится.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){
      const м=arr.findIndex(L=>L && L.id===ID);
      if(м>=0) arr[м]=L1044; else arr.push(L1044);
    }
  }catch(e){}

  /* ── состояние и обработчики ── */
  const С = ()=>{
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const перерисовать = ()=>{ if(typeof chRender==='function') chRender(0); };

  window.ru1044Найти=function(){ const s=С(); s.о1=1; перерисовать(); };
  window.ru1044Схема=function(i){ const s=С(); s.о4=i; перерисовать(); };
  window.ru1044Знак=function(i){ const s=С(); s.о7=i; перерисовать(); };
  window.ru1044Сравни=function(i){ const s=С(); s.о10=i; перерисовать(); };
  window.ru1044Разбор=function(i){ const s=С(); s.о13=i; перерисовать(); };

  window.ru1044Game=function(ответ){
    const st=С();
    if(st.gRes!=null){
      st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null;
    }else{
      const it=GAME[(st.gIdx||0)%GAME.length];
      st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1;
    }
    перерисовать();
  };
})();
