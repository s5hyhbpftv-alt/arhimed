/* ============ РУССКИЙ ЯЗЫК · УРОК 891 · «СЛОЖНОЕ ПРЕДЛОЖЕНИЕ» ============
   5 класс, «Синтаксис». Продолжение линии 877–888.

   ФОРМАТ — КАК У РУССКИХ УРОКОВ: window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT: .kicker, h2, .lead, .cap, .steps/.st,
   .ex-line, .cases, .rail, .pair/.card/.tag/.row/.split .word.wide, .verdict, .score.

   ИНТЕРАКТИВ В САМИХ КАДРАХ (не только в тренажёре):
     кадр 3  кнопка «показать основы» — предложение подсвечивает основы по клику;
     кадр 4  переключатель примеров: простое или сложное — считаем основы;
     кадр 5  выбор союза (и, а, но) — предложение перестраивается на глазах;
     кадр 9  переключатель «и»: однородные члены или две основы;
     кадр 13 разбор по клику: нажми на пример — увидишь основы и знак.
   Состояние хранится в CHS[lidKey(891)] и перерисовывается через chRender(0) —
   так же, как в уроках 608–610 каркаса.

   ПРАВИЛА (школьный курс, 5 класс):
     сложное предложение — две и более грамматические основы;
     простое: Мальчик читает книгу.   сложное: Мальчик читает, а девочка рисует.;
     сложносочинённые (части равноправны) — союзы И, А, НО, ИЛИ;
     сложноподчинённые (главная и зависимая часть) — союзы ЧТО, ЧТОБЫ,
       ПОТОМУ ЧТО, КОГДА, ЕСЛИ, КОТОРЫЙ;
     бессоюзные — части соединяются только знаком (чаще запятой);
     между частями сложного предложения ставится ЗАПЯТАЯ.
   ЛОВУШКА: однородные члены не делают предложение сложным:
     «Мальчик читает и рисует» — простое (одна основа, два сказуемых);
     «Мальчик читает, и девочка рисует» — сложное (две основы). */
(function(){
  'use strict';

  const ID = 891;
  const R = window.RUKIT;
  if(!R){ return; }
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const row   = (...xs)=>`<div class="row">${xs.join('')}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;
  const пр    = (t)=>`<p class="ex-line">${esc(t)}</p>`;
  const рельс = (стиль,...xs)=>`<div class="rail" style="${стиль||''}">${xs.join('')}</div>`;
  const двa    = (a,b)=>`<div class="cases">${a}${b}</div>`;
  const шаги  = (items)=>`<div class="steps">${items.map(([t,s],i)=>
    `<div class="st"><span class="n">${i+1}</span><span class="t">${esc(t)}<small>${esc(s)}</small></span></div>`).join('')}</div>`;
  const кноп  = (t,on,cls)=>`<button type="button" class="chip" style="flex:1 1 40%;font-size:17px"
    onclick="${on}">${esc(t)}</button>`;
  const кнопки= (...xs)=>`<div class="row">${xs.join('')}</div>`;

  /* ── тренажёр: десять заданий ── */
  const GAME=[
    ['Мальчик читает, а девочка рисует. Какое предложение?','сложное','Две грамматические основы: мальчик читает и девочка рисует.','простое'],
    ['Мальчик читает и рисует. Какое предложение?','простое','Одна основа: мальчик читает и рисует — это однородные сказуемые.','сложное'],
    ['Сколько основ в предложении с двумя подлежащими и двумя сказуемыми?','две','Каждая пара «подлежащее + сказуемое» — своя основа.','одна'],
    ['Каким союзом соединить равноправные части?','и','Союзы И, А, НО, ИЛИ соединяют равноправные части.','потому что'],
    ['«Я не пришёл, потому что заболел». Какая часть зависимая?','заболел','Союз ПОТОМУ ЧТО присоединяет зависимую часть.','не пришёл'],
    ['Что ставится между частями сложного предложения?','запятая','Между частями сложного предложения ставится запятая.','ничего'],
    ['Солнце село, и стало темно. Сколько основ?','две','Солнце село — первая, стало темно — вторая.','одна'],
    ['Как называются части сложного предложения без союзов?','бессоюзные','Связь только по смыслу и знаку.','сложносочинённые'],
    ['Найди сложное: «Ветер стих.» или «Ветер стих, и волны улеглись.»','второе','Во втором две основы.','первое'],
    ['Главная и зависимая часть — какое это предложение?','сложноподчинённое','Есть главная часть и зависимая.','сложносочинённое']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    const кн = варианты.map(о=>кноп(о,`ru891Game('${esc(о)}')`)).join('');
    const ответ = готово ? (верно ? window.RUFEED.note('ok','верно',it[2]) : window.RUFEED.note('no','исправить',it[2]))
                         : `<p class="verdict">Сначала посчитай грамматические основы.</p>`;
    return `${kick('16 · Тренажёр')}
      ${word(it[0],'выбери ответ')}
      ${ответ}
      ${кнопки(кн)}
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее задание.':'Сначала основы, потом ответ.')}`;
  }

  /* ── данные интерактивных кадров ── */
  const ПРИМЕРЫ=[
    ['Мальчик читает книгу.','одна','простое'],
    ['Мальчик читает, а девочка рисует.','две','сложное'],
    ['Солнце село, и стало темно.','две','сложное'],
    ['Ветер стих.','одна','простое'],
    ['Я не пришёл, потому что заболел.','две','сложное'],
    ['Дети играли и смеялись.','одна','простое']
  ];
  const РАЗБОРЫ=[
    ['Мальчик читает, а девочка рисует.','мальчик читает · девочка рисует','запятая перед союзом А, сложносочинённое'],
    ['Я не пришёл, потому что заболел.','я не пришёл · заболел','запятая перед союзом ПОТОМУ ЧТО, сложноподчинённое'],
    ['Солнце село, и стало темно.','солнце село · стало темно','запятая перед союзом И, сложносочинённое'],
    ['Дети играли и смеялись.','дети играли и смеялись','основа одна — простое, однородные сказуемые']
  ];
  const С_СОЮЗАМИ=[['Мальчик читает','девочка рисует'],['Солнце село','стало темно'],['Ветер стих','волны улеглись']];
  const И_ЛОВУШКА=[['Дети играли и смеялись.','однородные сказуемые: основа одна — простое предложение'],
                   ['Дети играли, и взрослые смеялись.','две основы — сложное предложение']];

  window.RULESSON({id:ID, game:GAME, frames:[
    (st)=>{
      const п = (st.о3==null)?0:1;
      const строки = ПРИМЕРЫ[п];
      const бл = пр(строки[0]);
      const отв = (st.о3==null) ? `<p class="verdict">Нажми кнопку — я подсвечу основы и посчитаю их.</p>`
        : `<p class="verdict ok">Основ: ${esc(строки[1])}. Это ${esc(строки[2])} предложение.</p>`;
      const кн = кноп('показать основы','ru891Основы()')+кноп('другой пример','ru891Основы(1)');
      const кап_подсказка = (st.о3==null) ? `<p class="cap">Проверим: сколько основ в этом предложении?</p>`
        : `<p class="cap">Основа — это подлежащее и сказуемое вместе.</p>`;
      return `${kick('01 · Разминка')}
        ${h2('От словосочетания — к предложению')}
        ${lead('На прошлом уроке мы разбирали словосочетания: «осенний лист» — не предложение, здесь нет грамматической основы. Сегодня научимся считать основы: по ним и отличается простое предложение от сложного.')}
        ${pair(card('словосочетание','осенний лист — нет основы'),card('предложение','Лист упал. — есть основа'))}
        ${пр(строки[0])}
        ${отв}
        ${кнопки(кн)}
        ${кап_подсказка}
        ${cap('Основа — это подлежащее и сказуемое. Сколько основ — таково и предложение.')}`;
    },

    ()=>`${kick('02 · Что такое основа')}
      ${h2('Подлежащее и сказуемое')}
      ${lead('Грамматическая основа — главные члены предложения: подлежащее и сказуемое. В предложении «Мальчик читает» основа одна: мальчик читает.')}
      ${шаги([['подлежащее','отвечает на вопрос кто? что?'],['сказуемое','отвечает на вопрос что делает?'],['вместе','это и есть грамматическая основа']])}
      ${пр('Мальчик читает. → мальчик читает (одна основа)')}
      ${cap('Однородные сказуемые основу не удваивают: «читает и рисует» — одна основа.')}`,

    (st)=>{
      const п = (st.о3==null)?0:1;
      const строки = ПРИМЕРЫ[п];
      const отв = (st.о3==null) ? `<p class="verdict">Нажми кнопку — я подсвечу основы и посчитаю их.</p>`
        : `<p class="verdict ok">Основ: ${esc(строки[1])}. Это ${esc(строки[2])} предложение.</p>`;
      return `${kick('03 · Интерактив: считаем основы')}
        ${h2('Нажми — и увидишь основы')}
        ${lead('Сложное предложение — то, в котором две и более грамматические основы. Проверим на примере.')}
        ${пр(строки[0])}
        ${отв}
        ${кнопки(кноп('показать основы','ru891Основы()')+кноп('другой пример','ru891Основы(1)'))}
        ${cards([['первая основа','мальчик читает'],['вторая основа','девочка рисует']])}
        ${cap('Части сложного предложения разделяются знаком — чаще всего запятой.')}`;
    },

    (st)=>{
      const к = (st.о4||0);
      const п = ПРИМЕРЫ[к];
      const кн = ПРИМЕРЫ.slice(0,3).map((х,i)=>кноп(х[0].slice(0,22)+'…',`ru891Пример(${i})`)).join('');
      return `${kick('04 · Интерактив: простое или сложное')}
        ${h2('Переключай примеры и считай основы')}
        ${lead('Нажимай на пример — и смотри, сколько в нём основ.')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">Основ: ${esc(п[1])} → это ${esc(п[2])} предложение.</p>
        ${cap('Одна основа — простое; две и больше — сложное.')}`;
    },

    (st)=>{
      const с = ['и','а','но'][st.о5||0];
      const п = С_СОЮЗАМИ[st.о5||0];
      const кн = ['и','а','но'].map((х,i)=>кноп(х,`ru891Союз(${i})`)).join('');
      return `${kick('05 · Интерактив: выбираем союз')}
        ${h2('Равноправные части соединяют союзы И, А, НО, ИЛИ')}
        ${lead('Нажми союз — предложение соберётся. Обрати внимание на запятую: она стоит перед союзом.')}
        ${кнопки(кн)}
        ${пр(`${п[0]}, ${с} ${п[1]}.`)}
        <p class="verdict ok">Части равноправны — это сложносочинённое предложение.</p>
        ${шаги([['запятая','ставится перед союзом'],['союзы','и, а, но, или'],['части','равноправны, ни одна не главная']])}
        ${cap('Такие части можно поменять местами — смысл почти не изменится.')}`;
    },

    ()=>`${kick('06 · Сложноподчинённые')}
      ${h2('Главная часть и зависимая')}
      ${lead('Если одна часть поясняет другую, перед нами сложноподчинённое предложение. Его присоединяют союзы что, чтобы, потому что, когда, если, который.')}
      ${рельс('flex-wrap:wrap',chip('что'),chip('чтобы'),chip('потому что'),chip('когда'),chip('если'),chip('который'))}
      ${пр('Я не пришёл, потому что заболел.')}
      ${pair(card('главная часть','я не пришёл'),card('зависимая часть','потому что заболел'))}
      ${cap('Зависимая часть отвечает на вопрос: почему? когда? при каком условии?')}`,

    ()=>`${kick('07 · Бессоюзные')}
      ${h2('Без союзов — только знак')}
      ${lead('Части можно соединить и без союза: тогда связь держится на смысле и интонации, а на письме — на знаке.')}
      ${пр('Солнце село, стало темно.')}
      ${cards([['запятая','части короткие и тесно связаны'],['точка с запятой','части длинные и самостоятельные'],['двоеточие и тире','в 5 классе встречаются реже']])}
      ${cap('Главное правило пятого класса: между частями сложного предложения ставится запятая.')}`,

    ()=>`${kick('08 · Знак')}
      ${h2('Запятая перед союзом')}
      ${lead('Между частями сложного предложения всегда ставится запятая — перед союзом или на месте его отсутствия.')}
      ${шаги([['1 найди основы','сколько их?'],['2 найди границу частей','где заканчивается первая мысль'],['3 поставь знак','запятая между частями']])}
      ${двa(пр('Мальчик читает, а девочка рисует.'),пр('Солнце село, и стало темно.'))}
      ${cap('Запятая не ставится внутри части — только на границе.')}`,

    (st)=>{
      const к = (st.о9||0);
      const п = И_ЛОВУШКА[к];
      const кн = ['однородные члены','две основы'].map((х,i)=>кноп(х,`ru891И(${i})`)).join('');
      return `${kick('09 · Ловушка: «и» бывает разным')}
        ${h2('Однородные члены или две основы?')}
        ${lead('Союз «и» встречается и в простом предложении с однородными членами, и в сложном. Различие только в числе основ.')}
        ${кнопки(кн)}
        ${пр(п[0])}
        <p class="verdict ok">${esc(п[1])}</p>
        ${двa(card('простое','Дети играли и смеялись — основа одна'),card('сложное','Дети играли, и взрослые смеялись — основы две'))}
        ${cap('Считай основы — и «и» перестанет путать.')}`;
    },

    ()=>`${kick('10 · Схемы')}
      ${h2('Как записывают строение предложения')}
      ${lead('Схема помогает увидеть части: квадратные скобки — части, знак между ними — граница.')}
      ${cards([['простое','[ — = ]'],['сложносочинённое','[ — = ], и [ — = ]'],['сложноподчинённое','[ — = ], (потому что — = )']])}
      ${пр('Мальчик читает, а девочка рисует. → [ — = ], а [ — = ]')}
      ${cap('Нарисуешь схему — увидишь, где ставить запятую.')}`,

    ()=>`${kick('11 · Разбор по шагам')}
      ${h2('Порядок действий')}
      ${шаги([['1 прочитай предложение','найди смысловые части'],['2 подчеркни основы','сколько пар «подлежащее + сказуемое»'],['3 определи границу','где заканчивается первая часть'],['4 поставь знак','запятая между частями'],['5 назови вид','сложносочинённое, сложноподчинённое или бессоюзное']])}
      ${cap('Пять шагов — и разбор готов.')}`,

    ()=>`${kick('12 · Как различить')}
      ${h2('Три вопроса к предложению')}
      ${cards([['сколько основ?','одна — простое, две и больше — сложное'],['есть ли союз?','есть — союзное, нет — бессоюзное'],['равны ли части?','равны — сложносочинённое, одна поясняет другую — сложноподчинённое']])}
      ${пр('Проверь на примере: «Солнце село, и стало темно» → основ две, союз есть, части равны.')}
      ${cap('Ответы на три вопроса дают полную характеристику.')}`,

    (st)=>{
      const к = (st.о13||0);
      const п = РАЗБОРЫ[к];
      const кн = РАЗБОРЫ.map((х,i)=>кноп('пример '+(i+1),`ru891Разбор(${i})`)).join('');
      return `${kick('13 · Интерактив: разбери по клику')}
        ${h2('Нажми на пример — увидишь разбор')}
        ${кнопки(кн)}
        ${пр(п[0])}
        ${cards([['основы',п[1]],['знак и вид',п[2]]])}
        ${cap('Сравни примеры: у четвёртого основа одна, поэтому предложение простое.')}`;
    },

    ()=>`${kick('14 · Сводка')}
      ${h2('Простое, сложносочинённое, сложноподчинённое, бессоюзное')}
      ${шаги([['простое','одна основа: Мальчик читает книгу.'],['сложносочинённое','равные части и союзы и, а, но: Мальчик читает, а девочка рисует.'],['сложноподчинённое','главная и зависимая: Я не пришёл, потому что заболел.'],['бессоюзное','только знак: Солнце село, стало темно.']])}
      ${cap('Во всех случаях между частями ставится запятая.')}`,

    ()=>`${kick('15 · В карман')}
      ${h2('Три фразы, которые стоит унести')}
      ${cards([['считай основы','сколько основ — таково и предложение'],['ищи границу','запятая ставится между частями'],['помни ловушку','«и» с однородными не делает предложение сложным']])}
      ${пр('Дети играли и смеялись. — простое.   Дети играли, и взрослые смеялись. — сложное.')}
      ${cap('Одно слово «и», а предложения разные: всё решает число основ.')}`,

    (st)=>тренер(st)
  ]});

  /* ── карточка каталога ── */
  const L891={
    id:ID, title:'Сложное предложение', ico:'🧱',
    src:'Русский язык · 5 класс · Синтаксис', subj:'rus', group:null,
    explain:[
      'Словосочетание основы не имеет, а предложение имеет: «осенний лист» — словосочетание, «Лист упал» — предложение. Грамматическая основа — это подлежащее и сказуемое вместе.',
      'Если в предложении одна основа, оно простое: «Мальчик читает книгу». Если основ две и больше, предложение сложное: «Мальчик читает, а девочка рисует».',
      'Сложное предложение состоит из частей, и каждая часть имеет свою основу. Части разделяются знаком: чаще всего запятой.',
      'Сложносочинённые предложения — те, где части равноправны и соединены союзами и, а, но, или. Запятая ставится перед союзом.',
      'Сложноподчинённые предложения — те, где одна часть главная, а другая зависимая. Их соединяют союзы что, чтобы, потому что, когда, если, который.',
      'Бессоюзные предложения соединяются без союза: только по смыслу и с помощью знака. «Солнце село, стало темно».',
      'Главное правило пятого класса: между частями сложного предложения ставится запятая — перед союзом или на месте его отсутствия.',
      'Порядок разбора: прочитать, подчеркнуть основы, найти границу частей, поставить знак, назвать вид предложения.',
      'Ловушка: союз «и» бывает и в простом предложении с однородными членами. «Дети играли и смеялись» — простое, потому что основа одна.',
      'Различие только в числе основ: «Дети играли и смеялись» — одна основа, «Дети играли, и взрослые смеялись» — две основы и запятая.',
      'Схемы помогают: простое [ — = ], сложносочинённое [ — = ], и [ — = ], сложноподчинённое [ — = ], (потому что — = ).',
      'Три вопроса к предложению: сколько основ? есть ли союз? равны ли части? Ответы дают полную характеристику.',
      'Три опоры: считай основы, ищи границу между частями, помни ловушку с союзом «и».',
      'Одно и то же слово «и» может стоять и в простом, и в сложном предложении: всё решает количество грамматических основ.',
      'В уроках каркаса есть интерактив: считай основы по кнопке, переключай примеры, выбирай союз, разбирай предложение по клику.',
      'Тренажёр: десять заданий по кругу. Определи, простое предложение или сложное, сосчитай основы и выбери знак.'
    ],
    tasks:[
      { q:'«Мальчик читает, а девочка рисует» — какое предложение?', kind:'choice',
        choices:['сложное','простое'], ans:0,
        hints:['Сколько основ?','Мальчик читает и девочка рисует.'],
        sol:'Сложное: две основы.' },
      { q:'«Мальчик читает и рисует» — какое предложение?', kind:'choice',
        choices:['простое','сложное'], ans:0,
        hints:['Сколько подлежащих?','Основа одна.'],
        sol:'Простое: однородные сказуемые.' },
      { q:'Что ставится между частями сложного предложения?', kind:'choice',
        choices:['запятая','ничего'], ans:0,
        hints:['Это граница частей.','Знак перед союзом.'],
        sol:'Запятая.' },
      { q:'«Я не пришёл, потому что заболел» — какое предложение?', kind:'choice',
        choices:['сложноподчинённое','сложносочинённое'], ans:0,
        hints:['Одна часть поясняет другую.','Союз ПОТОМУ ЧТО.'],
        sol:'Сложноподчинённое.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){
      const м=arr.findIndex(L=>L && L.id===ID);
      if(м>=0) arr[м]=L891; else arr.push(L891);
    }
  }catch(e){}

  /* ── состояние и обработчики ── */
  const С = ()=>{
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const перерисовать = ()=>{ if(typeof chRender==='function') chRender(0); };

  window.ru891Основы=function(дальше){
    const s=С();
    if(дальше){ s.о3 = (s.о3==null)?0:((s.о3+1)%2); } else { s.о3=0; }
    перерисовать();
  };
  window.ru891Пример=function(i){ const s=С(); s.о4=i; перерисовать(); };
  window.ru891Союз=function(i){ const s=С(); s.о5=i; перерисовать(); };
  window.ru891И=function(i){ const s=С(); s.о9=i; перерисовать(); };
  window.ru891Разбор=function(i){ const s=С(); s.о13=i; перерисовать(); };

  window.ru891Game=function(ответ){
    const st=С();
    if(st.gRes!=null){
      st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null;
    }else{
      const it=GAME[(st.gIdx||0)%GAME.length];
      st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1;
    }
    перерисовать();
  };
})();
