/* ========== МАТЕМАТИКА · УРОК 365 · «ДВИЖЕНИЕ НАВСТРЕЧУ» =====================
   4 класс, раздел «Одновременное движение».

   Сюжет выбран под мир 4 класса в «Пути» («Открытое море»): два судна выходят
   навстречу друг другу из двух портов. Раньше урок был четырьмя абзацами
   текста без единого рисунка; здесь у каждого шага своя сцена и свой вопрос.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     между портами 300 км; «Восток» 60 км/ч, «Запад» 40 км/ч
     скорость сближения 60 + 40 = 100 км/ч
     время встречи 300 : 100 = 3 ч
     пути до встречи 60·3 = 180 и 40·3 = 120; 180 + 120 = 300 ✓
     через 1 ч между ними 300 − 100 = 200 км; через 2 ч — 100 км
     обратная задача: 300 км за 3 ч → сближение 100; если один 60, второй 40
     удаление (в разные стороны): скорость тоже сумма, за 2 ч разрыв 200 км
   Практика: 240 и (50+30)=80 → 3 ч; 27 и (4+5)=9 → 3 ч; 400 и (70+30)=100 → 4 ч;
     через 2 ч из 240 при 80 остаётся 240 − 160 = 80 км;
     обратная: 180 км за 2 ч → сближение 90; один 50 → второй 40.

   МЕХАНИЗМЫ КАДРОВ РАЗНЫЕ (шаблон не повторяется):
     1  КАРТА: два порта, между ними мерная линия, она прочерчивается;
     2  ОДНОВРЕМЕННО: оба судна трогаются в один миг, над ними общие часы;
     3  ЧАС ПЕРВЫЙ: два куска пути съедают разрыв с двух сторон;
     4  ПОЛОСА СЖИМАЕТСЯ: промежуток сходится сразу с обеих сторон;
     5  ТРИ ЧАСА: полоса тает ступенями 300 → 200 → 100 → 0;
     6  ТОЧКА ВСТРЕЧИ: вспыхивает ближе к медленному судну;
     7  ПРОВЕРКА СУММОЙ: два отрезка 180 и 120 складываются в 300;
     8  ЧТО ОСТАЛОСЬ: из 300 вычитается пройденное за 2 часа;
     9  ОБРАТНЫЙ ХОД: из расстояния и времени добывается вторая скорость;
     10 УДАЛЕНИЕ: суда расходятся, и разрыв растёт с той же суммой;
     11 ЛОВУШКА: делить на одну скорость нельзя — сцена сравнения;
     12 ПРАКТИКА: пять уровней подряд;
     13–15 ТРЕНАЖЁРЫ: сближение, время встречи, ловушки.

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md: SMIL с кривой проекта
   0.23 1 0.32 1, одно главное движение на кадр плюс вспышка результата,
   состояние всегда видимо (не ниже 0.3), текст не масштабируется,
   prefers-reduced-motion выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 365;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a', SAND='#e8c48a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  const CSS=`
  #lvis .s6.l365{gap:14px}
  #lvis .s6.l365 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l365 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l365 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l365 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l365 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l365 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l365 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l365 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l365 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l365 .правило b{color:${GOLD}}
  #lvis .s6.l365 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l365 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l365 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l365 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l365 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l365 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l365 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l365 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l365 .уровни .точка.сейчас{background:${GOLD};animation:l365dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l365dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l365 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l365{-webkit-text-size-adjust:100%}
  #lvis .s6.l365 [data-anim]{animation:l365rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l365rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l365 [data-anim]{animation:none!important}
    #lvis .s6.l365 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l365 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l365-style');
      if(!s){ s=document.createElement('style'); s.id='l365-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  /* подпись проступает к нужному моменту цикла и дальше видна */
  /* Минимум 0.55, а не 0.3: на снимке в середине цикла кадр выглядел пустым —
     это ошибка 7 из deploy/АНИМАЦИЯ_УРОКОВ.md. Подпись проступает, но читается всегда. */
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  /* вспышка результата: кольцо расходится и гаснет */
  const кольцо = (x,y,r,цвет,длит,доля) => ДВИЖ ? `<circle cx="${x}" cy="${y}" r="${r}" fill="none"
      stroke="${цвет}" stroke-width="2">
      ${анК('r',r+';'+r+';'+(r+10)+';'+r,длит,'0;'+кт(доля)+';'+кт(доля+0.1)+';1')}
      ${анК('opacity','0;0;0.9;0',длит,'0;'+кт(доля)+';'+кт(доля+0.06)+';'+кт(доля+0.16))}
    </circle>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c365-небо" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2a3f33"/><stop offset="0.5" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#111c15"/>
      </linearGradient>
      <linearGradient id="c365-вода" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#7fd1ff"/><stop offset="0.4" stop-color="#3f8fc0"/>
        <stop offset="1" stop-color="#1b4a6b"/>
      </linearGradient>
      <linearGradient id="c365-парус" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.6" stop-color="#ecdfc0"/>
        <stop offset="1" stop-color="#cbb98e"/>
      </linearGradient>
      <linearGradient id="c365-корпус" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c79a5a"/><stop offset="0.55" stop-color="#8a5f30"/>
        <stop offset="1" stop-color="#53381c"/>
      </linearGradient>
      <linearGradient id="c365-полоса" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#ffd76a"/><stop offset="1" stop-color="#c8912f"/>
      </linearGradient>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c365-небо)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+16, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.86)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* МОРЕ: полоса воды под маршрутом */
  const море = (y,в) => `<g>
    <rect x="0" y="${y}" width="336" height="${в}" fill="url(#c365-вода)" opacity=".55"/>
    <rect x="0" y="${y}" width="336" height="2" fill="rgba(255,255,255,.3)"/>
  </g>`;

  /* СУДНО. Рисуется по тем же правилам, что сцены «Пути»: седловатый борт,
     задранный штевень со стороны хода, прямой парус на рее.
     направление: 1 — идёт вправо, −1 — влево. */
  const судно = (x,y,напр,цвет,м) => {
    const к = м||1, з = напр<0 ? -1 : 1;
    return `<g transform="translate(${x} ${y}) scale(${(з*к).toFixed(3)} ${к})">
      <path d="M-24 0c1 7 7 12 20 12s28-4 32-13" fill="url(#c365-корпус)" stroke="${ОБВОД}" stroke-width="1.3"/>
      <path d="M-24 0c14 4 38 4 52-1" fill="none" stroke="${ОБВОД}" stroke-width="1.1"/>
      <line x1="2" y1="-2" x2="2" y2="-22" stroke="${ОБВОД}" stroke-width="1.4"/>
      <path d="M-10 -20 h24 c3 7 3 12 1 17 h-26 c-2-5 -2-10 1-17 z"
        fill="url(#c365-парус)" stroke="${ОБВОД}" stroke-width="1.1"/>
      <path d="M-12 -20 h28" stroke="${ОБВОД}" stroke-width="1.3"/>
      <circle cx="14" cy="4" r="2" fill="${цвет}"/>
    </g>`;
  };

  /* ПОРТ: причал со столбиком и флагом.
     Подпись у крайних портов прижимается к своему краю кадра: центрированная
     надпись «А · 60 км/ч» наx = 30 срезалась левой кромкой (видно на снимке). */
  const порт = (x,y,имя,цвет) => {
    const якорь = x < 70 ? 'start' : (x > 266 ? 'end' : 'middle');
    const xп = якорь==='start' ? 8 : (якорь==='end' ? 328 : x);
    return `<g>
    <rect x="${x-14}" y="${y}" width="28" height="7" rx="2" fill="#6b5a3a" stroke="${ОБВОД}" stroke-width="1.1"/>
    <line x1="${x}" y1="${y}" x2="${x}" y2="${y-24}" stroke="${ОБВОД}" stroke-width="1.6"/>
    <path d="M${x} ${y-24} l14 5 l-14 5 z" fill="${цвет}" stroke="${ОБВОД}" stroke-width="1"/>
    ${т(xп,y+22,имя,14,МУТ,false,якорь)}
  </g>`;};

  /* ШКАЛА пути: линия с делениями и подписью расстояния */
  const шкала = (x1,x2,y,текст,цвет) => {
    const д = [0,0.25,0.5,0.75,1].map(k=>x1+(x2-x1)*k);
    return `<g>
      <line x1="${x1}" y1="${y}" x2="${x2}" y2="${y}" stroke="${цвет||МУТ}" stroke-width="1.4"/>
      ${д.map(x=>`<line x1="${x}" y1="${y-5}" x2="${x}" y2="${y+5}" stroke="${цвет||МУТ}" stroke-width="1.2"/>`).join('')}
      ${текст?подпись((x1+x2)/2,y-10,текст,цвет||GOLD,14):''}
    </g>`;
  };

  /* ЧАСЫ: циферблат со стрелкой, которая обходит круг за один «час» цикла */
  const часы = (x,y,r,длит,шагов) => `<g transform="translate(${x} ${y})">
    <circle cx="0" cy="0" r="${r}" fill="rgba(10,22,17,.85)" stroke="${GOLD}" stroke-width="1.6"/>
    ${[0,1,2,3].map(k=>{const a=k*Math.PI/2;
      return `<line x1="${(Math.sin(a)*(r-4)).toFixed(1)}" y1="${(-Math.cos(a)*(r-4)).toFixed(1)}"
        x2="${(Math.sin(a)*r).toFixed(1)}" y2="${(-Math.cos(a)*r).toFixed(1)}" stroke="${МУТ}" stroke-width="1.2"/>`;}).join('')}
    <line x1="0" y1="0" x2="0" y2="${-(r-5)}" stroke="${GOLD}" stroke-width="2">
      ${ДВИЖ?`<animateTransform attributeName="transform" type="rotate"
        values="${Array.from({length:(шагов||3)+1},(_,k)=>(k*360)).join(';')}"
        keyTimes="${Array.from({length:(шагов||3)+1},(_,k)=>(k/(шагов||3)).toFixed(3)).join(';')}"
        dur="${длит||'9s'}" repeatCount="indefinite" calcMode="linear"/>`:''}
    </line>
  </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Карта: два порта и 300 км между ними — линия прочерчивается */
  function F1(s){
    const в = s.карта;
    return ВОПРОС('Два судна стоят в портах. Между портами 300 км. Что нужно знать, чтобы понять, когда они встретятся?') +
      `<div class="pic">${свг(`
        ${фон(236)}
        ${т(168,26,'Два порта на одном море',14,GOLD,true)}
        ${море(150,70)}
        ${порт(34,150,'Порт А',BLUE)}
        ${порт(302,150,'Порт Б',SAND)}
        ${судно(116,176,1,BLUE,0.75)}
        ${судно(220,176,-1,SAND,0.75)}
        <g>
          <line x1="40" y1="112" x2="296" y2="112" stroke="${GOLD}" stroke-width="2" stroke-dasharray="256"
            stroke-dashoffset="256">
            ${анК('stroke-dashoffset','256;256;0;0','9s','0;0.08;0.5;1')}
          </line>
          <line x1="40" y1="104" x2="40" y2="120" stroke="${GOLD}" stroke-width="2"/>
          <line x1="296" y1="104" x2="296" y2="120" stroke="${GOLD}" stroke-width="2"/>
        </g>
        <g>${проявить('9s',0.56,0.66)}${подпись(168,96,'300 км',GOLD,16)}</g>
        <g>${проявить('9s',0.74,0.84)}${т(168,224,'у каждого судна своя скорость',14,ИНК)}</g>
      `,236)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='обе'?'hit':(в?'miss':''), 'Расстояние и обе скорости', "r365Карта('обе')")}
        ${BTN(4, в==='одна'?'miss':'', 'Расстояние и скорость одного судна', "r365Карта('одна')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='обе',
        в==='обе' ? 'Верно: навстречу идут двое, и разрыв сокращают оба сразу. Одной скорости мало.'
                  : 'Нет: пока мы знаем скорость только одного, второе судно тоже едет — и встреча случится раньше, чем мы посчитаем.') : ЖДЁТ()) +
      ПРАВИЛО('Навстречу — значит <b>расстояние между ними уменьшают оба сразу</b>.');
  }

  /* 2. Одновременно: оба трогаются в один миг */
  function F2(s){
    const в = s.старт;
    return ВОПРОС('Суда вышли ОДНОВРЕМЕННО. Кто из них раньше начал путь?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        ${море(140,74)}
        ${порт(34,140,'А',BLUE)}
        ${порт(302,140,'Б',SAND)}
        <g>${анТК('0 0;0 0;60 0;60 0','8s','0;0.14;0.7;1')}
          ${судно(66,160,1,BLUE,0.72)}</g>
        <g>${анТК('0 0;0 0;-60 0;-60 0','8s','0;0.14;0.7;1')}
          ${судно(270,160,-1,SAND,0.72)}</g>
        ${часы(168,66,26,'8s',1)}
        <g>${проявить('8s',0.2,0.3)}${подпись(168,116,'оба тронулись в один миг',GOLD,14)}</g>
        <g>${проявить('8s',0.76,0.86)}${т(168,228,'часы для них одни и те же',14,ИНК)}</g>
      `,238)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='вместе'?'hit':(в?'miss':''), 'Никто: они вышли вместе', "r365Старт('вместе')")}
        ${BTN(4, в==='быстрый'?'miss':'', 'Тот, кто быстрее', "r365Старт('быстрый')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='вместе',
        в==='вместе' ? 'Верно: слово «одновременно» и значит, что часы у них общие. Поэтому время в пути у обоих одинаковое.'
                     : 'Нет: быстрее — не значит раньше. Вышли они в один миг, просто один проходит за час больше.') : ЖДЁТ()) +
      ПРАВИЛО('Одновременно — <b>время в пути у обоих одинаковое</b>.');
  }

  /* 3. Первый час: два куска пути съедают разрыв с двух сторон */
  function F3(s){
    const в = s.сближение;
    return ВОПРОС('«Восток» идёт 60 км/ч, «Запад» — 40 км/ч. На сколько километров они сблизятся за один час?') +
      `<div class="pic">${свг(`
        ${фон(244)}
        ${т(168,24,'Один час пути',14,GOLD,true)}
        ${шкала(30,306,60,'300 км',GOLD)}
        <g>${проявить('9s',0.16,0.26)}
          <rect x="30" y="86" width="55" height="18" rx="4" fill="${BLUE}" opacity=".75"/>
          ${т(57,100,'60',14,'#0d1f2b',true)}</g>
        <g>${проявить('9s',0.3,0.4)}
          <rect x="269" y="86" width="37" height="18" rx="4" fill="${SAND}" opacity=".8"/>
          ${т(287,100,'40',14,'#2b1f0d',true)}</g>
        <g>${проявить('9s',0.46,0.56)}
          <rect x="85" y="86" width="184" height="18" rx="4" fill="none" stroke="${МУТ}" stroke-width="1.4" stroke-dasharray="5 4"/>
          ${т(177,100,'осталось 200',14,МУТ)}</g>
        ${море(126,66)}
        <g>${анТК('0 0;0 0;46 0;46 0','9s','0;0.1;0.62;1')}${судно(62,146,1,BLUE,0.72)}</g>
        <g>${анТК('0 0;0 0;-32 0;-32 0','9s','0;0.1;0.62;1')}${судно(278,146,-1,SAND,0.72)}</g>
        <g>${проявить('9s',0.68,0.78)}${подпись(168,214,'60 + 40 = 100 км за час',GREEN,16)}</g>
        <g>${проявить('9s',0.84,0.92)}${т(168,236,'это и есть скорость сближения',14,ИНК)}</g>
      `,244)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сто'?'hit':(в?'miss':''), 'На 100 км: 60 + 40', "r365Сближение('сто')")}
        ${BTN(4, в==='двадцать'?'miss':'', 'На 20 км: 60 − 40', "r365Сближение('двадцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сто',
        в==='сто' ? 'Верно: за час первое прошло 60 км, второе — 40 км, и обе эти части ушли из общего разрыва. 60 + 40 = 100.'
                  : 'Нет: вычитают, когда один догоняет другого. Здесь они идут навстречу, поэтому оба куска складываются: 60 + 40 = 100.') : ЖДЁТ()) +
      ПРАВИЛО('Скорость сближения — это <b>сумма скоростей</b>: 60 + 40 = 100 км/ч.');
  }

  /* 4. Полоса сжимается: разрыв уходит сразу с двух сторон */
  function F4(s){
    const в = s.полоса;
    return ВОПРОС('Почему скорости именно складывают, а не берут большую из них?') +
      `<div class="pic">${свг(`
        ${фон(226)}
        ${т(168,24,'Разрыв уходит с двух сторон',14,GOLD,true)}
        <g>
          <rect x="30" y="52" width="276" height="26" rx="6" fill="url(#c365-полоса)" opacity=".85">
            ${анК('width','276;276;92;92','9s','0;0.12;0.66;1')}
            ${анК('x','30;30;122;122','9s','0;0.12;0.66;1')}
          </rect>
          <rect x="30" y="52" width="276" height="26" rx="6" fill="none" stroke="${МУТ}" stroke-width="1.2"/>
        </g>
        <g>${анТК('0 0;0 0;92 0;92 0','9s','0;0.12;0.66;1')}
          <path d="M34 92 l16 0 l-6 -6 M34 92 l16 0 l-6 6" fill="none" stroke="${BLUE}" stroke-width="2"/></g>
        <g>${анТК('0 0;0 0;-92 0;-92 0','9s','0;0.12;0.66;1')}
          <path d="M302 92 l-16 0 l6 -6 M302 92 l-16 0 l6 6" fill="none" stroke="${SAND}" stroke-width="2"/></g>
        <g>${проявить('9s',0.2,0.3)}${т(84,120,'слева ест «Восток»',14,BLUE)}</g>
        <g>${проявить('9s',0.34,0.44)}${т(252,120,'справа — «Запад»',14,SAND)}</g>
        <g>${проявить('9s',0.6,0.7)}${подпись(168,158,'за час ушло 60 + 40',GREEN,16)}</g>
        <g>${проявить('9s',0.78,0.88)}
          ${т(168,192,'если бы шёл один,',14,ИНК)}
          ${т(168,212,'разрыв таял бы вдвое медленнее',14,МУТ)}</g>
      `,226)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='двое'?'hit':(в?'miss':''), 'Потому что разрыв сокращают двое', "r365Полоса('двое')")}
        ${BTN(4, в==='быстрый'?'miss':'', 'Потому что главное — кто быстрее', "r365Полоса('быстрый')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='двое',
        в==='двое' ? 'Верно: полоса расстояния укорачивается с обоих концов сразу, поэтому за час исчезает и 60 км, и 40 км.'
                   : 'Нет: быстрый пройдёт больше пути, но разрыв всё равно съедают оба. Поэтому берут сумму, а не большее число.') : ЖДЁТ()) +
      ПРАВИЛО('Оба работают на встречу, поэтому <b>складываем</b>, а не выбираем большее.');
  }

  /* 5. Три часа: полоса тает ступенями 300 → 200 → 100 → 0 */
  function F5(s){
    const в = s.время;
    return ВОПРОС('Между судами 300 км, сближаются на 100 км в час. Через сколько часов встреча?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${часы(298,34,22,'9s',3)}
        ${[0,1,2,3].map((ч,к)=>{
          const ш = 176*(1-ч/3), y = 56+к*44;
          return `<g>${проявить('9s',0.08+к*0.2,0.16+к*0.2)}
            ${т(24,y+17,'через '+ч+' ч',14,МУТ,false,'start')}
            <rect x="106" y="${y+4}" width="176" height="16" rx="5" fill="none" stroke="${МУТ}" stroke-width="1" opacity=".5"/>
            <rect x="106" y="${y+4}" width="${ш}" height="16" rx="5" fill="${ч===0?GOLD:(ч===3?GREEN:'#c8912f')}" opacity=".9"/>
            ${т(310,y+17,(300-100*ч)+'',14,ч===3?GREEN:ИНК,ч===3,'end')}
          </g>`;}).join('')}
        ${т(24,40,'осталось, км',14,МУТ,false,'start')}
        <g>${проявить('9s',0.82,0.9)}${подпись(168,236,'300 : 100 = 3 часа',GREEN,16)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три'?'hit':(в?'miss':''), 'Через 3 часа: 300 : 100', "r365Время('три')")}
        ${BTN(4, в==='пять'?'miss':'', 'Через 5 часов: 300 : 60', "r365Время('пять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три',
        в==='три' ? 'Верно: каждый час разрыв уменьшается на 100 км, и 300 км уходят за три часа.'
                  : 'Нет: делить на 60 нельзя — это скорость только одного судна. Делят на скорость сближения: 300 : 100 = 3.') : ЖДЁТ()) +
      ПРАВИЛО('Время встречи = <b>расстояние : скорость сближения</b>.');
  }

  /* 6. Точка встречи: ближе к медленному */
  function F6(s){
    const в = s.точка;
    return ВОПРОС('Суда встретились через 3 часа. Ближе к какому порту это произошло?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        ${море(128,76)}
        ${порт(30,128,'А · 60 км/ч',BLUE)}
        ${порт(306,128,'Б · 40 км/ч',SAND)}
        ${шкала(30,306,76,null,МУТ)}
        <line x1="30" y1="76" x2="196" y2="76" stroke="${BLUE}" stroke-width="5" stroke-linecap="round" opacity=".9"/>
        <line x1="196" y1="76" x2="306" y2="76" stroke="${SAND}" stroke-width="5" stroke-linecap="round" opacity=".9"/>
        <g>${проявить('9s',0.16,0.26)}${т(112,64,'180 км',14,BLUE)}</g>
        <g>${проявить('9s',0.3,0.4)}${т(252,64,'120 км',14,SAND)}</g>
        <g>${анТК('0 0;0 0;124 0;124 0','9s','0;0.1;0.6;1')}${судно(62,148,1,BLUE,0.72)}</g>
        <g>${анТК('0 0;0 0;-82 0;-82 0','9s','0;0.1;0.6;1')}${судно(278,148,-1,SAND,0.72)}</g>
        <line x1="196" y1="60" x2="196" y2="92" stroke="${GOLD}" stroke-width="2" stroke-dasharray="3 3"/>
        ${кольцо(196,148,26,GOLD,'9s',0.62)}
        <g>${проявить('9s',0.7,0.8)}${подпись(196,206,'встреча',GOLD,14)}</g>
        <g>${проявить('9s',0.84,0.92)}${т(168,230,'кто быстрее — тот прошёл больше',14,ИНК)}</g>
      `,238)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='б'?'hit':(в?'miss':''), 'Ближе к порту Б — к медленному', "r365Точка('б')")}
        ${BTN(4, в==='середина'?'miss':'', 'Ровно посередине', "r365Точка('середина')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='б',
        в==='б' ? 'Верно: за одно и то же время «Восток» прошёл 60·3 = 180 км, а «Запад» — только 40·3 = 120 км. Встреча ближе к тому, кто шёл медленнее.'
                : 'Нет: посередине встретились бы при одинаковых скоростях. Здесь один быстрее, значит он прошёл больше и встреча сместилась к порту Б.') : ЖДЁТ()) +
      ПРАВИЛО('Путь каждого = <b>своя скорость × общее время</b>.');
  }

  /* 7. Проверка суммой: 180 + 120 = 300 */
  function F7(s){
    const в = s.сумма;
    return ВОПРОС('Как проверить, что ответ верный?') +
      `<div class="pic">${свг(`
        ${фон(230)}
        ${т(168,26,'Два пути складываются в целое',14,GOLD,true)}
        <g>${анТК('0 0;0 0;0 0;0 0','9s','0;0.3;0.7;1')}
          <rect x="26" y="56" width="150" height="24" rx="6" fill="${BLUE}" opacity=".85"/>
          ${т(101,73,'180 км',14,'#0d1f2b',true)}</g>
        <g>${анТК('50 0;50 0;0 0;0 0','9s','0;0.24;0.62;1')}
          <rect x="176" y="56" width="100" height="24" rx="6" fill="${SAND}" opacity=".9"/>
          ${т(226,73,'120 км',14,'#2b1f0d',true)}</g>
        <g>${проявить('9s',0.66,0.76)}
          <rect x="26" y="104" width="250" height="24" rx="6" fill="none" stroke="${GREEN}" stroke-width="2"/>
          ${т(151,121,'300 км',14,GREEN,true)}</g>
        <g>${проявить('9s',0.82,0.9)}
          ${т(168,166,'180 + 120 = 300 — сошлось',16,ИНК)}
          ${т(168,196,'если сумма не равна расстоянию,',14,МУТ)}
          ${т(168,216,'где-то ошибка в счёте',14,МУТ)}</g>
      `,230)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сумма'?'hit':(в?'miss':''), 'Сложить пути обоих и сравнить с 300', "r365Сумма('сумма')")}
        ${BTN(4, в==='разность'?'miss':'', 'Вычесть меньший путь из большего', "r365Сумма('разность')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сумма',
        в==='сумма' ? 'Верно: вместе они прошли ровно всё расстояние, поэтому 180 + 120 должно дать 300.'
                    : 'Нет: разность 180 − 120 = 60 не говорит ни о чём. Проверяют суммой: вместе суда закрыли весь путь.') : ЖДЁТ()) +
      ПРАВИЛО('Проверка: <b>путь первого + путь второго = всё расстояние</b>.');
  }

  /* 8. Что осталось через 2 часа */
  function F8(s){
    const в = s.осталось;
    return ВОПРОС('Прошло 2 часа. Сколько километров между судами осталось?') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${часы(298,32,20,'8s',2)}
        <g>
          <rect x="26" y="66" width="284" height="28" rx="7" fill="none" stroke="${МУТ}" stroke-width="1.4"/>
          <rect x="26" y="66" width="284" height="28" rx="7" fill="${RED}" opacity=".45">
            ${анК('width','284;284;190;190','8s','0;0.14;0.66;1')}
          </rect>
          ${т(168,86,'пройдено вместе',14,ИНК)}
        </g>
        <g>${проявить('8s',0.22,0.32)}${т(120,118,'100 × 2 = 200 км',14,RED)}</g>
        <g>${проявить('8s',0.5,0.6)}
          <rect x="216" y="136" width="94" height="26" rx="7" fill="${GREEN}" opacity=".6"/>
          ${т(263,154,'осталось',14,'#0d1f16',true)}</g>
        <g>${проявить('8s',0.66,0.76)}${подпись(168,196,'300 − 200 = 100 км',GREEN,16)}</g>
        <g>${проявить('8s',0.84,0.92)}${т(168,222,'ещё час — и встреча',14,МУТ)}</g>
      `,232)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сто'?'hit':(в?'miss':''), '100 км: 300 − 100·2', "r365Осталось('сто')")}
        ${BTN(4, в==='двести'?'miss':'', '200 км: это и есть ответ', "r365Осталось('двести')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сто',
        в==='сто' ? 'Верно: за 2 часа вместе прошли 100·2 = 200 км, и от 300 остаётся 100 км.'
                  : 'Нет: 200 км — это сколько они уже прошли, а спрашивают, сколько ОСТАЛОСЬ. 300 − 200 = 100.') : ЖДЁТ()) +
      ПРАВИЛО('Осталось = <b>расстояние − скорость сближения × время</b>.');
  }

  /* 9. Обратный ход: находим вторую скорость */
  function F9(s){
    const в = s.обратная;
    return ВОПРОС('Известно: 300 км, встретились через 3 часа, «Восток» шёл 60 км/ч. Какова скорость «Запада»?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        ${т(168,26,'Обратный ход',14,GOLD,true)}
        <g>${проявить('9s',0.1,0.2)}
          <rect x="26" y="50" width="284" height="30" rx="8" fill="rgba(255,215,106,.14)" stroke="${GOLD}" stroke-width="1.4"/>
          ${т(168,70,'300 : 3 = 100 км/ч — сближение',16,GOLD)}</g>
        <g>${анТК('0 0;0 0;28 0;28 0','9s','0;0.3;0.66;1')}
          <rect x="40" y="104" width="120" height="26" rx="6" fill="${BLUE}" opacity=".85"/>
          ${т(100,122,'60',14,'#0d1f2b',true)}</g>
        <g>${проявить('9s',0.44,0.54)}
          <rect x="188" y="104" width="108" height="26" rx="6" fill="none" stroke="${SAND}" stroke-width="2" stroke-dasharray="5 4"/>
          ${т(242,122,'?',20,SAND,true)}</g>
        <g>${проявить('9s',0.7,0.8)}${подпись(168,172,'100 − 60 = 40 км/ч',GREEN,16)}</g>
        <g>${проявить('9s',0.86,0.94)}${т(168,212,'сначала сближение, потом вычитаем известную',14,ИНК)}</g>
      `,238)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сорок'?'hit':(в?'miss':''), '40 км/ч: сначала 300 : 3, потом − 60', "r365Обратная('сорок')")}
        ${BTN(4, в==='сто'?'miss':'', '100 км/ч: 300 : 3', "r365Обратная('сто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сорок',
        в==='сорок' ? 'Верно: 300 : 3 = 100 — это сумма двух скоростей. Отнимаем известную: 100 − 60 = 40 км/ч.'
                    : 'Нет: 100 км/ч — это скорость сближения, то есть сумма обеих. Скорость второго судна: 100 − 60 = 40.') : ЖДЁТ()) +
      ПРАВИЛО('Из расстояния и времени добывают <b>сумму скоростей</b>, а потом вычитают известную.');
  }

  /* 10. Удаление: в разные стороны — тоже сумма */
  function F10(s){
    const в = s.удаление;
    return ВОПРОС('А если суда выйдут из одной точки в РАЗНЫЕ стороны? Как быстро будет расти расстояние между ними?') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${т(168,26,'Разные стороны',14,GOLD,true)}
        ${море(120,70)}
        <line x1="168" y1="52" x2="168" y2="118" stroke="${GOLD}" stroke-width="1.6" stroke-dasharray="4 4"/>
        <g>${анТК('0 0;0 0;-96 0;-96 0','9s','0;0.12;0.66;1')}${судно(150,142,-1,BLUE,0.72)}</g>
        <g>${анТК('0 0;0 0;96 0;96 0','9s','0;0.12;0.66;1')}${судно(186,142,1,SAND,0.72)}</g>
        <g>
          <line x1="168" y1="94" x2="168" y2="94" stroke="${GREEN}" stroke-width="3" stroke-linecap="round">
            ${анК('x1','168;168;54;54','9s','0;0.12;0.66;1')}
            ${анК('x2','168;168;282;282','9s','0;0.12;0.66;1')}
          </line>
        </g>
        <g>${проявить('9s',0.7,0.8)}${подпись(168,80,'за час разрыв +100 км',GREEN,14)}</g>
        <g>${проявить('9s',0.84,0.92)}
          ${т(168,206,'скорость удаления — тоже сумма',14,ИНК)}
          ${т(168,226,'только расстояние не тает, а растёт',14,МУТ)}</g>
      `,232)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сумма'?'hit':(в?'miss':''), 'На 100 км в час — та же сумма', "r365Удаление('сумма')")}
        ${BTN(4, в==='разность'?'miss':'', 'На 20 км в час — разность', "r365Удаление('разность')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сумма',
        в==='сумма' ? 'Верно: каждый уходит в свою сторону, и обе части складываются — разрыв растёт на 60 + 40 = 100 км в час.'
                    : 'Нет: разность нужна, когда один догоняет другого. В разные стороны скорости складывают, как и навстречу.') : ЖДЁТ()) +
      ПРАВИЛО('Навстречу разрыв <b>тает</b> на сумму, в разные стороны — <b>растёт</b> на неё же.');
  }

  /* 11. Ловушка: делить на одну скорость нельзя */
  function F11(s){
    const в = s.ловушка;
    return ВОПРОС('Ученик написал: 300 : 60 = 5 часов. В чём ошибка?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        <g>${проявить('9s',0.08,0.18)}
          <rect x="22" y="44" width="292" height="60" rx="10" fill="rgba(232,106,90,.12)" stroke="${RED}" stroke-width="1.6"/>
          ${т(168,70,'300 : 60 = 5 ч',20,RED,true)}
          ${т(168,92,'так считают, когда второй стоит на месте',14,МУТ)}</g>
        <g>${проявить('9s',0.34,0.44)}
          <rect x="22" y="116" width="292" height="60" rx="10" fill="rgba(143,209,168,.12)" stroke="${GREEN}" stroke-width="1.6"/>
          ${т(168,142,'300 : 100 = 3 ч',20,GREEN,true)}
          ${т(168,164,'а он тоже идёт навстречу',14,МУТ)}</g>
        <g>${анТК('0 0;0 0;40 0;40 0','9s','0;0.55;0.9;1')}${судно(60,206,1,BLUE,0.66)}</g>
        <g>${проявить('9s',0.6,0.7)}${судно(276,206,-1,SAND,0.66)}</g>
        <g>${проявить('9s',0.84,0.92)}${т(168,232,'разница в два часа — из-за второго судна',14,ИНК)}</g>
      `,238)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='забыл'?'hit':(в?'miss':''), 'Забыл про скорость второго судна', "r365Ловушка('забыл')")}
        ${BTN(4, в==='делить'?'miss':'', 'Делить вообще нельзя, надо умножать', "r365Ловушка('делить')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='забыл',
        в==='забыл' ? 'Верно: 300 : 60 — это время, за которое одно судно прошло бы весь путь в одиночку. Но навстречу идёт второе, поэтому делить надо на 100.'
                    : 'Нет: делить как раз правильно — время это путь, делённый на скорость. Ошибка в том, на что делят: нужна скорость сближения, а не одного судна.') : ЖДЁТ()) +
      ПРАВИЛО('Делим на <b>сумму</b>, а не на скорость одного. Иначе встреча «опаздывает».');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Между городами 240 км. Скорости 50 и 30 км/ч навстречу. Скорость сближения?',
      варианты:[{т:'80 км/ч',ок:true},{т:'20 км/ч',ок:false}],
      разбор:'50 + 30 = 80 км/ч.' },
    { вопрос:'Те же 240 км, сближение 80 км/ч. Через сколько часов встреча?',
      варианты:[{т:'3 часа',ок:true},{т:'4 часа',ок:false}],
      разбор:'240 : 80 = 3 часа.' },
    { вопрос:'Пешеходы: 4 и 5 км/ч, между ними 27 км. Когда встретятся?',
      варианты:[{т:'через 3 часа',ок:true},{т:'через 6 часов',ок:false}],
      разбор:'4 + 5 = 9 км/ч; 27 : 9 = 3 часа.' },
    { вопрос:'240 км, сближение 80 км/ч. Сколько останется через 2 часа?',
      варианты:[{т:'80 км',ок:true},{т:'160 км',ок:false}],
      разбор:'80 · 2 = 160 прошли; 240 − 160 = 80 км осталось.' },
    { вопрос:'180 км, встретились через 2 часа. Один шёл 50 км/ч. Скорость второго?',
      варианты:[{т:'40 км/ч',ок:true},{т:'90 км/ч',ок:false}],
      разбор:'180 : 2 = 90 — сближение; 90 − 50 = 40 км/ч.' }
  ];

  function F12(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r365Reset()")}</div>` +
        ПРАВИЛО('Сближение — <b>сумма</b>, время — <b>расстояние : сумма</b>.');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ВОПРОС('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к,
        выбран===к ? (в.ок?'hit':'miss') : '',
        в.т, "r365Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : ЖДЁТ());
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Скорость сближения', задания:[
    {q:'50 км/ч и 30 км/ч навстречу. Сближение?', в:0, варианты:['80 км/ч','20 км/ч'], раз:'50 + 30 = 80.'},
    {q:'12 км/ч и 8 км/ч навстречу. Сближение?', в:0, варианты:['20 км/ч','4 км/ч'], раз:'12 + 8 = 20.'},
    {q:'Пешеходы 4 и 5 км/ч навстречу. Сближение?', в:1, варианты:['1 км/ч','9 км/ч'], раз:'4 + 5 = 9.'},
    {q:'60 км/ч и 40 км/ч навстречу. Сближение?', в:1, варианты:['24 км/ч','100 км/ч'], раз:'60 + 40 = 100.'}
  ]};
  const Т2 = { имя:'Время встречи', задания:[
    {q:'240 км, сближение 80 км/ч. Встреча через…', в:0, варианты:['3 часа','4 часа'], раз:'240 : 80 = 3.'},
    {q:'27 км, сближение 9 км/ч. Встреча через…', в:1, варианты:['9 часов','3 часа'], раз:'27 : 9 = 3.'},
    {q:'400 км, сближение 100 км/ч. Встреча через…', в:0, варианты:['4 часа','40 часов'], раз:'400 : 100 = 4.'},
    {q:'90 км, сближение 30 км/ч. Встреча через…', в:1, варианты:['2 часа','3 часа'], раз:'90 : 30 = 3.'}
  ]};
  const Т3 = { имя:'Ловушки', задания:[
    {q:'300 км, скорости 60 и 40. Ученик делит 300 : 60. Верно?', в:1,
      варианты:['Верно','Нет: делить надо на 100'], раз:'Делят на сумму скоростей: 300 : 100 = 3 ч.'},
    {q:'300 км, сближение 100 км/ч. Сколько ОСТАЛОСЬ через 2 часа?', в:0,
      варианты:['100 км','200 км'], раз:'Прошли 200, осталось 300 − 200 = 100.'},
    {q:'Скорости 60 и 40. Кто прошёл больше до встречи?', в:0,
      варианты:['Тот, у кого 60','Оба поровну'], раз:'Время общее, поэтому больше прошёл быстрый: 180 против 120.'},
    {q:'Суда разошлись в разные стороны, 60 и 40 км/ч. Разрыв растёт на…', в:1,
      варианты:['20 км/ч','100 км/ч'], раз:'В разные стороны скорости тоже складывают.'}
  ]};

  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ВОПРОС(з.q) +
      `<div class="ask">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r365T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r365TNext('"+ключ+"')")}</div>`
        : ЖДЁТ());
  }

  /* ================= СБОРКА ================= */
  const L365 = {
    id: ID,
    title: 'Движение навстречу',
    ico: '⛵',
    src: 'Начальная школа · 4 класс · Одновременное движение',
    subj: 'jun',
    explain: [
      'Два судна стоят в портах, между которыми 300 км. Чтобы понять, когда они встретятся, нужны обе скорости, а не одна.',
      'Вышли одновременно — значит часы у них общие, и время в пути одинаковое.',
      'За первый час «Восток» прошёл 60 км, «Запад» — 40 км: разрыв уменьшился на 60 + 40 = 100 км.',
      'Скорости складывают потому, что расстояние тает сразу с двух сторон.',
      'Каждый час уходит 100 км, поэтому 300 км тают за 300 : 100 = 3 часа.',
      'За 3 часа «Восток» прошёл 180 км, «Запад» — 120 км: встреча ближе к медленному.',
      'Проверка: 180 + 120 = 300 — вместе они закрыли весь путь.',
      'Через 2 часа пройдено 100 · 2 = 200 км, значит осталось 300 − 200 = 100 км.',
      'Обратный ход: 300 : 3 = 100 — это сумма скоростей; 100 − 60 = 40 км/ч у второго.',
      'Если идти в разные стороны, разрыв растёт на ту же сумму: 100 км в час.',
      'Ловушка: 300 : 60 = 5 ч — так считают, будто второе судно стоит. Делить надо на 100.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: скорость сближения.',
      'Тренажёр 2: время встречи.',
      'Тренажёр 3: ловушки.'
    ],
    check: {
      q: 'Скорости 50 и 30 км/ч, движение навстречу. Скорость сближения?',
      choices: ['80 км/ч','20 км/ч','50 км/ч'],
      ans: 0,
      exp: 'Навстречу скорости складывают: 50 + 30 = 80 км/ч.'
    },
    tasks: [
      { q:'Расстояние 240 км, скорость сближения 80 км/ч. Через сколько часов встреча?', kind:'choice',
        choices:['3 ч','4 ч'], ans:0,
        hints:['Время = расстояние : скорость сближения.','240 : 80.'],
        sol:'3 часа.' },
      { q:'Пешеходы идут навстречу со скоростями 4 и 5 км/ч. Между ними 27 км. Сколько останется через 2 часа?', kind:'choice',
        choices:['9 км','18 км'], ans:0,
        hints:['За 2 ч сблизились на (4 + 5) · 2 = 18 км.','27 − 18.'],
        sol:'9 км.' },
      { q:'300 км, встретились через 3 часа, один шёл 60 км/ч. Скорость второго?', kind:'choice',
        choices:['40 км/ч','100 км/ч'], ans:0,
        hints:['Сначала найди сумму скоростей: 300 : 3.','Потом вычти известную: 100 − 60.'],
        sol:'40 км/ч.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L365.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=тренажёр(s,'т1',Т1,1);
    else if(f===14) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);

    const ЗАГОЛОВКИ={
      1:'Два порта и 300 км',
      2:'Вышли одновременно',
      3:'Первый час',
      4:'Почему складывают',
      5:'Три часа до встречи',
      6:'Где встретятся',
      7:'Проверка суммой',
      8:'Что осталось',
      9:'Обратный ход',
      10:'В разные стороны',
      11:'Ловушка',
      12:'Практика',
      13:'Тренажёр 1',
      14:'Тренажёр 2',
      15:'Тренажёр 3'
    };
    el.innerHTML = `<div class="s6 l365" data-frame="${f}">
        <h2>${ЗАГОЛОВКИ[f]||'Движение навстречу'}</h2>
        ${сцена}
      </div>`;
  }

  window.r365Карта    =(к)=>{ S().карта=к; chRender(0); };
  window.r365Старт    =(к)=>{ S().старт=к; chRender(0); };
  window.r365Сближение=(к)=>{ S().сближение=к; chRender(0); };
  window.r365Полоса   =(к)=>{ S().полоса=к; chRender(0); };
  window.r365Время    =(к)=>{ S().время=к; chRender(0); };
  window.r365Точка    =(к)=>{ S().точка=к; chRender(0); };
  window.r365Сумма    =(к)=>{ S().сумма=к; chRender(0); };
  window.r365Осталось =(к)=>{ S().осталось=к; chRender(0); };
  window.r365Обратная =(к)=>{ S().обратная=к; chRender(0); };
  window.r365Удаление =(к)=>{ S().удаление=к; chRender(0); };
  window.r365Ловушка  =(к)=>{ S().ловушка=к; chRender(0); };
  window.r365Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r365Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r365T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=(s[ключ+'Шаг']||0)%набор.задания.length;
    const з=набор.задания[шаг];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r365TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L365; else arr.push(L365);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA365={render:render, L:L365};
})();
