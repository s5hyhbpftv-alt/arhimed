/* ============ МАТЕМАТИКА · УРОК 995 · «ПЕРЕВОД ОБЫКНОВЕННОЙ ДРОБИ В ДЕСЯТИЧНУЮ» ============
   5 класс. Сюжет: мастерская тканей — портной отрезает 3/4 метра, а на кассе
   нужна десятичная запись.

   СВЕРКА С ПЕТЕРСОНОМ (5 класс): «десятичные дроби (запись, сравнение,
   округление, действия, перевод)», «обыкновенные дроби (все действия,
   смешанные, часть от целого)». Источники: план-1-9/петерсон_1-9.md,
   план-1-9/углублённая_5-9.md.

   СДЕЛАНО ПО СТАНДАРТУ deploy/АНИМАЦИЯ_УРОКОВ.md:
     • в каждом кадре ровно одно объясняющее движение и одна вспышка итога;
     • примитивы ан / анТ / пульс / кольцо / летит, всегда видимые состояния
       (минимальная прозрачность 0.3), текст не масштабируется;
     • prefers-reduced-motion: SMIL-теги не выводятся вовсе, кадр показывает
       конечное состояние;
     • появление блоков 280 мс cubic-bezier(.23,1,.32,1), шаг 45 мс.

   УЧТЕНЫ НОВЫЕ СКИЛЫ:
     • `mobile-native` — `touch-action: manipulation` и подавление вспышки
       касания на кнопках, `text-size-adjust: 100%`, цели от 56 px;
     • `webapp-testing` — проверка ждёт `networkidle`, а не фиксированную паузу;
     • `frontend-design` — композиция кадров разная: лента, линейка, карточки,
       шкала нулей, набор фишек, деление в столбик, две полосы, разряды.

   ДВЕНАДЦАТЬ КАДРОВ:
     1 лента 3/4 м и касса;            2 что такое десятая: линейка;
     3 приём: домножаем до 10/100;     4 сколько нулей — столько знаков;
     5 три примера;                    6 обратный перевод 0,25 = 1/4;
     7 какие знаменатели годятся;      8 ловушка: 1/3 не переводится точно;
     9 сравнение 3/4 и 0,7;           10 чтение 0,75 по разрядам;
    11 практика — пять уровней;       12 тренажёр — четыре уровня. */
(function(){
  'use strict';

  const ID = 995;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';

  const CSS=`
  #lvis .s6.l995{gap:14px}
  #lvis .s6.l995 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l995 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l995 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l995 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l995 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l995 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l995 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l995 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l995 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l995 .правило b{color:${GOLD}}
  #lvis .s6.l995 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l995 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l995 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l995 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l995 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l995 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l995 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l995 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l995 .уровни .точка.сейчас{background:${GOLD};animation:l995dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l995dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l995 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l995{-webkit-text-size-adjust:100%}
  #lvis .s6.l995 [data-anim]{animation:l995rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l995rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l995 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l995 [data-anim]{animation:none!important}
    #lvis .s6.l995 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l995 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l995-style');
      if(!s){ s=document.createElement('style'); s.id='l995-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const пульс = (длит,нач) => ан('opacity','1;0.35;1',длит, нач!=null?'begin="'+нач+'s"':'');
  const кольцо = (x,y,r,цвет,длит,нач) =>
    `<circle cx="${x}" cy="${y}" r="${r}" fill="none" stroke="${цвет}" stroke-width="2">`+
      ан('opacity','0;0.9;0',длит,'keyTimes="0;0.5;1"'+(нач!=null?' begin="'+нач+'s"':''))+
      ан('r',r+';'+(r+9)+';'+r,длит,'keyTimes="0;0.5;1"'+(нач!=null?' begin="'+нач+'s"':''))+
    `</circle>`;
  const летит = (x0,y0,x1,y1,длит,нач) =>
    `<circle cx="${x0}" cy="${y0}" r="5" fill="${GOLD}">`+
      анТ('0 0;0 0;'+(x1-x0).toFixed(1)+' '+(y1-y0).toFixed(1)+';0 0',длит,
          'keyTimes="0;0.08;0.55;1"'+(нач!=null?' begin="'+нач+'s"':''))+
    `</circle>`;

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c995-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22362c"/><stop offset="1" stop-color="#152219"/>
      </linearGradient>
      <linearGradient id="c995-лента" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#7fd1ff"/><stop offset="1" stop-color="#3f86b8"/>
      </linearGradient>
      <linearGradient id="c995-ткань" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#ffd76a"/><stop offset="1" stop-color="#c9971f"/>
      </linearGradient>
      <linearGradient id="c995-карта" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2c4536"/><stop offset="1" stop-color="#1b2f22"/>
      </linearGradient>
      <filter id="c995-тень" x="-30%" y="-30%" width="160%" height="160%">
        <feGaussianBlur stdDeviation="2.2"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||190}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* дробь в рамке */
  const дробь = (x,y,числ,знам,цвет,кегль) => {
    const к=кегль||20;
    return `<g>
      <rect x="${x-24}" y="${y-30}" width="48" height="60" rx="10" fill="url(#c995-карта)" stroke="${цвет||ЛИНИЯ}" stroke-width="1.6"/>
      ${т(x,y-6,числ,к,цвет||ИНК,true)}
      <line x1="${x-14}" y1="${y+2}" x2="${x+14}" y2="${y+2}" stroke="${цвет||ИНК}" stroke-width="1.8"/>
      ${т(x,y+24,знам,к,цвет||ИНК,true)}
    </g>`;
  };

  /* карточка числа */
  const карта = (x,y,текст,подпись,цвет,кегль) => `
    <g>
      <rect x="${x}" y="${y}" width="86" height="52" rx="10" fill="url(#c995-карта)" stroke="${цвет||ЛИНИЯ}" stroke-width="1.6"
        filter="url(#c995-тень)"/>
      ${т(x+43,y+30,текст,кегль||20,цвет||ИНК,true)}
      ${подпись?т(x+43,y+46,подпись,12,МУТ):''}
    </g>`;

  /* ================= КАДРЫ ================= */

  /* 1. Лента 3/4 м и касса */
  function F1(s){
    const в = s.лента;
    const части=[0,1,2,3];
    return ВОПРОС('Портной отрезал 3/4 метра ткани. Как записать эту длину десятичной дробью?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Лента длиной один метр',14,GOLD,true)}
        ${части.map(i=>`<g>${пульс('7s',i*0.6)}
          <rect x="${34+i*68}" y="52" width="66" height="46" rx="6"
            fill="${i<3?'url(#c995-ткань)':'rgba(255,255,255,.05)'}"
            stroke="${i<3?'#8a5f05':ЛИНИЯ}" stroke-width="1.6"/>
          ${т(67+i*68,80,'1/4',16,i<3?'#2a2118':МУТ,true)}</g>`).join('')}
        ${т(168,122,'отрезали три части из четырёх — это 3/4 метра',12,ИНК)}
        ${летит(102,146,214,146,'6s',0)}
        ${карта(20,140,'3/4 м','на витрине')}
        ${карта(230,140,'0,75 м','на кассе',GOLD)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='десятичная'?'hit':(в?'miss':''), 'Нужно перевести дробь в десятичную запись', "r995Tape('десятичная')")}
        ${BTN(4, в==='оставить'?'miss':'', 'Можно оставить 3/4 — касса поймёт', "r995Tape('оставить')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='десятичная',
        в==='десятичная' ? 'Верно: 3/4 метра — это 0,75 метра. Ниже разберём, как из одной записи получается другая.'
                        : 'Нет: касса работает с десятичными, поэтому 3/4 нужно перевести: 3/4 = 0,75.') : ЖДЁТ()) +
      ПРАВИЛО('Одну и ту же длину можно записать и обыкновенной дробью, и <b>десятичной</b>.');
  }

  /* 2. Что такое десятая: линейка */
  function F2(s){
    const в = s.десятая;
    const деления=Array.from({length:11},(_,i)=>i);
    return ВОПРОС('Что означает первая цифра после запятой в записи 0,1?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Линейка: метр разделён на десять частей',14,GOLD,true)}
        <rect x="24" y="66" width="288" height="40" rx="6" fill="url(#c995-лента)" opacity=".25" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
        ${деления.map(i=>`<g>${i>0&&i<10?пульс('7s',i*0.5):''}
          <line x1="${24+i*28.8}" y1="66" x2="${24+i*28.8}" y2="${i%5===0?82:76}" stroke="${ИНК}" stroke-width="1.6"/>
          ${т(24+i*28.8,122,i===0?'0':(i===10?'1':'0,'+i),12,i===1?GOLD:МУТ,i===1)}</g>`).join('')}
        <rect x="52.8" y="66" width="28.8" height="40" fill="rgba(255,215,106,.28)" stroke="${GOLD}" stroke-width="2"/>
        ${т(168,152,'одна десятая метра — это 0,1 метра',12,ИНК)}
        ${т(168,176,'десятых в целом метре ровно десять',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='десятая'?'hit':(в?'miss':''), 'Одна десятая: 0,1 = 1/10', "r995Tenth('десятая')")}
        ${BTN(4, в==='сотая'?'miss':'', 'Одна сотая: 0,1 = 1/100', "r995Tenth('сотая')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='десятая',
        в==='десятая' ? 'Верно: первая цифра после запятой — десятые. 0,1 = 1/10, 0,3 = 3/10.'
                     : 'Нет: сотые идут второй цифрой после запятой. Первая — десятые: 0,1 = 1/10.') : ЖДЁТ()) +
      ПРАВИЛО('Первая цифра после запятой — <b>десятые</b>: 0,1 = 1/10; вторая — сотые: 0,01 = 1/100.');
  }

  /* 3. Приём: домножаем числитель и знаменатель */
  function F3(s){
    const в = s.приём;
    return ВОПРОС('Как из 3/4 получить запись со знаменателем 100?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Домножаем оба числа',14,GOLD,true)}
        <g>${пульс('7s',0)}${дробь(58,74,'3','4',BLUE)}</g>
        ${т(104,80,'×25',14,GOLD,true)}
        ${т(140,80,'→',20,МУТ)}
        <g>${пульс('7s',1.6)}${дробь(190,74,'75','100',GOLD)}</g>
        ${т(228,80,'=',20,МУТ)}
        <g>${пульс('7s',2.6)}${карта(238,48,'0,75',null,GREEN,20)}</g>
        ${летит(104,120,190,120,'6s',0)}
        <rect x="36" y="116" width="264" height="46" rx="10" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
        ${т(168,138,'3 · 25 = 75',14,ИНК)}
        ${т(168,156,'4 · 25 = 100',14,ИНК)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='умножить'?'hit':(в?'miss':''), 'Умножить и числитель, и знаменатель на 25', "r995How('умножить')")}
        ${BTN(4, в==='числитель'?'miss':'', 'Умножить только знаменатель', "r995How('числитель')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='умножить',
        в==='умножить' ? 'Верно: дроби равны, если числитель и знаменатель умножить на одно и то же число: 3/4 = 75/100.'
                      : 'Нет: так изменится значение дроби. Умножаем оба числа: 3/4 = 75/100.') : ЖДЁТ()) +
      ПРАВИЛО('Домножаем до 10, 100 или 1000: <b>3/4 = 75/100 = 0,75</b>.');
  }

  /* 4. Сколько нулей — столько знаков */
  function F4(s){
    const в = s.нули;
    const стр=[['10','1 знак','3/4 не подходит'],['100','2 знака','75/100 = 0,75'],['1000','3 знака','625/1000 = 0,625']];
    return ВОПРОС('Знаменатель 100. Сколько цифр будет после запятой?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Сколько нулей — столько знаков',14,GOLD,true)}
        ${стр.map(([зн,знаки,пр],i)=>{
          const y=46+i*44;
          return `<g>
            <rect x="20" y="${y}" width="296" height="36" rx="9" fill="rgba(255,255,255,.04)" stroke="${i===1?GOLD:ЛИНИЯ}" stroke-width="${i===1?2:1.4}"/>
            ${т(62,y+24,зн,20,i===1?GOLD:ИНК,true)}
            <g>${пульс('7s',i*1.2)}${т(150,y+24,знаки,14,i===1?GOLD:МУТ,true)}</g>
            ${т(258,y+24,пр,12,МУТ)}
          </g>`;
        }).join('')}
        <rect x="34" y="42" width="56" height="44" rx="8" fill="none" stroke="${GOLD}" stroke-width="2">
          ${ан('opacity','0.3;1;0.3','7s')}
        </rect>
        ${летит(62,142,190,142,'6s',0)}
        ${т(168,180,'запятая отступает вправо ровно на число нулей',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='две'?'hit':(в?'miss':''), 'Две цифры: 0,75', "r995Zeros('две')")}
        ${BTN(4, в==='одна'?'miss':'', 'Одна цифра: 0,7', "r995Zeros('одна')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='две',
        в==='две' ? 'Верно: в числе 100 два нуля, значит после запятой две цифры: 75/100 = 0,75.'
                  : 'Нет: нулей два, поэтому и знаков два: 75/100 = 0,75. Одна цифра была бы при знаменателе 10.') : ЖДЁТ()) +
      ПРАВИЛО('Знаменатель <b>10 — один знак, 100 — два, 1000 — три</b>.');
  }

  /* 5. Три примера */
  function F5(s){
    const в = s.примеры;
    const пр=[['1/2','5/10','0,5'],['3/5','6/10','0,6'],['7/20','35/100','0,35']];
    return ВОПРОС('Переведи 7/20 в десятичную дробь.') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Три перевода подряд',14,GOLD,true)}
        ${пр.map(([д,р,о],i)=>{
          const y=60+i*42;
          return `<g>${пульс('9s',i*1.1)}
            ${т(60,y+6,д,20,BLUE,true)}
            ${т(104,y+6,'=',20,МУТ)}
            ${т(164,y+6,р,20,ИНК,true)}
            ${т(232,y+6,'=',20,МУТ)}
            ${т(288,y+6,о,20,GOLD,true)}</g>`;
        }).join('')}
        ${кольцо(288,144,28,GOLD,'9s',2.4)}
        ${т(168,182,'домножаем знаменатель до 10 или 100',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='0,35'?'hit':(в?'miss':''), '0,35: 7/20 = 35/100', "r995Ex('0,35')")}
        ${BTN(4, в==='0,7'?'miss':'', '0,7: двадцать делится на два', "r995Ex('0,7')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='0,35',
        в==='0,35' ? 'Верно: 20 · 5 = 100, значит 7 · 5 = 35 — получается 35/100 = 0,35.'
                   : 'Нет: знаменатель нужно довести до 100, а не сокращать: 7/20 = 35/100 = 0,35.') : ЖДЁТ()) +
      ПРАВИЛО('1/2 = 0,5 · 3/5 = 0,6 · <b>7/20 = 0,35</b> — правило всегда одно.');
  }

  /* 6. Обратный перевод */
  function F6(s){
    const в = s.обратно;
    return ВОПРОС('Запиши 0,25 обыкновенной дробью в самом простом виде.') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Обратный перевод: от запятой к дроби',14,GOLD,true)}
        <g>${пульс('7s',0)}${карта(22,48,'0,25',null,BLUE,20)}</g>
        ${т(122,80,'→',20,МУТ)}
        <g>${пульс('7s',1.4)}${дробь(170,74,'25','100',GOLD)}</g>
        ${т(216,80,'→',20,МУТ)}
        <g>${пульс('7s',2.6)}${дробь(268,74,'1','4',GREEN)}</g>
        ${летит(108,80,146,80,'6s',0)}${летит(196,80,244,80,'6s',0.9)}
        ${т(168,138,'две цифры после запятой — знаменатель 100',12,ИНК)}
        ${т(168,162,'сокращаем на 25: 25/100 = 1/4',12,GOLD)}
        ${кольцо(268,74,34,GREEN,'7s',3.2)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='четверть'?'hit':(в?'miss':''), '1/4: 25/100 сокращаем на 25', "r995Back('четверть')")}
        ${BTN(4, в==='двадцать'?'miss':'', '25/100 — короче не станет', "r995Back('двадцать')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='четверть',
        в==='четверть' ? 'Верно: 0,25 = 25/100 = 1/4. Две цифры после запятой дают знаменатель 100, дальше сокращаем.'
                      : 'Нет: 25/100 сокращается на 25 — получается 1/4. Так запись короче.') : ЖДЁТ()) +
      ПРАВИЛО('Обратно: <b>0,25 = 25/100 = 1/4</b>. Цифр после запятой столько же, сколько нулей в знаменателе.');
  }

  /* 7. Какие знаменатели годятся */
  function F7(s){
    const в = s.годятся;
    const да=['2','4','5','8','10','20','25','50'];
    const нет=['3','6','7','9','11','12'];
    return ВОПРОС('Какие дроби переводятся в десятичную точно?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Смотрим на знаменатель',14,GOLD,true)}
        <rect x="16" y="40" width="304" height="60" rx="10" fill="rgba(143,209,168,.10)" stroke="${GREEN}" stroke-width="1.6"/>
        ${т(24,58,'только двойки и пятёрки — переводится точно',12,GREEN,true,'start')}
        ${да.map((ч,i)=>`<g>${пульс('9s',i*0.5)}
          <rect x="${28+i*36}" y="66" width="30" height="26" rx="7" fill="url(#c995-карта)" stroke="${GREEN}" stroke-width="1.4"/>
          ${т(43+i*36,84,ч,14,GREEN,true)}</g>`).join('')}
        <rect x="16" y="110" width="304" height="60" rx="10" fill="rgba(232,106,90,.10)" stroke="${RED}" stroke-width="1.6"/>
        ${т(24,128,'есть другие множители — точно не выйдет',12,RED,true,'start')}
        ${нет.map((ч,i)=>`<g>${пульс('9s',3+i*0.5)}
          <rect x="${28+i*50}" y="136" width="42" height="26" rx="7" fill="url(#c995-карта)" stroke="${RED}" stroke-width="1.4"/>
          ${т(49+i*50,154,ч,14,RED,true)}</g>`).join('')}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='двойки'?'hit':(в?'miss':''), 'Те, у которых знаменатель из двоек и пятёрок', "r995Which('двойки')")}
        ${BTN(4, в==='любые'?'miss':'', 'Любые дроби переводятся точно', "r995Which('любые')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='двойки',
        в==='двойки' ? 'Верно: 1/8 = 125/1000 = 0,125, а 1/3 так не перевести. Годятся знаменатели 2, 4, 5, 8, 10, 20, 25, 50.'
                    : 'Нет: перевод получается точно, только если знаменатель состоит из двоек и пятёрок. У 1/3 знаменатель другой.') : ЖДЁТ()) +
      ПРАВИЛО('Точно переводятся дроби со знаменателем из <b>двоек и пятёрок</b>: 2, 4, 5, 8, 10, 20, 25, 50.');
  }

  /* 8. Ловушка: 1/3 */
  function F8(s){
    const в = s.ловушка;
    return ВОПРОС('Переводим 1/3 в десятичную дробь. Что получится?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Треть метра делим на десятые',14,GOLD,true)}
        <rect x="30" y="48" width="276" height="40" rx="8" fill="url(#c995-лента)" opacity=".22" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
        ${[0,1,2].map(i=>`<g>${пульс('7s',i*0.8)}
          <rect x="${30+i*92}" y="48" width="92" height="40" fill="none" stroke="${GOLD}" stroke-width="2"/>
          ${т(76+i*92,74,'1/3',14,GOLD,true)}</g>`).join('')}
        ${т(168,112,'1 : 3 = 0,3333… — тройки не кончаются',16,ИНК,true)}
        ${кольцо(276,132,26,RED,'7s',1.2)}
        ${т(168,150,'проверка: 0,333… · 3 = 0,999… а не 1',12,МУТ)}
        ${т(168,176,'точного десятичного равенства нет',12,RED)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='приблизительно'?'hit':(в?'miss':''), 'Только приблизительно: 1/3 ≈ 0,33', "r995Trap('приблизительно')")}
        ${BTN(4, в==='точно'?'miss':'', 'Точно: 1/3 = 0,3', "r995Trap('точно')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='приблизительно',
        в==='приблизительно' ? 'Верно: 1/3 = 0,333… Точной конечной записи нет, поэтому пишут приближённо: 1/3 ≈ 0,33.'
                            : 'Нет: 0,3 · 3 = 0,9, а не 1. Точной десятичной записи у 1/3 нет — только приближённая.') : ЖДЁТ()) +
      ПРАВИЛО('Если в знаменателе есть другие множители, запись будет <b>приближённой</b>: 1/3 ≈ 0,33.');
  }

  /* 9. Сравнение */
  function F9(s){
    const в = s.сравнение;
    return ВОПРОС('Что больше: 3/4 или 0,7?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Приводим к одной записи',14,GOLD,true)}
        ${т(30,68,'3/4',16,BLUE,true,'start')}
        <rect x="80" y="52" width="220" height="26" rx="8" fill="rgba(255,255,255,.06)" stroke="${ЛИНИЯ}" stroke-width="1.2"/>
        <rect x="80" y="52" width="165" height="26" rx="8" fill="url(#c995-ткань)" stroke="#8a5f05" stroke-width="1.2"/>
        ${т(258,68,'0,75',14,GOLD,true)}
        ${т(30,120,'0,7',16,BLUE,true,'start')}
        <rect x="80" y="104" width="220" height="26" rx="8" fill="rgba(255,255,255,.06)" stroke="${ЛИНИЯ}" stroke-width="1.2"/>
        <rect x="80" y="104" width="154" height="26" rx="8" fill="url(#c995-ткань)" opacity=".55" stroke="#8a5f05" stroke-width="1.2"/>
        ${т(258,120,'0,70',14,ИНК,true)}
        ${кольцо(200,65,24,GREEN,'7s',0.6)}
        ${т(168,164,'3/4 = 0,75, а 0,75 > 0,70',16,GREEN,true)}
        ${т(168,184,'сравнивать можно только одинаковые записи',12,МУТ)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='дробь'?'hit':(в?'miss':''), '3/4 больше: это 0,75 против 0,70', "r995Cmp('дробь')")}
        ${BTN(4, в==='десятичная'?'miss':'', '0,7 больше: у неё цифра 7', "r995Cmp('десятичная')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='дробь',
        в==='дробь' ? 'Верно: 3/4 = 0,75, а 0,7 = 0,70. Сравниваем одинаково записанные числа: 0,75 > 0,70.'
                   : 'Нет: сравнить можно, только приведя к одной записи. 3/4 = 0,75, значит 3/4 больше 0,7.') : ЖДЁТ()) +
      ПРАВИЛО('Чтобы сравнить, приведи к одной записи: <b>3/4 = 0,75 > 0,7</b>.');
  }

  /* 10. Чтение по разрядам */
  function F10(s){
    const в = s.чтение;
    const цифры=['0',',','7','5'];
    const подписи=['целых','запятая','десятые','сотые'];
    return ВОПРОС('Как прочитать запись 0,75 по разрядам?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="190" fill="url(#c995-стена)"/>
        <rect x="0" y="0" width="336" height="190" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Читаем по разрядам',14,GOLD,true)}
        ${цифры.map((ч,i)=>{
          const x=54+i*58;
          const актив=(i===2||i===3);
          return `<g>
            <rect x="${x-24}" y="46" width="48" height="56" rx="10" fill="${актив?'rgba(255,215,106,.14)':'rgba(255,255,255,.04)'}"
              stroke="${актив?GOLD:ЛИНИЯ}" stroke-width="${актив?1.8:1.3}">${актив?пульс('7s',(i-2)*1.2):''}</rect>
            ${т(x,84,ч,24,актив?GOLD:ИНК,true)}
            ${т(x,122,подписи[i],12,актив?GOLD:МУТ)}
          </g>`;
        }).join('')}
        <rect x="30" y="140" width="276" height="36" rx="10" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
        ${т(168,164,'нуль целых семьдесят пять сотых',14,ИНК,true)}
      `,190)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сотые'?'hit':(в?'miss':''), 'Семь — десятые, пять — сотые', "r995Read('сотые')")}
        ${BTN(4, в==='десятые'?'miss':'', 'Семь — сотые, пять — десятые', "r995Read('десятые')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сотые',
        в==='сотые' ? 'Верно: после запятой первая цифра — десятые, вторая — сотые. 0,75 — семь десятых и пять сотых.'
                    : 'Нет: разряды идут слева направо: сначала десятые (7), потом сотые (5).') : ЖДЁТ()) +
      ПРАВИЛО('Читаем так: <b>0,75 — нуль целых семьдесят пять сотых</b>.');
  }

  /* 11. Практика: пять уровней */
  const УРОВНИ=[
    { вопрос:'Переведи 1/4 в десятичную дробь.', варианты:[
        {т:'0,25', ок:true,  почему:'1/4 = 25/100 = 0,25: домножили на 25.'},
        {т:'0,4', ок:false, почему:'0,4 — это 4/10. А 1/4 = 25/100 = 0,25.'}]},
    { вопрос:'Переведи 3/5 в десятичную дробь.', варианты:[
        {т:'0,6', ок:true,  почему:'3/5 = 6/10 = 0,6: домножили на 2.'},
        {т:'0,35', ок:false, почему:'0,35 — это 35/100. Для 3/5 знаменатель доводим до 10.'}]},
    { вопрос:'Переведи 9/20 в десятичную дробь.', варианты:[
        {т:'0,45', ок:true,  почему:'9/20 = 45/100 = 0,45: домножили на 5.'},
        {т:'0,9', ок:false, почему:'0,9 — это 9/10. Двадцать нужно довести до 100.'}]},
    { вопрос:'Запиши 0,4 обыкновенной дробью в простом виде.', варианты:[
        {т:'2/5', ок:true,  почему:'0,4 = 4/10 = 2/5: сократили на 2.'},
        {т:'1/4', ок:false, почему:'1/4 — это 0,25. У 0,4 знаменатель 10.'}]},
    { вопрос:'Что больше: 1/2 или 0,45?', варианты:[
        {т:'1/2: это 0,50, а 0,50 > 0,45', ок:true,  почему:'Привели к одной записи и сравнили.'},
        {т:'0,45: цифр после запятой больше', ок:false, почему:'Число цифр после запятой ничего не решает: 0,50 > 0,45.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Один приём: довести знаменатель до 10, 100 или 1000 и отсчитать столько же знаков после запятой.') +
        `<div class="ask">${BTN(4,'','пройти заново','r995Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r995Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Порядок один: смотрим знаменатель → доводим до 10, 100 или 1000 → <b>отсчитываем знаки</b>.');
  }

  /* 12. Тренажёр */
  const ТРЕНАЖЁР=[
    { ф:'Переведи 1/2 в десятичную дробь.', о:['0,5','0,2','0,12'], в:0,
      р:'1/2 = 5/10 = 0,5.' },
    { ф:'Переведи 3/4 в десятичную дробь.', о:['0,75','0,34','0,43'], в:0,
      р:'3/4 = 75/100 = 0,75.' },
    { ф:'Запиши 0,6 обыкновенной дробью.', о:['3/5','6/5','1/6'], в:0,
      р:'0,6 = 6/10 = 3/5.' },
    { ф:'Какая дробь переводится точно?', о:['1/8','1/3','1/7'], в:0,
      р:'8 = 2 · 2 · 2, значит 1/8 = 125/1000 = 0,125. У 3 и 7 есть другие множители.' }
  ];
  function F12(s){
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length, з=ТРЕНАЖЁР[и], отв=s.тОтвет, готово=отв!=null, верно=отв===з.в;
    return ТОЧКИ(ТРЕНАЖЁР.length, и, и) +
      A(2,'cap','Уровень '+(и+1)+' из '+ТРЕНАЖЁР.length) +
      ВОПРОС(з.ф) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(3+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r995Train(${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${s.тВерно||0} · ошибок: ${s.тОшибки||0} · всего: ${ТРЕНАЖЁР.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий уровень','r995NextQ()')}</div>` : '');
  }

  const L995={
    id: ID, title:'Перевод обыкновенной дроби в десятичную', ico:'🧵',
    src:'Математика · 5 класс · Обыкновенные и десятичные дроби', subj:'math',
    explain: [
      'В мастерской тканей портной отрезает 3/4 метра, а на кассе нужна десятичная запись: 0,75 метра. Это одна и та же длина, записанная по-разному. Обыкновенная дробь показывает части, десятичная — десятые, сотые и тысячные доли. Чтобы перейти от одной записи к другой, нужно привести знаменатель к 10, 100 или 1000 — и тогда запись получится сама.',

      'Разберём, что значит цифра после запятой. Метр делится на десять равных частей — дециметров. Одна такая часть и есть одна десятая метра: 0,1 = 1/10. Первая цифра после запятой — десятые, вторая — сотые (0,01 = 1/100), третья — тысячные (0,001 = 1/1000). Частая ошибка — считать первую цифру сотыми. Как проверить себя: назови разряд каждой цифры вслух: 0,1 — нуль целых одна десятая.',

      'Главный приём — основное свойство дроби: если числитель и знаменатель умножить на одно и то же число, значение дроби не изменится. Возьмём 3/4. Чтобы знаменатель стал 100, умножим его на 25; на столько же умножим числитель: 3/4 = 75/100. Теперь запись очевидна: 75 сотых — это 0,75. Частая ошибка — умножать только знаменатель: тогда дробь меняет значение. Как проверить себя: оба числа должны быть умножены на одно и то же.',

      'Есть короткое правило: сколько нулей в знаменателе, столько цифр после запятой. Если знаменатель 10 — один знак (3/10 = 0,3), если 100 — два (75/100 = 0,75), если 1000 — три (625/1000 = 0,625). Запятая отделяет столько цифр справа, сколько нулей у знаменателя. Частая ошибка — потерять нуль: написать 0,7 вместо 0,70. Как проверить себя: посчитай нули знаменателя и знаки после запятой.',

      'Потренируемся на трёх примерах. 1/2 = 5/10 = 0,5. 3/5 = 6/10 = 0,6. 7/20: двадцать доводим до ста, умножая на 5, значит 7 · 5 = 35 — получается 35/100 = 0,35. Частая ошибка — выбирать неправильный множитель и получать знаменатель 1000 там, где хватает 100. Как проверить себя: подбери самый простой множитель из 2, 5, 10, 25.',

      'Работает и обратный перевод: от запятой к обыкновенной дроби. 0,25 — две цифры после запятой, значит знаменатель 100: 0,25 = 25/100. Дальше сокращаем: 25/100 = 1/4. Частая ошибка — забыть сократить и оставить 25/100. Как проверить себя: раздели числитель на знаменатель — должно получиться исходное число.',

      'Не всякая дробь переводится точно. Если знаменатель состоит только из двоек и пятёрок, знаменатель доводится до 10, 100 или 1000, и запись конечна: 1/8 = 125/1000 = 0,125. Годятся знаменатели 2, 4, 5, 8, 10, 16, 20, 25, 50. Если в знаменателе есть другие множители — например 3, 6, 7, 9 — точной конечной записи не будет. Частая ошибка — считать, что переводится любая дробь. Как проверить себя: разложи знаменатель на простые множители.',

      'Посмотрим на ловушку. 1/3: делим единицу на три, получаем 0,3333… — тройки не кончаются. Проверка: 0,333… · 3 = 0,999…, а не единица. Значит, точного равенства нет, и пишут приближённо: 1/3 ≈ 0,33. Частая ошибка — писать 1/3 = 0,3: тогда 0,3 · 3 = 0,9, что меньше единицы. Как проверить себя: умножь десятичную запись обратно на знаменатель.',

      'Перевод нужен и для сравнения. Что больше: 3/4 или 0,7? Приводим к одной записи: 3/4 = 0,75, а 0,7 = 0,70 — значит 3/4 больше. Сравнивать обыкновенную дробь с десятичной «на глаз» нельзя. Частая ошибка — смотреть на количество цифр после запятой и считать, что 0,45 больше 0,5. Как проверить себя: приведи оба числа к одной записи и сравнивай разряды слева направо.',

      'Научимся читать запись. 0,75 читается «нуль целых семьдесят пять сотых»: семёрка стоит в разряде десятых, пятёрка — в разряде сотых. Полезно проговаривать вслух: так сразу слышно, сколько знаков и какой разряд. Частая ошибка — читать «нуль целых семь, пять» и путать разряды. Как проверить себя: назови разряд каждой цифры по очереди.',

      'Пройти практику: пять уровней — прямые переводы, обратный перевод и сравнение. Перед ответом определяй знаменатель. Дальше — тренажёр со счётом.',

      'Соберём правило. Перевод в десятичную: доводим знаменатель до 10, 100 или 1000, умножая и числитель, и знаменатель на одно число, затем отсчитываем знаков после запятой столько же, сколько нулей в знаменателе. Обратный перевод: цифры после запятой становятся числителем, а знаменатель — 10, 100 или 1000, потом сокращаем. Точно переводятся дроби со знаменателем из двоек и пятёрок; остальные дают приближённую запись.'
    ],
    check: {
      q:'Переведи 3/4 в десятичную дробь.',
      choices:['0,75','0,34','0,43'],
      ans:0,
      exp:'3/4 = 75/100 = 0,75: числитель и знаменатель умножили на 25.'
    },
    tasks: [
      { q:'Переведи 1/4 в десятичную дробь. Запиши ответ.', kind:'choice',
        choices:['0,25','0,4','0,14'], ans:0,
        hints:['Доведи знаменатель до 100.','4 · 25 = 100, значит и числитель умножаем на 25.'],
        sol:'1/4 = 25/100 = 0,25.' },
      { q:'Переведи 7/20 в десятичную дробь.', kind:'choice',
        choices:['0,35','0,7','0,27'], ans:0,
        hints:['20 · 5 = 100.','Умножь и числитель: 7 · 5 = 35.'],
        sol:'7/20 = 35/100 = 0,35.' },
      { q:'Запиши 0,6 обыкновенной дробью в простом виде.', kind:'choice',
        choices:['3/5','6/5','1/6'], ans:0,
        hints:['Одна цифра после запятой — знаменатель 10.','6/10 сокращаем на 2.'],
        sol:'0,6 = 6/10 = 3/5.' },
      { q:'Какая дробь переводится в десятичную точно?', kind:'choice',
        choices:['1/8','1/3','1/7'], ans:0,
        hints:['Разложи знаменатель на множители.','8 = 2 · 2 · 2.'],
        sol:'1/8 = 125/1000 = 0,125 — точно; 1/3 и 1/7 дают бесконечную запись.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L995.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else сцена=F12(s);

    const ЗАГОЛОВКИ={
      1:['Мастерская','3/4 метра и касса'],
      2:['Десятые','Метр на десять частей'],
      3:['Приём','Домножаем до сотых'],
      4:['Нули','Сколько нулей — столько знаков'],
      5:['Примеры','Три перевода'],
      6:['Обратно','От запятой к дроби'],
      7:['Какие годятся','Двойки и пятёрки'],
      8:['Ловушка','Треть не переводится точно'],
      9:['Сравнение','Приводим к одной записи'],
      10:['Чтение','Разряды после запятой'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр','Проверь себя']
    };
    const з=ЗАГОЛОВКИ[f]||['Дроби','Обыкновенная и десятичная'];
    el.innerHTML = `<div class="s6 l995" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r995Tape=(к)=>{ S().лента=к; chRender(0); };
  window.r995Tenth=(к)=>{ S().десятая=к; chRender(0); };
  window.r995How=(к)=>{ S().приём=к; chRender(0); };
  window.r995Zeros=(к)=>{ S().нули=к; chRender(0); };
  window.r995Ex=(к)=>{ S().примеры=к; chRender(0); };
  window.r995Back=(к)=>{ S().обратно=к; chRender(0); };
  window.r995Which=(к)=>{ S().годятся=к; chRender(0); };
  window.r995Trap=(к)=>{ S().ловушка=к; chRender(0); };
  window.r995Cmp=(к)=>{ S().сравнение=к; chRender(0); };
  window.r995Read=(к)=>{ S().чтение=к; chRender(0); };
  window.r995Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r995Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r995Train=(к)=>{
    const s=S();
    if(s.тОтвет!=null) return;
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length;
    s.тОтвет=к;
    if(к===ТРЕНАЖЁР[и].в) s.тВерно=(s.тВерно||0)+1; else s.тОшибки=(s.тОшибки||0)+1;
    chRender(0);
  };
  window.r995NextQ=()=>{ const s=S(); s.тНомер=((s.тНомер||0)+1)%ТРЕНАЖЁР.length; s.тОтвет=null; chRender(0); };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L995; else arr.push(L995);
  })();
  window.MA995={render:render, L:L995};
})();
