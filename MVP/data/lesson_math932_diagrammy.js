/* ============ МАТЕМАТИКА · УРОК 932 · «СТОЛБЧАТЫЕ И КРУГОВЫЕ ДИАГРАММЫ» ============
   5 класс. Сюжет: школьный опрос «что любят на перемене» — от сбора данных
   до двух диаграмм.

   ЭТАЛОН (уроки 995 и 830): движение в кадре — это САМА МАТЕМАТИКА.
     кадр 1  палочки-счёт ПРОСТАВЛЯЮТСЯ по одной: идёт сбор данных;
     кадр 2  клетки таблицы ЗАПОЛНЯЮТСЯ по очереди;
     кадр 3  числа превращаются в СТОЛБИКИ: столбцы растут от нуля;
     кадр 4  столбчатая диаграмма: оси, сетка, четыре столбца растут
             до своих значений, над каждым появляется число;
     кадр 5  цена деления по вертикальной оси: столбец доходит ровно
             до нужного деления;
     кадр 6  луч-указатель ПРОХОДИТ по столбцам и останавливается
             на самом высоком;
     кадр 7  круг = 360°: радиус ОБХОДИТ круг, оставляя градусные метки;
     кадр 8  транспортир: угол сектора отсчитывается по дуге;
     кадр 9  круговая диаграмма СОБИРАЕТСЯ по кусочкам по 10°;
     кадр 10 две диаграммы строятся рядом из одних данных;
     кадры 11–12 практика и тренажёр.

   БОЛЬШЕ АНИМАЦИЙ, ЧЕМ В ПРЕДЫДУЩИХ УРОКАХ: круговая диаграмма собирается
   из 36 кусочков по 10°, поэтому движение показывает сами градусы.

   КАЧЕСТВО ПРОРИСОВКИ: градиенты у столбцов и секторов, мягкие тени,
   скруглённые углы, оси со стрелками и штрихами, сетка, легенда с
   образцами цвета, транспортир с градусной шкалой и подписями через 30°.

   СВЕРКА С ПЕТЕРСОНОМ (5 класс): «столбчатые и круговые диаграммы»,
   «шкалы», «доли и дроби». Источники: план-1-9/петерсон_1-9.md,
   план-1-9/углублённая_5-9.md.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, движение вдоль осей, ничего за край кадра,
   prefers-reduced-motion выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 932;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ЛИЛ='#c9a6f2', ОРА='#f2b06a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';

  const CSS=`
  #lvis .s6.l932{gap:14px}
  #lvis .s6.l932 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l932 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l932 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l932 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l932 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l932 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l932 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l932 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l932 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l932 .правило b{color:${GOLD}}
  #lvis .s6.l932 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l932 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l932 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l932 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l932 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l932 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l932 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l932 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l932 .уровни .точка.сейчас{background:${GOLD};animation:l932dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l932dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l932 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l932{-webkit-text-size-adjust:100%}
  #lvis .s6.l932 [data-anim]{animation:l932rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l932rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l932 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l932 [data-anim]{animation:none!important}
    #lvis .s6.l932 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l932 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l932-style');
      if(!s){ s=document.createElement('style'); s.id='l932-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анП = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const светит = (длит,нач) => ан('opacity','1;0.5;1',длит, нач!=null?'begin="'+нач+'s"':'');

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c932-стена" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22362c"/><stop offset="1" stop-color="#152219"/>
      </linearGradient>
      <linearGradient id="c932-поле" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#f7f3e6"/><stop offset="1" stop-color="#ded4bb"/>
      </linearGradient>
      <linearGradient id="c932-полоса1" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#ffe08f"/><stop offset="0.55" stop-color="#ffd76a"/><stop offset="1" stop-color="#c9971f"/>
      </linearGradient>
      <linearGradient id="c932-полоса2" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#b6e8cd"/><stop offset="0.55" stop-color="#8fd1a8"/><stop offset="1" stop-color="#4f9b76"/>
      </linearGradient>
      <linearGradient id="c932-полоса3" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#b9e6ff"/><stop offset="0.55" stop-color="#7fd1ff"/><stop offset="1" stop-color="#3f86b8"/>
      </linearGradient>
      <linearGradient id="c932-полоса4" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#e6d2ff"/><stop offset="0.55" stop-color="#c9a6f2"/><stop offset="1" stop-color="#8a5fc0"/>
      </linearGradient>
      <radialGradient id="c932-блик" cx="0.35" cy="0.3" r="0.7">
        <stop offset="0" stop-color="#ffffff" stop-opacity="0.55"/>
        <stop offset="1" stop-color="#ffffff" stop-opacity="0"/>
      </radialGradient>
      <filter id="c932-тень" x="-30%" y="-30%" width="170%" height="170%">
        <feGaussianBlur stdDeviation="2.4"/>
      </filter>
      <filter id="c932-мягко" x="-20%" y="-20%" width="140%" height="140%">
        <feGaussianBlur stdDeviation="1.1"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||200}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const ЦВЕТА=['url(#c932-полоса1)','url(#c932-полоса2)','url(#c932-полоса3)','url(#c932-полоса4)'];

  /* СТОЛБЕЦ: тело с градиентом, блик, тень, рост от нуля и подпись значения */
  function столбец(x,основание,ш,высота,цвет,нач,подпись){
    const верх=основание-высота;
    return `<g>
      <rect x="${x+3}" y="${верх+4}" width="${ш}" height="${высота}" rx="5" fill="rgba(0,0,0,.35)" filter="url(#c932-тень)"/>
      <rect x="${x}" y="${основание}" width="${ш}" height="0" rx="5" fill="${цвет}" stroke="rgba(20,35,28,.55)" stroke-width="1.1">
        ${ан('y',основание+';'+основание+';'+верх+';'+верх,'7s','keyTimes="0;0.12;0.62;1" begin="'+нач+'s"')}
        ${ан('height','0;0;'+высота+';'+высота,'7s','keyTimes="0;0.12;0.62;1" begin="'+нач+'s"')}
      </rect>
      <rect x="${x}" y="${основание}" width="${ш}" height="0" rx="5" fill="url(#c932-блик)" opacity=".5">
        ${ан('y',основание+';'+основание+';'+верх+';'+верх,'7s','keyTimes="0;0.12;0.62;1" begin="'+нач+'s"')}
        ${ан('height','0;0;'+высота+';'+высота,'7s','keyTimes="0;0.12;0.62;1" begin="'+нач+'s"')}
      </rect>
      ${подпись?`<g>${ан('opacity','0.3;0.3;1;1','7s','keyTimes="0;'+Math.min(0.9,0.6+нач*0.12).toFixed(2)+';'+Math.min(0.97,0.68+нач*0.12).toFixed(2)+';1"')}
        ${т(x+ш/2,верх-12,подпись,14,GOLD,true)}</g>`:''}
    </g>`;
  }

  /* ОСИ с сеткой, штрихами и подписями */
  function оси(x0,y0,ш,в,дел,макс,подписи){
    let s=`<line x1="${x0}" y1="${y0}" x2="${x0}" y2="${y0-в-12}" stroke="${ИНК}" stroke-width="2.2"/>
      <path d="M${x0} ${y0-в-20} L${x0-6} ${y0-в-9} L${x0+6} ${y0-в-9} Z" fill="${ИНК}"/>
      <line x1="${x0}" y1="${y0}" x2="${x0+ш+12}" y2="${y0}" stroke="${ИНК}" stroke-width="2.2"/>
      <path d="M${x0+ш+20} ${y0} L${x0+ш+9} ${y0-6} L${x0+ш+9} ${y0+6} Z" fill="${ИНК}"/>`;
    for(let i=0;i<=дел;i++){
      const y=y0-i*(в/дел);
      s+=`<line x1="${x0-5}" y1="${y.toFixed(1)}" x2="${x0}" y2="${y.toFixed(1)}" stroke="${ЛИНИЯ}" stroke-width="1.4"/>`;
      s+=`<line x1="${x0}" y1="${y.toFixed(1)}" x2="${x0+ш}" y2="${y.toFixed(1)}" stroke="${ЛИНИЯ}" stroke-width=".7" opacity=".3"/>`;
      if(подписи) s+=т(x0-12,y.toFixed(1)+4,String(Math.round(i*макс/дел)),12,МУТ,false,'end');
    }
    return s;
  }

  /* КУСОЧЕК КРУГА по 10°: из них собирается круговая диаграмма */
  function кусочек(цx,цy,r,а1,а2,цвет,нач,вынос){
    const рад=a=>a*Math.PI/180;
    const x1=цx+r*Math.cos(рад(а1)), y1=цy+r*Math.sin(рад(а1));
    const x2=цx+r*Math.cos(рад(а2)), y2=цy+r*Math.sin(рад(а2));
    return `<path d="M${цx} ${цy} L${x1.toFixed(1)} ${y1.toFixed(1)} A${r} ${r} 0 0 1 ${x2.toFixed(1)} ${y2.toFixed(1)} Z"
      fill="${цвет}" stroke="#16241d" stroke-width=".8" opacity="0.92">
      ${светит('9s',нач)}${вынос||''}</path>`;
  }

  /* КРУГОВАЯ ДИАГРАММА: доли в градусах, каждая доля набирается кусочками по 10° */
  function круговая(цx,цy,r,доли,подписи){
    let s=`<circle cx="${цx}" cy="${цy}" r="${r}" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>`;
    let а=0, н=0;
    доли.forEach(([имя,градусы,цвет],д)=>{
      const конец=а+градусы;
      for(let к=а; к<конец; к+=10){
        s+=кусочек(цx,цy,r,к-90,Math.min(к+10,конец)-90,цвет,(н++)*0.07);
      }
      if(подписи){
        const сер=(а+конец)/2-90;
        const рад=сер*Math.PI/180;
        s+=`<g>${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;'+Math.min(0.86,0.3+н*0.014).toFixed(2)+';'+Math.min(0.94,0.38+н*0.014).toFixed(2)+';1"')}
          ${т((цx+Math.cos(рад)*r*0.62).toFixed(1),(цy+Math.sin(рад)*r*0.62+4).toFixed(1),имя,12,'#16241d',true)}</g>`;
      }
      а=конец;
    });
    return s;
  }

  /* ЛЕГЕНДА */
  const легенда = (x,y,элементы) => элементы.map(([имя,цвет],i)=>`
    <g>
      <rect x="${x}" y="${y+i*20}" width="14" height="14" rx="3" fill="${цвет}" stroke="rgba(20,35,28,.5)" stroke-width="1"/>
      ${т(x+22,y+i*20+12,имя,12,ИНК,false,'start')}
    </g>`).join('');

  /* ДАННЫЕ ОПРОСА */
  const ОПРОС=[['футбол',6],['рисование',3],['чтение',2],['музыка',1]];
  const ВСЕГО=12;
  const ДОЛИ=ОПРОС.map(([имя,ч],i)=>[имя,30*ч,ЦВЕТА[i]]);   /* 12 голосов → 30° на голос */

  /* ================= КАДРЫ ================= */

  /* 1. Сбор данных: палочки-счёт по одной */
  function F1(s){
    const в = s.сбор;
    return ВОПРОС('Ребята голосуют за любимое занятие. Сколько всего голосов собрано?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="200" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Собираем голоса палочками',14,GOLD,true)}
        ${Array.from({length:12},(_,i)=>{
          const группа=Math.floor(i/5), поз=i%5;
          const x=44+группа*76+поз*12, y=118;
          return `<g>${ан('opacity','1;0.35;1','9s','begin="'+(i*0.55).toFixed(2)+'s"')}
            <rect x="${x-2}" y="${y-42}" width="4.6" height="42" rx="2.2" fill="${ИНК}"/></g>`;
        }).join('')}
        ${Array.from({length:12},(_,i)=>`<g>${светит('9s',0.5+i*0.55)}
          ${т(30+i*0,0,'',12,МУТ)}</g>`).join('')}
        <g>${ан('opacity','1;1;1;1','9s')}
          <rect x="228" y="52" width="88" height="56" rx="12" fill="rgba(255,255,255,.05)" stroke="${GOLD}" stroke-width="1.6"/>
          ${т(272,72,'всего',12,МУТ)}
          ${т(272,98,'12',24,GOLD,true)}
        </g>
        ${т(122,150,'пять палочек — одна группа',12,МУТ)}
        ${т(168,182,'каждая палочка — один голос',12,ИНК)}
      `,200)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='двенадцать'?'hit':(в?'miss':''), '12 голосов: считаем палочки', "r932Сбор('двенадцать')")}
        ${BTN(4, в==='пять'?'miss':'', '5 голосов: столько в группе', "r932Сбор('пять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='двенадцать',
        в==='двенадцать' ? 'Верно: палочек двенадцать, значит и голосов двенадцать. Группы по пять помогают считать быстрее.'
                         : 'Нет: пять — это размер группы. Палочек двенадцать, значит голосов 12.') : ЖДЁТ()) +
      ПРАВИЛО('Сбор данных начинается со <b>счёта</b>: каждая палочка — один голос.');
  }

  /* 2. Таблица заполняется по очереди */
  function F2(s){
    const в = s.таблица;
    return ВОПРОС('В таблице четыре строки. Какое занятие собрало больше всего голосов?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="222" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="222" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Данные записываем в таблицу',14,GOLD,true)}
        <rect x="34" y="40" width="268" height="150" rx="12" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        <rect x="34" y="40" width="268" height="32" rx="12" fill="rgba(255,215,106,.16)" stroke="${ЛИНИЯ}" stroke-width="1.2"/>
        ${т(90,61,'занятие',12,GOLD,true)}${т(250,61,'голосов',12,GOLD,true)}
        ${ОПРОС.map(([имя,ч],i)=>{
          const y=94+i*28;
          return `<g>
            <line x1="34" y1="${y-19}" x2="302" y2="${y-19}" stroke="${ЛИНИЯ}" stroke-width=".8" opacity=".5"/>
            <rect x="44" y="${y-15}" width="14" height="14" rx="3" fill="${ЦВЕТА[i]}" stroke="rgba(20,35,28,.5)" stroke-width="1"/>
            ${т(66,y-3,имя,14,ИНК,false,'start')}
            <g>${ан('opacity','0.3;0.3;1;1','9s','keyTimes="0;'+(0.1+i*0.16).toFixed(2)+';'+(0.24+i*0.16).toFixed(2)+';1"')}
              ${т(250,y-3,String(ч),16,GOLD,true)}</g>
          </g>`;
        }).join('')}
        ${т(168,210,'итого 12 голосов',12,МУТ)}
      `,222)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='футбол'?'hit':(в?'miss':''), 'Футбол: 6 голосов — больше всех', "r932Tabl('футбол')")}
        ${BTN(4, в==='музыка'?'miss':'', 'Музыка: последняя строка', "r932Tabl('музыка')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='футбол',
        в==='футбол' ? 'Верно: в таблице видно — футбол 6, рисование 3, чтение 2, музыка 1. Больше всех у футбола.'
                    : 'Нет: последняя строка не самая большая. Сравни числа: 6 больше всех у футбола.') : ЖДЁТ()) +
      ПРАВИЛО('В таблице данные <b>сравнивают по числам</b>: 6 > 3 > 2 > 1.');
  }

  /* 3. Числа превращаются в столбики */
  function F3(s){
    const в = s.зачем;
    return ВОПРОС('Зачем числа из таблицы перерисовывают в диаграмму?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="200" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,24,'Числа и столбики',14,GOLD,true)}
        ${ОПРОС.map(([имя,ч],i)=>`<g>
          <rect x="20" y="${46+i*32}" width="112" height="26" rx="7" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.2"/>
          ${т(46,64+i*32,имя,12,МУТ,false,'start')}
          ${т(112,64+i*32,String(ч),16,GOLD,true)}
        </g>`).join('')}
        ${т(76,184,'числа',12,МУТ)}
        ${т(188,86,'→',20,МУТ)}
        ${столбец(150,166,32,76,ЦВЕТА[0],0.3,'6')}
        ${столбец(192,166,32,38,ЦВЕТА[1],0.6,'3')}
        ${столбец(234,166,32,25,ЦВЕТА[2],0.9,'2')}
        ${столбец(276,166,32,13,ЦВЕТА[3],1.2,'1')}
        ${т(230,184,'диаграмма',12,МУТ)}
      `,200)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='наглядно'?'hit':(в?'miss':''), 'Диаграмма показывает разницу на глаз', "r932Why('наглядно')")}
        ${BTN(4, в==='точнее'?'miss':'', 'Диаграмма считает точнее таблицы', "r932Why('точнее')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='наглядно',
        в==='наглядно' ? 'Верно: числа одни и те же, но столбики видно сразу — какой выше, тот и больше.'
                      : 'Нет: точность даёт таблица. Диаграмма нужна, чтобы сравнивать на глаз.') : ЖДЁТ()) +
      ПРАВИЛО('Диаграмма не считает точнее — она показывает <b>разницу наглядно</b>.');
  }

  /* 4. Столбчатая диаграмма: четыре столбца растут */
  function F4(s){
    const в = s.столбчатая;
    const x0=46, y0=158, в0=104;   /* 6 голосов = 104 px */
    const ш=34, шаг=46;
    return ВОПРОС('Столбец «футбол» поднялся до отметки 6. Что это значит?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="200" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Столбчатая диаграмма',14,GOLD,true)}
        ${оси(x0,y0,240,в0,6,6,true)}
        ${ОПРОС.map(([имя,ч],i)=>столбец(x0+10+i*52,y0,36,в0*ч/6,ЦВЕТА[i],i*0.3,String(ч))).join('')}
        ${['футбол','рисунок','чтение','музыка'].map((имя,i)=>т(x0+28+i*52,y0+20,имя,12,МУТ)).join('')}
      `,200)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), 'За футбол отдали 6 голосов', "r932Bar('шесть')")}
        ${BTN(4, в==='высокий'?'miss':'', 'Столбец просто самый высокий — числа не важны', "r932Bar('высокий')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: высота столбца равна числу голосов — шесть. Высота здесь и есть число.'
                    : 'Нет: высота столбца не «просто высота»: она равна числу. У футбола это 6 голосов.') : ЖДЁТ()) +
      ПРАВИЛО('В столбчатой диаграмме <b>высота столбца равна числу</b>.');
  }

  /* 5. Масштаб по оси */
  function F5(s){
    const в = s.масштаб;
    const x0=50, y0=158, в0=104;
    return ВОПРОС('Одно деление по вертикальной оси — один голос. До какого деления дойдёт столбец с числом 4?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="200" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Масштаб: одно деление — один голос',14,GOLD,true)}
        ${оси(x0,y0,236,в0,6,6,true)}
        ${столбец(x0+40,y0,44,в0*4/6,ЦВЕТА[2],0.3,'4')}
        <g>${светит('7s',1.4)}
          <line x1="${x0-6}" y1="${(y0-в0*4/6).toFixed(1)}" x2="${x0+110}" y2="${(y0-в0*4/6).toFixed(1)}"
            stroke="${GOLD}" stroke-width="1.6" stroke-dasharray="5 4"/></g>
        ${т(168,190,'цена деления по оси — 1 голос',12,МУТ)}
      `,200)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='четыре'?'hit':(в?'miss':''), 'До четвёртого деления: 4 голоса — 4 деления', "r932Scale('четыре')")}
        ${BTN(4, в==='шесть'?'miss':'', 'До верхнего деления: так выше', "r932Scale('шесть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='четыре',
        в==='четыре' ? 'Верно: цена деления 1, значит четырём голосам соответствуют четыре деления.'
                    : 'Нет: высота столбца равна числу, а не «как можно выше». Четыре голоса — до деления 4.') : ЖДЁТ()) +
      ПРАВИЛО('Высоту столбца отмеряют по <b>цене деления оси</b>: 4 голоса — 4 деления.');
  }

  /* 6. Чтение: указатель идёт по столбцам */
  function F6(s){
    const в = s.чтение;
    const x0=46, y0=158, в0=104, ш=34, шаг=46;
    return ВОПРОС('По диаграмме видно, какое занятие на первом месте. Какое?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="200" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Смотрим, где столбец выше',14,GOLD,true)}
        ${оси(x0,y0,240,в0,6,6,true)}
        ${ОПРОС.map(([имя,ч],i)=>столбец(x0+10+i*52,y0,36,в0*ч/6,ЦВЕТА[i],i*0.28,String(ч))).join('')}
        ${['футбол','рисунок','чтение','музыка'].map((имя,i)=>т(x0+28+i*52,y0+18,имя,12,МУТ)).join('')}
        <g>${анТ('0 0;0 0;'+(3*шаг)+' 0;'+(3*шаг)+' 0;0 0','8s','keyTimes="0;0.08;0.5;0.8;1"')}
          <rect x="${x0+10}" y="${y0-в0-8}" width="${ш+8}" height="${в0+16}" rx="8" fill="none" stroke="${GOLD}" stroke-width="2.2"/>
        </g>
        ${т(168,196,'у футбола 6 голосов — самый высокий столбец',12,ИНК)}
      `,200)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='футбол'?'hit':(в?'miss':''), 'Футбол: его столбец самый высокий', "r932Read('футбол')")}
        ${BTN(4, в==='музыка'?'miss':'', 'Музыка: её столбец самый узкий', "r932Read('музыка')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='футбол',
        в==='футбол' ? 'Верно: самый высокий столбец — у футбола, значит там и больше всего голосов.'
                    : 'Нет: ширина столбцов одинаковая. Сравниваем высоту: выше всех футбол.') : ЖДЁТ()) +
      ПРАВИЛО('Читают столбчатую диаграмму <b>по высоте столбцов</b>: выше — больше.');
  }

  /* 7. Круг = 360°, радиус обходит круг */
  function F7(s){
    const в = s.круг;
    const цx=110, цy=104, r=58;
    return ВОПРОС('На сколько градусов делится весь круг диаграммы?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="200" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Весь круг — 360 градусов',14,GOLD,true)}
        <circle cx="${цx}" cy="${цy}" r="${r}" fill="rgba(255,255,255,.04)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${Array.from({length:36},(_,i)=>{
          const а=(i*10-90)*Math.PI/180;
          const x1=цx+Math.cos(а)*(r-6), y1=цy+Math.sin(а)*(r-6);
          const x2=цx+Math.cos(а)*r, y2=цy+Math.sin(а)*r;
          return `<line x1="${x1.toFixed(1)}" y1="${y1.toFixed(1)}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}"
            stroke="${ЛИНИЯ}" stroke-width="${i%3===0?1.8:1}" opacity=".8">
            ${ан('opacity','1;0.4;1','9s','begin="'+(i*0.12).toFixed(2)+'s"')}</line>`;
        }).join('')}
        <g>${анП('0 '+цx+' '+цy+';0 '+цx+' '+цy+';360 '+цx+' '+цy+';360 '+цx+' '+цy,'9s','keyTimes="0;0.1;0.8;1"')}
          <line x1="${цx}" y1="${цy}" x2="${цx}" y2="${цy-r+8}" stroke="${GOLD}" stroke-width="2.6" stroke-linecap="round"/>
        </g>
        ${т(252,80,'360°',20,GOLD,true)}
        ${т(252,106,'полный круг',12,МУТ)}
        ${т(252,132,'12 голосов —',12,ИНК)}
        ${т(252,152,'30° на голос',12,GOLD)}
      `,200)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='триста шестьдесят'?'hit':(в?'miss':''), '360°: круг делится на 360 градусов', "r932Circ('триста шестьдесят')")}
        ${BTN(4, в==='сто'?'miss':'', '100°: как проценты', "r932Circ('сто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='триста шестьдесят',
        в==='триста шестьдесят' ? 'Верно: полный круг — 360°. Если 12 голосов, на каждый приходится 360 : 12 = 30°.'
                                 : 'Нет: 100 — это проценты, а не градусы. Круг делится на 360°.') : ЖДЁТ()) +
      ПРАВИЛО('Круговую диаграмму строят на <b>360°</b>: доля · 360 = угол сектора.');
  }

  /* 8. Транспортир: угол сектора */
  function F8(s){
    const в = s.транспортир;
    const цx=110, цy=150, r=88;
    return ВОПРОС('Половина круга занята футболом. Какой угол у этого сектора?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="200" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Угол сектора считаем по транспортиру',14,GOLD,true)}
        <path d="M${цx-r} ${цy} A${r} ${r} 0 0 1 ${цx+r} ${цy} Z" fill="rgba(255,255,255,.05)" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${Array.from({length:19},(_,i)=>{
          const а=(180-i*10)*Math.PI/180;
          const длинный=(i%3===0);
          const x1=цx+Math.cos(а)*(r-10), y1=цy-Math.sin(а)*(r-10);
          const x2=цx+Math.cos(а)*r, y2=цy-Math.sin(а)*r;
          const метка=длинный?`<g>${светит('9s',(i/3)*0.5)}
            ${т((цx+Math.cos(а)*(r-26)).toFixed(1),(цy-Math.sin(а)*(r-26)+4).toFixed(1),String(i*10),12,МУТ)}</g>`:'';
          return `<line x1="${x1.toFixed(1)}" y1="${y1.toFixed(1)}" x2="${x2.toFixed(1)}" y2="${y2.toFixed(1)}"
            stroke="${ЛИНИЯ}" stroke-width="${длинный?1.6:1}"/>${метка}`;
        }).join('')}
        <g>${анП('0 '+цx+' '+цy+';0 '+цx+' '+цy+';180 '+цx+' '+цy+';180 '+цx+' '+цy,'9s','keyTimes="0;0.1;0.7;1"')}
          <line x1="${цx}" y1="${цy}" x2="${цx}" y2="${(цy-r+12).toFixed(1)}" stroke="${GOLD}" stroke-width="2.6" stroke-linecap="round"/>
        </g>
        <circle cx="${цx}" cy="${цy}" r="4" fill="${ИНК}"/>
        ${т(252,86,'половина',16,GOLD,true)}
        ${т(252,112,'полкруга',12,МУТ)}
        ${т(252,140,'1/2 · 360 = 180°',14,GREEN,true)}
      `,200)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='сто восемьдесят'?'hit':(в?'miss':''), '180°: половина круга', "r932Prot('сто восемьдесят')")}
        ${BTN(4, в==='девяносто'?'miss':'', '90°: четверть круга', "r932Prot('девяносто')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сто восемьдесят',
        в==='сто восемьдесят' ? 'Верно: половина круга — это 1/2 · 360 = 180°.'
                              : 'Нет: 90° — это четверть. Половине соответствует 180°.') : ЖДЁТ()) +
      ПРАВИЛО('Угол сектора: <b>доля · 360</b>. Половина — 180°, четверть — 90°, треть — 120°.');
  }

  /* 9. Круговая диаграмма собирается по кусочкам */
  function F9(s){
    const в = s.круговая;
    return ВОПРОС('Круговая диаграмма построена по данным опроса. Какой сектор самый большой?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="200" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Круговая диаграмма из кусочков',14,GOLD,true)}
        ${круговая(104,112,72,ДОЛИ,true)}
        ${легенда(204,52,[['футбол 6',ЦВЕТА[0]],['рисование 3',ЦВЕТА[1]],['чтение 2',ЦВЕТА[2]],['музыка 1',ЦВЕТА[3]]])}
        ${т(168,196,'каждый кусочек — 10°, всего их 36',12,МУТ)}
      `,200)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='футбол'?'hit':(в?'miss':''), 'Футбол: 180° — половина круга', "r932Pie('футбол')")}
        ${BTN(4, в==='музыка'?'miss':'', 'Музыка: она в легенде первая снизу', "r932Pie('музыка')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='футбол',
        в==='футбол' ? 'Верно: у футбола 6 голосов из 12, то есть половина — сектор 180°, он и самый большой.'
                    : 'Нет: смотрим на размер сектора. У музыки 1 голос — 30°, это самый маленький сектор.') : ЖДЁТ()) +
      ПРАВИЛО('В круговой диаграмме <b>размер сектора = доля целого</b>: 6 из 12 — половина круга.');
  }

  /* 10. Две диаграммы рядом */
  function F10(s){
    const в = s.сравнение;
    const x0=40, y0=140, в0=76, ш=20, шаг=28;
    return ВОПРОС('Одни и те же данные показали двумя диаграммами. Что лучше видно на круговой?') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="200" fill="url(#c932-стена)"/>
        <rect x="0" y="0" width="336" height="200" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${т(168,22,'Столбчатая и круговая',14,GOLD,true)}
        ${оси(x0,y0,110,в0,4,6,true)}
        ${ОПРОС.map(([имя,ч],i)=>столбец(x0+8+i*шаг,y0,ш,в0*ч/6,ЦВЕТА[i],i*0.2,String(ч))).join('')}
        ${т(95,166,'сравниваем числа',12,МУТ)}
        ${круговая(246,106,58,ДОЛИ,false)}
        ${т(246,180,'видим доли целого',12,МУТ)}
      `,200)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='доли'?'hit':(в?'miss':''), 'Какую часть целого занимает каждое занятие', "r932Both('доли')")}
        ${BTN(4, в==='числа'?'miss':'', 'Точные числа голосов', "r932Both('числа')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='доли',
        в==='доли' ? 'Верно: круговая показывает доли целого — половина, четверть, шестая часть. Точные числа лучше видны в таблице.'
                   : 'Нет: точные числа читают в таблице. Круговая показывает доли целого.') : ЖДЁТ()) +
      ПРАВИЛО('Столбчатая — <b>сравнить числа</b>; круговая — <b>показать доли целого</b>.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Столбец поднялся до отметки 5 при цене деления 1. Сколько это единиц?', варианты:[
        {т:'5', ок:true,  почему:'Высота столбца равна числу: пять делений — пять единиц.'},
        {т:'1', ок:false, почему:'Одно деление — это цена деления, а не высота столбца.'}]},
    { вопрос:'В круговой диаграмме сектор занимает четверть круга. Сколько это градусов?', варианты:[
        {т:'90°', ок:true,  почему:'1/4 · 360 = 90°.'},
        {т:'120°', ок:false, почему:'120° — это треть круга: 1/3 · 360.'}]},
    { вопрос:'12 голосов разделили поровну между занятиями. По сколько голосов и какой угол?', варианты:[
        {т:'по 30°, если на голос приходится 30°', ок:true,  почему:'360 : 12 = 30° на каждый голос.'},
        {т:'по 12°, если считать по числу голосов', ок:false, почему:'Градусы не равны числу голосов: 360 : 12 = 30°.'}]},
    { вопрос:'Какая диаграмма лучше покажет, что футбол — это половина всех голосов?', варианты:[
        {т:'круговая: видно половину круга', ок:true,  почему:'Доли целого видно на круговой.'},
        {т:'столбчатая: у неё есть ось', ок:false, почему:'Столбчатая сравнивает числа, а долю целого показывает круговая.'}]},
    { вопрос:'Сектор 120°. Какую долю круга он занимает?', варианты:[
        {т:'1/3', ок:true,  почему:'120 : 360 = 1/3.'},
        {т:'1/2', ок:false, почему:'1/2 — это 180°. 120° — это треть.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Две формулы: высота столбца равна числу, а угол сектора равен доле, умноженной на 360.') +
        `<div class="ask">${BTN(4,'','пройти заново','r932Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r932Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Столбчатая: <b>высота = число</b>. Круговая: <b>угол = доля · 360</b>.');
  }

  /* 12. Тренажёр */
  const ТРЕНАЖЁР=[
    { ф:'Столбец доходит до деления 4. Сколько единиц он показывает?', о:['4','3','5'], в:0,
      р:'Высота столбца равна числу: четыре деления — четыре единицы.' },
    { ф:'Сектор занимает половину круга. Сколько это градусов?', о:['180°','90°','360°'], в:0,
      р:'Половина круга: 1/2 · 360 = 180°.' },
    { ф:'Сектор 90°. Какая это доля круга?', о:['1/4','1/3','1/2'], в:0,
      р:'90 : 360 = 1/4 — четверть круга.' },
    { ф:'Что лучше показывает доли целого?', о:['круговая диаграмма','столбчатая диаграмма','таблица'], в:0,
      р:'Доли целого видно на круговой диаграмме.' }
  ];
  function F12(s){
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length, з=ТРЕНАЖЁР[и], отв=s.тОтвет, готово=отв!=null, верно=отв===з.в;
    return ТОЧКИ(ТРЕНАЖЁР.length, и, и) +
      A(2,'cap','Уровень '+(и+1)+' из '+ТРЕНАЖЁР.length) +
      ВОПРОС(з.ф) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(3+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r932Train(${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${s.тВерно||0} · ошибок: ${s.тОшибки||0} · всего: ${ТРЕНАЖЁР.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий уровень','r932NextQ()')}</div>` : '');
  }

  const L932={
    id: ID, title:'Столбчатые и круговые диаграммы', ico:'📊',
    src:'Математика · 5 класс · Диаграммы', subj:'math',
    explain: [
      'В классе решили узнать, чем ребята любят заниматься на перемене. Каждый называл одно занятие, а дежурный ставил палочку. Так собирают данные: каждая палочка — один голос. Когда палочек стало двенадцать, счёт закончили. Дальше данные записывают в таблицу, а потом показывают диаграммой, чтобы разницу было видно сразу.',

      'Данные удобно сначала записать в таблицу: в одной колонке название, в другой — число голосов. Получилось: футбол 6, рисование 3, чтение 2, музыка 1. В таблице числа сравнивают по величине: шесть больше трёх. Таблица точна, но по ней не видно с одного взгляда, насколько одно больше другого. Частая ошибка — считать последнюю строку самой большой. Как проверить себя: сравни числа, а не порядок строк.',

      'Диаграмма нужна, чтобы сравнивать наглядно: числа превращают в столбики или в секторы. Точность при этом не увеличивается — она остаётся в таблице. Диаграмма отвечает на вопрос «что больше и насколько заметно». Частая ошибка — думать, что диаграмма считает точнее таблицы. Как проверить себя: спроси, что именно нужно — точное число или сравнение.',

      'Столбчатая диаграмма строится на осях. По вертикальной оси откладывают значения, по горизонтальной — названия. Высота каждого столбца равна числу: у футбола 6, значит столбец в шесть раз выше столбца с единицей. Столбцы рисуют одной ширины, чтобы сравнивать было честно. Частая ошибка — делать столбцы разной ширины. Как проверить себя: ширина одинаковая, сравниваем только высоту.',

      'У вертикальной оси есть цена деления — как у любой шкалы. Если одно деление равно одному голосу, то четырём голосам соответствует четвёртое деление, а шести — шестое. Ось подписывают, чтобы было понятно, что отложено. Частая ошибка — рисовать столбец «на глазок», не сверяясь с делениями. Как проверить себя: проведи от вершины столбца линию к оси и прочитай деление.',

      'Читают столбчатую диаграмму по высоте: самый высокий столбец — самое большое число, самый низкий — самое маленькое. Ширина при этом ничего не значит, она у всех одинаковая. Частая ошибка — сравнивать столбцы по ширине или по цвету. Как проверить себя: посмотри, у какого столбца вершина выше всех.',

      'Круговая диаграмма устроена иначе: целое — это круг, а каждая часть — сектор. Полный круг содержит 360 градусов, и это число — основа всех расчётов. Если всего двенадцать голосов, то один голос — это 360 : 12 = 30 градусов. Частая ошибка — путать градусы с процентами: 100 — это проценты, а круг делится на 360 градусов. Как проверить себя: сложи углы всех секторов — должно получиться 360.',

      'Чтобы построить сектор, долю умножают на 360. Половина круга — это 1/2 · 360 = 180 градусов, четверть — 90, треть — 120. Угол отмеряют транспортиром от вертикального радиуса. Частая ошибка — отмерять угол от горизонтали и получать зеркальную картинку. Как проверить себя: начинай от одного и того же радиуса и иди по кругу в одну сторону.',

      'Соберём круговую диаграмму по нашим данным. Всего двенадцать голосов, значит на каждый голос 30 градусов. Футбол — 6 · 30 = 180 градусов, это ровно половина круга. Рисование — 3 · 30 = 90, четверть. Чтение — 2 · 30 = 60. Музыка — 1 · 30 = 30. Сумма 180 + 90 + 60 + 30 = 360 — круг замкнулся. Частая ошибка — потерять градусы при подсчёте. Как проверить себя: сложи все углы и сверь с 360.',

      'Диаграммы дополняют друг друга. Столбчатая лучше показывает, насколько одно число больше другого: видно, что футбол вдвое больше рисования. Круговая лучше показывает доли целого: сразу видно, что футбол — это половина всех голосов. Частая ошибка — выбирать диаграмму не по задаче. Как проверить себя: спроси, что нужно показать — разницу чисел или часть от целого.',

      'Пройти практику: пять уровней — чтение столбцов, углы секторов, доли круга и выбор диаграммы. Дальше — тренажёр со счётом.',

      'Соберём правило. Данные собирают счётом, записывают в таблицу и показывают диаграммой. В столбчатой диаграмме высота столбца равна числу; столбцы одной ширины, значения читают по оси с её ценой деления. В круговой диаграмме целое — 360 градусов, а угол сектора равен доле, умноженной на 360. Столбчатая помогает сравнивать числа, круговая — показывать доли целого.'
    ],
    check: {
      q:'Сектор занимает четверть круга. Сколько это градусов?',
      choices:['90°','120°','180°'],
      ans:0,
      exp:'1/4 · 360 = 90°. Четверть круга — прямой угол.'
    },
    tasks: [
      { q:'Столбец поднялся до деления 5 при цене деления 1. Сколько единиц он показывает?', kind:'choice',
        choices:['5','1','6'], ans:0,
        hints:['Высота столбца равна числу.','Пять делений — пять единиц.'],
        sol:'Столбец показывает 5 единиц.' },
      { q:'Сектор занимает половину круга. Сколько это градусов?', kind:'choice',
        choices:['180°','90°','60°'], ans:0,
        hints:['Долю умножь на 360.','1/2 · 360.'],
        sol:'180° — половина круга.' },
      { q:'Сектор 120°. Какая это доля круга?', kind:'choice',
        choices:['1/3','1/4','1/2'], ans:0,
        hints:['Раздели угол на 360.','120 : 360.'],
        sol:'120 : 360 = 1/3.' },
      { q:'Что лучше покажет, что футбол — половина всех голосов?', kind:'choice',
        choices:['круговая диаграмма','столбчатая диаграмма','таблица'], ans:0,
        hints:['Ищем доли целого.','Половина круга видна сразу.'],
        sol:'Круговая: половина круга — сектор 180°.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L932.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else сцена=F12(s);

    const ЗАГОЛОВКИ={
      1:['Опрос','Каждая палочка — голос'],
      2:['Таблица','Данные по строкам'],
      3:['Зачем диаграмма','Числа и столбики'],
      4:['Столбчатая','Высота равна числу'],
      5:['Ось','Цена деления'],
      6:['Чтение','Где столбец выше'],
      7:['Круг','Триста шестьдесят градусов'],
      8:['Транспортир','Угол сектора'],
      9:['Круговая','Собираем по кусочкам'],
      10:['Сравнение','Числа и доли'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр','Проверь себя']
    };
    const з=ЗАГОЛОВКИ[f]||['Диаграммы','Столбчатая и круговая'];
    el.innerHTML = `<div class="s6 l932" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r932Сбор=(к)=>{ S().сбор=к; chRender(0); };
  window.r932Tabl=(к)=>{ S().таблица=к; chRender(0); };
  window.r932Why=(к)=>{ S().зачем=к; chRender(0); };
  window.r932Bar=(к)=>{ S().столбчатая=к; chRender(0); };
  window.r932Scale=(к)=>{ S().масштаб=к; chRender(0); };
  window.r932Read=(к)=>{ S().чтение=к; chRender(0); };
  window.r932Circ=(к)=>{ S().круг=к; chRender(0); };
  window.r932Prot=(к)=>{ S().транспортир=к; chRender(0); };
  window.r932Pie=(к)=>{ S().круговая=к; chRender(0); };
  window.r932Both=(к)=>{ S().сравнение=к; chRender(0); };
  window.r932Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r932Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r932Train=(к)=>{
    const s=S();
    if(s.тОтвет!=null) return;
    const и=(s.тНомер||0)%ТРЕНАЖЁР.length;
    s.тОтвет=к;
    if(к===ТРЕНАЖЁР[и].в) s.тВерно=(s.тВерно||0)+1; else s.тОшибки=(s.тОшибки||0)+1;
    chRender(0);
  };
  window.r932NextQ=()=>{ const s=S(); s.тНомер=((s.тНомер||0)+1)%ТРЕНАЖЁР.length; s.тОтвет=null; chRender(0); };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L932; else arr.push(L932);
  })();
  window.MA932={render:render, L:L932};
})();
