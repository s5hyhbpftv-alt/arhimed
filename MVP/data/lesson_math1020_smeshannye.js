/* ====== МАТЕМАТИКА · УРОК 1020 · «СЛОЖЕНИЕ И ВЫЧИТАНИЕ СМЕШАННЫХ ЧИСЕЛ» =====
   4 класс. Тема была заглушкой «готовится» в MVP/data/soon_lessons.js.

   ЗАЧЕМ БОЧКИ. Смешанное число трудно не считать, а УВИДЕТЬ: «две целых и три
   четверти» — это не значок, это две полные бочки и ещё три четверти третьей.
   Поэтому весь урок стоит на одном предмете: бочке пресной воды, разделённой
   обручами на четыре части. Целое — полная бочка, доля — уровень воды.
   Тогда два трудных места темы становятся физическими действиями:
     переход через единицу — две неполные бочки СЛИВАЮТСЯ в одну полную;
     занятие единицы      — полную бочку ВСКРЫВАЮТ на четыре четверти.
   Ребёнок нажимает «слить» и «вскрыть» сам, и видит, куда делась вода.

   Сюжет под мир 4 класса в «Пути» («Открытое море»): запас пресной воды на
   переходе до Сиракуз.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ (доли четвертные, как и положено в 4 классе —
   приведение к общему знаменателю здесь ещё не нужно):
     2¼ + 1¼ = 3½        (9/4 + 5/4 = 14/4 = 3 2/4)
     2¼ + 1¾ = 4 ровно   (9/4 + 7/4 = 16/4)
     5¾ − 2¼ = 3½        (23/4 − 9/4 = 14/4 = 3 2/4)
     3¼ − 1¾ = 1½        (13/4 − 7/4 = 6/4 = 1 2/4) — с занятием единицы
     проверка обратным действием: 1½ + 1¾ = 3¼ (6/4 + 7/4 = 13/4) ✓

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md, но движение здесь не «подсветка», а
   само вещество: уровень воды поднимается и опадает, поверхность покачивается,
   струя переливается из бочки в бочку, обручи проявляются при вскрытии.
   Всё это — по действию ребёнка (fill=freeze), кроме тихой волны на
   поверхности. prefers-reduced-motion выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 1020;
  const GOLD='#ffd76a', GREEN='#8fd1a8', ВОДА='#7fd1ff', RED='#e86a5a',
        ДУБ='#a97c40', ОБРУЧ='#cbb89a';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';
  const ДОЛЕЙ = 4;   /* бочка делится обручами на четверти */

  const CSS=`
  #lvis .s6.l1020{gap:14px}
  #lvis .s6.l1020 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l1020 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l1020 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l1020 .карт.задача{border-color:${GOLD}}
  #lvis .s6.l1020 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l1020 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l1020 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l1020 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l1020 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l1020 .правило b{color:${GOLD}}

  /* ── кран: наливаем и отливаем четверти ── */
  #lvis .s6.l1020 .кран{display:flex;align-items:center;gap:10px;width:100%;padding:12px;border-radius:16px;
    background:rgba(127,209,255,.08);border:1.5px solid rgba(127,209,255,.35)}
  #lvis .s6.l1020 .кран .сколько{flex:1;min-width:0;text-align:center;font-size:16px;line-height:1.25;color:${ИНК}}
  #lvis .s6.l1020 .кран .сколько b{display:block;font-size:32px;color:${ВОДА};font-variant-numeric:tabular-nums}
  #lvis .s6.l1020 .кран button{width:56px;height:56px;flex:0 0 56px;border-radius:14px;cursor:pointer;
    border:1px solid rgba(127,209,255,.5);background:rgba(127,209,255,.14);color:${ВОДА};
    font:inherit;font-size:24px;line-height:1;touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1020 .кран button:active{transform:translateY(2px);background:rgba(127,209,255,.26)}
  #lvis .s6.l1020 .кран button:disabled{opacity:.35;cursor:not-allowed}

  /* ── большая кнопка действия: «слить», «вскрыть» ── */
  #lvis .s6.l1020 .дело{width:100%;min-height:60px;padding:16px;border-radius:16px;cursor:pointer;
    font:inherit;font-size:20px;line-height:1.25;text-align:center;color:#16241c;
    border:0;background:linear-gradient(180deg,${GOLD},#c8912f);
    box-shadow:0 3px 0 rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.4);
    transition:transform 120ms ease, box-shadow 180ms ease}
  #lvis .s6.l1020 .дело:active{transform:translateY(2px);box-shadow:0 1px 0 rgba(0,0,0,.42)}
  #lvis .s6.l1020 .дело.сделано{background:rgba(143,209,168,.16);color:${GREEN};
    box-shadow:none;border:1.5px solid ${GREEN}}

  #lvis .s6.l1020 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l1020 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1020 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l1020 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l1020 .ask button.miss{border-color:var(--no)}

  #lvis .s6.l1020 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l1020 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1020 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l1020 .уровни .точка.сейчас{background:${GOLD};animation:l1020dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l1020dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l1020 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l1020{-webkit-text-size-adjust:100%}
  #lvis .s6.l1020 [data-anim]{animation:l1020rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l1020rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l1020 [data-anim]{animation:none!important}
    #lvis .s6.l1020 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l1020 .ask button,#lvis .s6.l1020 .кран button,#lvis .s6.l1020 .дело{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l1020-style');
      if(!s){ s=document.createElement('style'); s.id='l1020-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Запас воды</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ДЕЛО = (текст,on,сделано) =>
    `<button type="button" data-anim style="--i:3" class="дело${сделано?' сделано':''}" onclick="${on}">${текст}</button>`;

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анРаз = (имя,значения,длит,задержка) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" fill="freeze"
              ${задержка?'begin="'+задержка+'"':''}
              calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c1020-фон" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2b3d35"/><stop offset="0.55" stop-color="#1c2f27"/>
        <stop offset="1" stop-color="#101a15"/>
      </linearGradient>
      <linearGradient id="c1020-дуб" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#7a5528"/><stop offset="0.18" stop-color="#c79a5a"/>
        <stop offset="0.5" stop-color="#a97c40"/><stop offset="0.85" stop-color="#8a5f30"/>
        <stop offset="1" stop-color="#5c3f20"/>
      </linearGradient>
      <linearGradient id="c1020-вода" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#bfe6ff"/><stop offset="0.3" stop-color="#7fd1ff"/>
        <stop offset="1" stop-color="#2b6d99"/>
      </linearGradient>
      <clipPath id="c1020-нутро"><rect x="-17" y="-34" width="34" height="68" rx="6"/></clipPath>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||232}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c1020-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+16, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.86)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* ---------------- БОЧКА ----------------------------------------------
     x,y — центр бочки. налито — сколько четвертей (0…4), можно дробно.
     былоНалито — уровень, с которого вода доезжает (перелив на глазах).
     вскрыта — показывать ли линии четвертей внутри.
     Бочка рисуется клёпками и обручами; вода живёт внутри по clip-path,
     поэтому никогда не вылезает за края. */
  const бочка = (x,y,налито,былоНалито,вскрыта,цветПодписи,подпись_,масштаб) => {
    const В=68, Ш=34, м=масштаб||1;         /* нутро бочки; м — крупный план */
    const ур = (ч)=> -В/2 + В*(1-Math.max(0,Math.min(ДОЛЕЙ,ч))/ДОЛЕЙ);
    const y1 = ур(налито), y0 = ур(былоНалито==null?налито:былоНалито);
    const в1 = (В/2)-y1,   в0 = (В/2)-y0;
    return `<g transform="translate(${x} ${y}) scale(${м})">
      <!-- тело -->
      <path d="M-19 -34 q-5 34 0 68 h38 q5 -34 0 -68 z" fill="url(#c1020-дуб)" stroke="${ОБВОД}" stroke-width="${(1.4/м).toFixed(2)}"/>
      <g clip-path="url(#c1020-нутро)">
        <!-- пустая часть темнее: иначе не видно, докуда бочка НЕ налита -->
        <rect x="-17" y="-34" width="34" height="68" fill="rgba(10,20,16,.55)"/>
        <rect x="-17" y="${y0.toFixed(1)}" width="34" height="${Math.max(0,в0).toFixed(1)}" fill="url(#c1020-вода)" opacity=".95">
          ${анРаз('y', y0.toFixed(1)+';'+y1.toFixed(1), '640ms')}
          ${анРаз('height', Math.max(0,в0).toFixed(1)+';'+Math.max(0,в1).toFixed(1), '640ms')}
        </rect>
        ${налито>0?`<g>
          <path d="M-17 ${(y1+1).toFixed(1)} q8 -3 17 0 q9 3 17 0 v4 h-34 z" fill="#dff2ff" opacity=".6">
            ${анРаз('d','M-17 '+(y0+1).toFixed(1)+' q8 -3 17 0 q9 3 17 0 v4 h-34 z;M-17 '+(y1+1).toFixed(1)+' q8 -3 17 0 q9 3 17 0 v4 h-34 z','640ms')}
            ${ДВИЖ?`<animateTransform attributeName="transform" type="translate"
               values="0 0;1.6 0;0 0;-1.6 0;0 0" dur="3.6s" repeatCount="indefinite"/>`:''}
          </path></g>`:''}
        <!-- деления четвертей: ВНУТРИ нутра, под обручами, поэтому читаются
             как деления бочки, а не как пунктир на обруче -->
        ${вскрыта?`<g>${анРаз('opacity','0;1','420ms')}
          ${[1,2,3].map(к=>`<line x1="-17" y1="${(-В/2+В*к/ДОЛЕЙ).toFixed(1)}" x2="17" y2="${(-В/2+В*к/ДОЛЕЙ).toFixed(1)}"
            stroke="${GOLD}" stroke-width="${(1.6/м).toFixed(2)}" stroke-dasharray="4 3" opacity=".95"/>`).join('')}
        </g>`:''}
      </g>
      <!-- обручи поверх -->
      ${[-22,0,22].map(о=>`<rect x="-21" y="${о-3}" width="42" height="6" rx="2"
        fill="${ОБРУЧ}" opacity=".9" stroke="${ОБВОД}" stroke-width="${(0.8/м).toFixed(2)}"/>`).join('')}
      <!-- риски четвертей по бокам: видны и поверх обручей -->
      ${вскрыта?`<g>${анРаз('opacity','0;1','420ms')}
        ${[1,2,3].map(к=>{const yy=(-В/2+В*к/ДОЛЕЙ).toFixed(1);
          return `<line x1="-24" y1="${yy}" x2="-19" y2="${yy}" stroke="${GOLD}" stroke-width="${(1.8/м).toFixed(2)}"/>
                  <line x1="19" y1="${yy}" x2="24" y2="${yy}" stroke="${GOLD}" stroke-width="${(1.8/м).toFixed(2)}"/>`;}).join('')}
      </g>`:''}
      <path d="M-19 -34 q-5 34 0 68 h38 q5 -34 0 -68 z" fill="none" stroke="${ОБВОД}" stroke-width="${(1.4/м).toFixed(2)}"/>
      ${подпись_?`<text x="0" y="${(42+16/м).toFixed(1)}" text-anchor="middle" font-size="${(14/м).toFixed(1)}"
        fill="${цветПодписи||МУТ}" font-family="Georgia,'Times New Roman',serif">${esc(подпись_)}</text>`:''}
    </g>`;
  };

  /* СТРУЯ перелива между двумя бочками */
  const струя = (x1,x2,y) => ДВИЖ ? `<g>
    <path d="M${x1} ${y} C${(x1+x2)/2} ${y-22} ${(x1+x2)/2} ${y-22} ${x2} ${y}" fill="none"
      stroke="${ВОДА}" stroke-width="3" stroke-linecap="round" stroke-dasharray="4 6" opacity="0">
      ${анРаз('opacity','0;0.9','300ms')}
      <animate attributeName="stroke-dashoffset" values="0;-20" dur="0.7s" repeatCount="indefinite"/>
    </path></g>` : '';

  /* запись смешанного числа: целое и дробь столбиком */
  const смеш = (x,y,целых,числ,кегль,цвет) => {
    const к = кегль||24;
    if(!числ) return т(x,y,String(целых),к,цвет||ИНК,true);
    const сдв = String(целых).length*к*0.32;
    return `<g>
      ${т(x-сдв-4,y,String(целых),к,цвет||ИНК,true,'end')}
      ${т(x+8,y-к*0.34,String(числ),к*0.58,цвет||ИНК,true)}
      <line x1="${x+1}" y1="${y-к*0.28}" x2="${x+15}" y2="${y-к*0.28}" stroke="${цвет||ИНК}" stroke-width="1.4"/>
      ${т(x+8,y+к*0.28,String(ДОЛЕЙ),к*0.58,цвет||ИНК,true)}
    </g>`;
  };

  /* ================= КАДРЫ ================= */

  /* 1. Запас воды: две бочки неполные */
  function F1(s){
    return ЗАДАЧА('Перед переходом до Сиракуз на судно грузят пресную воду. В одном трюме <b>2 бочки и ещё четверть</b>, в другом — <b>1 бочка и три четверти</b>.') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${т(168,24,'Трюм левого борта',14,GOLD,true)}
        ${бочка(56,96,4,null,false,МУТ,null)}
        ${бочка(104,96,4,null,false,МУТ,null)}
        ${бочка(152,96,1,null,false,ВОДА,null)}
        ${подпись(104,44,'2 целых и 1/4',ВОДА,14)}
        ${бочка(232,96,4,null,false,МУТ,null)}
        ${бочка(280,96,3,null,false,ВОДА,null)}
        ${подпись(256,44,'1 целая и 3/4',ВОДА,14)}
        <line x1="192" y1="60" x2="192" y2="150" stroke="rgba(255,255,255,.16)" stroke-width="1.2"/>
        <g>${проявить('8s',0.5,0.6)}
          ${т(168,186,'полная бочка — это целое,',14,ИНК)}
          ${т(168,208,'уровень внутри — доля',14,МУТ)}</g>
      `,232)}</div>` +
      СКАЗ('Вопрос','Сколько воды всего? И хватит ли её, если в пути израсходуют <b>1 бочку и три четверти</b>?') +
      ПРАВИЛО('Смешанное число — это <b>целые бочки и доля</b> ещё одной.');
  }

  /* 2. КРАН: сам наливаешь четверти */
  function F2(s){
    const ч = Math.max(0, Math.min(4, s.кран==null?1:s.кран));
    const было = Math.max(0, Math.min(4, s.кранБыло==null?ч:s.кранБыло));
    const дробь = ч===0?'пусто':(ч===4?'полная бочка':ч+'/4');
    return ЗАДАЧА('Бочка разделена обручами на четыре части. Полей воды и посмотри, как называется уровень.') +
      `<div class="pic">${свг(`
        ${фон(226)}
        ${т(168,24,'Четыре части одной бочки',14,GOLD,true)}
        ${[1,2,3,4].map((д,i)=>`<g opacity="${д<=ч?1:0.28}">
          ${т(96+i*48,52,д+'/4',14,д<=ч?ВОДА:МУТ)}</g>`).join('')}
        ${бочка(168,120,ч,было,true,ВОДА,null,1.5)}
        <g>${проявить('7s',0.55,0.65)}
          ${подпись(168,212,ч===4?'это уже целая бочка':(ч===0?'ни одной четверти':'налито '+ч+' из 4'),
            ч===4?GREEN:ВОДА,14)}</g>
      `,226)}</div>` +
      A(3,'кран',
        `<button type="button" onclick="r1020Кран(-1)" ${ч>0?'':'disabled'} aria-label="отлить">−</button>
         <span class="сколько">налито<b>${дробь}</b></span>
         <button type="button" onclick="r1020Кран(1)" ${ч<4?'':'disabled'} aria-label="налить">+</button>`) +
      СКАЗ('Смотри', ч===4
        ? 'Четыре четверти — это <b>целая бочка</b>: 4/4 = 1.'
        : (ч===0 ? 'Бочка пуста. Нажимай «+», чтобы налить четверть.'
                 : 'Налито <b>'+ч+'/4</b>. До полной не хватает '+(4-ч)+'/4.')) +
      ПРАВИЛО('<b>4/4 = 1</b> — четыре четверти складываются в целое.');
  }

  /* 3. Складываем доли без перехода */
  function F3(s){
    const в = s.без;
    return ЗАДАЧА('Сложим 2¼ и 1¼. Сначала целые, потом доли.') +
      `<div class="pic">${свг(`
        ${фон(226)}
        ${т(168,24,'Целые к целым, доли к долям',14,GOLD,true)}
        ${бочка(52,100,4,null,false,МУТ,null)}
        ${бочка(96,100,4,null,false,МУТ,null)}
        ${бочка(140,100,1,null,true,ВОДА,'1/4')}
        ${т(174,100,'+',24,GOLD,true)}
        ${бочка(208,100,4,null,false,МУТ,null)}
        ${бочка(252,100,1,null,true,ВОДА,'1/4')}
        <g>${проявить('8s',0.44,0.54)}
          ${т(168,176,'целых: 2 + 1 = 3',16,ИНК)}
          ${т(168,200,'долей: 1/4 + 1/4 = 2/4',16,ВОДА)}</g>
      `,226)}</div>` +
      `<div class="ask">
        ${BTN(4, в==='3_2'?'hit':(в?'miss':''), 'Три целых и 2/4 (это же 3½)', "r1020Без('3_2')")}
        ${BTN(5, в==='3_1'?'miss':'', 'Три целых и 1/8', "r1020Без('3_1')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='3_2',
        в==='3_2' ? 'Верно: целые сложили с целыми (2 + 1 = 3), доли с долями (1/4 + 1/4 = 2/4). Вышло 3 и 2/4, то есть 3½.'
                  : 'Нет: знаменатель не меняется — доли остаются четвертями. Складываем только числители: 1/4 + 1/4 = 2/4.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Доли складывают <b>числителями</b>, знаменатель не трогают.');
  }

  /* 4. ПЕРЕХОД: две неполные сливаются в одну полную */
  function F4(s){
    const слито = !!s.слито;
    return ЗАДАЧА('А теперь 1/4 и 3/4 в разных бочках. Нажми «слить» и посмотри, что получится.') +
      `<div class="pic">${свг(`
        ${фон(232)}
        ${т(168,24,слито?'Получилась целая бочка':'Две неполные бочки',14,слито?GREEN:GOLD,true)}
        ${слито
          ? `${бочка(112,120,0,1,true,МУТ,'пусто')}
             ${бочка(224,120,4,3,true,GREEN,'4/4 = 1 целая')}
             ${струя(136,200,96)}`
          : `${бочка(112,120,1,null,true,ВОДА,'1/4')}
             ${бочка(224,120,3,null,true,ВОДА,'3/4')}`}
        <g>${проявить('7s',0.5,0.6)}
          ${подпись(168,206, слито?'1/4 + 3/4 = 4/4 = 1':'вместе 1/4 + 3/4', слито?GREEN:ВОДА,16)}</g>
      `,232)}</div>` +
      ДЕЛО(слито?'✓ вода перелита':'Слить в одну бочку', "r1020Слить()", слито) +
      (слито
        ? СКАЗ('Вот он, переход','Четыре четверти собрались в <b>целую бочку</b>. Когда доли дают целое, оно уходит к целым — это и называется переходом через единицу.')
        : СКАЗ('Прикинь заранее','1/4 и 3/4 — это вместе сколько четвертей?')) +
      ПРАВИЛО('Набралось <b>4/4</b> — это ещё одно целое.');
  }

  /* 5. Полный пример со сложением */
  function F5(s){
    const в = s.сумма;
    return ЗАДАЧА('Считаем весь запас: 2¼ + 1¾. Сколько бочек вышло?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        <g>${проявить('8s',0.08,0.18)}
          ${т(52,54,'целые',14,МУТ)}
          ${т(52,80,'2 + 1 = 3',16,ИНК,true)}</g>
        <g>${проявить('8s',0.26,0.36)}
          ${т(168,54,'доли',14,МУТ)}
          ${т(168,80,'1/4 + 3/4 = 4/4',16,ВОДА,true)}</g>
        <g>${проявить('8s',0.44,0.54)}
          ${т(286,54,'а 4/4 —',14,МУТ)}
          ${т(286,80,'это 1',16,GREEN,true)}</g>
        <line x1="26" y1="98" x2="310" y2="98" stroke="rgba(255,255,255,.18)" stroke-width="1.2"/>
        <g>${проявить('8s',0.62,0.72)}
          ${т(168,126,'3 + 1 = 4 целых бочки',16,GOLD,true)}</g>
        ${[0,1,2,3].map(i=>бочка(78+i*60,182,4,i===3?0:4,false,GREEN,null)).join('')}
      `,238)}</div>` +
      `<div class="ask">
        ${BTN(4, в==='4'?'hit':(в?'miss':''), 'Ровно 4 бочки', "r1020Сумма('4')")}
        ${BTN(5, в==='3_4'?'miss':'', 'Три бочки и 4/4', "r1020Сумма('3_4')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='4',
        в==='4' ? 'Верно: 2 + 1 = 3 целых, доли дали 4/4 — ещё одно целое. Итого ровно 4 бочки, дробной части не осталось.'
                : 'Почти: 3 и 4/4 — это то же самое, но так не записывают. Четыре четверти — целое, значит 3 + 1 = 4.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Если доли дали целое — <b>переносим его к целым</b>.');
  }

  /* 6. Вычитание без занятия */
  function F6(s){
    const в = s.минус;
    return ЗАДАЧА('В пути израсходовали 2¼ бочки, а было 5¾. Сколько осталось?') +
      `<div class="pic">${свг(`
        ${фон(262)}
        ${т(168,24,'Отливаем из запаса',14,GOLD,true)}
        <g>${проявить('8s',0.1,0.2)}
          ${т(52,62,'было',14,МУТ)}
          ${т(52,88,'5 и 3/4',16,ИНК,true)}</g>
        <g>${проявить('8s',0.28,0.38)}
          ${т(168,62,'убрали',14,МУТ)}
          ${т(168,88,'2 и 1/4',16,RED,true)}</g>
        <g>${проявить('8s',0.5,0.6)}
          ${т(286,62,'осталось',14,МУТ)}
          ${т(286,88,'3 и 2/4',16,GREEN,true)}</g>
        <line x1="26" y1="106" x2="310" y2="106" stroke="rgba(255,255,255,.18)" stroke-width="1.2"/>
        ${[0,1,2].map(i=>бочка(84+i*60,168,4,4,false,МУТ,null)).join('')}
        ${бочка(264,168,2,3,true,GREEN,'2/4')}
        <g>${проявить('8s',0.72,0.82)}
          ${/* подпись бочки «2/4» стоит на 226 — фраза ниже неё, а не поверх */''}
          ${т(168,252,'5 − 2 = 3 целых · 3/4 − 1/4 = 2/4',14,ИНК)}</g>
      `,262)}</div>` +
      `<div class="ask">
        ${BTN(4, в==='3_2'?'hit':(в?'miss':''), '3 бочки и 2/4 (это 3½)', "r1020Минус('3_2')")}
        ${BTN(5, в==='3_4'?'miss':'', '3 бочки и 4/4', "r1020Минус('3_4')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='3_2',
        в==='3_2' ? 'Верно: целые с целыми (5 − 2 = 3), доли с долями (3/4 − 1/4 = 2/4). Осталось 3 и 2/4, то есть три с половиной бочки.'
                  : 'Нет: доли тоже вычитают, а не складывают. 3/4 − 1/4 = 2/4.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Вычитают так же: <b>целые от целых, доли от долей</b>.');
  }

  /* 7. ЗАНЯТИЕ: вскрываем целую бочку */
  function F7(s){
    const вскрыта = !!s.вскрыта;
    return ЗАДАЧА('Теперь трудный случай: надо отлить 3/4, а в неполной бочке всего 1/4. Что делать?') +
      `<div class="pic">${свг(`
        ${фон(238)}
        ${т(168,24, вскрыта?'Целую вскрыли на четверти':'Долей не хватает',14, вскрыта?GREEN:RED,true)}
        ${вскрыта
          ? `${бочка(96,124,4,4,true,GOLD,'вскрыта')}
             ${бочка(212,124,1,1,true,ВОДА,'1/4')}
             ${струя(120,188,100)}
             ${подпись(168,208,'1 целая + 1/4 = 5/4',GREEN,16)}`
          : `${бочка(96,124,4,null,false,МУТ,'целая')}
             ${бочка(212,124,1,null,true,ВОДА,'1/4')}
             ${подпись(168,208,'из 1/4 три четверти не отлить',RED,14)}`}
      `,238)}</div>` +
      ДЕЛО(вскрыта?'✓ бочка вскрыта':'Вскрыть целую бочку', "r1020Вскрыть()", вскрыта) +
      (вскрыта
        ? СКАЗ('Вот оно, занятие','Целую бочку вскрыли и записали как <b>4/4</b>. Вместе с прежней четвертью стало <b>5/4</b> — теперь три четверти отлить можно.')
        : СКАЗ('Подумай','Целых бочек рядом хватает. Можно ли взять одну из них и превратить в четверти?')) +
      ПРАВИЛО('Не хватает долей — <b>берём одно целое и дробим его</b>.');
  }

  /* 8. Полный пример с занятием */
  function F8(s){
    const в = s.занятие;
    return ЗАДАЧА('Считаем: 3¼ − 1¾. Долей не хватает, поэтому занимаем целое.') +
      `<div class="pic">${свг(`
        ${фон(238)}
        <g>${проявить('8s',0.08,0.18)}
          <rect x="26" y="44" width="284" height="38" rx="10" fill="rgba(232,106,90,.12)" stroke="${RED}" stroke-width="1.4"/>
          ${т(168,69,'1/4 − 3/4 не получается',16,RED)}</g>
        <g>${проявить('8s',0.3,0.4)}
          <rect x="26" y="94" width="284" height="38" rx="10" fill="rgba(255,215,106,.14)" stroke="${GOLD}" stroke-width="1.4"/>
          ${т(168,119,'занимаем целое: 3¼ = 2 и 5/4',16,GOLD)}</g>
        <g>${проявить('8s',0.54,0.64)}
          <rect x="26" y="144" width="284" height="38" rx="10" fill="rgba(143,209,168,.14)" stroke="${GREEN}" stroke-width="1.6"/>
          ${т(168,169,'2 − 1 = 1 · 5/4 − 3/4 = 2/4',16,GREEN)}</g>
        <g>${проявить('8s',0.76,0.86)}
          ${т(168,212,'ответ: 1 и 2/4, то есть 1½',20,GOLD,true)}</g>
      `,238)}</div>` +
      `<div class="ask">
        ${BTN(4, в==='1_2'?'hit':(в?'miss':''), '1 бочка и 2/4', "r1020Занятие('1_2')")}
        ${BTN(5, в==='2_2'?'miss':'', '2 бочки и 2/4', "r1020Занятие('2_2')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='1_2',
        в==='1_2' ? 'Верно: заняли целое, стало 2 и 5/4. Дальше обычно: 2 − 1 = 1 и 5/4 − 3/4 = 2/4. Ответ 1 и 2/4 — это полторы бочки.'
                  : 'Целых осталось не 2, а 1: одно целое мы потратили на четверти, и от двух отняли одно.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Заняли целое — <b>у целых стало на одно меньше</b>.');
  }

  /* 9. Проверка обратным действием */
  function F9(s){
    const в = s.проверка;
    return ЗАДАЧА('Как убедиться, что 3¼ − 1¾ = 1½ посчитано верно?') +
      `<div class="pic">${свг(`
        ${фон(214)}
        ${т(168,24,'Обратное действие',14,GOLD,true)}
        <g>${проявить('7s',0.12,0.22)}
          ${бочка(70,104,4,null,false,МУТ,null)}
          ${бочка(114,104,2,null,true,ВОДА,'ответ 1 и 2/4')}</g>
        ${т(152,104,'+',24,GOLD,true)}
        <g>${проявить('7s',0.34,0.44)}
          ${бочка(190,104,4,null,false,МУТ,null)}
          ${бочка(234,104,3,null,true,ВОДА,'вычли 1 и 3/4')}</g>
        <g>${проявить('7s',0.6,0.7)}
          ${подпись(168,180,'2/4 + 3/4 = 5/4 = 1 и 1/4',ВОДА,14)}
          ${т(168,204,'1 + 1 + 1 = 3 целых и 1/4 — сошлось',14,GREEN)}</g>
      `,214)}</div>` +
      `<div class="ask">
        ${BTN(4, в==='сложить'?'hit':(в?'miss':''), 'Сложить ответ с тем, что вычитали', "r1020Проверка('сложить')")}
        ${BTN(5, в==='вычесть'?'miss':'', 'Вычесть ответ ещё раз', "r1020Проверка('вычесть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='сложить',
        в==='сложить' ? 'Верно: 1½ + 1¾ = 3¼ — получилось исходное число, значит вычли правильно.'
                      : 'Нет: проверяют обратным действием. Вычитание проверяют сложением.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Вычитание проверяют <b>сложением</b>.');
  }

  /* 10. Ловушка: доли и целые нельзя смешивать */
  function F10(s){
    const в = s.ловушка;
    return ЗАДАЧА('Ученик написал: 2¼ + 1¾ = 3 и 4/8. Где ошибка?') +
      `<div class="pic">${свг(`
        ${фон(214)}
        <g>${проявить('8s',0.1,0.2)}
          <rect x="22" y="40" width="292" height="54" rx="10" fill="rgba(232,106,90,.12)" stroke="${RED}" stroke-width="1.6"/>
          ${т(168,66,'3 и 4/8',20,RED,true)}
          ${т(168,86,'знаменатели сложили — так нельзя',14,МУТ)}</g>
        <g>${проявить('8s',0.42,0.52)}
          <rect x="22" y="106" width="292" height="54" rx="10" fill="rgba(143,209,168,.14)" stroke="${GREEN}" stroke-width="2"/>
          ${т(168,132,'4 целых',20,GREEN,true)}
          ${т(168,152,'доли складывают числителями',14,МУТ)}</g>
        <g>${проявить('8s',0.74,0.84)}
          ${т(168,190,'4/8 — это половина, а у нас вышло целое',14,ИНК)}</g>
      `,214)}</div>` +
      `<div class="ask">
        ${BTN(4, в==='знаменатель'?'hit':(в?'miss':''), 'Знаменатель складывать нельзя', "r1020Ловушка('знаменатель')")}
        ${BTN(5, в==='целые'?'miss':'', 'Ошибка в целых: должно быть 2', "r1020Ловушка('целые')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='знаменатель',
        в==='знаменатель' ? 'Верно: четверти остаются четвертями. 1/4 + 3/4 = 4/4 = 1, поэтому ответ 4 целых, а не 3 и 4/8.'
                          : 'С целыми всё верно: 2 + 1 = 3. Ошибка в долях — знаменатель не складывают.') : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Знаменатель — это <b>на сколько частей поделено</b>, он не меняется.');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'Сколько будет 1/4 + 2/4?',
      варианты:[{т:'3/4',ок:true},{т:'3/8',ок:false}],
      разбор:'Складывают числители: 1 + 2 = 3, знаменатель остаётся 4.' },
    { вопрос:'Сколько будет 2¼ + 1¼?',
      варианты:[{т:'3 и 2/4',ок:true},{т:'3 и 2/8',ок:false}],
      разбор:'Целые: 2 + 1 = 3. Доли: 1/4 + 1/4 = 2/4.' },
    { вопрос:'Сколько будет 2¼ + 1¾?',
      варианты:[{т:'4 целых',ок:true},{т:'3 и 4/4',ок:false}],
      разбор:'Доли дали 4/4 = 1, значит 3 + 1 = 4 целых.' },
    { вопрос:'Сколько будет 5¾ − 2¼?',
      варианты:[{т:'3 и 2/4',ок:true},{т:'3 и 4/4',ок:false}],
      разбор:'5 − 2 = 3 и 3/4 − 1/4 = 2/4.' },
    { вопрос:'Сколько будет 3¼ − 1¾?',
      варианты:[{т:'1 и 2/4',ок:true},{т:'2 и 2/4',ок:false}],
      разбор:'Заняли целое: 3¼ = 2 и 5/4. Тогда 2 − 1 = 1 и 5/4 − 3/4 = 2/4.' }
  ];

  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r1020Reset()")}</div>` +
        ПРАВИЛО('Целые к целым, доли к долям; <b>4/4 = 1</b>.');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к,
        выбран===к ? (в.ок?'hit':'miss') : '',
        в.т, "r1020Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Доли', задания:[
    {q:'1/4 + 1/4 = ?', в:0, варианты:['2/4','2/8'], раз:'Складывают числители, знаменатель тот же.'},
    {q:'3/4 + 1/4 = ?', в:1, варианты:['4/8','4/4, то есть 1'], раз:'Четыре четверти дают целое.'},
    {q:'3/4 − 1/4 = ?', в:0, варианты:['2/4','2/0'], раз:'3 − 1 = 2, знаменатель остаётся 4.'},
    {q:'2/4 + 2/4 = ?', в:1, варианты:['4/8','1 целое'], раз:'2 + 2 = 4, а 4/4 = 1.'}
  ]};
  const Т2 = { имя:'Смешанные', задания:[
    {q:'2¼ + 1¼ = ?', в:0, варианты:['3 и 2/4','3 и 2/8'], раз:'Целые 2 + 1 = 3, доли 1/4 + 1/4 = 2/4.'},
    {q:'2¼ + 1¾ = ?', в:1, варианты:['3 и 4/4','4 целых'], раз:'Доли дали целое: 3 + 1 = 4.'},
    {q:'5¾ − 2¼ = ?', в:0, варианты:['3 и 2/4','7 и 4/4'], раз:'Вычитаем, а не складываем: 5 − 2 = 3, 3/4 − 1/4 = 2/4.'},
    {q:'4¾ − 1¼ = ?', в:1, варианты:['3 и 4/4','3 и 2/4'], раз:'4 − 1 = 3, 3/4 − 1/4 = 2/4.'}
  ]};
  const Т3 = { имя:'Занятие целого', задания:[
    {q:'3¼ − 1¾: нужно ли занимать целое?', в:0, варианты:['Да: 1/4 меньше 3/4','Нет'], раз:'Из 1/4 три четверти не вычесть — занимаем.'},
    {q:'3¼ — это сколько, если занять целое?', в:1, варианты:['2 и 1/4','2 и 5/4'], раз:'Одно целое стало 4/4, плюс прежняя 1/4 — всего 5/4.'},
    {q:'3¼ − 1¾ = ?', в:0, варианты:['1 и 2/4','2 и 2/4'], раз:'2 − 1 = 1 и 5/4 − 3/4 = 2/4.'},
    {q:'Проверка: 1½ + 1¾ = ?', в:1, варианты:['2 и 5/4','3 и 1/4'], раз:'2/4 + 3/4 = 5/4 = 1 и 1/4; 1 + 1 + 1 = 3 и 1/4 ✓'}
  ]};

  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r1020T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r1020TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L1020 = {
    id: ID,
    title: 'Сложение и вычитание смешанных чисел',
    ico: '🛢',
    src: 'Начальная школа · 4 класс · Смешанные числа',
    subj: 'jun',
    explain: [
      'На судно грузят пресную воду: в одном трюме 2 бочки и четверть, в другом 1 бочка и три четверти. Полная бочка — целое, уровень внутри — доля.',
      'Бочка разделена обручами на четыре части. Налей воды сам и посмотри: четыре четверти дают целую бочку, 4/4 = 1.',
      'Складываем 2¼ и 1¼: целые с целыми (2 + 1 = 3), доли с долями (1/4 + 1/4 = 2/4).',
      'Слей 1/4 и 3/4 в одну бочку — получится ровно целая. Это и есть переход через единицу.',
      'Весь запас: 2¼ + 1¾ = 4 бочки ровно, дробной части не осталось.',
      'Вычитание такое же: 5¾ − 2¼ = 3 и 2/4, то есть три с половиной бочки.',
      'Трудный случай: отлить 3/4, когда налита всего 1/4. Вскрой целую бочку — она станет 4/4, и вместе с прежней четвертью выйдет 5/4.',
      'Считаем 3¼ − 1¾: заняли целое, стало 2 и 5/4; дальше 2 − 1 = 1 и 5/4 − 3/4 = 2/4. Ответ 1½.',
      'Проверка обратным действием: 1½ + 1¾ = 3¼ — сошлось.',
      'Ловушка: знаменатели не складывают. 1/4 + 3/4 — это 4/4 = 1, а не 4/8.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: доли.',
      'Тренажёр 2: смешанные числа.',
      'Тренажёр 3: занятие целого.'
    ],
    check: {
      q: 'Сколько будет 2¼ + 1¾?',
      choices: ['4 целых','3 и 4/8','3 и 2/4'],
      ans: 0,
      exp: 'Целые: 2 + 1 = 3. Доли: 1/4 + 3/4 = 4/4 = 1. Итого 4 целых.'
    },
    tasks: [
      { q:'Сколько будет 2¼ + 1¼?', kind:'choice',
        choices:['3 и 2/4','3 и 2/8'], ans:0,
        hints:['Целые складывай с целыми.','Знаменатель не меняется: 1/4 + 1/4 = 2/4.'],
        sol:'3 и 2/4, то есть 3½.' },
      { q:'Сколько будет 5¾ − 2¼?', kind:'choice',
        choices:['3 и 2/4','3 и 4/4'], ans:0,
        hints:['5 − 2 = 3.','3/4 − 1/4 = 2/4.'],
        sol:'3 и 2/4.' },
      { q:'Сколько будет 3¼ − 1¾?', kind:'choice',
        choices:['1 и 2/4','2 и 2/4'], ans:0,
        hints:['Из 1/4 три четверти не вычесть — займи целое.','3¼ = 2 и 5/4.'],
        sol:'1 и 2/4, то есть 1½.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L1020.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1);
    else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);

    const ЗАГОЛОВКИ={
      1:'Запас пресной воды',
      2:'Четыре части бочки',
      3:'Складываем доли',
      4:'Слить в одну',
      5:'Весь запас',
      6:'Отливаем',
      7:'Вскрыть целую',
      8:'Занятие целого',
      9:'Проверка',
      10:'Ловушка',
      11:'Практика',
      12:'Тренажёр 1',
      13:'Тренажёр 2',
      14:'Тренажёр 3'
    };
    el.innerHTML = `<div class="s6 l1020" data-frame="${f}">
        <h2>${ЗАГОЛОВКИ[f]||'Смешанные числа'}</h2>
        ${сцена}
      </div>`;
  }

  window.r1020Кран    =(д)=>{ const s=S(); s.кранБыло=(s.кран==null?1:s.кран);
    s.кран=Math.max(0,Math.min(4,(s.кран==null?1:s.кран)+д)); chRender(0); };
  window.r1020Слить   =()=>{ const s=S(); s.слито=!s.слито; chRender(0); };
  window.r1020Вскрыть =()=>{ const s=S(); s.вскрыта=!s.вскрыта; chRender(0); };
  window.r1020Без     =(к)=>{ S().без=к; chRender(0); };
  window.r1020Сумма   =(к)=>{ S().сумма=к; chRender(0); };
  window.r1020Минус   =(к)=>{ S().минус=к; chRender(0); };
  window.r1020Занятие =(к)=>{ S().занятие=к; chRender(0); };
  window.r1020Проверка=(к)=>{ S().проверка=к; chRender(0); };
  window.r1020Ловушка =(к)=>{ S().ловушка=к; chRender(0); };
  window.r1020Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r1020Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r1020T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=(s[ключ+'Шаг']||0)%набор.задания.length;
    const з=набор.задания[шаг];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r1020TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L1020; else arr.push(L1020);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA1020={render:render, L:L1020};
})();
