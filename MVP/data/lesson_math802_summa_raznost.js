/* ====== МАТЕМАТИКА · УРОК 802 · «ЗАДАЧИ НА СУММУ И РАЗНОСТЬ» ===============
   3 класс. Тема была заглушкой «готовится» в MVP/data/soon_lessons.js.

   ПОЧЕМУ КРЕН. Задачу «вместе 40, у одного на 6 больше» ребёнок обычно решает
   наугад: делит сумму пополам и приписывает разность целиком. Чтобы этого не
   случилось, разность должна стать ВИДИМОЙ и ВЕСОМОЙ. Поэтому весь урок стоит
   на одном предмете: шлюпке, которую кренит неравный груз. Мешки сложены в две
   стопки по бортам; лишние шесть на правом борту покрашены — это и есть
   разность, её видно глазами, и от неё лодка кренится.

   Тогда три школьных способа перестают быть тремя правилами и становятся тремя
   действиями с грузом:
     СНЯТЬ ЛИШНЕЕ     — убрали 6 мешков, стало поровну: (40 − 6) : 2 = 17;
     ДОБАВИТЬ ДО РОВНОГО — доложили 6, стало поровну по большему: (40 + 6) : 2 = 23;
     ПЕРЕНЕСТИ ПОЛОВИНУ РАЗНОСТИ — 3 мешка с тяжёлого борта на лёгкий, и крен
        уходит: 40 : 2 = 20 на борт. Отсюда сразу видно, почему разность делят
        пополам, а не приписывают целиком.
   Третий способ — морской, и именно он закрывает главную ошибку темы.

   СЮЖЕТ ПОД МИР 3 КЛАССА В «ПУТИ» («Вдоль берега»): первый переход от вехи к
   вехе. Судовой журнал висит над каждым кадром: крен и три вехи с отметками.

   ВСЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     веха 1 · груз:  40 и 6  → (40−6):2 = 17, 17+6 = 23; 17+23 = 40, 23−17 = 6 ✓
                     перенос половины разности: 6:2 = 3, 40:2 = 20 на борт ✓
     веха 2 · вода:  30 и 4  → (30−4):2 = 13, 13+4 = 17; перелить 4:2 = 2 → по 15 ✓
     веха 3 · путь:  54 и 10 → (54−10):2 = 22, 22+10 = 32; 22+32 = 54 ✓
     ловушка:        40:2 = 20, затем 20 и 26 — вместе 46, а не 40 ✗

   АНИМАЦИЯ по deploy/АНИМАЦИЯ_УРОКОВ.md: одно главное движение на кадр и одна
   вспышка результата. Движение здесь — сам груз: мешки переносятся с борта на
   борт, лишние снимаются, лодка выпрямляется. prefers-reduced-motion выключает
   SMIL полностью. */
(function(){
  'use strict';

  const ID = 802;
  const GOLD='#ffd76a', GREEN='#8fd1a8', RED='#e86a5a', ТИР='#7fd1c3',
        МОРЕ='#4f9fb8', МЕШОК='#cfae74', ДЕРЕВО='#a97c40';
  const ИНК='#f2f8f7', МУТ='#b9cfcb', ЛИНИЯ='#3f7f77', ОБВОД='#12201e';

  /* ---------- СУДОВОЙ ЖУРНАЛ: ход квеста ---------- */
  const ВЕХИ = [
    {ключ:'груз', имя:'Веха 1 · груз по бортам', числа:'40 и 6'},
    {ключ:'вода', имя:'Веха 2 · пресная вода',   числа:'30 и 4'},
    {ключ:'путь', имя:'Веха 3 · два дня пути',   числа:'54 и 10'}
  ];
  const взята = (s,к) => !!s['веха_'+к];

  const CSS=`
  #lvis .s6.l802{gap:14px}
  #lvis .s6.l802 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l802 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l802 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#1d3330,#122421);border:1.5px solid var(--line)}
  #lvis .s6.l802 .карт.задача{border-color:${ТИР}}
  #lvis .s6.l802 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l802 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l802 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l802 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l802 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${ТИР};
    background:linear-gradient(180deg,rgba(127,209,195,.14),rgba(127,209,195,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l802 .правило b{color:${ТИР}}

  /* ── судовой журнал ── */
  #lvis .s6.l802 .журнал{width:100%;padding:12px 14px;border-radius:16px;
    background:linear-gradient(180deg,rgba(127,209,195,.10),rgba(127,209,195,.03));
    border:1.5px solid rgba(127,209,195,.4);display:flex;flex-direction:column;gap:7px}
  #lvis .s6.l802 .журнал .шапка{display:flex;justify-content:space-between;align-items:baseline;gap:10px;
    font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l802 .журнал .шапка b{font-size:24px;letter-spacing:0;text-transform:none;color:${RED}}
  #lvis .s6.l802 .журнал .шапка b.ровно{color:${GREEN}}
  #lvis .s6.l802 .журнал ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:5px}
  #lvis .s6.l802 .журнал li{display:flex;align-items:center;gap:9px;font-size:16px;line-height:1.3;color:${МУТ}}
  #lvis .s6.l802 .журнал li i{flex:0 0 22px;width:22px;height:22px;border-radius:7px;font-style:normal;
    display:inline-flex;align-items:center;justify-content:center;font-size:14px;
    border:1.5px solid rgba(255,255,255,.22);color:var(--mut)}
  #lvis .s6.l802 .журнал li.есть{color:${ИНК}}
  #lvis .s6.l802 .журнал li.есть i{border-color:${GREEN};color:${GREEN};background:rgba(143,209,168,.14)}
  #lvis .s6.l802 .журнал li span{margin-left:auto;font-variant-numeric:tabular-nums;color:var(--mut);font-size:16px}

  /* ── рычаг разности ── */
  #lvis .s6.l802 .рычаг{display:flex;align-items:center;gap:10px;width:100%;padding:12px;border-radius:16px;
    background:rgba(127,209,195,.08);border:1.5px solid rgba(127,209,195,.34)}
  #lvis .s6.l802 .рычаг .сколько{flex:1;min-width:0;text-align:center;font-size:16px;line-height:1.25;color:${ИНК}}
  #lvis .s6.l802 .рычаг .сколько b{display:block;font-size:32px;color:${ТИР};font-variant-numeric:tabular-nums}
  #lvis .s6.l802 .рычаг button{width:56px;height:56px;flex:0 0 56px;border-radius:14px;cursor:pointer;
    border:1px solid rgba(127,209,195,.5);background:rgba(127,209,195,.14);color:${ТИР};
    font:inherit;font-size:24px;line-height:1;touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 140ms cubic-bezier(.23,1,.32,1),background 140ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l802 .рычаг button:active{transform:translateY(2px);background:rgba(127,209,195,.26)}
  #lvis .s6.l802 .рычаг button:disabled{opacity:.35;cursor:not-allowed}

  #lvis .s6.l802 .дело{width:100%;min-height:60px;padding:16px;border-radius:16px;cursor:pointer;
    font:inherit;font-size:20px;line-height:1.25;text-align:center;color:#10201d;
    border:0;background:linear-gradient(180deg,${ТИР},#3f9c8d);
    box-shadow:0 3px 0 rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.4);
    transition:transform 120ms ease, box-shadow 180ms ease}
  #lvis .s6.l802 .дело:active{transform:translateY(2px);box-shadow:0 1px 0 rgba(0,0,0,.42)}
  #lvis .s6.l802 .дело.сделано{background:rgba(143,209,168,.16);color:${GREEN};
    box-shadow:none;border:1.5px solid ${GREEN}}
  #lvis .s6.l802 .дело:disabled{background:rgba(255,255,255,.07);color:var(--mut);
    box-shadow:none;border:1.5px solid rgba(255,255,255,.16);cursor:not-allowed}

  #lvis .s6.l802 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l802 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;
    overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l802 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l802 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l802 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l802 .ask.пара button{flex:1 1 calc(50% - 6px);text-align:center}

  #lvis .s6.l802 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l802 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l802 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l802 .уровни .точка.сейчас{background:${ТИР};animation:l802dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l802dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l802 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l802{-webkit-text-size-adjust:100%}
  #lvis .s6.l802 [data-anim]{animation:l802rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l802rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l802 [data-anim]{animation:none!important}
    #lvis .s6.l802 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l802 .ask button,#lvis .s6.l802 .рычаг button,#lvis .s6.l802 .дело{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l802-style');
      if(!s){ s=document.createElement('style'); s.id='l802-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" onclick="${on}">${html}</button>`;
  const ЗАДАЧА = (текст) => A(2,'карт задача','<span class="метка">Веха</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const СКАЗ = (метка,текст) => A(9,'карт','<span class="метка">'+метка+'</span><div class="текст">'+текст+'</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));
  const ДЕЛО = (текст,on,сделано,можно) =>
    `<button type="button" data-anim style="--i:3" class="дело${сделано?' сделано':''}"
       ${можно===false&&!сделано?'disabled':''} onclick="${on}">${текст}</button>`;

  const ЖУРНАЛ = (s) => A(0,'журнал',
    `<div class="шапка"><span>Крен</span><b class="${s.перенесено?'ровно':''}">${s.перенесено?'выровнен':'6 мешков'}</b></div>
     <ul>${ВЕХИ.map(в=>{
       const есть = взята(s,в.ключ);
       return `<li class="${есть?'есть':''}"><i>${есть?'✓':'·'}</i>${в.имя}<span>${в.числа}</span></li>`;
     }).join('')}</ul>`);

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анРаз = (имя,значения,длит,задержка) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" fill="freeze"
              ${задержка?'begin="'+задержка+'"':''}
              calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>` : '';
  const анТРаз = (значения,длит,задержка) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}"
              fill="freeze" calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"
              ${задержка?'begin="'+задержка+'"':''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.55;0.55;1;1',длит,'0;'+кт(доля)+';'+кт(конец)+';1');
  const очередь = (номер,всего,длит) => {
    if(!ДВИЖ) return '';
    const д = 1/(всего+1), a = кт(номер*д), b = кт(номер*д+д*0.5), c = кт(номер*д+д*0.9);
    return анК('opacity','0.6;0.6;1;1;0.6',длит,'0;'+a+';'+b+';'+c+';1');
  };
  /* вспышка по обводу группы: круг у широких рядов садится поверх подписей */
  const рамкаВсп = (x0,y0,x1,y1,цвет,длит,доля) => ДВИЖ ? `<rect x="${x0}" y="${y0}"
      width="${x1-x0}" height="${y1-y0}" rx="10" fill="none" stroke="${цвет||ТИР}"
      stroke-width="2.2" opacity="0">
      ${анК('opacity','0;0;0.95;0',длит,'0;'+кт(доля)+';'+кт(доля+0.1)+';1')}
    </rect>` : '';

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c802-небо" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#2c4b57"/><stop offset="0.6" stop-color="#3f6f73"/>
        <stop offset="1" stop-color="#5d8f86"/>
      </linearGradient>
      <linearGradient id="c802-фон" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#22403c"/><stop offset="0.55" stop-color="#17302c"/>
        <stop offset="1" stop-color="#0d1c1a"/>
      </linearGradient>
      <linearGradient id="c802-море" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#7ec6d6"/><stop offset="1" stop-color="#1f5a71"/>
      </linearGradient>
      <linearGradient id="c802-дуб" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#c79a5a"/><stop offset="0.6" stop-color="#a97c40"/>
        <stop offset="1" stop-color="#6d4a22"/>
      </linearGradient>
      <linearGradient id="c802-мешок" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#a98a54"/><stop offset="0.35" stop-color="#e0c28a"/>
        <stop offset="1" stop-color="#8f7442"/>
      </linearGradient>
      <linearGradient id="c802-лишек" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#b8503f"/><stop offset="0.35" stop-color="#f09280"/>
        <stop offset="1" stop-color="#9c4132"/>
      </linearGradient>
      <linearGradient id="c802-добавка" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#4f9c72"/><stop offset="0.35" stop-color="#a8e6c2"/>
        <stop offset="1" stop-color="#3f7f5c"/>
      </linearGradient>
      <linearGradient id="c802-вода" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="#bfe6ff"/><stop offset="0.3" stop-color="#7fd1ff"/>
        <stop offset="1" stop-color="#2b6d99"/>
      </linearGradient>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||240}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c802-фон)"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  /* Подпись-плашка. Центр зажат с запасом 6 px: на прежних уроках подписи
     уже срезались кромкой кадра. */
  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||14, ш=String(текст).length*к*0.6+16, в=к+10;
    const cx = Math.min(336-ш/2-6, Math.max(ш/2+6, x));
    return `<g>
      <rect x="${(cx-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(8,18,17,.88)" stroke="${цвет||ТИР}" stroke-width="1.2"/>
      ${т(cx,y-2,текст,к,цвет||ТИР,true)}
    </g>`;
  };

  /* ---------------- СТОПКА МЕШКОВ ----------------------------------------
     Главная мера урока. Мешки считаются штуками, поэтому рисуются штуками:
     верхние `лишних` покрашены — это и есть разность, её видно глазами. */
  const В_РЯДУ = 3, РЯД = 8.4, ШАГ_X = 11.4;
  const мешок = (x,y,залив) => `<g transform="translate(${x.toFixed(1)} ${y.toFixed(1)})">
    <path d="M-5.4 4.4 q-2.6 -3.2 -1.5 -6.2 q0.8 -2.3 2.4 -3.3 l-0.7 -1.7 h5.4 l-0.7 1.7
             q1.6 1 2.4 3.3 q1.1 3 -1.5 6.2 z"
      fill="${залив}" stroke="rgba(18,12,5,.65)" stroke-width="0.7"/>
    <path d="M-2.7 -5.2 h5.4" stroke="rgba(18,12,5,.55)" stroke-width="0.7"/>
    <path d="M-3.4 1.6 q3.4 1.6 6.8 0" fill="none" stroke="rgba(255,240,210,.22)" stroke-width="0.7"/>
  </g>`;
  /* Куча мешков: заполняется снизу рядами по три, верхние `лишних` покрашены —
     это и есть разность, и она видна целыми рядами. */
  const куча = (x,низ,n,лишних,цветЛишних) => {
    const л = лишних||0;
    return Array.from({length:n},(_,i)=>{
      const ряд = Math.floor(i/В_РЯДУ), место = i%В_РЯДУ;
      const вРяду = Math.min(В_РЯДУ, n-ряд*В_РЯДУ);
      const x0 = x - (вРяду-1)*ШАГ_X/2 + место*ШАГ_X + (ряд%2?1.4:-1.4);
      const y0 = низ - 4.4 - ряд*РЯД;
      return мешок(x0, y0, (i>=n-л) ? (цветЛишних||'url(#c802-лишек)') : 'url(#c802-мешок)');
    }).join('');
  };

  /* ---------------- ШЛЮПКА С КОРМЫ ---------------------------------------
     Вид с кормы — единственный, на котором крен видно. Корпус и груз
     поворачиваются вместе, вода рисуется поверх и не поворачивается. */
  const шлюпка = (x,y,крен,левых,правых,лишних,цветЛишних,выпрямить) => {
    const ш=(n)=>n.toFixed(2);
    return `<g transform="translate(${x} ${y})"><g transform="rotate(${крен.toFixed(1)})">
      ${выпрямить&&ДВИЖ?`<animateTransform attributeName="transform" type="rotate"
        values="${выпрямить};${крен.toFixed(1)}" dur="820ms" fill="freeze"
        calcMode="spline" keyTimes="0;1" keySplines="${КРИВАЯ}"/>`:''}
      <path d="M-78 0 L-66 46 q66 18 132 0 L78 0 z" fill="url(#c802-дуб)"
        stroke="${ОБВОД}" stroke-width="${ш(1.6)}"/>
      ${[1,2,3,4].map(к=>`<path d="M${(-78+к*2.9).toFixed(1)} ${(к*9.2).toFixed(1)}
        q${(78-к*2.9).toFixed(1)} ${(15-к*2).toFixed(1)} ${(156-к*5.8).toFixed(1)} 0"
        fill="none" stroke="rgba(30,18,6,.26)" stroke-width="${ш(1.1)}"/>`).join('')}
      ${[-44,0,44].map(о=>`<line x1="${о}" y1="3" x2="${(о*0.86).toFixed(1)}" y2="44"
        stroke="rgba(30,18,6,.28)" stroke-width="${ш(1.3)}"/>`).join('')}
      <path d="M-66 46 q66 18 132 0" fill="none" stroke="rgba(20,12,4,.5)" stroke-width="${ш(2.6)}"/>
      <rect x="-80" y="-7" width="160" height="8" rx="3.4" fill="#c9ab7c"
        stroke="${ОБВОД}" stroke-width="${ш(1.1)}"/>
      ${куча(-34,-8,левых,цветЛишних?лишних:0,цветЛишних)}
      ${куча(34,-8,правых,цветЛишних?0:лишних)}
    </g></g>`;
  };

  /* вода поверх корпуса: видно, что тяжёлый борт сидит глубже */
  const волна = (высота,уровень) => `<g>
    <rect x="0" y="${уровень}" width="336" height="${высота-уровень}" fill="url(#c802-море)" opacity=".72"/>
    <path d="M0 ${уровень} q28 -4 56 0 q28 4 56 0 q28 -4 56 0 q28 4 56 0 q28 -4 56 0 q28 4 56 0"
      fill="none" stroke="#b9e6f2" stroke-width="1.6" opacity=".6">
      ${ДВИЖ?`<animateTransform attributeName="transform" type="translate"
         values="0 0;-56 0" dur="6s" repeatCount="indefinite"/>`:''}
    </path></g>`;

  /* ---------------- ПОЛОСКА-СХЕМА ----------------------------------------
     Переход от предмета к чертежу: та же стопка, положенная набок, с делениями
     по одному мешку — значит, её по-прежнему можно пересчитать. */
  const ЕД = 6.6;
  const полоска = (x,y,n,лишних,вид,подпись_,живой) => {
    const л = лишних||0, осн = n-л, в = 24;
    const залив = вид==='добавка' ? 'url(#c802-добавка)' : 'url(#c802-лишек)';
    return `<g>
      <rect x="${x}" y="${y}" width="${(осн*ЕД).toFixed(1)}" height="${в}" rx="3"
        fill="url(#c802-мешок)" stroke="${ОБВОД}" stroke-width="1.2"/>
      ${л?`<g>${живой?проявить('6s',0.42,0.6):''}
        <rect x="${(x+осн*ЕД).toFixed(1)}" y="${y}" width="${(л*ЕД).toFixed(1)}" height="${в}" rx="3"
        fill="${залив}" stroke="${ОБВОД}" stroke-width="1.2"/></g>`:''}
      ${Array.from({length:n-1},(_,i)=>`<line x1="${(x+(i+1)*ЕД).toFixed(1)}" y1="${y+(((i+1)%5)?в-7:2)}"
        x2="${(x+(i+1)*ЕД).toFixed(1)}" y2="${y+в-2}" stroke="rgba(20,14,6,.4)" stroke-width="0.9"/>`).join('')}
      ${подпись_?т(x-8,y+17,подпись_,14,МУТ,false,'end'):''}
    </g>`;
  };

  /* скобка над отрезком: показывает, о какой части идёт речь */
  const скобка = (x0,x1,y,текст,цвет,вниз) => {
    const з = вниз?1:-1, c=(x0+x1)/2;
    return `<g>
      <path d="M${x0} ${y} v${з*5} M${x1} ${y} v${з*5} M${x0} ${y+з*5} H${x1}"
        fill="none" stroke="${цвет||GOLD}" stroke-width="1.6"/>
      ${текст?т(c,y+з*(вниз?20:10),текст,14,цвет||GOLD,true):''}
    </g>`;
  };

  /* ---------------- БОЧОНОК С МЕРНОЙ ШКАЛОЙ ------------------------------ */
  const МАКС = 20;                  /* высота бочонка в кружках */
  const бочонок = (x,y,кружек,было,подпись_) => {
    const В=80, Ш=38;
    const ур = (к)=> -В/2 + В*(1-Math.max(0,Math.min(МАКС,к))/МАКС);
    const y1=ур(кружек), y0=ур(было==null?кружек:было);
    return `<g transform="translate(${x} ${y})">
      <path d="M-21 -40 q-5 40 0 80 h42 q5 -40 0 -80 z" fill="url(#c802-дуб)" stroke="${ОБВОД}" stroke-width="1.4"/>
      <g clip-path="url(#c802-нутро${x})">
        <rect x="-19" y="-40" width="38" height="80" fill="rgba(8,20,18,.6)"/>
        <rect x="-19" y="${y0.toFixed(1)}" width="38" height="${Math.max(0,40-y0).toFixed(1)}" fill="url(#c802-вода)" opacity=".95">
          ${анРаз('y', y0.toFixed(1)+';'+y1.toFixed(1), '620ms')}
          ${анРаз('height', Math.max(0,40-y0).toFixed(1)+';'+Math.max(0,40-y1).toFixed(1), '620ms')}
        </rect>
        ${кружек>0?`<path d="M-19 ${(y1+1).toFixed(1)} q9 -2.6 19 0 q10 2.6 19 0 v2.6 h-38 z" fill="#dff2ff" opacity=".45">
          ${анРаз('d','M-19 '+(y0+1).toFixed(1)+' q9 -3.4 19 0 q10 3.4 19 0 v4 h-38 z;M-19 '+(y1+1).toFixed(1)+' q9 -3.4 19 0 q10 3.4 19 0 v4 h-38 z','620ms')}
          ${ДВИЖ?`<animateTransform attributeName="transform" type="translate"
             values="0 0;1.8 0;0 0;-1.8 0;0 0" dur="3.8s" repeatCount="indefinite"/>`:''}
        </path>`:''}
      </g>
      <defs><clipPath id="c802-нутро${x}"><rect x="-19" y="-40" width="38" height="80" rx="5"/></clipPath></defs>
      ${[5,10,15].map(к=>`<line x1="19" y1="${ур(к).toFixed(1)}" x2="25" y2="${ур(к).toFixed(1)}"
        stroke="${МУТ}" stroke-width="1.4"/>`).join('')}
      ${[-26,0,26].map(о=>`<rect x="-23" y="${о-2.6}" width="46" height="5.2" rx="1.8"
        fill="#cbb89a" opacity=".92" stroke="${ОБВОД}" stroke-width="0.7"/>`).join('')}
      <path d="M-21 -40 q-5 40 0 80 h42 q5 -40 0 -80 z" fill="none" stroke="${ОБВОД}" stroke-width="1.4"/>
      ${подпись_?т(0,58,подпись_,14,МУТ):''}
    </g>`;
  };

  /* ---------------- ВЕХА НА БЕРЕГУ ---------------------------------------- */
  const веха = (x,низ,в) => `<g>
    <line x1="${x}" y1="${низ}" x2="${x}" y2="${низ-в}" stroke="#8a6f3f" stroke-width="2"/>
    <path d="M${x} ${низ-в} l9 4 l-9 4 z" fill="${GOLD}" stroke="${ОБВОД}" stroke-width="0.8"/>
  </g>`;

  /* ================= КАДРЫ ================= */
  const СУММА = 40, РАЗН = 6;
  const МЕНЬШЕ = (СУММА-РАЗН)/2;      /* 17 */
  const БОЛЬШЕ = МЕНЬШЕ+РАЗН;         /* 23 */

  /* 1. Веха 1: шлюпку кренит */
  function F1(s){
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Шлюпка отошла от берега и сразу легла на правый борт. В неё погрузили <b>40 мешков</b>, и справа их оказалось <b>на 6 больше</b>. Сколько мешков на каждом борту?') +
      `<div class="pic">${свг(`
        ${фон(242)}
        ${шлюпка(168,172,6,МЕНЬШЕ,БОЛЬШЕ,РАЗН)}
        ${волна(242,196)}
        ${подпись(168,24,'Шлюпку кренит',ТИР,16)}
        ${подпись(168,52,'всего 40 мешков',GOLD,14)}
        ${подпись(262,88,'справа на 6 больше',RED,14)}
        ${рамкаВсп(189,95,230,124,RED,'7s',0.2)}
      `,242)}</div>` +
      СКАЗ('Смотри','Красные мешки наверху правого борта — это и есть <b>разность</b>. Их видно: шесть штук. Именно из-за них лодка кренится.') +
      ПРАВИЛО('В таких задачах всегда известны две вещи: <b>сумма</b> — сколько всего, и <b>разность</b> — на сколько у одного больше.');
  }

  /* 2. Песочница: сумма та же, разность твоя */
  function F2(s){
    const D = Math.max(0, Math.min(20, s.разность==null?6:s.разность));
    const м = (СУММА-D)/2, б = м+D;
    const крен = Math.min(12, D*0.8);
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Мешков всегда сорок — их не убавить и не прибавить. Меняй только разность и смотри, что делается с бортами и с креном.') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${шлюпка(168,176,крен,м,б,D)}
        ${волна(256,202)}
        ${подпись(168,24,'Сумма та же, разность твоя',ТИР,14)}
        ${подпись(168,52,'слева '+м+' · справа '+б,GOLD,14)}
        <g>${проявить('7s',0.55,0.68)}
          ${подпись(168,246, D===0?'крен ушёл: по 20 на борт':('разность '+D+' — борт перевешивает'),
            D===0?GREEN:МУТ, 14)}</g>
      `,256)}</div>` +
      A(3,'рычаг',
        `<button type="button" onclick="r802Разн(-2)" ${D>0?'':'disabled'} aria-label="уменьшить разность">−</button>
         <span class="сколько">разность<b>${D}</b></span>
         <button type="button" onclick="r802Разн(2)" ${D<20?'':'disabled'} aria-label="увеличить разность">+</button>`) +
      СКАЗ('Смотри', D===0
        ? 'Разности нет — и борта равны: <b>40 : 2 = 20</b>. Вот от чего всё считается.'
        : 'Слева '+м+', справа '+б+'. Вместе всё те же 40, но разность <b>'+D+'</b> — и половина её ушла с одного борта на другой.') +
      ПРАВИЛО('Сумма делится пополам — это <b>ровный борт</b>. Разность уводит от него в обе стороны.');
  }

  /* 3. Способ первый: снять лишнее */
  function F3(s){
    const в = s.о1, верно = в==='ok', снято = !!s.снято;
    const прав = снято ? МЕНЬШЕ : БОЛЬШЕ;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Положим стопки набок — получится чертёж. Верхние шесть мешков лишние: <b>сними их</b>, и борта сравняются.') +
      `<div class="pic">${свг(`
        ${фон(254)}
        ${подпись(168,24,'Способ 1 · снять лишнее',ТИР,14)}
        ${полоска(96,86,МЕНЬШЕ,0,'','левый')}
        ${полоска(96,126,прав,снято?0:РАЗН,'лишек','правый',!снято)}
        ${снято?'':скобка(96+МЕНЬШЕ*ЕД,96+БОЛЬШЕ*ЕД,122,'лишние 6',RED,false)}
        ${снято?рамкаВсп(92,82,96+МЕНЬШЕ*ЕД+4,154,GREEN,'6s',0.14):''}
        ${скобка(96,96+(снято?МЕНЬШЕ:БОЛЬШЕ)*ЕД,166, снято?'стало 34':'вместе 40', снято?GREEN:GOLD, true)}
        ${верно
          ? т(168,208,'40 − 6 = 34',16,ИНК) + подпись(168,240,'34 : 2 = 17 · больший 17 + 6 = 23',GREEN,14)
          : подпись(168,224,'убери лишние 6 — борта сравняются',GOLD,14)}
      `,254)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), '17 — на меньшем борту', "r802О1('ok')")}
        ${BTN(5, в==='no'?'miss':'', '20 — половина от сорока', "r802О1('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Снял лишние 6 — осталось 34, и теперь борта равны: 34 : 2 = 17. Это меньший борт, а больший на 6 больше — 23.'
              : 'Нет: 20 — это по скольку было бы, если бы борта были равны. Но они не равны. Сначала убери разность: 40 − 6 = 34, и только потом дели: 34 : 2 = 17.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ДЕЛО(снято ? '✓ Лишние 6 сняты на берег' : 'Снять лишние 6 мешков', "r802Снять()", снято, верно) +
      ПРАВИЛО('<b>Меньшее = (сумма − разность) : 2.</b>');
  }

  /* 4. Способ второй: добавить до ровного */
  function F4(s){
    const в = s.о2, верно = в==='ok', долож = !!s.доложено;
    const лев = долож ? БОЛЬШЕ : МЕНЬШЕ;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Можно наоборот: не снимать лишнее, а <b>доложить недостающее</b>. Добавим на лёгкий борт 6 мешков — и борта опять сравняются, только по большему.') +
      `<div class="pic">${свг(`
        ${фон(254)}
        ${подпись(168,24,'Способ 2 · доложить до ровного',ТИР,14)}
        ${полоска(96,86,лев,долож?РАЗН:0,'добавка','левый',долож)}
        ${полоска(96,126,БОЛЬШЕ,0,'','правый')}
        ${долож?'':`<g>${проявить('6s',0.42,0.6)}${скобка(96+МЕНЬШЕ*ЕД,96+БОЛЬШЕ*ЕД,82,'не хватает 6',GREEN,false)}</g>`}
        ${долож?рамкаВсп(92,82,96+БОЛЬШЕ*ЕД+4,154,GREEN,'6s',0.14):''}
        ${скобка(96,96+БОЛЬШЕ*ЕД,166, долож?'стало 46':'вместе 40', долож?GREEN:GOLD, true)}
        ${верно
          ? т(168,208,'40 + 6 = 46',16,ИНК) + подпись(168,240,'46 : 2 = 23 · меньший 23 − 6 = 17',GREEN,14)
          : подпись(168,224,'доложи 6 — борта сравняются',GOLD,14)}
      `,254)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), '23 — на большем борту', "r802О2('ok')")}
        ${BTN(5, в==='no'?'miss':'', '26 — двадцать да ещё шесть', "r802О2('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Доложил 6 — стало 46, борта равны: 46 : 2 = 23. Это больший борт, меньший на 6 меньше — 17.'
              : 'Нет: 26 и 17 вместе дают 43, а мешков 40. Прибавлять разность к половине суммы нельзя — сначала прибавь её к сумме: 40 + 6 = 46, и подели: 46 : 2 = 23.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ДЕЛО(долож ? '✓ Доложено 6 мешков' : 'Доложить 6 мешков', "r802Доложить()", долож, верно) +
      ПРАВИЛО('<b>Большее = (сумма + разность) : 2.</b>');
  }

  /* 5. Морской способ: перенести половину разности. Главный кадр урока. */
  function F5(s){
    const в = s.о3, верно = в==='ok', перен = !!s.перенесено;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('А теперь по-морскому. Ничего не снимаем и не докладываем — <b>переносим</b> мешки с тяжёлого борта на лёгкий. Сколько перенести, чтобы шлюпка встала ровно?') +
      `<div class="pic">${свг(`
        ${фон(258)}
        ${перен
          ? шлюпка(168,176,0,20,20,3,'url(#c802-добавка)','6')
          : шлюпка(168,176,6,МЕНЬШЕ,БОЛЬШЕ,РАЗН)}
        ${волна(258,200)}
        ${подпись(168,24,'Морской способ · перенести половину',ТИР,14)}
        ${подпись(168,52, перен?'по 20 на борт — крен ушёл':'разность 6 · половина её 3',
          перен?GREEN:GOLD, 14)}
        ${перен?рамкаВсп(112,105,224,172,GREEN,'7s',0.18):''}
        ${перен?подпись(168,248,'40 : 2 = 20',GREEN,20):''}
      `,258)}</div>` +
      `<div class="ask пара">
        ${BTN(4, верно?'hit':(в?'miss':''), '3 мешка', "r802О3('ok')")}
        ${BTN(5, в==='no'?'miss':'', '6 мешков', "r802О3('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно: половину разности, то есть 3. Мешок ушёл с правого борта — там стало на один меньше, а на левом на один больше, и разница сократилась сразу на два. Три мешка съели всю разность в шесть.'
              : 'Нет: если перенести все 6, правый борт станет лёгким, а левый тяжёлым — крен просто перевернётся. Каждый перенесённый мешок меняет разницу на <b>два</b>, поэтому переносят её половину: 6 : 2 = 3.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ДЕЛО(перен ? '✓ Три мешка перенесены, крен ушёл' : 'Перенести 3 мешка налево', "r802Перенести()", перен, верно) +
      ПРАВИЛО('Ровный борт — это <b>сумма пополам</b>. Больший борт на половину разности выше, меньший — на столько же ниже.');
  }

  /* 6. Проверка: два замера, оба обязательны */
  function F6(s){
    const сум = !!s.суммаОК, раз = !!s.разностьОК;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Ответ 17 и 23 нельзя просто записать — его <b>проверяют дважды</b>: сходится ли сумма и сходится ли разность. Проверь сам.') +
      `<div class="pic">${свг(`
        ${фон(238)}
        ${подпись(168,24,'Проверка',ТИР,16)}
        <g>${очередь(0,2,'6s')}${полоска(96,64,МЕНЬШЕ,0,'','17')}</g>
        <g>${очередь(1,2,'6s')}${полоска(96,104,БОЛЬШЕ,РАЗН,'лишек','23')}</g>
        ${раз?скобка(96+МЕНЬШЕ*ЕД,96+БОЛЬШЕ*ЕД,100,'',GREEN,false):''}
        ${раз?рамкаВсп(96+МЕНЬШЕ*ЕД-3,100,96+БОЛЬШЕ*ЕД+3,131,GREEN,'6s',0.16):''}
        ${сум?скобка(96,96+БОЛЬШЕ*ЕД,142,'',GOLD,true):''}
        ${подпись(168,182, сум?'17 + 23 = 40 ✓':'сумма — ?', сум?GREEN:МУТ, 16)}
        ${подпись(168,216, раз?'23 − 17 = 6 ✓':'разность — ?', раз?GREEN:МУТ, 16)}
      `,238)}</div>` +
      `<div class="ask пара">
        ${BTN(4, сум?'hit':'', 'Проверить сумму', "r802Проверка('сумма')")}
        ${BTN(5, раз?'hit':'', 'Проверить разность', "r802Проверка('разность')")}
      </div>` +
      СКАЗ('Зачем дважды', (сум&&раз)
        ? 'Оба замера сошлись — ответ верный. Одной проверки мало: числа <b>20 и 20</b> дают верную сумму, но нулевую разность, а <b>17 и 23</b> — и то, и другое.'
        : 'Нажми обе кнопки. Пара чисел подходит, только если сходятся <b>и сумма, и разность</b>.') +
      ПРАВИЛО('Проверка — часть решения, а не вежливость: <b>сумма и разность, оба раза</b>.');
  }

  /* 7. Ловушка темы */
  function F7(s){
    const в = s.о4, верно = в==='ok';
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Вот как чаще всего ошибаются: делят сумму пополам, а потом приписывают разность целиком — 40 : 2 = 20, значит «20 и 26». Что здесь не так?') +
      `<div class="pic">${свг(`
        ${фон(296)}
        ${подпись(168,24,'Ловушка',ТИР,16)}
        ${подпись(88,58,'так неверно',RED,14)}
        ${полоска(104,72,20,0,'','20')}
        ${полоска(104,104,26,6,'лишек','26')}
        <g>${проявить('6s',0.5,0.62)}${подпись(168,158,'20 + 26 = 46, а мешков 40',RED,14)}</g>
        <line x1="24" y1="172" x2="312" y2="172" stroke="rgba(255,255,255,.16)" stroke-width="1.2"/>
        ${подпись(88,198,'так верно',GREEN,14)}
        ${полоска(104,208,МЕНЬШЕ,0,'','17')}
        ${полоска(104,240,БОЛЬШЕ,РАЗН,'лишек','23')}
        ${верно?рамкаВсп(100,204,100+БОЛЬШЕ*ЕД+8,268,GREEN,'6s',0.2):''}
        ${подпись(168,286,'17 + 23 = 40 · разность 6',GREEN,14)}
      `,296)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), 'Вместе выходит 46, а мешков 40', "r802О4('ok')")}
        ${BTN(5, в==='no'?'miss':'', 'Разность выходит не 6', "r802О4('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно. Разность у 20 и 26 как раз правильная — шесть. Ломается сумма: 20 + 26 = 46, шесть лишних мешков взялись из ниоткуда. Разность нужно было разделить пополам: 20 − 3 и 20 + 3.'
              : 'Нет, разность-то как раз сошлась: 26 − 20 = 6. Сломалась сумма: 20 + 26 = 46, а мешков 40. Половину суммы нельзя трогать целой разностью — только её половиной.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Половину суммы двигают на <b>половину разности</b>, а не на всю.');
  }

  /* 8. Веха 2: пресная вода */
  function F8(s){
    const в = s.о5, верно = в==='ok', перелито = !!s.перелито;
    const л = перелито?15:13, п = перелито?15:17;
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Вторая веха. В двух бочонках <b>30 кружек</b> воды, в правом <b>на 4 больше</b>. Сколько в каждом?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${подпись(168,24,'Веха 2 · пресная вода',ТИР,16)}
        ${подпись(168,54,'вместе 30 · справа на 4 больше',GOLD,14)}
        ${бочонок(92,134,л,перелито?13:null, верно?('левый '+л):'левый ?')}
        ${бочонок(244,134,п,перелито?17:null, верно?('правый '+п):'правый ?')}
        ${перелито&&ДВИЖ?`<path d="M232 96 C202 58 134 58 104 96" fill="none" stroke="#7fd1ff"
          stroke-width="3" stroke-linecap="round" stroke-dasharray="4 6" opacity="0">
          ${анРаз('opacity','0;0.9','300ms')}
          <animate attributeName="stroke-dashoffset" values="0;-20" dur="0.7s" repeatCount="indefinite"/>
        </path>`:''}
        ${перелито?рамкаВсп(64,86,272,182,GREEN,'6s',0.16):''}
        ${верно?подпись(168,222,'(30 − 4) : 2 = 13 · 13 + 4 = 17',GREEN,14)
               :подпись(168,222,'сними разность — потом дели',GOLD,14)}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), 'Слева 13, справа 17', "r802О5('ok')")}
        ${BTN(5, в==='no'?'miss':'', 'Слева 15, справа 19', "r802О5('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно: (30 − 4) : 2 = 13 и 13 + 4 = 17. Проверка: 13 + 17 = 30, 17 − 13 = 4.'
              : 'Нет: 15 и 19 вместе дают 34, а воды 30. Опять половину суммы сдвинули на всю разность вместо половины.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ДЕЛО(перелито ? '✓ Перелито 2 кружки, стало поровну' : 'Перелить 2 кружки налево', "r802Перелить()", перелито, верно) +
      ПРАВИЛО('Тот же приём на другом грузе: <b>разность пополам — и поровну</b>.');
  }

  /* 9. Веха 3: два дня пути — та же схема, но предмета уже нет */
  function F9(s){
    const в = s.о6, верно = в==='ok';
    return ЖУРНАЛ(s) +
      ЗАДАЧА('Третья веха, и переносить уже нечего. За два дня прошли <b>54 вехи</b>, во второй день — <b>на 10 больше</b>. Сколько вех прошли в первый день?') +
      `<div class="pic">${свг(`
        ${фон(256)}
        ${подпись(168,24,'Веха 3 · два дня пути',ТИР,16)}
        ${подпись(168,54,'всего 54 · во второй на 10 больше',GOLD,14)}
        <line x1="16" y1="100" x2="320" y2="100" stroke="#6b5a3a" stroke-width="2"/>
        ${[0,1,2,3,4,5,6,7].map(i=>`<g>${очередь(i,8,'8s')}${веха(30+i*38,100,16)}</g>`).join('')}
        ${полоска(104,124,22,0,'','1-й день')}
        ${полоска(104,160,32,10,'лишек','2-й день')}
        ${т(104+27*ЕД,177,'10',16,'#fff5f0',true)}
        ${верно?рамкаВсп(100,120,104+22*ЕД+4,150,GREEN,'6s',0.18):''}
        ${подпись(120,214,'всего 54',GOLD,14)}
        ${подпись(252,214,'разность 10',RED,14)}
        ${верно?подпись(168,246,'(54 − 10) : 2 = 22 · и 22 + 10 = 32',GREEN,14)
               :подпись(168,246,'та же схема, только без мешков',МУТ,14)}
      `,256)}</div>` +
      `<div class="ask">
        ${BTN(4, верно?'hit':(в?'miss':''), '22 вехи в первый день', "r802О6('ok')")}
        ${BTN(5, в==='no'?'miss':'', '27 вех в первый день', "r802О6('no')")}
      </div>` +
      (в!=null ? РАЗБОР(верно,
        верно ? 'Верно: (54 − 10) : 2 = 22, а во второй день 22 + 10 = 32. Проверка: 22 + 32 = 54, 32 − 22 = 10. Мешков нет, а способ тот же.'
              : 'Нет: 27 — это половина от 54, то есть «если бы поровну». Сначала сними разность: 54 − 10 = 44, и подели: 44 : 2 = 22.')
        : СКАЗ('Ответ','Выбери вариант выше.')) +
      ПРАВИЛО('Схема не зависит от груза: <b>мешки, кружки, вехи — всё одно</b>.');
  }

  /* 10. Итог перехода */
  function F10(s){
    const всё = ВЕХИ.every(в=>взята(s,в.ключ));
    return ЖУРНАЛ(s) +
      ЗАДАЧА(всё
        ? 'Журнал закрыт: три вехи взяты, крен выровнен. Шлюпка идёт ровно.'
        : 'В журнале отмечены не все вехи — вернись и доведи их. А пока посмотри, чем кончается переход.') +
      `<div class="pic">${свг(`
        <rect x="0" y="0" width="336" height="150" fill="url(#c802-небо)"/>
        <path d="M0 132 q46 -14 92 -4 q44 -12 88 2 q46 -12 92 -2 q34 -6 64 2 v22 H0 z"
          fill="#4a5a44" opacity=".85"/>
        <rect x="0" y="146" width="336" height="8" fill="#8d8163" opacity=".75"/>
        ${[0,1,2,3,4,5,6,7].map(i=>`<g>${очередь(i,8,'8s')}${веха(28+i*40,146,16)}</g>`).join('')}
        <rect x="0" y="0" width="336" height="254" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>
        ${шлюпка(168,174,0,20,20,0)}
        ${волна(254,198)}
        ${рамкаВсп(112,103,224,170,ТИР,'8s',0.18)}
        ${подпись(168,24,'Ровный киль',ТИР,16)}
        ${подпись(168,62,'17 + 23 = 40 · 23 − 17 = 6',GREEN,14)}
        ${т(168,242,'сумма и разность сошлись — курс держим',14,МУТ)}
      `,254)}</div>` +
      СКАЗ('Итог перехода','Три вехи — три одинаковые задачи на разном грузе: 40 и 6 → 17 и 23; 30 и 4 → 13 и 17; 54 и 10 → 22 и 32. Каждый раз сначала убирали разность, делили пополам и возвращали её обратно.') +
      ПРАВИЛО('<b>Меньшее = (сумма − разность) : 2. Большее = меньшее + разность.</b>');
  }

  /* ================= ПРАКТИКА ================= */
  const УРОВНИ = [
    { вопрос:'У двух лодок вместе 24 весла, у одной на 4 больше. Сколько вёсел у той, где меньше?',
      варианты:[{т:'10 вёсел',ок:true},{т:'12 вёсел',ок:false}],
      разбор:'(24 − 4) : 2 = 10, а у второй 10 + 4 = 14. Проверка: 10 + 14 = 24, 14 − 10 = 4.' },
    { вопрос:'В двух бочонках 26 кружек, в одном на 6 больше. Сколько в большем?',
      варианты:[{т:'16 кружек',ок:true},{т:'19 кружек',ок:false}],
      разбор:'(26 + 6) : 2 = 16, а в меньшем 16 − 6 = 10. Проверка: 16 + 10 = 26.' },
    { вопрос:'За два дня прошли 50 вех, во второй день на 8 больше. Сколько в первый?',
      варианты:[{т:'21 веха',ок:true},{т:'25 вех',ок:false}],
      разбор:'(50 − 8) : 2 = 21, во второй 21 + 8 = 29. Проверка: 21 + 29 = 50.' },
    { вопрос:'Сумма 36, разность 4. Можно ли ответить «18 и 22»?',
      варианты:[{т:'Нельзя: вместе выйдет 40',ок:true},{т:'Можно: разность верная',ок:false}],
      разбор:'Разность у 18 и 22 верная, но сумма ломается: 18 + 22 = 40, а надо 36. Верно 16 и 20.' },
    { вопрос:'У двоих 18 сухарей, у одного на 2 больше. Сколько отдать, чтобы стало поровну?',
      варианты:[{т:'1 сухарь',ок:true},{т:'2 сухаря',ок:false}],
      разбор:'Переносят половину разности: 2 : 2 = 1. После этого у каждого 18 : 2 = 9.' }
  ];

  function F11(s){
    const пройдено = s.практика||0;
    const уровень = Math.min(пройдено, УРОВНИ.length-1);
    const всё = пройдено>=УРОВНИ.length;
    const выбран = s.практикаУровень===уровень ? s.практикаВыбор : null;
    const у = УРОВНИ[уровень];
    if(всё){
      return ТОЧКИ(УРОВНИ.length,-1,УРОВНИ.length) +
        A(2,'карт верно','<span class="метка">Пять из пяти</span><div class="текст">✅ Все пять уровней пройдены. Дальше — три тренажёра.</div>') +
        `<div class="ask">${BTN(3,'','Пройти заново',"r802Reset()")}</div>` +
        ПРАВИЛО('<b>Меньшее = (сумма − разность) : 2.</b>');
    }
    return ТОЧКИ(УРОВНИ.length,уровень,пройдено) +
      ЗАДАЧА('Уровень '+(уровень+1)+' из '+УРОВНИ.length+'. '+у.вопрос) +
      `<div class="ask">` +
      у.варианты.map((в,к)=>BTN(3+к,
        выбран===к ? (в.ок?'hit':'miss') : '',
        в.т, "r802Pick("+уровень+","+к+")")).join('') +
      `</div>` +
      (выбран!=null ? РАЗБОР(у.варианты[выбран].ок, у.разбор) : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= ТРЕНАЖЁРЫ ================= */
  const Т1 = { имя:'Меньшее', задания:[
    {q:'Сумма 20, разность 4. Меньшее?', в:0, варианты:['8','10'], раз:'(20 − 4) : 2 = 8.'},
    {q:'Сумма 30, разность 10. Меньшее?', в:1, варианты:['15','10'], раз:'(30 − 10) : 2 = 10.'},
    {q:'Сумма 16, разность 2. Меньшее?', в:0, варианты:['7','8'], раз:'(16 − 2) : 2 = 7. Восемь — это если поровну.'},
    {q:'Сумма 50, разность 6. Меньшее?', в:1, варианты:['25','22'], раз:'(50 − 6) : 2 = 22.'}
  ]};
  const Т2 = { имя:'Большее', задания:[
    {q:'Сумма 20, разность 4. Большее?', в:0, варианты:['12','14'], раз:'(20 + 4) : 2 = 12.'},
    {q:'Сумма 28, разность 8. Большее?', в:1, варианты:['22','18'], раз:'(28 + 8) : 2 = 18.'},
    {q:'Сумма 40, разность 10. Большее?', в:0, варианты:['25','30'], раз:'(40 + 10) : 2 = 25.'},
    {q:'Сумма 14, разность 4. Большее?', в:1, варианты:['11','9'], раз:'(14 + 4) : 2 = 9.'}
  ]};
  const Т3 = { имя:'Проверка и перенос', задания:[
    {q:'Ответ «15 и 9» при сумме 24 и разности 6 — верен?', в:0, варианты:['Да','Нет'],
     раз:'15 + 9 = 24 ✓, 15 − 9 = 6 ✓ — сошлось дважды.'},
    {q:'Ответ «12 и 8» при сумме 20 и разности 6 — верен?', в:1, варианты:['Да','Нет'],
     раз:'Сумма сошлась, а разность нет: 12 − 8 = 4, а надо 6. Верно 13 и 7.'},
    {q:'Сумма 30, разность 8. Сколько перенести, чтобы стало поровну?', в:0, варианты:['4','8'],
     раз:'Половину разности: 8 : 2 = 4. После этого у каждого 15.'},
    {q:'Сумма 44, разность 0. Сколько у каждого?', в:1, варианты:['44','22'],
     раз:'Разности нет — значит просто пополам: 44 : 2 = 22.'}
  ]};

  function тренажёр(s,ключ,набор,номер){
    const шаг = (s[ключ+'Шаг']||0) % набор.задания.length;
    const з = набор.задания[шаг];
    const ответ = s[ключ+'Ответ'];
    const верно = s[ключ+'Верно']||0, ошибки = s[ключ+'Ошибки']||0;
    return ТОЧКИ(набор.задания.length,шаг,шаг) +
      A(1,'score','Тренажёр '+номер+' · '+набор.имя+' · верно '+верно+', ошибок '+ошибки) +
      ЗАДАЧА(з.q) +
      `<div class="ask пара">` +
      з.варианты.map((в,к)=>BTN(3+к,
        ответ===к ? (к===з.в?'hit':'miss') : (ответ!=null&&к===з.в?'hit':''),
        в, "r802T('"+ключ+"',"+к+")")).join('') +
      `</div>` +
      (ответ!=null
        ? РАЗБОР(ответ===з.в, з.раз) + `<div class="ask">${BTN(10,'','Следующее задание →',"r802TNext('"+ключ+"')")}</div>`
        : СКАЗ('Ответ','Выбери вариант выше.'));
  }

  /* ================= СБОРКА ================= */
  const L802 = {
    id: ID,
    title: 'Задачи на сумму и разность',
    ico: '⚖️',
    src: 'Начальная школа · 3 класс · Сумма и разность',
    subj: 'jun',
    explain: [
      'Шлюпку кренит: 40 мешков, справа на 6 больше. Красные мешки наверху — это и есть разность, её видно глазами.',
      'Мешков всегда сорок. Двигай разность и смотри: сумма пополам — это ровный борт, разность уводит от него в обе стороны.',
      'Способ первый: снять лишние 6, и борта сравняются. 40 − 6 = 34, 34 : 2 = 17 — меньший борт, больший 17 + 6 = 23.',
      'Способ второй: доложить недостающие 6. 40 + 6 = 46, 46 : 2 = 23 — больший борт, меньший 23 − 6 = 17.',
      'Морской способ: перенести половину разности. Каждый перенесённый мешок меняет разницу на два, поэтому переносят 3, и крен уходит: 40 : 2 = 20 на борт.',
      'Ответ проверяют дважды: сходится ли сумма (17 + 23 = 40) и сходится ли разность (23 − 17 = 6). Одной проверки мало.',
      'Ловушка: 40 : 2 = 20, а потом «20 и 26». Разность-то верная, но вместе выходит 46. Половину суммы двигают на половину разности.',
      'Веха вторая: 30 кружек воды, в правом бочонке на 4 больше. (30 − 4) : 2 = 13 и 17; перелил 2 — стало поровну.',
      'Веха третья: 54 вехи за два дня, во второй на 10 больше. Переносить нечего, а способ тот же: (54 − 10) : 2 = 22 и 32.',
      'Итог: три вехи — три одинаковые задачи на разном грузе. Сначала убрать разность, поделить пополам, вернуть обратно.',
      'Практика: пять уровней подряд.',
      'Тренажёр 1: меньшее число.',
      'Тренажёр 2: большее число.',
      'Тренажёр 3: проверка и перенос.'
    ],
    check: {
      q: 'Вместе 40 мешков, на правом борту на 6 больше. Сколько на левом?',
      choices: ['17 мешков','20 мешков','23 мешка'],
      ans: 0,
      exp: 'Сначала убираем разность: 40 − 6 = 34. Потом делим пополам: 34 : 2 = 17.'
    },
    tasks: [
      { q:'Вместе 40 мешков, справа на 6 больше. Сколько мешков слева?', kind:'choice',
        choices:['17','20'], ans:0,
        hints:['Двадцать — это если бы борта были равны.','Сними разность: 40 − 6 = 34, потом подели.'],
        sol:'(40 − 6) : 2 = 17.' },
      { q:'В двух бочонках 30 кружек, в правом на 4 больше. Сколько в правом?', kind:'choice',
        choices:['17','15'], ans:0,
        hints:['Ищем большее число.','(30 + 4) : 2.'],
        sol:'(30 + 4) : 2 = 17.' },
      { q:'Разность бортов 6 мешков. Сколько перенести, чтобы стало поровну?', kind:'choice',
        choices:['3','6'], ans:0,
        hints:['Каждый перенесённый мешок меняет разницу на два.','Переносят половину разности.'],
        sol:'6 : 2 = 3.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L802.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=тренажёр(s,'т1',Т1,1);
    else if(f===13) сцена=тренажёр(s,'т2',Т2,2);
    else сцена=тренажёр(s,'т3',Т3,3);

    const ЗАГОЛОВКИ={
      1:'Шлюпку кренит', 2:'Сумма и разность', 3:'Снять лишнее', 4:'Доложить до ровного',
      5:'Перенести половину', 6:'Проверка', 7:'Ловушка', 8:'Веха 2 · вода',
      9:'Веха 3 · два дня', 10:'Ровный киль', 11:'Практика',
      12:'Тренажёр 1', 13:'Тренажёр 2', 14:'Тренажёр 3'
    };
    el.innerHTML = `<div class="s6 l802" data-frame="${f}">
        <h2>${ЗАГОЛОВКИ[f]||'Сумма и разность'}</h2>
        ${сцена}
      </div>`;
  }

  window.r802Разн   =(д)=>{ const s=S();
    s.разность=Math.max(0,Math.min(20,(s.разность==null?6:s.разность)+д)); chRender(0); };
  window.r802О1     =(к)=>{ S().о1=к; chRender(0); };
  window.r802О2     =(к)=>{ S().о2=к; chRender(0); };
  window.r802О3     =(к)=>{ S().о3=к; chRender(0); };
  window.r802О4     =(к)=>{ S().о4=к; chRender(0); };
  window.r802О5     =(к)=>{ const s=S(); s.о5=к; if(к==='ok') s.веха_вода=true; chRender(0); };
  window.r802О6     =(к)=>{ const s=S(); s.о6=к; if(к==='ok') s.веха_путь=true; chRender(0); };
  window.r802Снять   =()=>{ const s=S(); s.снято=!s.снято; chRender(0); };
  window.r802Доложить=()=>{ const s=S(); s.доложено=!s.доложено; chRender(0); };
  window.r802Перенести=()=>{ const s=S(); s.перенесено=!s.перенесено;
    s.веха_груз=!!s.перенесено; chRender(0); };
  window.r802Перелить =()=>{ const s=S(); s.перелито=!s.перелито; chRender(0); };
  window.r802Проверка =(что)=>{ const s=S();
    if(что==='сумма') s.суммаОК=!s.суммаОК; else s.разностьОК=!s.разностьОК; chRender(0); };
  window.r802Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r802Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r802T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=(s[ключ+'Шаг']||0)%набор.задания.length;
    const з=набор.задания[шаг];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r802TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L802; else arr.push(L802);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA802={render:render, L:L802};
})();
