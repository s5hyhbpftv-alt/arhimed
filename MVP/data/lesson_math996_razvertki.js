/* ============ МАТЕМАТИКА · УРОК 996 · «РАЗВЁРТКИ ОБЪЁМНЫХ ТЕЛ» ============
   5 класс. Сюжет: мастерская коробок — от заготовки-развёртки к готовой коробке.

   ЭТАЛОН (995, 830, 932, 1032): движение в кадре — это САМА МАТЕМАТИКА.
     кадр 1  три объёмных тела оживают по очереди: у каждого вспыхивают грани;
     кадр 2  на параллелепипеде по очереди вспыхивают ГРАНЬ, РЕБРО и ВЕРШИНА,
             затем появляется счёт 6 · 12 · 8;
     кадр 3  развёртка и тело показаны рядом: шесть пар граней вспыхивают
             по очереди — видно, что это одна и та же фигура;
     кадр 4  шесть квадратов развёртки куба появляются по одному;
     кадр 5  три разные развёртки куба: в каждой вспыхивают шесть квадратов;
     кадр 6  развёртка параллелепипеда: три ПАРЫ равных прямоугольников;
     кадр 7  развёртка пирамиды: основание и четыре треугольника, затем пирамида;
     кадр 8  цилиндр: прямоугольник сворачивается в трубу, круги становятся
             основаниями;
     кадр 9  площадь поверхности: 54 единичных квадрата ЗАПОЛНЯЮТ развёртку
             куба по одному;
     кадр 10 параллелепипед 2×3×4: три пары граней заполняются единичными
             квадратами, сумма 52;
     кадр 11 практика из пяти уровней; кадры 12–14 — ТРИ ТРЕНАЖЁРА.

   КАЧЕСТВО ПРОРИСОВКИ: объёмные тела нарисованы по трём видимым граням
   с градиентами и обводкой, штриховые линии сгиба, подписи измерений,
   единичная сетка на гранях, мягкие тени.

   СВЕРКА С ПЕТЕРСОНОМ (5 класс): «прямоугольный параллелепипед, куб,
   развёртки», «объём параллелепипеда, развёртки», «площадь поверхности».
   Источники: план-1-9/петерсон_1-9.md, план-1-9/углублённая_5-9.md.

   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, ничего за край кадра, keyTimes строго в [0;1],
   prefers-reduced-motion выключает SMIL полностью. */
(function(){
  'use strict';

  const ID = 996;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const ЛИЛ='#c9a6f2', ОРА='#f2b06a';
  const ИНК='#f6efe0', МУТ='#cbb89a', ЛИНИЯ='#3f7a5f';
  const ТУШЬ='#1e3128';   /* подписи на светлой бумаге: контраст 12:1 */

  const CSS=`
  #lvis .s6.l996{gap:14px}
  #lvis .s6.l996 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l996 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l996 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l996 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l996 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l996 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l996 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l996 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l996 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l996 .правило b{color:${GOLD}}
  #lvis .s6.l996 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l996 .ask button{flex:1 1 44%;min-height:56px;padding:12px 14px;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l996 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l996 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l996 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l996 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l996 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l996 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l996 .уровни .точка.сейчас{background:${GOLD};animation:l996dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l996dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l996 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l996{-webkit-text-size-adjust:100%}
  #lvis .s6.l996 [data-anim]{animation:l996rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l996rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l996 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l996 [data-anim]{animation:none!important}
    #lvis .s6.l996 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l996 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l996-style');
      if(!s){ s=document.createElement('style'); s.id='l996-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анТ = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анП = (значения,длит,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const светит = (длит,нач) => ан('opacity','1;0.45;1',длит, нач!=null?'begin="'+нач+'s"':'');
  /* мягкая вспышка с задержкой: доля цикла задаётся в кт */
  const проявить = (длит,доля,конец) => ан('opacity','0.3;0.3;1;1',длит,'keyTimes="0;'+кт(доля)+';'+кт(конец)+';1"');

  /* ================= РИСУНКИ ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c996-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#26382e"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#131f18"/>
      </linearGradient>
      <linearGradient id="c996-перед" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#d9a13a"/><stop offset="0.5" stop-color="#b8842a"/>
        <stop offset="1" stop-color="#8a6116"/>
      </linearGradient>
      <linearGradient id="c996-верх" x1="0" y1="0" x2="1" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffe9a8"/><stop offset="0.5" stop-color="#f3cf6a"/>
        <stop offset="1" stop-color="#dcae43"/>
      </linearGradient>
      <linearGradient id="c996-бок" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#8e661a"/><stop offset="0.55" stop-color="#644711"/>
        <stop offset="1" stop-color="#3d2c08"/>
      </linearGradient>
      <linearGradient id="c996-перед2" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#8cc9ea"/><stop offset="0.45" stop-color="#5aa3cd"/>
        <stop offset="1" stop-color="#2b6488"/>
      </linearGradient>
      <linearGradient id="c996-верх2" x1="0" y1="0" x2="1" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#cfe8f7"/><stop offset="0.55" stop-color="#9fc9e4"/>
        <stop offset="1" stop-color="#79a9c9"/>
      </linearGradient>
      <linearGradient id="c996-бок2" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#5b9fc9"/><stop offset="0.6" stop-color="#356f96"/>
        <stop offset="1" stop-color="#22506e"/>
      </linearGradient>
      <linearGradient id="c996-перед3" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#9ed7b6"/><stop offset="0.45" stop-color="#6db894"/>
        <stop offset="1" stop-color="#38704f"/>
      </linearGradient>
      <linearGradient id="c996-верх3" x1="0" y1="0" x2="1" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#cdeeda"/><stop offset="0.55" stop-color="#9ccfae"/>
        <stop offset="1" stop-color="#7cb08f"/>
      </linearGradient>
      <linearGradient id="c996-бок3" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#6aa98a"/><stop offset="0.6" stop-color="#417f61"/>
        <stop offset="1" stop-color="#2c5c45"/>
      </linearGradient>
      <linearGradient id="c996-плоско" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fbf8ef"/><stop offset="0.6" stop-color="#ece4cf"/>
        <stop offset="1" stop-color="#d5caae"/>
      </linearGradient>
      <radialGradient id="c996-блик" cx="0.32" cy="0.28" r="0.72" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fffdf4" stop-opacity=".75"/>
        <stop offset="0.55" stop-color="#fff6dd" stop-opacity=".22"/>
        <stop offset="1" stop-color="#ffffff" stop-opacity="0"/>
      </radialGradient>
      <filter id="c996-тень" x="-45%" y="-45%" width="200%" height="200%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="4.2" result="размытие"/>
        <feOffset in="размытие" dx="4" dy="6" result="сдвиг"/>
        <feFlood flood-color="#0b1c2a" flood-opacity="0.5" result="цвет"/>
        <feComposite in="цвет" in2="сдвиг" operator="in"/>
      </filter>
      <filter id="c996-контакт" x="-60%" y="-120%" width="220%" height="340%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.6" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#231a12" flood-opacity="0.40" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c996-отброс" x="-60%" y="-120%" width="220%" height="340%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="6" result="р"/>
        <feOffset in="р" dx="5" dy="7" result="с"/>
        <feFlood flood-color="#0b1c2a" flood-opacity="0.22" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c996-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="3" stitchTiles="stitch" result="шум"/>
        <feColorMatrix in="шум" type="saturate" values="0"/>
      </filter>
    </defs>`;

  /* фон кадра: градиент, зерно и рамка */
  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c996-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c996-шум)" opacity="0.06"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||236}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  /* ОБЪЁМНОЕ ТЕЛО: перед, верх и бок — три видимые грани.
     Построение (y растёт вниз, глубина уходит вправо-вверх на гл):
       перед: (x,y) (x+ш,y) (x+ш,y-в) (x,y-в)
       верх:  (x,y-в) (x+ш,y-в) (x+ш+гл,y-в-гл) (x+гл,y-в-гл)
       бок:   (x+ш,y-в) (x+ш,y) (x+ш+гл,y-гл) (x+ш+гл,y-в-гл)
     Здесь была ошибка: у верхней грани задние вершины брали y-гл вместо
     (y-в)-гл, поэтому верх уезжал вниз, боковая грань вырождалась в
     треугольник, и куб читался как квадрат со стрелкой (нашёл владелец).
     Тона плоские и различимые: перед — основной, верх — светлее, бок — темнее. */
  function коробка(x,y,ш,в,гл,цвета,нач,подписи){
    const ц=цвета||['#e0ab3c','#ffe08f','#a87c1c'];
    const x1=x, y1=y-в, x2=x+ш, y2=y, x3=x+ш+гл;
    const yt=y1-гл, yb=y-гл;          /* задние верх и низ */
    return `<g>
      <ellipse cx="${(x+ш/2+гл/2).toFixed(1)}" cy="${y+6}" rx="${(ш/2+гл/2+6).toFixed(1)}" ry="7"
        fill="#0b1c2a" opacity=".22" filter="url(#c996-отброс)"/>
      <ellipse cx="${(x+ш/2).toFixed(1)}" cy="${y+2}" rx="${(ш/2+2).toFixed(1)}" ry="4"
        fill="#231a12" opacity=".4" filter="url(#c996-контакт)"/>
      <polygon points="${x1},${y1} ${x2},${y1} ${x3},${yt} ${x1+гл},${yt}" fill="${ц[1]}" stroke="#33291e" stroke-width="1.8" stroke-linejoin="round">
        ${проявить('9s',(нач||0.05)+0.1,(нач||0.05)+0.16)}</polygon>
      <polygon points="${x2},${y1} ${x2},${y2} ${x3},${yb} ${x3},${yt}" fill="${ц[2]}" stroke="#33291e" stroke-width="1.8" stroke-linejoin="round">
        ${проявить('9s',(нач||0.05)+0.2,(нач||0.05)+0.26)}</polygon>
      <polygon points="${x1},${y2} ${x2},${y2} ${x2},${y1} ${x1},${y1}" fill="${ц[0]}" stroke="#33291e" stroke-width="1.8" stroke-linejoin="round">
        ${проявить('9s',нач||0.05,(нач||0.05)+0.06)}</polygon>
      ${подписи?`<g>${проявить('9s',(нач||0.05)+0.3,(нач||0.05)+0.34)}
        ${т(x+ш/2,y+20,подписи,12,МУТ)}</g>`:''}
    </g>`;
  }

  /* ПИРАМИДА: основание — ромб, видны две передние грани, задние рёбра штриховые */
  function пирамида(x,y,ш,выс,глуб,цвета,нач){
    const ц=цвета||['#e0ab3c','#a87c1c'];
    const л=[x,y], п=[x+ш/2,y+глуб], пр=[x+ш,y], з=[x+ш/2,y-глуб], в=[x+ш/2,y-выс];
    return `<g>
      <ellipse cx="${(x+ш/2).toFixed(1)}" cy="${y+глуб/2+6}" rx="${(ш/2+глуб/2+6).toFixed(1)}" ry="7"
        fill="#0b1c2a" opacity=".22" filter="url(#c996-отброс)"/>
      <ellipse cx="${(x+ш/2).toFixed(1)}" cy="${y+глуб/2+2}" rx="${(ш/2).toFixed(1)}" ry="4"
        fill="#231a12" opacity=".38" filter="url(#c996-контакт)"/>
      <polygon points="${л} ${п} ${в}" fill="${ц[0]}" stroke="#33291e" stroke-width="1.8">${проявить('9s',нач||0.05,(нач||0.05)+0.08)}</polygon>
      <polygon points="${п} ${пр} ${в}" fill="${ц[1]}" stroke="#33291e" stroke-width="1.8">${проявить('9s',(нач||0.05)+0.08,(нач||0.05)+0.16)}</polygon>
      <line x1="${л[0]}" y1="${л[1]}" x2="${з[0]}" y2="${з[1]}" stroke="rgba(20,35,28,.45)" stroke-width="1.2" stroke-dasharray="4 3"/>
      <line x1="${пр[0]}" y1="${пр[1]}" x2="${з[0]}" y2="${з[1]}" stroke="rgba(20,35,28,.45)" stroke-width="1.2" stroke-dasharray="4 3"/>
      <line x1="${з[0]}" y1="${з[1]}" x2="${в[0]}" y2="${в[1]}" stroke="rgba(20,35,28,.35)" stroke-width="1.2" stroke-dasharray="4 3"/>
      <polygon points="${л} ${п} ${пр} ${з}" fill="none" stroke="rgba(20,35,28,.5)" stroke-width="1.3"/>
    </g>`;
  }

  /* РАЗВЁРТКА из прямоугольников: сетка долей */
  function развёртка(x,y,ячейки,цвета,нач,номера){
    let s='';
    ячейки.forEach(([кл,ряд,ш,в,имя],i)=>{
      const x1=x+кл*72, y1=y+ряд*72;
      s+=`<rect x="${x1}" y="${y1}" width="${ш}" height="${в}" rx="3"
        fill="${цвета[i%цвета.length]}" stroke="rgba(20,35,28,.55)" stroke-width="1.3">
        ${проявить('10s',(нач||0)+i*0.05,(нач||0)+0.05+i*0.05)}</rect>`;
      if(номера) s+=`<g>${проявить('10s',(нач||0)+0.1+i*0.05,(нач||0)+0.14+i*0.05)}
        ${т(x1+ш/2,y1+в/2+6,имя||String(i+1),16,GOLD,true)}</g>`;
    });
    return s;
  }

  /* ЕДИНИЧНАЯ СЕТКА на грани: n×m клеток, каждая появляется по очереди */
  function клетки(x,y,кл,ш,в,цвет,нач){
    let s='', н=0;
    for(let р=0;р<в;р++) for(let к=0;к<ш;к++){
      const x1=x+к*кл, y1=y+р*кл;
      s+=`<rect x="${x1+0.4}" y="${y1+0.4}" width="${кл-0.8}" height="${кл-0.8}" fill="${цвет}" opacity=".8">
        ${проявить('12s',(нач||0)+н*0.012,(нач||0)+0.03+н*0.012)}</rect>`;
      н++;
    }
    return s;
  }

  /* ================= КАДРЫ ================= */

  /* 1. Три объёмных тела */
  function F1(s){
    const в = s.тела;
    return ВОПРОС('Какие объёмные тела ты знаешь?') +
      `<div class="pic">${свг(`
        ${фон(216)}
        ${т(168,22,'Объёмные тела',14,GOLD,true)}
        ${коробка(34,150,58,58,26,null,0.05,'куб')}
        ${коробка(126,150,72,44,28,['#5aa3cd','#b9e0f5','#2f6b93'],0.4,'параллелепипед')}
        ${пирамида(232,126,84,58,26,['#6db894','#3d7a5c'],0.72)}
        ${т(274,176,'пирамида',12,МУТ)}
        ${т(168,196,'у каждого тела есть грани, рёбра и вершины',12,ИНК)}
      `,216)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='объёмные'?'hit':(в?'miss':''), 'Куб, параллелепипед и пирамида — объёмные тела', "r996Tel('объёмные')")}
        ${BTN(4, в==='плоские'?'miss':'', 'Это плоские фигуры: их можно положить на стол', "r996Tel('плоские')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='объёмные',
        в==='объёмные' ? 'Верно: у этих тел есть высота, их нельзя уложить на лист — они занимают место в пространстве.'
                      : 'Нет: куб, параллелепипед и пирамида имеют высоту, это объёмные тела, а не плоские фигуры.') : ЖДЁТ()) +
      ПРАВИЛО('Объёмное тело занимает место в пространстве: у него есть <b>грани, рёбра и вершины</b>.');
  }

  /* 2. Грань, ребро, вершина */
  function F2(s){
    const в = s.элементы;
    const x=52, y=168, ш=96, в0=62, гл=34;
    return ВОПРОС('Сколько у параллелепипеда граней, рёбер и вершин?') +
      `<div class="pic">${свг(`
        ${фон(228)}
        ${т(168,22,'Грань, ребро, вершина',14,GOLD,true)}
        <polygon points="${x},${y} ${x+ш},${y} ${x+ш},${y-в0} ${x},${y-в0}" fill="url(#c996-перед)" stroke="rgba(20,35,28,.6)" stroke-width="1.4">
          ${светит('9s',0.1)}</polygon>
        <polygon points="${x},${y-в0} ${x+ш},${y-в0} ${x+ш+гл},${y-в0-гл} ${x+гл},${y-в0-гл}" fill="url(#c996-верх)" stroke="rgba(20,35,28,.6)" stroke-width="1.4"/>
        <polygon points="${x+ш},${y-в0} ${x+ш},${y} ${x+ш+гл},${y-гл} ${x+ш+гл},${y-в0-гл}" fill="url(#c996-бок)" stroke="rgba(20,35,28,.6)" stroke-width="1.4"/>
        ''}
        <line x1="${x+ш}" y1="${y}" x2="${x+ш+гл}" y2="${y-гл}" stroke="${GOLD}" stroke-width="4">${светит('9s',2.6)}</line>
        <line x1="${x+ш}" y1="${y}" x2="${x+ш}" y2="${y-в0}" stroke="${GOLD}" stroke-width="4">${светит('9s',2.6)}</line>
        <circle cx="${x+ш}" cy="${y-в0}" r="7" fill="${GREEN}">${светит('9s',4.4)}</circle>
        <g>${проявить('9s',0.5,0.56)}${т(268,66,'грань',16,GOLD,true)}</g>
        <g>${проявить('9s',3,3.06)}${т(268,110,'ребро',16,BLUE,true)}</g>
        <g>${проявить('9s',4.8,4.86)}${т(268,154,'вершина',16,GREEN,true)}</g>
        <g>${проявить('9s',5.4,5.6)}
          ${т(150,214,'6 граней · 12 рёбер · 8 вершин',14,ИНК,true)}</g>
      `,228)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), '6 граней, 12 рёбер, 8 вершин', "r996Elem('шесть')")}
        ${BTN(4, в==='восемь'?'miss':'', '8 граней, 6 рёбер, 12 вершин', "r996Elem('восемь')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: у параллелепипеда 6 граней (по две равные), 12 рёбер и 8 вершин.'
                    : 'Нет: числа перепутаны. Граней 6, рёбер 12, вершин 8 — граней меньше всего, рёбер больше всего.') : ЖДЁТ()) +
      ПРАВИЛО('У параллелепипеда <b>6 граней, 12 рёбер и 8 вершин</b>.');
  }

  /* 3. Развёртка и тело рядом: пары граней вспыхивают по очереди */
  function F3(s){
    const в = s.чтоТакое;
    const цвета=['url(#c996-перед)','url(#c996-перед2)','url(#c996-перед3)',
                 'url(#c996-верх)','url(#c996-верх2)','url(#c996-верх3)'];
    return ВОПРОС('Что такое развёртка объёмного тела?') +
      `<div class="pic">${свг(`
        ${фон(244)}
        ${т(168,22,'Развёртка и тело',14,GOLD,true)}
        ''}
        ${[[0,1],[1,1],[2,1],[3,1],[1,0],[1,2]].map(([кл,ряд],i)=>{
          const x1=24+кл*40, y1=70+ряд*40;
          return `<rect x="${x1}" y="${y1}" width="38" height="38" rx="3" fill="${цвета[i]}"
            stroke="rgba(20,35,28,.55)" stroke-width="1.2" stroke-dasharray="4 3">
            ${светит('10s',i*0.7)}</rect>`;
        }).join('')}
        ${т(100,214,'шесть квадратов — развёртка',12,ИНК)}
        ${т(200,134,'→',20,GOLD,true)}
        ${коробка(216,178,48,48,22,['#e0ab3c','#ffe08f','#a87c1c'],0.2,null)}
        ${т(252,214,'то же тело',12,МУТ)}
        ${т(168,236,'одна и та же фигура: плоская и сложенная',12,МУТ)}
      `,244)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='плоская'?'hit':(в?'miss':''), 'Плоская фигура, из которой складывают тело', "r996Net('плоская')")}
        ${BTN(4, в==='макет'?'miss':'', 'Уменьшенная копия тела', "r996Net('макет')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='плоская',
        в==='плоская' ? 'Верно: развёртка — плоская фигура; если её сложить по линиям сгиба, получится объёмное тело.'
                     : 'Нет: макет — это готовое тело. Развёртка плоская, её складывают и получают тело.') : ЖДЁТ()) +
      ПРАВИЛО('<b>Развёртка</b> — плоская фигура, из которой складывается объёмное тело.');
  }

  /* 4. Шесть квадратов развёртки куба */
  function F4(s){
    const в = s.куб;
    const места=[[0,1],[1,1],[2,1],[3,1],[1,0],[1,2]];
    return ВОПРОС('Сколько квадратов в развёртке куба?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,22,'Развёртка куба',14,GOLD,true)}
        ${места.map(([к,ряд],i)=>{
          const x1=104+к*52, y1=52+ряд*52;
          return `<g>
            <rect x="${x1}" y="${y1}" width="50" height="50" rx="4" fill="url(#c996-плоско)"
              stroke="${ТУШЬ}" stroke-width="1.4" stroke-dasharray="6 4">${проявить('11s',i*0.08,i*0.08+0.06)}</rect>
            <g>${проявить('11s',i*0.08+0.03,i*0.08+0.09)}
              ${т(x1+25,y1+33,String(i+1),20,ТУШЬ,true)}</g>
          </g>`;
        }).join('')}
        <g>${проявить('11s',0.62,0.72)}
          ${т(168,232,'6 квадратов — 6 граней куба',14,ИНК,true)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='шесть'?'hit':(в?'miss':''), 'Шесть: у куба шесть граней', "r996Cube('шесть')")}
        ${BTN(4, в==='четыре'?'miss':'', 'Четыре: квадраты идут в один ряд', "r996Cube('четыре')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='шесть',
        в==='шесть' ? 'Верно: у куба шесть граней-квадратов, значит и в развёртке шесть квадратов.'
                    : 'Нет: в ряду четыре квадрата, но ещё два пристроены сверху и снизу — всего шесть.') : ЖДЁТ()) +
      ПРАВИЛО('В развёртке куба <b>шесть квадратов</b> — по одному на каждую грань.');
  }

  /* 5. Три разные развёртки куба: по строке на каждую, клетки крупные */
  function F5(s){
    const в = s.разные;
    /* все три фигуры проверены расчётом: сворачиваются в куб без наложений */
    const наборы=[[[0,0],[1,0],[2,0],[3,0],[1,-1],[1,1],'крест'],
                  [[0,0],[1,0],[1,1],[2,1],[2,2],[3,2],'змейка'],
                  [[0,0],[0,1],[1,1],[2,1],[3,1],[0,2],'буква Т']];
    const кл=26;
    return ВОПРОС('Сколько разных развёрток можно построить для куба?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,22,'Развёртки куба бывают разные',14,GOLD,true)}
        ${наборы.map((набор,ф)=>{
          const имя=набор[набор.length-1];
          const клетки=набор.slice(0,6);
          const минX=Math.min(...клетки.map(к=>к[0])), максX=Math.max(...клетки.map(к=>к[0]));
          const минY=Math.min(...клетки.map(к=>к[1])), максY=Math.max(...клетки.map(к=>к[1]));
          const ш=(максX-минX+1)*кл, в0=(максY-минY+1)*кл;
          const x0=64-ш/2, y0=32+ф*62;
          return `<g>
            ${клетки.map(([кк,рр],i)=>{
              const x1=x0+(кк-минX)*кл, y1=y0+(рр-минY)*кл;
              return `<rect x="${x1}" y="${y1}" width="${кл}" height="${кл}" fill="url(#c996-плоско)"
                stroke="${ТУШЬ}" stroke-width="1.6" stroke-dasharray="6 4">
                ${светит('12s',(ф*6+i)*0.35)}</rect>`;
            }).join('')}
            ${т(272,y0+в0/2+2,имя,14,GOLD,true)}
            ${т(272,y0+в0/2+22,'6 квадратов',12,МУТ)}
          </g>`;
        }).join('')}
        ${т(168,246,'у всех развёрток шесть квадратов, всего их одиннадцать',12,ИНК)}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='одиннадцать'?'hit':(в?'miss':''), 'Куб имеет одиннадцать разных развёрток', "r996Variants('одиннадцать')")}
        ${BTN(4, в==='одна'?'miss':'', 'Только одна: крест из шести квадратов', "r996Variants('одна')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='одиннадцать',
        в==='одиннадцать' ? 'Верно: у куба одиннадцать различных развёрток, и в каждой по шесть квадратов.'
                          : 'Нет: крест — только одна из них. Всего у куба одиннадцать разных развёрток.') : ЖДЁТ()) +
      ПРАВИЛО('У куба <b>одиннадцать</b> различных развёрток; в каждой шесть квадратов.');
  }

  /* 6. Развёртка параллелепипеда: три пары равных граней */
  function F6(s){
    const в = s.паралл;
    /* ширина и высота каждой грани: b×c, a×c, b×c, a×c в ряду, a×b сверху и снизу */
    const ряд=[[44,36,'b×c'],[56,36,'a×c'],[44,36,'b×c'],[56,36,'a×c']];
    let x=64, y=126;
    const клеткиРяда=[];
    ряд.forEach(([ш,в0,имя],i)=>{ клеткиРяда.push({x:x,y:y,ш:ш,в:в0,имя:имя,i:i}); x+=ш; });
    const верх={x:64+44, y:126-44, ш:56, в:44, имя:'a×b', i:4};
    const низ={x:64+44, y:126+36, ш:56, в:44, имя:'a×b', i:5};
    const все=клеткиРяда.concat([верх,низ]);
    return ВОПРОС('Из чего состоит развёртка прямоугольного параллелепипеда?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,22,'Развёртка параллелепипеда',14,GOLD,true)}
        ${все.map(г=>`<g>
          <rect x="${г.x}" y="${г.y}" width="${г.ш}" height="${г.в}" rx="3" fill="url(#c996-плоско)"
            stroke="${ТУШЬ}" stroke-width="1.4" stroke-dasharray="6 4">${проявить('11s',г.i*0.07,г.i*0.07+0.05)}</rect>
          <g>${проявить('11s',г.i*0.07+0.03,г.i*0.07+0.08)}
            ${т(г.x+г.ш/2,г.y+г.в/2+6,г.имя,14,ТУШЬ,true)}</g>
        </g>`).join('')}
        <g>${проявить('11s',0.66,0.76)}
          ${т(168,238,'три пары равных граней: a×b, b×c, a×c',12,ИНК)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='три пары'?'hit':(в?'miss':''), 'Из трёх пар равных прямоугольников', "r996Box('три пары')")}
        ${BTN(4, в==='шесть разных'?'miss':'', 'Из шести разных прямоугольников', "r996Box('шесть разных')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='три пары',
        в==='три пары' ? 'Верно: противоположные грани равны, поэтому в развёртке три пары одинаковых прямоугольников.'
                      : 'Нет: грани повторяются парами: две a×b, две b×c и две a×c.') : ЖДЁТ()) +
      ПРАВИЛО('Развёртка параллелепипеда — <b>три пары равных прямоугольников</b>.');
  }

  /* 7. Развёртка пирамиды: основание и четыре треугольника */
  function F7(s){
    const в = s.пирамида;
    const бx=84, бy=112, ст=48, выс=42;
    const верш=[[бx+ст/2,бy-выс],[бx+ст/2,бy+ст+выс],[бx-выс,бy+ст/2],[бx+ст+выс,бy+ст/2]];
    const стороны=[[[бx,бy],[бx+ст,бy]],[[бx,бy+ст],[бx+ст,бy+ст]],
                   [[бx,бy],[бx,бy+ст]],[[бx+ст,бy],[бx+ст,бy+ст]]];
    return ВОПРОС('Из чего состоит развёртка четырёхугольной пирамиды?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,22,'Развёртка пирамиды',14,GOLD,true)}
        <rect x="${бx}" y="${бy}" width="${ст}" height="${ст}" rx="3" fill="url(#c996-перед2)"
          stroke="rgba(20,35,28,.55)" stroke-width="1.4" stroke-dasharray="5 4">
          ${проявить('11s',0.04,0.1)}</rect>
        ${верш.map(([вх,вy],i)=>`<polygon points="${стороны[i][0][0]},${стороны[i][0][1]} ${стороны[i][1][0]},${стороны[i][1][1]} ${вх},${вy}"
          fill="url(#c996-перед)" stroke="rgba(20,35,28,.55)" stroke-width="1.4" stroke-dasharray="5 4">
          ${проявить('11s',0.12+i*0.08,0.18+i*0.08)}</polygon>`).join('')}
        ${т(108,232,'основание и четыре треугольника',12,ИНК)}
        <g>${анТ('0 0;0 0;-14 0;-14 0;0 0','11s','keyTimes="0;0.55;0.66;0.88;1"')}
          <path d="M182 136 L214 136" stroke="${GOLD}" stroke-width="2.4"/>
          <path d="M208 130 L218 136 L208 142 Z" fill="${GOLD}"/>
        </g>
        ${пирамида(224,158,88,62,28,['#e0ab3c','#a87c1c'],0.62)}
        ${т(268,232,'пирамида',12,МУТ)}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='основание'?'hit':(в?'miss':''), 'Квадратное основание и четыре треугольника', "r996Pyr('основание')")}
        ${BTN(4, в==='шесть'?'miss':'', 'Шесть одинаковых квадратов', "r996Pyr('шесть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='основание',
        в==='основание' ? 'Верно: у четырёхугольной пирамиды основание — квадрат, а боковых граней четыре треугольника.'
                       : 'Нет: шесть квадратов бывает только у куба. У пирамиды одно основание и четыре боковые грани-треугольника.') : ЖДЁТ()) +
      ПРАВИЛО('Развёртка четырёхугольной пирамиды: <b>основание и четыре треугольника</b>.');
  }

  /* 8. Цилиндр: прямоугольник и два круга */
  function F8(s){
    const в = s.цилиндр;
    return ВОПРОС('Из чего состоит развёртка цилиндра?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,22,'Развёртка цилиндра',14,GOLD,true)}
        <ellipse cx="78" cy="62" rx="34" ry="13" fill="url(#c996-верх2)" stroke="rgba(20,35,28,.6)" stroke-width="1.4">
          ${проявить('11s',0.05,0.11)}</ellipse>
        <rect x="44" y="78" width="68" height="86" rx="3" fill="url(#c996-плоско)"
          stroke="rgba(20,35,28,.55)" stroke-width="1.4" stroke-dasharray="5 4">
          ${проявить('11s',0.2,0.26)}</rect>
        <ellipse cx="78" cy="180" rx="34" ry="13" fill="url(#c996-верх2)" stroke="rgba(20,35,28,.6)" stroke-width="1.4">
          ${проявить('11s',0.35,0.41)}</ellipse>
        ${т(78,224,'прямоугольник и два круга',12,ИНК)}
        <g>${анТ('0 0;0 0;-16 0;-16 0;0 0','11s','keyTimes="0;0.5;0.62;0.86;1"')}
          <path d="M150 132 L196 132" stroke="${GOLD}" stroke-width="2.4"/>
          <path d="M190 126 L200 132 L190 138 Z" fill="${GOLD}"/>
        </g>
        <ellipse cx="262" cy="86" rx="36" ry="13" fill="#b9e0f5" stroke="rgba(20,35,28,.6)" stroke-width="1.4"/>
        <ellipse cx="266" cy="166" rx="46" ry="9" fill="#0b1c2a" opacity=".22" filter="url(#c996-отброс)"/>
        <ellipse cx="262" cy="157" rx="36" ry="6" fill="#231a12" opacity=".38" filter="url(#c996-контакт)"/>
        <path d="M226 86 L226 150 A36 13 0 0 0 298 150 L298 86" fill="#5aa3cd"
          stroke="#33291e" stroke-width="1.8" opacity=".95">${светит('11s',0.7)}</path>
        <ellipse cx="262" cy="150" rx="36" ry="13" fill="#2f6b93" stroke="#33291e" stroke-width="1.6" opacity=".8"/>
        <path d="M238 96 L238 142" stroke="rgba(255,255,255,.22)" stroke-width="4"/>
        ${т(262,224,'цилиндр',12,МУТ)}
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='прямоугольник'?'hit':(в?'miss':''), 'Прямоугольник и два круга', "r996Cyl('прямоугольник')")}
        ${BTN(4, в==='квадрат'?'miss':'', 'Квадрат и один круг', "r996Cyl('квадрат')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='прямоугольник',
        в==='прямоугольник' ? 'Верно: боковая поверхность цилиндра разворачивается в прямоугольник, а два основания дают два круга.'
                           : 'Нет: круга два — это основания. Боковая поверхность даёт прямоугольник.') : ЖДЁТ()) +
      ПРАВИЛО('Развёртка цилиндра: <b>прямоугольник и два круга</b>.');
  }

  /* 9. Площадь поверхности куба с ребром 3: 54 единичных квадрата */
  function F9(s){
    const в = s.площадь;
    const кл=10, ст=3*кл, шаг=ст+3;
    const места=[[0,1],[1,1],[2,1],[3,1],[1,0],[1,2]];
    return ВОПРОС('Ребро куба 3 см. Сколько квадратных сантиметров бумаги нужно на всю развёртку?') +
      `<div class="pic">${свг(`
        ${фон(268)}
        ${т(168,22,'Площадь развёртки куба',14,GOLD,true)}
        ${места.map(([к,r],i)=>{
          const x1=96+к*шаг, y1=40+r*шаг;
          return `<g>
            <rect x="${x1}" y="${y1}" width="${ст}" height="${ст}" rx="2" fill="rgba(255,255,255,.05)"
              stroke="rgba(20,35,28,.6)" stroke-width="1.3"/>
            ${клетки(x1,y1,кл,3,3,'url(#c996-перед)',i*0.12)}
          </g>`;
        }).join('')}
        <g>${проявить('13s',0.86,0.92)}
          ${т(168,152,'на каждой грани 3 · 3 = 9 клеток',14,ИНК)}
          ${т(168,182,'6 граней · 9 клеток',14,ИНК)}
          ${т(168,216,'6 · 9 = 54',20,GREEN,true)}
          ${т(168,250,'квадратных сантиметра бумаги',12,МУТ)}</g>
      `,268)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пятьдесят четыре'?'hit':(в?'miss':''), '54: шесть граней по девять клеток', "r996Area('пятьдесят четыре')")}
        ${BTN(4, в==='девять'?'miss':'', '9: столько клеток на одной грани', "r996Area('девять')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пятьдесят четыре',
        в==='пятьдесят четыре' ? 'Верно: на одной грани 3 · 3 = 9 клеток, граней шесть, значит 6 · 9 = 54.'
                               : 'Нет: девять клеток только на одной грани. Всего граней шесть: 6 · 9 = 54.') : ЖДЁТ()) +
      ПРАВИЛО('Площадь поверхности куба: <b>6 · a · a</b>. При ребре 3 получается 54.');
  }

  /* 10. Материал на коробку 2 × 3 × 4 */
  function F10(s){
    const в = s.материал;
    const кл=8;
    return ВОПРОС('Коробка 2 × 3 × 4. Сколько квадратных единиц материала пойдёт на её развёртку?') +
      `<div class="pic">${свг(`
        ${фон(268)}
        ${т(168,22,'Материал на коробку',14,GOLD,true)}
        ${коробка(18,116,60,42,22,['#e0ab3c','#ffe08f','#a87c1c'],0.05,null)}
        ${т(48,140,'2 × 3 × 4',12,ИНК)}
        ${[[176,44,3,2,0.10],[248,44,4,3,0.34],[176,116,2,4,0.58]].map(([x1,y1,ш,в0,нач],и)=>`
          <g>
            <rect x="${x1}" y="${y1}" width="${ш*кл}" height="${в0*кл}" rx="2" fill="rgba(255,255,255,.05)"
              stroke="rgba(20,35,28,.6)" stroke-width="1.2"/>
            ${клетки(x1,y1,кл,ш,в0,'url(#c996-перед2)',нач)}
            ${т(x1+ш*кл/2,y1+в0*кл+16,ш+' × '+в0,12,GOLD,true)}
          </g>`).join('')}
        <g>${проявить('14s',0.88,0.94)}
          ${т(168,196,'2·(2·3) + 2·(3·4) + 2·(2·4)',14,ИНК,true)}
          ${т(168,224,'= 12 + 24 + 16 = 52',16,GREEN,true)}</g>
        ${т(168,250,'каждую пару граней считаем дважды',12,МУТ)}
      `,268)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='пятьдесят два'?'hit':(в?'miss':''), '52: двенадцать, двадцать четыре и шестнадцать', "r996Mat('пятьдесят два')")}
        ${BTN(4, в==='двадцать четыре'?'miss':'', '24: площадь самой большой грани', "r996Mat('двадцать четыре')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='пятьдесят два',
        в==='пятьдесят два' ? 'Верно: каждая пара граней считается дважды: 12 + 24 + 16 = 52 квадратных единицы.'
                            : 'Нет: 24 — это только пара больших граней. Нужны все три пары: 12 + 24 + 16 = 52.') : ЖДЁТ()) +
      ПРАВИЛО('Площадь поверхности параллелепипеда: <b>2 · (ab + bc + ac)</b>; здесь 52.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Сколько квадратов в развёртке куба?', варианты:[
        {т:'шесть', ок:true,  почему:'У куба шесть граней, значит шесть квадратов.'},
        {т:'четыре', ок:false, почему:'В ряду четыре, но ещё два пристроены — всего шесть.'}]},
    { вопрос:'Сколько рёбер у прямоугольного параллелепипеда?', варианты:[
        {т:'12', ок:true,  почему:'6 граней, 12 рёбер и 8 вершин.'},
        {т:'8', ок:false, почему:'Восемь — это вершин, а рёбер двенадцать.'}]},
    { вопрос:'Из чего состоит развёртка цилиндра?', варианты:[
        {т:'прямоугольник и два круга', ок:true,  почему:'Боковая поверхность и два основания.'},
        {т:'три прямоугольника', ок:false, почему:'Три пары прямоугольников — у параллелепипеда.'}]},
    { вопрос:'Ребро куба 2. Чему равна площадь всей развёртки?', варианты:[
        {т:'24', ок:true,  почему:'6 · 2 · 2 = 24.'},
        {т:'12', ок:false, почему:'12 — это площадь трёх граней; граней шесть.'}]},
    { вопрос:'Из чего состоит развёртка четырёхугольной пирамиды?', варианты:[
        {т:'квадрат и четыре треугольника', ок:true,  почему:'Основание и четыре боковые грани.'},
        {т:'шесть квадратов', ок:false, почему:'Шесть квадратов — развёртка куба.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Считаем грани тела — столько же частей в развёртке; площадь развёртки равна площади поверхности тела.') +
        `<div class="ask">${BTN(4,'','пройти заново','r996Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r996Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Развёртка повторяет все грани тела, поэтому её площадь равна <b>площади поверхности</b>.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · элементы тела',
    задания:[
      { ф:'Сколько граней у куба?', о:['6','8','12'], в:0, р:'У куба шесть граней-квадратов.' },
      { ф:'Сколько вершин у параллелепипеда?', о:['8','6','12'], в:0, р:'Вершин восемь.' },
      { ф:'Сколько рёбер у четырёхугольной пирамиды?', о:['8','5','12'], в:0, р:'4 ребра основания и 4 боковых — всего 8.' },
      { ф:'Сколько граней у треугольной призмы?', о:['5','6','8'], в:0, р:'2 треугольника и 3 прямоугольника — 5 граней.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · развёртки',
    задания:[
      { ф:'Сколько квадратов в развёртке куба?', о:['6','4','8'], в:0, р:'По одному на каждую грань — шесть.' },
      { ф:'Что даёт развёртку цилиндра?', о:['прямоугольник и два круга','три пары прямоугольников','шесть квадратов'], в:0,
        р:'Боковая поверхность — прямоугольник, основания — два круга.' },
      { ф:'Из чего состоит развёртка четырёхугольной пирамиды?', о:['квадрат и четыре треугольника','шесть квадратов','два круга'], в:0,
        р:'Основание и четыре боковые грани.' },
      { ф:'Сколько разных развёрток у куба?', о:['11','6','1'], в:0, р:'У куба одиннадцать различных развёрток.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · площадь поверхности',
    задания:[
      { ф:'Ребро куба 3. Чему равна площадь развёртки?', о:['54','27','18'], в:0, р:'6 · 3 · 3 = 54.' },
      { ф:'Ребро куба 5. Чему равна площадь развёртки?', о:['150','125','30'], в:0, р:'6 · 5 · 5 = 150.' },
      { ф:'Коробка 1 × 1 × 1. Сколько материала нужно?', о:['6','3','1'], в:0, р:'6 · 1 · 1 = 6.' },
      { ф:'Коробка 2 × 3 × 4. Чему равна площадь поверхности?', о:['52','24','26'], в:0, р:'2·(6 + 12 + 8) = 52.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r996T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r996TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L996={
    id: ID, title:'Развёртки объёмных тел', ico:'📦',
    src:'Математика · 5 класс · Объёмные тела', subj:'math',
    explain: [
      'В мастерской коробок из плоского листа делают объёмные тела. Лист бумаги плоский, а коробка объёмная — значит, где-то между ними есть связь. Эта связь называется развёрткой: если разрезать коробку по рёбрам и разложить её на столе, получится плоская фигура. А если такую фигуру сложить по линиям сгиба и склеить, снова получится коробка.',

      'Вспомним, из чего состоит объёмное тело. Грани — это плоские поверхности, из которых тело собрано: у коробки их шесть. Рёбра — линии, по которым сходятся грани: их двенадцать. Вершины — точки, где сходятся рёбра: их восемь. У четырёхугольной пирамиды граней пять (основание и четыре боковые), рёбер восемь, вершин пять. Частая ошибка — путать грани и рёбра. Как проверить себя: грань можно положить на ладонь — это поверхность, а ребро тонкое, как линия.',

      'Развёртка — это плоская фигура, повторяющая все грани тела в том порядке, в каком они соединены. Поэтому площадь развёртки равна площади поверхности тела: сколько бумаги уйдёт на развёртку, столько и на коробку. Частая ошибка — считать, что развёртка меньше тела: она не меньше и не больше, она та же поверхность, только расправленная. Как проверить себя: сложи развёртку мысленно по линиям сгиба — должны получиться все грани без пропусков.',

      'Развёртка куба состоит из шести квадратов. Классическая развёртка — крест: четыре квадрата в ряд и по одному сверху и снизу от второго. Всего у куба одиннадцать различных развёрток, но в каждой обязательно шесть квадратов. Частая ошибка — нарисовать пять или семь квадратов. Как проверить себя: посчитай квадраты — их должно быть ровно шесть, по одному на грань.',

      'Развёртка прямоугольного параллелепипеда состоит из трёх пар равных прямоугольников: две грани a×b, две b×c и две a×c. Противоположные грани коробки всегда равны, поэтому и в развёртке они повторяются. Частая ошибка — делать все шесть прямоугольников разными. Как проверить себя: найди в развёртке пары одинаковых прямоугольников — их должно быть три пары.',

      'У пирамиды развёртка другая: в основании квадрат, а по его сторонам пристроены четыре треугольника — будущие боковые грани. У треугольной пирамиды основание — треугольник, и боковых граней три. Частая ошибка — забыть одну боковую грань. Как проверить себя: треугольников должно быть столько же, сколько сторон у основания.',

      'У круглых тел развёртки тоже есть. Боковая поверхность цилиндра разворачивается в прямоугольник: если обернуть цилиндр бумагой и развернуть её, получится прямоугольник, ширина которого равна длине окружности основания. Основания дают два круга. У конуса боковая поверхность разворачивается в сектор круга, а основание — один круг. Частая ошибка — рисовать у цилиндра один круг вместо двух. Как проверить себя: у цилиндра два основания, значит и два круга.',

      'Развёртка помогает посчитать материал. Площадь поверхности куба с ребром a равна 6 · a · a: шесть одинаковых квадратов. Если ребро 3, получается 6 · 9 = 54 квадратных сантиметра. Площадь поверхности параллелепипеда считается как 2 · (ab + bc + ac): каждую пару граней берут дважды. Для коробки 2 × 3 × 4 это 2 · (6 + 12 + 8) = 52. Частая ошибка — посчитать только одну грань из пары. Как проверить себя: пересчитай пары — их три, и каждая входит в сумму дважды.',

      'Полезно проверять себя на единичных квадратах: разбей каждую грань на клетки со стороной 1 и пересчитай их. У куба с ребром 3 на каждой грани 9 клеток, всего 54. У коробки 2 × 3 × 4 грани дают 6, 12 и 8 клеток, а с учётом пар — 12, 24 и 16, то есть 52. Частая ошибка — потерять клетки на невидимых гранях. Как проверить себя: считай все грани, включая задние и нижние.',

      'Развёртки встречаются в жизни постоянно: коробки складывают из вырубленных заготовок, пакеты делают из плоской плёнки, а жестянщик выкраивает трубу из прямоугольного листа. Умение читать развёртку — это умение посчитать, сколько нужно материала и как его вырезать с наименьшими отходами.',

      'Пройти практику: пять уровней — число граней, развёртки тел, площадь поверхности. Соберём правило: развёртка — плоская фигура из всех граней тела; площадь развёртки равна площади поверхности; у куба шесть квадратов, у параллелепипеда три пары прямоугольников, у пирамиды основание и треугольники, у цилиндра прямоугольник и два круга.',

      'Первый тренажёр — про элементы тела: считаем грани, рёбра и вершины у куба, параллелепипеда, пирамиды и призмы. Полезно держать в голове связку: грани — плоские, рёбра — линии, вершины — точки.',

      'Второй тренажёр — про развёртки: из чего состоит развёртка куба, цилиндра, пирамиды и сколько всего развёрток у куба. Здесь помогает правило: сколько граней у тела, столько частей и в развёртке.',

      'Третий тренажёр — про площадь поверхности: считаем материал для куба и параллелепипеда. Помни две формулы: у куба 6 · a · a, у параллелепипеда 2 · (ab + bc + ac).'
    ],
    check: {
      q:'Из чего состоит развёртка цилиндра?',
      choices:['прямоугольник и два круга','три пары прямоугольников','шесть квадратов'],
      ans:0,
      exp:'Боковая поверхность цилиндра разворачивается в прямоугольник, а два основания дают два круга.'
    },
    tasks: [
      { q:'Сколько граней у куба?', kind:'unit', ans:6, tol:0,
        hints:['Посчитай плоские поверхности.','Верх, низ и четыре бока.'],
        sol:'Шесть граней.' },
      { q:'Сколько квадратов в развёртке куба?', kind:'choice',
        choices:['6','4','8'], ans:0,
        hints:['Сколько граней — столько частей.','У куба шесть граней.'],
        sol:'Шесть квадратов.' },
      { q:'Ребро куба 3. Чему равна площадь развёртки?', kind:'choice',
        choices:['54','27','18'], ans:0,
        hints:['На каждой грани 3 · 3 клетки.','Граней шесть.'],
        sol:'6 · 9 = 54.' },
      { q:'Из чего состоит развёртка четырёхугольной пирамиды?', kind:'choice',
        choices:['квадрат и четыре треугольника','шесть квадратов','два круга'], ans:0,
        hints:['Основание и боковые грани.','Треугольников столько, сколько сторон у основания.'],
        sol:'Квадрат и четыре треугольника.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L996.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Объёмные тела','Куб, параллелепипед, пирамида'],
      2:['Элементы','Грань, ребро, вершина'],
      3:['Развёртка','Плоская и сложенная'],
      4:['Развёртка куба','Шесть квадратов'],
      5:['Одиннадцать','Разные развёртки куба'],
      6:['Параллелепипед','Три пары граней'],
      7:['Пирамида','Основание и треугольники'],
      8:['Цилиндр','Прямоугольник и круги'],
      9:['Площадь','54 единичных квадрата'],
      10:['Материал','Коробка 2 × 3 × 4'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','Элементы тела'],
      13:['Тренажёр 2','Развёртки'],
      14:['Тренажёр 3','Площадь поверхности']
    };
    const з=ЗАГОЛОВКИ[f]||['Развёртки','Объёмные тела'];
    el.innerHTML = `<div class="s6 l996" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r996Tel=(к)=>{ S().тела=к; chRender(0); };
  window.r996Elem=(к)=>{ S().элементы=к; chRender(0); };
  window.r996Net=(к)=>{ S().чтоТакое=к; chRender(0); };
  window.r996Cube=(к)=>{ S().куб=к; chRender(0); };
  window.r996Variants=(к)=>{ S().разные=к; chRender(0); };
  window.r996Box=(к)=>{ S().паралл=к; chRender(0); };
  window.r996Pyr=(к)=>{ S().пирамида=к; chRender(0); };
  window.r996Cyl=(к)=>{ S().цилиндр=к; chRender(0); };
  window.r996Area=(к)=>{ S().площадь=к; chRender(0); };
  window.r996Mat=(к)=>{ S().материал=к; chRender(0); };
  window.r996Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r996Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r996T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r996TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  if(window.WAVE_B){
    const прежний=window.WAVE_B[ID];
    window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ try{ прежний(el); }catch(e2){} } };
  }
  (function(){
    const arr=window.ARH_LESSONS;
    if(!arr || !arr.length) return;
    const место=arr.findIndex(L=>L && L.id===ID);
    if(место>=0) arr[место]=L996; else arr.push(L996);
  })();
  window.MA996={render:render, L:L996};
})();
