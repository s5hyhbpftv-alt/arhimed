/* ============ МАТЕМАТИКА · 6 КЛАСС · 648 · «ДЕЙСТВИЯ С РАЦИОНАЛЬНЫМИ ЧИСЛАМИ: СЛОЖЕНИЕ И ВЫЧИТАНИЕ» ============
   Школьная линия, полка «Математика» (subj:'math', group НЕ ставим), рядом с
   уроками 6 класса 193–199 (MVP/data/vis_wk.js): проценты, отношения, круг,
   углы, уравнения. По «плану Петерсона» 6 класса темы «сложение и вычитание
   рациональных чисел» в приложении не было: grep по MVP/data это подтвердил —
   слова «рациональн», «отрицательн» в уроках 6 класса не встречаются.

   О номере. 648 свободен: проверено дважды — grep-ом по MVP/data (заняты
   601–647: 601–614 русский, 615–620 Мишутка-русский, 621–642 Путь Мишутки,
   643–646 Мастерские Сиракуз, 647 нет) и замером живого window.ARH_LESSONS
   в приложении (477 уроков, максимум 646, дублей id нет). Дублей номер не даёт.

   Устройство как у школьных уроков 6 класса: объект урока с полем explain —
   по шагу на экран, внизу экрана рисунок к шагу, затем «Проверю себя» и
   задачи тренажёра. Рисунки — свои функции: координатная прямая со стрелками
   сдвига, таблица знаков, термометр, долг. Вектор только своими функциями,
   эмодзи в SVG нет.

   Усиленное объяснение (требование владельца): каждый разбор идёт по шагам
   («Шаг 1. Отмечаем −3. Шаг 2. Прибавить −5 значит сдвинуться влево на 5.
   Шаг 3. Попадаем в −8. Проверка: |−3| + |−5| = 8, знак минус»), у каждого
   из восьми заданий есть кнопка «частая ошибка» (сложить модули и не
   поставить знак; перепутать вычитание отрицательного со сложением) и кнопка
   «проверка» (пройти по прямой), а где можно — и «второй способ» (через долг).
   Отдельные шаги: таблица «правило знаков» и блок «почему это так».

   Кегли 12/14/16/20/24/32, цвета проекта (золото #ffd76a, зелёный #8fd1a8,
   красный #e86a5a, фон #0b1712/#16241d). Контраст: #e8e0cc на #0b1712 —
   13,9 : 1; на бумаге #2a2118 на #fdf6e0 — 13,9 : 1; #2e6b46 — 8,0 : 1;
   #9c2f22 — 7,4 : 1; #8a6a1f — 5,0 : 1; #5c6b85 — 5,4 : 1. Ниже 4,5 : 1 нет.

   Подключение. Файл встаёт одной строкой в MVP/index.html после
   vis_wk.js — как lesson_olimp_*.js. Запись регистрируется сама: заменяет
   урок 648 в window.ARH_LESSONS, а если его там ещё нет (каталог без
   подключённой строки) — добавляет, поэтому дубль не появится.                    */
(function(){
  const ID = 648;

  /* ---------- палитра проекта ---------- */
  const GOLD='#ffd76a', GREEN='#8fd1a8', RED='#e86a5a', BLUE='#7fd1ff', IVORY='#e8e0cc', MUTE='#cbb89a';
  const BOARD='#0b1712', BOARD2='#16241d', LINE2='#4a6b56';
  const INK='#2a2118', MUT='#5c6b85', RULE='#d8c9a8', PAPER='#fdf6e0', CELL='#fffdf7';
  const OK='#2e6b46', NO='#9c2f22', GOLDD='#8a6a1f', GOLDT='#c99a1e', BLUED='#2e6db4';

  /* ================= УРОК ================= */
  const L = {
    id: ID, title: 'Действия с рациональными числами: сложение и вычитание', ico: '±',
    src: 'Математика · 6 класс · Рациональные числа', subj: 'math',
    explain: [
      /* 0 */ 'Рациональные числа — это все числа со знаком: положительные, отрицательные и нуль. Прямая с нулём посередине: вправо — числа со знаком «+», влево — со знаком «−». Нуль не положительный и не отрицательный.',
      /* 1 */ 'Каждое число на прямой — это точка. Расстояние от нуля до числа называется модулем: |−3| = 3, |5| = 5, |0| = 0. Модуль не бывает отрицательным — это длина, а длина минуса не имеет.',
      /* 2 */ 'Сложение на прямой: к первому слагаемому прибавить второе — значит сдвинуться от него. Прибавить положительное число — сдвиг вправо, прибавить отрицательное — сдвиг влево.',
      /* 3 */ 'Задание 1. −3 + (−5). Шаг 1. Отмечаем −3. Шаг 2. Прибавить −5 значит сдвинуться влево на 5. Шаг 3. Попадаем в −8. Проверка: |−3| + |−5| = 3 + 5 = 8, знак минус. Значит, −3 + (−5) = −8.',
      /* 4 */ 'Почему это так. Оба числа отрицательные — оба тянут влево. Долг 3 и долг 5 дают вместе долг 8. Поэтому модули складываются, а знак остаётся минус: два «минуса» дают «минус».',
      /* 5 */ 'Задание 2. −8 + 5. Шаг 1. Отмечаем −8. Шаг 2. Прибавить 5 значит сдвинуться вправо на 5. Шаг 3. Попадаем в −3. Проверка: |−8| больше, чем |5|, поэтому |−8| − |5| = 8 − 5 = 3 и знак числа с большим модулем — минус. Значит, −8 + 5 = −3.',
      /* 6 */ 'Почему знаки вычитаются. Два движения идут в разные стороны и гасят друг друга: 8 шагов влево и 5 шагов вправо. Остаётся 3 шага влево. Долг 8 и доход 5: долг уменьшается до 3, значит остаётся должен 3.',
      /* 7 */ 'Правило «модули вычесть, поставить знак большего». Когда знаки разные, из большего модуля вычитаем меньший, а знак берём у того числа, чей модуль больше. Проверка на −8 + 5: |−8| = 8, |5| = 5; 8 − 5 = 3; больший модуль у −8, его знак минус, поэтому −3.',
      /* 8 */ 'Порядок не спасает. 8 + (−9): вычитаем модули 9 − 8 = 1, больший модуль у −9, поэтому 8 + (−9) = −1. Доход 8 и потеря 9 — снова должен 1. И различай знак числа и знак действия: в записи −3 + (−5) плюс — это действие, а минусы — знаки чисел.',
      /* 9 */ 'Таблица знаков — короткая шпаргалка для сложения. Знаки одинаковые: модули складываем, знак оставляем. Знаки разные: из большего модуля вычитаем меньший, ставим знак числа с большим модулем.',
      /* 10 */ 'Вычитание — это сдвиг влево. Задание 5. −2 − 7. Шаг 1. Отмечаем −2. Шаг 2. Вычесть 7 значит сдвинуться влево на 7. Шаг 3. Попадаем в −9. Проверка: |−2| + |7| = 2 + 7 = 9, знак минус. Значит, −2 − 7 = −9.',
      /* 11 */ 'Чтобы вычитание не путалось, переводим его в сложение: a − b = a + (−b). Знак действия меняем на «+», а у вычитаемого числа меняем знак. Например, 5 − 8 = 5 + (−8) = −3, а −4 − 9 = −4 + (−9) = −13.',
      /* 12 */ 'Задание 4. 4 − (−6). Здесь вычитаем долг 6 — убираем его. Убрать долг выгодно, поэтому становится больше, чем 4. По правилу: 4 − (−6) = 4 + 6 = 10. На прямой: шаг вправо на 6 от 4 — попадаем в 10.',
      /* 13 */ 'Почему вычитание отрицательного становится сложением. Минус перед скобкой переворачивает знак числа внутри: −(−6) = +6. Поэтому 4 − (−6) = 4 + 6. Отнять долг — всё равно что прибавить такую же сумму денег.',
      /* 14 */ 'Два действия — один порядок работы: вычитание переводим в сложение, а потом применяем таблицу знаков. Проверка одна и та же — движение по прямой: 4 − (−6) = 10 (вправо на 6) и −3 + (−5) = −8 (влево на 5).',
      /* 15 */ 'Задание 6. Цепочка −4 + 9 + (−7). Идём слева направо и на каждом шаге держим в голове одно число. Шаг 1. −4 + 9 = 5: от −4 сдвигаемся вправо на 9. Шаг 2. 5 + (−7) = −2: от 5 сдвигаемся влево на 7. Проверка: положительные 9, отрицательные −4 и −7 дают −11, а 9 − 11 = −2 — то же самое.',
      /* 16 */ 'В цепочке считаем строго слева направо. Менять местами нельзя: замена 6 − 2 на 2 − 6 даёт уже другое число. А вот порядок прибавления менять можно: −4 + 9 + (−7) и 9 + (−7) + (−4) дают один и тот же −2.',
      /* 17 */ 'Задание 7. Долг и зарплата. Долг 2500 рублей — это −2500, зарплата 4000 рублей — это +4000. Шаг 1. Была зарплата: 4000 + (−2500) = 1500. Шаг 2. Был долг: −2500 + 4000 = 1500. Проверка: |−2500| = 2500, |4000| = 4000; 4000 − 2500 = 1500, больший модуль у +4000, знак плюс.',
      /* 18 */ 'Задание 8. Температура. Было −7 °C, стало +3 °C. Изменение — это разность: 3 − (−7) = 3 + 7 = 10, то есть потеплело на 10 °C. Проверка по прямой: от −7 до +3 ровно 10 шагов вправо, значит прибавилось +10.',
      /* 19 */ 'Частые ошибки урока. Первая: сложить модули и не поставить знак — «−3 + (−5) = 8» вместо −8, прямая сразу показывает −8. Вторая: перепутать вычитание отрицательного со сложением — «4 − (−6) = −2» вместо 10. Третья: поставить знак первого числа, а не того, у которого модуль больше — «−8 + 5 = 8». Проверка одна: пройти по прямой и считать шаги.',
      /* 20 */ 'Как проверить себя. Обязательно пройди по прямой: поставь палец на первое число, отсчитай шаги (вправо для «+», влево для «−») и сравни с ответом. В конце сойдись с таблицей знаков: одинаковые знаки — модули сложить, разные — вычесть и взять знак большего модуля.',
      /* 21 */ 'Проверь себя. −8 + 5: вычитаем модули 8 − 5 = 3 и ставим знак −8, потому что 8 больше 5. Ответ −3. Дальше тест, а за ним восемь заданий: в каждом есть кнопка «частая ошибка» и кнопка «проверка» по прямой.'
    ],
    check: { q: 'Чему равно −8 + 5?', choices: ['−3', '3', '−13', '13'], ans: 0,
      exp: 'Знаки разные: из большего модуля вычитаем меньший — 8 − 5 = 3, и ставим знак числа с большим модулем: у −8 он минус. Значит, −3. Проверка по прямой: от −8 пять шагов вправо — попадаем в −3.' },
    tasks: [
      { q: '−6 + (−9): найди сумму.', kind: 'unit', ans: -15, tol: 0,
        hints: ['Знаки одинаковые — модули складываем, знак оставляем: 6 + 9.',
                'Частая ошибка: сложить модули и не поставить знак — ответ «15». Сумма двух отрицательных не может быть положительной: прямая показывает −15.',
                'Проверка по прямой: от −6 влево на 9 шагов — попадаем в −15.'],
        sol: '−6 + (−9) = −(6 + 9) = −15. Проверка по прямой: от −6 девять шагов влево → −15.' },
      { q: '−5 + 8: найди сумму.', kind: 'unit', ans: 3, tol: 0,
        hints: ['Знаки разные — вычитаем модули: 8 − 5.',
                'Частая ошибка: поставить знак первого числа и написать «−3». Знак берём у числа с большим модулем, а |8| больше |−5|.',
                'Проверка по прямой: от −5 вправо на 8 шагов — попадаем в +3.'],
        sol: '−5 + 8 = +(8 − 5) = +3. Проверка по прямой: от −5 восемь шагов вправо → 3.' },
      { q: '3 − (−7): найди значение.', kind: 'unit', ans: 10, tol: 0,
        hints: ['Вычитание отрицательного числа — это прибавление: 3 − (−7) = 3 + 7.',
                'Частая ошибка: перепутать вычитание отрицательного со сложением и написать «3 − 7 = −4». Минус перед скобкой переворачивает знак: −(−7) = +7.',
                'Проверка по прямой: от 3 семь шагов вправо — попадаем в 10.'],
        sol: '3 − (−7) = 3 + 7 = 10. Проверка по прямой: от 3 семь шагов вправо → 10.' }
    ]
  };

  let UID = 0;
  /* ================= геометрия листа =================
     Главное решение: система координат листа РАВНА карточке урока. Замерено,
     что карточка на телефоне 390 px — 374 px, лист внутри неё — 346–360 px
     (лента «Книга знаний» и полосы прокрутки отнимают остальное). Если взять
     эту же ширину и как viewBox, одна единица листа = один CSS-пиксель:
     кегль 16 в системе координат виден на экране как ровно 16 px, стрелки,
     отступы и цели касания тоже считаются в настоящих пикселях.
     Раньше система была фиксированной (320), и на 390-й ширине кегли
     разъезжались — от 14 до 20 px вместо 16.                                 */
  /* Доступная ширина листа внутри карточки, без CSS-переполнения и без
     сюрпризов для кеглей: одна единица системы координат = один CSS-пиксель.
     Кегль 16 = 16 px на телефоне; на планшете и ноутбуке карточка шире, и
     лист масштабируется целиком (media-запрос ниже поднимает потолок до
     560 px, тогда 16 px кегля становится ≈24 px — крупнее, но не мельче).   */
  /* Действительная ширина листа: карточка урока минус её поля и рамка —
     замерено 374 − 2·12 − 2 = 348 px на телефоне 390 px. Ниже 300 px лист
     не сжимается (иначе строка короче 30 знаков), выше 418 px не растёт
     (иначе кегль 16 на ноутбуке превращается в 25 px).                       */
  const cardW = () => {
    try{ return Math.max(298, Math.min(416, (document.documentElement.clientWidth||390) - 44)); }
    catch(e){ return 348; }
  };
  const paperW = () => cardW();
  const S = k => k;                                 /* кегль = CSS-пиксели листа */
  const rect=(x,y,w,h,fill,stroke,r,sw)=>`<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="${r==null?8:r}" fill="${fill}"`
    +(stroke?` stroke="${stroke}" stroke-width="${sw==null?1.4:sw}"`:'')+`/>`;
  const circ=(cx,cy,r,fill,stroke,sw)=>`<circle cx="${cx}" cy="${cy}" r="${r}" fill="${fill}"`
    +(stroke?` stroke="${stroke}" stroke-width="${sw==null?1.5:sw}"`:'')+`/>`;
  const txtStr=s=>String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
  /* текст с «ореолом» бумажного цвета: цифры на прямой не сливаются с линиями */
  function tx(x,y,s,size,fill,bold,anchor,halo){
    let t=txtStr(s);
    const sw=(size*0.11+0.8).toFixed(2);
    if(halo!==false) t=`<tspan stroke="${PAPER}" stroke-width="${sw}" stroke-linejoin="round" paint-order="stroke" fill="${fill}">${t}</tspan>`;
    return `<text x="${x}" y="${y}" text-anchor="${anchor||'middle'}" font-size="${size}" fill="${fill}"`
      +(bold?' font-weight="bold"':'')+` font-family="Georgia,'Times New Roman',serif">${t}</text>`;
  }
  function fitTxt(x,y,boxW,s,size,fill,bold){
    let k=size, need=String(s).length*k*0.60;
    if(need>boxW) k=Math.max(S(16),Math.floor(boxW/Math.max(1,String(s).length)/0.60));
    return tx(x,y,s,k,fill,bold);
  }
  function chip(x,y,w,h,text,fill,stroke,color,size){
    const k=size||S(16);
    let s=rect(x,y,w,h,fill,stroke,h/2,(h/2)-0.2);
    s+=tx(x+w/2,y+h/2+k*0.35,text,k,color||fill,true);
    return s;
  }
  function arrow(x1,y1,x2,y2,color,w){
    const sw=w||2.4, id='m6a'+(++UID);
    return `<defs><marker id="${id}" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="5.5" markerHeight="5.5" orient="auto-start-reverse">`
      +`<path d="M0 0 L10 5 L0 10 z" fill="${color}"/></marker></defs>`
      +`<line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${color}" stroke-width="${sw}" stroke-linecap="round" marker-end="url(#${id})"/>`;
  }
  /* стрелка-сдвиг над прямой: дуга из a в b, показывает перемещение */
  function jump(scale, yBase, a, b, color, up, label){
    const x1=scale.x(a), x2=scale.x(b), u=up?1:-1;
    const top=u>0? yBase-17 : yBase+17;
    const my=top - u*10;
    const id='m6j'+(++UID);
    let s=`<defs><marker id="${id}" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="5" markerHeight="5" orient="auto-start-reverse">`
      +`<path d="M0 0 L10 5 L0 10 z" fill="${color}"/></marker></defs>`
      +circ(x1,yBase,4.6,color)
      +`<path class="m6dr" d="M ${x1} ${top} Q ${(x1+x2)/2} ${my} ${x2} ${top}" fill="none" stroke="${color}" stroke-width="2.6" stroke-linecap="round" marker-end="url(#${id})"/>`
      +`<text x="${(x1+x2)/2}" y="${u>0? top-21 : top+29}" text-anchor="middle" font-size="${S(16)}" font-weight="bold" fill="${color}" font-family="Georgia,'Times New Roman',serif">${txtStr(label)}</text>`;
    return s;
  }
  /* ---------- координатная прямая ----------
     min..max, шаг 1. Цвет числа: холодное (минус) — синий, тёплое (плюс) — красный.
     Подписи ставятся через клетку, если на клетку меньше 22 единиц: при кегле 16
     соседние двузначные подписи иначе сливаются. Через одну — читается. */
  function numberLine(W,y0,min,max,opt){
    const o=opt||{};
    const x0=14, x1=W-14;
    const x=v=>x0+(v-min)/(max-min)*(x1-x0);
    const scale={ x:x, y0:y0, min:min, max:max };
    const клетка=(x1-x0)/(max-min);
    const каждый=Math.max(1,Math.ceil(24/клетка));   /* 24 единицы — ширина подписи «−10» */
    let s=`<line x1="${x0-8}" y1="${y0}" x2="${x1+8}" y2="${y0}" stroke="#8c7a5c" stroke-width="2.4" stroke-linecap="round"/>`;
    s+=arrow(x1+4,y0,Math.min(W-4,x1+18),y0,'#8c7a5c',2.4);
    s+=arrow(x0-4,y0,Math.max(4,x0-18),y0,'#8c7a5c',2.4);
    for(let v=min;v<=max;v++){
      const big=(v===0), col=v<0?BLUED:(v>0?NO:INK);
      s+=`<line x1="${x(v)}" y1="${y0-(big?9:6)}" x2="${x(v)}" y2="${y0+(big?9:6)}" stroke="${big?INK:'#a8977a'}" stroke-width="${big?2.2:1.4}"/>`;
      if((v%каждый===0)||o.allLabels){
        s+=tx(x(v),y0+28,String(v).replace('-','−'),S(big?16:16),big?GOLDD:col,big,undefined,false);
      }
    }
    s+=tx(W-10,y0-14,'О',S(16),INK,true);
    if(o.extra) s+=o.extra(scale);
    return { svg:s, scale:scale };
  }
  /* ---------- бумажный лист ----------
     У svg один атрибут масштабирования — width:100%: лист всегда по ширине
     карточки, а высота выводится из пропорции viewBox и подставляется
     инлайновым aspect-ratio. Поэтому на узком экране (320 px) ничего не
     вылезает, а кегли остаются того же относительного размера.               */
  function paper(inner,H){
    const W=paperW();
    /* Ширина svg задана и в viewBox, и в пикселях: 348 px листа при viewBox
       348 единиц — одна единица = один CSS-пиксель без остатка 2 px на рамку
       (.m6-paper теряет 2 px). При этом max-width:100% и preserveAspectRatio
       не дают листу вылезти за карточку.                                      */
    return `<div class="m6-paper-wrap" style="max-width:${W}px"><div class="m6-paper">`
      +`<svg class="m6-svg" viewBox="0 0 ${W} ${H}" width="100%" height="${H}" preserveAspectRatio="xMidYMid meet" role="img">`
      +`<defs><linearGradient id="m6pg" x1="0" y1="0" x2="0" y2="1">`
      +`<stop offset="0" stop-color="#fffaea"/><stop offset="1" stop-color="#fbf1d6"/></linearGradient>`
      +`<pattern id="m6grid" width="10" height="10" patternUnits="userSpaceOnUse">`
      +`<path d="M10 0 L0 0 0 10" fill="none" stroke="rgba(96,128,168,.18)" stroke-width="1"/></pattern></defs>`
      +`<rect x="0" y="0" width="${W}" height="${H}" rx="12" fill="url(#m6pg)"/>`
      +`<rect x="0" y="0" width="${W}" height="${H}" rx="12" fill="url(#m6grid)" opacity=".55"/>`
      +`<line x1="12" y1="8" x2="12" y2="${H-8}" stroke="rgba(200,90,100,.45)" stroke-width="1"/>`
      +inner(W)+`</svg></div></div>`;
  }
  /* заголовок кадра внутри листа — служебная подпись, 16 px */
  const head=(W,text)=>tx(W/2,24,text,S(16),MUT,false);
  /* кнопки-шаги внутри рисунка: цель не меньше 44 CSS-пикселей по высоте
     (48 единиц при ширине карточки 320 — с запасом на узких телефонах)
     и зазор 10 единиц — это больше 8 px из стандарта. */
  function btns(x,y,list){
    let s='', cx=x;
    for(const b of list){
      if(!b) continue;
      const w=b.w||96;
      s+=`<g class="m6-hit" onclick="${b.on}">`
        +rect(cx,y,w,48,b.fill,b.stroke,10,1.6)
        +tx(cx+w/2,y+31,b.text,S(16),b.color,true)+`</g>`;
      cx+=w+10;
    }
    return s;
  }
  /* панель частой ошибки / проверки / второго способа.
     Внутри панели плоский текст: подпись (жирная) стоит в начале, а дальше фраза
     по 41–45 знаков в строке. Раньше подпись стояла своей колонкой, но при
     кегле 16 и ширине 320 она сталкивалась с текстом — проверено замером
     прямоугольников текста в браузере (наложения). Если текст не влезает
     в четыре строки, кегль сам опускается, но не ниже 14 в системе координат:
     это 16 px на экране телефона и 17,5 px на планшете.                        */
  function note(x,y,w,h,kind1,label,kindColor,color,body){
    let s=rect(x,y,w,h,kind1,kindColor,10,1.8);
    let size=S(16), max=45;
    const вСтроки=(k)=>{
      const слова=String(body).split(' '); let стр='', строки=[];
      for(const сл of слова){
        if((стр+' '+сл).trim().length>k){ строки.push(стр.trim()); стр=сл; }
        else стр=(стр+' '+сл).trim();
      }
      if(стр) строки.push(стр);
      return строки;
    };
    let строки=вСтроки(max);
    while(строки.length>4 && size>S(14)){ size-=1; max=Math.round(max*1.06); строки=вСтроки(max); }
    строки=строки.slice(0,4);
    const шаг=size*1.7, верх=y+size+18;
    строки.forEach((t,i)=>{
      if(i===0){
        const k=label.length*size*0.62+8;
        s+=tx(x+16,верх+i*шаг,label,size,kindColor,true,'start');
        s+=tx(x+16+k,верх+i*шаг,t,size,color,false,'start');
      } else {
        s+=tx(x+16,верх+i*шаг,t,size,color,false,'start');
      }
    });
    return s;
  }
  const ERR_BG='#fdecea', CHK_BG='#eef6ef', TWO_BG='#fdf3d8';

  /* ================= СЦЕНЫ =================
     Все сцены верстаются в системе координат шириной 320. Числа рядом с прямой —
     кегль 16, пояснения — 20, главный ответ — 24–32. Мельче 16 в содержании
     не используется: на 320-й ширине это ровно 16 px на экране.               */

  /* 0 · знакомство: прямая от −5 до 5, слева холодно, справа тепло */
  const sc0=()=>{ const H=222, W=paperW();
    let s=head(W,'все числа со знаком — на одной прямой');
    s+=numberLine(W,110,-5,5,{}).svg;
    s+=tx(W/2,196,'слева от нуля — минус, справа — плюс',S(16),MUT);
    return paper(()=>s,H); };

  /* 1 · модуль — расстояние до нуля */
  const sc1=()=>{ const H=306, W=paperW();
    let s=head(W,'модуль — расстояние от нуля, оно без минуса');
    const nl=numberLine(W,132,-5,5,{extra:(sc)=>{
      let a=arrow(sc.x(-3),164,sc.x(0),164,BLUED,2.4);
      a+=tx((sc.x(-3)+sc.x(0))/2,190,'|−3| = 3',S(16),BLUED,true);
      a+=arrow(sc.x(3),164,sc.x(0),164,RED,2.4);
      a+=tx((sc.x(0)+sc.x(3))/2,190,'|3| = 3',S(16),RED,true);
      return a; }});
    s+=nl.svg;
    s+=rect(16,212,W-32,50,CHK_BG,OK,10,1.6);
    s+=tx(W/2,243,'расстояние не бывает отрицательным',S(16),OK,true);
    return paper(()=>s,H); };

  /* 2 · как ходить по прямой */
  function sc2(){ const H=346, W=paperW();
    const st=st6(LV.id); if(st.d==null) st.d=1;
    const d=st.d, from=-2, to=from+d;
    let s=head(W,'сложение — это сдвиг: «+» вправо, «−» влево');
    s+=numberLine(W,152,Math.min(-6,from,to)-1,Math.max(6,from,to)+1,{
      extra:(sc)=>jump(sc,152,from,to,d>0?RED:BLUED,true,(d>0?'+':'−')+Math.abs(d))}).svg;
    s+=btns(16,194,[
      {text:'+3',w:62,on:`M6ACT('d',3)`,fill:d===3?TWO_BG:CELL,stroke:d===3?GOLDT:RULE,color:INK},
      {text:'−5',w:62,on:`M6ACT('d',-5)`,fill:d===-5?ERR_BG:CELL,stroke:d===-5?NO:RULE,color:INK},
      {text:'−1',w:62,on:`M6ACT('d',-1)`,fill:d===-1?'#e9f2fb':CELL,stroke:d===-1?BLUED:RULE,color:INK},
      {text:'сбросить',w:104,on:`M6ACT('rst')`,fill:'#f7f0de',stroke:RULE,color:INK}]);
    s+=note(16,244,W-32,86,d>0?CHK_BG:ERR_BG,(d>0?'Вправо: ':'Влево: '),d>0?OK:NO,
      INK,'из −2 шагаем на '+Math.abs(d)+' и приходим в '+String(to).replace('-','−')+'.');
    return paper(()=>s,H);
  }

  /* 3 · задание 1: −3 + (−5) = −8 */
  const sc3=()=>{ const H=272, W=paperW();
    let s=head(W,'задание 1 · −3 + (−5)');
    s+=numberLine(W,136,-9,1,{extra:(sc)=>
      jump(sc,136,-3,-8,BLUED,true,'−5')+circ(sc.x(-3),136,4.8,INK)}).svg;
    s+=tx(W/2,222,'–3 → влево 5 → –8, знак минус',S(16),INK);
    s+=chip(W/2-84,236,168,32,'−3 + (−5) = −8',CHK_BG,OK,OK,S(16));
    return paper(()=>s,H); };

  /* 4 · почему так: долг 3 и долг 5 */
  const sc4=()=>{ const H=308, W=paperW(); let s=head(W,'почему два минуса дают минус');
    const mid=W/2;
    s+=rect(12,36,mid-18,96,'#eef2fa','#9db6d8',10,1.6);
    s+=tx(mid/2-2,64,'на прямой',S(16),MUT,true);
    s+=tx(mid/2-2,92,'оба сдвига влево,',S(16),INK);
    s+=tx(mid/2-2,116,'поэтому складываем',S(16),INK);
    s+=rect(mid+6,36,mid-18,96,'#fdecea','#e2a6a0',10,1.6);
    s+=tx(mid+mid/2-2,64,'через долг',S(16),MUT,true);
    s+=tx(mid+mid/2-2,92,'3 + 5 = 8, значит',S(16),INK);
    s+=tx(mid+mid/2-2,116,'должен 8',S(16),INK);
    s+=tx(W/2,164,'|−3| + |−5| = 3 + 5 = 8',S(20),GOLDD,true);
    s+=tx(W/2,190,'модули сложились, знак остался «−»',S(16),MUT);
    s+=chip(W/2-84,206,168,36,'−3 + (−5) = −8',CHK_BG,OK,OK,S(16));
    s+=tx(W/2,270,'второй способ — через долг',S(16),GOLDD,true);
    s+=tx(W/2,292,'долг 3 рубля и долг 5 рублей — 8 рублей долга',S(16),MUT);
    return paper(()=>s,H); };

  /* 5 · задание 2: −8 + 5 = −3 */
  const sc5=()=>{ const H=300, W=paperW(); let s=head(W,'задание 2 · −8 + 5');
    s+=numberLine(W,140,-9,1,{extra:(sc)=>
      jump(sc,140,-8,-3,RED,true,'+5')+circ(sc.x(-8),140,4.8,INK)}).svg;
    s+=rect(16,196,W-32,86,CHK_BG,OK,10,1.8);
    s+=tx(28,226,'Шаг 1. Отмечаем −8.',S(16),INK,false,'start');
    s+=tx(28,250,'Шаг 2. Вправо на 5. Шаг 3. Попадаем в −3.',S(16),INK,false,'start');
    s+=tx(28,274,'Проверка: 8 − 5 = 3, знак минус',S(16),OK,true,'start');
    return paper(()=>s,H); };

  /* 6 · принцип: вычитаем модули, знак большего */
  const sc6=()=>{ const H=304, W=paperW(); let s=head(W,'модули вычесть, знак — большего');
    const bx=W/2-104;
    s+=arrow(bx,66,bx,104,BLUED,2.6)+tx(bx-14,58,'−8',S(20),BLUED,true);
    s+=arrow(bx+208,104,bx+208,66,RED,2.6)+tx(bx+222,58,'+5',S(20),RED,true);
    s+=tx(W/2-30,88,'8 влево',S(16),BLUED);
    s+=tx(W/2+30,122,'5 вправо',S(16),RED);
    s+=tx(W/2,172,'8 − 5 = 3',S(32),GOLDD,true);
    s+=rect(16,204,W-32,92,CHK_BG,OK,10,1.8);
    s+=tx(28,238,'модуль −8 больше модуля +5,',S(16),INK,false,'start');
    s+=tx(28,262,'поэтому в ответе знак минус:',S(16),INK,false,'start');
    s+=tx(28,288,'−8 + 5 = −3',S(20),OK,true,'start');
    return paper(()=>s,H); };

  /* 7 · порядок не спасает: 8 + (−9) */
  const sc7=()=>{ const H=296, W=paperW(); let s=head(W,'первое число может быть и положительным');
    s+=numberLine(W,144,-4,9,{extra:(sc)=>
      jump(sc,144,8,-1,BLUED,true,'−9')+circ(sc.x(8),144,4.8,INK)}).svg;
    s+=rect(16,204,W-32,80,ERR_BG,NO,10,1.8);
    s+=tx(28,232,'доход 8, потеря 9 — снова должен 1',S(16),NO,true,'start');
    s+=tx(28,256,'9 − 8 = 1, больший модуль у −9,',S(16),INK,false,'start');
    s+=tx(28,280,'значит 8 + (−9) = −1',S(20),INK,true,'start');
    return paper(()=>s,H); };

  /* 8 · таблица знаков (правило) */
  const sc8=()=>{ const H=318, W=paperW(); let s=head(W,'два случая — и всё сложение');
    const y=38, rh=100, x=12, w=W-24;
    s+=rect(x,y,w,rh,'#eef2fa','#9db6d8',10,1.8);
    s+=tx(x+14,y+30,'знаки одинаковые',S(20),BLUED,true,'start');
    s+=tx(x+14,y+58,'модули складываем, знак оставляем',S(16),INK,false,'start');
    s+=tx(x+14,y+88,'−7 + (−2) = −9',S(20),BLUED,true,'start');
    s+=rect(x,y+rh+12,w,rh,'#eef6ef','#8fbf9f',10,1.8);
    s+=tx(x+14,y+rh+42,'знаки разные',S(20),OK,true,'start');
    s+=tx(x+14,y+rh+70,'из большего модуля вычитаем меньший',S(16),INK,false,'start');
    s+=tx(x+14,y+rh+100,'6 + (−4) = 2',S(20),OK,true,'start');
    s+=tx(W/2,300,'проверь на прямой каждую строку',S(16),MUT);
    return paper(()=>s,H); };

  /* 9 · −2 − 7 = −9 */
  const sc9=()=>{ const H=288, W=paperW(); let s=head(W,'задание 5 · −2 − 7');
    s+=numberLine(W,140,-10,0,{extra:(sc)=>
      jump(sc,140,-2,-9,BLUED,true,'−7')+circ(sc.x(-2),140,4.8,INK)}).svg;
    s+=tx(W/2,206,'и действие, и число ведут влево',S(16),MUT);
    s+=tx(W/2,236,'|−2| + |7| = 2 + 7 = 9',S(20),GOLDD,true);
    s+=chip(W/2-72,250,144,32,'−2 − 7 = −9',CHK_BG,OK,OK,S(16));
    return paper(()=>s,H); };

  /* 10 · вычитание = сложение с противоположным */
  const sc10=()=>{ const H=302, W=paperW(); let s=head(W,'вычитание переводим в сложение');
    s+=chip(W/2-104,38,208,44,'a − b = a + (−b)',CELL,GOLDT,INK,S(20));
    s+=tx(W/2,110,'знак действия меняем на «+»,',S(16),INK);
    s+=tx(W/2,134,'у вычитаемого числа меняем знак',S(16),INK);
    s+=rect(16,152,W-32,50,'#f7f0de',RULE,10,1.6);
    s+=tx(28,183,'5 − 8 = 5 + (−8) = −3',S(16),INK,true,'start');
    s+=rect(16,212,W-32,50,'#f7f0de',RULE,10,1.6);
    s+=tx(28,243,'−4 − 9 = −4 + (−9) = −13',S(16),INK,true,'start');
    s+=tx(W/2,286,'проверка прямой: −3 правее −8, −13 левее −4',S(16),MUT);
    return paper(()=>s,H); };

  /* 11 · 4 − (−6) = 10 */
  const sc11=()=>{ const H=300, W=paperW(); let s=head(W,'задание 4 · 4 − (−6)');
    s+=numberLine(W,140,0,11,{extra:(sc)=>
      jump(sc,140,4,10,RED,true,'+6')+circ(sc.x(4),140,4.8,INK)}).svg;
    s+=rect(16,204,W-32,80,CHK_BG,OK,10,1.8);
    s+=tx(28,226,'минус перед скобкой переворачивает знак:',S(16),INK,false,'start');
    s+=tx(28,250,'−(−6) = +6 — идём вправо',S(16),INK,false,'start');
    s+=tx(28,280,'4 − (−6) = 4 + 6 = 10',S(20),OK,true,'start');
    return paper(()=>s,H); };

  /* 12 · цепочка −4 + 9 + (−7) = −2 */
  function sc12(){ const H=338, W=paperW();
    const st=st6(LV.id), k=Math.min(2,Math.max(0,st.c||0));
    let s=head(W,'задание 6 · −4 + 9 + (−7)');
    s+=numberLine(W,138,-5,9,{extra:(sc)=>{
      let a=jump(sc,138,-4,5,RED,true,'+9');
      if(k>=1) a+=circ(sc.x(5),138,4.8,INK);
      if(k>=2) a+=jump(sc,138,5,-2,BLUED,true,'−7');
      return a; }}).svg;
    s+=btns(16,180,[
      k<1?{text:'шаг 1',w:96,on:`M6ACT('c')`,fill:TWO_BG,stroke:GOLDT,color:INK}:null,
      k<2?{text:'шаг 2',w:96,on:`M6ACT('c')`,fill:CELL,stroke:RULE,color:INK}:null,
      k>=2?{text:'заново',w:110,on:`M6ACT('crst')`,fill:CELL,stroke:RULE,color:INK}:null]);
    if(k>=2){
      s+=rect(16,242,W-32,80,CHK_BG,OK,10,1.8);
      s+=tx(28,270,'слева направо: −4 + 9 = 5,',S(16),INK,false,'start');
      s+=tx(28,294,'затем 5 + (−7) = −2',S(16),INK,false,'start');
      s+=tx(28,316,'второй способ: 9 − 11 = −2',S(16),OK,true,'start');
    } else {
      s+=tx(W/2,266,'идём слева направо, держим одно число',S(16),MUT);
      s+=tx(W/2,294,'менять местами нельзя: 6 − 2 и 2 − 6 — разное',S(16),MUT);
    }
    return paper(()=>s,H);
  }

  /* 13 · долг и зарплата */
  const sc13=()=>{ const H=306, W=paperW(); let s=head(W,'задание 7 · долг 2500 и зарплата 4000');
    s+=rect(16,38,W-32,72,ERR_BG,NO,10,1.8);
    s+=tx(32,66,'долг 2500 — это −2500',S(20),NO,true,'start');
    s+=tx(32,94,'уменьшает счёт',S(16),INK,false,'start');
    s+=rect(16,122,W-32,72,CHK_BG,OK,10,1.8);
    s+=tx(32,150,'зарплата 4000 — это +4000',S(20),OK,true,'start');
    s+=tx(32,178,'увеличивает счёт',S(16),INK,false,'start');
    s+=tx(W/2,236,'−2500 + 4000 = +1500',S(24),GOLDD,true);
    s+=tx(W/2,262,'долг закрыт полностью — на счёте 1500 рублей',S(16),MUT);
    s+=tx(W/2,288,'проверка: 4000 − 2500 = 1500, знак «+»',S(16),MUT);
    return paper(()=>s,H); };

  /* 14 · температура: было −7, стало +3 */
  const sc14=()=>{ const H=300, W=paperW(); let s=head(W,'задание 8 · было −7 °C, стало +3 °C');
    s+=thermo(30,54,128,-7);
    s+=thermo(W-116,54,128,3);
    s+=arrow(W/2-52,120,W/2+52,120,RED,2.6);
    s+=tx(W/2,108,'+10 °C',S(20),RED,true);
    s+=rect(16,200,W-32,78,CHK_BG,OK,10,1.8);
    s+=tx(28,228,'изменение — это разность:',S(16),INK,false,'start');
    s+=tx(28,258,'3 − (−7) = 3 + 7 = 10',S(20),OK,true,'start');
    return paper(()=>s,H); };

  /* термометр: трубка от −10 до +10, ртуть по значению */
  function thermo(x,y,h,val){
    const top=y+S(24), bot=y+h-S(30);
    const py=v=>bot-((v+10)/20)*(bot-top);
    let s=`<rect x="${x+16}" y="${top-10}" width="${S(22)}" height="${bot-top+20}" rx="${S(11)}" fill="${CELL}" stroke="${RULE}" stroke-width="1.6"/>`;
    s+=`<rect x="${x+16+S(4)}" y="${py(val)}" width="${S(14)}" height="${bot-py(val)}" rx="${S(7)}" fill="${val<0?BLUED:RED}"/>`;
    s+=circ(x+16+S(11),bot+S(12),S(13),val<0?BLUED:RED);
    for(let v=-10;v<=10;v+=5){
      const yy=py(v), big=(v===0||v%10===0);
      s+=`<line x1="${x+S(40)}" y1="${yy}" x2="${x+S(48)}" y2="${yy}" stroke="${MUT}" stroke-width="${big?2:1.2}"/>`;
      if(v%10===0) s+=tx(x+S(56),yy+S(6),String(v).replace('-','−'),S(16),v===0?INK:MUT,big,'start',false);
    }
    s+=tx(x+S(28),y+S(8),'термометр',S(16),MUT);
    return s;
  }

  /* 15 · частые ошибки — таблица «так нельзя» и «как поймать» */
  function sc15(){ const H=386, W=paperW();
    let s=head(W,'частые ошибки и проверка прямой');
    const rows=[
      ['−3 + (−5) = 8', 'нет знака'],
      ['4 − (−6) = −2', 'знак перед скобкой'],
      ['−8 + 5 = 8', 'взял знак первого'],
      ['5 − 8 = −13', 'сложил модули']
    ];
    const x=12, w=W-24, y0=36, rh=58;
    s+=tx(x+8,y0+16,'так нельзя',S(16),NO,true,'start');
    s+=tx(x+w-8,y0+16,'как поймать',S(16),OK,true,'end');
    rows.forEach((r,i)=>{
      const y=y0+24+i*(rh+6);
      s+=rect(x,y,w,rh,i%2?CELL:'#fdecea',i%2?RULE:'#e2a6a0',10,1.4);
      s+=tx(x+12,y+36,r[0],S(16),NO,true,'start');
      s+=tx(x+w-12,y+36,r[1],S(16),INK,false,'end');
    });
    s+=tx(W/2,y0+30+rows.length*(rh+6)+8,'проверка одна: шаги по прямой',S(16),GOLDD,true);
    return paper(()=>s,H);
  }

  /* 16 · как проверить себя: разбор по шагам */
  function sc16(){ const H=336, W=paperW();
    const st=st6(LV.id), go=st.go||0;
    let s=head(W,'проверь себя · −4 + 9 + (−7)');
    const lines=[
      'Шаг 1. Из −4 вправо на 9 — попадаем в 5.',
      'Шаг 2. Из 5 влево на 7 — попадаем в −2.',
      'Шаг 3. Знаки разные: 7 − 5 = 2, у −7 знак минус.',
      'Проверка. 9 − 11 = −2 — сходится.'
    ];
    lines.slice(0,Math.min(4,go+1)).forEach((t,i)=>{
      const y=40+i*44;
      s+=rect(16,y,W-32,38,i%2?TWO_BG:CHK_BG,i%2?RULE:'#8fbf9f',9,1.5);
      s+=tx(28,y+25,t,S(16),INK,false,'start');
    });
    if(go>=3) s+=tx(W/2,240,'−4 + 9 + (−7) = −2',S(24),GOLDD,true);
    else s+=tx(W/2,240,'−4 + 9 + (−7) = ?',S(20),MUT,true);
    s+=btns(16,256,[
      go<3?{text:'следующий шаг',w:168,on:`M6ACT('go')`,fill:TWO_BG,stroke:GOLDT,color:INK}:null,
      go>=3?{text:'заново',w:110,on:`M6ACT('rst')`,fill:CELL,stroke:RULE,color:INK}:null]);
    return paper(()=>s,H);
  }

  /* 17 · три быстрых вопроса по рисункам */
  function sc17(){ const H=344, W=paperW();
    const st=st6(LV.id), pick=st.pick;
    const qs=[
      {q:'−3 + (−5) = ?', opts:['−8','8','−2'], ans:0, why:'модули складываем: 3 + 5 = 8, знак минус'},
      {q:'−8 + 5 = ?', opts:['−3','3','−13'], ans:0, why:'8 − 5 = 3, знак числа с большим модулем'},
      {q:'4 − (−6) = ?', opts:['10','−2','2'], ans:0, why:'минус перед скобкой: −(−6) = +6, значит 4 + 6'}
    ];
    const cur=st.q||0, T=qs[cur];
    let s=head(W,'три быстрых вопроса по рисункам');
    s+=tx(W/2,68,T.q,S(24),INK,true);
    const bw=(W-32-12)/3;
    T.opts.forEach((o,i)=>{
      const x=16+i*(bw+6);
      const on=(pick===i), right=(i===T.ans);
      const bg=on?(right?CHK_BG:ERR_BG):CELL, bd=on?(right?OK:NO):RULE;
      s+=`<g class="m6-hit" onclick="M6ACT('pick',${i})">`+rect(x,86,bw,48,bg,bd,10,1.6)
        +tx(x+bw/2,117,o,S(20),on?(right?OK:NO):INK,true)+`</g>`;
    });
    if(pick==null){
      s+=tx(W/2,168,'нажми на ответ — покажу правило',S(16),MUT);
    } else {
      s+=note(16,146,W-32,88,pick===T.ans?CHK_BG:ERR_BG,pick===T.ans?'Верно: ':'Разбор: ',
        pick===T.ans?OK:NO,INK,T.why);
      s+=tx(W/2,248,pick===T.ans?'проверка по прямой сходится':'правильный ответ: '+T.opts[T.ans],
        S(16),pick===T.ans?OK:NO,true);
    }
    s+=btns(16,268,[
      cur<2?{text:'следующий вопрос',w:184,on:`M6ACT('nq')`,fill:TWO_BG,stroke:GOLDT,color:INK}
           :{text:'к тесту',w:120,on:`M6ACT('nq')`,fill:CHK_BG,stroke:OK,color:OK},
      pick!=null?{text:'заново',w:100,on:`M6ACT('prst')`,fill:CELL,stroke:RULE,color:INK}:null]);
    return paper(()=>s,H);
  }

  /* 18 · шпаргалка */
  const sc18=()=>{ const H=322, W=paperW(); let s=head(W,'шпаргалка урока');
    s+=chip(16,38,W-32,46,'сложение: знак и модули',CELL,GOLDT,INK,S(20));
    s+=tx(W/2,110,'одинаковые знаки — модули сложить',S(16),INK);
    s+=tx(W/2,134,'разные — вычесть и взять знак большего',S(16),INK);
    s+=chip(16,150,W-32,46,'вычитание: a − b = a + (−b)',CELL,BLUED,INK,S(20));
    s+=tx(W/2,222,'минус перед скобкой переворачивает знак',S(16),INK);
    s+=rect(16,238,W-32,72,CHK_BG,OK,10,1.8);
    s+=tx(28,266,'проверка всегда одна:',S(16),OK,true,'start');
    s+=tx(28,292,'шаги по прямой — вправо и влево',S(16),INK,false,'start');
    return paper(()=>s,H); };

  /* ================= восемь заданий: ошибка, проверка, второй способ =================
     Рисунок задания, под ним три кнопки, под ними ровно одна панель. Высота
     кадра одна и та же при любом нажатии, поэтому ничего не прыгает.          */
  function taskScene(n){
    return function(){
      const W=paperW(), st=st6(LV.id), T=TASKS[n-1], mode=st.mode;
      const H=362;
      let s=head(W,'задание '+n+' · '+T.head);
      s+=T.art(W);
      s+=btns(10,186,[
        {text:'ошибка',w:100,on:`M6ACT('mode','err')`,fill:mode==='err'?ERR_BG:CELL,stroke:mode==='err'?NO:RULE,color:mode==='err'?NO:INK},
        {text:'проверка',w:112,on:`M6ACT('mode','chk')`,fill:mode==='chk'?CHK_BG:CELL,stroke:mode==='chk'?OK:RULE,color:mode==='chk'?OK:INK},
        T.two?{text:'способ 2',w:110,on:`M6ACT('mode','two')`,fill:mode==='two'?TWO_BG:CELL,stroke:mode==='two'?GOLDT:RULE,color:mode==='two'?GOLDD:INK}:null]);
      const body = mode==='err'?T.err : (mode==='two'?T.two : T.chk);
      const kind = mode==='err'?ERR_BG:(mode==='two'?TWO_BG:CHK_BG);
      const kcol = mode==='err'?NO:(mode==='two'?GOLDT:OK);
      const label= mode==='err'?'Ошибка: ':(mode==='two'?'Способ 2: ':'Проверка: ');
      s+=note(10,246,W-20,106,kind,label,kcol,INK,body);
      return paper(()=>s,H);
    };
  }

  const TASKS=[
    { head:'−3 + (−5)',  art:(W)=>nl6(W,132,-9,0,-3,-8,BLUED,'−5'),
      err:'Сложить модули и не поставить знак: «3 + 5 = 8, ответ 8». На прямой мы ушли влево от −3 и оказались в −8, а не в 8.',
      chk:'От −3 пять шагов влево — попадаем в −8. Модули: |−3| + |−5| = 8, знак минус. Значит, −3 + (−5) = −8.',
      two:'Долг 3 рубля и долг 5 рублей — вместе 8 рублей долга, то есть −8 рублей. Сумма двух долгов всегда долг.' },
    { head:'−8 + 5',     art:(W)=>nl6(W,132,-9,1,-8,-3,RED,'+5'),
      err:'Взять знак первого числа и написать «8». Смотри на модули: |−8| больше |5|, значит в ответе знак минус.',
      chk:'От −8 пять шагов вправо — попадаем в −3. 8 − 5 = 3, знак числа с большим модулем: −3. Совпало.',
      two:'Долг 8 и доход 5: доход гасит часть долга, остаётся долг 3 — это −3. Долг уменьшился, но не исчез.' },
    { head:'−2 − 7',     art:(W)=>nl6(W,132,-10,0,-2,-9,BLUED,'−7'),
      err:'Поставить знак плюс, потому что «минус на минус даёт плюс». Здесь только одно вычитание: вычесть 7 — значит сдвинуться влево.',
      chk:'От −2 семь шагов влево — попадаем в −9. Модули: 2 + 7 = 9, знак минус. Значит, −2 − 7 = −9.',
      two:'Через сложение: −2 − 7 = −2 + (−7) = −9. Долг 2 рубля вырос ещё на 7 — стал 9 рублей долга.' },
    { head:'4 − (−6)',   art:(W)=>nl6(W,132,0,11,4,10,RED,'+6'),
      err:'Перепутать вычитание отрицательного со сложением и написать «4 − 6 = −2». На прямой от 4 мы идём вправо, а не влево.',
      chk:'От 4 шесть шагов вправо — попадаем в 10. Правило: 4 − (−6) = 4 + 6 = 10. Совпало.',
      two:'Убрать долг 6 рублей — всё равно что получить 6 рублей: 4 + 6 = 10. Отнять долг выгодно, поэтому число растёт.' },
    { head:'−5 + 8',     art:(W)=>nl6(W,132,-6,9,-5,3,RED,'+8'),
      err:'Оставить знак минус: «−3». Знак берём не у первого числа, а у того, чей модуль больше: |8| больше |−5|.',
      chk:'От −5 восемь шагов вправо: −5 → 0 (пять шагов), ещё три — попадаем в 3. 8 − 5 = 3, знак плюс.',
      two:'Долг 5 и доход 8: доход закрывает долг и оставляет 3 в плюсе — это +3.' },
    { head:'−6 + 2 + (−5)', art:(W)=>nl6(W,132,-7,3,-6,-4,BLUED,'+2'),
      err:'Сложить сразу все модули: «6 + 2 + 5 = 13» и потерять знак. Считать нужно шагами: сначала −6 + 2, потом результат с −5.',
      chk:'Слева направо: −6 + 2 = −4 (вправо на 2), затем −4 + (−5) = −9 (влево на 5). Проверка: 2 − 11 = −9. Совпало.',
      two:'Сложить отдельно плюсы и минусы: плюсы дают +2, минусы дают −6 и −5, то есть −11. Итого 2 − 11 = −9.' },
    { head:'−3000 + 4500', art:(W)=>money6(W),
      err:'Решить, что долг больше: «остался должен 1500». На самом деле 4500 больше 3000, поэтому счёт в плюсе.',
      chk:'Модули: 4500 − 3000 = 1500, знак у числа с большим модулем — плюс. Проверка: 3000 + 1500 = 4500. Совпало.',
      two:'Через деньги: зарплата сначала закрывает долг 3000, и остаётся 1500 рублей на счету — это +1500.' },
    { head:'3 − (−7) по температуре', art:(W)=>temp6(W),
      err:'Считать изменение как «−7 + 3 = −4» и решить, что стало холоднее. Изменение — это разность: конечное минус начальное.',
      chk:'От −7 до 0 — семь шагов, от 0 до +3 — ещё три: всего 10 шагов вправо, значит +10 °C.',
      two:'Сложением: 3 − (−7) = 3 + 7 = 10. Потеплело на 10 °C: от −7 °C до +3 °C.' }
  ];
  /* единый мини-рисунок задания: прямая со стрелкой сдвига */
  function nl6(W,y,min,max,from,to,color,label){
    let s='';
    if(label) { const nl=numberLine(W,y,min,max,{extra:(sc)=>
      jump(sc,y,from,to,color,true,label)+circ(sc.x(from),y,4.6,INK)}); s+=nl.svg; }
    else s+=numberLine(W,y,min,max,{}).svg;
    return s;
  }
  /* долг и зарплата: две полосы разного знака и итог */
  function money6(W){
    let s='';
    s+=tx(W/2,52,'долг 3000 и зарплата 4500',S(16),INK,true);
    s+=rect(20,66,W/2-30,50,ERR_BG,NO,9,1.6);
    s+=tx(W/4+5,98,'−3000',S(20),NO,true);
    s+=rect(W/2+10,66,W/2-30,50,CHK_BG,OK,9,1.6);
    s+=tx(W*3/4-5,98,'+4500',S(20),OK,true);
    s+=tx(W/2,150,'4500 − 3000 = 1500',S(20),GOLDD,true);
    s+=tx(W/2,176,'знак у числа с большим модулем — плюс',S(16),INK);
    s+=chip(W/2-96,188,192,34,'на счету +1500',CELL,RULE,INK,S(16));
    return s;
  }
  /* температура: термометр до/после и шаг изменения */
  function temp6(W){
    let s='';
    s+=tx(W/2,54,'было −7 °C, стало +3 °C',S(16),INK,true);
    s+=thermo(30,66,118,-7);
    s+=thermo(W-116,66,118,3);
    s+=arrow(W/2-52,126,W/2+52,126,RED,2.6);
    s+=tx(W/2,114,'+10 °C',S(20),RED,true);
    s+=tx(W/2,168,'разность: 3 − (−7) = 10',S(16),INK);
    return s;
  }

  /* Кадры 19–26 — восемь заданий урока против шагов объяснения:
       19 задание 1 — −3 + (−5)      шаг 3
       20 задание 2 — −8 + 5         шаг 5
       21 задание 5 — −2 − 7         шаг 10
       22 задание 4 — 4 − (−6)       шаг 12
       23 знаки разные — −5 + 8      шаг 7
       24 задание 6 — цепочка        шаг 15
       25 задание 7 — долг и зарплата шаг 17
       26 задание 8 — температура    шаг 18                                        */
  const SCENES=[sc0,sc1,sc2,sc3,sc4,sc5,sc6,sc7,sc8,sc9,sc10,sc11,sc12,sc13,sc14,sc15,sc16,sc17,sc18,
                taskScene(1),taskScene(2),taskScene(3),taskScene(4),taskScene(5),taskScene(6),taskScene(7),taskScene(8)];

  /* ================= состояние и обработчики ================= */
  function st6(id){ const lk=(typeof lidKey==='function')?lidKey(id):String(id);
    if(!window.M6ST) window.M6ST={};
    if(!M6ST[lk]) M6ST[lk]={};
    return M6ST[lk]; }
  window.M6ACT=function(act,a,b){
    const st=st6(ID);
    if(act==='rst'){ for(const k in st) delete st[k]; st._at=null; }
    else if(act==='go'){ st.go=(st.go||0)+1; }
    else if(act==='d'){ st.d=a; }
    else if(act==='c'){ st.c=Math.min(2,(st.c||0)+1); }
    else if(act==='crst'){ st.c=0; }
    else if(act==='mode'){ st.mode=(st.mode===a)?null:a; }
    else if(act==='pick'){ if(st.pick==null) st.pick=a; }
    else if(act==='nq'){ st.q=((st.q||0)+1)%3; st.pick=null; }
    else if(act==='prst'){ st.q=0; st.pick=null; }
    const el=document.getElementById('lvis'); if(el){ try{ chRender(0); }catch(e){} }
  };

  /* ================= показ урока ================= */
  window.WAVE_B=window.WAVE_B||{};
  const baseWave=window.WAVE_B[ID];
  window.WAVE_B[ID]=function(el){
    css648();                       /* правила листа к моменту первой отрисовки точно на месте */
    let step=0;
    try{ step=(typeof LV!=='undefined'&&LV)?(LV.step||0):0; }catch(e){ step=0; }
    const st=st6(ID);
    if(st._at!==step){
      st._at=step; st.go=0; st.pick=null; st.d=1;
      if(step===12) st.c=0;
      if(step===17) st.q=0;
    }
    const sc=SCENES[step];
    if(sc){
      try{ el.innerHTML=`<div class="m6-wrap">${sc()}</div>`; return; }
      catch(e){ try{ console.error('лист 648, кадр '+step+':',e); }catch(_){} }
    }
    if(baseWave){ try{ baseWave(el); return; }catch(e){} }
    el.innerHTML='';
  };

  /* ================= стили =================
     Важно: строка подключения листа стоит в <head>, и на момент выполнения файла
     document.head ещё null — стили вставить некуда, а ошибка ловится пустым catch
     (так и было: лист рисовался без своих правил, лист растягивался на всю
     карточку). Поэтому кладём стили отложенно: сразу, по DOMContentLoaded,
     по load и таймером, и повторяем при первой отрисовке кадра.               */
  const CSS648=
        '#lvis .m6-wrap{width:100%;display:flex;justify-content:center;}'+
        '#lvis .m6-paper-wrap{width:100%;margin:0 auto;}'+
        '#lvis .m6-paper{width:100%;border:1px solid '+RULE+';border-radius:14px;overflow:hidden;'+
          'box-shadow:0 10px 26px rgba(26,20,12,.28),inset 0 1px 0 rgba(255,255,255,.7);}'+
        '#lvis .m6-svg{display:block;width:100%;height:auto}'+
        '#lvis .m6-svg text{-webkit-font-smoothing:antialiased;text-rendering:geometricPrecision}'+
        '#lvis .m6-hit{cursor:pointer}'+
        '#lvis .m6-hit rect{transition:transform 140ms cubic-bezier(.23,1,.32,1),stroke 140ms ease}'+
        '#lvis .m6-hit:active rect{transform:translateY(2px)}'+
        '#lvis .m6-hit:focus-visible rect{stroke:'+GOLDT+';stroke-width:2.6}'+
        '#lvis .m6-dr{stroke-dasharray:240;stroke-dashoffset:240;animation:m6draw 520ms cubic-bezier(.23,1,.32,1) 60ms both}'+
        '@keyframes m6draw{to{stroke-dashoffset:0}}'+
        '@media (prefers-reduced-motion: reduce){#lvis .m6-dr{animation:none;stroke-dashoffset:0}}';
  function css648(){
    try{
      const голова=document.head||document.getElementsByTagName('head')[0];
      if(!голова) return;
      let e=document.getElementById('m6-648-css');
      if(!e){ e=document.createElement('style'); e.id='m6-648-css'; голова.appendChild(e); }
      if(e.textContent!==CSS648) e.textContent=CSS648;
    }catch(e){}
  }
  css648();
  try{ window.addEventListener('DOMContentLoaded',css648); window.addEventListener('load',css648); }catch(e){}
  try{ setTimeout(css648,300); }catch(e){}

  /* ================= регистрация в каталоге =================
     Тонкость сборки, из-за которой обычного push мало: четыре файла собирают
     window.ARH_LESSONS заново и присваивают его целиком —
       data/lessons.js (252 урока), data/lessons_draft.js (393),
       data/game.js (441, добавляет игру), data/inf_56.js (478, курс 500–535).
     Поэтому урок, добавленный push-ем ДО них, исчезает: массив заменяют
     новым. Проверено замером (журнал присваиваний ARH_LESSONS в браузере).
     Отсюда два уровня защиты:
       1) ставим записывающее свойство ARH_LESSONS: любое новое присваивание
          получает наш урок, если его там ещё нет. Свойство конфигурируемое,
          поведение массива не меняется — это обычный массив;
       2) если собрать свойство не удалось — досыпаем урок по load и таймером.
     Ничего чужого при этом не переписывается: урок добавляется только тогда,
     когда урока 648 в каталоге нет.                                          */
  (function(){
    /* Возвращает массив с нашим уроком. Важно: в геттере отдаём именно тот
       массив, который присвоили, — иначе следующий файл (data/lessons_draft.js
       читает ARH_LESSONS и добавляет свои уроки) получил бы пустой массив и
       каталог остался бы без уроков. На этом уже обожглись при первой сборке. */
    function вставить(массив){
      if(!массив || typeof массив.length!=='number') return массив;
      try{
        for(let i=0;i<массив.length;i++){
          if(массив[i] && массив[i].id===ID){ массив[i]=L; return массив; }
        }
        массив.push(L);
      }catch(e){}
      return массив;
    }
    let сейчас=вставить(window.ARH_LESSONS);
    let перехвачено=false;
    try{
      Object.defineProperty(window,'ARH_LESSONS',{
        configurable:true, enumerable:true,
        get:function(){ return сейчас; },
        set:function(v){ сейчас=вставить(v); }
      });
      перехвачено=true;
    }catch(e){ /* свойство занято — работают запасные проверки ниже */ }
    if(!перехвачено){
      const досыпать=()=>{ сейчас=вставить(window.ARH_LESSONS); };
      try{ window.addEventListener('DOMContentLoaded',досыпать); }catch(e){}
      try{ window.addEventListener('load',досыпать); }catch(e){}
      try{ let n=0; const t=setInterval(()=>{ досыпать(); if(++n>20) clearInterval(t); },250); }catch(e){}
    }
  })();
})();
