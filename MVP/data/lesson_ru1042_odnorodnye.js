/* ============ РУССКИЙ ЯЗЫК · УРОК 1042 · «ОДНОРОДНЫЕ ЧЛЕНЫ И ДВОЕТОЧИЕ» ============
   5 класс, «Синтаксис и пунктуация». Продолжение линии 877–884 (речь, текст,
   фонетика, орфоэпия, О и ё после шипящих, Ы и и после ц).

   ФОРМАТ — КАК У РУССКИХ УРОКОВ (vis_ru.js): window.RULESSON({id, game, frames}).
   Кадры — HTML-блоки каркаса RUKIT: .kicker, <h2>, .lead, .cap, .row,
   .card/.nm/.ex, .tag, .pair, .split/.word, .score. SVG-холста и эмодзи нет.
   Последний кадр — тренажёр на массиве game. Кадров четырнадцать: урок
   подробный, каждый знак разбирается отдельно.

   ПРАВИЛА (школьный курс, 5 класс):
     однородные члены отвечают на ОДИН вопрос и относятся к ОДНОМУ слову:
       «ромашки, васильки, колокольчики» — что? при «росли»;
     однородными бывают любые члены: подлежащие, сказуемые, второстепенные;
     БЕЗ СОЮЗОВ — запятая: «На лугу росли ромашки, васильки, колокольчики»;
     одиночный И (ДА в значении И) — запятой НЕТ: «ромашки и васильки»,
       «Мал да удал»;
     повторяющиеся И…И, НИ…НИ, ИЛИ…ИЛИ — запятая ЕСТЬ:
       «и ромашки, и васильки», «ни ветра, ни солнца»;
     союзы А, НО — запятая ЕСТЬ: «Он не гулял, а читал», «хмурый, но тёплый»;
     ДВОЕТОЧИЕ — когда обобщающее слово стоит ПЕРЕД однородными:
       «В корзине лежали разные грибы: подберёзовики, лисички, сыроежки»;
     ТИРЕ — когда обобщающее слово стоит ПОСЛЕ однородных:
       «Подберёзовики, лисички, сыроежки — все грибы были крепкие»;
     обобщающие слова: всё, все, всюду, везде, всегда, никогда, никто, ничего.
   ЛОВУШКИ: двоеточие без обобщающего слова («лежали: грибы, ягоды» ✗);
   «однородные» с разными главными словами («красивые дети и умные взрослые» ✗). */
(function(){
  'use strict';

  const ID = 1042;
  const R = window.RUKIT;
  if(!R){ return; }
  /* палитра каркаса: у русского набора она задаётся локально в каждом уроке */
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const chip  = (t,c)=>`<span class="tag" style="font-size:16px;${c?('border-color:'+c+';color:'+c):''}">${esc(t)}</span>`;
  const card  = (nm,ex)=>`<div class="card"><div class="nm">${esc(nm)}</div><div class="ex">${esc(ex)}</div></div>`;
  const cards = (items)=>`<div class="row">${items.map(([nm,ex])=>card(nm,ex)).join('')}</div>`;
  const pair  = (a,b)=>`<div class="pair">${a}${b}</div>`;
  const row   = (...xs)=>`<div class="row">${xs.join('')}</div>`;
  const lead  = (t)=>`<p class="lead">${esc(t)}</p>`;
  const cap   = (t)=>`<p class="cap">${esc(t)}</p>`;
  const h2    = (t)=>`<h2>${esc(t)}</h2>`;
  const kick  = (t)=>`<div class="kicker">${esc(t)}</div>`;
  const word  = (б,под)=>`<div class="split"><span class="word wide"><b>${esc(б)}</b><i>${esc(под)}</i></span></div>`;
  const RU    = (t,c)=>`<span style="color:${c||'#ffd76a'};font-weight:600">${esc(t)}</span>`;

  /* ── тренажёр: десять заданий по кругу ── */
  const GAME=[
    ['На лугу росли ромашки, васильки, колокольчики. Какой знак между однородными?',
     'запятые','Без союзов между однородными ставится запятая.','двоеточие'],
    ['На лугу росли ромашки и васильки. Нужна ли запятая перед «и»?',
     'запятой нет','Одиночный союз И — запятая не ставится.','запятая нужна'],
    ['В корзине лежали разные грибы: подберёзовики, лисички. Почему двоеточие?',
     'есть обобщающее слово','Обобщающее слово «разные грибы» стоит перед однородными.','просто перечисление'],
    ['Подберёзовики, лисички, сыроежки — все грибы были крепкие. Какой знак перед обобщающим словом?',
     'тире','Обобщающее слово «все грибы» стоит после однородных — ставится тире.','двоеточие'],
    ['Он не гулял, а читал. Нужна ли запятая?',
     'запятая нужна','Союз А всегда отделяется запятой.','запятой нет'],
    ['Он и читал, и писал. Нужна ли запятая?',
     'запятая нужна','Повторяющийся союз И…И — запятая ставится.','запятой нет'],
    ['День был хмурый, но тёплый. Нужна ли запятая?',
     'запятая нужна','Союз НО отделяется запятой.','запятой нет'],
    ['Мал да удал. Нужна ли запятая перед «да»?',
     'запятой нет','Одиночное ДА в значении И — запятой нет.','запятая нужна'],
    ['В корзине лежали: грибы, ягоды. Всё ли верно?',
     'ошибка: нет обобщающего слова','Двоеточие ставится только при обобщающем слове.','всё верно'],
    ['Красивые, умные дети. Это однородные члены?',
     'да, оба слова о детях','Оба слова отвечают на один вопрос и относятся к слову «дети».','нет, разные главные слова']
  ];
  window.RUGAME = window.RUGAME || {};
  window.RUGAME[ID] = GAME;

  function тренер(st){
    const it = GAME[(st.gIdx||0)%GAME.length];
    const готово = st.gRes!=null, верно = st.gRes===true;
    const варианты = ((st.gIdx||0)%2) ? [it[3], it[1]] : [it[1], it[3]];
    return `${kick('14 · Тренажёр')}
      ${word(it[0],'выбери ответ')}
      ${готово ? (верно ? R.NOTE('ok','верно',it[2]) : R.NOTE('no','исправить',it[2]))
               : `<p class="verdict">Сначала найди однородные члены и посмотри, есть ли обобщающее слово.</p>`}
      <div class="row">${варианты.map(о=>
        `<button type="button" class="chip" style="flex:1 1 40%;font-size:18px"
           onclick="ru1042Game('${esc(о)}')">${esc(о)}</button>`).join('')}</div>
      <p class="score">верно: ${st.gOk||0} · ошибок: ${st.gBad||0} · всего: ${GAME.length}</p>
      ${cap(готово?'Нажми любую кнопку — следующее задание.':'Сначала вопрос, потом ответ.')}`;
  }

  window.RULESSON({id:ID, game:GAME, frames:[
    ()=>`${kick('01 · Что такое однородные члены')}
      ${h2('Отвечают на один вопрос — и про одно и то же')}
      ${lead('Прочитаем: «На лугу росли ромашки, васильки, колокольчики». Кто росли? — ромашки, васильки, колокольчики. Все три слова отвечают на один вопрос и относятся к одному слову «росли».')}
      ${row(chip('кто? что?',GOLD),chip('ромашки',GREEN),chip('васильки',GREEN),chip('колокольчики',GREEN))}
      ${pair(card('признак однородности','один вопрос и одно главное слово'),card('как проверить','задай вопрос от главного слова к каждому'))}`,

    ()=>`${kick('02 · Какими членами они бывают')}
      ${h2('Однородными бывают любые члены предложения')}
      ${cards([['подлежащие','ромашки, васильки и колокольчики росли'],['сказуемые','он читал, писал и считал'],['второстепенные','купили красные, синие и белые шары']])}
      ${lead('Главное не то, какой это член, а то, что слова равны между собой: ни одно не подчиняется другому.')}
      ${cap('Если слова подчиняются друг другу — это не однородные члены: «дом у дороги».')}`,

    ()=>`${kick('03 · Без союзов — запятая')}
      ${h2('Перечисляем — ставим запятые')}
      ${lead('Между однородными членами без союзов всегда ставятся запятые.')}
      ${card('пример','На лугу росли ромашки, васильки, колокольчики.')}
      ${pair(card('как читаем','с интонацией перечисления'),card('что ставим','запятую между каждой парой'))}
      ${cap('Запятая не ставится перед первым словом и после последнего.')}`,

    ()=>`${kick('04 · Одиночный союз И или ДА')}
      ${h2('Один союз — запятой нет')}
      ${lead('Если союз И (или ДА в значении И) стоит один раз, запятая не нужна.')}
      ${pair(card('без запятой','ромашки и васильки · Мал да удал'),card('подсказка','союз один — запятой нет'))}
      ${cap('Сравни: «ромашки и васильки» — без запятой, а «ромашки, васильки» — с запятой.')}`,

    ()=>`${kick('05 · Повторяющиеся союзы')}
      ${h2('И…И, НИ…НИ, ИЛИ…ИЛИ — запятая есть')}
      ${lead('Если союз повторяется, между однородными ставится запятая — как при перечислении.')}
      ${row(chip('и ромашки, и васильки',GOLD),chip('ни ветра, ни солнца',GOLD),chip('или дождь, или снег',GOLD))}
      ${cap('Правило простое: союз один — запятой нет; союз повторяется — запятая есть.')}`,

    ()=>`${kick('06 · Союзы А и НО')}
      ${h2('Спорят — ставим запятую')}
      ${lead('Союзы А и НО всегда отделяются запятой: они противопоставляют однородные члены.')}
      ${pair(card('с запятой','Он не гулял, а читал · День был хмурый, но тёплый'),card('сравни','Он гулял и читал — запятой нет'))}
      ${cap('Союз И соединяет, союзы А и НО противопоставляют.')}`,

    ()=>`${kick('07 · Обобщающее слово')}
      ${h2('Главный герой двоеточия')}
      ${lead('Обобщающее слово называет всё перечисленное вместе: «разные грибы», «все деревья», «везде». Именно оно решает, будет ли двоеточие.')}
      ${row(chip('всё'),chip('все'),chip('всюду'),chip('везде'),chip('всегда'),chip('никто'),chip('ничего'))}
      ${cap('Без обобщающего слова двоеточие не ставится — и это частая ошибка.')}`,

    ()=>`${kick('08 · Двоеточие')}
      ${h2('Обобщающее слово перед однородными')}
      ${lead('Если обобщающее слово стоит ПЕРЕД однородными членами, между ними ставится двоеточие.')}
      ${card('пример','В корзине лежали разные грибы: подберёзовики, лисички, сыроежки.')}
      ${pair(card('что перед двоеточием','обобщающее слово «разные грибы»'),card('что после','сами однородные члены'))}
      ${cap('Схема: обобщающее слово : однородные члены.')}`,

    ()=>`${kick('09 · Тире')}
      ${h2('Обобщающее слово после однородных')}
      ${lead('Если обобщающее слово стоит ПОСЛЕ однородных членов, ставится тире.')}
      ${card('пример','Подберёзовики, лисички, сыроежки — все грибы были крепкие.')}
      ${pair(card('до тире','однородные члены'),card('после тире','обобщающее слово «все грибы»'))}
      ${cap('Схема: однородные члены — обобщающее слово.')}`,

    ()=>`${kick('10 · Три знака рядом')}
      ${h2('Запятая, двоеточие, тире')}
      ${cards([['запятая','между однородными: ромашки, васильки, колокольчики'],['двоеточие','после обобщающего: разные грибы: подберёзовики, лисички'],['тире','перед обобщающим: подберёзовики, лисички — все грибы']])}
      ${cap('Знак выбирается по месту обобщающего слова: перед перечислением — двоеточие, после — тире, а между самими членами — запятые.')}`,

    ()=>`${kick('11 · Как найти однородные')}
      ${h2('Порядок действий')}
      ${cards([['1. найди главное слово','от него будем задавать вопрос'],['2. задай вопрос','к каждому подозреваемому слову'],['3. сравни ответы','один вопрос — однородные члены'],['4. посмотри на союзы','один союз или повторяется'],['5. проверь обобщающее','есть ли слово «все», «всё», «везде»']])}
      ${cap('Пять шагов — и знак находится сам.')}`,

    ()=>`${kick('12 · Ловушки')}
      ${h2('Две ошибки, которые встречаются чаще всего')}
      ${lead('Первая: двоеточие без обобщающего слова. Вторая: «однородные» слова с разными главными словами.')}
      ${pair(card('неверно','В корзине лежали: грибы, ягоды. · красивые дети и умные взрослые'),card('верно','В корзине лежали грибы и ягоды. · красивые и умные дети'))}
      ${cap('Двоеточию нужно обобщающее слово, а однородным — одно главное слово.')}`,

    ()=>`${kick('13 · В карман')}
      ${h2('Три фразы, которые стоит унести')}
      ${cards([['без союзов','ромашки, васильки, колокольчики'],['один союз И','ромашки и васильки — без запятой'],['с обобщающим словом','разные грибы: подберёзовики, лисички']])}
      ${cap('А если обобщающее слово в конце — ставим тире: «подберёзовики, лисички — все грибы».')}`,

    (st)=>тренер(st)
  ]});

  /* ── карточка каталога ── */
  const L1042={
    id:ID, title:'Однородные члены и двоеточие', ico:'📝',
    src:'Русский язык · 5 класс · Синтаксис и пунктуация', subj:'rus', group:null,
    explain:[
      'Однородные члены отвечают на один вопрос и относятся к одному и тому же слову: «На лугу росли ромашки, васильки, колокольчики» — все три слова отвечают на вопрос что? при слове «росли».',
      'Однородными бывают любые члены предложения: подлежащие (ромашки, васильки и колокольчики), сказуемые (читал, писал и считал), второстепенные (красные, синие и белые шары). Главное — слова равны между собой.',
      'Между однородными членами без союзов ставится запятая: «ромашки, васильки, колокольчики». Читаем их с интонацией перечисления.',
      'Если союз И (или ДА в значении И) стоит один раз, запятая не нужна: «ромашки и васильки», «Мал да удал». Один союз — запятой нет.',
      'Если союз повторяется — И…И, НИ…НИ, ИЛИ…ИЛИ, — запятая ставится: «и ромашки, и васильки», «ни ветра, ни солнца».',
      'Союзы А и НО всегда отделяются запятой, потому что противопоставляют: «Он не гулял, а читал», «День был хмурый, но тёплый».',
      'Обобщающее слово называет всё перечисленное вместе: разные грибы, все деревья, всё, все, всюду, везде, всегда, никто, ничего. Именно оно решает, будет ли двоеточие.',
      'Двоеточие ставится, когда обобщающее слово стоит перед однородными членами: «В корзине лежали разные грибы: подберёзовики, лисички, сыроежки». Схема: обобщающее слово : однородные члены.',
      'Тире ставится, когда обобщающее слово стоит после однородных членов: «Подберёзовики, лисички, сыроежки — все грибы были крепкие». Схема: однородные члены — обобщающее слово.',
      'Три знака рядом: запятая между самими однородными, двоеточие после обобщающего слова перед перечислением, тире перед обобщающим словом в конце.',
      'Порядок действий: найти главное слово, задать от него вопрос к каждому слову, проверить, что вопрос один, посмотреть на союзы и найти обобщающее слово.',
      'Две частые ошибки: двоеточие без обобщающего слова («В корзине лежали: грибы, ягоды» — неверно) и «однородные» слова с разными главными словами («красивые дети и умные взрослые»).',
      'Запомним три опоры: без союзов — запятая, один союз И — без запятой, с обобщающим словом — двоеточие или тире.',
      'Тренажёр: десять заданий по кругу. В каждом предложении нужно выбрать знак или сказать, нужна ли запятая.'
    ],
    tasks:[
      { q:'«На лугу росли ромашки, васильки, колокольчики». Какой знак между однородными?', kind:'choice',
        choices:['запятые','двоеточие'], ans:0,
        hints:['Есть ли обобщающее слово?','Союзов нет.'],
        sol:'Запятые: однородные без союзов.' },
      { q:'«На лугу росли ромашки и васильки». Нужна ли запятая?', kind:'choice',
        choices:['запятой нет','запятая нужна'], ans:0,
        hints:['Сколько раз стоит союз И?','Союз один.'],
        sol:'Запятой нет: одиночный союз И.' },
      { q:'«В корзине лежали разные грибы: подберёзовики, лисички». Почему двоеточие?', kind:'choice',
        choices:['есть обобщающее слово','просто перечисление'], ans:0,
        hints:['Что стоит перед перечислением?','«Разные грибы» — обобщающее слово.'],
        sol:'Двоеточие из-за обобщающего слова.' },
      { q:'«Подберёзовики, лисички, сыроежки — все грибы были крепкие». Какой знак перед обобщающим словом?', kind:'choice',
        choices:['тире','двоеточие'], ans:0,
        hints:['Где стоит обобщающее слово?','Оно в конце.'],
        sol:'Тире: обобщающее слово после однородных.' }
    ]
  };
  try{
    const arr=window.ARH_LESSONS;
    if(arr){
      const м=arr.findIndex(L=>L && L.id===ID);
      if(м>=0) arr[м]=L1042; else arr.push(L1042);
    }
  }catch(e){}

  function игра(ответ){
    const lk=(typeof lidKey==='function')?lidKey(ID):String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    const st=CHS[lk];
    if(st.gRes!=null){
      st.gIdx=((st.gIdx||0)+1)%GAME.length; st.gRes=null;
    }else{
      const it=GAME[(st.gIdx||0)%GAME.length];
      st.gRes=(ответ===it[1]);
      if(st.gRes) st.gOk=(st.gOk||0)+1; else st.gBad=(st.gBad||0)+1;
    }
    if(typeof chRender==='function') chRender(0);
  }
  window.ru1042Game=function(о){ try{ игра(о); }catch(e){} };
})();
