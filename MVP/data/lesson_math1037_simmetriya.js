/* ============ МАТЕМАТИКА · УРОК 1037 · «СИММЕТРИЯ ФИГУР» ============
   6 класс. Осевая и центральная симметрия.

   ПРАВИЛО ЭТОГО УРОКА (просьба владельца: «не переходя на шаблоны»):
   у каждого кадра СВОЙ механизм движения, а не один каркас на все:
     кадр 1  СКЛАДЫВАНИЕ: левая половина бабочки наезжает на ось и ложится
             на правую (масштаб по X к оси + сгущение тени на сгибе);
     кадр 2  ИЗМЕРЕНИЕ: от точки к оси растёт перпендикуляр, появляется
             знак прямого угла и парные засечки равных расстояний;
     кадр 3  ПОСТРОЕНИЕ: для каждой вершины — перпендикуляр, дуга циркуля
             и зеркальная вершина (линии рисуются штрихом);
     кадр 4  ПРОВЕРКА: две фигуры складываются по оси, у одной половинки
             совпадают, у другой нет — вердикт ✓ / ✗;
     кадр 5  ЛУЧ И СЕРЕДИНА: из центра через точку идёт луч, центр помечен
             как середина отрезка;
     кадр 6  ПОВОРОТ НА 180°: фигура поворачивается вокруг центра и попадает
             на своё центрально-симметричное место (это НЕ складывание);
     кадр 7  СЧЁТ ОСЕЙ: оси квадрата появляются по очереди, каждая с проверкой;
     кадр 8  ЗЕРКАЛЬНАЯ ЛИНЕЙКА: по буквам едет зеркало и проверяет совпадение;
     кадр 9  ДВЕ ПРОВЕРКИ: вертикальная не проходит, горизонтальная проходит;
     кадр 10 ДВА ДЕЙСТВИЯ РЯДОМ: одна и та же фигура складывается и
             поворачивается — видно разницу осевой и центральной симметрии.

   ВСЕ ГЕОМЕТРИЧЕСКИЕ ЧИСЛА ПРОВЕРЕНЫ РАСЧЁТОМ:
     отражение (40,60) при оси x=100 → (160,60); при оси y=80 → (40,100)
     центральная симметрия (40,60) от центра (100,80) → (160,100)
     |A−O| = |A′−O| = 63,25 — центр есть середина отрезка
     оси: квадрат — 4, прямоугольник — 2, ромб — 2, равносторонний
     треугольник — 3, правильный пятиугольник — 5, круг — бесконечно много
     буквы: вертикальная ось — А, М, Т, П, Ш; горизонтальная — В, Е, З, К, С, Э;
     обе — Ж, Н, О, Х, Ф; ни одной — R, S, G, Я

   ГРАФИКА: бумага с зерном и сгибом, стеклянное зеркало, дуги циркуля,
   штриховые построения, засечки равных отрезков, металлическая линейка;
   у каждого предмета ДВЕ тени, зерно и виньетка по фону.

   АНИМАЦИЯ: SMIL calcMode="spline", keySplines="0.23 1 0.32 1".
   СТАНДАРТ deploy/АНИМАЦИЯ_УРОКОВ.md: состояние всегда видимо (не ниже 0.3),
   текст не масштабируется, keyTimes строго в [0;1], reduced-motion выключает
   SMIL полностью, ничего не выходит за край кадра. */
(function(){
  'use strict';

  const ID = 1037;
  const GOLD='#ffd76a', GREEN='#8fd1a8', BLUE='#7fd1ff', RED='#e86a5a';
  const РОЗ='#f2a0b8', ЛИЛ='#c9a6f2';
  const ИНК='#fdf8ec', МУТ='#ded0b0', ЛИНИЯ='#4d8f70', ОБВОД='#241d14';

  const CSS=`
  #lvis .s6.l1037{gap:14px}
  #lvis .s6.l1037 .pic{width:100%;max-width:352px;margin:0 auto}
  #lvis .s6.l1037 .pic svg{display:block;width:100%;height:auto}
  #lvis .s6.l1037 .карт{width:100%;border-radius:16px;padding:14px;display:flex;flex-direction:column;gap:8px;
    background:linear-gradient(180deg,#22362c,#17261e);border:1.5px solid var(--line)}
  #lvis .s6.l1037 .карт.вопрос{border-color:${GOLD}}
  #lvis .s6.l1037 .карт.верно{border-color:${GREEN}}
  #lvis .s6.l1037 .карт.ошибка{border-color:${RED}}
  #lvis .s6.l1037 .карт .метка{font-size:14px;letter-spacing:.08em;text-transform:uppercase;color:var(--mut)}
  #lvis .s6.l1037 .карт .текст{font-size:20px;line-height:1.45;color:var(--ink)}
  #lvis .s6.l1037 .правило{width:100%;padding:14px;border-radius:14px;border:2px solid ${GOLD};
    background:linear-gradient(180deg,rgba(255,215,106,.14),rgba(255,215,106,.05));font-size:20px;line-height:1.45}
  #lvis .s6.l1037 .правило b{color:${GOLD}}
  #lvis .s6.l1037 .ask{display:flex;gap:10px;flex-wrap:wrap;width:100%}
  #lvis .s6.l1037 .ask button{flex:1 1 100%;min-height:56px;height:auto;padding:13px 16px;overflow-wrap:anywhere;word-break:break-word;font-size:16px;font-weight:600;line-height:1.3;text-align:left;
    touch-action:manipulation;-webkit-tap-highlight-color:transparent;
    transition:transform 150ms cubic-bezier(.23,1,.32,1),border-color 150ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1037 .ask button:active{transform:translateY(2px)}
  #lvis .s6.l1037 .ask button.hit{border-color:var(--ok)}
  #lvis .s6.l1037 .ask button.miss{border-color:var(--no)}
  #lvis .s6.l1037 .уровни{display:flex;gap:8px;align-items:center;width:100%}
  #lvis .s6.l1037 .уровни .точка{flex:1 1 0;height:10px;border-radius:6px;background:rgba(255,255,255,.09);
    transition:background 200ms cubic-bezier(.23,1,.32,1)}
  #lvis .s6.l1037 .уровни .точка.пройдено{background:${GREEN}}
  #lvis .s6.l1037 .уровни .точка.сейчас{background:${GOLD};animation:l1037dot 1.6s cubic-bezier(.23,1,.32,1) infinite}
  @keyframes l1037dot{0%,100%{opacity:1}50%{opacity:.45}}
  #lvis .s6.l1037 .score{font-size:16px;color:var(--mut);text-align:center;font-variant-numeric:tabular-nums}
  #lvis .s6.l1037{-webkit-text-size-adjust:100%}
  #lvis .s6.l1037 [data-anim]{animation:l1037rise 280ms cubic-bezier(.23,1,.32,1) both;animation-delay:calc(min(var(--i,0),5)*45ms)}
  @keyframes l1037rise{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}
  @media (max-width:370px){ #lvis .s6.l1037 .ask button{flex:1 1 100%} }
  @media (prefers-reduced-motion: reduce){
    #lvis .s6.l1037 [data-anim]{animation:none!important}
    #lvis .s6.l1037 .уровни .точка.сейчас{animation:none!important}
    #lvis .s6.l1037 .ask button{transition:none}
  }`;

  function css(){
    try{
      if(window.RUKIT && RUKIT.frameCss) RUKIT.frameCss();
      let s=document.getElementById('l1037-style');
      if(!s){ s=document.createElement('style'); s.id='l1037-style'; document.head.appendChild(s); }
      if(s.textContent!==CSS) s.textContent=CSS;
    }catch(e){}
  }

  const S = () => {
    const lk = (typeof lidKey==='function') ? lidKey(ID) : String(ID);
    if(typeof CHS==='undefined') window.CHS={};
    if(!CHS[lk]) CHS[lk]={};
    return CHS[lk];
  };
  const A = (i,cls,html) => `<div data-anim style="--i:${i}" class="${cls||''}">${html}</div>`;
  const BTN = (i,cls,html,on,off) =>
    `<button type="button" data-anim style="--i:${i}" class="${cls||''}" ${off?'disabled':''} onclick="${on}">${html}</button>`;
  const ВОПРОС = (текст) => A(2,'карт вопрос','<span class="метка">Вопрос</span><div class="текст">'+текст+'</div>');
  const РАЗБОР = (верно,текст) =>
    A(9,'карт '+(верно?'верно':'ошибка'),
      '<span class="метка">'+(верно?'Верно':'Разбор ошибки')+'</span><div class="текст">'+(верно?'✅ ':'❌ ')+текст+'</div>');
  const ЖДЁТ = () => A(9,'карт','<span class="метка">Ответ</span><div class="текст">Выбери один из вариантов выше — разбор появится здесь.</div>');
  const ПРАВИЛО = (текст) => A(40,'правило',текст);
  const ТОЧКИ = (всего,сейчас,пройдено) => A(1,'уровни',
    Array.from({length:всего},(_,к)=>
      `<span class="точка ${к<пройдено?'пройдено':(к===сейчас?'сейчас':'')}"></span>`).join(''));

  /* ================= АНИМАЦИЯ: КРИВЫЕ ПРОЕКТА ================= */
  const ДВИЖ = !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  const КРИВАЯ = '0.23 1 0.32 1';
  const сплайны = (n) => Array.from({length:n},()=>КРИВАЯ).join(';');
  const ан = (имя,значения,длит,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite" ${доп||''}/>` : '';
  const анК = (имя,значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animate attributeName="${имя}" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анТК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="translate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анВК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="rotate" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анМК = (значения,длит,keyTimes,доп) =>
    ДВИЖ ? `<animateTransform attributeName="transform" type="scale" values="${значения}" dur="${длит}" repeatCount="indefinite"
              calcMode="spline" keyTimes="${keyTimes}" keySplines="${сплайны(keyTimes.split(';').length-1)}" ${доп||''}/>` : '';
  const анД = (путь,длит,доп) =>
    ДВИЖ ? `<animateMotion dur="${длит}" repeatCount="indefinite" path="${путь}" ${доп||''}/>` : '';
  const кт = (v) => Math.min(0.95, Math.max(0.03, v)).toFixed(2);
  const проявить = (длит,доля,конец) => анК('opacity','0.3;0.3;1;1',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');
  /* линия рисуется штрихом — построения «карандашом» */
  const рисуется = (длина,длит,доля,конец) => анК('stroke-dashoffset',длина+';'+длина+';0;0',длит,
    '0;'+кт(доля)+';'+кт(конец)+';1');

  /* ================= РИСУНОК ================= */
  const esc = s => String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const т = (x,y,текст,кегль,цвет,жирный,якорь) =>
    `<text x="${x}" y="${y}" text-anchor="${якорь||'middle'}" font-size="${кегль||14}"
       ${жирный?'font-weight="bold"':''} fill="${цвет||ИНК}"
       font-family="Georgia,'Times New Roman',serif">${esc(текст)}</text>`;

  const ОПРЕДЕЛЕНИЯ = `
    <defs>
      <linearGradient id="c1037-стена" x1="0" y1="0" x2="0" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#2a3f33"/><stop offset="0.45" stop-color="#1e3128"/>
        <stop offset="1" stop-color="#111c15"/>
      </linearGradient>
      <linearGradient id="c1037-крыло" x1="0" y1="0" x2="0.4" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#ffd98a"/><stop offset="0.4" stop-color="#f2a04a"/>
        <stop offset="0.75" stop-color="#c9662a"/><stop offset="1" stop-color="#8c3f18"/>
      </linearGradient>
      <linearGradient id="c1037-крыло2" x1="0" y1="0" x2="0.4" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#d8c0ff"/><stop offset="0.4" stop-color="#a887e8"/>
        <stop offset="0.75" stop-color="#6f52b0"/><stop offset="1" stop-color="#3f2c6b"/>
      </linearGradient>
      <linearGradient id="c1037-фигура" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#a8dcff"/><stop offset="0.45" stop-color="#5fa8d8"/>
        <stop offset="1" stop-color="#2b6d99"/>
      </linearGradient>
      <linearGradient id="c1037-фигура2" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#c7efd8"/><stop offset="0.45" stop-color="#8fd1a8"/>
        <stop offset="1" stop-color="#3f7a5c"/>
      </linearGradient>
      <linearGradient id="c1037-бумага" x1="0" y1="0" x2="0.2" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#fdfaf1"/><stop offset="0.5" stop-color="#f2ead6"/>
        <stop offset="1" stop-color="#d9cfb4"/>
      </linearGradient>
      <linearGradient id="c1037-стекло" x1="0" y1="0" x2="0.3" y2="1" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#eaf6ff" stop-opacity=".6"/>
        <stop offset="0.5" stop-color="#bcdcf2" stop-opacity=".25"/>
        <stop offset="1" stop-color="#6f9dbb" stop-opacity=".22"/>
      </linearGradient>
      <linearGradient id="c1037-металл" x1="0" y1="0" x2="1" y2="0" color-interpolation="linearRGB">
        <stop offset="0" stop-color="#5d6a76"/><stop offset="0.18" stop-color="#e8eef4"/>
        <stop offset="0.5" stop-color="#9fb0bd"/><stop offset="0.8" stop-color="#57636f"/>
        <stop offset="1" stop-color="#434d57"/>
      </linearGradient>
      <filter id="c1037-тень" x="-70%" y="-70%" width="240%" height="240%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="3.2" result="р"/>
        <feOffset in="р" dx="3" dy="5" result="с"/>
        <feFlood flood-color="#0a1a24" flood-opacity="0.4" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c1037-контакт" x="-70%" y="-150%" width="240%" height="400%" color-interpolation-filters="linearRGB">
        <feGaussianBlur in="SourceAlpha" stdDeviation="1.3" result="р"/>
        <feOffset in="р" dx="0" dy="1" result="с"/>
        <feFlood flood-color="#1d1508" flood-opacity="0.4" result="ц"/>
        <feComposite in="ц" in2="с" operator="in"/>
      </filter>
      <filter id="c1037-мягко" x="-40%" y="-40%" width="180%" height="180%" color-interpolation-filters="linearRGB">
        <feGaussianBlur stdDeviation="1.2"/>
      </filter>
      <filter id="c1037-шум" x="0" y="0" width="100%" height="100%" color-interpolation-filters="linearRGB">
        <feTurbulence type="fractalNoise" baseFrequency="0.85" numOctaves="4" stitchTiles="stitch" result="ш"/>
        <feColorMatrix in="ш" type="saturate" values="0"/>
      </filter>
    </defs>`;

  const свг = (тело, высота) =>
    `<svg viewBox="0 0 336 ${высота||252}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
       ${ОПРЕДЕЛЕНИЯ}${тело}</svg>`;

  const фон = (высота) =>
    `<rect x="0" y="0" width="336" height="${высота}" fill="url(#c1037-стена)"/>
     <rect x="0" y="0" width="336" height="${высота}" filter="url(#c1037-шум)" opacity="0.05"/>
     <rect x="0" y="0" width="336" height="${высота}" fill="none" stroke="${ЛИНИЯ}" stroke-width="1.6"/>`;

  const подпись = (x,y,текст,цвет,кегль) => {
    const к=кегль||13, ш=String(текст).length*к*0.62+14, в=к+10;
    return `<g>
      <rect x="${(x-ш/2).toFixed(1)}" y="${(y-в+3).toFixed(1)}" width="${ш.toFixed(1)}" height="${в}"
        rx="${(в/2).toFixed(1)}" fill="rgba(10,22,17,.84)" stroke="${цвет||GOLD}" stroke-width="1.2"/>
      ${т(x,y-2,текст,к,цвет||GOLD,true)}
    </g>`;
  };

  /* ОСЬ СИММЕТРИИ: штриховая линия со свечением и подписью */
  const ось = (x1,y1,x2,y2,нач,подписьТекст) => `<g>${проявить('13s',нач,нач+0.06)}
    <line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${GOLD}" stroke-width="2.4" stroke-dasharray="8 5"/>
    <line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="rgba(255,215,106,.3)" stroke-width="6" filter="url(#c1037-мягко)"/>
    
  </g>`;

  /* БУМАЖНАЯ ФИГУРА: заливка градиентом, зерно, тень, обводка */
  const фигура = (d,материал,нач) => `<g>
    <path d="${d}" transform="translate(2,4)" fill="#08171f" opacity=".32" filter="url(#c1037-тень)"/>
    <path d="${d}" fill="url(#c1037-${материал})" stroke="${ОБВОД}" stroke-width="2">
      ${нач!=null?проявить('13s',нач,нач+0.06):''}</path>
    <path d="${d}" fill="none" stroke="${ОБВОД}" stroke-width="2"/>
  </g>`;

  /* ЗЕРКАЛО: стеклянная полоса */
  const зеркало = (x,y,ш,в,нач) => `<g>${проявить('13s',нач,нач+0.05)}
    <rect x="${x+2}" y="${y+3}" width="${ш}" height="${в}" rx="4" fill="#08171f" opacity=".35" filter="url(#c1037-тень)"/>
    <rect x="${x}" y="${y}" width="${ш}" height="${в}" rx="4" fill="url(#c1037-стекло)" stroke="rgba(200,235,255,.6)" stroke-width="1.2"/>
    <line x1="${x+3}" y1="${y+4}" x2="${x+ш-3}" y2="${y+4}" stroke="rgba(255,255,255,.5)" stroke-width="1.4"/>
  </g>`;

  /* ЗАСЕЧКА равных отрезков */
  const засечка = (x1,y1,x2,y2,нач,кол) => {
    const mx=(x1+x2)/2, my=(y1+y2)/2, dx=x2-x1, dy=y2-y1, len=Math.hypot(dx,dy)||1;
    const nx=-dy/len*5, ny=dx/len*5;
    return Array.from({length:кол||1},(_,i)=>`<g>${проявить('13s',нач,нач+0.05)}
      <line x1="${(mx+nx*0.4+(i-(кол-1)/2)*4*nx/5).toFixed(1)}" y1="${(my+ny*0.4+(i-(кол-1)/2)*4*ny/5).toFixed(1)}"
        x2="${(mx-nx*0.4+(i-(кол-1)/2)*4*nx/5).toFixed(1)}" y2="${(my-ny*0.4+(i-(кол-1)/2)*4*ny/5).toFixed(1)}"
        stroke="${GREEN}" stroke-width="2.2"/></g>`).join('');
  };

  /* ЗНАК ПРЯМОГО УГЛА */
  const прямойУгол = (v,a,b,размер,нач,цвет) => {
    const u1=[(a[0]-v[0])/Math.hypot(a[0]-v[0],a[1]-v[1]),(a[1]-v[1])/Math.hypot(a[0]-v[0],a[1]-v[1])];
    const u2=[(b[0]-v[0])/Math.hypot(b[0]-v[0],b[1]-v[1]),(b[1]-v[1])/Math.hypot(b[0]-v[0],b[1]-v[1])];
    const к=размер||10;
    const p1=[v[0]+u1[0]*к,v[1]+u1[1]*к], p3=[v[0]+u2[0]*к,v[1]+u2[1]*к];
    const p2=[v[0]+(u1[0]+u2[0])*к,v[1]+(u1[1]+u2[1])*к];
    return `<g>${проявить('13s',нач,нач+0.05)}
      <path d="M${p1[0].toFixed(1)} ${p1[1].toFixed(1)} L${p2[0].toFixed(1)} ${p2[1].toFixed(1)} L${p3[0].toFixed(1)} ${p3[1].toFixed(1)}"
        fill="none" stroke="${цвет||GOLD}" stroke-width="1.8"/></g>`;
  };

  /* ================= КАДРЫ ================= */

  /* 1. БАБОЧКА: складывание крыла по оси */
  function F1(s){
    const в = s.бабочка;
    const осьX=168;
    /* четыре крыла и тело: верхние крылья шире, нижние уже */
    const верхЛево='M168 96 C146 62 108 54 100 78 C94 98 130 104 168 104 Z';
    const низЛево='M168 104 C138 108 112 116 116 134 C120 150 148 138 168 122 Z';
    const верхПраво='M168 96 C190 62 228 54 236 78 C242 98 206 104 168 104 Z';
    const низПраво='M168 104 C198 108 224 116 220 134 C216 150 188 138 168 122 Z';
    return ВОПРОС('Крылья бабочки совпадут, если сложить лист по средней линии?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Складываем по оси',14,GOLD,true)}
        <rect x="40" y="40" width="256" height="120" rx="6" fill="url(#c1037-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
          ${проявить('13s',0.04,0.12)}</rect>
        <rect x="40" y="40" width="256" height="120" rx="6" fill="none" filter="url(#c1037-шум)" opacity=".08"/>
        ${фигура(верхПраво,'крыло2',0.16)}
        ${фигура(низПраво,'крыло2',0.2)}
        <ellipse cx="168" cy="108" rx="5" ry="26" fill="url(#c1037-металл)" stroke="${ОБВОД}" stroke-width="1.4"/>
        <g>${анМК('1 168 100;1 168 100;0.04 168 100;0.04 168 100;1 168 100','14s','0;0.3;0.56;0.82;1')}
          ${фигура(верхЛево,'крыло',0.1)}
          ${фигура(низЛево,'крыло',0.14)}
        </g>
        ${ось(осьX,38,осьX,162,0.06)}
        ${/* тень сгиба */''}
        <rect x="${осьX-8}" y="40" width="16" height="120" fill="rgba(20,35,28,.25)" filter="url(#c1037-мягко)">
          ${анК('opacity','0;0;0.5;0.5;0','14s','0;0.3;0.56;0.82;1')}</rect>
        <g>${проявить('13s',0.6,0.7)}
          ${подпись(168,196,'половинки совпали — фигура симметрична',GREEN,12)}</g>
        <g>${проявить('13s',0.8,0.9)}
          ${т(168,228,'прямая сгиба и есть ось симметрии',14,ИНК,true)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='да'?'hit':(в?'miss':''), 'Да: половинки совпадут при складывании', "r1037Бабочка('да')")}
        ${BTN(4, в==='нет'?'miss':'', 'Нет: крылья разного цвета', "r1037Бабочка('нет')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='да',
        в==='да' ? 'Верно: линия сгиба делит фигуру на две одинаковые половины — это и есть ось симметрии.'
                 : 'Нет: цвет не важен, важна форма. Половинки совпадают, значит ось есть.') : ЖДЁТ()) +
      ПРАВИЛО('Фигура симметрична, если при складывании <b>половинки совпадают</b>.');
  }

  /* 2. ИЗМЕРЕНИЕ: перпендикуляр к оси и равные расстояния */
  function F2(s){
    const в = s.ось_точки;
    const осьX=182, y=120, xA=92, xB=272;
    return ВОПРОС('Точка и её отражение. Что делает ось с отрезком между ними?') +
      `<div class="pic">${свг(`
        ${фон(244)}
        ${т(168,24,'Ось — серединный перпендикуляр',14,GOLD,true)}
        ${ось(осьX,44,осьX,196,0.04)}
        <line x1="${xA}" y1="${y}" x2="${xB}" y2="${y}" stroke="${BLUE}" stroke-width="2"
          stroke-dasharray="240" stroke-dashoffset="240">${рисуется(240,'13s',0.16,0.4)}</line>
        <circle cx="${xA}" cy="${y}" r="7" fill="url(#c1037-фигура)" stroke="${ОБВОД}" stroke-width="1.6">
          ${проявить('13s',0.08,0.16)}</circle>
        <circle cx="${xB}" cy="${y}" r="7" fill="url(#c1037-фигура2)" stroke="${ОБВОД}" stroke-width="1.6">
          ${проявить('13s',0.42,0.5)}</circle>
        ${прямойУгол([осьX,y],[xA,y],[осьX,y-30],11,0.5)}
        ${засечка(xA,y,осьX,y,0.56,2)}
        ${засечка(осьX,y,xB,y,0.64,2)}
        <g>${проявить('13s',0.66,0.76)}
          ${подпись(120,168,'расстояния равны',GREEN,12)}
          ${подпись(238,168,'угол прямой',GOLD,12)}</g>
        <g>${проявить('13s',0.8,0.9)}
          ${т(168,222,'ось делит отрезок пополам и под прямым углом',12,ИНК)}</g>
      `,244)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='середина'?'hit':(в?'miss':''), 'Делит пополам и под прямым углом', "r1037Точки('середина')")}
        ${BTN(4, в==='параллельно'?'miss':'', 'Проходит параллельно отрезку', "r1037Точки('параллельно')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='середина',
        в==='середина' ? 'Верно: ось симметрии — серединный перпендикуляр к отрезку между точкой и её отражением.'
                      : 'Нет: ось пересекает отрезок, а не идёт рядом. И пересекает ровно посередине под прямым углом.') : ЖДЁТ()) +
      ПРАВИЛО('Ось — <b>серединный перпендикуляр</b>: пополам и под 90°.');
  }

  /* 3. ПОСТРОЕНИЕ: циркуль и перпендикуляры */
  function F3(s){
    const в = s.построение;
    const осьX=150;
    const A=[54,92], B=[92,70], C=[72,134];
    const отр=([x,y])=>[2*осьX-x,y];
    return ВОПРОС('Как построить отражение треугольника?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Строим отражение по точкам',14,GOLD,true)}
        ${ось(осьX,44,осьX,196,0.04)}
        ${фигура(`M${A} L${B} L${C} Z`.replace(/,/g,' '),'фигура',0.08)}
        ${[A,B,C].map((p,i)=>{
          const q=отр(p);
          const длина=Math.hypot(q[0]-p[0],q[1]-p[1]);
          return `<g>
            <line x1="${p[0]}" y1="${p[1]}" x2="${q[0]}" y2="${q[1]}" stroke="${BLUE}" stroke-width="1.4"
              stroke-dasharray="${длина.toFixed(0)}" stroke-dashoffset="${длина.toFixed(0)}">
              ${рисуется(длина.toFixed(0),'13s',0.16+i*0.06,0.34+i*0.06)}</line>
            <path d="M${p[0]} ${p[1]} A26 26 0 0 1 ${(p[0]+(q[0]>p[0]?10:-10))} ${(p[1]-16)}" fill="none"
              stroke="${ЛИЛ}" stroke-width="1.4" opacity=".85">${проявить('13s',0.36+i*0.06,0.44+i*0.06)}</path>
            <circle cx="${q[0]}" cy="${q[1]}" r="6" fill="url(#c1037-фигура2)" stroke="${ОБВОД}" stroke-width="1.5">
              ${проявить('13s',0.44+i*0.06,0.5+i*0.06)}</circle>
          </g>`;
        }).join('')}
        ${фигура(`M${отр(A)} L${отр(B)} L${отр(C)} Z`.replace(/,/g,' '),'фигура2',0.62)}
        <g>${проявить('13s',0.7,0.8)}
          ${подпись(168,212,'каждая точка — на том же расстоянии',GREEN,12)}</g>
        <g>${проявить('13s',0.84,0.92)}
          ${т(168,240,'дуги откладывают равные расстояния',12,МУТ)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='перпендикуляр'?'hit':(в?'miss':''), 'Из каждой вершины провести перпендикуляр и отложить столько же', "r1037Строим('перпендикуляр')")}
        ${BTN(4, в==='сдвинуть'?'miss':'', 'Сдвинуть фигуру целиком вправо', "r1037Строим('сдвинуть')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='перпендикуляр',
        в==='перпендикуляр' ? 'Верно: каждая вершина отражается отдельно — по перпендикуляру к оси на такое же расстояние.'
                            : 'Нет: сдвиг даёт копию, а не отражение. Нужно отразить каждую вершину через ось.') : ЖДЁТ()) +
      ПРАВИЛО('Отражение строят <b>по вершинам</b>: перпендикуляр и равное расстояние.');
  }

  /* 4. ПРОВЕРКА: складываем две разные фигуры */
  function F4(s){
    const в = s.проверка;
    const ось1=96, ось2=250;
    /* слева симметричная «пэ»-фигура, справа несимметричная «R» */
    const пэ='M60 96 L60 60 L132 60 L132 96 L112 96 L112 78 L80 78 L80 96 Z';
    const R='M214 96 L214 58 L248 58 L248 74 L232 84 L250 96 L214 96 Z';
    return ВОПРОС('Какая из двух фигур симметрична относительно своей оси?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,24,'Проверяем складыванием',14,GOLD,true)}
        ${ось(ось1,44,ось1,170,0.04)}
        ${ось(ось2,44,ось2,170,0.04)}
        <g>${анМК('1 96 100;1 96 100;0.05 96 100;0.05 96 100;1 96 100','14s','0;0.28;0.5;0.76;1')}
          <path d="${пэ}" fill="url(#c1037-фигура)" stroke="${ОБВОД}" stroke-width="1.8"/></g>
        <g>${анМК('1 250 100;1 250 100;0.05 250 100;0.05 250 100;1 250 100','14s','0;0.32;0.54;0.78;1')}
          <path d="${R}" fill="url(#c1037-фигура2)" stroke="${ОБВОД}" stroke-width="1.8"/></g>
        ${проявить('13s',0.5,0.6)}
        <g>${проявить('13s',0.54,0.64)}
          ${подпись(96,186,'совпала ✓',GREEN,14)}</g>
        <g>${проявить('13s',0.78,0.88)}
          ${подпись(250,186,'не совпала ✗',RED,14)}</g>
        <g>${проявить('13s',0.86,0.94)}
          ${т(168,222,'у второй фигуры такой оси нет',12,ИНК)}
          ${т(168,242,'но ось может быть у другой прямой',12,МУТ)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='первая'?'hit':(в?'miss':''), 'Первая: её половинки совпадают', "r1037Проверка('первая')")}
        ${BTN(4, в==='вторая'?'miss':'', 'Вторая: она сложнее', "r1037Проверка('вторая')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='первая',
        в==='первая' ? 'Верно: у первой половинки совпали — значит она симметрична относительно этой оси.'
                     : 'Нет: сложность ни при чём. У второй половинки не совпали, значит относительно этой оси она не симметрична.') : ЖДЁТ()) +
      ПРАВИЛО('Симметрию проверяют <b>складыванием</b>: совпало — симметрична.');
  }

  /* 5. ЦЕНТРАЛЬНАЯ СИММЕТРИЯ: луч через центр */
  function F5(s){
    const в = s.центральная;
    const O=[168,124], A=[96,88], B=[240,160];
    return ВОПРОС('Точка и её центрально-симметричная пара. Где центр?') +
      `<div class="pic">${свг(`
        ${фон(244)}
        ${т(168,24,'Центральная симметрия',14,GOLD,true)}
        <line x1="${A[0]}" y1="${A[1]}" x2="${B[0]}" y2="${B[1]}" stroke="${BLUE}" stroke-width="2"
          stroke-dasharray="200" stroke-dashoffset="200">${рисуется(200,'13s',0.1,0.36)}</line>
        <circle cx="${A[0]}" cy="${A[1]}" r="7" fill="url(#c1037-фигура)" stroke="${ОБВОД}" stroke-width="1.6">
          ${проявить('13s',0.06,0.14)}</circle>
        <circle cx="${B[0]}" cy="${B[1]}" r="7" fill="url(#c1037-фигура2)" stroke="${ОБВОД}" stroke-width="1.6">
          ${проявить('13s',0.38,0.46)}</circle>
        <circle cx="${O[0]}" cy="${O[1]}" r="9" fill="url(#c1037-металл)" stroke="${ОБВОД}" stroke-width="1.6">
          ${проявить('13s',0.5,0.58)}</circle>
        ${засечка(A[0],A[1],O[0],O[1],0.58,1)}
        ${засечка(O[0],O[1],B[0],B[1],0.66,1)}
        <g>${проявить('13s',0.62,0.72)}
          ${подпись(120,74,'до центра',BLUE,12)}
          ${подпись(224,182,'после центра',GREEN,12)}</g>
        <g>${проявить('13s',0.78,0.88)}
          ${т(168,214,'центр лежит ровно посередине',14,ИНК,true)}
          ${т(168,238,'и точки на одной прямой с ним',12,МУТ)}</g>
      `,244)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='середина'?'hit':(в?'miss':''), 'Центр — середина отрезка между точками', "r1037Центр('середина')")}
        ${BTN(4, в==='рядом'?'miss':'', 'Центр — рядом с одной из точек', "r1037Центр('рядом')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='середина',
        в==='середина' ? 'Верно: при центральной симметрии центр делит отрезок пополам, и обе точки лежат на одной прямой с ним.'
                      : 'Нет: расстояния равны, значит центр ровно посередине. Видно по одинаковым засечкам.') : ЖДЁТ()) +
      ПРАВИЛО('Центральная симметрия: <b>центр — середина</b> отрезка между точками.');
  }

  /* 6. ПОВОРОТ НА 180° */
  function F6(s){
    const в = s.поворот;
    const ц=[168,124];
    const d='M120 96 L146 76 L182 92 L172 132 L134 140 Z';
    return ВОПРОС('Как ещё получить центрально-симметричную фигуру?') +
      `<div class="pic">${свг(`
        ${фон(248)}
        ${т(168,24,'Поворот на 180° вокруг центра',14,GOLD,true)}
        <path d="M216 96 L242 76 L278 92 L268 132 L230 140 Z" fill="url(#c1037-фигура2)" stroke="${ОБВОД}"
          stroke-width="1.8" opacity=".85">${проявить('13s',0.08,0.16)}</path>
        <g>${анВК('0 168 124;0 168 124;180 168 124;180 168 124;0 168 124','15s','0;0.24;0.66;0.86;1')}
          <path d="${d}" fill="url(#c1037-фигура)" stroke="${ОБВОД}" stroke-width="1.8" opacity=".92"/>
        </g>
        <circle cx="${ц[0]}" cy="${ц[1]}" r="8" fill="url(#c1037-металл)" stroke="${ОБВОД}" stroke-width="1.6"/>
        <path d="M168 88 A36 36 0 1 1 132 124" fill="none" stroke="${GOLD}" stroke-width="2" stroke-dasharray="6 4">
          ${проявить('13s',0.3,0.4)}</path>
        <g>${проявить('13s',0.44,0.54)}
          ${подпись(196,72,'поворот на 180°',GOLD,12)}</g>
        <g>${проявить('13s',0.72,0.82)}
          ${подпись(168,186,'фигура встала на место отражения',GREEN,12)}</g>
        <g>${проявить('13s',0.88,0.96)}
          ${т(168,224,'поворот на 180° — это и есть центральная симметрия',12,ИНК)}</g>
      `,248)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='поворот'?'hit':(в?'miss':''), 'Повернуть фигуру на 180° вокруг центра', "r1037Поворот('поворот')")}
        ${BTN(4, в==='сдвиг'?'miss':'', 'Сдвинуть фигуру на её ширину', "r1037Поворот('сдвиг')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='поворот',
        в==='поворот' ? 'Верно: при повороте на 180° вокруг центра каждая точка переходит в свою пару — это центральная симметрия.'
                      : 'Нет: сдвиг не даёт центральной симметрии. Нужен поворот на 180° вокруг центра.') : ЖДЁТ()) +
      ПРАВИЛО('Центральная симметрия — это <b>поворот на 180°</b>.');
  }

  /* 7. СКОЛЬКО ОСЕЙ: квадрат, прямоугольник, треугольник */
  function F7(s){
    const в = s.оси;
    const квадрат=[['168','84','168','164'],['128','124','208','124'],['140','96','196','152'],['196','96','140','152']];
    return ВОПРОС('Сколько осей симметрии у квадрата?') +
      `<div class="pic">${свг(`
        ${фон(250)}
        ${т(168,24,'Оси квадрата появляются по очереди',14,GOLD,true)}
        ${фигура('M128 84 L208 84 L208 164 L128 164 Z','фигура',0.04)}
        ${квадрат.map(([x1,y1,x2,y2],i)=>`<g>${проявить('13s',0.14+i*0.1,0.22+i*0.1)}
          <line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${GOLD}" stroke-width="2" stroke-dasharray="7 5"/></g>`).join('')}
        <g>${проявить('13s',0.62,0.72)}
          ${подпись(168,196,'у квадрата четыре оси',GREEN,14)}</g>
        <g>${проявить('13s',0.76,0.86)}
          ${т(168,224,'у прямоугольника — две, у треугольника — три',12,ИНК)}
          ${т(168,244,'у круга — бесконечно много',12,МУТ)}</g>
      `,250)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='четыре'?'hit':(в?'miss':''), '4: две через середины сторон и две по диагоналям', "r1037Оси('четыре')")}
        ${BTN(4, в==='две'?'miss':'', '2: только через середины сторон', "r1037Оси('две')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='четыре',
        в==='четыре' ? 'Верно: у квадрата четыре оси — две через середины сторон и две по диагоналям. А вот у неквадратного прямоугольника диагонали осями не будут.'
                     : 'Нет: диагонали квадрата тоже оси, потому что квадрат складывается по диагонали. Всего четыре.') : ЖДЁТ()) +
      ПРАВИЛО('Число осей зависит от фигуры: квадрат — 4, прямоугольник — 2.');
  }

  /* 8. БУКВЫ: зеркальная линейка */
  function F8(s){
    const в = s.буквы;
    return ВОПРОС('У какой буквы есть горизонтальная ось симметрии?') +
      `<div class="pic">${свг(`
        ${фон(246)}
        ${т(168,24,'Зеркало проверяет букву',14,GOLD,true)}
        <rect x="20" y="54" width="296" height="80" rx="6" fill="url(#c1037-бумага)" stroke="rgba(90,72,40,.5)" stroke-width="1">
          ${проявить('13s',0.04,0.12)}</rect>
        <rect x="20" y="54" width="296" height="80" rx="6" fill="none" filter="url(#c1037-шум)" opacity=".08"/>
        ${['А','К','R','Ж'].map((б,i)=>`<g>${проявить('13s',0.1+i*0.06,0.18+i*0.06)}
          ${т(70+i*66,108,б,32,'#2b2110',true)}</g>`).join('')}
        ${/* зеркало едет по буквам */''}
        <g>${анТК('0 0;0 0;198 0;198 0;0 0','15s','0;0.16;0.72;0.9;1')}
          <line x1="70" y1="60" x2="70" y2="128" stroke="${BLUE}" stroke-width="2.4"/>
          <line x1="70" y1="60" x2="70" y2="128" stroke="rgba(127,209,255,.35)" stroke-width="6" filter="url(#c1037-мягко)"/>
        </g>
        <g>${проявить('13s',0.38,0.48)}
          ${подпись(70,150,'вертикальная: А',GREEN,12)}</g>
        <g>${проявить('13s',0.66,0.76)}
          ${подпись(202,150,'горизонтальная: К',GOLD,12)}</g>
        <g>${проявить('13s',0.86,0.94)}
          ${т(168,194,'А, М, Т — вертикальная;  В, Е, К — горизонтальная',12,ИНК)}
          ${т(168,218,'Ж, Н, О, Х, Ф — обе;  R и S — ни одной',12,МУТ)}</g>
      `,246)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='К'?'hit':(в?'miss':''), 'У буквы К: верх и низ совпадают', "r1037Буквы('К')")}
        ${BTN(4, в==='А'?'miss':'', 'У буквы А: левая и правая половины совпадают', "r1037Буквы('А')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='К',
        в==='К' ? 'Верно: у буквы К совпадают верхняя и нижняя половины — ось горизонтальная. У А ось вертикальная.'
                : 'Нет: у А ось вертикальная — совпадают левая и правая половины. А горизонтальная ось есть у К, В, Е, С.') : ЖДЁТ()) +
      ПРАВИЛО('Ось может быть <b>вертикальной, горизонтальной</b> — или их две сразу.');
  }

  /* 9. ЛОВУШКА: ось есть, но не всякая */
  function F9(s){
    const в = s.ловушка;
    return ВОПРОС('Фигура симметрична относительно горизонтальной прямой. Значит ли это, что у неё есть вертикальная ось?') +
      `<div class="pic">${свг(`
        ${фон(244)}
        ${т(168,24,'Одна ось не обещает другую',14,GOLD,true)}
        ${/* буква К: горизонтальная ось есть, вертикальной нет */''}
        <g>${проявить('13s',0.06,0.14)}
          <path d="M96 74 L140 74 L140 116 L96 116 Z" fill="url(#c1037-фигура)" stroke="${ОБВОД}" stroke-width="1.8"/>
          <path d="M140 74 L172 74 L172 86 L146 95 L172 116 L140 116 Z" fill="url(#c1037-фигура)" stroke="${ОБВОД}" stroke-width="1.8"/>
        </g>
        <line x1="70" y1="95" x2="196" y2="95" stroke="${GREEN}" stroke-width="2.2" stroke-dasharray="7 5">
          ${анК('opacity','0;0;1;1','14s','0;0.24;0.34;1')}</line>
        ${подпись(222,95,'горизонтальная ✓',GREEN,12)}
        <line x1="134" y1="60" x2="134" y2="132" stroke="${RED}" stroke-width="2.2" stroke-dasharray="7 5">
          ${анК('opacity','0;0;1;1','14s','0;0.52;0.62;1')}</line>
        ${подпись(134,152,'вертикальная ✗',RED,12)}
        <g>${проявить('13s',0.72,0.82)}
          ${подпись(168,186,'одна ось есть, другая не подходит',ИНК,12)}</g>
        <g>${проявить('13s',0.86,0.94)}
          ${т(168,220,'фигуру проверяют для КАЖДОЙ прямой отдельно',12,МУТ)}</g>
      `,244)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='нет'?'hit':(в?'miss':''), 'Нет: вертикальная ось не подходит', "r1037Ловушка('нет')")}
        ${BTN(4, в==='да'?'miss':'', 'Да: раз одна есть, есть и вторая', "r1037Ловушка('да')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='нет',
        в==='нет' ? 'Верно: каждую ось проверяют отдельно. Горизонтальная подошла, вертикальная — нет.'
                  : 'Нет: оси независимы. Эта фигура складывается сверху вниз, но не слева направо.') : ЖДЁТ()) +
      ПРАВИЛО('Симметрию проверяют <b>для конкретной прямой</b>.');
  }

  /* 10. СВОДКА: два действия рядом */
  function F10(s){
    const в = s.сводка;
    return ВОПРОС('Чем центральная симметрия отличается от осевой?') +
      `<div class="pic">${свг(`
        ${фон(252)}
        ${т(168,24,'Складывание и поворот',14,GOLD,true)}
        ${/* слева складывание */''}
        <g>${проявить('13s',0.05,0.14)}
          <rect x="18" y="44" width="144" height="120" rx="6" fill="rgba(255,255,255,.03)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
          <path d="M60 96 L60 68 L120 68 L120 96 L104 96 L104 84 L76 84 L76 96 Z" fill="url(#c1037-фигура)" stroke="${ОБВОД}" stroke-width="1.6"/>
          <line x1="90" y1="52" x2="90" y2="152" stroke="${GOLD}" stroke-width="2" stroke-dasharray="6 4"/>
        </g>
        <g>${анМК('1 90 100;1 90 100;0.06 90 100;0.06 90 100;1 90 100','15s','0;0.2;0.44;0.72;1')}
          <path d="M60 96 L60 68 L90 68 L90 96 L76 96 L76 84 L60 84 Z" fill="url(#c1037-фигура)" stroke="${ОБВОД}" stroke-width="1.6" opacity=".8"/>
        </g>
        ${т(90,190,'осевая: сгиб',14,GREEN,true)}
        ${/* справа поворот */''}
        <g>${проявить('13s',0.3,0.4)}
          <rect x="174" y="44" width="144" height="120" rx="6" fill="rgba(255,255,255,.03)" stroke="${ЛИНИЯ}" stroke-width="1.4"/>
          <circle cx="246" cy="104" r="7" fill="url(#c1037-металл)" stroke="${ОБВОД}" stroke-width="1.5"/>
        </g>
        <g>${анВК('0 246 104;0 246 104;180 246 104;180 246 104;0 246 104','15s','0;0.5;0.78;0.9;1')}
          <path d="M212 88 L236 76 L266 88 L258 120 L224 118 Z" fill="url(#c1037-фигура2)" stroke="${ОБВОД}" stroke-width="1.6"/>
        </g>
        ${т(246,190,'центральная: поворот',14,GOLD,true)}
        <g>${проявить('13s',0.8,0.9)}
          ${т(168,222,'осевая отражение — центральная поворот',12,ИНК)}
          ${т(168,244,'у центральной осей может не быть вовсе',12,МУТ)}</g>
      `,252)}</div>` +
      `<div class="ask">
        ${BTN(3, в==='поворот'?'hit':(в?'miss':''), 'Осевая — складывание по прямой, центральная — поворот на 180°', "r1037Свод('поворот')")}
        ${BTN(4, в==='одно'?'miss':'', 'Это одно и то же', "r1037Свод('одно')")}
      </div>` +
      (в!=null ? РАЗБОР(в==='поворот',
        в==='поворот' ? 'Верно: осевая симметрия — отражение относительно прямой, центральная — поворот на 180° вокруг точки.'
                      : 'Нет: это разные преобразования. При центральной симметрии прямая-ось не нужна вовсе.') : ЖДЁТ()) +
      ПРАВИЛО('Осевая — <b>отражение</b>, центральная — <b>поворот на 180°</b>.');
  }

  /* 11. Практика */
  const УРОВНИ=[
    { вопрос:'Половинки фигуры совпали при складывании. Что это значит?', варианты:[
        {т:'Фигура симметрична относительно линии сгиба', ок:true,  почему:'Совпадение половинок — определение симметрии.'},
        {т:'Фигура состоит из равных частей, но об оси ничего не скажешь', ок:false, почему:'Совпадение как раз и означает ось симметрии.'}]},
    { вопрос:'Как ось расположена к отрезку между точкой и её отражением?', варианты:[
        {т:'Делит пополам под прямым углом', ок:true,  почему:'Ось — серединный перпендикуляр.'},
        {т:'Проходит параллельно ему', ок:false, почему:'Ось пересекает отрезок, а не идёт рядом.'}]},
    { вопрос:'Сколько осей у квадрата?', варианты:[
        {т:'4', ок:true,  почему:'Две через середины сторон и две по диагоналям.'},
        {т:'2', ок:false, почему:'Диагонали квадрата тоже оси.'}]},
    { вопрос:'Центральная симметрия — это…', варианты:[
        {т:'поворот на 180° вокруг центра', ок:true,  почему:'Каждая точка переходит в свою пару.'},
        {т:'сдвиг фигуры', ок:false, почему:'Сдвиг не даёт симметрии.'}]},
    { вопрос:'У буквы К есть горизонтальная ось. Есть ли вертикальная?', варианты:[
        {т:'нет: оси проверяют отдельно', ок:true,  почему:'Горизонтальная подходит, вертикальная — нет.'},
        {т:'да: раз одна есть, есть и другая', ок:false, почему:'Оси независимы.'}]}
  ];
  function F11(s){
    const пройдено = s.практика||0;
    const готово = пройдено>=УРОВНИ.length;
    const и = Math.min(пройдено, УРОВНИ.length-1);
    if(готово){
      return ТОЧКИ(УРОВНИ.length, и, пройдено) +
        ВОПРОС('Все пять уровней пройдены. Что помогало?') +
        РАЗБОР(true,'Две проверки: сложить по прямой — это осевая симметрия; повернуть на 180° вокруг точки — центральная.') +
        `<div class="ask">${BTN(4,'','пройти заново','r1037Reset()')}</div>`;
    }
    const ур=УРОВНИ[и];
    const выбран = (s.практикаУровень===и) ? s.практикаВыбор : null;
    return ТОЧКИ(УРОВНИ.length, и, пройдено) +
      A(2,'cap','Уровень '+(и+1)+' из '+УРОВНИ.length) +
      ВОПРОС(ур.вопрос) +
      '<div class="ask">' + ур.варианты.map((в,к)=>
        BTN(3+к, выбран===к?(в.ок?'hit':'miss'):'', в.т, `r1037Pick(${и},${к})`)).join('') + '</div>' +
      (выбран!=null ? РАЗБОР(ур.варианты[выбран].ок, ур.варианты[выбран].почему) : ЖДЁТ()) +
      ПРАВИЛО('Сгиб — <b>осевая</b>, пол-оборота — <b>центральная</b>.');
  }

  /* 12–14. Три тренажёра */
  const Т1={
    имя:'Тренажёр 1 · осевая симметрия',
    задания:[
      { ф:'Сколько осей у квадрата?', о:['4','2','1'], в:0, р:'Две по сторонам и две по диагоналям.' },
      { ф:'Сколько осей у прямоугольника, не квадрата?', о:['2','4','1'], в:0, р:'Только через середины сторон.' },
      { ф:'Ось делит отрезок между точкой и отражением…', о:['пополам под прямым углом','пополам под острым углом'], в:0, р:'Серединный перпендикуляр.' },
      { ф:'У буквы А ось…', о:['вертикальная','горизонтальная'], в:0, р:'Левая и правая половины совпадают.' }
    ]
  };
  const Т2={
    имя:'Тренажёр 2 · центральная симметрия',
    задания:[
      { ф:'Центральная симметрия — это…', о:['поворот на 180°','отражение от прямой'], в:0, р:'Вокруг центра.' },
      { ф:'Где центр относительно пары точек?', о:['посередине','ближе к одной'], в:0, р:'Расстояния равны.' },
      { ф:'Сколько осей у круга?', о:['бесконечно много','четыре'], в:0, р:'Любая прямая через центр.' },
      { ф:'Сколько осей у равностороннего треугольника?', о:['3','1','6'], в:0, р:'По одной из каждой вершины.' }
    ]
  };
  const Т3={
    имя:'Тренажёр 3 · ловушки',
    задания:[
      { ф:'У фигуры есть горизонтальная ось. Значит, есть и вертикальная?', о:['не обязательно','да, всегда'], в:0, р:'Оси проверяют отдельно.' },
      { ф:'У буквы К есть вертикальная ось?', о:['нет','да'], в:0, р:'Верх и низ совпадают, лево и право — нет.' },
      { ф:'Буква R симметрична?', о:['нет ни одной оси','есть вертикальная'], в:0, р:'Ножка уходит вправо.' },
      { ф:'Совпали ли половинки — значит…', о:['есть ось симметрии','фигура равна себе'], в:0, р:'Это определение осевой симметрии.' }
    ]
  };

  function тренер(набор, ключ, состояние, шаг){
    const з=набор.задания[шаг % набор.задания.length];
    const отв=состояние[ключ+'Ответ'], готово=отв!=null, верно=отв===з.в;
    const точки=Array.from({length:набор.задания.length},(_,к)=>
      `<span class="точка ${к<шаг?'пройдено':(к===шаг?'сейчас':'')}"></span>`).join('');
    return A(0,'уровни',точки) +
      A(2,'карт вопрос','<span class="метка">'+набор.имя+'</span><div class="текст">'+з.ф+'</div>') +
      A(3,'cap','Уровень '+(шаг+1)+' из '+набор.задания.length) +
      '<div class="ask">' + з.о.map((о,к)=>
        BTN(4+к, готово&&к===з.в?'hit':(готово&&к===отв?'miss':''), о, `r1037T('${ключ}',${к})`)).join('') + '</div>' +
      (готово ? РАЗБОР(верно, з.р) : ЖДЁТ()) +
      `<p class="score">верно: ${состояние[ключ+'Верно']||0} · ошибок: ${состояние[ключ+'Ошибки']||0} · всего: ${набор.задания.length}</p>` +
      (готово ? `<div class="ask">${BTN(5,'','следующий вопрос',`r1037TNext('${ключ}')`)}</div>` : '');
  }
  function F12(s){ return тренер(Т1,'т1',s,s.т1Шаг||0); }
  function F13(s){ return тренер(Т2,'т2',s,s.т2Шаг||0); }
  function F14(s){ return тренер(Т3,'т3',s,s.т3Шаг||0); }

  const L1037={
    id: ID, title:'Симметрия фигур', ico:'🦋',
    src:'Математика · 6 класс · Геометрия', subj:'math',
    explain: [
      'Бабочка села на лист бумаги. Если сложить лист по средней линии, левое крыло ляжет ровно на правое. Линия сгиба называется осью симметрии, а сама фигура — симметричной относительно этой прямой. Так симметрию и проверяют: мысленно сложить фигуру по прямой и посмотреть, совпадут ли половинки.',

      'Разберём точнее. Возьмём точку и её отражение. Ось симметрии проходит ровно посередине между ними и под прямым углом к соединяющему их отрезку. Иначе говоря, ось — это серединный перпендикуляр: она делит отрезок пополам и стоит к нему под 90°. Расстояния от каждой точки до оси одинаковы. Частая ошибка — считать, что ось проходит под наклоном. Как проверить себя: посмотри на знак прямого угла и парные засечки.',

      'Чтобы построить отражение фигуры, отражают каждую вершину по отдельности. Из вершины проводят перпендикуляр к оси, измеряют расстояние и откладывают такое же с другой стороны — удобно делать это циркулем. Полученные точки соединяют, и получается отражение. Частая ошибка — сдвинуть фигуру целиком: при сдвиге получается копия, а не отражение. Как проверить себя: расстояние от каждой вершины до оси должно сохраниться.',

      'Проверим на двух фигурах. Первая похожа на букву П: если сложить её по вертикальной оси, половинки совпадут — фигура симметрична. Вторая похожа на букву R: как её ни складывай по этой же оси, половинки разные — относительно этой прямой фигура не симметрична. Частая ошибка — судить по сложности фигуры. Как проверить себя: складывай и сравнивай.',

      'Кроме осевой симметрии бывает центральная. Возьмём две точки и точку-центр. Если центр лежит ровно посередине между ними, а сами точки — на одной прямой с центром, то они симметричны относительно центра. Расстояния от центра до каждой точки равны. Частая ошибка — путать центр симметрии с осью. Как проверить себя: здесь нет прямой-оси, есть только точка.',

      'Центральную симметрию можно получить поворотом. Если повернуть фигуру на 180° вокруг центра, каждая её точка перейдёт в свою пару, и фигура встанет на место своего центрально-симметричного отражения. Это и есть определение: центральная симметрия — поворот на 180°. Частая ошибка — думать, что центральная симметрия даёт то же, что осевая. Как проверить себя: при осевой мы складываем, при центральной — поворачиваем.',

      'Сколько бывает осей? У квадрата их четыре: две проходят через середины противоположных сторон и две — по диагоналям. У прямоугольника, который не квадрат, осей только две: диагонали его осями не будут. У равностороннего треугольника три оси, у правильного пятиугольника пять, а у круга — бесконечно много, потому что любая прямая через центр делит его пополам. Частая ошибка — считать, что у прямоугольника четыре оси, как у квадрата. Как проверить себя: попробуй сложить по диагонали.',

      'Симметрия встречается и в буквах. У буквы А ось вертикальная: левая и правая половины совпадают. У буквы К ось горизонтальная: совпадают верх и низ. У букв Ж, Н, О, Х и Ф есть обе оси сразу. А у букв R, S, G и Я осей нет вовсе. Частая ошибка — считать, что у всех букв ось вертикальная. Как проверить себя: приложи зеркало сверху и сбоку.',

      'Важно помнить: одна ось не обещает другую. У буквы К есть горизонтальная ось, но вертикальной нет — слева и справа фигура разная. Поэтому, говоря «фигура симметрична», всегда уточняют, относительно какой прямой. Частая ошибка — проверять симметрию «вообще». Как проверить себя: назови конкретную прямую и проверь именно её.',

      'Соберём вместе. Осевая симметрия — это отражение относительно прямой: фигура складывается по оси, и половинки совпадают. Центральная симметрия — это поворот на 180° вокруг точки-центра. Ось всегда серединный перпендикуляр к отрезку между точкой и её отражением, а центр — середина такого отрезка. Частая ошибка — путать эти два вида. Как проверить себя: спроси, есть ли здесь прямая-ось или только точка.',

      'Пройти практику: пять уровней про складывание, построение отражения, оси квадрата и ловушку «одна ось не обещает другую». Дальше ждут три тренажёра.',

      'Первый тренажёр — осевая симметрия: складываем и считаем оси.',

      'Второй тренажёр — центральная симметрия: поворот на 180° и точка-центр.',

      'Третий тренажёр — ловушки: проверяем каждую ось отдельно и не путаем виды симметрии.'
    ],
    check: {
      q:'Сколько осей симметрии у квадрата?',
      choices:['4','2','1'],
      ans:0,
      exp:'Две оси через середины сторон и две по диагоналям — всего четыре.'
    },
    tasks: [
      { q:'Как ось расположена к отрезку между точкой и её отражением?', kind:'choice',
        choices:['Делит пополам под прямым углом','Проходит параллельно'], ans:0,
        hints:['Ось — серединный перпендикуляр.','Расстояния равны, угол 90°.'],
        sol:'Пополам и под прямым углом.' },
      { q:'Центральная симметрия — это…', kind:'choice',
        choices:['поворот на 180°','отражение от прямой'], ans:0,
        hints:['Здесь нет прямой-оси.','Есть только точка-центр.'],
        sol:'Поворот на 180° вокруг центра.' },
      { q:'У буквы К есть горизонтальная ось. Есть ли вертикальная?', kind:'choice',
        choices:['нет','да'], ans:0,
        hints:['Сравни левую и правую половины.','Оси проверяют отдельно.'],
        sol:'Нет.' },
      { q:'Сколько осей у прямоугольника, который не квадрат?', kind:'choice',
        choices:['2','4'], ans:0,
        hints:['Диагонали не подходят.','Складывается только по серединам сторон.'],
        sol:'2.' }
    ]
  };

  function render(el){
    css();
    const s = S();
    const step = Math.max(0, Math.min(L1037.explain.length-1, (typeof LV!=='undefined'&&LV.step)||0));
    const f = step+1;
    let сцена='';
    if(f===1) сцена=F1(s);
    else if(f===2) сцена=F2(s);
    else if(f===3) сцена=F3(s);
    else if(f===4) сцена=F4(s);
    else if(f===5) сцена=F5(s);
    else if(f===6) сцена=F6(s);
    else if(f===7) сцена=F7(s);
    else if(f===8) сцена=F8(s);
    else if(f===9) сцена=F9(s);
    else if(f===10) сцена=F10(s);
    else if(f===11) сцена=F11(s);
    else if(f===12) сцена=F12(s);
    else if(f===13) сцена=F13(s);
    else сцена=F14(s);

    const ЗАГОЛОВКИ={
      1:['Бабочка','Складываем по оси'],
      2:['Ось и точки','Серединный перпендикуляр'],
      3:['Построение','Отражение по вершинам'],
      4:['Проверка','Совпало или нет'],
      5:['Центр','Точка посередине'],
      6:['Поворот','180° вокруг центра'],
      7:['Сколько осей','Квадрат и другие'],
      8:['Буквы','Зеркало проверяет'],
      9:['Одна ось','Не обещает другую'],
      10:['Сравнение','Сгиб и поворот'],
      11:['Практика','Пять уровней'],
      12:['Тренажёр 1','Осевая симметрия'],
      13:['Тренажёр 2','Центральная симметрия'],
      14:['Тренажёр 3','Ловушки']
    };
    const з=ЗАГОЛОВКИ[f]||['Симметрия фигур','Геометрия'];
    el.innerHTML = `<div class="s6 l1037" data-frame="${f}">
        <h2>${з[1]}</h2>
        ${сцена}
      </div>`;
  }

  window.r1037Бабочка=(к)=>{ S().бабочка=к; chRender(0); };
  window.r1037Точки=(к)=>{ S().ось_точки=к; chRender(0); };
  window.r1037Строим=(к)=>{ S().построение=к; chRender(0); };
  window.r1037Проверка=(к)=>{ S().проверка=к; chRender(0); };
  window.r1037Центр=(к)=>{ S().центральная=к; chRender(0); };
  window.r1037Поворот=(к)=>{ S().поворот=к; chRender(0); };
  window.r1037Оси=(к)=>{ S().оси=к; chRender(0); };
  window.r1037Буквы=(к)=>{ S().буквы=к; chRender(0); };
  window.r1037Ловушка=(к)=>{ S().ловушка=к; chRender(0); };
  window.r1037Свод=(к)=>{ S().сводка=к; chRender(0); };
  window.r1037Pick=(уровень,вариант)=>{
    const s=S();
    s.практикаУровень=уровень; s.практикаВыбор=вариант;
    if(УРОВНИ[уровень].варианты[вариант].ок) s.практика=уровень+1;
    chRender(0);
  };
  window.r1037Reset=()=>{ const s=S(); s.практика=0; s.практикаВыбор=null; s.практикаУровень=null; chRender(0); };
  window.r1037T=(ключ,вариант)=>{
    const s=S();
    if(s[ключ+'Ответ']!=null) return;
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    const шаг=s[ключ+'Шаг']||0;
    const з=набор.задания[шаг % набор.задания.length];
    s[ключ+'Ответ']=вариант;
    if(вариант===з.в) s[ключ+'Верно']=(s[ключ+'Верно']||0)+1; else s[ключ+'Ошибки']=(s[ключ+'Ошибки']||0)+1;
    chRender(0);
  };
  window.r1037TNext=(ключ)=>{
    const s=S();
    const набор = ключ==='т1'?Т1:(ключ==='т2'?Т2:Т3);
    s[ключ+'Шаг']=((s[ключ+'Шаг']||0)+1)%набор.задания.length;
    s[ключ+'Ответ']=null;
    chRender(0);
  };

  function зарегистрировать(){
    try{
      if(!window.VISKW) window.VISKW={};
      window.VISKW[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      if(window.WAVE_B) window.WAVE_B[ID]=function(el){ try{ render(el); }catch(e){ el.innerHTML=''; } };
      const arr=window.ARH_LESSONS;
      if(arr && arr.length){
        const место=arr.findIndex(L=>L && L.id===ID);
        if(место>=0) arr[место]=L1037; else arr.push(L1037);
      }
    }catch(e){}
  }
  зарегистрировать();
  document.addEventListener('DOMContentLoaded', зарегистрировать);
  window.addEventListener('load', зарегистрировать);
  window.MA1037={render:render, L:L1037};
})();
