/* АРХИМЕД MVP · agent.js — «мозг» голосового агента (смысловые ответы без внешних API) */
'use strict';
const AGENT = (function(){
  const FAQ = [
    { keys:['дирихле','голуб','клетк','кролик'], txt:'Принцип Дирихле: если n предметов разложить по m ячейкам и n больше m, то хотя бы в одной ячейке окажется ⌈n/m⌉ предметов. Пример: 10 голубей и 7 клеток — где-то точно двое, потому что 10 больше 7.' },
    { keys:['инвариант','не меняет','постоянн'], txt:'Инвариант — это величина, которая не меняется при любом разрешённом действии: например, чётность суммы, остаток при делении. Если инвариант разный в начале и в конце — превращение невозможно.' },
    { keys:['чётност','чётн','нечётн'], txt:'Следи за чётностью! Сумма двух чётных — чётная, двух нечётных — тоже чётная, а чётного и нечётного — нечётная. Многие задачи «можно ли?» решаются проверкой чётности до и после.' },
    { keys:['оценк','пример','минимальн','максимальн'], txt:'«Оценка и пример» — способ решать задачи на минимум и максимум: сначала докажи, что меньше (или больше) нельзя, — это оценка. Потом покажи пример, где это значение достигается. Два шага — и ответ точный.' },
    { keys:['перевёрт','наоборот','разност'], txt:'Число и его перевёртыш: если abc записать наоборот, получится cba. Сумма равна 101·(a+c) + 20·b, а разность всегда 99·(a−c) — то есть делится на 99. Так решается почти любая задача про перевёрнутые числа.' },
    { keys:['турнир','партий','очк','кругов'], txt:'В круговом турнире каждый играет с каждым один раз. Партий в турнире из n игроков: n·(n−1)/2. Очки считай по правилам: победа, ничья, поражение.' },
    { keys:['делимост','делится','остатк','признак'], txt:'Признаки делимости: на 2 — число чётное, на 3 и на 9 — сумма цифр делится на 3 или 9, на 5 — оканчивается на 0 или 5. Остатки при делении помогают делить объекты на группы.' },
    { keys:['взвешиван','вес','монет'], txt:'Задачи на взвешивания: весы дают три исхода — легче, тяжелее, равно. Поэтому за одно взвешивание можно различить до трёх вариантов, за два — до девяти. Дели объекты на три части!' },
    { keys:['дроб','процент','доля'], txt:'Дробь — это часть целого. Процент — это сотая доля: 25% это четверть. «Во сколько раз больше» — раздели одно на другое.' },
    { keys:['комбинатор','способ','перестановк','размещ'], txt:'Комбинаторика считает варианты: перестановки — n!, размещения — с учётом порядка, сочетания — без. Главное правило: если выборы независимы, количество вариантов перемножается.' },
    { keys:['граф','дорог','рукопожат','друз'], txt:'Если надо посчитать связи «каждый с каждым» (дороги, рукопожатия): из n объектов выходит n−1 связь, но каждая посчитана дважды — дели на 2: n·(n−1)/2.' },
    { keys:['средн','среднее арифмет'], txt:'Среднее арифметическое — это «поровну на всех»: сложи все значения и раздели на их количество.' },
    { keys:['скорост','движени','встрет','гонц'], txt:'Если объекты движутся навстречу, их скорости складываются. Время встречи = расстояние ÷ скорость сближения. Не усредняй скорости, если время в пути разное — считай через «весь путь ÷ всё время».' },
    { keys:['площад','периметр','геометр'], txt:'Периметр — длина границы (сумма сторон), площадь — сколько квадратиков внутри (длина × ширина для прямоугольника). Для «собранных» фигур ищи, из каких частей они состоят.' },
    { keys:['всош','всш','олимпиад','этап','как готов','подготов'], txt:'ВсОШ по математике в 6–7 классах: школьный этап — 8 задач с «клонами», потом муниципальный. Главные темы: логика, Дирихле, инварианты, чётность, комбинаторика, «оценка и пример». Тренируйся в Пути и Книге знаний, потом пробуй Тур — как настоящий.' },
    { keys:['информатик','двоичн','бит','код','алгоритм','программ','логик и'], txt:'Информатика на острове Цитадель: двоичный код — числа из 0 и 1 (5 = 101₂); бит кодирует 2 варианта, n бит — 2ⁿ. Бинарный поиск: вопрос «да/нет» делит варианты пополам. Шаги +1/+2 считаются числами Фибоначчи. Логика: И — оба должны быть 1, ИЛИ — хотя бы один, НЕ — наоборот.' },
    { keys:['кто ты','ты кто','архимед','как тебя'], txt:'Я Архимед — твой голосовой помощник по олимпиадной математике, физике и химии, а теперь и по информатике. Объясняю задачи голосом и подсказываю приёмы.' },
    { keys:['как дела','привет','здравств','добрый'], txt:'Привет! Я готов объяснить задачу или рассказать про приём. Скажи, например: «что такое инвариант» или «объясни задачу».' },
    { keys:['спасибо','молодец','круто','супер'], txt:'Пожалуйста! Если хочешь — решим ещё одну задачу или разберём новый приём.' },
    { keys:['помощ','что ты умеешь','команд'], txt:'Я умею: объяснить текущую задачу, дать подсказку, рассказать решение, перейти дальше, объяснить любой олимпиадный приём (Дирихле, инвариант, чётность…) и переключить голос.' }
  ];
  function clean(s){ return String(s||'').toLowerCase().replace(/[ё]/g,'е'); }
  function taskCtx(){
    if(typeof CUR!=='undefined'&&CUR&&CUR.task&&document.querySelector('.q')) return CUR.task;
    return null;
  }
  function lessonCtx(){
    if(typeof LV!=='undefined'&&LV&&LV.id&&document.querySelector('.sdot')) { try{ return lessonById(LV.id); }catch(e){} }
    return null;
  }
  function answer(q){
    const s=clean(q); if(!s) return '';
    // вопросы про текущую задачу/урок
    const t=taskCtx(), L=lessonCtx();
    if(/задач|эта задач|прочитай|объясни/i.test(q)&&t){
      return 'Слушай задачу. '+t.story+' Вопрос: '+t.q;
    }
    if(/подсказ/i.test(q)&&t){ return t.hints[0]||'Первая подсказка: подумай, что здесь неизвестно и что не меняется.'; }
    if(/решен|разбор|как решить/i.test(q)&&t){
      if(DB.tasks[t.id]&&DB.tasks[t.id].done) return 'Решение Архимеда. '+t.sol;
      return 'Сначала реши задачу сам — потом я объясню решение. Если трудно, попроси подсказку.';
    }
    if(/урок|шаг|книг/i.test(q)&&L&&L.explain){ return 'Это урок про '+L.title+'. '+(L.explain[0]||''); }
    // FAQ по приёмам
    for(const f of FAQ){ if(f.keys.some(k=>s.includes(clean(k)))) return f.txt; }
    // темы текущей задачи как вопрос «что за приём»
    if(t&&/какой приём|приём|идея|метод|как решать/i.test(q)){
      return 'Сначала разберись, что дано и что спрашивают. Потом проверь: это перебор, чётность, инвариант, Дирихле, комбинаторика или «оценка и пример»? Если не уверен — я объясню любой приём.' ;
    }
    // арифметический помощник: «сколько будет ...?»
    const ar=s.replace(/плюс/g,'+').replace(/минус/g,'-').replace(/умножить на/g,'*').replace(/умножить/g,'*').replace(/разделить на/g,':').replace(/разделить/g,':').replace(/делить на/g,':').replace(/икс|×/g,'*').replace(/на/g,'*');
    const expr=ar.match(/(\d+)\s*([+\-*:])\s*(\d+)/);
    if(expr){ const a=+expr[1], op=expr[2], b=+expr[3];
      let r=null;
      if(op==='+') r=a+b; else if(op==='-') r=a-b;
      else if(op==='*'||op==='x'||op==='×') r=a*b;
      else if(op===':'&&b!==0) r=a/b;
      if(r!==null) return a+' '+op+' '+b+' = '+r+'.';
    }
    return 'Я лучше всего объясняю задачи и олимпиадные приёмы. Скажи, например: «что такое принцип Дирихле», «какая здесь подсказка» или «объясни задачу».';
  }
  return { answer };
})();
